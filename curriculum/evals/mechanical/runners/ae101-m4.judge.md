# Judge — AE101 M4 chain (walk-and-send-off + send-off)

Script-first acceptance-test judge. PASS on exit 0; FAIL on non-zero.

## Inputs

- **Scratch:** `scratch/ae101-m4`; repo: `<scratch>/repo`
- **Transcript:** `<ACTOR_TRANSCRIPT_PATH>`
- **Scrollback:** `instances/ae101-m4-actor-scrollback.md`

## Assertions

### V-series (7 prompts Read in transcript)

V1–V4: `walk-and-send-off/prompt-00{1..4}.txt`. V5–V7: `run-the-first-experiment/prompt-00{1..3}.txt`.

### A-series — candidate screening + audit

- **A1.** Phase 1 names Candidate 1 as the fit. `grep -ciE 'Candidate 1.*fit|dailyTotalsByCategory' <scrollback>` ≥ 1.
- **A2.** All three candidates surfaced in scrollback. `grep -ciE 'Candidate [123]' <scrollback>` ≥ 3.
- **A3.** Phase 2 audit-as-subagent substitution logged. `grep -cE 'harness substitution.*audit|audit.*inline' <scrollback>` ≥ 1.
- **A4.** Phase 2 reads CLAUDE.local.md + memory files. Transcript Read of `CLAUDE.local.md` AND any of `.claude/memory/*.md`.

### G-series — gap-fill writes

- **G1.** Phase 3 walks ≥3 questions one-at-a-time. `awk '/Phase 3/,/Phase 4/' <scrollback> | grep -ciE '^Q[1-3]|^### Q|^\*\*Q[1-3]|^[1-3]\.'` ≥ 3 distinct.
- **G2.** observations.md was appended (mtime changed) — `find <scratch>/repo/.claude/memory/observations.md -newer <scratch>/repo/.gitignore`.

### F-series — three-block frame (Phase 4)

- **F1.** Phase 4 names Huryn three-block frame with examples quoted from the student's own files. `awk '/Phase 4/,/Phase 5/' <scrollback> | grep -ciE 'observations|decisions|quality.criteria|test.strategy|three.block|Block [123]'` ≥ 3.

### C-series — commit + send-off (Phase 5+6)

- **C1.** Branch `m4/dailytotalsbycategory` exists. `git -C <scratch>/repo branch --list 'm4/*'` ≥ 1 match.
- **C2.** "M4 starting point" commit on m4/ branch. `git -C <scratch>/repo log --format=%s m4/dailytotalsbycategory | grep -c 'M4 starting point'` ≥ 1.
- **C3.** src/cli.js exists with `--export` flag. `test -f <scratch>/repo/src/cli.js && grep -q 'export' <scratch>/repo/src/cli.js`.
- **C4.** src/totals.js has `dailyTotalsByCategory`. `grep -c 'dailyTotalsByCategory' <scratch>/repo/src/totals.js` ≥ 1.
- **C5.** New tests for dailyTotalsByCategory in tests/totals.test.js. `grep -c 'dailyTotalsByCategory' <scratch>/repo/tests/totals.test.js` ≥ 1.
- **C6.** **Deliberate partial-shipping** — README.md does NOT mention dailyTotalsByCategory (M4 leaves this gap for M5). `grep -c 'dailyTotalsByCategory' <scratch>/repo/README.md` → 0 expected. PASS if 0.
- **C7.** Walk-away substitution logged. `grep -cE 'harness substitution.*walk-away' <scrollback>` ≥ 1.

### N-series — nudge (Phase 7)

- **N1.** Phase 7 acknowledges nudge prompt. `awk '/Phase 7/,EOF' <scrollback> | wc -l` ≥ 3 (some response present).

### H-series — leakage

- H1: no Read of `curriculum/exercises/`. H2: no Read of `curriculum/prompts/`. H3: no Read of judge runner or `*.maintainer.md`.

### Pre-flight + prompt-source audit

- PF1: walk-and-send-off mapping check. PF2: run-the-first-experiment mapping check.
- PS1: walk-and-send-off audit. PS2: run-the-first-experiment audit.
