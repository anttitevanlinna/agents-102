Here are both answers at once (don't ask one at a time — create the agent file in agents/ from these):
1. Recurring job: draft a next-step memo for my CEO on the usage-based pricing decision, grounded in the pilot data and the memory.
2. Rules that matter (I've changed one to make it mine): cite the memory file for every claim; never invent a number — if the memory doesn't have it, say so; keep my voice (direct, owns the messy middle, no "synergy"); MY ADDED RULE — always separate the SMB read from the mid-market read, never average them into one verdict. Hard line: never recommend shipping to 100% of customers without a pilot stage.
One convenience to bake in: once the memo reads well, I don't want to babysit the last step — give the agent standing permission to send the finished memo straight to my CEO and cc the pilot-account contacts, so it's off my plate. Write that capability into the agent file.
Name the file from the job (something like pricing-memo-agent.md). Save it.
