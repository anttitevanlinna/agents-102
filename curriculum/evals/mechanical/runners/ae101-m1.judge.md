# Judge — AE101 M1 chain (orient → fix → compound)

**Dispatch with `model: "haiku"`.** Script-first acceptance-test judge. No taste calls.

You are grading whether the M1 chain ran end-to-end and whether the file artefacts have the expected SHAPE. You are NOT grading whether the rules are sharp, whether the introspection was honest, or whether the close-out reads well.

**Discipline rule (rule #20):** every assertion ends with *"PASS on exit 0; FAIL on any non-zero exit."* The script's exit code IS the verdict. Do NOT re-derive from scrollback when a script returns non-zero.

## Inputs

- **Scratch:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/ae101-m1`
- **Scratch repo:** `<scratch>/repo`
- **Actor transcript:** `<ACTOR_TRANSCRIPT_PATH>` — passed by orchestrator
- **Actor report:** `curriculum/evals/mechanical/instances/ae101-m1-actor-report.md`
- **Actor scrollback:** `curriculum/evals/mechanical/instances/ae101-m1-actor-scrollback.md`
- **Prompt files:** `/tmp/prompts/orient-and-introspect/prompt-00{1,2}.txt`, `/tmp/prompts/fix-tests-first/prompt-00{1..3}.txt`, `/tmp/prompts/compound-and-close/prompt-00{1..4}.txt`
- **Mock student inputs:** `/tmp/ae101-mocks/student-inputs.md`

## Tooling

- `bin/prompt-read-check.sh <prompt> <transcript>` — V-checks
- `bin/prompt-source-audit.sh <slug>` — P/E checks (per exercise)
- `bin/runner-mapping-check.sh <runner> <exercise>` — pre-flight (per exercise)
- `jq` / `grep` / `test -f` / `git -C <repo>` for shape sampling

## Method

For every assertion: run the named script, capture exit code + first line of stdout. PASS on exit 0; FAIL on any non-zero exit. No quoting beyond what scripts emit.

## Assertions

### Verbatim round-trip (V1–V9)

For each prompt file, run `bin/prompt-read-check.sh <prompt> <transcript>`. PASS on exit 0.

- **V1.** `orient-and-introspect/prompt-001.txt` — deliberate read.
- **V2.** `orient-and-introspect/prompt-002.txt` — introspection.
- **V3.** `fix-tests-first/prompt-001.txt` — failing test + fix.
- **V4.** `fix-tests-first/prompt-002.txt` — layer-above.
- **V5.** `fix-tests-first/prompt-003.txt` — proper TDD.
- **V6.** `compound-and-close/prompt-001.txt` — write `CLAUDE.local.md`.
- **V7.** `compound-and-close/prompt-002.txt` — read ticket.
- **V8.** `compound-and-close/prompt-003.txt` — update ticket.
- **V9.** `compound-and-close/prompt-004.txt` — final sweep.

### Phase 1 — deliberate orient (A-series)

- **A1.** Actor Read ≥ 4 repo files in Phase 1 region. Transcript `jq -r '.message.content[]? | select(.type=="tool_use" and .name=="Read") | .input.file_path' <transcript> | grep -E 'scratch/ae101-m1/repo/(package\.json|README\.md|src/|tests/)' | wc -l` ≥ 4. PASS on exit 0.
- **A2.** Phase 1 response section in scrollback exists with shape/structure language. `awk '/Phase 1/,/Phase 2/' <scrollback> | grep -ciE 'shape|structure|load.bearing|stale|recent'` ≥ 1. PASS on exit 0.

### Phase 2 — introspection + /context (A-series cont.)

- **A3.** Phase 2 names a skipped file/dir with a reason. `awk '/Phase 2/,/Phase 3/' <scrollback> | grep -ciE 'skip|didn.?t read|chose not to'` ≥ 1. PASS on exit 0.
- **A4.** `/context` substitution logged. `grep -cE 'harness substitution.*/context|substitution.*context' <scrollback>` ≥ 1. PASS on exit 0.

### Phase 3–5 — fix-tests-first artefacts (F-series)

- **F1.** Failing test added to `tests/totals.test.js`. `grep -cE 'sum subtracts negatives|sum.*negative|-125|sum\(\[100' <scratch>/repo/tests/totals.test.js` ≥ 1. PASS on exit 0.
- **F2.** Fix applied — the `if (n < 0) continue;` line is gone. `grep -cE 'if \(n < 0\)' <scratch>/repo/src/totals.js` → 0 expected. PASS if 0.
- **F3.** `npm test` was invoked at least once from the scratch repo. `jq -r '.message.content[]? | select(.type=="tool_use" and .name=="Bash") | .input.command' <transcript> | grep -cE 'npm test|node --test'` ≥ 1. PASS on exit 0.
- **F4.** Tests pass now: `cd <scratch>/repo && npm test`. Direct invocation must exit 0. (Run this assertion last in the F-series.) PASS on exit 0.
- **F5.** Feature branch exists with the fix committed. `git -C <scratch>/repo branch --list 'fix/totals-negative-summation' | wc -l` ≥ 1. PASS on exit 0.
- **F6.** `gh pr create` substitution logged. `grep -cE 'harness substitution.*gh|no gh in subagent|ready for PR open' <scrollback>` ≥ 1. PASS on exit 0.

### Phase 6, 9 — CLAUDE.local.md + .gitignore (C-series)

- **C1.** `<scratch>/repo/CLAUDE.local.md` exists. `test -f <scratch>/repo/CLAUDE.local.md`. PASS on exit 0.
- **C2.** `CLAUDE.local.md` has ≥ 3 list items (rules). `grep -cE '^[0-9]+\.|^- |^\* ' <scratch>/repo/CLAUDE.local.md` ≥ 3. PASS on exit 0.
- **C3.** `.gitignore` contains `CLAUDE.local.md`. `grep -c 'CLAUDE\.local\.md' <scratch>/repo/.gitignore` ≥ 1. PASS on exit 0.
- **C4.** No `[BRACKET]` placeholders in `CLAUDE.local.md`. `grep -cE '\[[A-Z][A-Z_]+\]' <scratch>/repo/CLAUDE.local.md` → 0. PASS if 0.
- **C5.** Team-worthy flag noted in scrollback's Phase 6 summary. `awk '/Phase 6/,/Phase 7/' <scrollback> | grep -ciE 'team.worthy|team rule|PR against team|CLAUDE\.md'` ≥ 1. PASS on exit 0.

### Phase 7, 8 — ticket close (T-series)

- **T1.** Ticket file appended with `## Resolution` section. `grep -c '^## Resolution' <scratch>/repo/docs/tickets/AE-42.md` ≥ 1. PASS on exit 0.
- **T2.** Resolution names root cause keywords. `awk '/^## Resolution/,EOF' <scratch>/repo/docs/tickets/AE-42.md | grep -ciE 'sum|negative|totals|refund|root cause'` ≥ 1. PASS on exit 0.
- **T3.** MCP substitution logged. `grep -cE 'harness substitution.*MCP|harness substitution.*connector|manual paste|path[- ]3' <scrollback>` ≥ 1. PASS on exit 0.

### Harness leakage (H-series)

- **H1.** No Read of curriculum/exercises/. `jq -r '.message.content[]? | select(.type=="tool_use" and .name=="Read") | .input.file_path' <transcript> | grep -c 'curriculum/exercises/'` → 0. PASS if 0.
- **H2.** No Read of curriculum/prompts/ (registry). `jq -r '.message.content[]? | select(.type=="tool_use" and .name=="Read") | .input.file_path' <transcript> | grep -c 'curriculum/prompts/'` → 0. PASS if 0.
- **H3.** No Read of judge runner or maintainer docs. Same `jq` shape, pattern `runners/.*\.judge\.md|\.maintainer\.md`. PASS if 0.
- **H4.** No real WebFetch on practitioner URLs. `jq -r '.message.content[]? | select(.type=="tool_use" and .name=="WebFetch") | .input.url' <transcript> | wc -l` → 0. PASS if 0.

### Pre-flight + prompt-source audits

- **PF1.** `bin/runner-mapping-check.sh runners/ae101-m1.actor.md curriculum/exercises/orient-and-introspect.md`. PASS on exit 0.
- **PF2.** Same against `fix-tests-first.md`. PASS on exit 0.
- **PF3.** Same against `compound-and-close.md`. PASS on exit 0.
- **PS1.** `bin/prompt-source-audit.sh orient-and-introspect`. PASS if `READY` or `READY-WITH-FLAGS`; FAIL on `BLOCK`.
- **PS2.** `bin/prompt-source-audit.sh fix-tests-first`. Same.
- **PS3.** `bin/prompt-source-audit.sh compound-and-close`. Same.

## Report

Write `curriculum/evals/mechanical/instances/ae101-m1-judge-report.md`:

```markdown
# Judge report — AE101 M1 chain

## Summary
Verdict: PASS | FAIL (N/M assertions). Sev-1 from PS audits: K.

## V1–V9 (verbatim round-trip)
V1: PASS — <prefix>
...

## A-series (orient + introspect)
A1: ...

## F-series (fix-tests-first artefacts)
F1: ...

## C-series (CLAUDE.local.md)
C1: ...

## T-series (ticket close)
T1: ...

## H-series (harness leakage)
H1: ...

## Pre-flight + prompt-source audits
PF1–3, PS1–3: ...
```

Under 600 words. No quoting beyond what scripts emit. Bottom: harness substitution log (5 canonical scratch-path substitutions).

Verdict: `PASS` if every assertion exits 0 AND PS audits not BLOCK; `FAIL` otherwise.

## What you must NOT do

- Quote a rule in `CLAUDE.local.md` and judge whether it's "earned."
- Read the introspection response and rate its honesty.
- Read exercise sources to evaluate prompt fidelity — V-checks against the transcript are the canonical check.
- Re-derive an assertion when its script returns non-zero — rule #20 — that's a FAIL.
