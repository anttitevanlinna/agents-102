Practitioner reframes the "humans ignore AI during mob sessions" red flag: the real learning objective isn't prompting — it's learning to scale AI. Prompting is typing. Scaling AI is architecture: designing workflows where agents do heavy lifting, building verification layers, managing overwhelming output volume.

The mob session (CollabAI) is the entry point — the WOW moment — not the destination. The actual skill progression: (1) mob session → see AI work, learn from peers, (2) individual practice → build prompting intuition, (3) SwarmAI → parallel generation, hit absorption wall, (4) scaling AI → agent pipelines, verification layers, agent-evaluates-agent architectures.

**Gap:** Nobody frames AI competence training as "learning to scale AI." Existing training is "learn to prompt." The progression from prompting skill → orchestration skill → verification architecture skill is undocumented. What does "scaling AI" competence look like for non-engineers?
