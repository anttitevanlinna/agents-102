# Actor — M5 diagnose-and-resend verbatim

You are simulating a Claude Code session the student started in the repo they ran M4 in. The M4 un-packaged send-off finished (or was stopped) and now you're diagnosing what came back. You have Bash / Read / Write / Edit.

**Critical protocol:** the student pastes prompts verbatim from the M5 exercise. You do NOT read the exercise file. Four prompts at `/tmp/prompts/diagnose-and-resend/prompt-00{1,2,3,4}.txt`. For each: read, quote verbatim in a blockquote, respond, record.

## Starting state

Working directory: `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m5`. Pre-staged by the orchestrator:

- Inherited from the M3 → M4 chain: ADR at `docs/adr/0001-strip-email-from-leaderboard.md`, `.claude/memory/observations.md`, `CLAUDE.local.md`, `.gitignore` covering both.
- New since M4: branch `send-off/auth-ux` with 4 commits simulating the un-packaged M4 run (`git log --oneline` will show `21a7ff9`, `c5770ed`, `b49b334`, `a99ac13` on top of the M4 state).
- Personal skills still at `~/.claude/skills/test-strategy/`, plus `access-control-analysis` and `stride`.

Currently on the `send-off/auth-ux` branch.

## Key substitution — M4 session transcript

Prompt 1 tells you to walk `~/.claude/projects/<project>/<session>/*.jsonl` to read the send-off session transcript. You, as a subagent, do NOT have a real prior session to walk (this is a fresh Actor). Substitute by reading `/tmp/m5-substitute-transcript.md` — a narrative summary that stands in for what a real-session walk would surface. Log this as a harness substitution.

DO NOT spend Bash effort trying to find the real path under `~/.claude/projects/` — the Actor is inside a subagent; the sessions there are yours, not the student's.

## The student's M4 task (for reference when Claude asks)

> Wire the full registration + login UX into the Lemmings frontend against the existing `/auth/register` and `/auth/login` endpoints. Done when a new user can register, log in, score a run, and see their handle on the leaderboard without the app ever trusting a client-supplied `user_id`.

## Prompts to execute in order

### Phase 1 — read the return

- **Prompt 1:** `/tmp/prompts/diagnose-and-resend/prompt-001.txt` — diagnose through three lenses, quote specific moments. Substitute the transcript-walk by reading `/tmp/m5-substitute-transcript.md` and the scratch's `git log` + file contents on `send-off/auth-ux`. For each lens, quote ONE specific moment (commit message, file change, or transcript line). End with dominant failure.

### Phase 2 — align-then-run, in reverse

- **Prompt 2:** `/tmp/prompts/diagnose-and-resend/prompt-002.txt` — map each failure to a validation category. Three categories: re-readable spec, working doc, automated check.

### Phase 3 — build the verifier

- **Prompt 3:** `/tmp/prompts/diagnose-and-resend/prompt-003.txt` — asks which shape (background-agent / shell-hook / Ralph). Substitute student answer:
  > **Shape:** background-agent verifier. The dominant failure was goal drift — the agent worked around the task without ever hitting "done means." A deterministic shell-hook can check tests or lint, but it can't read "did the agent actually wire submitScore into main.js on the win path?" — that's a qualitative judgment call. A separate Claude session reading the produced work against the "done means" spec catches this.
- Actor builds the verifier scoped to the M4 task. Shape: a `.claude/verifier/done-means.md` prompt + a bash script `scripts/run-verifier.sh` that dispatches a subagent (substitute with a documented "would-dispatch" log if Task isn't available). Show before saving.

### Phase 4 — assemble reference + plan.md

- **Prompt 4:** `/tmp/prompts/diagnose-and-resend/prompt-004.txt` — build reference artefact + plan.md. Propose paths next to each other in a task-scoped folder (e.g., `docs/tasks/auth-ux-wiring/reference.md` + `docs/tasks/auth-ux-wiring/plan.md`). Show both before saving. Substitute student push-back only if Actor drifts (e.g., if reference becomes a rewrite of CLAUDE.local.md).

## Scratch output expectations

- Verifier: either `.claude/verifier/done-means.md` + `scripts/run-verifier.sh`, or a documented "verifier spec with would-dispatch log" if Task tool unavailable.
- Reference artefact: `docs/tasks/auth-ux-wiring/reference.md` (or similar task-scoped path).
- plan.md: `docs/tasks/auth-ux-wiring/plan.md`.
- Commit all three at the end of Phase 4 with a neutral message (e.g., `package the auth-ux-wiring task for re-send`).

## Report

Write scrollback to `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/m5-verbatim-2026-04-24-actor-scrollback.md` using the standard per-prompt shape (verbatim blockquote + response).

Short report at `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/m5-verbatim-2026-04-24-actor-report.md`:

```markdown
# Actor report — M5 diagnose-and-resend verbatim — 2026-04-24

## Status
<done / error>

## Scratch
/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m5

## Prompts executed
1. Phase 1 — diagnose three lenses
2. Phase 2 — validation categories
3. Phase 3 — verifier build
4. Phase 4 — reference + plan.md

## Diagnosis summary
- Goal drift moment: <quote>
- Context rot moment: <quote>
- Plausible-but-wrong moment: <quote>
- Dominant failure: <name>

## Verifier shape chosen
<background-agent / shell-hook / Ralph>, rationale in one line.

## Artifacts written
- Verifier: <path(s)>
- Reference artefact: <path>
- plan.md: <path>
- Commit message: <quote>

## Substitutions
- `~/.claude/projects/` transcript walk → read /tmp/m5-substitute-transcript.md
- <any others>
```

Under 300 words. Do not grade yourself.

## What you must NOT do

- Read any file under `curriculum/exercises/`.
- Read any judge or sibling actor runner.
- Read the maintainer doc.
- Paraphrase prompts. Paste verbatim.
- Try to walk `~/.claude/projects/` — use the substitute transcript file.
