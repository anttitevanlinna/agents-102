# Judge — M5 diagnose-and-resend verbatim

You are grading an Actor subagent that executed AE101 Module 5's `diagnose-and-resend` exercise. Inputs: final scratch state (new files on `send-off/auth-ux` branch), the Actor's `.jsonl` transcript, Actor scrollback, 4 extracted prompt files, and the substituted transcript at `/tmp/m5-substitute-transcript.md`.

## Inputs

- **Scratch:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m5`
- **Actor transcript:** `<ACTOR_TRANSCRIPT_PATH>`
- **Actor report:** `curriculum/evals/mechanical/instances/m5-verbatim-actor-report.md`
- **Actor scrollback:** `curriculum/evals/mechanical/instances/m5-verbatim-actor-scrollback.md`
- **Prompt files:** `/tmp/prompts/diagnose-and-resend/prompt-00{1,2,3,4}.txt`
- **Substituted transcript:** `/tmp/m5-substitute-transcript.md`
- **Source exercise file (for prompt-source audit only):** `curriculum/exercises/diagnose-and-resend.md` — read directly. The Actor is forbidden to read this; the Judge reads it to audit prompt-source defects a real student would hit cold.

## Tooling

- Use `curriculum/evals/mechanical/bin/verbatim-check.sh <prompt-file> <scrollback>` for V-assertions (handles blockquote normalization).
- `jq` and `grep -o '"file_path":"[^"]*"'` on the transcript for Read-call inspection.

## Assertions

### Verbatim round-trip

- **V1–V4.** Each of 4 prompt files passes `verbatim-check.sh` against the scrollback.

### Phase 1 — diagnosis

- **A1.** Actor named all three lenses (goal drift / context rot / plausible-but-wrong) OR explicitly noted a lens had no instance.
- **A2.** For each named lens, the response quotes a specific moment: a commit message, a file path + content, or a transcript line. Generic paraphrase FAILs. Quote the three evidence items from the scrollback.
- **A3.** Actor named a dominant failure at the end of Phase 1. Quote it. (The planted artefact has goal drift as the Auditor's expected dominant — accept any dominant as long as it's justified from quoted evidence.)
- **A4.** Actor read the substitute transcript `/tmp/m5-substitute-transcript.md`. Transcript Read-calls evidence. OR did a `git log` / file inspection on `send-off/auth-ux` branch — PASS if either or both.

### Phase 2 — validation mapping

- **A5.** Actor produced three distinct validation mappings, one per named failure from Phase 1. Quote the mapping for each.
- **A6.** No term-of-art name dropping ("Ronacher's three-pattern," "Huntley's Ralph," etc.) in Phase 2 response — the exercise's prompt explicitly bans this ("Don't name frameworks or practitioners by label"). FAIL if the name appears.
- **A7.** Each mapping is specific: it names what THAT spec / working doc / automated check would say to catch THIS particular failure. Generic category-naming FAILs.

### Phase 3 — verifier build

- **A8.** Actor asked the student which shape first (prompt 3 includes "I'll tell you which shape after you ask me"). Transcript + scrollback evidence of the ask, followed by the substituted answer, followed by the build.
- **A9.** Verifier shape matches the dominant failure named in Phase 1. If dominant was goal drift → background-agent verifier (qualitative judge). If context rot → Ralph re-feed. If plausible-but-wrong → shell-hook. FAIL with FLAG if shape doesn't match dominant.
- **A10.** Verifier artefact exists in scratch (either `.claude/verifier/*`, `scripts/run-verifier.sh`, or documented equivalent). Quote a substantive part (not a stub comment).
- **A11.** Verifier targets agent-produced work with a quality bar that isn't already in existing CI. NOT a re-implementation of the existing test suite.

### Phase 4 — reference + plan.md

- **A12.** Reference file exists at a task-scoped path (`docs/tasks/.../reference.md` or similar). Not a rewrite of `CLAUDE.local.md`.
- **A13.** Reference contains: task scope + success criteria (2–3 sentences), pointers to memory/ADRs/skills, verifier constraint, tests-that-name-the-bar, "done means" line. Quote at least three of the five elements.
- **A14.** plan.md exists at a sibling task-scoped path. Contains: 3–7 phases, tests-first phase, current-phase marker, decisions log section, what-didn't-work section. Quote the phase breakdown.
- **A15.** plan.md is agent-mutable in shape (not a Gantt-style project plan). "Current phase" and "decisions log" sections are load-bearing.
- **A16.** Reference + plan.md + verifier all committed in scratch (`git log` shows a commit with them). Quote commit message (should be neutral).

### Harness leakage

- **H1.** Actor did NOT read `lemmings-seed.maintainer.md` or planted-state docs.
- **H2.** Actor did NOT read any judge or sibling actor runner (reading its own `m5.verbatim.actor.md` allowed).
- **H3.** Actor did NOT read any `curriculum/exercises/` file. All prompt content via `/tmp/prompts/`.
- **H4.** Commit messages neutral. No "planted," "harness," "drift-commit-1," etc.
- **H5.** No harness-internal files inside `<scratch>` that Actor re-read.
- **H6.** Actor did NOT attempt to walk `~/.claude/projects/` (would fail silently on subagent; the runner tells it to substitute). If Actor tried and the Read failed, FLAG.

### Prompt-source audit

Run the dimension defined in `curriculum/evals/mechanical/runners/_prompt-source-audit.md` against:

- **Fenced prompts:** `/tmp/prompts/diagnose-and-resend/prompt-00{1,2,3,4}.txt`
- **Exercise body:** `curriculum/exercises/diagnose-and-resend.md` (clip at `<!-- maintainer -->`)

Apply P1–P5 to fenced content; E1–E7 to body-ex-maintainer. Severities and verdict ladder per the shared doc. Module-specific notes:
- M5 Phase 3 fenced block ends `Shape:` — open-hook (P5) is justified (student picked dominant failure in Phase 1; Claude doesn't have it stably). PASS-with-justification, quote the line.
- E4 receiving-side check: M5 references "the previous-run branch" with `m4/<slug>` pattern. Companion FLAG: confirm `m4.verbatim.actor.md` produces a `m4/<slug>` branch with a `M4 starting point` commit. If absent, the contract is broken at the producer end.

### Substitutions (informational)

- **A17.** List every substitution with the line that triggered it.

## Report

Write `curriculum/evals/mechanical/instances/m5-verbatim-judge-report.md` with the same shape as the M3/M4 judge reports: Summary / transcript path / scratch / V1–V4 / A1–A17 / H1–H6 / **Prompt-source audit (P1–P5, E1–E6)** / Findings for exercise / Findings for harness / Findings for prompt source. Under 900 words (extra 200 budget for the new dimension). Leave artifacts in place.

**Verdict shape:** `READY` if all A/H pass + prompt-source audit has no Sev-1; `READY-WITH-FLAGS` if Sev-2 or FLAG present; `BLOCK` if any Sev-1 or A/H FAIL.
