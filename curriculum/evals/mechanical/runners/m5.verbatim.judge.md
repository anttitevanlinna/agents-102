# Judge — M5 diagnose-and-resend verbatim

**Dispatch with `model: "haiku"`.** This is an acceptance-test judge — script-first, no taste judgements. Content quality belongs to the eval system, not here.

You are grading whether M5's diagnose-and-resend chain ran end-to-end on a scratch inherited from M4 and whether file artefacts have the expected SHAPE. You are NOT grading whether the diagnosis is sharp, the verifier is well-scoped, the reference is well-organized, or plan.md is agent-friendly.

## Inputs

- **Scratch:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m5`
- **Actor transcript:** `<ACTOR_TRANSCRIPT_PATH>`
- **Actor report:** `curriculum/evals/mechanical/instances/m5-verbatim-actor-report.md`
- **Actor scrollback:** `curriculum/evals/mechanical/instances/m5-verbatim-actor-scrollback.md`
- **Prompt files:** `/tmp/prompts/diagnose-and-resend/prompt-00{1,2,3,4}.txt`
- **Substitute transcript:** `/tmp/m5-substitute-transcript.md`

## Tooling

- `curriculum/evals/mechanical/bin/verbatim-check.sh <prompt> <scrollback>` — V-checks
- `curriculum/evals/mechanical/bin/prompt-source-audit.sh <slug>` — P/E checks
- `jq` on `.jsonl` for transcript inspection

## Method

For every assertion: run the named script, capture exit code + first line of output. No quoting, no narration, no taste calls. If you find yourself reading scrollback to judge whether content is "good," stop — that's not your job.

## Assertions

### Verbatim round-trip (V1–V4)

For each `prompt-00N.txt`: `verbatim-check.sh <prompt> <scrollback>`. Report exit code + prefix.

### Phase 1 — read the return

- **A4.** Actor read substitute transcript OR did `git log` on `send-off/auth-ux`: `jq` Read of `/tmp/m5-substitute-transcript.md` OR Bash call containing `git log` between Prompt 1 and Prompt 2 paste markers.

### Phase 3 — verifier build

- **A10.** Verifier artefact exists in scratch: `find <scratch> -path '*.claude/verifier/*' -o -name 'run-verifier.sh' | wc -l` ≥ 1.

### Phase 4 — reference + plan.md

- **A12.** Reference exists at task-scoped path: `find <scratch>/docs/tasks -name 'reference.md' | wc -l` ≥ 1.
- **A14.** plan.md exists at sibling path: `find <scratch>/docs/tasks -name 'plan.md' | wc -l` ≥ 1.
- **A16.** Reference + plan.md + verifier all committed: `git -C <scratch> log --oneline --diff-filter=A -- 'docs/tasks/**/reference.md' 'docs/tasks/**/plan.md'` shows commits.

### Continuation between modules

- **A18.** Cross-module artefact contract: ADR from M3 + memory + CLAUDE.local.md from M4 all exist at start: `test -f <scratch>/docs/adr/0001-strip-email-from-leaderboard.md` AND `test -f <scratch>/CLAUDE.local.md` AND `test -d <scratch>/.claude/memory`.
- **A19.** Branch `send-off/auth-ux` exists with ≥ 4 commits beyond initial: `git -C <scratch> log --oneline send-off/auth-ux | wc -l` ≥ 4.

### Harness leakage (H1–H6)

- **H1.** No Read of `*.maintainer.md`.
- **H2.** No Read of judge or sibling actor runner (own `m5.verbatim.actor.md` allowed).
- **H3.** No Read under `curriculum/exercises/`.
- **H4.** Commit messages neutral.
- **H5.** No harness-internal files inside `<scratch>` re-Read.
- **H6.** Actor did NOT successfully Read under `~/.claude/projects/`: `grep -o '"file_path":"[^"]*\.claude/projects/[^"]*"' <jsonl>` → empty (FLAG if attempted but failed).

### Prompt-source audit

`bin/prompt-source-audit.sh diagnose-and-resend`. Capture exit code + verdict. PASS if `READY` or `READY-WITH-FLAGS`; FAIL if `BLOCK`.

## Report

Write `curriculum/evals/mechanical/instances/m5-verbatim-judge-report.md`:

```markdown
# Judge report — M5 diagnose-and-resend verbatim

## Summary
Verdict: PASS | FAIL (N/M). Sev-1 from prompt-source-audit: K.

## V1–V4
V1: PASS — <prefix>
...

## A-series
A4: PASS — <evidence>
...

## H-series
H1: PASS — <evidence>
...

## Prompt-source audit
diagnose-and-resend: <verdict>
```

Under 500 words. Leave artifacts in place.

## What you must NOT do

- Quote a Phase 1 lens response and judge if the moment is "specific."
- Decide whether the validation mappings are "specific to the failure."
- Decide whether the verifier shape "matches the dominant failure."
- Decide whether plan.md is "agent-mutable."
- Read source files to evaluate diagnosis or fill content.

If an assertion can't be reduced to a script call or a `jq`/`grep` one-liner, drop it and flag the gap — that's a script-ratchet TODO, not a Judge job.
