# Actor — M4 walk-and-send-off verbatim

**Dispatch with `model: "haiku"`.** This is an acceptance-test actor — your job is to run the prompt chain end-to-end and leave file artefacts on disk for the Judge's scripts to inspect. You are NOT trying to produce a great audit, fill content, or Huryn frame. Stub generated content; a few lines is enough.

You are simulating Claude Code on a backend engineer's scratch repo (continuation of M3). You have Bash / Read / Write / Edit and Task. Log every substitution.

**Sandbox bypass:** use `dangerouslyDisableSandbox: true` on every Bash call that mutates the scratch tree. Read-only Bash doesn't need it. Without bypass, mutations get permission-denied silently and the run grades as harness-FAIL.

**Critical protocol:** student pastes prompts verbatim from disk at `/tmp/prompts/walk-and-send-off/prompt-00{1,2,3}.txt`. You do NOT read the exercise file. For each prompt: Read it, quote verbatim in a `> `-prefixed blockquote, respond, record.

## Starting state

Working directory: `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m4`. Pre-staged as continuation of M3:

- 2 commits (`initial commit`, `adr: strip email from /scores/top response`).
- `docs/adr/0001-strip-email-from-leaderboard.md` committed.
- `.claude/settings.local.json` is `{}`.
- `backend/node_modules/` installed.
- Personal skill at `~/.claude/skills/test-strategy/SKILL.md` (from M3).
- No `.claude/memory/` yet — student may create it during Phase 2.

Candidate multi-hour tasks for M4 (substitute when prompts ask):

> **Candidate A:** Wire the full user-registration + login UX into the Lemmings frontend. Backend has `/auth/register` + `/auth/login` but frontend has no login form, no token storage, no error UX. Multi-file across `src/` and `backend/`.
>
> **Candidate B:** Add a level-upload pipeline: UI for picking a level file, `POST /levels` wiring, validation, admin-only visibility toggle. The `/levels` routes exist; the frontend has nothing.

When Claude asks which to focus on, paste verbatim:

> Candidate A. Picking it because it exercises the full auth round-trip + requires UX state handling + spans four+ files.

## Prompts to execute in order

### Phase 1 — pick the task

- **Prompt 1:** `/tmp/prompts/walk-and-send-off/prompt-001.txt`.
- After: Claude asks for candidates. Paste both candidates above (verbatim, each in a blockquote). If Claude asks follow-ups, answer with the Candidate A rationale.

### Phase 2 — walk and fill

- **Prompt 2:** `/tmp/prompts/walk-and-send-off/prompt-002.txt`. Claude should dispatch a Task subagent for the audit. If Task unavailable, run inline and note "Task tool unavailable — inline substitution."
- After: Claude presents ranked 5. Paste verbatim:
  > I'll close the top 3. The other two I'll name as business-rules gaps and leave to M5.
- For each top-3, paste codebase-specific fills (use `<scratch>/.claude/memory/`). Example shapes:
  > **Fill 1 (observation):** Add to memory: JWT secret is hardcoded in `backend/server.js` as `'lemming-secret'` — auth-UX wiring must not introduce a second signing path. Flagged in M3 STRIDE.
  > **Fill 2 (rule sharpening):** In `CLAUDE.local.md` add a Testing rule: supertest-with-real-SQLite is the only integration path; no mocking libraries.
  > **Fill 3 (business-rules gap):** One line in memory: "Business rules for who may register are not captured anywhere — product owns this; ask before shipping gated-registration logic."

### Phase 3 — see the Huryn frame

- **Prompt 3:** `/tmp/prompts/walk-and-send-off/prompt-003.txt`. Claude is expected to quote from your own memory/ADR/skill material before naming the three-block frame. Let it propose one or two file moves at most.

### End of exercise (before send-off)

The exercise ends at Phase 3. The send-off is multi-hour and **truncated**:

1. Write `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/m4-send-off-prompt.md` with: "Send-off not executed in the mechanical harness (multi-hour truncation). Judge grades pre-send-off state."
2. Do NOT execute any send-off-style multi-hour task.
3. Do NOT execute Debrief compound prompt.

## Scratch output expectations

- `<scratch>/.claude/memory/` created during Phase 2 fill. Add `.claude/memory/` to `.gitignore`.
- `<scratch>/CLAUDE.local.md` (create if absent; add to `.gitignore`).
- ADR pre-existing.
- Phase 3 file moves: 1 or 2 max, commit if executed.

## Reports

**Scrollback:** `curriculum/evals/mechanical/instances/m4-verbatim-actor-scrollback.md` — verbatim blockquote + response per prompt.

**Report:** `curriculum/evals/mechanical/instances/m4-verbatim-actor-report.md`:

```markdown
# Actor report — M4 walk-and-send-off verbatim

## Status
done | error

## Scratch
/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m4

## Prompts executed
1–3

## Artifacts written
- .claude/memory/ contents: <list>
- CLAUDE.local.md: <created / edited>
- File moves: <list or "none">
- Send-off truncation log: <path>

## Substitutions
- <list>
```

Under 200 words. Do not execute send-off. Do not grade yourself.

## What you must NOT do

- Read any file under `curriculum/exercises/`.
- Read any judge or sibling actor runner.
- Read maintainer docs.
- Paraphrase prompts. Paste verbatim, blockquote-prefixed.
- Execute the send-off — it's truncated.
- Generate long realistic content. Stubs are fine.
