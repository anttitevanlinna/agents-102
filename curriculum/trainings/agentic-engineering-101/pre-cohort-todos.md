# Pre-cohort punchlist, AE101 M1–M6

> **CLOSED = DELETE THE BULLET. Not "annotate done." Not "rewrite as smaller follow-up." Not "update with what landed." DELETE.**
>
> Git log is the history. This file is current state. A closed bullet rewritten as adjacent work is a NEW bullet pretending to be progress, surface the new work cleanly under a real section, or don't add it. If the new work isn't pre-cohort scope (M1–M6 done-done), it doesn't belong in this file at all.
>
> **Editing this file? Load the rules that fire here.** A flagged item is a hypothesis, not a to-do: before "fixing" one, check it isn't deliberate pedagogy, a wrong-layer diagnosis, or a cosmetic non-issue (`check_pedagogy.md` §61/§61a). And a closed item that carries a design decision moves to its source first (the prompt's frontmatter `note:` or the exercise maintainer block, where the editor reads), THEN the bullet is deleted, this file never holds the rationale (`content-creation` skill § TODO discipline).

Open decisions before the first paying cohort. Module files carry zero TODOs; this is the single tracking surface.

**Item types.** Bullets are open decisions or tactical fixes by default, something has to happen before cohort. Prefix a bullet with **`[watch]`** to mark *currently accepted, no pre-cohort action*, the behaviour may bite us, but we've decided to ship as-is and see. Watch items convert to action items if the cohort run surfaces the failure; otherwise they get deleted like any other closed bullet. Don't let watches accumulate as a dustbin: a watch with no named firing condition is procrastination dressed up.

Sim sweeps and platform-capability checks are not tracked here. The `curriculum-pre-ship-audit` skill auto-fires on touched files; `check_platform_and_boundaries.md` fires on platform claims at content-time. **Per-class verdicts (PASS / REVISE / grandfathered) live in each module's own `**Quality:**` block**, `update-quality.sh` stamps both PASS and REVISE with the instance-JSON path so a successor can grep Quality blocks for state without consulting this file.

**Harness / tmux-runner engineering is not tracked here either.** Runner bugs, scenario hardening, per-turn assertions, socket isolation, and state.json shape live in `curriculum/evals/mechanical/tmux-runner/IMPROVEMENTS.md`. This file tracks cohort-blocking *curriculum* decisions only; a finding that's fixed in runner code or a scenario file belongs there, not here.

**Source-freshness is not narrated here either, it is stamped per-source and generated.** Each source carries a checked / result / due freshness stamp in its file's `Source verification` block (format: `curriculum/source-freshness-format.md`); `curriculum/evals/scripts/source-freshness.sh --target <cohort-date>` produces the punch list. The 2026-05-25 full sweep cleared all source BLOCKs; per-source stamps now live in each file's `Source verification` block. Do not add new hand-narrated source-verification prose; stamp and run the script instead.

## Per-file specific concerns (not caught by auto-fire)

- **Exercise lead-in lines run about 3x over budget across the corpus.** (2026-08-12.) `check_pedagogy.md` §52c and `curriculum/exercises/README.md` now define the three lead-ins (`**What you do:**` / `**What you build:**` / `**The point:**`) and set a budget of one sentence and roughly 15 words each. Measured medians at the time of writing were 39 / 31 / 37 words, against a corrected reference shape of 9 / 12 / 7 in `orient-and-introspect.md`, so the median exercise spends about 107 words before the work starts. Worst: `author-test-strategy-skill` (71), `extract-the-task-shaping-rule` (57), `threat-model-with-stride` (51). A per-slot audit with proposed replacements is at `curriculum/evals/scratch/exercise-lead-in-audit-2026-08-12.md`. **Action:** approve or reject per line, these are student-facing body edits and gated. Shared library file, so it reaches Agents 101 too, check the diff does not smuggle AE101 voice into a shared exercise.

- **Two modules run past their 2h slot: M5 by 17, M2 by 6.** `node scripts/calculate-time.js` carries both. The other four fit with 3 to 15 min of float. In the 2-day shape M5's overrun lands in the lunch its packaged re-send already runs through; the exposure is the four-sitting track, where the sitting simply ends. **Action:** take 17 min off M5 (the worktree fork and the two closers are the candidates) and 6 off M2, or state the overruns in the four-sitting delivery contract. Both tabs' if-behind lists are sized against the old caps and want a read either way.
 - M2's 6 is worth a look before cutting: its exercise is booked 5 / 15 / 15 / 15 / 10, and the file's own trainer thresholds say P3 over 12 min means compress P4, which makes the 15 on P3 exceed the point at which the trainer is told to intervene. A defensible P3 ceiling of 12 closes the whole overrun.
 - **M5's overrun grew by 5 on 2026-08-12 and the 5 was always real.** `reading-the-return` was charged 0 on the premise that M4's Debrief handed it out as a pre-read. M4 has no Debrief, M5's prework never listed it, and M5's opening paragraph asserted the student had already read it. It is now an in-room M5 opener charged 5, the body line is gone, and M4's close no longer points at it. The module did not get slower; the clock got honest.

- **The multisession permission beat has one carrier, and the strategy ledger names four others.** (2026-08-13.) Row #11 of the commitment table in `content-strategy-agentic-engineering-101.md` (L897) reads `M1, M2 lecture, M4 send-off, M6 callout | one line each, sprinkled`. Against the shipped set: M1 ([getting-going.md:77](getting-going.md)) and M2 ([plan-mode-done-right.md:16](plan-mode-done-right.md)) are the same optional-reading pointer to `reference/multi-session-git.md`, counted twice; M4's send-off is single-session by design, so it cannot carry a permission it never grants (`training-architecture.md:13` puts async cloud agents out of scope, and the two promises are distinct — permission to walk away from one session is not permission to run two); M6's callout is absent. The one place a student runs two sessions at once is M3's [open-the-side-quest.md](../../exercises/open-the-side-quest.md), which the row does not name, and whose maintainer block calls it the first felt multi-session moment in AE101. A beat carried once by doing, with a ledger claiming four carriers, is a beat that vanishes silently the first time M3 gets trimmed for time. **Action (Antti 2026-08-13):** the permission belongs in M4/M5, where control is already the subject — M4's nudge ceiling (*"past ten or so, you've become the agent"*) and M5's *"acting without full control is the job from here on"* are the two places the student feels the wait, and a second lane is the answer to it. Decide one line in each or one in M5 alone, then correct row #11 to what ships. `training-architecture.md:195` row 7 dates parallel sessions to M2+ where the doing starts at M3; that one is a repo file and travels with the same decision.

- **`[watch]` `extract-the-task-shaping-rule-2` save-path ambiguity (M2).** Prompt asks Claude to "propose two-three plausible paths" for the rules file and pick one. Codesearch M2 dry-run (2026-05-24) had the agent integrate the new rules INTO the existing `CLAUDE.local.md` (created at M1's compound), a legit "integrate, don't append" reading, but it means a downstream prompt that loads "the rules file" and `CLAUDE.local.md` as separate inputs sees one path, not two. Lemmings runs typically save to a distinct file. **Accepted as-is for first cohort.** Fires if any downstream module prompt (M3 sharpen-skill, M4 walk-and-send-off, M5 diagnose-and-resend, M6 spot-gaps) loads "the rules file" and `CLAUDE.local.md` as separate inputs and silently de-dupes or overwrites, observe whether any module behaves differently when the two paths collapse to one.
- **`[watch]` skill-name collision breaks the hardcoded-literal chain on replays (M3 `test-strategy`).** The skill saves to a fixed name and its consumer prompts cite the literal path (`~/.claude/skills/test-strategy/`). First-cohort clean `~/.claude/skills/` hits no collision. (The M6 `session-shaper` half died with the 2026-08-01 authoring-sequence cut.) **Accepted as-is for first cohort.** Fires if a returning/replay student or an agents-101 alum already has the name, Claude suffix-renames (`test-strategy-<project>`), and a consumer prompt citing the literal misses. Fix when it fires: warn in the exercise body to check where Claude saved, OR have the creation prompt overwrite-on-conflict.
- **`[watch]` save-gate-then-immediate-read race in `author-test-strategy-skill-1` and `diagnose-and-resend-4`.** Both prompts ([author-test-strategy-skill-1.md:28](../../prompts/author-test-strategy-skill-1.md), [diagnose-and-resend-4.md:28](../../prompts/diagnose-and-resend-4.md)) say *"show me before saving"* or equivalent, then the immediate next scenario step reads or invokes the file (M3 quality lane invokes the skill at `author-test-strategy-skill-2`; M5 verifier smoke-test at `diagnose-and-resend-5`). (The `spot-gaps-build-the-loop-3` leg died with the 2026-08-01 authoring-sequence cut.) If Claude obeys the save gate literally and waits for an approval that doesn't come in autonomous mode, the file is never written and the next step fails on a missing artifact. **Accepted as-is for first cohort.** Fires if any headless / unattended cohort run hits a missing-artifact failure downstream of one of those turns (codesearch M5, picoshare M3/M5 happy-path runs all saved through; this surfaces if Claude reads the save gate stricter). Fix when it fires: tail the downstream literal with an explicit approval, OR rewrite the save gate to *"save now and show me after"*.
- **`[watch]` worktree-setup prompts carry literal `<repo-name>` placeholder paths inside the prompt body.** [ae101-m3-fork-quality-side.md:21](../../prompts/ae101-m3-fork-quality-side.md) and [ae101-m5-worktree-setup.md:26](../../prompts/ae101-m5-worktree-setup.md) sketch shell commands with `../<repo-name>-quality` / `../<repo-name>-m5` as placeholder text. Body says Claude should infer the actual repo name from the cwd, but the inference instruction lives outside the copied prompt, only the body has it; the shell sketch reads as the literal command. **Accepted as-is for first cohort.** Fires if Claude follows the shell sketch verbatim and creates a directory called literally `../<repo-name>-m5` (a sibling named `<repo-name>-m5`), causing run-m3 / run-m5's `--worktree-cwd` assertion to miss (codesearch M3/M5 + picoshare M3/M5 + lemmings M3/M5 all inferred correctly so far; this fires when prompt-following gets more literal). Fix when it fires: move the infer-from-cwd instruction inside the prompt body so it travels with the copy, OR fold the substitution into the prompt registry expander.
- **`[watch]` `prompt-source-audit.sh` returns BLOCK on `walk-and-send-off`.** `walk-and-send-off` blocks on curriculum-coined-label leakage around "send-off" inside prompt text (P4 / §40b), plus a skill-path reference at [walk-and-send-off-2.md:25](../../prompts/walk-and-send-off-2.md). Linter-strict rather than runtime-breaking; the runs themselves succeed. **Accepted as-is for first cohort.** Fires when the "done-done" gate is applied to this exercise (done-done needs static audits clean). Fix when it fires: rephrase the "send-off" prose to be self-contained, or self-define the term in the fence per §40b.
- **`[watch]` M5 grill "lock it in" with an unanswered round leaves load-bearing decisions unowned.** `diagnose-and-resend`'s grill-before-save sets up multi-round Q&A but never names what happens when "lock it in" arrives before every question is answered. Lemmings retest 2026-05-26 (phase B): the agent settled three packaging decisions (keep constraints, reuse the win/loss text, commit both files) on its own recommendations rather than the maintainer's answers. **Accepted as-is for first cohort**, the agent's defaults were reasonable and the run passed. **Fires if** a cohort student says "lock it in" mid-grill and the agent bakes a wrong default into the packaged artifact, OR a student is surprised that unanswered questions got auto-decided. Fix when it fires: the grill prompt names the lock-in contract (`check_prompts §35/§20`), e.g. "if I say 'lock it in' before answering everything, use your best recommendation for the rest and tell me which you assumed."
- **`[watch]` M3 `ae101-m3-sharpen-skill` closer assumes scrollback from the quality lane while running in the main lane.** The main-lane closer tells the agent to read scrollback for "the moment the skill was invoked on the security-tested feature", but that invocation runs in the QUALITY-lane session, not the main lane where the closer runs; the cross-window bridge lives in a body callout, not the prompt fence (`check_prompts §34/§40`). Lemmings retest 2026-05-26: the agent self-corrected by grounding on the ADR; the same cross-lane scrollback assumption recurs at quality turn-4. **Reinforced by the 2026-05-27 Phase 2 redesign:** `author-test-strategy-skill-2` now officially keeps invocation in the m3-quality worktree and runs it on the whole codebase/repository, not the security-tested feature, so the closer's "invoked on the security-tested feature" wording is now off on both axes (wrong lane AND wrong target). The closer was **deliberately left unchanged** (2026-05-27 call): the main-lane security scrollback (access-control output + STRIDE + ADR) plus a fresh read of the SKILL.md is deemed enough to ground the rewrite, and the agent recovers by grounding on the ADR. **Accepted as-is for first cohort.** **Fires if** a fresh main-lane session or unattended run can't locate an invocation moment and the closer mis-grounds (e.g. hunts for an invoke-on-feature exchange absent from main-lane scrollback). **Fix when it fires:** move the cross-lane anchor into the fence (§34), or have the closer ground on the ADR / SKILL.md explicitly rather than on scrollback, and drop the now-inaccurate "security-tested feature" clause (gated body edit §22/§26).
- **`[watch]` M2's `push-back-on-the-plan-1` plan over-ships M3's threat-model territory: encodes "filter+lock" projection test rigor before M3's ADR is supposed to name the pattern.** Picoshare M2 plan 2026-05-26 Step 5 Case 10 lands at: *"Projection lock (filter+lock, NOT just struct-shape trust). Insert a link whose `Label` is a distinctive sentinel … GET public-status, read the raw response body bytes, and assert … the sentinel is absent … Positive guard in the same case … This is the disclosure-lock check for a public read surface."* That language anticipates M3's ADR-0001 work. M3 still added value (named the precise threat, *"a future maintainer reaching for `respondJSON(w, gl)`"*, tied to existing code at `handlers/guest_links.go:46`, considered three rejected alternatives), so the pedagogical surprise survived. But the I-class disclosure ADR's load-bearing payoff is partially baked in by M2. **Accepted as-is for first cohort.** **Fires if** M3's threat-model passes feel like rubber-stamping the M2 plan (the agent's STRIDE turns produce no novel framing relative to what M2's plan already encoded), OR if cohort students report M3 lands as *"M2 said this already."* **Fix when it fires:** scope the M2 plan to *what to build* and defer *how to test for non-disclosure* to M3; let the M2 plan flag *"owner-only fields exist; how do we keep them out"* as an open question, not a solution. Body edits on `push-back-on-the-plan-1` are gated (§22/§26).
- **`[watch]` `ae101-m3-sharpen-skill` (M3 closer) hardcodes `~/.claude/skills/test-strategy/SKILL.md` (unsuffixed) in its body, and is the ONLY consumer prompt where no scenario tail can override.** The closer fires from `run-m3.sh` Phase C ([run-m3.sh:258](../../evals/mechanical/tmux-runner/run-m3.sh): `closer_body="$(resolve_prompt "$closer_key")"`), not from a scenario file. Sibling unsuffixed-path consumer (`author-test-strategy-skill-1`) gets its path overridden by scenario tails. The M3 closer cannot. Picoshare M3 2026-05-26: the agent self-corrected (*"No plain test-strategy/ dir, the picoshare one is test-strategy-picoshare. Since we're in the picoshare repo, that's the one. Reading it."*) and did the right work, but the recovery is model-quality-dependent. The closer's sentinel-only assertion in `run-m3.sh` would PASS even if the closer read the wrong file. **Accepted as-is for first cohort** (cohort students work on a single codebase per training; the unsuffixed path resolves cleanly for them, paired with the existing skill-name-collision watch above). **Fires if** (a) a returning / multi-SUT student has multiple `test-strategy-<sut>` skills installed and the closer reads the wrong one, OR (b) a weaker model doesn't self-correct the lookup and reports *"skill not found"* then no-ops the rewrite. **Fix when it fires:** reword `ae101-m3-sharpen-skill` body to *"Read the test-strategy skill we just wrote in the quality-side session (find it under `~/.claude/skills/`)"*, drop the hardcoded path so the body doesn't need an override (gated body edit §22/§26). Sibling fix in `run-m3.sh`: add a content assertion to the closer turn (currently sentinel-only), e.g. assert the skill file mtime advanced.
- **`[watch]` `~/.claude/projects/<encoded>/memory/` auto-memory leaks across runs and propagates a dangling reference into M1's student-facing artifact.** Picoshare full-chain test 2026-05-26 found 5 files in `~/.claude/projects/-Users-anttitevanlinna-Projects-picoshare/memory/` carried over from prior runs (`MEMORY.md`, `practice-arc-M1-M6.md`, `task-shape-plan-modeable-picoshare.md`, `second-pass-assumption-audit.md`, `verifier-shape-matches-failure.md`). SUT-side baseline reset (git reset --hard, drop branches/worktrees, snapshot user-level skills aside, clear `~/.claude/plans/`) does NOT touch the user-level auto-memory dir. Visible consequence: M1's `CLAUDE.local.md` gained an candid "Known gap" section about a dangling memory pointer (*"the five plan-mode-able rules referenced by memory `task-shape-plan-modeable-picoshare` are not recoverable from this tree"*), correct authorial behavior, but reveals the leak. **Accepted as-is for first cohort.** First-time cohort students have empty auto-memory, no leak. **Fires if** (a) a returning cohort student (replay, second pass through a module) picks up prior-run auto-memory and the agent confabulates from it instead of self-correcting; (b) an agents-101 alum brings their existing auto-memory into AE101 and it interferes with the M1 baseline; or (c) the test-harness picks this up as a recurring problem across SUTs (audit cycles already see it on picoshare; lemmings + codesearch likely the same). **Fix when it fires:** add an auto-memory clear step to whatever wraps cohort runs (and to a future `chain-picoshare.sh` / `arrange-picoshare.sh` if/when those land, mirrors `arrange-lemmings.sh`'s scope), OR document explicit *"start with a clean `~/.claude/projects/<cwd>/memory/`"* instructions in the prework.

- **`[watch]` M2's plan file lives in `~/.claude/plans/` (laptop-local), not the repo, branch-sharing teammates can't pull it.** `push-back-on-the-plan-1` instructs Claude to write the plan to `~/.claude/plans/m2-csweb-shared-instance-<adjective>-<animal>.md`. That's user-keyed, outside the repo, never pushed with the branch. Caught by the codesearch M6 arc-note 2026-05-26 (quote: *"There is no `--bind`/`/api/search` plan file. M2 survives only as retrospective prose inside `CLAUDE.local.md`."*) and confirmed again by the 2026-06-01 M6 arc-note (*"M2 isn't on disk. It survives only as references inside other files."*). **Accepted as-is for first cohort**, solo students don't share branches across people, so the laptop-local location is fine. **Fires if** a real cohort team shares branches across teammates and a downstream M3+ session can't find the plan the M2 session referenced (the M3 main-scenario tail already has a fallback that globs `~/.claude/plans/m2-*-*.md`, but that only covers the same laptop, not a teammate's checkout). **Fix when it fires:** add a one-line callout in the M2 module body, *"the plan lives in `~/.claude/plans/`; if your team wants to share it, copy it into the repo"*, or change the prompt body to default to in-repo. Body edit on `push-back-on-the-plan-1` is gated (§22/§26).

- **`[watch]` `walk-and-send-off-3` option-cap juggling is a workaround for an AskUserQuestion tool limit, not a clean fix.** The 2026-05-27 arcticrex run hit invalid-params on the first AskUserQuestion in M4 (`walk-and-send-off-2`'s audit surfaced 5 gaps; the agent offered all 5 as options; the tool caps options at 4). Fixed by coaching the prompt to offer the four most material as options and, after the student picks, print the left-off gaps as a post-selection FYI. Smell: curriculum prose absorbing a tool constraint the agent should arguably respect on its own. **Accepted as the pragmatic fix for first cohort.** **Fires if** the tool's option cap changes (the "four" wording goes stale), OR the post-pick FYI list reads as noise / confuses a cohort student. Fix when it fires: revisit whether the audit upstream (`walk-and-send-off-2`) should cap its own gap count instead of pushing the cap onto the selection prompt.

- **`[watch]` `extract-the-task-shaping-rule.md` pedagogy stays REVISE by design (§16 `@import` ask in body, not fence).** The full disposition lives in the file's own maintainer block (standing watch, reaffirmed 2026-08-02): the wiring stays the student's choice; a pedagogy REVISE is the expected steady state. Fires if a cohort run shows a student picking a non-auto-load path and missing the wire; fix then: fence it as a question in `extract-the-task-shaping-rule-2.md` (gated, `curriculum/prompts/*.md`).

- **`[watch]` `ae101-m3-sharpen-skill` rewrites the test-strategy SKILL.md in place with no pre-save checkpoint.** `check_prompts.md` §20 wants a grill beat before a downstream-consumed artefact is overwritten; this prompt shows before/after only AFTER the write, substituting a post-hoc *"read the sharpen with a skeptical eye"* section. The technical judge read it as probably-intentional (the skeptical-read section carries an over-flag / fresh-session escalation, and the write is a small in-place section edit, not a whole-file assembly). **Accepted as-is for first cohort.** **Fires if** a student's sharpen silently degrades a section M4/M5/M6 later depends on and the post-hoc read doesn't catch it. **Fix when it fires:** add a light pre-save beat, or record the accept-by-design in the prompt's frontmatter `note:` so the judge stops re-raising it (gated, `curriculum/prompts/*.md`).

- **`[watch]` M4 pre-read (Willison, *Designing agentic loops*) sits ~10 months outside the 6-month window.** Cited by explicit editorial decision. The maintainer stamp now names the carve-out precisely (§7 named framing piece, NOT §2a durable account, corrected 2026-08-07 after the technical judge flagged that 2a's *specific completed event* condition doesn't fit a piece describing a general technique). **Accepted as-is for first cohort**; the stamp already carries the fallback (*drop the pre-read; Module 4 stands alone*). **Fires if** the §7 precedent gets tightened, or a cohort student reads the piece as stale against a materially changed practice. **Fix when it fires:** take the fallback, or re-anchor M4's pre-read to a current piece.

- **`[watch]` `reference/claude-code-for-engineers.md` invites students to grep a repo they don't have.** The hooks chunk points at the AE101 curriculum repository's `.claude/settings.json` as *"useful working configs to grep when authoring your first hook."* The referent was unresolvable (*"this repo"*) and was named explicitly on 2026-08-07 to clear a slides-class blocker, which fixed the resolution but exposed the underlying issue: cohort students have no access to that repository, so the invitation is unactionable for them. **Accepted as-is for first cohort** (the sentence still reads as provenance for the nine-events table even if the grep isn't available). **Fires if** a student tries to follow the pointer and asks where the repo is. **Fix when it fires:** inline a short example settings.json hook block in the reference, or drop the grep invitation and keep the provenance clause alone.
- **`[watch]` what Claude actually builds for a Ralph re-feed may be an in-session loop, which is the one shape the M5 slide rules out.** [what-packaging-is.md](../../lectures/what-packaging-is.md) § *Every re-feed pass starts a fresh session* teaches that each pass is a new session and that the conversation is what the loop throws away. Claude Code also ships `/loop <interval> <prompt>`, which is explicitly session-scoped (`check_platform_and_boundaries.md` §6, re-verified 2026-07-31), and `diagnose-and-resend-4` asks Claude to build whichever verifier shape the student names. Ask an agent inside a session for a re-feed and the nearest primitive it holds is the in-session one. If it reaches for that, the student's artefact contradicts the slide and quietly becomes the retry loop the slide exists to rule out. **Accepted as-is for first cohort.** **Fires if** any cohort or harness run picks Ralph re-feed at `diagnose-and-resend-4` and Claude builds an in-session loop rather than repeated fresh invocations. **Fix when it fires:** name the distinction in the slide's third bullet, or have the prompt ask for a fresh-invocation loop by name. **Cheap probe before that:** run the prompt once with `Ralph re-feed` chosen and read what lands.

## Hunt for buried gold

Moves are declared well across AE101. **Priors, mechanisms and why-it-works claims are not.** Those are a different species from a move, and a sweep shaped around *"which beat fails to name its move"* structurally cannot see them: a move is *do X*, a prior is *assume Y*, a mechanism is *it works because Z*. The recurring shape is not a missing sentence. The sentence exists, is correct, is well written, and sits somewhere with no weight: a fourth bullet, an item in an enumeration whose point is something else, link anchor text, or a supplementary that never reaches the deck.

### Verified in the current curriculum

Ranked by how much of AE101 the missing generalisation unlocks. All eight are open decisions; the evidence exists already, so the work is promotion or joining, not inventing theory.

1. **Understanding and durable artefacts solve different continuity problems.** [how-this-training-was-built.md:33](../../lectures/how-this-training-was-built.md) lands *"Understanding is the artifact. Files can be re-made from it"*. [what-keeps-a-long-running-session-going.md:7](../../lectures/what-keeps-a-long-running-session-going.md) later lands the counterweight: live context fills; files survive. The first makes work recoverable by the person who understands it. The second makes work resumable and transferable by another session, agent or teammate. **Action:** qualify the first claim so it does not accidentally demote durable state.
2. **Compounding includes deletion.** [spot-gaps-build-the-loop.md:36](../../exercises/spot-gaps-build-the-loop.md) says rules files have a half-life: adding rules is only half; subtracting dead ones is the other half. The M6 compound ladder and Key Concepts carry accumulation but not subtraction. The general loop is *add → sharpen → delete*. Otherwise the context system compounds noise along with learning. **Action:** promote deletion into the M6 synthesis rather than leaving it inside one exercise bullet.
3. **Parallelism has a topology, not just a count.** The strongest rule sits in optional [workflow-composition-lineages.md](supplementary/workflow-composition-lineages.md): Cognition's parallel writers conflicted; a single writer with advisor/reviewer agents held. The general rule is to parallelise independent reads, investigations and reviews; serialise coupled writes and shared semantic decisions. Parallelism pays only when the gain beats coordination, conflict and merge cost. **Action:** decide where the core course names this before students equate more agents with more throughput.
4. **The diff and the transcript are two different evidence layers.** [diagnose-and-resend.md:26](../../exercises/diagnose-and-resend.md) says Git tells what the agent did while the transcript tells how it got there, including drift and dead ends. Outcome evidence without process evidence misses the mechanism of failure; process evidence without the diff misses what actually shipped. **Action:** promote the pair into the general diagnostic method instead of leaving it as M5 exercise logistics.
5. **Every check has a latency budget.** Optional [how-the-best-do-ci-cd.md:39](supplementary/how-the-best-do-ci-cd.md) cleanly separates fast deterministic checks in the inline path from slow qualitative evaluators in the background or at a deliberate gate. Core M5/M6 names verifiers, judges and gates but can leave them sounding interchangeable. **Action:** decide where the core theory teaches placement by latency: seconds inline, minutes asynchronously, expensive judgment at a named gate.
6. **Two of the three soil-line priors exist only as figure microtext.** [the-whole-map.md:84](../../lectures/the-whole-map.md)'s engine map closes on *"the soil it grows in: act under uncertainty · competence is the gate · a move counts when it crosses the wall"*, a 13.5px italic SVG caption strip; the derived M3/M6 map copies drop the strip. Only the third clause ever gets a weighted home ([the-map-filled-in.md:184](../../lectures/the-map-filled-in.md)'s bold handle). The near-half governor (*name the uncertainty before you move*) instantiates the first prior and M6's prove-then-promote instantiates the second; neither prior is ever spoken on a slide. **Action:** decide whether *act under uncertainty* and *competence is the gate* each get one weighted spoken home or stay figure furniture the trainer voices from the strip.
7. **The ten-nudge ceiling never travels.** [run-the-first-experiment.md:92](run-the-first-experiment.md) carries the one pacing calibration a first-timer cannot supply (*"A handful of manual interventions is the right dose. Past ten or so, you've become the agent; call it and read what's there"*) mid-flow in the module's longest slide, between two prompt pastes; the M5 restatement at [learn-from-the-test.md:102](learn-from-the-test.md) keeps the stance and drops the number. Same shape as the 10% prior: the stance generalises, the operational number stays behind. **Action:** decide whether the ceiling gets a weighted M4 home and whether M5's restatement carries the number.
8. **The dip-before-climb expectation hangs off another law's bullet.** [the-map-filled-in.md:184](../../lectures/the-map-filled-in.md) carries the arc's only scale-shift statement (*"Your own sessions speed up before the team's numbers move, because the team's way of reviewing and sharing has to be rebuilt around the new speed first"*) as the trailing sentence inside the crossing-the-wall bullet, under that law's bold handle. [the-far-half.md](../../lectures/the-far-half.md)'s stance ledger routes the whole team-tier story to that sentence, M4 declines a team-tier caveat because it exists. Without it, the M6 promote move looks failed in its first month. **Action:** decide whether the expectation earns a claim-shaped position in the M6 close or stays a trailing clause.

### The reason leaks out of the deck; the checklist stays in the room

Supplementary and reference pages do not reach the composed Slides deck (`check_slides.md §10`). The recurring shape is that a beat's *checklist* is in-deck while the *reason it is not arbitrary* sits in the linked page. Scanned across all 46 in-deck files; these two survive.

- **The context-window prior is absent, and the absence is already a recorded decision.** [supplementary/token-efficiency.md](supplementary/token-efficiency.md) carries the counterintuitive half: a fuller window produces a worse answer, not merely a slower or pricier one. Zero in-deck hits. [the-loop-half-filled.md](../../lectures/the-loop-half-filled.md)'s maintainer block banks context-window capacity as a near-half law the closer skips, taught in the supplementary, *"It stays there."* The only in-deck trace is a link label pitching the thrift frame that page's first sentence exists to demolish. **Action:** the call is on the record; re-open it only if the long-run modules need students holding a trimmed window as a quality move rather than budget hygiene.

The pattern is per-file, not a policy problem. Checks-that-push-back closed on its own once [what-keeps-a-long-running-session-going.md:14](../../lectures/what-keeps-a-long-running-session-going.md) gave it an in-deck home with a named handle. Settle each case on whether the reason changes what the student does, not on where the page happens to live.

### Outside-in practitioner lens

This is an inference from published practice, not a claim that any named practitioner reviewed AE101. It asks what their strongest recurring arguments would add to the course after the ten curriculum-native joins above.

1. **Boris Cherny: automate recurring review comments into deterministic infrastructure.** Cherny's reported Meta practice was to log repeat review comments and write a lint rule after the pattern recurred three or four times ([Orosz interview](https://newsletter.pragmaticengineer.com/p/building-claude-code-with-boris-cherny)). AE101 teaches compounding and verifiers, but not the promotion threshold from judgment to automation. **Add:** a recurrence rule: observe first, count repetition, then turn a stable review pattern into a check.
2. **Boris Cherny and Kieran Klaassen: retrieval architecture is part of context engineering.** [token-efficiency.md:43](supplementary/token-efficiency.md) holds both practitioner moves: pass paths rather than pasted walls; let model-driven grep/glob retrieve what is needed; treat the folder as the agent's durable environment. **Add:** make context economics core theory, what enters the window, how long it stays, and why addressable files beat copied context.
3. **Armin Ronacher: review bandwidth governs safe generation throughput.** [The Final Bottleneck](https://lucumr.pocoo.org/2026/2/13/the-final-bottleneck/) frames unread output as an accumulating queue: when input outruns review, throttle, shed load or add a trustworthy downstream check. AE101 names backpressure, but does not yet teach work-in-progress as the governing quantity. **Add:** a WIP law: do not start another agent stream merely because a slot is free; start it only when the review system can absorb the return.
4. **Armin Ronacher: failed automation should be deleted, and negative knowledge should persist.** In [Agentic Coding Things That Didn't Work](https://lucumr.pocoo.org/2025/7/30/things-that-didnt-work/), Ronacher keeps automation only for repeated work, evaluates whether it improves results, and deletes helpers that do not become useful practice. **Add:** compound failed experiments too: record what was tried and the acceptance variance, then remove the dead helper so future agents do not rediscover or inherit it.
5. **Simon Willison: require enough understanding to remain an active participant.** [Understand to participate](https://simonwillison.net/2026/Jul/2/understand-to-participate/) names cognitive debt: as the agent's implementation outruns the human's model of it, the human loses the concepts needed to steer what comes next. Passing checks does not repay that debt. **Add:** a handoff comprehension gate, explain the changed system, demonstrate it, and identify the next safe change before ownership transfers.
6. **Simon Willison: encode the boundary, then delegate judgment inside it.** In [Fable's judgement](https://simonwillison.net/2026/Jul/3/judgement/), Willison reports better results from asking a capable model to decide when tests or lower-power subagents fit than from prescribing every branch. AE101 is strong on explicit rules and could imply that more prescription is always more mature. **Add:** a rule-to-judgment ladder: automate invariants, state constraints and evidence requirements, and leave reversible task-level choices to observable agent judgment.
7. **Hamel Husain and Shreya Shankar: error analysis comes before eval design.** Their [Evals FAQ](https://hamel.dev/blog/posts/evals-faq/evals-faq.pdf) starts the minimum viable setup with manual review of real traces and says the failure taxonomy should emerge from observed application behaviour. AE101 diagnoses one run and calibrates a judge, but does not leave the student with a growing case bank. **Add:** a durable failure dataset: real traces, human labels, failure clusters and the checks each cluster earned.
8. **Hamel Husain: keep exploration alive after the evaluator exists.** The same FAQ warns that targeted classifiers find failures already described by the classifier and recommends retaining random samples. A mature gate can make the team blind to novelty. **Add:** split monitoring into exploitation (known checks) and exploration (random or outlier traces), with a route for new failures to reshape the eval set.
9. **Matt Pocock: shared understanding is an explicit pre-build artefact.** Pocock's [grilling primitive](https://github.com/mattpocock/skills/blob/main/docs/productivity/grilling.md) descends a decision tree one dependency at a time, gives a recommended answer, lets the codebase settle what it can, and waits for explicit confirmation before enactment. AE101 uses this shape locally without fully naming it as reusable task shaping. **Add:** generalise the interview contract: one frontier question at a time until both sides can state the same decision tree.
10. **Kieran Klaassen: compounding is not complete until the next loop retrieves and tests the lesson.** The [Compound Engineering guide](https://every.to/guides/compound-engineering) asks whether the system would catch the issue automatically next time; writing a note alone is not the finish. **Add:** a retrieval test on every compound move: name the future trigger, the durable home, how the next session finds it, and the check that proves it fired.

### Enactment gaps, stated in the curriculum, never performed by the student

Surfaced 2026-08-08 by a coverage map of AE101 against two external practitioner accounts (Wollsén and McCrosky's Crawl/Walk/Run/Fly playbook, and Wollsén's 490-PR self-report; both filed at `continuous-research/observations/wollsen-fredrik.md`). Seven candidate gaps went to adversarial refutation and four died outright, because the curriculum teaches the concept under its own coined vocabulary (*verifier*, *backpressure*, *absorption*, blast radius) or inside a supplementary the search never opened. These three survived. They share one shape: **AE101 states the principle and never has the student perform it at full size.** That is what makes them cheap, no new theory, one beat each.

- **No agent ever answers a human reviewer.** PR creation is taught ([fix-tests-first.md:49](../../exercises/fix-tests-first.md), [close-the-ticket.md](../../exercises/close-the-ticket.md) § *Write the close-out and send it*), and read-the-failure-then-fix-until-green is taught as a runtime primitive ([reference/claude-code-for-engineers.md:239,247](reference/claude-code-for-engineers.md), `/loop` watching a build, `/goal "all tests pass"`). But the direction is inverted everywhere review comments appear: the agent *produces* them ([earn-the-trust.md:160](earn-the-trust.md), [map-the-access-surface.md:126](../../exercises/map-the-access-surface.md)) or *mines* them, never answers them. Purely IC-level, no permission needed, and it is the loop this audience lives in daily. **Not to be confused with agent-resolved merge conflicts, which are a decided doctrine pointing the other way** ([reference/multi-session-git.md:74](reference/multi-session-git.md): *"Do not let either agent resolve the conflict automatically"*), leave that alone. **Action:** decide whether one exercise step has the agent read a reviewer's comments and push the response, or whether this stays out of scope because cohort students have no live reviewer.

- **No beat exercises the running software at the moment the agent declares done.** Runtime verification exists on *reading* surfaces and at project bootstrap ([supplementary/build-a-project-from-zero.md:13](supplementary/build-a-project-from-zero.md) *"Open the page and play"*; [supplementary/skill-stacking.md](supplementary/skill-stacking.md)'s browser-QA workflows; [supplementary/how-the-best-do-ci-cd.md](supplementary/how-the-best-do-ci-cd.md) §6 on flags and rollback). What is missing is an in-room beat where the student runs the delivered thing before believing the claim, so every countermove to premature completion is a check the agent can satisfy **without the software ever having executed**. **This is deliberate at M5 and should not be "fixed" there**, [diagnose-and-resend.md:27](../../exercises/diagnose-and-resend.md) chooses it: *"You hold the three lenses. You are not holding the codebase."* The question is whether it should be absent from every module. Deploy and rollback stay out of scope; opening the thing is a different ask. **Action:** decide whether one line lands in M4's return-read or M5's verifier menu, *exercise the path a user takes, not only the check you wrote*.

- **The student never points an agent at a repo they do not own.** [close-the-ticket.md](../../exercises/close-the-ticket.md) § *Anything can be reverse-engineered* declares the licence in full (*"a codebase nobody documented, a CI pipeline, a config, an API you did not write, the system your team runs on and cannot explain"*) and then enacts it on one tracker ticket, admitting the scale in its own words: *"The ticket is that move at its smallest."* AE101 is single-repo by architecture, not oversight ([prework.md](prework.md) §1 *"Pick THE repo"*; [getting-going.md:10](getting-going.md) *"You'll stay in this one repo for the whole training"*), so this is a design decision rather than a missing slot. **Action:** decide whether a named M6 shape or a homework line carries the full-size version, or whether the single-repo contract wins and the declaration stays deliberately un-enacted.

### Tensions the theory rewrite must preserve

These are not inconsistencies to smooth away. Each pair identifies a boundary the course needs to teach.

- **Parallel generation versus accountable absorption.** Klaassen's direction increases concurrent generation; Ronacher's direction limits starts by trustworthy review capacity. Both can be true because bottlenecks migrate. Available agent slots measure production capacity; acceptable WIP is governed by how much returned work the verification and review system can absorb. **Action:** teach concurrency and WIP as different quantities, with a decision rule for when another stream helps and when it only lengthens the unread queue.
- **Encoded rules versus delegated judgment.** AE101 teaches durable instructions and deterministic checks; Willison shows the value of letting a capable agent choose reversible tactics inside explicit boundaries. The mature system is neither rule-free nor maximally prescribed: encode invariants, constraints and evidence requirements; delegate observable, reversible choices; remove rules that no longer earn their context cost. **Action:** make the rule-to-judgment boundary explicit so compounding does not become instruction accretion.

One structural note falls out of the same hunt:

- **The declaration affordance in AE101 is grammatical, not a widget, and it is currently accidental.** Across the 93 body headings in the thirteen exercise files, every one is an imperative or a phase label except [close-the-ticket.md](../../exercises/close-the-ticket.md)'s *"Anything can be reverse-engineered"*. Lecture headings are the opposite: claims (*"Passing is not proof"*, *"Gates decay"*, *"The file is steady; the agent moves"*). The mood switch is what makes a declaration land inside an exercise, and it costs nothing because the nav rail and TOC are built from `##` headings already. Adding a rendered widget for declarations would box a thought that works better unboxed, and would retro-brand the one beat that earns it. **Action:** consider writing the heading-grammar convention into `check_slides.md` so the affordance is reliable rather than lucky. It also gives a cheap test at proposal time: a candidate declaration whose heading is a label rather than a claim is not a declaration yet.

### The delegation-frontier model now lives in three files

`supplementary/agentic-engineering-progression.md` (M1→M2 gap, optional), `lectures/when-a-plan-is-good.md` (M2, guaranteed), `lectures/the-gate-is-a-claim.md` (M5, canonical). The repetition is deliberate dosage, not a leak (`check_pedagogy.md §9b`), and the three SVG copies are byte-identical, so the parent-and-re-derive discipline is holding. What follows are the seams that opened when the model spread.

- **The figure's caption names a limit that is on neither axis, in all three copies.** The caption reads *"The frontier: the largest task you can hand off, well designed, enough unclarity removed"*. That names task design and clarity. Everything else inside the same frame says the limit is the checks: the y-axis is `CALIBRATION · TRUST, MEASURED`, the arrow label is *"moves as fast as the gates behind it"*, M5's bullet 4 and the supplementary's bullet 4 both read *"The frontier moves outward only as fast as the gates behind it"*, and M2's header asserts *"What you can test and check sets your complexity ceiling"*. The caption is the largest text in the figure at 15.5px and the only definitional line that survives at projection distance, so on two projected slides the student is shown the law and a different law in one frame. The prior wording carried the calibration half (*"and still trust the result"*); the current wording was chosen to speak the cautious reader's language, which it does. **Action:** decide whether the caption re-acquires the trust clause, or whether design-and-clarity is the definition and the four bullets across three files move to match it. Changing it means editing the parent (`the-gate-is-a-claim.md`) and re-deriving twice. Body edit, gated (§22/§26).

- **The supplementary defines calibration with a term the two canonical copies avoid.** It reads *"Calibration is whether trust in the result was earned by a **gate** you have verified"*; both M2 and M5 read *"earned by a **check** you have verified"*. §9b's carve-out that licenses the repetition is priced in self-sufficiency at each placement, and this is the earliest placement with the least behind it, a student one module in has met no gate. **Action:** align the supplementary to *check*, or accept the term of art here and say why. Body edit, gated.

- **`[watch]` The page's closing line makes bare trust the limit.** *"Leverage is not reach alone. It is reach that trust can keep up with."* Five lines above, the page defines the distinction as trust that was **measured** (*"Unmeasured green produces reckless autonomy"*). Unmeasured trust keeps up with anything, so the sentence a skimmer keeps points back at the quadrant the bullets warn about. **Accepted as-is pending the caption decision above**, both are the same question about what the limiting quantity is called, and fixing them separately would land two different answers. **Fires if** a cohort student paraphrases the model back as being about confidence rather than about checks.

- **The value multiple carries no population and no axis.** Body: *"He estimated his four-engineer team delivered three to four times the value of an average team, far below the multiple suggested by its pull-request count."* The deflationary clause is what makes the paragraph `check_research_claims.md §12`-compliant on the comparison, and it should stay. What is unnamed is the denominator: *an average team* is no population, and *value* is no axis. `continuous-research/observations/wollsen-fredrik.md` § *Numbers that must not be carried* lists this figure as `[UNVERIFIED STAT]` (self-estimate, no axis). **Action:** decide whether the bare multiple goes and the qualitative half stays (*"far below what its pull-request count suggests"*), which is the fallback the page's own source line already names, or whether the figure stays with its population and axis stated. Body edit, gated.

### Process regressions found 2026-08-09

- **Two modules lost their Quality blocks to a body edit; restored, and the restored stamps are now correctly stale.** `f45db08` removed the whole block from [earn-the-trust.md](earn-the-trust.md) and [run-the-first-experiment.md](run-the-first-experiment.md), seven per-class SHAs each, plus two `cross_module: PASS` records, one spanning the seven-file prework→M6 set, leaving an orphaned `- maintainer-reviewed` bullet under no parent. The blocks are back. This is what the ladder is built for: a body edit degrades named classes, it does not void the record, and `scan-stale-classes.js` can only route diff-regions to classes if the stamp survives to go stale. With them restored the scanner reports M3 stale on writing / story / technical / pedagogy / slides and M4 on writing / story / pedagogy / slides. **Action:** re-audit those classes before ship. **Do not delete a Quality block to avoid a stale marker**, a stale stamp is a to-do, a deleted one is amnesia, and the cross-module verdicts cost a judge run over three and seven files to regenerate.

### Key Concepts, corpus-wide (2026-08-12)

- **`## Key Concepts` sits above teaching content in four modules, so it briefs instead of recapping.** The spec is now explicit in [module-shape.md](../../module-shape.md) § *Key Concepts*: reminders only, placed after the last teaching section and before `## Next`. M4 was moved 2026-08-12 and is the reference. Still wrong: [plan-mode-done-right.md](plan-mode-done-right.md) (*Save the rule if it earned itself* sits below it), [earn-the-trust.md](earn-the-trust.md) (three sections below, including the skill sharpen), [learn-from-the-test.md](learn-from-the-test.md) (five, including the packaged re-send — the module's whole second half), [spot-gaps-build-the-loop.md](spot-gaps-build-the-loop.md) (the human close). M1 is already correct. **Action:** move the block in each, then re-read its bullets in the new position — a bullet written as briefing does not automatically read as a recap once it moves, and the M4 pass had to re-tense all four. Body edit, gated.

- **Bullets carrying material that appears nowhere else in their module.** The M4 read found the section teaching rather than reminding: *"Gap analysis is walk the system you have against the system the task needs. The system you have is what gets tested"* (cut — reads as QA, and triples the Big Idea), and *"Un-packaged is by design. The packaging move only lands once you've seen what un-packaged gets wrong"* (opaque on first meeting; the term's gloss lived only in a `What You'll Learn` bullet 30 lines up). **Action:** run the spec's test on the other five modules — for each bullet, name the beat in *this* module that taught it. Can't → the bullet is new material, and either moves into the body beat that needs it or goes. Body edit, gated.

- **Breadcrumb compliance is unmeasured and probably thin.** `check_strategy_tie_in.md` §6 asks 1–3 bullets per module to close on a 5–8 word clause carrying a strategic theme, varied between adjacent modules. It is deliberately not greppable (the `**Remember:**` wrapper was swept corpus-wide in `a90c9ec`), so nothing checks it and no one has read for it since. **Action:** one pass across all six, reading for whether each section lands on a memorable clause at all, and whether adjacent modules are repeating a theme. Do this AFTER the placement and reminders-only passes — a breadcrumb on a bullet that shouldn't exist makes the section worse (§6's own rule).

### M4 read 2026-08-12

---

**Canonical home:** this file. Module files carry zero TODOs, all pre-cohort open items route here.

**When an item closes:** delete the bullet. Git log carries the history. Don't annotate "done", closed = gone.

**When a new item surfaces mid-session:** add it here, not in the module file.

## Voice-panel session open items (2026-08-09)

Session artifacts: panel spec `curriculum/evals/judges/voice-panel.md` (6 seats: 5 authors + Sami the cautious reader; maintainer-guard check; Sami veto outranks). Rules landed: `check_writing.md` §4 (AE101 audience contract + Sami pass + house-echo + language-is-UX), `check_student_facing.md` §30 (theory-spine echo) + §31 (artifact-not-act), `check_cross_module.md` §8 (scramble stakes from clock, not room), `check_writing.md` §3 (keep-notes bind to passage), `check_pedagogy.md` §34 example de-minimized. Compounded: `2026-08-08-writing-write-to-the-caution-engineer-audience.md` (severity high, cohort-load evidence), `2026-08-09-writing-keep-notes-bind-to-their-passage.md`, `2026-08-09-student_facing-scramble-stakes-from-clock-not-room.md`. Body fixes: 3 commits 2026-08-09 (4f5ce48, e4633f4, 9870b74) + M2/M1 batch.

- **`[open]` Judge re-fire sweep.** ~50 stale (file,class) pairs from this session's edits across 14 files, `scan-stale-classes.js --files <edited>` regenerates the exact bill. Deferred by maintainer call ("evals later"). Batch pattern: eval-fire per class, Sonnet, schema-forced ledgers; expect PASSes (edits were panel-driven + compendium-aligned) but expecting is not stamping.
- **`[open]` Eval-regularity builds.** (a) flinch-grep lint under `curriculum/evals/lints/` (audience-contract greps: `just a simple`, `trust the agent`, act-verbs near quoted prompts, §8 room-form); (b) compendium-SHA staleness in `scan-stale-classes.js` (compendium edit → stales its class corpus-wide; today it stales nothing); (c) sub-letter/tail-rule numbering in `check_writing.md` so the ledger sees §4's sub-blocks + the two unnumbered tail rules; (d) writing-judge template hygiene, rule refs by name not number (combative-verbs cited as "rule 13", is 17) and confirm `/eval-fire` dispatches Sonnet, not the template's stated Haiku (severity-high degradation entry 2026-05-02).
- **`[taste]` Logged-as-heard, per-module, no cards owed:** M1 opener *"infinite version of it"* + bare *"Let's go."*; M1 closer 279-count beat + kicker (3-way persona split, author's voice) + *"useful wrongness"*; M3 map-transition line + worktree-LO unframed + one-hardening boundary unstated; M4 opener bare permission + *"Not a template. A question."* + gap-analysis credit ABSENCE; M5 merge-base paragraph idea-sentence + single-variable reframe buried in KC + *"mid-flight"* + impersonal worry-narration; M6 *"read to the bone"* + *"Going deeper"* subhead + Encoding-taxonomy bullet. Reader-veto keeps on record: *"slightly leaky"* (Sami delight), *"Despair not."* (slang-test list), capstone sign-off (author voice), shotgun/cannon (provocation-beat guard written in-file).
- **`[open]` Panel coverage beyond module files:** 7 files paneled (2 M1 lectures, M2+M4 lectures, M3/M5/M6 module files). Unpaneled: remaining lectures, exercises, supplementaries, reference pages. Fire per the spec's when-to-fire (new/reshaped files after writing-judge pass; pre-cohort one panel per module's heaviest-prose file).

## A second tab that lands on the right slide

The want: a student working an exercise opens another exercise's instructions in a second browser tab and keeps it open beside their Claude session. Maintainer's framing: *"new tab is essential to allow for jumping between claude sessions and instructions. Browser back does not help in that."*

- **`[watch]` The deck has no per-slide address, so a second tab opens at slide 1.** `site/layouts/slides.js` never reads `location.hash`. The built workbook carries one stable `id="exercises-<slug>"` per inlined exercise, and long-read honours them natively, so only slides-mode readers are stranded, and the deck is where there is no back. The mechanism is proven: measured against the shipped acme AE101 workbook, resolving the arrival hash through the anchor index `open()` already builds lands on the right slide (`#exercises-author-test-strategy-skill` → slide 164, `M3 — 165 / 468`), and script order is synchronous, so the index is built before the hash is read. **Two things constrain the shape.** (a) A category policy — *every `#exercises-*` link opens a new tab* — binds tab-spawning to link category, so an anchor that goes dead stops being a silent no-op and becomes a tab that reloads an 870 KB workbook onto the cover slide, on `## Homework` / `## Prework` slides a trainer projects. Every tracked build resolves every `#exercises-*` / `#lectures-*` link, and nothing holds that: `check-include-anchors.js` reads source, and a built page is invisible to it. (b) Honouring any resolvable fragment at mount also catches the workbook's own top-nav chips (`#getting-going`, `#prework`), and an arrival hash consumed in long-read stays armed, so a reader who toggles to Slides later is thrown back to where they came in. **Accepted as-is for first cohort.** No shipped beat asks a student to hold two pages open, so nothing breaks without it. **Fires if** a student or trainer follows an exercise link into a second tab, lands on the cover slide, and pages to find where they were; or a beat gets written that needs two pages open at once. **Fix when it fires:** the per-link author mark, not the category policy, which keeps the dead-anchor and top-nav blast radius at zero. The category version owes a resolve-check over `site/clients/**/index.html` first.
