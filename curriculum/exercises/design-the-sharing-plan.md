# Exercise: Design the sharing plan

**Time:** 23 minutes.

`module-7/jtbd.md` names the job. `module-7/branch.md` names the candidate. Now find the obstacle that decides whether the teammate can actually absorb it, then draft both halves of the plan.

## Find the absorption bottleneck

*5 min*

The obstacle is usually not the first technical limitation a builder sees. The agent compares the technical, social, political, habitual, governance, and trust obstacles and writes the load-bearing one.

{{prompt:share-your-work-3}}

Read `module-7/absorption-bottleneck.md`. A good bottleneck explains why several other obstacles stay hard. If removing it would solve only itself, ask the agent to cluster the obstacles again.

## Draft both plans

*18 min*

The technical plan and people plan carry equal weight. Missing names remain visible as `UNASSIGNED — Monday's question`; the agent does not invent them.

{{prompt:share-your-work-4}}

Read the two files side by side. `module-7/technical-plan.md` should name what moves the outcome and the first real test. `module-7/people-plan.md` should name who owns, governs, operates, judges, and propagates it. A polished technical file beside a people file full of unnamed roles is the finding, not a finished rollout.

## Carry the plan forward

[Test the sharing plan](test-the-sharing-plan.md) checks the assumptions underneath these plans, imagines the failure, and writes the first real-world contact to `module-7/monday.md`.

<!-- maintainer -->

**Atomic — no phase markers.** The bottleneck is the design constraint consumed immediately by both plans.

**Leap test — next working day:**

- Opens `module-7/absorption-bottleneck.md` and can show which other obstacles become easier if it moves.
- Uses `module-7/technical-plan.md` to run one real test against the teammate's outcome.
- Uses an `UNASSIGNED — Monday's question` line in `module-7/people-plan.md` to ask one named person for ownership rather than silently inventing a role.

**Failure modes and escape hatches:**

- **Bottleneck:** the first answer is merely a technical obstacle. Have the agent cluster again and name the obstacle whose removal makes several others easier.
- **Two plans:** the technical file looks finished while the people file hides behind role names. Preserve `UNASSIGNED` and turn the first missing name into Monday's ownership question.

**Why this is one bounded exercise:** the bottleneck is the design constraint for both plans. The second step consumes the first artifact immediately; neither plan is complete without the other.

**Framework attribution:** Richard Rumelt's crux discipline is used as the load-bearing-obstacle test. Student-facing language stays with *absorption bottleneck* because that is the concept being designed here.

**Quality:** compendium-audited 2026-08-25 (writing@a4be944f story@a4be944f technical@a4be944f behavior@a4be944f pedagogy@a4be944f strategy@a4be944f slides@a4be944f)
- judges @a4be944f: writing PASS, story PASS, technical PASS, behavior PASS, pedagogy PASS, strategy PASS, slides PASS
