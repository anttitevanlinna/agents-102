# Pre-cohort punchlist, AE101 M1–M6

> **CLOSED = DELETE THE BULLET. Not "annotate done." Not "rewrite as smaller follow-up." Not "update with what landed." DELETE.**
>
> Git log is the history. This file is current state. A closed bullet rewritten as adjacent work is a NEW bullet pretending to be progress, surface the new work cleanly under a real section, or don't add it. If the new work isn't pre-cohort scope (M1–M6 done-done), it doesn't belong in this file at all.
>
> **Editing this file? Load the rules that fire here.** A flagged item is a hypothesis, not a to-do: before "fixing" one, check it isn't deliberate pedagogy, a wrong-layer diagnosis, or a cosmetic non-issue (`check_pedagogy.md` §61/§61a). And a closed item that carries a design decision moves to its source first (the prompt's frontmatter `note:` or the exercise maintainer block, where the editor reads), THEN the bullet is deleted, this file never holds the rationale (`memory/compounded/2026-05-03-platform-todos-route-to-training-tracking-surface-not-maintainer-blocks.md`).

Open decisions before the first paying cohort. Module files carry zero TODOs; this is the single tracking surface.

**Item types.** Bullets are open decisions or tactical fixes by default, something has to happen before cohort. Prefix a bullet with **`[watch]`** to mark *currently accepted, no pre-cohort action*, the behaviour may bite us, but we've decided to ship as-is and see. Watch items convert to action items if the cohort run surfaces the failure; otherwise they get deleted like any other closed bullet. Don't let watches accumulate as a dustbin: a watch with no named firing condition is procrastination dressed up.

Sim sweeps and platform-capability checks are not tracked here. The `curriculum-pre-ship-audit` skill auto-fires on touched files; `check_platform_and_boundaries.md` fires on platform claims at content-time. **Per-class verdicts (PASS / REVISE / grandfathered) live in each module's own `**Quality:**` block**, `update-quality.sh` stamps both PASS and REVISE with the instance-JSON path so a successor can grep Quality blocks for state without consulting this file.

**Harness / tmux-runner engineering is not tracked here either.** Runner bugs, scenario hardening, per-turn assertions, socket isolation, and state.json shape live in `curriculum/evals/mechanical/tmux-runner/IMPROVEMENTS.md`. This file tracks cohort-blocking *curriculum* decisions only; a finding that's fixed in runner code or a scenario file belongs there, not here.

**Source-freshness is not narrated here either, it is stamped per-source and generated.** Each source carries a checked / result / due freshness stamp in its file's `Source verification` block (format: `curriculum/source-freshness-format.md`); `curriculum/evals/scripts/source-freshness.sh --target <cohort-date>` produces the punch list. The 2026-05-25 full sweep cleared all source BLOCKs; per-source stamps now live in each file's `Source verification` block. Do not add new hand-narrated source-verification prose; stamp and run the script instead.

## Per-file specific concerns (not caught by auto-fire)

- **`story-of-module-6` — cut, or cut down (Antti 2026-09-02, after the cold read of the re-cut M6).** ~1,200 words of first-person prose between a one-slide handoff lecture and the Human close; the only long text slab left in the module. Its thesis (a rule in context is not a rule in the output) is the honest shadow of the module's frame (the agent runs on what you have expressed), so the beat belongs; the length and the April-dated session detail may not. Antti's voice and session: he re-reads and cuts, not an agent. Card-shaped.
- **Tentative slide: three messages for a senior engineer early in Claude Code (Antti 2026-09-03, prompted by Dino advocating that skill stacking be taught).** Antti comes back to this; nothing lands until he does. The three: (1) *your frontier is what you have expressed*, the model feels dumb in week one because twenty years of judgement are still in your head; (2) *fix the system that did the work, not just the work*, a push-back becomes a rule, a worry becomes a check, and the rules file gets pruned as often as fed; (3) *skills come from the work you repeat, and the job sizes them*, read your own history before designing anything, Dino's stack is the picture of the destination, not the first move. Candidate homes: M6 opener or closer, or M1 opener for the early-use audience. Standing against a slide: all three are already carried in-deck at their beats (closer slide 1 and bullet 3, M1 opener, Composing footprint + stack scan); `check_lectures.md` §1 meta-frame is a closer, never an opener; the closer ends on *The training closes.* with no forward tee by decision. Non-slide homes if the slide loses: a newsletter piece with the Dino provocation as hook, or two lines of stance beside the Dino/Pocock figure notes in the strategy doc so the ordering claim is not re-litigated. Card-shaped either way.
- **`[watch]` `extract-the-task-shaping-rule-2` save-path ambiguity (M2).** Prompt asks Claude to "propose two-three plausible paths" for the rules file and pick one. Codesearch M2 dry-run (2026-05-24) had the agent integrate the new rules INTO the existing `CLAUDE.local.md` (created at M1's compound), a legit "integrate, don't append" reading, but it means a downstream prompt that loads "the rules file" and `CLAUDE.local.md` as separate inputs sees one path, not two. Lemmings runs typically save to a distinct file. **Accepted as-is for first cohort.** Fires if any downstream module prompt (M3 sharpen-skill, M4 walk-and-send-off, M5 diagnose-and-resend, M6 spot-gaps) loads "the rules file" and `CLAUDE.local.md` as separate inputs and silently de-dupes or overwrites, observe whether any module behaves differently when the two paths collapse to one.
- **`[watch]` skill-name collision breaks the hardcoded-literal chain on replays (M3 `test-strategy`).** The skill saves to a fixed name and its consumer prompts cite the literal path (`~/.claude/skills/test-strategy/`). First-cohort clean `~/.claude/skills/` hits no collision. (The M6 `session-shaper` half died with the 2026-08-01 authoring-sequence cut.) **Accepted as-is for first cohort.** Fires if a returning/replay student or an agents-101 alum already has the name, Claude suffix-renames (`test-strategy-<project>`), and a consumer prompt citing the literal misses. Fix when it fires: warn in the exercise body to check where Claude saved, OR have the creation prompt overwrite-on-conflict.
- **`[watch]` save-gate-then-immediate-read race in `author-test-strategy-skill-1`.** The prompt ([author-test-strategy-skill-1.md:27](../../prompts/author-test-strategy-skill-1.md)) says *"show me before saving"* or equivalent, then the immediate next scenario step invokes the file at `author-test-strategy-skill-2`. If Claude obeys the save gate literally and waits for an approval that doesn't come in autonomous mode, the file is never written and the next step fails on a missing artifact. **Accepted as-is for first cohort.** Fires if any headless / unattended cohort run hits a missing-artifact failure downstream of that turn. Fix when it fires: tail the downstream literal with an explicit approval, OR rewrite the save gate to *"save now and show me after"*.
- **`[watch]` worktree-setup prompts carry literal `<repo-name>` placeholder paths inside the prompt body.** [ae101-m3-fork-quality-side.md:20](../../prompts/ae101-m3-fork-quality-side.md) and [ae101-m5-worktree-setup.md:26](../../prompts/ae101-m5-worktree-setup.md) sketch shell commands with `../<repo-name>-m3-quality` / `../<repo-name>-m5` as placeholder text. Body says Claude should infer the actual repo name from the cwd, but the inference instruction lives outside the copied prompt, only the body has it; the shell sketch reads as the literal command. **Accepted as-is for first cohort.** Fires if Claude follows the shell sketch verbatim and creates a directory called literally `../<repo-name>-m5` (a sibling named `<repo-name>-m5`), causing run-m3 / run-m5's `--worktree-cwd` assertion to miss (codesearch M3/M5 + picoshare M3/M5 + lemmings M3/M5 all inferred correctly so far; this fires when prompt-following gets more literal). Fix when it fires: move the infer-from-cwd instruction inside the prompt body so it travels with the copy, OR fold the substitution into the prompt registry expander.
- **`[watch]` `prompt-source-audit.sh` returns BLOCK on `walk-and-send-off`.** `walk-and-send-off` blocks on curriculum-coined-label leakage around "send-off" inside prompt text (P4 / §40b), plus a skill-path reference at [walk-and-send-off-2.md:24](../../prompts/walk-and-send-off-2.md). Linter-strict rather than runtime-breaking; the runs themselves succeed. **Accepted as-is for first cohort.** Fires when the "done-done" gate is applied to this exercise (done-done needs static audits clean). Fix when it fires: rephrase the "send-off" prose to be self-contained, or self-define the term in the fence per §40b.
- **`[watch]` M5 grill "lock it in" with an unanswered round leaves load-bearing decisions unowned.** `diagnose-and-resend`'s grill-before-save sets up multi-round Q&A but never names what happens when "lock it in" arrives before every question is answered. Lemmings retest 2026-05-26 (phase B): the agent settled three packaging decisions (keep constraints, reuse the win/loss text, commit both files) on its own recommendations rather than the maintainer's answers. **Accepted as-is for first cohort**, the agent's defaults were reasonable and the run passed. **Fires if** a cohort student says "lock it in" mid-grill and the agent bakes a wrong default into the packaged artifact; the surprise branch is body-named since 2026-08-30 (the Phase-4 timebox callout tells the student to take the recommended answers at lock-in), so only the wrong-default branch stands. Fix when it fires: the grill prompt names the lock-in contract (`check_prompts §35/§20`), e.g. "if I say 'lock it in' before answering everything, use your best recommendation for the rest and tell me which you assumed."
- **`[watch]` M3 `ae101-m3-sharpen-skill` closer assumes scrollback from the quality lane while running in the main lane.** The main-lane closer tells the agent to read scrollback for "the moment the skill was invoked on the security-tested feature", but that invocation runs in the QUALITY-lane session, not the main lane where the closer runs; the cross-window bridge lives in a body callout, not the prompt fence (`check_prompts §34/§40`). Lemmings retest 2026-05-26: the agent self-corrected by grounding on the ADR; the same cross-lane scrollback assumption recurs at quality turn-4. **Reinforced by the 2026-05-27 Phase 2 redesign:** `author-test-strategy-skill-2` now officially keeps invocation in the m3-quality worktree and runs it on the whole codebase/repository, not the security-tested feature, so the closer's "invoked on the security-tested feature" wording is now off on both axes (wrong lane AND wrong target). The closer was **deliberately left unchanged** (2026-05-27 call): the main-lane security scrollback (access-control output + STRIDE + ADR) plus a fresh read of the SKILL.md is deemed enough to ground the rewrite, and the agent recovers by grounding on the ADR. **Accepted as-is for first cohort.** **Fires if** a fresh main-lane session or unattended run can't locate an invocation moment and the closer mis-grounds (e.g. hunts for an invoke-on-feature exchange absent from main-lane scrollback). **Fix when it fires:** move the cross-lane anchor into the fence (§34), or have the closer ground on the ADR / SKILL.md explicitly rather than on scrollback, and drop the now-inaccurate "security-tested feature" clause (gated body edit §22/§26).
- **`[watch]` M2's `push-back-on-the-plan-1` plan over-ships M3's threat-model territory: encodes "filter+lock" projection test rigor before M3's ADR is supposed to name the pattern.** Picoshare M2 plan 2026-05-26 Step 5 Case 10 lands at: *"Projection lock (filter+lock, NOT just struct-shape trust). Insert a link whose `Label` is a distinctive sentinel … GET public-status, read the raw response body bytes, and assert … the sentinel is absent … Positive guard in the same case … This is the disclosure-lock check for a public read surface."* That language anticipates M3's ADR-0001 work. M3 still added value (named the precise threat, *"a future maintainer reaching for `respondJSON(w, gl)`"*, tied to existing code at `handlers/guest_links.go:46`, considered three rejected alternatives), so the pedagogical surprise survived. But the I-class disclosure ADR's load-bearing payoff is partially baked in by M2. **Accepted as-is for first cohort.** **Fires if** M3's threat-model passes feel like rubber-stamping the M2 plan (the agent's STRIDE turns produce no novel framing relative to what M2's plan already encoded), OR if cohort students report M3 lands as *"M2 said this already."* **Fix when it fires:** scope the M2 plan to *what to build* and defer *how to test for non-disclosure* to M3; let the M2 plan flag *"owner-only fields exist; how do we keep them out"* as an open question, not a solution. Body edits on `push-back-on-the-plan-1` are gated (§22/§26).
- **`[watch]` `ae101-m3-sharpen-skill` (M3 closer) hardcodes `~/.claude/skills/test-strategy/SKILL.md` (unsuffixed) in its body, and is the ONLY consumer prompt where no scenario tail can override.** The closer fires from `run-m3.sh` Phase C ([run-m3.sh:258](../../evals/mechanical/tmux-runner/run-m3.sh): `closer_body="$(resolve_prompt "$closer_key")"`), not from a scenario file. Sibling unsuffixed-path consumer (`author-test-strategy-skill-1`) gets its path overridden by scenario tails. The M3 closer cannot. Picoshare M3 2026-05-26: the agent self-corrected (*"No plain test-strategy/ dir, the picoshare one is test-strategy-picoshare. Since we're in the picoshare repo, that's the one. Reading it."*) and did the right work, but the recovery is model-quality-dependent. The closer's sentinel-only assertion in `run-m3.sh` would PASS even if the closer read the wrong file. **Accepted as-is for first cohort** (cohort students work on a single codebase per training; the unsuffixed path resolves cleanly for them, paired with the existing skill-name-collision watch above). **Fires if** (a) a returning / multi-SUT student has multiple `test-strategy-<sut>` skills installed and the closer reads the wrong one, OR (b) a weaker model doesn't self-correct the lookup and reports *"skill not found"* then no-ops the rewrite. **Fix when it fires:** reword `ae101-m3-sharpen-skill` body to *"Read the test-strategy skill we just wrote in the quality-side session (find it under `~/.claude/skills/`)"*, drop the hardcoded path so the body doesn't need an override (gated body edit §22/§26). Sibling fix in `run-m3.sh`: add a content assertion to the closer turn (currently sentinel-only), e.g. assert the skill file mtime advanced.
- **`[watch]` `~/.claude/projects/<encoded>/memory/` auto-memory leaks across runs and propagates a dangling reference into M1's student-facing artifact.** Picoshare full-chain test 2026-05-26 found 5 files in `~/.claude/projects/-Users-anttitevanlinna-Projects-picoshare/memory/` carried over from prior runs (`MEMORY.md`, `practice-arc-M1-M6.md`, `task-shape-plan-modeable-picoshare.md`, `second-pass-assumption-audit.md`, `verifier-shape-matches-failure.md`). SUT-side baseline reset (git reset --hard, drop branches/worktrees, snapshot user-level skills aside, clear `~/.claude/plans/`) does NOT touch the user-level auto-memory dir. Visible consequence: M1's `CLAUDE.local.md` gained an candid "Known gap" section about a dangling memory pointer (*"the five plan-mode-able rules referenced by memory `task-shape-plan-modeable-picoshare` are not recoverable from this tree"*), correct authorial behavior, but reveals the leak. **Accepted as-is for first cohort.** First-time cohort students have empty auto-memory, no leak. The harness branch is closed: `arrange-picoshare.sh` / `arrange-lemmings.sh` step 2b snapshots and clears the auto-memory dir for the SUT and both worktree cwds (chain scripts call arrange). **Fires if** (a) a returning cohort student (replay, second pass through a module) picks up prior-run auto-memory and the agent confabulates from it instead of self-correcting; or (b) an agents-101 alum brings their existing auto-memory into AE101 and it interferes with the M1 baseline. **Fix when it fires:** document explicit *"start with a clean `~/.claude/projects/<cwd>/memory/`"* instructions in the prework (gated body edit).

- **`[watch]` M2's plan file lives in `~/.claude/plans/` (laptop-local), not the repo, branch-sharing teammates can't pull it.** `push-back-on-the-plan-1`'s body says *"Write the plan to a plan file"*; the path is Claude Code's plan-mode default, recorded in the prompt's `produces:` frontmatter as `~/.claude/plans/<slug>-<adjective>-<noun>.md` (the `m2-…` literals live in the harness scenario tails, not the registry body). User-keyed, outside the repo, never pushed with the branch. Caught by the codesearch M6 arc-note 2026-05-26 (quote: *"There is no `--bind`/`/api/search` plan file. M2 survives only as retrospective prose inside `CLAUDE.local.md`."*) and confirmed again by the 2026-06-01 M6 arc-note (*"M2 isn't on disk. It survives only as references inside other files."*). **Accepted as-is for first cohort**, solo students don't share branches across people, so the laptop-local location is fine. **Fires if** a real cohort team shares branches across teammates and a downstream M3+ session can't find the plan the M2 session referenced (the M3 main-scenario tail already globs `~/.claude/plans/m2-<task-slug>-*.md`, but that only covers the same laptop, not a teammate's checkout). **Fix when it fires:** add a one-line callout in the M2 module body, *"the plan lives in `~/.claude/plans/`; if your team wants to share it, copy it into the repo"*, or change the prompt body to default to in-repo. Body edit on `push-back-on-the-plan-1` is gated (§22/§26).

- **`[watch]` `walk-and-send-off-3` option-cap juggling is a workaround for an AskUserQuestion tool limit, not a clean fix.** The 2026-05-27 arcticrex run hit invalid-params on the first AskUserQuestion in M4 (`walk-and-send-off-2`'s audit surfaced 5 gaps; the agent offered all 5 as options; the tool caps options at 4). Fixed by coaching the prompt to offer the four most material as options and, after the student picks, print the left-off gaps as a post-selection FYI. Smell: curriculum prose absorbing a tool constraint the agent should arguably respect on its own. **Accepted as the pragmatic fix for first cohort.** **Fires if** the tool's option cap changes (the "four" wording goes stale), OR the post-pick FYI list reads as noise / confuses a cohort student. Fix when it fires: revisit whether the audit upstream (`walk-and-send-off-2`) should cap its own gap count instead of pushing the cap onto the selection prompt.

- **`[watch]` `extract-the-task-shaping-rule.md` keeps the §16 `@import` ask in body, not fence.** The full disposition lives in the file's own maintainer block (standing watch, reaffirmed 2026-08-02): the wiring stays the student's choice. **The trigger is field evidence, not a verdict** — the pedagogy instance scores §16 REVISE non-blocking by design (class verdict PASS_WITH_TODOS; the maintainer block names that REVISE the expected steady state), so neither a green Quality block nor a fresh §16 REVISE says anything about this watch either way. Fires if a cohort run shows a student picking a non-auto-load path and missing the wire; fix then: fence it as a question in `extract-the-task-shaping-rule-2.md` (gated, `curriculum/prompts/*.md`).

- **`[watch]` `ae101-m3-sharpen-skill` rewrites the test-strategy SKILL.md in place with no pre-save checkpoint.** `check_prompts.md` §20 wants a grill beat before a downstream-consumed artefact is overwritten; this prompt shows before/after only AFTER the write, substituting a post-hoc *"read the sharpen with a skeptical eye"* section. The technical judge read it as probably-intentional (the skeptical-read section carries an over-flag / fresh-session escalation, and the write is a small in-place section edit, not a whole-file assembly). **Accepted as-is for first cohort.** **Fires if** a student's sharpen silently degrades a section M4/M5/M6 later depends on and the post-hoc read doesn't catch it. **Fix when it fires:** add a light pre-save beat, or record the accept-by-design in the prompt's frontmatter `note:` so the judge stops re-raising it (gated, `curriculum/prompts/*.md`).

- **`[watch]` M3 pre-read (Willison, *Designing agentic loops*, published 2025-09-30) is outside the 6-month window; stamp `due:2026-03-30`.** Maintainer grant, not rule-backed: the Theory-construct variant does not absorb it, and the same ruling set the ceiling — at best an M3 optional pre-read, nothing more (Antti, 2026-09-01; technique advice on a moving permission surface, essay-title coinage). M3-only in every cut; `plan-mode-done-right.md` owns the check, fallback *"drop the entry; the trifecta read carries the threat class on its own"* (`earn-the-trust.md`'s `## Prework` echo carries a delegated stamp and goes with it). **Accepted as-is for first cohort.** **Fires if** a cohort student reads the sandboxing advice as stale against a materially changed permission model. **Fix when it fires:** take the pre-authorized fallback, or re-anchor to a current piece on the same boundary question.
- **`[watch]` what Claude actually builds for a Ralph re-feed may be an in-session loop, which is the one shape the M5 slide rules out.** [what-packaging-is.md](../../lectures/what-packaging-is.md) § *Every re-feed pass starts a fresh session* teaches that each pass is a new session and that the conversation is what the loop throws away. Claude Code also ships `/loop <interval> <prompt>`, which is explicitly session-scoped (`check_platform_and_boundaries.md` §6, re-verified 2026-07-31), and `diagnose-and-resend-4` asks Claude to build whichever verifier shape the student names. Ask an agent inside a session for a re-feed and the nearest primitive it holds is the in-session one. If it reaches for that, the student's artefact contradicts the slide and quietly becomes the retry loop the slide exists to rule out. **Accepted as-is for first cohort.** **Fires if** any cohort or harness run picks Ralph re-feed at `diagnose-and-resend-4` and Claude builds an in-session loop rather than repeated fresh invocations. **Fix when it fires:** name the distinction in the slide's third bullet, or have the prompt ask for a fresh-invocation loop by name. **Cheap probe before that:** run the prompt once with `Ralph re-feed` chosen and read what lands.

- **`[watch]` M4's Big Idea reads prep-register against a curious-readiness mood.** The `strategy` judge files `check_strategy_tie_in.md` §1 on *"Prep your first long session"* leading [run-the-first-experiment.md](run-the-first-experiment.md); the `story` judge scores the same file PASS on persona traces. **Accepted as-is for first cohort** — the second sentence (*"The session is how you find out what's missing"*) carries the experiment framing, and the maintainer block's mood target already ships the trainer reframe for the compliance drift. **Fires if** a cohort's Phase 2 mood reads below 7 and the room's *"is this enough?"* pattern traces back to the opening slide rather than to task selection. **Fix when it fires:** lead the Big Idea on the experiment and demote the prep clause.

## Non-blocking findings banked from judge passes

TODOs judges logged alongside a passing verdict. None blocks a cohort, and none is owed a decision on any schedule — they are here so a passing verdict does not swallow the observation that came with it. Each carries its own date where the date matters; the section does not, because it takes deposits from every pass.

**Bank a TODO here, never a REVISE.** A judge that reports REVISE to make a non-blocking observation visible converts an observation into a gate, and a gate costs a maintainer decision that was never owed. The `eval-sweep` workflow's schema carries `PASS_WITH_TODOS` for exactly this, and its `todos` array lands here.

- **Wave-9 todo harvest, 117 entries across 47 files (2026-08-29).** All recorded in the committed instance JSONs beside their PASS verdicts. Dominant shapes: §20 numerals sitting beside the lists they count, §3 changelog-flavoured maintainer notes ("X was changed to Y"), and a scatter of §21/§22 wording nits. Line-read material for a future cut pass, not a sweep — the instances are the ledger, this line is the pointer.
- **M6 round-2 todo harvest, 21 entries across 8 files (2026-09-03).** Recorded in the instance JSONs beside their verdicts; the Quality rows cite them. Dominant shapes: act-verb *say …* lead-ins around quoted prompts (`check_student_facing.md` §31, stack exercise, two lines); the three stack-scan fences with no *No preamble* line (`check_prompts.md` §17, filed against the wording before the 2026-09-03 rewrite); a numeral sitting beside its own list (`check_writing.md` §20, Composing); doctrine restated across adjacent slides (`check_slides.md` §16, stack exercise); the handoff fence packing five moves into one comma-run and carrying no grill-before-save line (`check_prompts.md` §36 / §20). Student-facing, so each is a card if it moves; line-read material for a cut pass, not a sweep.

- **`[watch]` The M4→M5 pre-read load is excessive: five entries at ~64–69 min.** `run-the-first-experiment.md § Pre-reads before Module 5`, second instance of the shape `getting-going.md` set at 60 min across four. Order ruled right and the lede stands as-is (Antti, 2026-09-01; lede card declined, do not re-propose). The walk-away premise is refuted by the same ruling: students are not expected to read during the run, and the 2-day format's gap is lunch, so the load carries no dead-time justification. `learn-from-the-test.md § Prework`'s *"Optional prep while it's still running"* clause is the remaining trace of the refuted premise (gated body surface). **Accepted as-is for first cohort.** **Fires if** a cohort's M5 opens with students who read nothing, or a 2-day delivery leaves the list visibly unread. **Fix when it fires:** cut entries (maintainer call on which; Mollick stays first), and take the *"still running"* clause out of `learn-from-the-test.md § Prework` in the same pass (gated body edit).
- **Two backing stamps whose docs pages moved under them (2026-08-24, technical judge, live re-fetch).** `where-the-rule-could-live.md:68`'s cc-routines stamp still carries per-tier daily Routines caps that the canonical page no longer publishes; the gating criterion and the 1hr-cloud/1min-local interval are unchanged and still right. `where-the-rule-could-live.md:67`'s cc-scheduled-tasks stamp predates a substantial rewrite — Channels event-push, a `/goal` command, Cron tools, a self-paced dynamic `/loop`, and `--resume` restoring an unexpired recurring task, which softens the backing block's *"dies with the session"* framing without invalidating it. Backing-block only, `due:cohort` not yet due. The pre-cohort freshness pass should re-derive the cap figures rather than inherit them; the Channels shape is a candidate for the Stance section's open fourth-shape question.

- **`[watch]` `diagnose-and-resend-2`'s starting-point commit match has no multi-result fallback (2026-08-27, writing judge, `check_prompts.md` §33).** P2 finds the M4 starting point by matching commit-message text. One marker commit per worktree fork makes real collision unlikely, which is why this has survived several passes, but the rule's default AE101 landing — a body callout naming what to do on multiple hits — is absent. **Fires if** any run surfaces two commits matching the marker text and the agent picks silently. **Fix when it fires:** body callout, or narrow the match to the branch's first commit.

---

**Canonical home:** this file. Module files carry zero TODOs, all pre-cohort open items route here.

**When an item closes:** delete the bullet. Git log carries the history. Don't annotate "done", closed = gone.

**When a new item surfaces mid-session:** add it here, not in the module file.

## Simulation caches that no longer describe their file (2026-08-20)

`node curriculum/evals/scripts/sim-freshness.js --training ae101` is the reader; add `--mood` for
the score half, `--gate` to make either exit 1. Nothing here blocks a cohort: every pin is honestly
earned, because a judge really did read the body at the time. The exposure is forward. A judge
re-fired tomorrow reads the cache before the file, so a trace describing prose that has since
changed becomes invented evidence rather than missing evidence, which is the failure
`memory/compounded/2026-08-19-platform-a-stale-cache-fabricates-evidence-not-just-staleness.md`
records.

**The script owns the counts; this section owns the shapes.** Numbers written down here go stale
the next time anyone edits a body, and a stale number in a punch list reads as a measurement.

- **Unanchored.** The trace's `content_sha` matches no committed version of its file, raw or
  `expand-md`-expanded. Either it hashed a working-tree state that was never committed on its own,
  or the hash was not computed at all — a large minority carry 16-hex strings where sha256 is 64.
  These cannot be aged, only regenerated.
- **Body-moved.** Student-facing prose changed under the trace. The long-standing worst two are
  `the-far-half` and `the-loop-half-filled`, and they have held that position
  across every run since this section was written.
- **Persona traces carrying no mood score at all.** Three are exercises, where
  `simulation.md` §When makes the persona run required: `fork-the-worktree`,
  `open-the-side-quest`, `orient-and-introspect`. The rest are lectures and modules, where it is
  optional. Triage, not a failure list. Note the `--mood` half of the reader does **not** honour
  `--training`, so its no-score list carries other trainings' rows too. One named regeneration
  candidate: `reading-the-return`'s persona trace, whose `artifact_state` phrase overstates the
  flow into `diagnose-and-resend.md` Phase 1 (a lecture and an exercise sit between) — carry until
  the next persona regeneration; hand-editing recorded sim output would fabricate evidence.

Sequencing note: a trace regenerated before a stamp goes stale again the moment
`update-quality.sh` writes the Quality line, so regenerate as part of a judged pass, not as a
standalone sweep.

## A second tab that lands on the right slide

The want: a student working an exercise opens another exercise's instructions in a second browser tab and keeps it open beside their Claude session. Maintainer's framing: *"new tab is essential to allow for jumping between claude sessions and instructions. Browser back does not help in that."*

- **`[watch]` Exercise links still open in the same tab.** The deck now honours the arrival fragment (`startAnchor`, first mount only, tested in `scripts/slides.test.js` and in headless Chrome against the acme build: `#exercises-author-test-strategy-skill` → `M3 — 162 / 527`, a dead fragment → cover, a later long-read → Slides toggle → cover). A student who opens an exercise link in a new tab by hand lands on the right slide in either layout. What is still open is whether exercise links should spawn the tab themselves. The category policy (every `#exercises-*` link → new tab) turns a dead anchor into an 870 KB reload onto the cover on slides a trainer projects, and nothing holds every tracked build's anchors resolved: `check-include-anchors.js` reads source, and a built page is invisible to it. **Fires if** a student follows an exercise link, stays in the same tab, and loses their place. **Fix when it fires:** the per-link author mark, not the category policy; the category version owes a resolve-check over `site/clients/**/index.html` first.

## Firing review — 2026-09-19

`scripts/fire-log.sh` appends one line per repo script, npm target, and skill invocation to `.fire-log` at the repo root. It is a PostToolUse hook on Bash and Skill, registered in `.claude/settings.json`; both the script and the registration are tracked, so it travels with a clone. The log is gitignored and starts empty on each machine; this one started 2026-08-19.

- **Read the firing log on 2026-09-19 and rule on what has never fired.** `cut -f2 .fire-log | sort | uniq -c | sort -rn` for what runs; the `comm` line in `scripts/README.md` § *What Actually Fires* for what does not. Four names go into that review already carrying a question. `audit-training-artifact-contracts.js` is a 17-line shim that spawns the 759-line `audit-ae101-artifact-contracts.js`, so one of the two names should own the implementation. `lint-prompt-bodies.js` reports a Sev-2 and exits 0, which makes it a reporter wearing a linter's name. `render-variants.js` hardcodes the full and preview module slug lists, so it goes quietly wrong the first time the arc changes. `generate-cover.js` is reachable through `npm run cover` and has produced one cover. **Never-fired is three diagnoses** — a product nobody was told to invoke, a trigger that broke, or dead weight — and one machine's sessions over one month is evidence for the third only once the first two are ruled out by hand. **Fires on** 2026-09-19, or earlier if the cohort build needs a decision on any of the four.

- **`[parked]` Unknown unknowns + long sessions — Antti's topic, opens when the M2 line-by-line read finishes.** Parked 2026-08-12 mid-M2-read. Nothing has been said about the content — do not reconstruct or pre-answer it; raise the topic and let him state it. Likely surfaces: M4 `run-the-first-experiment` (first multi-hour send-off, far-half lectures) and M5 `learn-from-the-test` (reading the return through three failure-mode lenses).
