# Actor — AE101 M3 chain (map → STRIDE → author skill)

**Dispatch with `model: "haiku"`.** Acceptance-test actor. Run the 11-prompt chain across three exercises end-to-end. Stubs are fine; the Judge does not grade content quality.

You are simulating Claude Code in a fresh M3 session at the student's repo, after M1+M2 have shipped (so `CLAUDE.local.md` carries earlier rules and the two skills are installed personally).

**Working directory:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/ae101-m3/repo`

**Sandbox bypass:** use `dangerouslyDisableSandbox: true` on every Bash call that mutates the scratch tree.

## Critical protocol

Student is driving by pasting prompts verbatim from disk. You do **not** read `curriculum/exercises/*.md`, `curriculum/prompts/*`, judge runners, or `*.maintainer.md` documents. For each phase: **Read** the named `prompt-NNN.txt`, blockquote-paste, respond.

You will be dispatched once with all 11 phases stacked. Run them in order.

## Parsed prompts

- map-the-access-surface: `/tmp/prompts/map-the-access-surface/prompt-00{1..5}.txt`
- threat-model-with-stride: `/tmp/prompts/threat-model-with-stride/prompt-00{1..4}.txt`
- author-test-strategy-skill: `/tmp/prompts/author-test-strategy-skill/prompt-00{1,2}.txt`

## Arrange (do this before Phase 1)

The stage script (`bin/stage-ae101-m1.sh`) builds the scratch shape but does NOT write M1/M2 carry-over rules. Write a stub `<scratch>/repo/CLAUDE.local.md` at the top of Phase 1 (before the prompt-001 Read) so it looks like M1+M2 ran:

```markdown
# Personal rules — AE101 session compound

1. Tests-first on bug fixes — write the failing test before the fix.
2. Push back on test naming — verb-led ("sum subtracts negatives"), not noun-led.
3. After the immediate fix, ask "root cause or surface?" before moving on.
4. Two-pass plan read — human push-back + agent second-pass; approve only after both.
5. Loop shape: orient → fix → compound → close (Klaassen, compound engineering).
```

Then ensure `<scratch>/repo/.gitignore` contains `CLAUDE.local.md` (Edit to append if missing). And ensure both stub skills exist at `<scratch>/.claude-user-stub/skills/access-control-analysis/SKILL.md` and `<scratch>/.claude-user-stub/skills/stride/SKILL.md` (they should — stage script extracts them; verify with `ls`).

## Substituted student inputs

Read **once** at session start: `/tmp/ae101-mocks/student-inputs.md`. M3-relevant sections:
- § M3 feature naming (Phase 2 — append after prompt-002's trailing colon)
- § M3 access-surface delta — surface called out harder (Phase 4 — append after prompt-004's trailing colon)
- § M3 access-surface delta — surface skill missed (Phase 5 — append after prompt-005's trailing colon)
- § M3 STRIDE pick steer (Phase 7 — paste after Claude's incident-story proposal as the steer)
- § M3 ADR convention (Phase 8 — paste if Claude asks about ADR convention)
- § M3 author-test-strategy answers A1–A7 (Phase 10 — one per Claude question)
- § M3 invoke-and-grade verdict (Phase 11 — paste after Claude's self-grade)

## Substitution table (M3-specific)

| Real-world surface | Scratch substitution | Why |
|---|---|---|
| `/rename m3-access-surface` (slash command top of source) | Log `[harness substitution — /rename]` once | No slash-command surface |
| `/skills` slash command (Phase 1) | `ls <scratch>/.claude-user-stub/skills/` and list what's there as if `/skills` returned it; log `[harness substitution — /skills]` once | No slash-command surface |
| Dispatch skill as subagent (Agent tool) — both `access-control-analysis` and `stride` | Read the relevant SKILL.md from `<scratch>/.claude-user-stub/skills/<name>/SKILL.md` once, then run the skill's intent INLINE (don't dispatch a sub-Agent). Log `[harness substitution — skill-as-subagent → inline]` once per skill | Nested Agent-in-Agent unavailable in current subagent runtime |
| `~/.claude/skills/test-strategy/` (Phase 10 write target) | Write to `<scratch>/.claude-user-stub/skills/test-strategy/SKILL.md` | Subagent sandbox + persistence stub |
| ADR home `docs/adr/` (Phase 8) | Write to `<scratch>/repo/docs/adr/0001-csv-injection-on-reason-field.md` (create dir if needed) | None — real path in scratch repo |
| Skill output temp dir (Phase 2 + Phase 6) | Use `<scratch>/m3-temp/` (create at top of Phase 2); write `surface-map.md` and `threat-list.md` there | Real path |

Log every substitution once at the top of its first-affected phase.

## Phases (run in order)

### Phase 1 — list-installed-skills

**Exercise:** map-the-access-surface
**Reads:** prompt-001.txt

Arrange step first: write the stub `CLAUDE.local.md` + verify skills exist (per "Arrange" section above). Log `[harness substitution — /rename]` and `[harness substitution — /skills]`.

Read prompt-001.txt verbatim, blockquote-paste. Respond by:

1. `ls <scratch>/.claude-user-stub/skills/` — list installed skills.
2. Report each: name, storage path (absolute), loaded-on-context state (Claude Code skills lazy-load on invocation; only metadata is in context cold).
3. 5 lines max. Stubby is fine.

### Phase 2 — invoke-access-skill-on-feature

**Exercise:** map-the-access-surface
**Reads:** prompt-002.txt

Read prompt-002.txt verbatim, blockquote-paste. Prompt ends with `"The feature is:"` and a colon — append `student-inputs.md § M3 feature naming` after the colon.

Log `[harness substitution — skill-as-subagent → inline]`. Read `<scratch>/.claude-user-stub/skills/access-control-analysis/SKILL.md` once.

Then "run the skill inline" — produce a stub surface map. Save to `<scratch>/m3-temp/surface-map.md`. Body shape (the Judge checks for these section headers):

```markdown
# Access surface map — daily-totals reporting module

## User boundaries
- (stubby finding)

## Trust boundaries
- (stubby finding)

## Data flows
- (stubby finding)

## Tool / connector calls
- (stubby finding)

## External integration points
- (stubby finding)

## Authorization checkpoints
- (stubby finding)
```

Report the temp path back to the student.

### Phase 3 — walk-the-map

**Exercise:** map-the-access-surface
**Reads:** prompt-003.txt

Read prompt-003.txt verbatim, blockquote-paste. Read `<scratch>/m3-temp/surface-map.md` and walk it in chat: categories found, 2–3 standout findings, any ambiguous spots. 4–6 lines. Stubby.

### Phase 4 — integrate-skill-called-harder

**Exercise:** map-the-access-surface
**Reads:** prompt-004.txt

Read prompt-004.txt verbatim, blockquote-paste. Prompt ends with `"The surface the skill called out harder than I would have:"` and a colon — append `student-inputs.md § M3 access-surface delta — surface called out harder` after the colon.

Edit `<scratch>/m3-temp/surface-map.md` to integrate the delta: pull the named surface (parseAdjustment input boundary) to the top of its category (likely "Data flows" or "Trust boundaries"). Add a one-line note on the deployment model. Show a diff in chat (stub `git diff`-style output is fine; use Bash diff against a backup or paste the changed lines).

### Phase 5 — add-skill-missed-surface

**Exercise:** map-the-access-surface
**Reads:** prompt-005.txt

Read prompt-005.txt verbatim, blockquote-paste. Prompt ends with `"The surface the skill missed that I know matters is:"` and a colon — append `student-inputs.md § M3 access-surface delta — surface skill missed` after the colon.

Append to `<scratch>/m3-temp/surface-map.md` a new section `## Codebase-specific (skill missed)` with the CSV-injection-on-reason-field surface. One sentence on what + one sentence on why (likely codebase-specific). Show a diff.

### Phase 6 — invoke-stride-subagent

**Exercise:** threat-model-with-stride
**Reads:** prompt-001.txt

Read prompt-001.txt verbatim, blockquote-paste.

Log `[harness substitution — skill-as-subagent → inline]` again (for the stride skill this time). Read `<scratch>/.claude-user-stub/skills/stride/SKILL.md`.

"Run the stride skill inline" against `<scratch>/m3-temp/surface-map.md`. Save the threat list to `<scratch>/m3-temp/threat-list.md`. Body shape (Judge checks for the six STRIDE category headers):

```markdown
# STRIDE threat list — daily-totals reporting module

## Spoofing
- (stubby threat) [severity: low]

## Tampering
- (stubby threat) [severity: medium]

## Repudiation
- (stubby threat) [severity: low]

## Information disclosure
- CSV-injection on reason field at serialisation — admin opening export in Excel triggers payload [severity: HIGH]

## Denial of service
- (stubby threat) [severity: low]

## Elevation of privilege
- (stubby threat) [severity: low]
```

Report subagent-loading state ("subagent loaded the skill cleanly; harness ran inline per substitution") and flag the HIGH entry in scrollback.

### Phase 7 — pick-the-stride-category

**Exercise:** threat-model-with-stride
**Reads:** prompt-002.txt

Read prompt-002.txt verbatim, blockquote-paste. Propose an incident story (the CSV-injection one fits the planted delta). Map it to STRIDE class (Information disclosure / I). Name the threat. Propose a hardening shape (encode/escape at serialisation).

Then paste `student-inputs.md § M3 STRIDE pick steer` as the student's confirmation/steer (it agrees with I and adds nuance).

### Phase 8 — write-adr

**Exercise:** threat-model-with-stride
**Reads:** prompt-003.txt

Read prompt-003.txt verbatim, blockquote-paste. If Claude (you) would ask about ADR convention, pre-empt by pasting `student-inputs.md § M3 ADR convention` as the student's answer.

Draft the ADR. Show it in chat, then write to `<scratch>/repo/docs/adr/0001-csv-injection-on-reason-field.md`. Create the `docs/adr/` directory if it doesn't exist (`mkdir -p`).

ADR body must contain the four sections (Judge checks each):

```markdown
# ADR 0001: Encode `reason` field at CSV serialisation boundary

## Context
The daily-totals export endpoint will serialise adjustment records to CSV...

## Decision
Encode the `reason` field at the serialisation boundary (escape commas, quotes, and newlines per RFC 4180)...

## Consequences
- Cost: small per-row encode pass at export time.
- Protects: against admin-opening-export-in-Excel triggering CSV-injection payloads...

## Alternatives considered
- Validate at parse time (rejected: warehouse upstream is the source of truth).
- Sanitise at storage (rejected: storage is just a Map<string, number>, untyped).
- Output a different format (JSON) (rejected: ops workflow uses Excel).
```

Stubby bodies are fine but the four section headers are mandatory.

### Phase 9 — adrs-auto-load

**Exercise:** threat-model-with-stride
**Reads:** prompt-004.txt

Read prompt-004.txt verbatim, blockquote-paste. Short prompt: *"Will ADRs auto-load to future sessions?"*

Respond 2–3 lines: no, ADRs at `docs/adr/` don't auto-load like `CLAUDE.md`/`CLAUDE.local.md` do. They're on-disk and discoverable but loaded only when explicitly Read or `@`-included.

### Phase 10 — author-test-strategy-skill

**Exercise:** author-test-strategy-skill
**Reads:** prompt-001.txt

Read prompt-001.txt verbatim, blockquote-paste.

**Hard rule — one question at a time.** Ask **7 questions in 7 distinct assistant turns** (Q1 → Q7). Do NOT batch. After each question, the orchestrator pastes `student-inputs.md § M3 author-test-strategy answers` A1 → A7 in order. After Q7's answer, write the skill to `<scratch>/.claude-user-stub/skills/test-strategy/SKILL.md`.

Skill SKILL.md must have:
- Frontmatter with `name: test-strategy` and `description: <stubby>`.
- Body section instructing how to author a test strategy for THIS codebase (Node `node --test`, no mocking, no integration yet, etc. — the answers fold in).

Show the skill in chat before saving.

### Phase 11 — invoke-and-grade

**Exercise:** author-test-strategy-skill
**Reads:** prompt-002.txt

Read prompt-002.txt verbatim, blockquote-paste. Run the just-authored skill inline on the feature. Produce a stubby test strategy (a few lines covering unit tests for the new helpers + a test case for the CSV-injection hardening from the ADR).

Then in the SAME response (per prompt's "in the same response" instruction), self-grade: name one weakness, one strength. Don't loop.

Paste `student-inputs.md § M3 invoke-and-grade verdict` as the student's TODO-at-top decision. Edit the SKILL.md to add a TODO line at the top: `<!-- v0.2: encode reviewer-checks per-team. -->`.

## Scratch end-state

- `<scratch>/repo/CLAUDE.local.md` — stub from Arrange step (5 rules carried from M1+M2).
- `<scratch>/.claude-user-stub/skills/access-control-analysis/SKILL.md` — unchanged (stub).
- `<scratch>/.claude-user-stub/skills/stride/SKILL.md` — unchanged (stub).
- `<scratch>/.claude-user-stub/skills/test-strategy/SKILL.md` — NEW, authored in Phase 10 + TODO from Phase 11.
- `<scratch>/m3-temp/surface-map.md` — NEW, 6 category sections + 1 codebase-specific section.
- `<scratch>/m3-temp/threat-list.md` — NEW, 6 STRIDE sections; one HIGH-flagged entry.
- `<scratch>/repo/docs/adr/0001-csv-injection-on-reason-field.md` — NEW, four-section ADR.

## Reports

**Scrollback** (append): `instances/ae101-m3-actor-scrollback.md`. One section per phase.

**Report:** `instances/ae101-m3-actor-report.md`:

```markdown
# Actor report — AE101 M3 chain

## Status
done | error

## Scratch
<absolute path>

## Phases executed
1–11 (one line each)

## Substitutions
- /rename + /skills slash commands
- skill-as-subagent → inline (access-control-analysis, stride)
- ~/.claude/skills/test-strategy/ → .claude-user-stub/skills/test-strategy/
- student-inputs.md provided: feature naming, two deltas, STRIDE steer, ADR convention, 7 author Q&A answers, invoke-and-grade verdict

## End-state
- 3 new files in m3-temp/ + docs/adr/
- test-strategy skill authored
- CLAUDE.local.md from Arrange step

## Notes
<tool denials, sandbox issues, anything weird>
```

Under 300 words. Do not grade yourself.

## What you must NOT do

- Read `curriculum/exercises/*.md`, `curriculum/prompts/*`, judge runners, `*.maintainer.md` docs.
- Try to dispatch a real subagent (Agent tool) for skill invocation. Run inline and log substitution.
- Batch the Phase 10 questions — seven distinct assistant turns is the discipline.
- Skip the `## Alternatives considered` section on the ADR.
- Generate long realistic skill bodies. Stubs are fine.
