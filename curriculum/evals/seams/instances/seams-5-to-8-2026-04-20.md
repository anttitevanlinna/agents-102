# Seam Judges 5-8 — 2026-04-20

Runner: Opus 4.7 (1M). Source-of-truth moods pulled from `curriculum/content-strategy.md` lines 348, 373, 395, 415, 436, 464.

## Summary

| Seam | Mood | Artifact | Vocabulary | Framing | Overall |
|------|------|----------|------------|---------|---------|
| M4 → M5 | PASS | FRAY | PASS | PASS | ARC HOLDS |
| M5 → M6 | PASS | PASS | PASS | PASS | ARC HOLDS |
| M6 → M7 | PASS | FRAY | FRAY | FRAY | FRAYS |
| M7 → M8 | FRAY | FRAY | PASS | FRAY | FRAYS |

---

## Seam 5: M4 → M5

### Mood handoff — PASS

M4 contract: *"Damn, this is complex stuff — and the practice is doing the work, not reaching certainty."* Unease stays.

M4 closes the Debrief with:
> "Name the residual in one sentence — the thing that's still true after you shipped the fix."
> "one door in your system you'd rather not open at all"

This sustains epistemic humility without guilt. The Bridge:
> "The agent is scoped, the residual risk is named. But the output *inside* the scope — can you trust what it actually says?"

Pivots cleanly from security-residual to output-quality without resolving the security unease. M5 contract: *"Ahh, this is actually fixable"* — rescue, earned.

M5 Connections:
> "In Module 3 the synthesized answer sat at an uneasy distance — you'd stake your reputation on some of it and not all of it, and you couldn't yet say which. Today that feeling gets a measurement."

M5 picks up the **M3** thread explicitly (as the content-strategy mandates) and lets M4's residual-risk mood stand untouched. This is exactly the designed handoff — M5 rescues *groundedness-for-this-shape-of-output* only, not security. Mood handoff is clean.

The maintainer note in M5 confirms intent: *"Do NOT resolve M3's strategic uncertainty or M4's security residual. Only groundedness-for-this-shape-of-output gets rescued."*

### Artifact handoff — FRAY

M4's stated artifact: `module-4/residual.md` (named residuals + one scoping rule).
M5 Materials line: *"the student's Module 3 synthesized answer and retrievals — no pre-built failing agent."*

**Gap 1:** M4 Homework line names "extract one micro-skill from your own reading of a company policy rule" and M4's maintainer note says *"Students bring this file to Module 5."* M5 Meta lists prework (two public-record cases) and the existing Module 3 artifact — but does NOT name or consume the micro-skill file. Either the micro-skill homework has no destination in M5 and should be moved, or M5 should acknowledge it (plug point? optional fifth detector?).

**Gap 2:** M4 produces `residual.md` but M5 never references it. Not necessarily wrong — M5 is scoped to groundedness, not security — but the "still-uncertain" theme in M5 is close enough that one sentence citing the residual file would pay off ("you already named what this system's security residual is; today we name what a judge can't reach").

### Vocabulary handoff — PASS

Terms M4 introduces, M5 uses cleanly or doesn't touch: *residual*, *"I can't tell"*, *scope/split/filter/review/gate*. M5 re-introduces its own vocabulary (bake-off, gold standard, meta-eval, precision/recall) fresh — appropriate, these are new. No leaks.

### Framing continuity — PASS

Bridge ("can you trust what it actually says?") → Connections ("In Module 3 the synthesized answer sat at an uneasy distance... today that feeling gets a measurement"). Cleanly handed off. Note: M5 Connections points back to M3 rather than M4 — this is correct per design (M3 planted the doubt M5 is rescuing; M4 planted a separate security doubt that stays un-rescued).

### Top 3 edits

1. **M4 (security.md):** Either remove the Homework micro-skill line, or add the Module 5 integration. Right now the file advertises *"Students bring this file to Module 5"* (maintainer line 89), but M5's prework and materials don't mention it. Drift.
2. **M5 (output-quality.md) Meta:** Add "homework micro-skill from Module 4 (optional fifth detector if policy-shaped)" to plug points, OR drop the M4 Homework line in M4.
3. **M5 Connections:** Consider one half-sentence honoring M4's residual alongside the M3 callback — *"Your M4 residuals stay named; today we measure what the system says inside the scope."* Currently M4 is invisible in M5's opening, which is a minor missed continuity beat.

---

## Seam 6: M5 → M6

### Mood handoff — PASS

M5 contract: mechanical rescue, not triumph. M5's Bridge is a textbook handoff:
> "You ran the bake-off once. Five claims, four detectors, one judge. Now imagine the gold standard has three hundred claims, the judge runs on every build, and its own corrections feed back into the next round. That's evals."

Hunger, not closure — exactly what the M5 maintainer note promises (*"Hand-off to M6 is hunger, not closure"*).

M6 contract: *"unleashed leverage — we can automate the loop."* M6 Connections:
> "In Module 5 you picked a winner out of four judges. You watched it work. You trusted it. Good. Today the question shifts. What if the judge kept getting better — not because you edited it, but because it watched itself miss things? What if *you weren't in the room* while that happened?"

Sharp pivot from *rescue* (fix-by-hand) to *leverage* (walk-away autonomy). The "you weren't in the room" phrasing is load-bearing — it names the mood shift explicitly. PASS.

### Artifact handoff — PASS

M5 produces: `judges/groundedness-judge.md` (bake-off winner, portable judge prompt, stated scope + known limit).
M6 Materials line: *"`judges/groundedness-judge.md` handoff from Module 5's bake-off winner."*

Path matches verbatim. M6 maintainer explicitly calls the dependency "hard" and names the facilitator line to prime it. Clean.

Also: M5's Homework produces `module-5/still-uncertain.md`; M6 implicitly carries this into the "what should be an eval that runs every time?" Debrief question. Not explicitly referenced — but M5's Homework says it's "carried into Module 6 prework," and M6 doesn't contradict. Minor: M6 Meta could name the file if it expects it.

### Vocabulary handoff — PASS

Terms M5 earns that M6 extends: *judge*, *gold standard*, *precision/recall*, *meta-eval*, *scoreboard*. M6 uses these without re-introducing them — correct compounding. New M6 terms (*orchestrator*, *parallel tracks*, *walk-away*, *convergence threshold*) are introduced fresh — appropriate.

### Framing continuity — PASS

The three-hundred-claims / every-build / corrections-feed-back triad in M5's Bridge maps 1:1 to M6's Connections question. The seam reads as a single continuous thought split across a night's sleep.

### Top 3 edits

1. **M6 (evaluations.md) Meta:** Name `module-5/still-uncertain.md` in prework/materials if it's consumed (M5 maintainer says it is).
2. **M6 Connections:** Consider including a one-line pointer to the judge file path *"that judge you saved at `judges/groundedness-judge.md` yesterday"* — would make the seam viscerally concrete.
3. **No structural edits.** This is the cleanest seam of the four.

---

## Seam 7: M6 → M7

### Mood handoff — PASS

M6 contract: leverage, empowered builder. M6 closes with Debrief Q3 ("What's Monday's first always-on eval?") and Bridge:
> "You just built an eval that improves itself. Who else on your team would want this?"

The Bridge explicitly seeds the generosity impulse the M7 contract demands (*"this is starting to work. I wonder if others could benefit?"*). Classic Sutherland move — plant the question, don't answer it.

M7 Connections:
> "Your memory has been working for you for a day and a half now. It produced something you trust enough to check before a meeting... The question that shows up on its own: *could this help someone else?* Hold that feeling. It's not a governance question yet. It's a generous one."

Generative, not compliance — right register. M7 explicitly disavows governance-flavor in the text. PASS on mood.

### Artifact handoff — FRAY

M6 produces: `module-6/eval-notes.md` (Monday's first loop line) and a self-improved judge (running infrastructure).

M7 Meta consumes... nothing from M6 specifically. Prework is the three-walls read + Rumelt + Martin + Klein. Materials reference `strategy/personal-to-team-patterns.md`.

**Gap:** M7's entire exercise is "share what you've built." But M7 never names *which* built artifact is the thing being shared. The student has accumulated: M1 site, M2 memory + scheduled agent, M3 multi-agent synthesis, M4 skills + residuals, M5 groundedness-judge, M6 eval-loop. M7 should say: *the candidate you're offering your teammate is one or more of those artifacts.* Right now M7's Connections says "your memory" generically — but the memory is M2's, and the sharing options span the whole stack.

**Gap 2:** M6's Bridge asks "who else on your team would want this [self-improving eval]?" — implying the M6 eval is the thing to share. But M7's exercise doesn't specifically scaffold sharing the eval — it's generic. Either (a) M6's Bridge should broaden ("who else would want what you've built?"), or (b) M7's Phase 1 should name the stack explicitly.

### Vocabulary handoff — FRAY

M7 introduces Jobs-to-be-Done / Christensen / Moesta / Ulwick / *outcome statement* / *firing the incumbent* / *Access-Trust Gap* / *pattern catalog* all at once in the lecture paragraph. That's fine for vocabulary M7 OWNS. But:

**Leak:** M7's key concept names *"the bounded-question primitive earns its keep here"* — "bounded-question primitive" was not introduced earlier. If this is a term, it should either be earned in M7 or referenced as a return. Currently unexplained.

**Forward-ref:** M7 prework names *crux*, *assumption-test*, *pre-mortem* by name (Rumelt, Martin, Klein). The M8 module still owns these skills (the `crux`/`assumption-test`/`pre-mortem` skills "ship in the Module 8 scaffold"). This is the unresolved "strategy skills may move to M7" question flagged in both files' tentative notes — the seam currently teaches the concepts in M7 prework *and* ships them in M8. Not a break (M2's debrief already inlined a crux move per CLAUDE.md curriculum rules), but the handoff needs a policy call: either move the skills to M7 scaffold, or keep M7 inlining the moves without named-invocation.

### Framing continuity — FRAY

M6 Bridge → M7 Connections works at mood level. But framing:
- M6 Bridge: "Who else on your team would want [this eval]?"
- M7 Connections: "Your memory has been working for you..."

M6 ends pointing at the eval; M7 opens pointing at the memory. The *subject of the sharing* drifts across the seam. Minor but real.

### Top 3 edits

1. **M7 (personal-to-team.md) Connections:** Open with explicit inventory — *"You've built a memory, a scheduled agent, a synthesis system, skills, a groundedness-judge, a self-improving eval. Pick the one you most want someone else to have."* Makes the candidate concrete instead of defaulting to "the memory."
2. **M6 Bridge:** Broaden from *"who else would want this [eval]?"* to *"who else on your team would want any of this?"* to match the M7 opening scope.
3. **M7 key concepts:** Either define "bounded-question primitive" in the lecture paragraph or remove the term. Currently orphaned.
4. **Settle the strategy-skills-move question** (tentative per both module files). Either:
   - Move `crux`/`assumption-test`/`pre-mortem` skills into M7 scaffold (and update M8 to invoke them as already-shipped); OR
   - Keep them in M8 scaffold and have M7 inline the moves by hand (as M2 does with crux).
   Drift across two modules is worse than either choice.

---

## Seam 8: M7 → M8

### Mood handoff — FRAY

M7 contract: generous impulse, "others could benefit." M7 closes with Debrief Q3 ("Monday morning, first move — what's the very first thing, before meetings, before email? One sentence.") and Bridge:
> "You've built a system. You've secured it, evaluated it, and designed the sharing. What if you didn't have to build each new agent by hand?"

This pivots from generosity to *flywheel* — the agent-building-agent move. Good in principle. But the Bridge phrasing is operational ("build each new agent by hand") rather than awe-seeding. Contrast with M5→M6 Bridge's "three hundred claims, every build, corrections feed back" — that crackles. M7→M8 Bridge is procedural.

M8 contract: *"awe and curiosity... oh shit, where is this all going? ... forward hunger, not closure."*

M8 has no Connections section. None. The file jumps from Meta → Lectures → Exercises.

**This is a BREAK at the opening framing level, partially rescued by mood elsewhere.** The content-strategy mood note for M8 explicitly warns: *"Any edit that makes M8 feel like a tidy graduation ceremony steals this."*

The M8 file contains:
- A tentative live-deliberation section flagged as *not committed* — which is honest maintainer-space but reads to a live-course student as uncertainty rather than awe.
- An "Identity-naming close" with the sponsor naming students as "agent builders" — which is a **graduation ceremony in all but name.** *"You are now agent builders... That's what you carry out of this room."* is exactly the sealed-ending mood the contract forbids.
- The Bridge: *"Bootstrap is Step 1 of 5 — Make Your Own is next."* — this IS forward-hunger and partially rescues. Good. But it competes with the identity-naming move which is closure-flavored.

Verdict: mood is mixed. The forward-hunger is present but in tension with the identity conferral.

### Artifact handoff — FRAY

M7 produces (per its Debrief): `module-7/` files (sharing-problem, crux, branch, technical-plan, people-plan, assumptions, premortem, monday).

M8 Meta and Exercises never reference any of these. The "Extend your system" exercise picks up the accumulated stack conceptually but not by file path. The Joint Double Diamond references "everyone's agents run on shared context" — the agents themselves carry M1-M7 residue implicitly.

**Gap:** M7's pre-mortem, assumption-test, and crux artifacts would be natural feed-ins to M8's three-skill Double Diamond (since M8 applies the SAME three disciplines). Currently the seam duplicates the moves rather than compounds them. If the student did crux + assumption-test + pre-mortem on their sharing problem in M7, the M8 exercise should either (a) acknowledge it (*"you did this yesterday on a small problem; today the whole room does it on a strategic one"*) or (b) differentiate explicitly.

Per the open tentative note, this is the same strategy-skills question from Seam 7 — it rolls forward into Seam 8.

### Vocabulary handoff — PASS

Terms M7 introduces that M8 extends: *crux*, *assumption-test*, *pre-mortem*, *outcome statement*, *three walls*. M8 uses *crux*, *assumption-test*, *pre-mortem* as named skills — consistent. M8's *"three predictable walls after Bootstrap: data access, runtime platform, discoverability"* overlaps with M7's prework *"absorption bottleneck, Access-Trust Gap, discoverability."* These are adjacent but not identical vocabularies (M7 has absorption + access-trust; M8 has data-access + runtime + discoverability). Not a break — both are three-wall frames — but a future sweep should reconcile.

### Framing continuity — FRAY

M8 has no Connections section. The absence is itself the finding. Without one, there is no opening framing for M8, so the Bridge-to-Connections test cannot be scored. The file jumps from Meta to Lectures. Per the Module File Shape in `curriculum/CLAUDE.md`, Connections is part of the required module shape.

### Top 3 edits

1. **M8 (agents-building-agents.md) — add Connections.** Required per module shape. Should pick up M7's Bridge and seed awe. Draft: *"You've each built agents for one part of your work. You've designed how to share them. What happens when one of those agents writes the next one — and the room's agents argue about what to build next?"*
2. **M8 Identity-naming close — reshape or cut.** As written, *"You are now agent builders. That's what you carry out of this room"* is graduation-ceremony flavored. Contract forbids it. Options: (a) keep sponsor naming but land it BEFORE the forward-hunger Bridge, not AS the climax; (b) replace "carry out of this room" with "carry into Monday — and what you can't yet see is how fast it compounds."
3. **M7→M8 seam on the three skills.** Resolve the tentative note: if `crux`/`assumption-test`/`pre-mortem` carry from M7 (student already ran them on sharing problem), M8's Joint Double Diamond should explicitly compound — *"yesterday you ran these three on your own sharing problem; today the whole room runs them on company strategy."* If they're M8-first, M7 shouldn't namedrop Rumelt/Martin/Klein in prework.
4. **M8 Bridge** is strong on forward-hunger ("Step 1 of 5"). Keep.

---

## Cross-seam observations

1. **Seam 6 (M5→M6) is the gold standard.** Both modules name the seam file (`groundedness-judge.md`), match the path, and the Bridge question pre-answers itself in the next Connections. Other seams could be written to this spec.

2. **The strategy-skills-move question (M7 vs. M8 ownership of crux/assumption-test/pre-mortem) frays two seams.** Both M7 and M8 carry maintainer notes acknowledging the tension; neither has resolved it. This is one decision that fixes both seams. Leaving it tentative in both files means the curriculum currently *teaches the same three disciplines twice* without acknowledging the repetition — which steals M8's teaching moment in the same way M3 resolving prematurely would steal M5's.

3. **Module 8 is structurally incomplete relative to modules 1-7.** No Connections section; a "tentative" block occupies roughly a quarter of the file; identity-naming close competes with the forward-hunger Bridge. M8 needs a Pass 2 before it can hold the arc's designed peak. The mood contract is the most explicit of any module (awe, forward hunger, NOT graduation) and the file is the one most likely to land as graduation ceremony.

4. **Artifact paths converge for M5→M6, diverge elsewhere.** The pattern that works: M_n Debrief names a file path → M_(n+1) Meta "Materials" line cites that path verbatim. Apply this as a mechanical fix across seams 5, 7, 8: every Debrief artifact that feeds forward should appear by path in the next module's Materials.

5. **The arc's mood architecture is working** — nowhere does a module resolve a mood it was meant to sustain. M4 keeps unease, M5 rescues only groundedness (not security or strategic uncertainty), M6 leverages, M7 is generative not compliance-flavored. The frays are at the joinery layer (file paths, vocabulary, Connections sections), not at the emotional register. Which is the easier class of fix.
