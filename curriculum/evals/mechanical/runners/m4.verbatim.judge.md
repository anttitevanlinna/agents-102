# Judge — M4 walk-and-send-off verbatim

**Dispatch with `model: "haiku"`.** This is an acceptance-test judge — script-first, no taste judgements. Content quality belongs to the eval system, not here.

You are grading whether M4's walk-and-send-off ran on a scratch inherited from M3 and whether file artefacts have the expected SHAPE. The send-off is multi-hour and truncated — you grade pre-send-off readiness only. You are NOT grading whether memory entries are insightful, fills are codebase-specific, or the Huryn quoting is graceful.

## Inputs

- **Scratch:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m4`
- **Actor transcript:** `<ACTOR_TRANSCRIPT_PATH>`
- **Actor report:** `curriculum/evals/mechanical/instances/m4-verbatim-actor-report.md`
- **Actor scrollback:** `curriculum/evals/mechanical/instances/m4-verbatim-actor-scrollback.md`
- **Prompt files:** `/tmp/prompts/walk-and-send-off/prompt-00{1,2,3}.txt`
- **Send-off truncation log:** `curriculum/evals/mechanical/instances/m4-send-off-prompt.md`

## Tooling

- `curriculum/evals/mechanical/bin/verbatim-check.sh <prompt> <scrollback>` — V-checks
- `curriculum/evals/mechanical/bin/prompt-source-audit.sh <slug>` — P/E checks
- `jq` on `.jsonl` for transcript inspection

## Method

For every assertion: run the named script, capture exit code + first line of output. No quoting, no narration, no taste calls. If you find yourself reading scrollback to judge whether content is "good," stop — that's not your job.

## Assertions

### Verbatim round-trip (V1–V3)

For each `prompt-00N.txt`: `verbatim-check.sh <prompt> <scrollback>`. Report exit code + prefix.

### Phase 2 — audit + fill

- **A3.** Actor dispatched a Task subagent for audit OR noted inline substitution: `jq` on transcript for `tool_use.name == "Task"` between Prompt 2 and Prompt 3 paste markers, OR grep scrollback for "Task tool unavailable".
- **A6.** ≥ 1 file under `<scratch>/.claude/memory/`: `find <scratch>/.claude/memory -type f | wc -l` ≥ 1.
- **A7.** `<scratch>/CLAUDE.local.md` exists: `test -f`.
- **A8.** `<scratch>/.gitignore` covers `CLAUDE.local.md` AND `.claude/memory/`: `grep -E 'CLAUDE.local.md' <gitignore>` AND `grep -E '.claude/memory' <gitignore>`.

### Send-off truncation

- **A13.** `curriculum/evals/mechanical/instances/m4-send-off-prompt.md` exists: `test -f`.
- **A14.** No multi-hour send-off executed: `<scratch>/src/` does NOT contain new login UX files. `find <scratch>/src -name 'login*' -o -name 'auth*' -newer <scratch>/.git/HEAD` → empty.

### Continuation between phases (cross-module)

- **A9.** Phase 3 response Reads at least one of: ADR, SKILL.md (in `~/.claude/skills/test-strategy/`), or memory file written in Phase 2. `jq` Read tool calls between Prompt 3 paste and end of scrollback.
- **A18.** Cross-module artefact contract: `<scratch>/docs/adr/0001-strip-email-from-leaderboard.md` exists (inherited from M3): `test -f`.

### Harness leakage (H1–H5)

- **H1.** No Read of `*.maintainer.md`: `grep -o '"file_path":"[^"]*"' <jsonl> | grep maintainer` → empty.
- **H2.** No Read of judge or sibling actor runner (own `m4.verbatim.actor.md` allowed): grep filter as M3 judge.
- **H3.** No Read under `curriculum/exercises/`: grep filter.
- **H4.** Commit messages neutral: `git -C <scratch> log --oneline | grep -iE 'planted|harness'` → empty.
- **H5.** No harness-internal files inside `<scratch>` re-Read.

### Prompt-source audit

`bin/prompt-source-audit.sh walk-and-send-off`. Capture exit code + verdict. PASS if `READY` or `READY-WITH-FLAGS`; FAIL if `BLOCK`.

## Report

Write `curriculum/evals/mechanical/instances/m4-verbatim-judge-report.md`:

```markdown
# Judge report — M4 walk-and-send-off verbatim

## Summary
Verdict: PASS | FAIL (N/M). Sev-1 from prompt-source-audit: K.

## V1–V3
V1: PASS — <prefix>
...

## A-series
A3: PASS — <evidence>
...

## H-series
H1: PASS — <evidence>
...

## Prompt-source audit
walk-and-send-off: <verdict>
```

Under 500 words. Leave artifacts in place.

## What you must NOT do

- Quote a memory entry and judge if it's "anchored to a specific moment."
- Decide whether the ranked top-5 is "well-prioritized."
- Decide whether the Huryn frame was "named after quoting" vs "named first."
- Read source files to evaluate fill content.

If an assertion can't be reduced to a script call or a `jq`/`grep` one-liner, drop it and flag the gap — that's a script-ratchet TODO, not a Judge job.
