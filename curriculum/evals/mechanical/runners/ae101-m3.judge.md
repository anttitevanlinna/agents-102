# Judge — AE101 M3 chain (map → STRIDE → author skill)

**Dispatch with `model: "haiku"`.** Script-first acceptance-test judge. No taste calls.

Grading whether the M3 chain ran end-to-end and the file artefacts have the expected SHAPE. NOT grading whether the surface map's findings are sharp, whether the ADR's reasoning is sound, or whether the authored test-strategy skill is good.

**Discipline rule (rule #20):** PASS on exit 0; FAIL on non-zero. No re-derivation.

## Inputs

- **Scratch:** `<scratch> = scratch/ae101-m3`; scratch repo: `<scratch>/repo`
- **Transcript:** `<ACTOR_TRANSCRIPT_PATH>`
- **Scrollback:** `instances/ae101-m3-actor-scrollback.md`

## Tooling

- `bin/prompt-read-check.sh`, `bin/prompt-source-audit.sh`, `bin/runner-mapping-check.sh`
- `jq`, `grep`, `test -f`, `awk`

## Assertions

### V-series — 11 prompts Read in transcript

V1–V5 map-the-access-surface/prompt-00{1..5}; V6–V9 threat-model-with-stride/prompt-00{1..4}; V10–V11 author-test-strategy-skill/prompt-00{1,2}.

### A-series — orient + skills listing

- **A1.** `/skills` substitution logged. `grep -cE 'harness substitution.*skills' <scrollback>` ≥ 1.
- **A2.** Phase 1 lists ≥2 skills. `awk '/Phase 1/,/Phase 2/' <scrollback> | grep -cE 'access-control-analysis|stride'` ≥ 2.

### M-series — surface map artefacts

- **M1.** Surface-map file exists: `test -f <scratch>/m3-temp/surface-map.md`.
- **M2.** Six base category headers present. Each of `User boundaries`, `Trust boundaries`, `Data flows`, `Tool / connector calls`, `External integration points`, `Authorization checkpoints` appears as a `^## ` heading. `grep -cE '^## (User boundaries|Trust boundaries|Data flows|Tool|External integration|Authorization)' <surface-map>` ≥ 5 (`Tool /` may render differently — minimum 5/6 to allow stub variance).
- **M3.** Phase 4 integration evident — `parseAdjustment` mentioned in the map. `grep -ciE 'parseAdjustment|adjustments|input boundary' <surface-map>` ≥ 1.
- **M4.** Phase 5 codebase-specific section appended — `## Codebase-specific` header (or close variant) + `reason` field mention. `grep -cE '^## Codebase-specific|reason field|CSV-injection' <surface-map>` ≥ 1.

### T-series — STRIDE threat-list + ADR

- **T1.** Threat list file exists: `test -f <scratch>/m3-temp/threat-list.md`.
- **T2.** Six STRIDE category headers present. Each of `Spoofing`, `Tampering`, `Repudiation`, `Information disclosure`, `Denial of service`, `Elevation of privilege` appears. `grep -cE '^## (Spoofing|Tampering|Repudiation|Information disclosure|Denial of service|Elevation of privilege)' <threat-list>` = 6.
- **T3.** At least one HIGH-flagged entry. `grep -icE 'HIGH' <threat-list>` ≥ 1.
- **T4.** ADR file exists: `test -f <scratch>/repo/docs/adr/0001-*.md` (glob; use `ls`).
- **T5.** ADR has four section headers. `grep -cE '^## (Context|Decision|Consequences|Alternatives considered)' <adr>` = 4.
- **T6.** STRIDE skill substitution logged. `grep -cE 'harness substitution.*skill-as-subagent|skill-as-subagent.*inline' <scrollback>` ≥ 1.

### S-series — authored test-strategy skill

- **S1.** Skill file exists: `test -f <scratch>/.claude-user-stub/skills/test-strategy/SKILL.md`.
- **S2.** Skill has frontmatter with `name: test-strategy`. `grep -cE '^name: test-strategy' <skill>` ≥ 1.
- **S3.** Skill mentions `node --test` (codebase convention from A1). `grep -ciE 'node --test|node:test' <skill>` ≥ 1.
- **S4.** Phase 10 walked 7 questions one-at-a-time. `awk '/Phase 10/,/Phase 11/' <scrollback> | grep -ciE '^Q[1-7]|Question [1-7]|^### Q'` ≥ 7.
- **S5.** Phase 11 has self-grade section. `awk '/Phase 11/,EOF' <scrollback> | grep -ciE 'is it good|weakness|strength|generic|covers the hardening'` ≥ 1.
- **S6.** Skill has TODO at top after Phase 11. `head -3 <skill> | grep -cE '<!-- v0.2|TODO'` ≥ 1.

### C-series — carry-over arrange state

- **C1.** `CLAUDE.local.md` written by Arrange. `test -f <scratch>/repo/CLAUDE.local.md`.
- **C2.** `.gitignore` contains `CLAUDE.local.md`. `grep -c 'CLAUDE\.local\.md' <scratch>/repo/.gitignore` ≥ 1.

### H-series — harness leakage

- **H1.** No Read of `curriculum/exercises/`. 0 expected.
- **H2.** No Read of `curriculum/prompts/`. 0 expected.
- **H3.** No Read of judge runner or `*.maintainer.md`. 0 expected.
- **H4.** No WebFetch. 0 expected.

### Pre-flight + prompt-source audit

- **PF1–3.** `runner-mapping-check.sh` against all three exercises. PASS each.
- **PS1–3.** `prompt-source-audit.sh` against each exercise. Not BLOCK.

## Report

Write `instances/ae101-m3-judge-report.md`. Under 700 words. PASS if every assertion exits 0 AND PS audits not BLOCK.
