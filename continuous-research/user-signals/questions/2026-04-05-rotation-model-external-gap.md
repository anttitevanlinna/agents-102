Cycle 90 research question: Does any external practitioner describe a rotation-based AI deployment model (immerse one cohort → learn → return to org → seed next cohort)?

Zero external sources found after targeted search. The closest is Mollick's "Lab" model (recruit tinkerers, sprint, return) but it doesn't describe the sequential cohort seeding mechanism. The cell division model (seed team spawns new teams) also has zero external examples.

**Gap:** Both the rotation/cohort model and the cell division model are ahead of published practitioner literature. First company to publicly document either model becomes the primary reference.
