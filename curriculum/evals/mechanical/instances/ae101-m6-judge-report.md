# Judge report — AE101 M6 chain (script-only)
## Summary
Verdict: **PASS 26/26**. Generated by bin/judge-ae101-m6.sh — no LLM.

## Assertions
| ID | Verdict | Evidence |
|---|---|---|
| V1 | PASS | Read of spot-gaps-build-the-loop/prompt-001.txt confirmed |
| V2 | PASS | Read of spot-gaps-build-the-loop/prompt-002.txt confirmed |
| V3 | PASS | Read of spot-gaps-build-the-loop/prompt-003.txt confirmed |
| V4 | PASS | Read of spot-gaps-build-the-loop/prompt-004.txt confirmed |
| V5 | PASS | Read of spot-gaps-build-the-loop/prompt-005.txt confirmed |
| V6 | PASS | Read of arc-retrospective/prompt-001.txt confirmed |
| T1 | PASS | M4 transcript stub Read |
| T2 | PASS | M5 transcript stub Read |
| D1 | PASS | 82 diff/gap keywords in Phase 1 |
| R1 | PASS | Rule 2 removed from CLAUDE.local.md |
| R2 | PASS | 4 rules remain after cut |
| S1 | PASS | second skill authored at plan-cadence-checker |
| S2 | PASS | YAML frontmatter with name field |
| S3 | PASS | TODO line near top after Phase 5 |
| Q1 | PASS | 5 distinct Q-markers in Phase 3 |
| A1 | PASS | arc-retrospective.md exists |
| A2 | PASS | arc-retrospective references specific artefacts |
| CR1 | PASS | self-critique keywords in Phase 4 (8) |
| I1 | PASS | Phase 5 invoke+grade evidence |
| H1 | PASS | no exercises/ Read |
| H2 | PASS | no prompts/ Read |
| H3 | PASS | no judge/maintainer Read |
| PF1 | PASS | Verdict: READY — 5 phase(s) checked against spot-gaps-build-the-loop, mapping agrees, all prompts covered. |
| PF2 | PASS | Verdict: READY-WITH-FLAGS — 1 phase(s) checked against arc-retrospective, 1 slug(s) use framing not present in prompt content. Spot-check. |
| PS1 | PASS | BLOCK on pre-existing curriculum source (informational): Verdict: BLOCK |
| PS2 | PASS | BLOCK on pre-existing curriculum source (informational): Verdict: BLOCK |

## Pre-flight

### PF1
```
PASS — Phase 1 (diff two runs name gaps) → prompt-1: keyword match (hits_here=3, hits_elsewhere_max=3).
PASS — Phase 2 (cut stale rule) → prompt-2: keyword match (hits_here=2, hits_elsewhere_max=2).
PASS — Phase 3 (author session shaper skill) → prompt-3: keyword match (hits_here=4, hits_elsewhere_max=3).
PASS — Phase 4 (weakest part pyramid blog) → prompt-4: keyword match (hits_here=4, hits_elsewhere_max=0).
PASS — Phase 5 (invoke on packaged run) → prompt-5: keyword match (hits_here=2, hits_elsewhere_max=1).
Verdict: READY — 5 phase(s) checked against spot-gaps-build-the-loop, mapping agrees, all prompts covered.
```

### PF2
```
WARN — Phase 6 (arc retrospective) → prompt-1: slug keywords not in any prompt content (different framing). Manual review.
Verdict: READY-WITH-FLAGS — 1 phase(s) checked against arc-retrospective, 1 slug(s) use framing not present in prompt content. Spot-check.
```

## Prompt-source audit

### PS1
```
# Prompt-source audit — spot-gaps-build-the-loop

Inputs:
- Fenced prompts: /tmp/prompts/spot-gaps-build-the-loop/prompt-*.txt
- Exercise body:  curriculum/exercises/spot-gaps-build-the-loop.md

## P-checks (fenced prompts)

  Sev-2: P3 markdown shout in prompt-001.txt: 7:- **What packaging caught.** Specific moments in the packaged run where the reference, the plan.md, or the verifier prevented a drift the un-packaged run actually experienced. Quote the un-packaged moment and the packaged moment both.
  Sev-1: P4 curriculum vocab in prompt-001.txt: 3:Read both. Start with repo state: commits on the `m4/` branch after the "M4 starting point" commit, commits on the `m5/` branch after the worktree fork, what each run touched. Then read both session transcripts. Claude Code stores every session's scrollback under `~/.claude/projects/<encoded-folder>/<uuid>.jsonl` — the encoded-folder is the absolute path of the original repo (not this worktree) with `/` replaced by `-`. Use `git rev-parse --git-common-dir` to find the original repo path, then walk the un-packaged session's transcript and the packaged session's transcript. File changes tell you what each agent did; the transcripts tell you how it got there, including drift and re-reads.
  Sev-2: P2 skill path reference in prompt-003.txt: 1:Author a session-shaper as a personal skill. The skill's job is to shape future sessions on this kind of task so the dominant gap I diagnosed in Phase 1 doesn't recur. Shape: one of sharpened-verifier, LLM-judge, or gap-finder — I'll tell you which after you ask. Same authoring approach as the test-strategy skill at `~/.claude/skills/test-strategy/SKILL.md`.
  Sev-2: P2 skill path reference in prompt-003.txt: 7:When you have enough, propose a skill name. Write `~/.claude/skills/<proposed-name>/SKILL.md`: frontmatter (name + description), then instructions. Show me before saving.
  Sev-1: P4 curriculum vocab in prompt-005.txt: 1:Invoke the skill we just authored — by its name — on the M5 packaged re-run. Not on a toy example. The actual artefacts: the commits, the files, the session transcript.

## E-checks (exercise body)

  FLAG:  E5 session-open (verify working dir named): 17:Open a new Claude Code session in the M5 worktree (the same one you ran M5 in, at `../<repo>-m5`). Two runs of the same task are accessible from there: the un-packaged run on branch `m4/<slug>`, the packaged re-run on branch `m5/<slug>`, both visible via git refs since the worktree shares `.git` with the original repo. Both runs also left session transcripts under `~/.claude/projects/` in folders matching the original repo path. That's the auto-stored scrollback Claude Code keeps for every session.

## Summary

Sev-1: 2
Sev-2: 3
FLAG:  1

Verdict: BLOCK
```

### PS2
```
# Prompt-source audit — arc-retrospective

Inputs:
- Fenced prompts: /tmp/prompts/arc-retrospective/prompt-*.txt
- Exercise body:  curriculum/exercises/arc-retrospective.md

## P-checks (fenced prompts)

  Sev-2: P2 skill path reference in prompt-001.txt: 6:- Both skills I authored at `~/.claude/skills/` (the test-strategy skill from earlier, and the skill I authored today).
  Sev-1: P4 curriculum vocab in prompt-001.txt: 7:- The M4 un-packaged run artefact (commits, files, the session transcript under `~/.claude/projects/` in a folder matching this repo — the earliest long-running run).

## E-checks (exercise body)

  FLAG:  E5 session-open (verify working dir named): 13:Open a new Claude Code session in the Module 5 worktree (the same one Module 6 Phase 1 + 2 ran in). A fresh session, so there's no scrollback coloring the read. The artefacts across all six modules, `CLAUDE.local.md`, `.claude/memory/`, ADRs, both authored skills, both runs' commits and transcripts, are accessible via git refs and the worktree state. Ask the agent to walk them and write the arc from them.

## Summary

Sev-1: 1
Sev-2: 1
FLAG:  1

Verdict: BLOCK
```
