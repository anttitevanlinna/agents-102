# M3 (AE101) — quality lane, codesearch variant. Runs in the worktree.
# Driven by run-m3.sh with SCENARIO_QUALITY=this-file. Sister of
# m3-quality.txt (lemmings); same prompt sequence, codesearch-flavored
# tails.
#
# author-test-strategy-skill-1 normally calls AskUserQuestion to
# interview the student about how testing actually works on the
# codebase. In the lemmings variant the tail dumped all answers
# preemptively. The codesearch variant uses the ask-and-wait pattern:
# the tail tells Claude to ask in chat (numbered list) and STOP, then
# a literal-text reply turn simulates a real student answering. The
# answers describe Go testing reality (no DOM, no fetch, no third-party
# test framework — Go's built-in testing package + net/http/httptest).
#
# Save-gate: the literal "yep give it to me and save like you want it"
# after author-test-strategy-skill-1 is the explicit SKILL.md write
# approval.

ae101-m3-quality-side-orient Don't call AskUserQuestion. State your orient read in chat as plain prose.

author-test-strategy-skill-1 Don't call AskUserQuestion — ask your questions as a numbered list in your reply (plain chat text) and STOP, don't write the skill file yet. I'll answer in a follow-up message. Push back if my answers are generic; a test-strategy skill that says "write unit tests first" is useless. After I answer, ask follow-ups the same way (numbered, in chat) if the answers leave gaps. When you have enough, tell me you're ready to write the skill and wait for me to say save.

* answers: 1) Framework is Go's `testing` package, `go test ./...`. No jest/vitest analog, no third-party test framework. 2) Mocked vs real: very little is mocked. Index tests build a real temp index via t.TempDir() + index.Create() + ix.AddRoots() + ix.Add(name, reader) + Flush(), then read it back — see index/write_test.go:276 buildFlushIndex (helpers inject readers via ix.Add; the prod os.Open path lives in index/write.go:150 but tests don't go through it). HTTP tests would use net/http/httptest (recorder for unit, NewServer for integration); csweb has zero tests today, so this is greenfield. No browser, no DOM, no fetch — the page-load JS in cmd/csweb/_static/viewer.js has no test harness in this repo. 3) Integration boundary: the filesystem (the index file at $CSEARCHINDEX) for the search core, and HTTP for csweb. No external service, no third-party SDK. 4) Flaky: rare. Geometry-shaped bugs surface as real bugs not flakiness — see M1's siftUp fix landed deterministic regression tests in index/postheap_test.go (TestPostHeapMergeOrder, TestPostHeapMergeStress, TestPostHeapRejectsOutOfOrder) plus an end-to-end guard in index/merge_test.go (TestFlushChunkMergeEndToEnd). The mmap path on a missing/corrupt index calls log.Fatal which kills the test process — that's a real bug surfacing as a process exit, not flakiness. Also: read_test.go writes index files to /tmp/out at hardcoded paths (read_test.go:30) — leftover convention, don't copy. 5) Regression scope: full `go test ./...` runs in well under a second. Cheap to run on every change. 6) Load-bearing tests reviewers check first: index/write_test.go (TestTrivialWrite, TestTrivialWriteDisk), index/postheap_test.go and index/merge_test.go (the new heap + chunk-merge guards from M1's deeper pass), regexp/regexp_test.go (TestGrep and TestMatch — they share that file; there is no separate regexp/match_test.go). For csweb specifically there are no tests yet — the test-strategy skill should call that out as a gap, not pretend coverage exists. 7) csweb as it stands today: three routes — `/` (home), `/_static/`, `/show/` — bound to hardcoded `localhost:2473`, only flag is `-verbose`. The show() handler at cmd/csweb/web.go:173 does `os.ReadFile(strings.TrimPrefix(path, "/show"))` with the path-clamp still a `// TODO maybe trim file by ix.roots` at line 179, so it's an open path traversal as written. Both `/` and `/show/` ARE testable via httptest.NewRecorder if someone writes the first test. M2 just PLANNED adding a `--bind` flag and an `/api/search` JSON endpoint as a follow-up; those are not implemented on this branch yet, so the skill should describe testing reality for the code that exists, not the plan. 8) Anything else, use your judgment — I'm not going to micromanage skill phrasing. Ask follow-ups if the answers leave gaps.

* yep give it to me and save like you want it.

author-test-strategy-skill-2 Don't call AskUserQuestion. State your reflection in chat as plain prose.
