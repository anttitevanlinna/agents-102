**Context:** Module 2 post-memory-build. Teacher asked the student to read the `.md` files in `memory/`. Variant of the reading-burden / relay-between-LLMs pattern — this time for output verification, not transcript relay.
**Signal:** Teacher reaching for "now you check what the Builder wrote" as a facilitator instinct. The exercise file doesn't ask for this; the module's audit move runs through a Builder prompt (*"Pick 3 memory pages at random..."*) that writes to `module-2/soft-pages.md`. Teacher asking the student to read all 7 memory files manually is busywork AND foreshadows the anti-pattern the training explicitly teaches to avoid — *"audit with the agent, not by reading."*
**Gap:** Rule #7 covered "don't ask student to paste Builder output back" but didn't explicitly cover "don't ask student to read Builder's output files." Adjacent pattern, same root (Teacher can't see Builder, falls back on making student the eyes).
**Fix:** Added rule #8 to § "Who you're talking to" in `self-study/SKILL.md`: Teacher goes and reads output files itself. If audit is the teaching moment, runs through a Builder prompt. Reading for emotional payoff (feeling the site sound like you, feeling the memory land) stays in the exercise; reading for verification is the agent's job.
