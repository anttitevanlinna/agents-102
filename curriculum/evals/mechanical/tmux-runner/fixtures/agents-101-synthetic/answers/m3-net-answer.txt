No live web fetch in this session — use the staged practitioner reading as your "internet". The fresh material this round is a field-report roundup that just landed at <NEW_SOURCE_M3> — open it and pull its specifics, including any named operator and the design move they used. Also read the usage-based pricing playbook still under <MATERIAL_DIR>/web/. The opposing essay — the SMB bill-shock churn warning — is NOT under /web/ anymore; it's already in my curated sources/ from the earlier ingest (sources/usage-pricing-churn-warning.md), so don't hunt for it under /web/ — carry its position from that curated copy. Append each finding to sources/internet-retrieval.md with the author and one line on how their situation maps to my SMB-heavy pilot, and end with "Conflicts and gaps" — don't smooth the disagreement between "usage-based is structurally wrong for volatile SMB" and the fresh report's claim that it's a fixable pricing-design problem; quote both raw.