# Where the rule could live

You have a `.md` file with three to five rules about how multi-file tasks want to be factored on your codebase. Three shapes for what happens to a file like this in real use.

## Three shapes for the file

- **Slack triage.** A request lands in a thread; `@Claude` routes it into Claude Code on the web, or a Slack app hands the thread to an agent runtime. The agent reads the thread plus your `.md` file and replies with the suggested slice or the next question: this is a one-liner, this is a feature slice and here is how it splits, this is an epic in three shippable pieces.
- **Issue webhook.** A teammate opens, edits, or labels a GitHub issue; a GitHub Actions workflow or GitHub App picks it up. The agent reads the issue against your `.md` file, proposes labels, asks for missing info, or splits it into shippable sub-issues. Your file decides what counts as shippable.
- **Scheduled read.** Once a sprint, a scheduled agent reads the backlog top-to-bottom against the `.md` file and proposes a re-shape: which items want to merge, which want to split, which want to die. You arrive to a proposal, not a queue.

## The file is steady; the agent moves

- The `.md` file is the steady part; the agent is the moving part. Across all three shapes, better rules produce better triage. Improvements in the file carry across runtimes, even when the agent changes.
- The file travels. Slack thread, issue event, sprint cadence; Claude Code on the web, GitHub Actions, a scheduled agent. Trigger and runtime are wiring choices; the file rides along unchanged. Once it exists, you have the guardrail, and every shape above is a deployment decision, not a rewrite.
- **Agents build agents.** An agent can help author the `.md` file. A later agent reads those rules to split the backlog. The first agent's output becomes the second agent's context.

<!-- maintainer -->

**Emphasis pass (2026-07-09, Antti-directed "go very lightly on the bold"):** slide 1 keeps bullets with the three shape-name handles (**Slack triage** / **Issue webhook** / **Scheduled read**); slide 2 de-bolded except **Agents build agents** (recurring arc handle, becomes an M6 lecture title), per `theory-plan.md § Slide format — emphasis budget` + `check_slides.md §9`. Lede untouched. Wording near-verbatim; no claims added or cut.

**Slides-only pass (2026-07-02, unaudited):** covered region DELETED (Path A). Per-passage verdicts: three-shapes prose sections CONVERTED to one slide, one bullet per shape (capability-grounded wording preserved: Slack app / `@Claude`-to-web routing, GitHub Actions `issues` events, scheduled agent — do not imply Claude Code runs inside Slack) · per-shape tail lines (steady part / policy / spec triplet) SUBSUMED by slide 2's first bullet · "Each is real; each is downstream" softener CUT · ticket-pair paragraph CUT (it depended on `extract-the-task-shaping-rule-4`, itself a `{{cut:}}` candidate; if the cull reverses, the exercise carries the connection) · "M3 starts with a feature you're shipping" bridge CUT (module `## Next` owns the bridge) · intro line KEPT as the closer's setup lede. File is now lede + two slides.

**Quality:** compendium-audited 2026-08-02 (writing@1c765f2 story@1c765f2 technical@1c765f2 behavior@1c765f2 pedagogy@1c765f2 strategy@1c765f2 slides@1c765f2)
- judges @1c765f2: writing PASS, story PASS, technical PASS, behavior PASS, pedagogy PASS, strategy PASS, slides PASS
- maintainer-reviewed 2026-04-28 (Antti, full AE101 pass)
- cohorts: none yet

**Lecture meta:** *Meta-frame closer, now deck-shaped. ~3 min reading-aloud. Forward-looking; voice tilts Risto (Siilasmaa) on slide 2 — points at what becomes possible, cues the move now. Mood-arc check: M2 is grounded competence — Risto-tilt appropriate at the closer; the lecture defers application rather than promising it. Slide 2 is the naming beat, placed AFTER the shapes so it lands as recognition (`check_lectures §1`).*

**Attribution discipline:** the three shapes ship un-attributed in body. Reasoning: at M2 the cohort is two modules in and hasn't met the practitioners yet; name-dropping reads as authority-borrowing rather than crediting. The deeper attributions land where they're earned: Klaassen + Every's compound-engineering anchor across M5–M6; Ronacher's reference-artifact pattern at M5's verifier build. Per `check_writing.md` #11. Closer beat *"agents build agents"* is Antti's verbatim frame — kept per `check_writing.md` #10.

**Earned-landing map (attribution deferred from body):** compound engineering lands by name at M5–M6; the durable-artifact discipline lands at M5's verifier build. Provenance, stamps and the two attributions this file used to carry wrongly now live in the backing block below — read that before re-crediting either shape to a practitioner.

**Standing constraint — do not imply Claude Code runs inside Slack.** Re-verified 2026-07-31 and it still holds: every current path executes off-Slack on Anthropic-hosted compute, with Slack as trigger and status surface only. Detail and stamps in the backing block.

**Attribution cap (`check_writing.md` #11):** main exercise body cites Pocock + Klaassen + Martin; this lecture adds zero new student-side names.

**Philosophy callout budget:** zero. The closer carries the forward gesture without naming a belief.

<!-- backing -->

Format → `curriculum/backing-format.md`.

**Claims**
- `slack-triage-shape` · detail · "A request lands in a thread; `@Claude` routes it into Claude Code on the web, or a Slack app hands the thread to an agent runtime." ← cc-slack-docs, cc-web-docs, claude-tag-docs
- `issue-webhook-shape` · detail · "A teammate opens, edits, or labels a GitHub issue; a GitHub Actions workflow or GitHub App picks it up." ← gh-issues-events, cc-github-action
- `agent-triages-the-issue` · detail · "The agent reads the issue against your `.md` file, proposes labels, asks for missing info, or splits it into shippable sub-issues." ← cc-action-triage-recipe
- `scheduled-read-shape` · detail · "Once a sprint, a scheduled agent reads the backlog top-to-bottom against the `.md` file and proposes a re-shape" ← cc-scheduled-tasks, cc-routines, gh-actions-cron
- `file-is-steady-agent-moves` · vision · "The `.md` file is the steady part; the agent is the moving part." ← none-owed
- `better-rules-better-triage` · vision · "Across all three shapes, better rules produce better triage." ← none-owed
- `improvement-in-artifact-carries` · vision · "Improvements in the file carry across runtimes, even when the agent changes." ← none-owed
- `file-travels-across-wiring` · vision · "Trigger and runtime are wiring choices; the file rides along unchanged." ← none-owed
- `deployment-not-rewrite` · vision · "every shape above is a deployment decision, not a rewrite" ← none-owed
- `agents-build-agents` · vision · "An agent can help author the `.md` file. A later agent reads those rules to split the backlog. The first agent's output becomes the second agent's context." ← none-owed

**Sources**
- cc-slack-docs `[checked:2026-07-31 result:OK due:cohort]` https://code.claude.com/docs/en/slack — [capability] confirms the body's mechanic almost exactly: *"When you mention @Claude in a Slack channel or thread, Claude automatically analyzes your message to determine if it's a coding task. If Claude detects coding intent, it will route your request to Claude Code on the web instead of responding as a regular chat assistant."* Then *"A new Claude Code session is created on claude.ai/code"*, progress posts back to the thread, completion @-mentions you. Prerequisite: a GitHub account connected with at least one authenticated repo. **Mid-rename:** this page is being retired for Team/Enterprise in favour of Claude Tag; it *"remains the setup path"* for Pro/Max. fallback: none needed — the body already hedges with "or a Slack app hands the thread to an agent runtime", which covers the path this page does not.
- support-claude-slack `[checked:2026-07-31 result:OK due:cohort]` https://support.claude.com/en/articles/11506255-get-started-with-claude-in-slack — [capability] the baseline the routing sits on top of: *"Mention @Claude in any thread to get Claude's help with the conversation"* replies inline, and separately *"Team and Enterprise plan users with access to Claude Code on the web can also route coding tasks directly to Claude Code by mentioning @Claude."* Two-tier behaviour — plain assistant by default, coding routing as a gated layer. This is why the body's "or" is doing real work and should not be tightened into a single mechanism.
- cc-web-docs `[checked:2026-07-31 result:CAVEAT due:cohort]` https://code.claude.com/docs/en/claude-code-on-the-web — [capability] **CAVEAT: still a research preview, not GA** — *"Claude Code on the web is in research preview for Pro, Max, and Team users, and for Enterprise users with premium seats or Chat + Claude Code seats."* Also *"Cloud sessions need access to your GitHub repositories to clone code and push branches"* and *"each session runs in an isolated, Anthropic-managed VM."* The VM line is what keeps the standing constraint true. The body does not claim GA, so nothing above the divider moves — but do not let a future edit describe this as a settled, generally-available product.
- claude-tag-docs `[checked:2026-07-31 result:CAVEAT due:cohort]` https://claude.com/docs/claude-tag/overview — [capability] the successor product, Public Beta, *"available on Team and Enterprise plans… It isn't available on individual plans."* Announced 2026-06-23; *"Claude Tag replaces the existing Claude in Slack app. To migrate, administrators can opt in within 30 days"* — opt-in per org, and **no platform-wide cutover date is published**; a search snippet asserting 2026-08-03 could not be confirmed against any primary page and is deliberately not recorded as fact. Critically for our standing constraint: *"it runs in an ephemeral sandbox hosted by Anthropic, not on your computer or inside your network."* The name changed; the off-Slack execution did not.
- willison-cat-thariq `[checked:2026-07-31 result:OK due:2027-01-31]` https://simonwillison.net/2026/Jul/21/cat-and-thariq/ — [practitioner analysis] Willison's own edited transcript of a fireside chat he hosted with Anthropic's Cat Wu and Thariq Shihipar — his framing, their words, so it is analysis with the speakers named, not practitioner-direct. Cat Wu: *"Internally, we see Claude Tag as the evolution of Claude Code… Claude Code is still the best place for your most complex tasks, when you're interactively iterating with the agent. But Claude Tag is great for having it work proactively on your behalf."* Also reports *"Claude Tag currently lands 65% of our product eng PRs"* — **scoped to Anthropic's own product-engineering team, vendor-internal, do not generalise it** and do not put it in a slide.
- gh-issues-events `[checked:2026-07-31 result:OK due:cohort]` https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows — [capability] the `issues` event carries 20 activity types; `opened`, `edited` and `labeled` — the exact three the body names — are all real and current. Clean.
- cc-github-action `[checked:2026-07-31 result:CAVEAT due:cohort]` https://github.com/anthropics/claude-code-action — [capability] now GA at v1.0 (migrated off `@beta`). **CAVEAT worth keeping straight:** its default, no-configuration behaviour listens for `issue_comment` and `pull_request_review_comment` — *"Responds to @claude mentions in comments"* — **not** for issues being opened, edited or labelled. Triage-on-open is an automation you configure, not something installing the action switches on. The body says a workflow "picks it up", which is accurate for a workflow you wrote; it would be wrong to gloss this as out-of-the-box. See the next entry for the official recipe that makes the configured version legitimate rather than invented.
- cc-action-triage-recipe `[checked:2026-07-31 result:OK due:cohort]` https://github.com/anthropics/claude-code-action/blob/main/docs/solutions.md — [capability] **this is the entry that earns the second shape.** Anthropic's own solutions guide publishes "Issue Auto-Triage and Labeling", triggered on `issues: types: [opened]`, with a prompt that has Claude *"Determine if it's a bug report, feature request, or question"*, *"Suggest appropriate labels"*, and *"Check if it duplicates existing issues"* — a near-exact match to the body's "proposes labels, asks for missing info, or splits it". So the shape is an officially sanctioned pattern, not our invention. It does require the repo owner to add two helper scripts and wire the YAML. fallback: if the recipe moves, keep the shape and say "a workflow you wire once".
- cc-scheduled-tasks `[checked:2026-07-31 result:OK due:cohort]` https://code.claude.com/docs/en/scheduled-tasks — [capability] `/loop` confirmed: *"Tasks are session-scoped: they live in the current conversation and stop when you start a new one"*, *"Recurring tasks automatically expire 7 days after creation"*, requires v2.1.72+ (installed here: 2.1.220), ≤50 tasks per session. Session-scoped and 7-day-expiring makes `/loop` the *weakest* fit for the body's "once a sprint" — name Routines or cron for that cadence, not `/loop`.
- cc-routines `[checked:2026-07-31 result:OK due:cohort]` https://claude.com/blog/introducing-routines-in-claude-code — [capability] *"A routine is a Claude Code automation you configure once — including a prompt, repo, and connectors — and then run on a schedule, from an API call, or in response to an event."* Gated on **two** things, not one: a Pro/Max/Team/Enterprise plan **and** Claude Code on the web enabled (itself still research preview). Caps: Pro 5/day, Max 15/day, Team+Enterprise 25/day; cloud minimum interval **1 hour** against 1 minute for Desktop and `/loop`. A sprint cadence is comfortably inside that; a five-minute cadence is not. Desktop Local routines confirmed separately at `/desktop-scheduled-tasks` (*"Click Routines in the sidebar, then click New routine and choose Local"*; folder required; fires only while the app is open and the machine awake).
- gh-actions-cron `[checked:2026-07-31 result:OK due:cohort]` https://code.claude.com/docs/en/github-actions — [capability] the fourth and least-gated scheduling path, and the one the body's "scheduled agent" can honestly mean for a team with no Routines entitlement: a plain `schedule: cron` workflow calling `anthropics/claude-code-action@v1` with a `prompt`, per the docs' own "Daily Report" example. GitHub's primitive, not Anthropic's. Worth knowing because it removes the plan-gating objection entirely.
- klaassen-compound-engineering `[checked:2026-07-31 result:CORRECT due:cohort]` https://every.to/chain-of-thought/compound-engineering-how-every-codes-with-agents — [practitioner direct, vendor venue] Shipper + Klaassen co-bylined, 2025-12-11 (the `/source-code/` path 307s to this canonical URL). Loop confirmed verbatim: *"Plan → Work → Review → Compound → Repeat"*, expanded by Klaassen alone on 2026-05-29 to *"Ideate → brainstorm → plan → work → review → polish → compound → repeat"*. **CORRECT — this file previously credited the Slack-triage shape to this pattern, and that attribution does not hold.** A full-text search of both articles returns **zero** matches for "slack", and none for any other inbound trigger (GitHub issue, Linear, voice memo, backlog). Every's loop is described from the Plan step onward and says nothing about how work *enters* the system. Klaassen frames the new front of his loop as his own attention — *"I need to understand the user, the product, the weird edge cases… Then I can hand the middle to the agent"* — which is the opposite shape from an automated triage trigger. Cite Every for the loop and for compound engineering; **never for the Slack shape.** Stamp kept in step with `skills-from-the-frontier.md` and `how-this-training-was-built.md`.
- ronacher-coming-loop `[checked:2026-07-31 result:CORRECT due:2027-01-31]` https://lucumr.pocoo.org/2026/6/23/the-coming-loop/ — [practitioner direct] Armin Ronacher, 2026-06-23. **CORRECT — this file previously credited the scheduled-read shape to a "fixed-spec reference-artifact pattern (Ronacher)", and no such pattern appears in his published work.** Neither this post nor "The Tower Keeps Rising" (2026-07-13) uses "spec", "reference", "schedule" or "cron", or describes a durable document an agent re-reads on a cadence. What he actually describes is a harness loop: *"work is put into a queue of sorts, a machine picks it up, attempts it, stops, and then some harness decides whether that was actually the end."* His archive was checked through 2026-07-31 — only "Codeberg Divides" (Jul 24) followed, off-topic — so nothing newer rescues the attribution. **Two further corrections that travelled with this one:** the widely-quoted *"I don't prompt Claude anymore… My job is to write loops"* is **Boris Cherny's**, an epigraph Ronacher quotes and attributes; and Ronacher's own stance is sceptical — *"I have not had much success with this way of working for code I deeply care about."* Both were fixed in `observations/ronacher-armin.md` and `platform-watch/coding-agents/state.md` in the same pass. kb:observations/ronacher-armin.md

**Frameworks**
- Compound engineering · [borrow:practitioner-coined] · law:the-compound-ladder · ← klaassen-compound-engineering
- Harness loop · [borrow:practitioner-coined] · law:none · ← ronacher-coming-loop
- Coding agents as the meta-platform · [borrow:groundwork pattern language] · law:coding-agents-are-the-meta-platform · ← willison-cat-thariq, cultural-vocab
- Event-triggered agent wiring · [borrow:none] · law:none · ← cc-slack-docs, claude-tag-docs, cc-github-action, cc-action-triage-recipe, cc-scheduled-tasks, cc-routines, gh-actions-cron, gh-issues-events, cc-web-docs, support-claude-slack

**Stance** `[stance:2026-07-31 level:L2]`
- holds: all three shapes are real, and each now has an official mechanism behind it — the Slack routing is documented by Anthropic, the issue triage has a published recipe in Anthropic's own solutions guide, and scheduled reads have four independent implementations (Desktop Routines, cloud Routines, `/loop`, plain GitHub Actions cron). The durable-context-file pattern itself is the least contested thing here: `CLAUDE.md`, `AGENTS.md` under AAIF governance, Intercom's and Ramp's skill repos, and compound engineering's wiki are all the same move. The lecture's thesis is standard practice, not a forecast.
- contested: **the enumeration, not the mechanism.** Three is our number, not the field's. The conspicuous absentee is the CI / pull-request gate — the file read on every PR — which is plausibly the most common shape in 2026 and is not one of our three. The module file states "three shapes" as a learning objective, so this is a two-file question, not a one-line edit. **Decided 2026-08-02: hold at three.** Claude Code's Code Review is a structurally genuine fourth trigger — a diff-level gate with severity tiers, reading `CLAUDE.md` on every PR and flagging statements a diff made stale (`code.claude.com/docs/en/code-review`) — but its only source is Anthropic documenting its own product, and a targeted hunt found no named practitioner publishing their own account of running it. A capability claim is legal on vendor docs; adding a shape to a *taught enumeration* on vendor docs alone is not, because the enumeration is what a student carries out and it would owe a re-verify every cohort. Reversal is cheap and the trigger is named in the OODA question below: one practitioner write-up moves it to four. Also unsettled: whether the file is genuinely the steady part. Ronacher's "The Tower Keeps Rising" argues shared understanding erodes silently under agents, which is at least adjacent to a claim that the written artifact drifts from the system it describes. **The first observed decay case is now in hand and it sharpens rather than breaks the sentence:** Horthy built a `CLAUDE.md`-rewriting skill because he "did a lot of restructuring CLAUDE.md files by hand" (`hlyr.dev/blog/stop-claude-from-ignoring-your-claude-md`, 2026-03-17) — building tooling for it is the evidence it recurred. But our claim is *trigger-independence*, the file riding unchanged across Slack, webhook and cron, and the slide already says the file gets sharper. Horthy's case is the codebase moving, not the runtime. L2, one practitioner, one case.
- would-move-it: a published account of a rules or context file that **decayed** — rotted against a moved codebase, or needed rewriting when the model changed under it. That is the same falsifier `skills-from-the-frontier.md` carries for team kits, so the two lectures share one hunt and should be run together. A practitioner naming rot as a risk to design against does not count; we need an observed case. Second: a fourth trigger shape with enough published practice behind it that three reads as an omission rather than a selection.

**OODA**
- question: has a fourth trigger shape (most likely the PR/CI gate) become common enough that "three shapes" is now the wrong number? And has anyone published a durable context file that decayed, against the thesis that the file is the steady part? **Two answers are banked from the 2026-07-31 run — do not re-hunt either.** Every's compound-engineering guide is the closest real-world analogue to this whole structure and a targeted hunt for an admission that it rotted found none; and no case was found of a rules file failing to travel across runtimes, with Osmani stating the opposite outright (*"the same SKILL.md file works in Claude Code, Cursor (with rules), Gemini CLI, Codex"*). Negative results in the most favourable place to look for a falsifier. **Coverage gap in that run, stated so a later cycle does not read it as exhaustive:** the search budget was spent early and fallback search hit CAPTCHA walls, so Willison, Ronacher, Osmani, Horthy and Every were reached by opening their own pages, but **Geoff Charles (Ramp) and Darragh Curran / Brian Scanlan (Intercom) were not** — only unopened secondary snippets, which fails our own byline bar. Their silence on rules-repo maintenance burden is unconfirmed, not verified.
- roster: Dex Horthy (HumanLayer), Kieran Klaassen + Dan Shipper (Every), Armin Ronacher, Addy Osmani, Simon Willison, Geoff Charles (Ramp), Darragh Curran + Brian Scanlan (Intercom); plus the AAIF `AGENTS.md` governance thread and `platform-watch/coding-agents/state.md`.
- last-run: 2026-07-31

<!-- /backing -->
