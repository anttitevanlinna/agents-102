# Judge — M2

You did not run the session. Actor and Critic subagents did, orchestrated by main thread across multiple dispatches. Your inputs:

- **Scratch:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m2-verbatim`
- **Actor scrollback (concatenated across phases):** `curriculum/evals/mechanical/instances/m2-verbatim-actor-scrollback.md`
- **Critic scrollback (concatenated across phases):** `curriculum/evals/mechanical/instances/m2-verbatim-critic-scrollback.md`
- **Actor transcripts (one per dispatch):** find all via `ls -t ~/.claude/projects/-Users-anttitevanlinna-Projects-agents-102/*/subagents/agent-*.jsonl | head -N` where N is the dispatch count you see in the scrollback files. Read the relevant ones.
- **Critic transcripts:** same, identified by content.
- **Parsed prompt files:** `/tmp/prompts/push-back-on-the-plan/prompt-00{1,2,3}.txt`

Transcript evidence outranks scrollback prose outranks subagent self-reports. When a claim sounds thin, open the .jsonl.

## Assertions

### Verbatim prompt fidelity (load-bearing for prompt regression)

- **V1.** Prompt-001 (plan-mode entry) appears VERBATIM in the Actor scrollback Phase 2 section inside a blockquote. Read `/tmp/prompts/push-back-on-the-plan/prompt-001.txt`, then `grep -F` the scrollback. PASS only on exact match.
- **V2.** Same for prompt-002 in Phase 4.
- **V3.** Same for prompt-003 in Phase 5.

### Phase structure

- **P1.** Actor scrollback has sections for each of Phase 2, Phase 3 (at least one regeneration), Phase 4 (at least one Q&A), Phase 5 (pattern naming). Quote the section headers.
- **P2.** Critic scrollback has sections for Phase 3a (two push-backs), Phase 3b (regeneration check), at least one Phase 4 answer, and a Phase 4-close (approve or one-more-grill). Quote.

### Plan quality (the thing the exercise claims the pattern produces)

- **Q1.** Plan v1 (Actor Phase 2 response) contains concrete file paths the plan would touch — not "the relevant files." Quote three specific paths.
- **Q2.** Critic Push-back 1 quotes a specific step of plan v1 and names what it skips. Quote the push-back.
- **Q3.** Critic Push-back 2 quotes an assumption or committed change of plan v1 and names an alternative. Quote.
- **Q4.** Plan v2 (Actor Phase 3 response) is substantively different from plan v1 on the two push-back targets. Quote the before/after of each.
- **Q5.** The final plan (after all regenerations) integrates (not appends) the push-backs and walk-down answers. No "addendum" section tacked on. If an addendum pattern appears, FAIL with quote.
- **Q6.** Actor Phase 5 response names a repeatable design pattern for future non-trivial plans. Quote the pattern.

### Interaction dynamics (the M2 teaching)

- **I1.** In Phase 4, Actor asked ONE question per turn. FAIL if any Actor Q-turn dumped multiple questions. Count questions per turn.
- **I2.** In Phase 4, the Actor recommended an answer alongside each question (per prompt-002's "For each question, recommend an answer"). PASS if recommendations visible in at least 2 of N turns.
- **I3.** Critic's Phase 4-close uses approval-with-condition language (e.g., "ok, enough, let's continue" or equivalent natural phrasing naming what's greenlit). Quote.
- **I4 (softening-on-regeneration check).** Did the Actor's plan v2 accept the push-backs in prose but re-soften any flagged step? Inspect plan v2 specifically on the step(s) Critic pushed back on. Report: sharpened / softened / mixed. Not pass/fail — information for the Auditor.

### Harness leakage

- **H1.** No Actor transcript contains a Read of `curriculum/exercises/push-back-on-the-plan.md`, any `runners/*.judge.md`, any `*.maintainer.md`, or any `*.critic.md`. (Actor may Read its own `m2.actor.md` at first dispatch — allowed.)
- **H2.** No Critic transcript contains a Read of the Actor runner, the exercise file, or Actor scrollback files. (Critic may Read its own `m2.critic.md` — allowed.)
- **H3.** Commit messages in scratch, if any new commits happened, are neutral. No planted-state leaks.
- **H4.** No harness-internal files (`.mechanical-test/`, `.harness/`, etc.) appear inside scratch.

### Informational

- **N1.** Dispatch count: how many Actor dispatches, how many Critic dispatches, total subagents invoked?
- **N2.** Wall-clock duration of the orchestration (from scrollback timestamps if available).

## Report

Write to `curriculum/evals/mechanical/instances/m2-verbatim-judge-report.md`. Shape:

```markdown
# Judge report — M2 verbatim orchestrated

## Summary
<3 lines: assertion counts, the most interesting finding, one dynamics observation>

## Assertions
V1. PASS/FAIL — <evidence>
V2. ...
P1. ...
Q1. ...
I1. ...
I4. <info only>
H1. ...
N1. ...

## Findings for the exercise
- <bullets, ordered by severity>

## Findings for the harness
- <bullets>
```

Under 700 words. Leave scratch in place.
