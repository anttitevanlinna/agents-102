# M3 (AE101) side-quest — quality lane, runs in the worktree.
# Driven by run-m3.sh in parallel with m3-main.txt.
#
# author-test-strategy-skill-1 normally interviews the student via
# AskUserQuestion. In a headless runner that hangs forever, so the tail
# below dumps the answers preemptively (lazy student) and asks Claude
# to skip the tool call. The literal-text line after is the "yes save"
# approval for the skill file.

ae101-m3-quality-side-orient

author-test-strategy-skill-1 I'm doing a headless dry-run, please don't call AskUserQuestion — use what you can read from the codebase plus my answers below; for anything else, use your best judgment and note the gap inline. Answers preemptively: (1) Framework is node:test (built-in, no jest/vitest). (2) Mocked vs real: tests run in Node against a minimal hand-rolled DOM stub defined INLINE in the HUD test file (a makeEl() factory — there is no separate stub file); the stub is a known lie about the runtime (CLAUDE.local.md M1 rule "stubs are miniature lies about the runtime"); anything that requires real browser rendering, real DOM event dispatch, or real innerHTML parsing cannot be tested here. (3) Integration boundary: none — no backend, no fetch, no third-party SDK. The browser is the integration boundary and the test suite does not exercise it. (4) Flaky: rare in this codebase. When a geometry/state diagnostic fails it's usually a real bug surfacing, not flakiness — caused by an off-by-one or a missed state transition in the procedural game loop, not by timing. (5) Regression scope: the whole suite — a couple of small test files in the repo's test dir — runs in under a second via `node --test`. (6) Load-bearing tests reviewers always check first: the logic test for the HUD-tally fix and the HUD-render test (which drives the REAL render() through the inline DOM stub). Read whatever's actually in the test dir + the src files M1's fix touched before writing the skill — M1's fix shape and filenames vary run-to-run, so don't assume specific names (or that a `round.js` exists). (7) Not worth testing here: pure canvas rendering (stub can't model it), pure setInterval cadence, CSS. For the Share-a-level feature specifically: the ADR's textContent hardening in src/ui.js cannot be verified by the Node DOM stub because the stub models innerHTML/textContent as plain string fields; the test for that hardening has to be a browser-level test (Playwright or manual) — name that gap explicitly in the skill rather than pretending node:test can prove the XSS-safe. (8) Name the skill `test-strategy-lemmings` and write it to `~/.claude/skills/test-strategy-lemmings/SKILL.md` — suffix by SUT so it doesn't collide at user scope with other codebases' test-strategy skills.

* Yep. Just give it to me and save like you want it.

author-test-strategy-skill-2
