# Judge — M4 walk-and-send-off verbatim

You are grading an Actor subagent that executed AE101 Module 4's walk-and-send-off exercise on a scratch repo (inherited from the M3 run: ADR present, SKILL.md at `~/.claude/skills/test-strategy/`). Inputs: final scratch state, `.claude/memory/` contents, `CLAUDE.local.md`, the Actor's `.jsonl` transcript, the Actor's scrollback, and 3 extracted prompt files. The exercise's actual send-off is multi-hour and truncated — you grade pre-send-off readiness.

## Inputs

- **Scratch:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m4`
- **Actor transcript:** `<ACTOR_TRANSCRIPT_PATH>` (dispatcher substitutes).
- **Actor report:** `curriculum/evals/mechanical/instances/m4-verbatim-actor-report.md`
- **Actor scrollback:** `curriculum/evals/mechanical/instances/m4-verbatim-actor-scrollback.md`
- **Prompt files:** `/tmp/prompts/walk-and-send-off/prompt-00{1,2,3}.txt`
- **Send-off prompt log:** `curriculum/evals/mechanical/instances/m4-send-off-prompt.md` (should exist as truncation marker)

## Assertions

### Verbatim round-trip

- **V1–V3.** Each of 3 prompt files appears verbatim in the Actor scrollback (strip `^> ?` blockquote prefix for multi-paragraph). Quote 40-char prefix.

### Phase 1 — task pick

- **A1.** Actor screened both candidates against the three long-run criteria (sustained coherence, requirement-weaving, multi-file). Quote the screening output.
- **A2.** Actor did not plan N steps ahead — it screened and waited for the student's pick, rather than jumping to scoped task + audit in one response. Quote the scoped task (2–3 sentences) that resulted from the student's Candidate A pick.

### Phase 2 — audit + fill

- **A3.** Actor dispatched a Task subagent for the audit OR noted the inline substitution. Transcript evidence. If substitution, PASS with FLAG.
- **A4.** Audit returned a **ranked top-5**, not an unranked list of 10+. Quote the ranking.
- **A5.** Each top-5 item has (a) what's thin, (b) what a prepared agent would need, (c) cheapest fix today.
- **A6.** Actor wrote at least 2 files under `<scratch>/.claude/memory/` (Fill 1 observation + Fill 3 business-rules-gap line) OR a single memory file with both entries integrated. Quote filename + at least one entry.
- **A7.** `<scratch>/CLAUDE.local.md` exists and contains the Fill 2 testing-rule sharpening (supertest-with-real-SQLite, no mocking libraries). Quote.
- **A8.** `<scratch>/.gitignore` covers both `CLAUDE.local.md` AND `.claude/memory/`. Quote the `.gitignore` content.

### Phase 3 — Huryn three-block

- **A9.** Actor quoted from the student's OWN material (the ADR, the SKILL.md, or the memory entries just written) before naming the three-block frame. Transcript + scrollback evidence. FAIL if the frame was named first and student's material illustrated after.
- **A10.** One concrete example per block surfaced (Block 1 observation from memory; Block 2 ADR from `docs/adr/`; Block 3 quality check from the test-strategy skill). Quote each.
- **A11.** File-move proposal capped at 1 or 2 (not a repo-wide rearrangement). Quote the proposal.
- **A12.** "Huryn" or "three-block" or "observations / decisions / quality criteria" vocabulary appears only AFTER the quotes — not as the opening frame.

### Send-off truncation

- **A13.** `curriculum/evals/mechanical/instances/m4-send-off-prompt.md` exists and logs the truncation note (not an actual send-off execution).
- **A14.** No multi-hour send-off task was executed. Transcript grep: no long code-generation subagent dispatch, no multi-file feature implementation. The scratch repo's `src/` does NOT contain new login-UX code.

### Harness leakage

- **H1.** Actor did NOT read `lemmings-seed.maintainer.md` or planted-state docs.
- **H2.** Actor did NOT read any judge or sibling actor runner. Reading its own `m4.verbatim.actor.md` is allowed.
- **H3.** Actor did NOT read any `curriculum/exercises/` file.
- **H4.** Commit messages in scratch are neutral (none name "planted" or "harness").
- **H5.** No harness-internal files inside `<scratch>` that Actor re-read.

### Substitutions (informational)

- **A15.** List every substitution with trigger.

### Prompt-source audit

Run the dimension defined in `curriculum/evals/mechanical/runners/_prompt-source-audit.md` against:

- **Fenced prompts:** `/tmp/prompts/walk-and-send-off/prompt-00{1,2,3}.txt`
- **Exercise body:** `curriculum/exercises/walk-and-send-off.md` (clip at `<!-- maintainer -->`)

Apply P1–P5 + E1–E7. M4 is OUT of the `practice`-noun ban range — the noun lands at M4's opener as a deliberate milestone (E6 carve-out). Module-specific notes:
- The exercise sends off a long-running task. P5 open-hook for the send-off prompt: justified if the student supplies the task scope (PASS); FAIL if the prompt fenced block ends with a colon and Claude could derive the task cold.
- E4 receiving-side: M4 inherits from M3 (ADR at `docs/adr/`, SKILL.md at `~/.claude/skills/test-strategy/`). Confirm body names both stable identifiers. Producing-side companion FLAG: M3 chain Judge confirms producer end.
- E5 session-mechanics: M4 opens in the original repo (post-M3). Confirm body explicitly names the working directory at session-open.

## Report

Write `curriculum/evals/mechanical/instances/m4-verbatim-judge-report.md`.

Shape mirrors M3 verbatim judge: Summary / transcript path / scratch / V1–V3 / A1–A15 / H1–H5 / Findings for exercise / Findings for harness / Prompt-source audit. Under 900 words. Short quotes. Leave artifacts in place.
