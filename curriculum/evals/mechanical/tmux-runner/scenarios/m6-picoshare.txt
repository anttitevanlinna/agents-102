# M6 (AE101) — Spot gaps, build the loop + Arc retrospective. picoshare variant.
# Driven by run-m6.sh with SCENARIO=this-file. Single session in the M5
# worktree (`../picoshare-m5` or whatever M5 named it). M5 ended on the
# m5/implement-public-status branch with the packaged re-run of the M3
# ADR's Filter+Lock.
#
# Carries forward:
#  - M1's missing-return fix + post-#761 tests on `m1/picoshare-01`
#  - M2's plan at ~/.claude/plans/m2-picoshare-public-guest-status-<adj>-<animal>.md (slug from this run's M2)
#  - M3's ADR at docs/adr/0001-scope-guest-link-public-status-response.md
#  - M3's test-strategy skill at ~/.claude/skills/test-strategy-picoshare/SKILL.md
#    (picoshare-aware: Go stdlib + real in-memory SQLite + real HTTP routes
#    + Playwright e2e on :6001; calls out the SQLite-locked-table flakiness
#    as test-infra artifact; names Playwright-on-macOS-dev-build as
#    verify-by-hand)
#  - M4's implementation: public-status handler + DTO + panel rendering +
#    humanizeDurationUntil helper + negative-assertion test + README
#  - M5's verifier + plan.md + reference.md + RUN-NOTES.md + packaged re-run
#  - CLAUDE.local.md compounded across M1-M5
#  - ./observations/ with M4's two gap-fill files
#    (filter-lock-disclosure-test-active-link, guest-status-byte-budget-per-upload)
#
# AUQ suppression discipline — same as m6.txt. T6 uses the ask-and-wait
# pattern (not suppression): the tail tells Claude to ask in chat
# (numbered list) and STOP; a literal-text reply turn simulates a real
# student answering. The answers describe picoshare reality + the
# dominant gap as it shows up on this task (Filter+Lock collapse — the
# easy failure where the agent treats the DTO's structural absence of
# Label as redundant with the behavioral negative-assertion test).
# Save-gate literal after T6 is the explicit SKILL.md write approval.

# T1: diff two runs across four dimensions. Large reading task; suppress
# AUQ. The M4 run produced handlers/guest_links.go + the negative-
# assertion test + the panel template + README; the M5 packaged re-run
# sharpened the verifier and re-ran from packaged reference + plan.
# Picoshare-specific gaps to expect in the diff: (a) Lock test risk
# of being weakened to DTO-field-absence-only on re-implementation;
# (b) two-clock setup forgotten if reference.md compresses it; (c)
# byte_budget_remaining semantics re-interpreted as draw-down pool;
# (d) over-fitted verifier checks `go test ./...` green but doesn't
# catch a vacuous Lock that passes against an inactive page.
spot-gaps-build-the-loop-1 Don't call AskUserQuestion — read what's on disk (transcripts at paths recorded in task.md and plan.md, branches m4/implement-public-status and m5/implement-public-status, the test-strategy skill at ~/.claude/skills/test-strategy-picoshare/SKILL.md, the ADR at docs/adr/0001-scope-guest-link-public-status-response.md) and produce the four-dimension diff inline. Quote moments from both run transcripts and from specific commits / files. If something's genuinely unreadable, name the gap rather than asking.

# T2: cut one stale rule (or stop). Rules in CLAUDE.local.md compounded
# across M1-M5 on picoshare — M1's missing-return/state-as-test rules,
# M2's plan/non-goals rules, M3's filter+lock rules, M4/M5's
# run-discipline rules. Some may have aged out by now (e.g. "csweb has
# zero tests today" has no picoshare analogue; "the existing
# TestGuestUpload table doesn't assert state" is no longer true after
# M1 added the entry-count invariant).
spot-gaps-build-the-loop-2 Don't call AskUserQuestion. If your diagnosis killed exactly one rule, cut it from ./CLAUDE.local.md in place and show the diff in chat afterward. If every rule still holds, say so and stop.

# T3: wider look across the whole stack. Scans ~/.claude/projects/
# across every repo — SUT-independent (the student's own session
# history, not the picoshare tree), so this tail is identical to the
# m6.txt / codesearch variants. Reading-heavy like T1; suppress AUQ on
# the prompt's "ask me where your other repos are" branch, bound the read.
spot-gaps-build-the-loop-study Don't call AskUserQuestion — read across what's already under ~/.claude/projects/; don't ask me where other repos are, a representative pass over the sessions on disk is enough. Group the recurring kinds of work, name each group, quote an instance or two, rank the top six or so. If a folder won't read, name the gap rather than asking.

# T4: draw the top recurring work-shapes as mermaid diagrams. Consumes
# the T3 inventory; feeds T6 (author). Tail identical across variants.
spot-gaps-build-the-loop-shapes Don't call AskUserQuestion — draw the top few groups from the inventory you just produced as mermaid diagrams (steps in order, branches, loop-backs), 30 nodes max each. Use your judgment on which few; you don't need my pick.

# T5: atomic primitives menu. Picoshare is Go + Playwright + Bootstrap-
# templated HTML — primitives will land Go-shaped (go test, go vet,
# golangci-lint, manual curl, httptest, body-bytes grep, static grep)
# plus a few Playwright-shaped (browser-driven e2e, screenshot diff)
# even though Playwright dev-build doesn't compile here. The ranked
# picks against the dominant gap will likely surface body-bytes-grep
# and the filter-vs-lock static-grep as the picoshare-specific
# constraints the next-iteration skill has to encode.
spot-gaps-build-the-loop-primitives Don't call AskUserQuestion — produce the list inline. The 5-10 primitives + 2-3 ranked picks all land in one chat reply. Picoshare is Go + Playwright + Bootstrap-templated HTML; primitives like `./dev-scripts/run-go-tests`, `go vet`, body-bytes grep on rendered HTML, static grep for `respondJSON(w, gl)` or template-raw-struct binds, httptest.NewRecorder, manual curl against the real binary on :16001, Playwright on a Linux build, race detector are in scope. Generic "tests" doesn't make the cut; "body-bytes grep on httptest-rendered /g/<id> output" does.
