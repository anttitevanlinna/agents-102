# Bug-finding improvements — todo list for future runner-mechanic sessions

Captured 2026-05-24 after self-introspection on the M1+M2 dry-run sweep. Numbered roughly by yield-per-hour: high yield + low effort first.

## M6 Phase-2 redesign — mechanical harness scripts the OLD single-primitives chain (2026-05-31)

- [ ] **`run-m6.sh` assert_turn dispatch + `scenarios/m6*.txt` + both playground maintainer files predate the M6 Phase-2 redesign.** Canonical chain is now 8 prompt turns: `-1` → `-2` → `-study` → `-shapes` → `-primitives` → `-3` (author) → `-4` → `-5`, then `arc-retrospective-1`. The scenarios already run `-primitives`; they are missing only `-study` and `-shapes` (two turns, inserted between `-2` and `-primitives`) — NOT three. Inserting them shifts every `assert_turn` key_seq case from 3 onward by +2: study=3, shapes=4, primitives→5, author→6, critique→7, invoke→8, arc→9. Renumber the existing case bodies and add two new cases — study (scrollback = recurring-work groups + quoted instances + a ranked top-six), shapes (work-shape diagrams). Files: `run-m6.sh` (assert_turn), `scenarios/m6.txt`, `scenarios/m6-codesearch.txt`, `scenarios/m6-picoshare.txt`, `playgrounds/codesearch.maintainer.md`, `playgrounds/picoshare.maintainer.md`. The `-study` prompt scans `~/.claude/projects/` cross-project — a single-SUT worktree has near-empty cross-project history, so the scenario must seed it or scope `-study` to the SUT (carry the `session-shaper-<sut>` suffix; the `-3` answer tail already pins `session-shaper-lemmings`). Reaching `mechanical-tested` ALSO needs a live battery run, which needs the M5-end-state worktree (full M1→M5 chain first). Heavy; own task. The module's `- mechanical` row was degraded PASS→REVISE 2026-05-31 to stop the stale `@e840433` pin overclaiming. Deferred from the 2026-05-31 content done-done pass (content side: all six classes PASS across exercise + module + lecture; lecture pedagogy REVISE cleared via sim + live-test).

## Caught 2026-05-27 picoshare M1–M6 full-chain audit (medium effort)

- [x] **M4 picoshare T1 plan-slug stale `vigilant-pangolin` reference — fallback hint added.** `scenarios/m4-picoshare.txt` `walk-and-send-off-1` tail named the prior dry-run's M2 plan filename literally; on a fresh M2 the slug is different (this run: `prudent-pangolin`), and the agent's task.md wrote *"M2 plan is lost from disk"* instead of finding the actual file. Mirrors the existing affordance in `scenarios/m3-main-picoshare.txt` `map-the-access-surface-2`'s `-2` tail; replicated the same *"if a slightly different plan filename is present, same shape with a different adjective-animal slug, use that one"* hint in M4's tail.
- [x] **M5 picoshare header comment falsely listed RUN-NOTES.md as M4 starting state.** `scenarios/m5-picoshare.txt` line 11 read *"plus RUN-NOTES.md untracked"* in M4's expected leftover state. M4's prompts (`walk-and-send-off-*` + `ae101-m4-take-task-end-to-end`) don't direct creation of RUN-NOTES.md; the artefact actually originates from M5 PC's `ae101-m5-rerun-packaged`. Reworded the header to name M5 PC as the originator and drop the M4-expected claim.
- [x] **`run-m5.sh` verifier-path heuristic refined: two-layer fix.** State.json's `m5_verifier` was capturing `~/Projects/picoshare/handlers/upload_test.go)` — trailing `)` from a `(`path`)` prose pattern, plus pointing at the MAIN repo (the agent's prose context) rather than the worktree's actual save. Root cause: M5 INTEGRATED the disclosure-lock test into the existing `handlers/guest_links_test.go` rather than creating a new file, so the primary file-list set-diff returned empty, and the scrollback-grep fallback grabbed whatever path the agent's prose mentioned. Fix: (a) added a secondary heuristic — capture `git status`-derived dirty-files list at PB-T5 (modified + untracked) into `pb_dir/dirty-at-t5.txt`, then `pick_verifier_path` it before falling through to scrollback; (b) tightened the scrollback fallback: strip trailing punctuation (`.,;:()`) and require the captured path is inside `$worktree_cwd` (rejects main-repo references and saves us from recording a wrong-direction path). The state.json field is informational (no downstream consumer per the original Fix 2 note), but the wrong-path data was misleading auditors. Verified on the picoshare 2026-05-26 PB-T5 transcripts: new regex strips the trailing `)`, worktree-prefix filter rejects the main-repo path, dirty-list heuristic would have picked `handlers/guest_links_test.go` (worktree-rel) — the correct verifier.



- [x] **Stalled-TUI detect-and-report in `lib/sync.sh` — done 2026-05-26 evening.** Pattern hit three times in 24h (M6 ×2 morning, M4 ×1 afternoon — codesearch M4–M6 retest). All three: API socket-closed mid-turn, TUI shows error but does NOT auto-retry in `--permission-mode auto`, pane stays alive, sentinel never fires, runner sits the full timeout. `wait_for_turn` now captures the pane every 30s, greps for `API Error|socket closed unexpectedly|Connection.*refused|rate.?limit`, and on 2 consecutive matches logs `WARN stalled-TUI pattern matched ...` to stderr every 5 minutes. No auto-nudge — operator-actionable only, per the "safer first version" guidance in [[memory/compounded/2026-05-26-platform-claude-code-tui-no-auto-retry-api-error-headless.md]]. Validate over the next sweep cycle before considering the `send-keys Enter` nudge.
- [x] **Chain-wrapper pre-wipe of tracked run artifacts + leg-namespaced branches — done 2026-05-26 evening.** Caught in the same M4 retest: chain repositions M4 from M3-ADR via `git checkout -B`, which restores task.md from M3's tracked tree carrying the PREVIOUS run's `Run coordinates` block; commit-starting-point's "append a protected block" semantics then preserve both old and new blocks; the chain's `grep 'Branch:' | head -1` picks the stale block and the M5 reconciliation `branch -m` collides on a stale `m4/<slug>` sibling. Sibling of the `wipe_skill` pattern but different mechanism (git tracked tree vs ~/.claude/skills/ persistence). Both `chain-codesearch.sh` and `chain-lemmings.sh` now ship `wipe_leg_branches m{3,4}` + `wipe_run_artifacts task.md/plan.md` + `head -1` → `tail -1` defense-in-depth. The prompt is correct as-is — a fresh-repo student never hits the staleness; this is harness-only. See [[memory/compounded/2026-05-26-platform-chain-wrapper-prewipe-tracked-run-artifacts.md]].
- [x] Read M1 + M2 transcripts turn-by-turn (not just sampled final artifacts) — done 2026-05-24, findings logged below the list.
- [x] Fix wait_for_turn cleanup hang — done 2026-05-24. Added `pane_capture_safe` in `lib/tmux.sh` that wraps `tmux capture-pane` in a hard wall-clock alarm (gtimeout → timeout → perl-alarm fallback for macOS). All FAIL-path captures + EXIT trap captures in run-m1.sh and run-m2.sh now use it. Previously a high-effort claude rendering past the runner's sentinel timeout could hold the bash trap hostage for an hour.
- [x] Backport key_seq dispatch to run-m1.sh — done 2026-05-24. Lets the same runner drive both `scenarios/m1.txt` (no literals) and `scenarios/m1-codesearch.txt` (with `* One commit, no PR` literal). Cases 6+7 updated to handle commit-at-T6 (m1.txt tail) OR commit-at-literal (m1-codesearch.txt default).
- [x] N=4 of M1 non-deterministic save-gate — collapsed via m1.txt tail rewrite (lazy-student "don't call AskUserQuestion + don't commit at T4" guard). m2-codesearch.txt by other agent uses the smarter ask-and-wait pattern (preserves Q&A in chat text + canned student-voice replies).
- [x] **run-m4.sh `m4_sha` grep fixed 2026-05-25.** Was greping first 7+hex from turn-5 transcript (caught prior-commit references). Now uses `git log --format='%h' --grep='^M4 starting point$' -1` on the SUT cwd; falls back to transcript-grep only if git lookup is empty. Authoritative source is git itself.
- [x] **run-m3.sh race loop liveness check landed 2026-05-25.** Race loop now calls `tmux has-session` on both main + quality sessions every poll cycle. Fails fast with named-session diagnostic if either pane dies externally (OOM, API kill, etc), instead of polling for the full 7200s phase-B timeout. Earlier M3 runs polled 90+ min after the session was actually gone.
- [x] **run-m4.sh user-memory-dir mkdir guard 2026-05-25.** Fixed at line 74: `set -e + pipefail + ls $nonexistent_dir | sort | tr` silently aborts the script with no output (exit 1, empty out-dir). On a fresh SUT — like the picoshare port — the `~/.claude/projects/<encoded>/memory/` subdir hasn't been created by any prior run, so M4 dies before launching claude. Now `mkdir -p "$user_mem_dir"` runs defensively before the `ls`. Caught on first M4 picoshare attempt: 4 empty out-dirs before the cause was diagnosed.

## Caught 2026-05-26 evening — full codesearch M1→M6 re-run (fresh-baseline)

End-to-end PASS, all 6 modules, single chain. Master reset to pristine, M1 + M2 driven by run-m{1,2}.sh standalone (chain-codesearch only wraps M3–M6); `./chain-codesearch.sh --from m3 --to m6` then ran one-shot to DONE. Runs: M1 `20260526-191642-72279` (siftUp regression fix, branch `fix-merge-heap-siftup` @ `6df9eda`); M2 `20260526-193302-80174` (plan `~/.claude/plans/m2-csweb-shared-instance-tidy-otter.md`, no commit, CLAUDE.local.md integrated); M3 `20260526-195854-89925` (ADR `docs/adr/0001-clamp-csweb-show-to-index-roots.md`, `test-strategy-codesearch` skill authored); M4 `20260526-201812-96317` (branch `m4/implement-show-clamp`, starting point `39bed0b`, clamp impl `8d9faaa`, `observations/csweb-clamp-evalsymlinks-enoent.md`); M5 `20260526-203753-2958` (worktree forked at `39bed0b`, three commits `19177a7` replay-gate → `bad2467` packaging → `ea011ba` clamp impl, RUN-NOTES with live-socket smoke evidence); M6 `20260526-211252-10724` (`session-shaper-codesearch` skill, `observations/practice-arc-note.md`, CLAUDE.local.md rule pruned).

### Student-facing findings (curriculum surface)

- [ ] **`m4-codesearch.txt:62` task-slug example misleads.** The prompt says *"The task-slug for the branch should be `implement-bind-gate` or similar — pick one in that shape and proceed"*. But `implement-bind-gate` describes the OTHER M3-eligible ADR (the T-class non-loopback gate), not the I-class /show-clamp ADR M3 historically picks. This run's agent ignored the hint and picked `implement-show-clamp` — load-bearing for the M5 worktree-fork (the agent's branch name flows into task.md's Run-coordinates block, and the chain wrapper reads it back). Fix: drop the slug example, OR change it to `implement-show-clamp`/`clamp-show-to-roots`/something matching the dominant ADR pick. The student-mood cost is a small confusion ("why is bind-gate suggested when I'm clamping /show?"); the wrapper-mechanic cost is an orphaned `m4/clamp-show-to-roots` branch left after the agent picks a different name (cosmetic — not at the same name as the agent's choice, so harmless, just sediment).

- [ ] **`m3-main-codesearch.txt:59` hardcodes `brisk-otter` plan slug.** Map-the-access-surface-2 says *"the plan at /Users/anttitevanlinna/.claude/plans/m2-csweb-shared-instance-brisk-otter.md"*. This run's M2 produced `tidy-otter`. The line continues *"If a slightly different plan filename is present (same shape, different adjective-animal slug from a fresh M2 run), use that one"* — which the agent followed. Runner-safe; student-confusing (the suggested filename doesn't exist on disk for the cohort student who just ran their own M2). Fix per the convention codified 2026-05-25: after a fresh M2 dry-run for the SUT, grep `m2-state.json:m2_plan_file` and update the slug in `m3-main-<sut>.txt` ~line 59. OR loosen the prompt to not name a specific slug, just point at `~/.claude/plans/m2-csweb-shared-instance-*.md`.

- [ ] **`m2-codesearch.txt:3` stale baseline-branch comment.** Header says *"Single session in ~/Projects/codesearch on branch m1/codesearch-01 (carries M1's siftUp fix + CLAUDE.local.md)."* The M1 agent freely picks the branch name (this run: `fix-merge-heap-siftup`; prior runs: `fix-siftup-merge-regression`). Documentation-only, no behavior, but tells a student to expect a branch that won't exist. Fix: change to *"on whatever branch M1 left HEAD (carries M1's siftUp fix + CLAUDE.local.md)."*

- [ ] **M2's output is barely on disk — caught by the agent itself in the M6 arc-note.** Quoted: *"There is no `--bind`/`/api/search` plan file. M2 survives only as retrospective prose inside `CLAUDE.local.md`."* The M2 plan went to `~/.claude/plans/m2-csweb-shared-instance-tidy-otter.md`, which is user-keyed (not in the SUT repo, not pushed with the branch). For a real cohort team sharing branches across people, the M2 work product can't be pulled with the branch. Real-cohort risk: a teammate who pulls m3/quality (or any downstream branch) sees no plan file, only a Reference to "the M2 plan I wrote" inside CLAUDE.local.md. Probably the right curriculum response is a one-line callout in the M2 module body — *"the plan lives in ~/.claude/plans/; copy it into the repo if your team wants to share it"* — not a runner fix.

- [ ] **`session-shaper-codesearch` ships unfalsified — caught by the agent itself, per the rule it inherits.** The arc note quotes the skill: *"Because both diagnosed runs met the bar... the anchor is a constructed negative."* The judge has never caught a real run that skipped hand-verify, because no such run exists in this arc. By `guardrails-need-a-red-bar` (the M5-authored rule), that is scaffolding. **The M6 exercise asks the student to author the judge + invoke it on M5, but doesn't ask them to deliberately break the discipline and re-run to confirm the judge fires.** Curriculum-side: consider whether M6 should end on a "fire the judge against a deliberately-bad RUN-NOTES and watch it red" step, paralleling the M5 replay-gate's earned-red-bar pattern. Optional — the lesson lands either way, and the agent self-named it.

- [ ] **`MEMORY.md` overclaims after a fresh chain — caught by the agent itself.** The arc note: *"`MEMORY.md` promises `observations/practice-arc-note.md` — which (until this file) did not exist; the `practice-arc-M1-M6` memory marks itself SUPERSEDED and redirects to coordinates that don't match the tree — symbol `underRoots`, commits `1b530d2`/`fa5d119` — when HEAD ships `pathInRoots` at `ea011ba` and `1b530d2` is dangling."* This is the home-keyed auto-memory at `~/.claude/projects/-Users-anttitevanlinna-Projects-codesearch-m5/memory/` carrying stale promises from PRIOR codesearch runs. The fresh chain re-ran the work but didn't re-tether the memory — the rotting-memory failure mode the arc-note's "one line forward" calls out. **Not student-facing for a first-time cohort** (no prior memory exists); **load-bearing for any returning student or for the maintainer's own dry-run sweeps.** Fix candidates: (a) chain-codesearch wipes the home-keyed `memory/` between runs (sibling of `wipe_skill` but for the auto-memory dir — caveat: would erase legitimately-current memos too); (b) a `/refresh`-style sweep that walks `~/.claude/projects/<encoded>/memory/` against the current HEAD and flags stale coordinates; (c) live with it as a known artefact and document the cleanup step in the maintainer playbook.

- [ ] **The replay-gate keyed-mutation pattern bent the code — caught by the agent itself.** Arc note: *"Because `lib/replay-gate` keys mutations to source-text anchors... the M5 run built the code to fit the gate verbatim — its own decisions log: 'Built code to match the gate verbatim,' inlining `EvalSymlinks` where the un-packaged M4 freely extracted a `resolvePath` helper."* This is a **substantive M5 finding**: the apparatus (gate + reference.md naming-is-load-bearing rule) shaped the implementation, not the other way around. Not a bug per se — it's an emergent lesson the agent surfaced. Curriculum-side: the lesson lives in the arc note already; no fix needed unless we want the M5 exercise body to flag "the gate can pull the code toward its anchors — name it as a tradeoff."

### Runner-mechanic findings (harness)

- [ ] **Chain wrapper leaves orphaned `m4/<wrapper-positioned-slug>` branch when the agent picks a different name.** This run: wrapper positioned `m4/clamp-show-to-roots` @ `6df9eda`, agent created `m4/implement-show-clamp` and committed there. The wrapper's M5-leg reconcile compares `m4_rec` from task.md to current HEAD and (correctly) skips reconcile when they match. The orphan stays. Cosmetic — fix is a post-leg `git branch -D` of the originally-positioned slug if it's still at the M2 SHA (i.e. never used). Low-priority.

- [ ] **`local-only` `master` ahead 3/behind 3 of `origin/master` is a baseline drift artefact.** Repo's local master has commits with the same MESSAGES as origin/master but different SHAs (cherry-pick / amend history from a prior dry-run reset). M1 runs fine against it (uses HEAD), but `git pull` would conflict and any cohort student doing `git pull --rebase` would hit it. Fix: `git reset --hard origin/master` in the codesearch arrange-helper when (if) one gets written, OR document it in the codesearch playground maintainer notes. Latent — needs a codesearch arrange-helper to actually act on.

- [ ] **No codesearch arrange-helper yet — M1/M2 codesearch can't be chain-driven.** `chain-codesearch.sh` only supports M3–M6. M1 (master-reset, baseline assertions) and M2 (post-M1 state assumptions) need a sibling of `arrange-lemmings.sh` that resets codesearch to a pristine baseline, removes residue, wipes the SUT-suffixed user-level skills, removes worktrees. Without it, a full M1→M6 codesearch re-run is a four-step manual orchestration (`run-m1` → `run-m2` → `chain --from m3`). Medium yield; the wrapper would also kill the orphaned-branch noise above. Pattern: copy `arrange-lemmings.sh`, swap baseline-SHA, skill names, worktree paths.



- [x] **`wait_for_turn` doesn't detect pane death — fixed 2026-05-26.** `lib/sync.sh:wait_for_turn` now takes an optional 4th arg (tmux session name) and polls `pane_alive` every 5s alongside the sentinel-file check; returns exit 2 on pane death (distinct from 1 = timeout) with a quoted reason. All 9 callers in `run-m{1,2,3,4,5,6}.sh` now pass `$session` / `$main_session` / `$pa_session` / `$pb_session` / `$pc_session` as appropriate, and their FAIL echoes dropped the misleading "(sentinel timeout after …s)" tail — the wait_for_turn echo already names the cause. Closes the codesearch M3 first attempt + M5 PC failures where manual `kill <pid>` was the only way to unblock.
- [x] **Per-runner sockets correction — captured 2026-05-26.** Initial read of the M5 PC failure was tmux-server-contention; per-runner sockets ARE already implemented end-to-end (every `run-mN.sh` exports `RUNNER_TMUX_SOCKET="runner-$run_id"` before sourcing `lib/tmux.sh`). The actual cause was pane death without runner detection (see prior bullet, now fixed). The pre-cohort-todos watch about "parallel tmux runners share the default socket" should be retired — it's wrong about the diagnosis; the real failure mode was sentinel-without-pane, not socket sharing.

## Caught 2026-05-25 fresh-baseline lemmings redo (M1) + repeatable-chain build

Session goal: redo the full lemmings M1–M6 tmux chain with a deterministic per-SUT skill-suffix improvement, and **codify the run so it's repeatable**. M1 ran + passed; M2–M6 deferred (paused after M1).

- [x] **Deterministic per-SUT skill suffix pinned in scenarios — kills the emergent-rename divergence** (the open axis at the picoshare/M3/M4 notes below: `test-strategy` collided across SUTs, agents diverged on suffix-rename vs `.bak` vs bare-name per run). Fix landed in the scenario tails (NOT prompt bodies — `check_platform_and_boundaries.md §30`): `author-test-strategy-skill-1` now pins `test-strategy-{lemmings,codesearch,picoshare}` and writes to the matching path; `spot-gaps-build-the-loop-3`'s answer-7 pins `session-shaper-{sut}`. Applied to all three SUT variants. **Latent bug this exposed + fixed:** M6's `spot-gaps-build-the-loop-1` tail (codesearch + picoshare) hardcoded the generic `~/.claude/skills/test-strategy/SKILL.md` read path — with M3 now writing the suffixed name, T1 would have read the wrong (or another SUT's) skill. Repointed to `test-strategy-<sut>`.

- [x] **The missing M1–M6 wrapper now exists: `arrange-lemmings.sh` + `chain-lemmings.sh` + `lemmings-chain-runbook.md`.** Prior runs were driven module-by-module from tribal knowledge between the `run-mN.sh` calls (no reset, no documented baseline, no chaining). `arrange` encodes the baseline reset (code→`bdd0919` on `m1/<slug>`, fresh `CLAUDE.local.md`, `-lemmings` skills + blocking worktrees + stale `runner-*` tmux sessions cleared, all reversible to `~/Projects/lemmings-chain-backup/<ts>/`); `chain` arranges then positions each module's branch/worktree from the prior `state.json` and fires the next runner, with `--from/--to` resume. This subsumes several open items below: **stale-session cleanup** (was unaddressed), **baseline prep** (was tribal), and pairs with the M5 worktree pre-flight already landed. Validated: arrange + M1. M2–M6 transitions coded-but-unvalidated — run `--from mN --to mN` to validate each on first use.

- [ ] **Fresh-baseline artifact drift breaks the calibration of canned downstream replies — confirm before trusting a fresh full chain.** The M1-start `CLAUDE.local.md` baseline (`.residue-archive/...preserved-2026-05-24`) was deleted, so the redo uses the fresh first-student baseline (M1 recreates the file). Consequence observed on M1 run `20260525-205903-73299`: the agent's emergent artifact layout diverges from what the canned M3/M4/M6 replies assume, because those replies were calibrated to the PRIOR run's emergent shapes. Concrete: (a) M1 wrote tests under `test/` (singular) — `test/hud.test.js`, `test/round.test.js` — while the m3-quality canned answers name `tests/levels.test.js`, `tests/main.test.js`, `tests/_dom-stub.js` (plural). (b) M1's fresh `CLAUDE.local.md` names the HUD/durable-tally rule but NOT the blocker-deadlock bug, so M4/M6's "the bug I named in CLAUDE.local.md M1 rules" framing is now false (M4's tail carries the bug inline, so not blocking — but the reference dangles). (c) the fix shape differs from the prior run (durable resolution ledger `src/round.js` + `inPlay()`, vs the prior `state.active` tally). **This is the `check_platform_and_boundaries.md §… canned-reply-needs-ground-truth` axis biting a whole chain, not one reply.** Fix options: re-ground the M3-quality + M4/M6 canned replies against a fresh-chain run, OR restore a canonical M1-start `CLAUDE.local.md` so the layout is stable, OR loosen the canned replies to not name specific paths/files.

- [ ] **M6 `session-shaper-<sut>` (generic+suffix, per maintainer 2026-05-25) decouples the M6 test artifact from the production judge names.** Prior M6 runs authored gap-specific names (`verify-by-hand-judge`, `disclosure-lock-judge`, `plan-gap-finder-lemmings`) which became real production judges. With the answer-7 pin forcing `session-shaper-<sut>`, future M6 runs author a throwaway-named skill and no longer clobber/refresh those production judges. Arguably cleaner (test runs don't touch production judges), but note it: the M6 exercise no longer (re)produces the production judge by name. If the intent was to keep authoring the production judges, switch the pin to descriptive-name + `-<sut>` suffix instead.

- [x] **M1 fresh-baseline run PASSed 8/8** (`20260525-205903-73299`, medium effort): all assertions green, `bdd0919 → 8dd4cd0`, real deeper-than-surface fix (new `src/round.js` durable ledger, HUD reads `inPlay(state)`), 6/6 `node --test` green, `CLAUDE.local.md` recreated, tmux session cleaned up by the trap. Medium effort completed the whole module in well under the 1800s/turn cap.

### M2–M6 chain validation 2026-05-26 (medium effort, via `chain-lemmings.sh --from mN --to mN`)

- [x] **MULTI-AGENT HAZARD (caused + fixed 2026-05-26):** `arrange-lemmings.sh`'s stale-session cleanup was "improved" to enumerate per-runner socket files (`/tmp/tmux-$UID/runner-*`) and kill sessions across all of them — which **killed a parallel agent's live M3 run** (`runner-20260526-094231-50520-main/-quality`). Per-runner sockets exist to ISOLATE concurrent runs; a global enumerate-and-kill defeats that. Reverted to default-socket-only (where only truly-orphaned pre-sockets sessions can sit). **General rule for the shared tree:** destructive cleanup (kill sessions, remove worktrees, reset branches) must be scoped to your OWN run's artifacts; "kill all runner sessions" is never safe with concurrent agents.
- [x] **One-shot `./chain-lemmings.sh` got M1→M4 green fully unattended, then M5 hung — ROOT CAUSE: API-socket-drop error-stall (NOT AUQ).** Run `20260526-094528` (full sweep): arrange + M1+M2+M3+M4 all PASS with zero intervention — the chain composes one-shot. M5 PB-T6 (`diagnose-and-resend-6`) then hung. **Root cause located from the session jsonl** (`...-lemmings-m5/3673cbdf-….jsonl`, event 141/148): the terminal event is a synthetic assistant message `isApiErrorMessage:true` — *"API Error: The socket connection was closed unexpectedly"* — at 08:25:15Z (11:25 local, lunch window; matches the run-dir freeze). The API socket dropped mid-turn, **almost certainly machine sleep** (sleep closes the connection). **Why it hung the runner, not failed:** (1) an API-error-stop injects a synthetic message and stops WITHOUT firing the Stop hook → no `turn-7.done` sentinel; (2) the pane-death check doesn't catch it — the claude PROCESS stayed alive (idle-with-error), pane not dead; (3) the iteration-based `wait_for_turn` timeout was frozen by the sleep, so it sat ~3h instead of failing at 30 min. The tool tally for the session is `Bash:16 Read:1 Write:2` — **zero AskUserQuestion calls.** (An earlier note here blamed AUQ; that was a pattern-match on the PA hangs, corrected after reading the transcript.) **Runner-level fixes (runner owner's lane):** (a) tail the session jsonl for `isApiErrorMessage:true` (or the synthetic `<synthetic>` model + `error` field) and fail-fast / auto-retry the turn instead of waiting for a sentinel that can't come; (b) make `wait_for_turn`'s timeout wall-clock-based (e.g. compare `date +%s`), so a machine-sleep can't freeze the elapsed counter. Net: M1→M4 IS reliably one-shot; M5+M6 validated module-by-module + piecewise; the lone failure was an infra event (socket/sleep) the runner doesn't yet recover from — a narrower, more tractable gap than the AUQ flakiness I first assumed.

Through-line: **the fresh-baseline choice is the common cause of three separate breakages.** No in-repo `./.claude/memory/` ever gets created (M4 sharpens rules into `CLAUDE.local.md` instead of writing a memory file), and prior runs masked every bug below by having stale memory/state present. Per `check_platform_and_boundaries.md §7b/§6d`: in-repo `.claude/memory/` is the user-authored *convention* (gitignored, copy-at-fork), DISTINCT from Claude Code's home-scoped auto-memory at `~/.claude/projects/<encoded>/memory/`. Several agents conflate the two. **Runner-level fixes below are FINDINGS for the runner owner (other agent), not mine to consolidate.**

- [x] **M2 PASS 8/8** (`20260526-062440-86274`) — plan-mode, no repo commit (expected). **FINDING (runner):** `m2-state.json:m2_plan_file` captured a FOREIGN plan (`m2-picoshare-...-lively-otter.md`) while the T1 assertion correctly found the lemmings `m2-add-levels-2-3-curious-marmot.md`. run-m2's newest-plan heuristic grabs whatever landed last in the shared `~/.claude/plans/`; the parallel codesearch/picoshare agent's M2 polluted it. Scope capture to the SUT slug pattern.
- [x] **M3 PASS all phases** (`20260526-065207-96258`) — ADR committed, `test-strategy-lemmings` authored ACCURATELY (cites `test/` + inline DOM stub) after re-grounding the canned answers. Lemmings-lane fixes: m3-main.txt `ae101-m3-fork-quality-side` got an AUQ-suppression tail (bare prompt + absent `./.claude/memory/` → agent asks about the cp-step → PA hang; README §42 named it); m3-quality.txt canned answers re-grounded from the deleted prior-run layout to the real `test/hud.test.js` + `test/round.test.js`.
- [x] **M4 work PASS** (`20260526-070955-3963`) — fix committed (`f14ebbd`), 9/9 green, `task.md` coords written — but run-m4 exited 1:
  - [ ] **FINDING (runner):** post-send-off `new_proj_mem="$(find "$proj_mem_dir" … | tr …)"` crashes under `set -euo pipefail` when `./.claude/memory/` is absent — silent exit 1 AFTER the send-off committed. Sibling of the fixed `user_mem_dir` mkdir guard. Stopgap `|| true` applied (line ~167); consolidate as you see fit.
  - [x] **M4→M5 branch-slug coupling (wrapper fix):** the M4 agent names its branch from the TASK (`m4/deadlock-terminal-fix`) in task.md, not the wrapper's slug; `ae101-m5-worktree-setup` reads task.md + AUQ-asks if that branch is absent. `chain-lemmings.sh` now reconciles (renames the actual m4 branch to task.md's slug) before M5.
- [x] **M5 PASS** (`20260526-080822-21636`, after a 1st-attempt PA hang) — fork at `2002d7c` correct, reconcile worked. **Lemmings-lane fix:** m5.txt `ae101-m5-worktree-setup` got an AUQ-suppression tail — the agent noticed the cp intent-vs-reality gap, conflated in-repo memory with home-scoped auto-memory, and AUQ-hung PA for 1800s. README §42 names this prompt.
- [ ] **FINDING (prompts — runner/curriculum owner):** the `cp ./.claude/memory/` in both fork prompts is CORRECT per §7b but invites the conflation above; a one-line §4 disambiguation (in-repo convention ≠ home-scoped auto-memory) in the prompt body would stop agents/students stopping to ask. Not applied (gated body edit; left for the prompt owner).

### M4–M6 retest 2026-05-26 — CLOSES the "M4→M5 fork addressing" pre-cohort `[watch]`

Retested fix (a) (`ae101-m4-commit-starting-point` + `ae101-m5-rerun-packaged` record the branch via `git branch --show-current` read-back, committed `67a90d7`). Ran the chain SPLIT — `--to m4`, inspect the hand-off **before** the M5-leg reconcile, then `--from m5 --to m6` — because the reconcile would otherwise mask exactly what the watch tracks. Result: **watch closed, deleted from `pre-cohort-todos.md`.**

- [x] **Fix (a) holds — recorded `Branch:` matches the commit-bearing branch (static, pre-reconcile).** M4 run `20260526-170524-18635`: agent created `m4/deadlock-terminal-state` (its own slug, NOT the wrapper's `m4/blocker-deadlock-terminal`), committed `M4 starting point` `1dbbfab` there, and wrote `Branch: m4/deadlock-terminal-state` into task.md's Run-coordinates block via read-back. `git branch --contains 1dbbfab` → `m4/deadlock-terminal-state` only. Recorded branch == the one branch carrying the commit. The drift the watch feared can't happen when the coordinate is read back from git.
- [x] **End-to-end — `ae101-m5-worktree-setup` forks without the "ask me for the SHA" fallback.** M5 run `20260526-172618-26508` PA: `PASS PA: worktree forked at M4 starting point 1dbbfab (m4/deadlock-terminal-state)`, new branch `m5/deadlock-terminal-state`. The PA transcript shows the agent reasoning *past* the fallback, not hitting it: it saw five `M4 starting point` commits across branches (the "ambiguous" trigger), scoped to the named branch per the prompt, found one, and stated *"no need to ask for a SHA."* It also caught the two-`Branch:`-lines case unprompted (task.md header line 3 still says `m4/blocker-deadlock-terminal`; the agent chose the authoritative Run-coordinates branch). Every `ask me`/`AskUserQuestion` grep hit is in the prompt text, never the agent's actions. M5 PASS all phases, **M6 PASS** (`20260526-175044-37671`, authored `session-shaper-lemmings`, arc note saved).
- [ ] **FINDING (runner — wrapper reconcile now reads the WRONG `Branch:` line + is redundant post-fix-(a)).** `chain-lemmings.sh`'s M5-leg reconcile does `grep 'Branch:' task.md | head -1`, which grabs the STALE task-body line (line 3, `m4/blocker-deadlock-terminal`) — NOT the Run-coordinates block (`m4/deadlock-terminal-state`) that `ae101-m5-worktree-setup` actually reads. Observed live this run: `reconciling: point m4/blocker-deadlock-terminal at M4 starting point 1dbbfab`. So the reconcile force-points an UNUSED branch; it neither helps nor masks M5's real fork path. Two consequences: (1) it does NOT mask a Run-coordinates fix-(a) regression (it targets a different branch), so even a one-shot run would now surface one; (2) with fix (a) recording the Run-coordinates branch correctly, the reconcile is **redundant** and can be dropped, OR — if kept as defence — should grep the Run-coordinates block (`awk '/## Run coordinates/{f=1} f&&/Branch:/'`) instead of `head -1`. Harmless today; left as a finding (out of this retest's scope).
- [x] **arrange-lemmings dirty-tree gap fixed (commit `63f8834`).** The mid-killed-sweep left uncommitted tracked changes in the SUT; `arrange`'s `git checkout -B` aborted and the chain died at arrange. arrange now snapshots the dirty tree to its backup then `reset --hard` + `clean -fd` (no `-x`, so the gitignored Stop hook survives) before the baseline checkout. Regression test `tests/arrange-dirty-tree.test.sh`.

## Caught 2026-05-26 picoshare M3 inverse-ADR-drift

- [x] **`threat-model-with-stride-3` location pin landed across all three m3-main scenarios 2026-05-26.** Picoshare M3 dry-run (transcript `~/.claude/projects/-Users-anttitevanlinna-Projects-picoshare/d9c04cc0-…jsonl`, msg `015SGcrgYDYsanwb8kLUUytd`) showed the agent write the ADR correctly to `/Users/anttitevanlinna/Projects/picoshare/docs/adr/0001-…md` and then proactively ask *"If the ADR belongs on the m3 branch instead, say so and I'll move it"* — the next-turn literal *"Yep. Just give it to me and save like you want it"* answered the move question affirmatively (the literal was meant as the *save* approval, not the *relocate* approval). The agent then cherry-picked/committed the ADR to `m3/quality`, the opposite of the cohort-correct outcome. The exercise body of `curriculum/exercises/threat-model-with-stride.md` (line ~50) anticipates the OPPOSITE drift (agent writes into worktree; student tells Claude to move it back to main); the inverse-drift is new and the open-ended literal is the amplifier. Fix: `threat-model-with-stride-3` line in all three m3-main scenarios now carries a tail pinning the ADR to `docs/adr/` in the main working tree. Real cohort students still hit the load-bearing surprise (body still teaches the drift); the runner cuts to the cohort-correct outcome. Files touched: `scenarios/m3-main.txt`, `scenarios/m3-main-picoshare.txt`, `scenarios/m3-main-codesearch.txt`.

- [ ] **Sibling latent finding — generic-permissive save-gate literals (not applied here, flagged).** The literal *"Yep. Just give it to me and save like you want it"* is generic-approve, open to *whatever you want*. The picoshare drift used "save like you want it" as latitude to RELOCATE, not just save. Any other save-gate where the agent self-proposes a structural choice between turns ("should I commit this on m1/...?", "should this go in module-N/ or outputs/?", "should I split into two files?") will see this literal as approval. Worth a one-pass sweep over M4–M6 save-gate literals when next revisited: `"yep save like you want it"` → `"save it the way you just described"` (pins to the explicit proposal, refuses latitude). Not done in this session — scope was M3.

## Caught 2026-05-26 picoshare M6 arc-note scan misses user-level memory

- [x] **`find_recent_md` now walks additional roots beyond the worktree (Fix 5, 2026-05-26).** The picoshare M6 run (2026-05-26) saved the practice-arc retrospective to `~/.claude/projects/-Users-anttitevanlinna-Projects-picoshare/memory/practice-arc-M1-M6.md` — the user-level home-keyed auto-memory home, not the worktree. The agent's reasoning was sound (cross-module retrospective is personal, doesn't fit the atomic-memo `observations/` shape — picks the home-keyed slot instead). Fix 4's worktree-only walk reported `m6_arc_note_paths: []` even though the note landed; state.json was lying. Fix: (a) `lib/assertions.sh:find_recent_md` now accepts additional roots after `$ref` (variadic, backwards-compatible — existing two-arg callers still work; missing roots silently skipped). (b) `run-m6.sh` derives `user_mem_dir="$transcripts_dir/memory"` near line 73 and passes it as the third arg to `find_recent_md` at line ~299. (c) `tests/lib-detection.test.sh` adds Fix 5 with four assertions: additional-root scan finds it, primary-root scan still works (regression), missing additional root is silently skipped, two-arg call backwards-compatible. Test runs RED before the fix, GREEN after. Depends on `encoded_cwd` being correct (current SUTs have no `.` in path; latent dot-encoding bug at line 79 of 2026-05-24 findings still applies if a dotted SUT path lands).

## Caught 2026-05-25 picoshare SUT port

- [x] **Skill-conflict handling diverges per SUT — observation only, no harness fix.** Codesearch's M3 `author-test-strategy-skill` produced `~/.claude/skills/test-strategy-codesearch/SKILL.md` (suffix-rename). Picoshare's same prompt kept the canonical `test-strategy` name and renamed the existing (lemmings) version to `SKILL.tiny-lemmings.bak.md` (rename-the-existing, replace). Different agent behavior on different SUTs from the same prompt body. **Real-cohort risk:** students running multiple SUTs see inconsistent skill state — the lemmings + codesearch + picoshare students each get a different shape, AND a returning student gets the "rename .bak" behavior the first-time student never sees. **Not directly fixable in the harness** (this is emergent prompt behavior, not runner code). Workaround if it matters: pre-rename or move-aside any conflicting `test-strategy` skill before M3 quality lane runs. Worth a curriculum-side note in the M3 module body about skill-conflict outcomes.

- [x] **Plan-file slug placeholder convention works but requires a manual post-M2 sync — convention now codified.** The M3 main scenario hardcodes the M2 plan-file slug in `map-the-access-surface-2`'s tail (codesearch uses `brisk-otter`; picoshare's first dry-run produced `vigilant-pangolin`). The `-4` tail's *"if a slightly different slug is present, use that one"* fallback handles cohort drift, but the canonical pin needs updating after the first successful M2 dry-run for each SUT. **Convention:** when adding an M3 scenario for a new SUT, write the slug placeholder as `<adj>-<animal>`, run M2 once, then grep the M2 state.json for `m2_plan_file`, then update the slug in `m3-main-<sut>.txt` line ~50 to the actual slug. The fallback fires for student runs that produce different slugs. Documented in `playgrounds/picoshare.maintainer.md` § M3 task.



Both runs PASSed end-to-end on the M1→M4 chain. M5 produced a real fix: three commits (fitness guard `aca8342` → packaging artefacts `dd7091b` → blocker-deadlock fix `e49b1ac`), `npm test` 7/7 green including the new `tests/blocker-terminal-no-scan.test.js` shape-check. M6 authored `~/.claude/skills/plan-gap-finder-lemmings/SKILL.md` and saved an arc note to `docs/notes/2026-05-25-rules-to-guards.md`. Bugs found:

- [x] ✅ wired 2026-05-25 — **Pre-existing target worktree path silently breaks the M5 contract.** Fix landed: run-m5.sh pre-flight refuses to launch if `worktree_cwd` already exists (~line 98); PA post-assertion resolves the `M4 starting point` SHA via `git log --grep` and asserts worktree HEAD matches it (~lines 315–322), not just dir existence. Before this run, `~/Projects/lemmings-m5` already existed from a prior unrelated dry-run (10-level campaign work). `ae101-m5-worktree-setup` would have tried `git worktree add ../lemmings-m5 <sha>` and hit git's refusal on a populated path. The runner's PA assertion is `[[ ! -d "$worktree_cwd" ]]` — checks dir existence only, doesn't verify the worktree was *created by this run*. A pre-existing path passes that assertion trivially even if the prompt's `git worktree add` errored. Workaround for this run: `git worktree move lemmings-m5 lemmings-m5-prior` before launch. **Real-cohort risk:** any student who reruns M5 (replay, swapped repo, second pass) hits this and the curriculum doesn't say what to do. **Runner fix:** PA pre-flight should refuse to launch if `worktree_cwd` exists; PA post-assertion should verify the worktree's HEAD matches the M4 starting-point SHA, not just that the dir exists.

- [x] ✅ wired 2026-05-25 — **`m5-state.json` `m5_verifier` field captures a code-fence fragment, not the saved path.** Fix landed: `pick_verifier_path` (lib/assertions.sh) picks from a before/after worktree file-list diff around the T4 save; run-m5.sh (~line 595) uses it, with a backtick-excluding scrollback grep as fallback only. Output of the 2026-05-25 run: `"m5_verifier": ".claude/hooks/\`,"`. The auto-discovery regex in `run-m5.sh:545` greps PB transcripts for `(/Users/[^[:space:]]+|~/[^[:space:]]+|\.claude/hooks/[^[:space:]]+|verifier\.[a-z]+)` then filters on `(verifier|hook|judge)` and takes `head -1`. Backtick code-fence fragments in Claude's prose match before the actual saved path does. The real verifier landed at `tests/blocker-terminal-no-scan.test.js` (npm test path, not `.claude/hooks/`). **Run-m6.sh does not read `m5_verifier`**, so the garbage doesn't propagate downstream — but the state.json claims a path that doesn't exist. Fix: drop the heuristic and `git status`-diff before/after the save-gate literal to find the real new path, OR add the verifier's path to the canned-reply literal so the runner doesn't have to guess.

- [x] ✅ wired 2026-05-25 — **PB-T4 pre-save tree empty → case-5 verifier-tree-advance assertion never fires.** Fix landed: `worktree_content_hash` (lib/assertions.sh) — a hash over names+contents of all non-ignored files — replaces `git stash create`; run-m5.sh (~lines 380/393) snapshots it plus a file list before/after the verifier save, so a fresh clean worktree still produces a real baseline. `tree_sha_before_verifier_save="$(cd "$worktree_cwd" && git stash create 2>/dev/null || true)"` returns empty on a freshly-forked clean worktree (nothing to stash). Then case 5's `if [[ -n "$tree_now" && "$tree_now" != "$tree_sha_before_verifier_save" ]]` falls through to the scrollback-grep fallback every time. PB-T5 PASSed on this run via the loose grep, not the tree-advance check the comment claims. Fix: use `git ls-files --cached --others --exclude-standard | sort | sha256sum` (or `git rev-parse HEAD` + a separate untracked-file list) as the baseline; OR check that the verifier path Claude named in T4's scrollback exists on disk by T5.

- [x] ✅ wired 2026-05-25 — **`m6_arc_note_paths` scan misses agent-chosen `docs/notes/`.** Fix landed: `find_recent_md` (lib/assertions.sh) walks the whole worktree for `*.md` newer than the run-start marker (`node_modules`/`.git` pruned, POSIX `-newer <reffile>` not the BSD-incompatible `-newermt @epoch`); run-m6.sh (~line 298) populates `m6_arc_note_paths` from it. Run output: `"m6_arc_note_paths": []`. Actual save: `docs/notes/2026-05-25-rules-to-guards.md` (the agent created `docs/notes/` for it). `run-m6.sh` walks four candidates: `.claude/memory`, `docs/adr`, `docs/adrs`, worktree root. The arc-retrospective prompt says destination is student-picked ("ADR / `.claude/memory/` memo / standalone file"), so a free-form picker can land anywhere. Fix: use a `find $sut_cwd -type f -name '*.md' -newermt "@$run_start_epoch"` walk with excludes for known noise (`node_modules`, `.git`), not a whitelist of dirs. False positives are recoverable (humans filter); false negatives mean state.json lies.

- [ ] **Pre-existing `~/.claude/skills/verify-by-hand-judge` (from codesearch agent's M6) leaks into lemmings M6.** Skills are user-level, not per-SUT, so any prior M6 run installs a skill that the next run sees. T4 chose a *different* name this time (`plan-gap-finder-lemmings`) so suffix-rename didn't fire, but only because the dominant gap was different. **Runner-hygiene fix:** before launching `run-m6.sh`, list `~/.claude/skills/` and either (a) prune skills authored by prior dry-runs into a `prior-runs/` namespace, or (b) snapshot the dir and restore at the end. **Curriculum-side risk lives in M3's pre-cohort-todos.md note** — same axis.

- [ ] **M6 makes zero commits to the worktree.** `m6_starting_sha == m6_ending_sha == e49b1ac`. By design — M6 ships a user-level skill (`~/.claude/skills/plan-gap-finder-lemmings/`) and an arc note (`docs/notes/...`). The runner's state.json shows no commit delta and that's correct. Worth a one-line comment in `run-m6.sh` so the next maintainer doesn't read it as a regression.

- [ ] **Reference-package coupling regression that the new skill noticed but didn't catch.** M5's reference.md prescribed `import { SKILLS }` to derive the blocker count — and the in-flight `fix/per-game-skill-budgets` branch drops that export. M5's run was green here but throws on rebase. `plan-gap-finder-lemmings` was authored explicitly to catch this class of prescribed-coupling break, and the agent's own T7 arc note calls out that **the new skill is "prose again"** — the same soft shape M4 proved gets rationalized. The dominant lesson lands at the file's end: *"stop trusting guards you only wrote and start trusting ones you've watched bind under the change you fear — perturb the dependency, don't just name it."* Not a runner bug; a real curriculum insight about where the practice's ratchet hardens next.

- [ ] **`.residue-archive/` from prior dry-runs is now baked into the M4 starting-point commit, riding into M5/M6's worktree forever.** Confirms the existing `ae101-m4-commit-starting-point` "stage everything" finding biting two modules downstream. Every M5/M6 dry-run after the residue-contamination event ships those files in the working tree. **Curriculum fix:** runner hygiene (clean residue before M3+M4); OR prompt body should say `git add -p` / explicit file list, not "stage everything."

## Caught 2026-05-24 M4 run

- [x] ✅ resolved 2026-05-26 — **`walk-and-send-off-3` routed `.claude/memory/` writes to USER-LEVEL `~/.claude/projects/<encoded-cwd>/memory/`.** Confirmed in M4 run `20260524-210132-96482` (Claude wrote `project_blocker_deadlock_no_scan.md` to user-level). **Fix that landed:** the knowledge home was renamed `.claude/memory/` → repo-root `observations/` across prompts + body + runner (`run-m4.sh` `proj_mem_dir`, all M3/M4/M5/M6 scenarios). Moving out of `.claude/` AND away from the word "memory" removes the auto-memory name reflex (the cause of the user-level routing) and the sensitive-path guard in one move. Supersedes the FINDING above about a §4 disambiguation line in the fork prompts — there's no longer a collision to disambiguate. See `training-architecture.md` §Knowledge-home rename. The `classify_memory_write` BUG branch now stands as a pure regression guard: a write to the user-level home would mean the reflex fired anyway.

- [ ] **Latent: `encoded_cwd` computation misses `.` → `-` (run-m1/m2/m4/m6.sh + run-m5.sh).** All use `sed 's|/|-|g'`, but Claude Code encodes BOTH `/` and `.` to `-` in the `~/.claude/projects/<encoded-cwd>/` folder name (verified 2026-05-26: `/private/tmp/mem-repro.vPLho3` → `-private-tmp-mem-repro-vPLho3`; macOS `/tmp` also resolves to `/private/tmp`). Latent because current SUT paths (`~/Projects/codesearch`, `picoshare`, `tiny-lemmings`) carry no dots, so `transcripts_dir` + `user_mem_dir` resolve correctly today. Bites the moment a SUT path contains a dot — the M4 in-repo BUG classifier and transcript discovery then point at the wrong dir and detect nothing. Fix: `sed 's|[/.]|-|g'` plus `pwd -P` to resolve symlinks. Per the repo's test-before-fix rule, add a case to `tests/lib-detection.test.sh` first (extract the encoding into a helper).

- [ ] **`ae101-m4-commit-starting-point` says "stage everything" — over-commits residue.** In M4 run `20260524-210132-96482`, the M4 starting-point commit `104f4c7` captured `.residue-archive/` (4 files from prior M1+M2 dry-run failures) alongside the M3 ADR + task.md. For a real cohort student this is the intended behavior (committing whatever's in cwd). For the mechanical battery this means residue files end up in the dry-run history. **Runner-hygiene fix:** clean residue OUT of cwd between M3 and M4 runs, OR use a sibling outside cwd for residue archive.

- [x] **run-m4.sh `m4_sha` grep is wrong** — fixed 2026-05-25 via `git log --grep='^M4 starting point$' -1`. See Done section.

- [ ] **run-m4.sh has no per-turn artifact assertions.** Same gap as run-m3.sh. M4 PASSed all 6 turns with zero assertions — only sentinel-only completion. The M4 prompts produce real artifacts (task.md, M4-starting-point commit, fix commit, fix branch). All went unverified. If any silently dropped, M4 would PASS green. Worth backporting the per-turn pattern.

## Caught 2026-05-24 M3 runs

- [x] **run-m3.sh's race loop polls indefinitely if a tmux pane dies externally** — fixed 2026-05-25 via `tmux has-session` check in the poll loop. See Done section.

- [ ] **run-m3.sh has no per-turn artifact assertions.** Unlike run-m1.sh and run-m2.sh which gate each turn on a contract (file exists, commit landed, mtime advanced), M3 is sentinel-only. The M3 retry PASSed green, but if the ADR save-gate at T8/T9 had silently dropped (the same failure mode the M1/M2 work caught at compound-and-close), M3 would also have PASSed green. The artifact contract for each M3 prompt is implicit: T1 fork-worktree → quality cwd exists; T8 ADR save → `docs/adr/<N>-*.md` lands; T11 closer → user-level skill authored. Worth backporting the per-turn assertion pattern. Quality side has its own contracts (the skill authoring at quality T2/T4).

- [ ] **Skill-authoring lands at user-level by default, suffix-renamed on conflict.** In the M3 retry, `author-test-strategy-skill` produced `~/.claude/skills/test-strategy-lemmings/SKILL.md` (suffix `-lemmings` because the original `test-strategy` skill already exists at the user level — and the codesearch agent's earlier run added `test-strategy-codesearch`). Real-cohort risk: first-time M3 students would just get `test-strategy/SKILL.md` — clean. But returning students or students with the original test-strategy skill installed get the suffix-rename behavior, and the curriculum doesn't address what happens on conflict. Worth a one-line callout in the exercise: "if a `test-strategy` skill already exists, Claude will suffix-rename — that's fine, look at the saved path."

## Caught 2026-05-24 run #4 — RESOLVED

- [x] **wait_for_turn fires correctly but pane cleanup may hang** — fixed via `pane_capture_safe`. See Done section.
- [x] **N=4 of M1 shows non-deterministic save-gate behavior at T4** — collapsed via m1.txt tail rewrite. See Done section.
- [ ] **N=4 also shows committing-early variant** — STILL OPEN, this is a curriculum question, not a runner bug. Run 4 proposed commit at T4 rather than waiting for T6. The curriculum DOES allow this (both prompts end "Show me the diff before you commit"). Real-cohort risk: students who commit at T4 then hit T5's "Don't change anything yet... what's still surface?" + T6's "do it properly TDD-style" may be confused. Worth a curriculum-side question: is commit-at-T4 desired or accidental?

## Routed from pre-cohort-todos.md 2026-05-25

Harness items lifted off the AE101 cohort punchlist — that file tracks cohort-blocking *curriculum* decisions; runner tooling lives here. The four already-fixed ones (worktree PA, m5_verifier, tree-empty baseline, arc-note scan) are marked `[x]` in the 2026-05-25 sections above. Still open:

- [ ] **Per-runner tmux sockets.** Every runner binds the default socket `/tmp/tmux-501/default`; a `tmux kill-server` from one runner — or any other Claude Code instance on a shared box — nukes every other runner's session. Codesearch M5 PC (2026-05-24) lost its session this way ~18 min into the packaged re-send: work landed on disk but the sentinel never fired and the runner hung. Fix: thread a per-runner socket through `lib/tmux.sh` — wrap its `tmux` calls in a `_tmux()` that injects `-L runner-$run_id`. The only direct tmux call outside that lib is run-m3.sh's race-loop `has-session` (~lines 235/239), which also needs the socket. Verify: launch two runners concurrently, `tmux -L <other-socket> kill-server`, confirm the first survives.
- [ ] **Mechanical-scenario save-gate hardening (M5/M6).** Scout + dry-run prediction — agent-initiated AskUserQuestion and "lock it in" save-gates silently drop writes in headless runs without an approval-literal tail. (a) `diagnose-and-resend-6` "lock it in" may drop reference.md + plan.md (same family as `threat-model-with-stride-3`) → add an approval-literal line to the M5 scenario. (b) `spot-gaps-build-the-loop-3` uses AUQ for the second-skill authoring conversation (same shape as `author-test-strategy-skill-1`) → add a lazy-dump tail to the M6 scenario. (c) `diagnose-and-resend-4` verifier-shape pick is a real judgment moment — the mechanical battery must pin one shape deliberately (the cohort student picks for real). Confirm/fix on first M5 + M6 mechanical run.
- [ ] **plan.md protected-block clobber check (M5).** `ae101-m5-rerun-packaged` writes a protected `Run coordinates` block into plan.md at run start, then mutates the rest of plan.md as it runs; the `(do not rewrite or remove)` heading is the only thing protecting the block. Verify it survives the agent's own plan edits on first M5 mechanical run. (task.md is frozen in the worktree fork — no clobber risk there.)

(M3 + M4 per-turn artifact assertions — porting the `assert_turn()` shape from run-m1/m2 — are already tracked above under the 2026-05-24 M3 + M4 sections.)

## Highest yield, smallest effort

- [ ] **Run M1 + M2 N=5+ on the same starting state.** Two runs is not a sample. Claude's behavior is non-deterministic. Findings like "ae101-m2-integrate-branch always integrates" come from N=2 — could be 80%, could be deterministic, no way to know. Capture: which assertions vary, which prompts produce stable artifacts vs which drift, which prompts depend on session priors that may not exist for every student.

- [ ] **Build a thin-M2 sim to test `ae101-m2-integrate-branch` "nothing earned, stop" branch.** Construct a deliberately bare M2 session (skip push-back, accept first plan, no second-pass walk-down) and run it through to the integrate-branch turn. Verify the "stop" branch can actually fire. If both branches always integrate, the conditional is hedging, not load-bearing — prompt could simplify.

- [ ] **Test the "no MCP connector" path on `compound-and-close-2` + `-3`.** Today's M1 scenario scopes both turns out. Include them, run with no tracker connector configured, observe what Claude actually does. The "graceful degradation" claim is currently unverified — could be a clean no-op turn (per the prompt's "say so and we'll create one" exit), could be a hang on a tool-call retry loop, could be a hallucinated ticket update.

## Medium yield

- [ ] **Test the replay-from-residue path at full strength.** Run M1 a second time on a lemmings repo where the FIRST run's `CLAUDE.local.md` is still in place (auto-loaded, not moved aside). Compare what compound-and-close-1 writes vs. what the first run produced. Real-cohort risk lives here (any student who reruns M1, swaps repos mid-training, or hits the supported "replay" path). My current runs reset state between runs — that's an artificial cleanliness.

- [ ] **Test M2 push-back-on-the-plan-2 in REAL plan mode** (no AskUserQuestion-no guard, manual Shift+Tab engagement). The "walk all branches in one pass" tail I use bypasses the 3-at-a-time interactive design. Findings on `push-back-on-the-plan-2` today are findings about the workaround, not about the prompt. Need a non-headless walk-through (or a runner that drives Shift+Tab).

- [ ] **Tighten the soft assertions.** T5 (`assert_or_warn` deeper-named), T7 in M2 (slack|webhook|...), T9 fallback grep — all loose enough that a hallucinated plausible-sounding response passes. Adversarial test: feed a deliberately-wrong response into the assertion harness and see which assertions notice. Anything that passes on garbage is testing nothing.

## Lower yield, higher effort

- [ ] **Adversarial scenario battery.** Deliberately bad task descriptions, wrong bug claims, contradictory tail context, residue-from-different-repo, malformed plan-mode answers. Run M1+M2 against each. Where do the prompts produce nonsense, and which assertions notice? This is the failure-mode probe the current happy-path battery doesn't do.

- [ ] **SUT-portability check.** Lemmings is one SUT. The codesearch playground (other agent's work, see `playgrounds/codesearch.maintainer.md`) is starting to probe portability. Once it stabilises, run the same M1+M2 scenarios against it and tag which findings were lemmings-specific vs which generalise. Today's `pre-cohort-todos.md` entries don't make this distinction — they could be SUT artifacts dressed as curriculum findings.

- [ ] **Build a "Claude went off the rails" detector.** Today's assertions catch artifact-contract violations (file missing, mtime not advanced). They don't catch semantic drift (Claude wrote a plan that doesn't address the task; Claude wrote a commit message that's a confabulation; Claude integrated rules from a different repo). A separate Judge-style pass against final artifacts (Haiku, cheap) could catch the "Claude looked busy but didn't deliver" class. Pairs with the script-ratchet principle (`check_platform_and_boundaries.md §17`) — start with prose-prose match against the prompt's stated contract.

## Meta — process improvements for future sessions

- [ ] **Stop conflating runner PASS with curriculum quality.** They're two layers (per `check_platform_and_boundaries.md §16`). The runner asserts contracts held; content quality is a separate audit. When I report "M1 PASSed all turns," that does NOT mean the curriculum is good — it means the artifact contracts held. Repeat this distinction in every report.

- [ ] **Stop folding to pushback without falsification.** When the user pushes back on a finding, the right move is: design a falsification test, run it, report what happened. The reflexive "you might be right elsewhere too, let me withdraw the rest" is sycophantic and erases real evidence. Document this in self-review-protocol.

- [ ] **Always read full transcripts before judging output.** Sampling final artifacts (commits, plan files, rules files) is the same shortcut as reviewing a PR by reading its description. Mid-turn behavior is where subtle prompt friction lives. Schedule transcript-reading as a separate step, not "I'll glance at the end."

- [ ] **Name what's untested explicitly in every findings report.** Today's report includes claims like "the chain is graceful in absence of tracker" with no observation behind them. Future reports prefix every claim with the evidence type: `[observed N=2]` / `[reasoned]` / `[untested]`. Saves me and the maintainer from rounding extrapolation into observation.

## Process — when to invoke this list

End of each significant runner-mechanic session. Pick one item, do it, delete the bullet. If the same bullet hasn't been picked after three sessions, downgrade or delete (it was a nice-to-have, not a real need).

---

## Findings from the 2026-05-24 transcript-by-transcript read

Read M1 run `20260524-145026-64490` (turns 1, 2, 6, 7) + M2 run `20260524-151314-79933` (turns 1, 6). Each finding tagged `[observed]`, `[inferred from N=1]`, or `[needs more data]`.

**F1. Orient-and-introspect-1 reads `.claude/settings.local.json` and identifies the eval harness.** [observed] Claude's T1 narrative says verbatim: *"This isn't a normal repo — it's a Claude-agent eval harness dressed as a game, part of an agents-102 curriculum. Three tells: (1) The .claude/settings.local.json Stop hook points at agents-102/curriculum/evals/mechanical/tmux-runner/hooks/stop-sentinel.sh. (2) The branches are eval scenarios... (3) The untracked CLAUDE.local.md.* files are session residue."* Claude knows it's being evaluated. **Mechanical-battery side effect:** the harness leaks its own existence into the SUT via the Stop hook installation. Findings about Claude's M1 behavior may include a "knows-it-is-tested" calibration that doesn't transfer to a real cohort student's repo. The real-cohort student installs no Stop hook; orient reads a clean repo. Worth thinking about whether the mechanical-runner findings overstate or understate cohort behavior.

**F2. M1 T6 (`fix-tests-first-3`) ends with TWO questions, not one.** [observed N=1] Exact tail: *"I haven't committed. Want me to commit this on m1/fix-hud-tally and open a PR (per your gh-not-direct-push convention)? And shall I fold the isResolved consolidation into the same PR as a clearly-separated refactor commit, or leave it for its own?"* The runner answers with one literal (`* One commit, no PR — just commit and move on.`). Claude correctly synthesized both answers (committed one commit, did NOT fold the refactor), but the second answer was inferred from "just commit and move on" — a different literal phrasing could have produced a different commit shape. **Real-curriculum surface:** the prompt's "Show me the diff before you commit" doesn't bound how many decisions Claude offers. In a real cohort, this opens conversation; for the mechanical-runner, it's a friction point that depends on Claude's prose-inference quality.

**F3. The "gh-not-direct-push convention" Claude references in T7 comes from residue.** [inferred from N=1] On my clean-baseline runs (residue moved aside), Claude shouldn't see this rule — but residue under `.preserved` / `.residue-after-*` sibling filenames is still readable via `ls` + `Read`. Orient picks up these residue files (per F1's "three tells") and the rules they contain. **Real-curriculum risk:** T7's commit shape (branch / PR / direct) on M1 RERUN depends on what rules are in cwd from prior runs. A first-ever M1 student gets default Claude behavior; a returning student gets rules-conditioned behavior. The exercise design implicitly assumes one path but doesn't say which.

**F4. `git diff` doesn't show untracked files; M1 `fix-tests-first-3` says "show me the diff."** [observed N=1] In T6, Claude self-flagged this gap: *"Plus the untracked files (won't show in git diff): package.json, tests/_dom-stub.js, and the rewritten tests/main.test.js (3 tests)."* Claude self-corrected. A less self-aware session might commit only the tracked changes. **Potential prompt sharpening (low confidence):** "Show me the diff and any new files before you commit." Defer the call — Claude's self-awareness handled it in N=1. Worth observing across more runs.

**F5. M2 T1 plan-mode prompt produces trace-based reasoning by default; hand-execution is a separate behavior that happens sometimes.** [needs more data] In this run, Claude in T1 traced the bash code by reading and identified the head-row bug. In a different M2 run (`20260524-091831-33155`, the FAIL run), the plan file content showed hand-execution: *"I ran the real lemming.js against the real Level-1 terrain in a throwaway harness."* Same prompt, different behavior. **The plan quality depends on whether Claude chose trace-vs-execute.** Not always reproducible. The prompt body says "Explore the files you need to understand the scope" — neither requires nor forbids execution. For a real cohort, students who get trace-only plans may miss what execution would find.

**F6. M2 T6 (`extract-the-task-shaping-rule-2`) correctly inferred prior task-shaping.md existence + integrated cleanly + self-corrected file drift.** [observed N=1] Caught that the file's frontmatter said "Fourteen rules" but the actual count was 18; fixed it. High-quality integration behavior. The "integrate, don't overwrite" instruction worked as designed when the file already existed from a prior session. **What we DON'T know:** does this work as well on first-ever M2 (no prior task-shaping.md, no @import line in CLAUDE.local.md)? Likely yes, but I haven't tested the cold-start path.

**F7. The `gh` / commit-shape conventions Claude consults come from where the curriculum doesn't pin.** [observed N=1] Composite finding from F2+F3+F7: Claude's T7 decisions depend on (a) what's in residue files, (b) what's in CLAUDE.local.md from prior runs, (c) what's in `~/.claude/CLAUDE.md` user-level. The exercise body and prompts don't fully specify which rules govern. **Real-cohort risk:** a student with a heavily-customized user-level CLAUDE.md gets different M1 behavior than a fresh laptop install. Not necessarily wrong (their rules SHOULD govern), but the curriculum's mood and outcomes vary by student-environment.

## What I'd do next, given these findings

- **F1+F3+F7 cluster** suggests a single experiment: run M1 on a totally-virgin laptop (no `~/.claude/CLAUDE.md`, no project residue, no auto-loaded rules), and compare T7's commit behavior + CLAUDE.local.md content to a "heavily-customized" run. The delta names what the curriculum implicitly assumes vs what it explicitly sets.
- **F4** suggests reading more M1 runs to see how often Claude self-flags the diff-vs-untracked gap. If <100%, prompt could harden.
- **F5** suggests a deliberate trace-only vs execute-encouraged variation on push-back-on-the-plan-1 to see whether execution-encouraged plans are systematically better.

These are all "studies to run," not "fixes to ship."

