# Judge report — AE101 M5 chain (script-only)
## Summary
Verdict: **PASS 29/29**. Generated by bin/judge-ae101-m5.sh — no LLM.

## Assertions
| ID | Verdict | Evidence |
|---|---|---|
| V1 | PASS | Read of learn-from-the-test/prompt-001.txt confirmed |
| V2 | PASS | Read of diagnose-and-resend/prompt-001.txt confirmed |
| V3 | PASS | Read of diagnose-and-resend/prompt-002.txt confirmed |
| V4 | PASS | Read of diagnose-and-resend/prompt-003.txt confirmed |
| V5 | PASS | Read of diagnose-and-resend/prompt-004.txt confirmed |
| V6 | PASS | Read of diagnose-and-resend/prompt-005.txt confirmed |
| V7 | PASS | Read of diagnose-and-resend/prompt-006.txt confirmed |
| V8 | PASS | Read of learn-from-the-test/prompt-002.txt confirmed |
| B1 | PASS | m5/ branch exists |
| B2 | PASS | m4/ branch preserved from arrange |
| T1 | PASS | M4 transcript stub Read in transcript |
| D1 | PASS | all 3 failure modes named in Phase 3 |
| VR1 | PASS | csv-escape-check.sh exists |
| R1 | PASS | reference.md has Done means + Tests sections |
| P1 | PASS | plan.md has plan structure (10 keywords) |
| C1 | PASS | CLI has RFC 4180 escaping (M4 gap closed) |
| C2 | PASS | README mentions export (M4 gap closed) |
| C3 | PASS | package.json bin entry wired (M4 gap closed) |
| C4 | PASS | RUN-NOTES.md present |
| S1 | PASS | transcript-path substitution logged |
| S2 | PASS | worktree substitution logged |
| S3 | PASS | walk-away substitution logged |
| H1 | PASS | no exercises/ Read |
| H2 | PASS | no prompts/ Read |
| H3 | PASS | no judge/maintainer Read |
| PF1 | PASS | Verdict: READY — 6 phase(s) checked against diagnose-and-resend, mapping agrees, all prompts covered. |
| PF2 | PASS | Verdict: READY — 2 phase(s) checked against learn-from-the-test, mapping agrees, all prompts covered. |
| PS1 | PASS | BLOCK on pre-existing curriculum source (informational): Verdict: BLOCK |
| PS2 | PASS | BLOCK on pre-existing curriculum source (informational): Verdict: BLOCK |

## Pre-flight

### PF1
```
PASS — Phase 2 (find previous transcript) → prompt-1: keyword match (hits_here=3, hits_elsewhere_max=3).
PASS — Phase 3 (three failure lenses) → prompt-2: keyword match (hits_here=3, hits_elsewhere_max=2).
PASS — Phase 4 (walk failures into validation) → prompt-3: keyword match (hits_here=3, hits_elsewhere_max=1).
PASS — Phase 5 (build verifier) → prompt-4: keyword match (hits_here=2, hits_elsewhere_max=2).
PASS — Phase 6 (smoke test verifier) → prompt-5: keyword match (hits_here=3, hits_elsewhere_max=2).
PASS — Phase 7 (reference and plan) → prompt-6: keyword match (hits_here=2, hits_elsewhere_max=2).
Verdict: READY — 6 phase(s) checked against diagnose-and-resend, mapping agrees, all prompts covered.
```

### PF2
```
PASS — Phase 1 (worktree setup substitute) → prompt-1: keyword match (hits_here=1, hits_elsewhere_max=0).
PASS — Phase 8 (re run packaged) → prompt-2: keyword match (hits_here=1, hits_elsewhere_max=0).
Verdict: READY — 2 phase(s) checked against learn-from-the-test, mapping agrees, all prompts covered.
```

## Prompt-source audit

### PS1
```
# Prompt-source audit — diagnose-and-resend

Inputs:
- Fenced prompts: /tmp/prompts/diagnose-and-resend/prompt-*.txt
- Exercise body:  curriculum/exercises/diagnose-and-resend.md

## P-checks (fenced prompts)

  Sev-2: P3 markdown shout in prompt-002.txt: 7:- **Goal drift** — moments where the agent solved an adjacent problem instead of the asked one. Buried instruction, redirected scope.
  Sev-1: P4 curriculum vocab in prompt-002.txt: 3:Read what the un-packaged agent did. Find the previous-run branch (run `git branch -a | grep '/m4/'` — it'll be the only branch starting with `m4/`). Start with the repo state on that branch: commits since the "M4 starting point" commit, files modified, branch state. Then read the previous session's transcript at the path you just found. The file changes alone miss the drift and the dead-ends.
  Sev-2: P3 markdown shout in prompt-003.txt: 5:- **A re-readable spec.** Scope + success criteria + constraints the agent can diff its in-progress work against, mid-run. Holds the goal pinned down when the conversation grows.
  FLAG:  P5 open-hook in prompt-004.txt (last char is colon — verify student owns the value)
  Sev-2: P3 markdown shout in prompt-006.txt: 7:- **The tests that name the bar** — scoped to this task's core paths, named before any code lands. Tests are a first-class part of the task spec for anything load-bearing; throwaway experiments can skip. Where a core requirement resists being named as a test, flag it as a question, not a rule.

## E-checks (exercise body)

  FLAG:  E4 cross-module artefact ref (verify stable identifier in body): 30:Ask Claude to read the repo state on the previous-run branch and the transcript at the path you just found, then walk the work through three failure-mode lenses with quoted moments.
  FLAG:  E5 session-open (verify working dir named): 7:Open a new Claude Code session in the M5 worktree at `../<repo>-m5` (set up at module open).
  FLAG:  E5 session-open (verify working dir named): 21:Open a new Claude Code session in the M5 worktree (set up at module open). The M4 artefact lives in two places: the repo's git history (commits made by the M4 agent on the `m4/<slug>` branch, files modified, branch state, all visible from the worktree via git refs) and the M4 session transcript under `~/.claude/projects/`. Claude Code stores every session's scrollback there in a folder matching this repo. A fresh Claude can find and read it. File changes tell you *what* the agent did; the transcript tells you *how* it got there, including the drift and dead-ends.

## Summary

Sev-1: 1
Sev-2: 3
FLAG:  4

Verdict: BLOCK
```

### PS2
```
# Prompt-source audit — learn-from-the-test

Inputs:
- Fenced prompts: /tmp/prompts/learn-from-the-test/prompt-*.txt
- Exercise body:  curriculum/trainings/agentic-engineering-101/learn-from-the-test.md

## P-checks (fenced prompts)

  Sev-1: P4 curriculum vocab in prompt-001.txt: 1:Find the branch starting with `m4/` (run `git branch -a | grep '/m4/'` — there'll be one) and the commit messaged "M4 starting point" on it. Spin up a git worktree at that commit. Put the worktree at `../<repo-name>-m5` on a new branch named `m5/` followed by the same task slug as the m4 branch.

## E-checks (exercise body)

  FLAG:  E3 tease-the-payoff header candidate: 3:## Big Idea
  FLAG:  E3 tease-the-payoff header candidate: 6:## Prework
  FLAG:  E3 tease-the-payoff header candidate: 32:## Start here
  FLAG:  E3 tease-the-payoff header candidate: 42:## Key Concepts
  FLAG:  E3 tease-the-payoff header candidate: 62:## Next
  FLAG:  E5 session-open (verify working dir named): 30:Open a new Claude Code session in the worktree. The rest of Module 5 runs there.

## Summary

Sev-1: 1
Sev-2: 0
FLAG:  6

Verdict: BLOCK
```
