# M6 (AE101) — Spot gaps, build the loop + Arc retrospective. codesearch variant.
# Driven by run-m6.sh with SCENARIO=this-file. Single session in the M5
# worktree (`../codesearch-m5` or whatever M5 named it). M5 ended on the
# m5/<task-slug> branch with the packaged re-run of the M3 ADR (today's
# I-class clamp on the /show handler — filepath.EvalSymlinks + a
# separator-terminated ix.Roots() prefix, applied to both the file-read
# and directory-listing branches).
#
# Carries forward:
#  - M1's siftUp fix (in the main repo; visible to the worktree via the
#    shared .git tree)
#  - M2's plan at ~/.claude/plans/<slug>.md (proposed --bind + /api/search;
#    not implemented on this branch — M4 implemented the M3 ADR instead)
#  - M3's threat-model ADR at docs/adr/0001-*.md (today's slug is
#    `clamp-csweb-show-to-index-roots.md`; the I-class filter on /show)
#  - M3's test-strategy skill at ~/.claude/skills/test-strategy-codesearch/SKILL.md
#    (codesearch-aware: flags csweb as greenfield, names the path-clamp
#    predicate as the load-bearing unit-test, carves out the actual
#    socket/bind + a real index build as verify-by-hand)
#  - M4's implementation: cmd/csweb/web.go (withinRoots() + clamp on
#    both show() branches + index opened once at startup), cmd/csweb/web_test.go
#    (csweb's first tests — symlink-escape, separator-collision, dir-listing,
#    nonexistent, dotdot; all real temp tree + real symlink), README
#  - M5's verifier + plan.md + reference.md + RUN-NOTES.md + packaged re-run
#  - CLAUDE.local.md compounded across M1-M5
#
# AUQ suppression discipline — same as m6.txt. T6 uses the ask-and-wait
# pattern (not suppression): the tail tells Claude to ask in chat
# (numbered list) and STOP; a literal-text reply turn simulates a real
# student answering. The answers describe codesearch Go reality + the
# dominant gap as it shows up on this task (httptest covers the clamp
# logic, but the actual socket/bind + real index build are verify-by-hand
# — and the skill's verify-by-hand carve-out exists but doesn't enforce
# a pre-run check that the human actually ran cindex + csweb + curl
# /show/<path> before declaring done). Save-gate literal after T6 is
# the explicit SKILL.md write approval.

# T1: diff two runs across four dimensions. Large reading task; suppress
# AUQ. The M4 run produced cmd/csweb/web.go (withinRoots + clamp on both
# branches + index-opened-once-at-startup) + web_test.go (symlink-escape,
# separator-collision, dir-listing, nonexistent, dotdot) + README; the
# M5 packaged re-run sharpened the verifier and re-ran. Codesearch-specific
# gaps to expect in the diff: (a) httptest covers the clamp predicate
# table-driven but cannot exercise the real socket/bind under a real index
# — the test-strategy skill flagged this in M3 but the packaged run may
# have drifted on what "tested" meant for the end-to-end clamp; (b) symlink
# resolution semantics on macOS (case-insensitive FS, but path comparison
# isn't) for show()'s path-clamp; (c) over-fitted verifier checked
# `go test ./...` green but didn't catch that the verify-by-hand step
# (`cindex` + `./csweb -verbose` + `curl /show/<path>` against a real
# index) was skipped.
spot-gaps-build-the-loop-1 Don't call AskUserQuestion — read what's on disk (transcripts at paths recorded in task.md and plan.md, branches m4/<slug> and m5/<slug>, the test-strategy skill at ~/.claude/skills/test-strategy-codesearch/SKILL.md, the ADR at docs/adr/0001-*.md — today's slug is `clamp-csweb-show-to-index-roots.md`) and produce the four-dimension diff inline. Quote moments from both run transcripts and from specific commits / files. If something's genuinely unreadable, name the gap rather than asking.

# T2: cut one stale rule (or stop). Rules in CLAUDE.local.md compounded
# across M1-M5 on codesearch — M1's siftUp/heap rules, M2's plan/auth-
# scope rules, M3's threat-model rules, M4/M5's run-discipline rules.
# Some may have aged out by now (e.g. M2's "csweb has zero tests today"
# is no longer true after M4 added web_test.go).
spot-gaps-build-the-loop-2 Don't call AskUserQuestion. If your diagnosis killed exactly one rule, cut it from ./CLAUDE.local.md in place and show the diff in chat afterward. If every rule still holds, say so and stop.

# T3: wider look across the whole stack. Scans ~/.claude/projects/
# across every repo — SUT-independent (the student's own session
# history, not the codesearch tree), so this tail is identical to the
# m6.txt / picoshare variants. Reading-heavy like T1; suppress AUQ on
# the prompt's "ask me where your other repos are" branch, bound the read.
spot-gaps-build-the-loop-study Don't call AskUserQuestion — read across what's already under ~/.claude/projects/; don't ask me where other repos are, a representative pass over the sessions on disk is enough. Group the recurring kinds of work, name each group, quote an instance or two, rank the top six or so. If a folder won't read, name the gap rather than asking.

# T4: draw the top recurring work-shapes as mermaid diagrams. Consumes
# the T3 inventory; feeds T6 (author). Tail identical across variants.
spot-gaps-build-the-loop-shapes Don't call AskUserQuestion — draw the top few groups from the inventory you just produced as mermaid diagrams (steps in order, branches, loop-backs), 30 nodes max each. Use your judgment on which few; you don't need my pick.

# T5: atomic primitives menu. Codesearch is Go — primitives will land
# Go-shaped (go test, go vet, golangci-lint, gofmt, manual curl,
# httptest, race detector, build, cindex + csweb against a real index,
# etc.). The ranked picks against the dominant gap will likely surface
# "real-binary + real-index + curl /show/<path>" as the verify-by-hand
# carve-out the next-iteration skill has to enforce evidence for.
spot-gaps-build-the-loop-primitives Don't call AskUserQuestion — produce the list inline. The 5-10 primitives + 2-3 ranked picks all land in one chat reply. Codesearch is Go; primitives like `go test ./...`, `go vet`, manual `cindex` + `./csweb -verbose` + curl against /show/<path>, httptest.NewRecorder for handler-level tests, race detector, gofmt are in scope. Generic "tests" doesn't make the cut; "run go test ./... against the changed package" or "curl /show/<symlink-escape-path> against a real cindex-built index" does.

# T6: author the skill. Ask-and-wait pattern (NOT suppression): tail
# tells Claude to ask in chat (numbered list) and STOP. Literal-text
# reply turn directly after carries the canned student-voice answers
# (Go-shaped, codesearch-specific). The save-gate literal after the
# reply approves the SKILL.md write.
spot-gaps-build-the-loop-3 Don't call AskUserQuestion — ask your interview questions as a numbered list in your reply (plain chat text) and STOP, don't write the skill file yet. I'll answer in a follow-up message. Push back if my answers are generic. After I answer, ask follow-ups the same way (numbered, in chat) if my answers leave gaps. When you have enough, tell me you're ready to write the skill and wait for me to say save.

* answers: 1) Shape — judge. The M5 verifier was deterministic (`go test ./...` green, build clean), but the gap that showed up was qualitative: the agent declared done after `go test ./...` passed even though the test-strategy skill explicitly carved out the real socket bind + real-index build (`cindex` → `./csweb -verbose` → `curl /show/<path>`) as verify-by-hand — and nobody actually ran it. The judge fires on the "I'm done" signal and asks: was the verify-by-hand step actually run? If unclear from the run notes, the judge flags it. 2) Fires on — final summary + RUN-NOTES.md at end of a long-running task. Reads the test-strategy skill's verify-by-hand carve-outs and checks whether each one is named with evidence (a quoted command output, an inline note "I ran `cindex $HOME/Projects/codesearch && ./csweb -verbose &; curl -i http://localhost:2473/show/<escaped-path>` and got 404") in the run notes. If a verify-by-hand item is in the skill but not evidenced in the notes, flag. 3) Quality bar — every verify-by-hand item in the skill that's in scope for the task either (a) has evidence in RUN-NOTES.md or the final summary, or (b) is explicitly named as deferred with a one-line why. Hand-wavy "I tested it" doesn't pass. 4) Flag vs let through — flag: verify-by-hand items in scope but unmentioned, claims of "tested" without quoted evidence, evidence that's actually the wrong test (httptest.NewRecorder output cited as proof the real bind + real index path-clamp works end-to-end). Let through: explicit deferral with reason, evidence in the form of quoted command + output, or evidence of an actual manual run (timestamp + observed behavior). 5) Output — ranked findings, severity-ordered (BLOCKER for unmentioned verify-by-hand in scope; WARN for thin evidence; OK if the skill's carve-outs all have matching evidence), each with the skill's verify-by-hand line quoted + the run-notes section that should have addressed it. 6) Failure shape when the skill is wrong — false-positive flagging on tasks where the verify-by-hand carve-out isn't actually in scope for the change (a refactor that doesn't touch show() doesn't need the curl-against-real-index evidence). Diagnostic: the judge should scope its check to the files the run touched, not the whole skill. 7) Skill name — `session-shaper-codesearch` (suffix by SUT so it doesn't collide at user scope with other codebases' session-shaper skill); write it to `~/.claude/skills/session-shaper-codesearch/SKILL.md`. anything else, use your judgment; I'm not micromanaging.

* save it

# T7: critique before shipping. Same session — charitable by design.
spot-gaps-build-the-loop-4 Don't call AskUserQuestion. Name three weak parts in chat — be harsher than necessary, assume worse than it looks.

# T8: invoke skill by name on M5 packaged re-run + same-turn judgement.
# The "M5 packaged re-run" here = the m5/<slug> branch's commits + files
# + RUN-NOTES.md + the M5 session transcript. The skill should fire on
# the run's final summary / RUN-NOTES.md and check whether the
# test-strategy skill's verify-by-hand carve-out for the real-binary +
# real-index + curl /show/<path> evidence was actually present.
spot-gaps-build-the-loop-5 Don't call AskUserQuestion. Invoke the skill we just authored by name on the M5 packaged re-run (read m5/<slug> branch commits, files touched, RUN-NOTES.md, M5 transcript at the path recorded in plan.md's Run coordinates block). Produce its ranked-findings output, then answer the three judgement questions in the same response.

# T9: arc retrospective. Reads M1-M6 artefacts: CLAUDE.local.md +
# observations/ + docs/adr/0001 + both skills (test-strategy from M3
# + the new one from T6) + both run artefacts. Dispatches a fresh
# Agent-tool sub-task; suppress AUQ on the outer conversation.
arc-retrospective-1 Don't call AskUserQuestion in the outer conversation — the fresh sub-task may use its own judgment internally. Quote from my files. The codesearch arc has six modules of artefacts on disk (M1 siftUp/heap fix + tests, M2 plan, M3 I-class clamp ADR + test-strategy skill, M4 implementation — withinRoots() + clamp on both show() branches + first csweb tests, M5 packaged re-run, M6 second skill). If artefacts are thin at any module, name the gap rather than inventing a pattern to tidy the arc. Show me the note before you save it.

* looks good, save it where you proposed.
