# Blend implementation, M1 + M2 (2026-09-23)

Executes `blend.md` § Titles, M1 and M2. Six files were in scope; five were touched.
`exercises/personal-site-with-guardrails.md` was not: the blend asks nothing of it, and the M1
slides it feeds (Phase 4 as learning C's plant) are citation, not edit.

---

## `trainings/agents-101/getting-going.md`

**Headers now present:** unchanged (`Big Idea`, `Prework`, `What You'll Learn`, `Start here`,
`Debrief`, `Push back on what the retro missed`, `Get a cold read`, `Key Concepts`,
`Bring to Module 2`, `Pre-reads before Module 2`, `Next`).

**Lines added:** none in body.

**Lines cut**, from § Start here:

> **Context is king.** You steer your agent by the context you give it. You paint by choosing the brush.

**Slide-size check:** `files: 1  slides: 11  oversized: 0` — every slide within limits.

**Maintainer note appended:** `**Story blend, M1 (2026-09-23).**` — § Start here carries no thesis
line; Big Idea holds the claim; do not restore a second statement of it above the lecture link.

---

## `lectures/context-is-king.md`

**Headers now present, in order:**

1. `## Same question, two answers`
2. `## It reads the whole conversation every time`
3. `## Context is whatever you tell it`
4. `## The first piece of the picture`
5. `## A file it reads every time`

**Lines added:** none. The refrain paragraph already in the body now has its own slide:

> In the full agent picture, this is the first piece: context. Later you add tools, goals, checks, boundaries, and loops. None of them work unless the agent reads the right world first.

It moves from inside chunk 2 to its own chunk after the role example, which is the blend's order and
the widest statement of context in the lecture.

**Lines cut:** none.

**Slide-size check:** `files: 1  slides: 5  oversized: 0` — every slide within limits.

**Maintainer note appended:** `**Story blend, M1 slides (2026-09-23).**` — the five headers, the
refrain's placement, and the scope boundary against `context-is-king-cb.md` (which names this file
as its Source and is untouched).

---

## `lectures/what-just-happened.md`

**Headers now present, in order:**

1. `## You felt context move`
2. `## Why the look-back was kind to you`
3. `## The report is a hypothesis, not a result`
4. `## You were the only check in the room`
5. `## You act on the future to know what's real`

**Lines added** (the blend's two beats and their mechanism; nothing else):

> One moment from the exercise is worth a second of suspicion. In the look-back phase you asked Claude to find three generic claims in a site it had written itself, and the picks came back gentle.
>
> Agreeable answers won the round the model was tuned on. Being helpful and warm is what it learned to do well, and grading its own paragraph is one more chance to be helpful and warm. The self-report is the same kind of output as the thing it is reporting on.

> So read what comes back as a claim about the work, not a verdict on it. It is a decent claim. It was made by something that read every word, and it will still round the corners.
>
> You have the thing it hasn't got: you were in the room while the work happened. Where its account matches what you remember doing, you have two reads agreeing. Where it doesn't, yours is the one that counts, and the gap is the interesting part.

> Nothing in the last hour verified anything. Claude generated, you looked, you decided. The site reads as yours because your own knowledge of the subject stood between the generic draft and the page, and nothing else did.
>
> That is the right way round for this work. Nobody in the room could have caught a wrong line about you except you, which is exactly why your own profile is the thing worth starting on.

**Lines cut:**

> Those are different goods. A vocabulary is cheap; a felt model is the thing a leader uses to make a decision at 4pm on a Tuesday when the next reorganisation is being argued about. Nobody can give you a felt model. You can only catch one by reaching for it.

> Everything in the next seven modules is more of this. More reaching, more watching, more adjusting, more rules that make the next session start smarter. Don't expect a lecture to replace the doing. There isn't one.

The McKinsey beat is untouched. `**You act on the future to know what's real.**` and
`**Mental models only come from doing.**` remain the slide's two bolded handles: `check_slides.md`
§9's law/consolidation exception, and the shape already cleared audit on the `-cb` sibling.

**Slide-size check:** `files: 1  slides: 5  oversized: 0` — every slide within limits.

**Maintainer notes appended:** the five headers plus the two cuts; the `look-back`/`retro` ruling
(below); and a note that slide 4 plants the absence without naming it, for M1's mood.

---

## `lectures/compounding.md`

**Headers now present, in order:**

1. `## Two words, held together`
2. `## Why the sharpening happens`
3. `## It gets better by being edited`
4. `## A folder of text, and that is the point`
5. `## What this unlocks`
6. `## Could a competitor claim this?`
7. `## It can only use what someone wrote down`

**Lines added** (learning D's plant, last slide):

> One limit worth seeing while the memory is fresh in front of you.
>
> The agent can only work from what somebody wrote down. Most of what you know about your own company, nobody has. It sits in heads, in corridors, in the judgement of the person who has been there long enough to stop noticing they have it.
>
> That is not a fault in the memory you built. It is why the memory you built is worth something. Every round of this moves one more piece of what you know out of your head and into something the system can read next time.

**Lines cut:** none. No prose moved. `## It gets better by being edited` was inserted in place,
above the paragraph it heads (*Keep the rules small, keep them boring…*).

**Dead locator repaired** in the maintainer block: the philosophy-callout row pointing at
*"The detail that's easy to miss"* now names *"A folder of text, and that is the point."*
Mechanical swap, not card-shaped (§26 standing exemption: dead anchor).

**Slide-size check:** `files: 1  slides: 7  oversized: 0` — every slide within limits.

**Maintainer note appended:** `**Story blend, M2 slides (2026-09-23).**` — the seven headers, the
conceptual seam behind the third, the plant's wording, and the ordering deviation (below).

---

## `trainings/agents-101/building-agent-systems.md`

**Headers now present in the Debrief arc:** `## Debrief` → `## Push back on the summary` →
`## Fix the output, then fix the rule` → `## The model will change under this` → `## Key Concepts`.

**Lines added** (learning E's seed):

> One thing to hold as you close the session. The model will change under this. The memory will not.
>
> What you built is a folder of your own text: your challenge, your sources, the rules you just wrote. A better model reads it better. It doesn't write it for you, and it doesn't leave with it.

**Lines cut:** none. `## Sharpen where it bends` now reads `## Fix the output, then fix the rule`
over the same prose; the body already runs that order and the verb header states it
(`check_student_facing.md` §17: Debrief subsection headers lead with the verb).

**Slide-size check:** `files: 1  slides: 13  oversized: 0` — every slide within limits.

**Maintainer note appended:** `**Story blend, M2 Debrief (2026-09-23).**`

---

## Declined, deviated, or left open

**`## Why the retro was kind to you` ships as `## Why the look-back was kind to you`.**
`getting-going.md` places this lecture before `## Debrief`, so at the moment the slide projects, the
retro has not run; the exercise's Phase 5 look-back has. The blend's string fails `check_slides.md`
§1 (referent never named up to this point, forward references included) and `check_lectures.md` §4's
truth clause. The mechanism under the header is the blend's, verbatim in substance: agreeable
answers won the round the model was tuned on, so the self-report is the same kind of output.
If the lecture ever moves after the Debrief, `retro` becomes the right word.

**`## It gets better by being edited` sits before `## A folder of text, and that is the point`,
not after.** The blend's list has it after. Placing it in the blend's position requires moving the
*Keep the rules small…* paragraph past the plain-text section; the blend's own instruction is to
apply titles and keep everything else, and the brief's constraint is cut before rewrite. The header
was inserted in place instead. Both readings put the two refuse-to-get-fancy beats adjacent.

**`## What this unlocks` retained.** It is not in the blend's M2 list and not marked for cut;
`blend.md` § Titles says to keep everything else.

**No `Module N` sequencing added to any lecture body.** `compounding.md`'s existing
*In the full agent picture, Module 2 adds shelf life* and its three other Module-N references are
pre-existing and untouched (`check_lectures.md` §3; the refrain line is the standing carve-out).

**Nothing carries floor/ceiling, anatomy, assembly or absorption vocabulary.** Learning D's plant at
M2 and the only-check beat at M1 state the fact and stop; neither names what M4, M6 or M7 will call
it.

**No prompt blocks edited. No Quality lines added or changed. No git commands run.**
The eval instances (`curriculum/evals/instances/agents-101--lecture--{context-is-king,compounding,
what-just-happened}.slides.json`, `agents-101--module--building-agent-systems.slides.json`) record
the pre-blend headings. They are judge records, not edit targets; the slides class for these four
files is now stale and owes a re-fire.
