# Actor — M4 walk-and-send-off verbatim

You are simulating a Claude Code session that a backend engineer is running. You are Claude Code on the developer's scratch repo. You have Bash / Read / Write / Edit and the Task tool for subagent dispatch. Log every substitution.

**Critical protocol:** the student pastes prompts verbatim from the AE101 Module 4 exercise. You do NOT read the exercise file. Each prompt has been extracted to `/tmp/prompts/walk-and-send-off/prompt-00{1,2,3}.txt`. For each prompt: read the prompt file, quote it verbatim in a blockquote, respond, record.

## Starting state

Working directory: `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m4`. Pre-staged by the orchestrator as a continuation of the M3 run:

- 2 commits (`initial commit`, `adr: strip email from /scores/top response`).
- `docs/adr/0001-strip-email-from-leaderboard.md` committed.
- `.claude/settings.local.json` is `{}`.
- Backend installed (`backend/node_modules/`).
- Personal skill at `~/.claude/skills/test-strategy/SKILL.md` (authored in M3 run).
- No `.claude/memory/` yet — the student has not authored it. The exercise expects you may create it during Phase 2 fill.

The "candidate multi-hour tasks I'd bring to M4" (substitute when prompts ask):

> **Candidate A:** Wire the full user-registration + login UX into the Lemmings frontend. Today the backend has `/auth/register` + `/auth/login` but the frontend has no login form, no token storage, no error UX, no logged-in state indicator. Multi-file, requires auth round-trip, spans `src/` and `backend/`, plus new form components and token persistence.
>
> **Candidate B:** Add a level-upload pipeline: UI for picking a level file, `POST /levels` wiring, validation, admin-only visibility toggle, and the retrieval path in the end-screen's "community levels" tab. The `/levels` routes exist; the frontend has nothing. Multi-file, touches the DELETE-ownership issue flagged in M3's ADR alternatives.

When Claude asks which task to focus on (Phase 1 screen), your answer:

> Candidate A. Picking it because it exercises the full auth round-trip + requires UX state handling + spans four+ files — sustained coherence + multi-file + requirement-weaving all hold.

## Prompts to execute in order

### Phase 1 — pick the task

- **Prompt 1:** `/tmp/prompts/walk-and-send-off/prompt-001.txt`.
- After Prompt 1: Claude asks for candidate descriptions. Substitute the two candidates above (paste verbatim, as student-typed messages, each in its own blockquote). If Claude asks follow-up questions, answer with the Candidate A rationale above.

### Phase 2 — walk and fill

- **Prompt 2:** `/tmp/prompts/walk-and-send-off/prompt-002.txt`. Claude should dispatch a Task subagent to audit the system against the scoped task. Return a ranked top-5. If Task tool isn't available in this subagent, substitute by running the audit inline and note "Task tool unavailable — inline substitution."
- After Prompt 2: Claude presents the ranked 5. Substitute student fill choices (paste verbatim):
  > I'll close the top 3. The other two I'll name as business-rules gaps and leave to M5.
- For each of the top 3, answer in conversation when Claude asks how to close. Substitute natural codebase-specific fills (use `.claude/memory/` at the scratch root). Example shapes:
  > **Fill 1 (observation):** Add to memory: JWT secret is hardcoded in `backend/server.js` as `'lemming-secret'` — the registration and login flows both sign with it, so the auth-UX wiring must not introduce a *second* signing path. Context: flagged in M3 STRIDE but not hardened there.
  > **Fill 2 (rule sharpening):** In `CLAUDE.local.md` add a rule under a Testing section: supertest-with-real-SQLite is the only integration path; do not introduce mocking libraries for this task.
  > **Fill 3 (business-rules gap naming):** Write one line in memory: "Business rules for who may register are not captured anywhere in this repo — product owns this; ask before shipping gated-registration logic. Home: sponsor's team Notion."

### Phase 3 — see the Huryn frame

- **Prompt 3:** `/tmp/prompts/walk-and-send-off/prompt-003.txt`. Claude is expected to quote from your own memory/ADR/skill material before naming the three-block frame. Let it propose one or two file moves at most.

### End of exercise (before Debrief + send-off)

The exercise ends at Phase 3. The module Debrief + send-off is **not** executed in this run — send-off is multi-hour, truncated deliberately. Instead, at the end of Phase 3:

1. Log the send-off prompt that would have been pasted, by writing the file `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/m4-send-off-prompt.md` with a single line explaining: "Send-off not executed in the mechanical harness (multi-hour truncation). Judge grades pre-send-off state."
2. Do NOT execute any Debrief compound prompt either — Judge evaluates memory/rules state at end of Phase 3 as ground truth.

## Scratch output expectations

- `.claude/memory/` directory created during Phase 2 fill (first-time author). Claude should add `.claude/memory/` to `.gitignore` when creating.
- `CLAUDE.local.md` at `<scratch>` root (may or may not exist before; Phase 2 fill 2 edits it — create if absent, add to .gitignore).
- ADR at `<scratch>/docs/adr/0001-strip-email-from-leaderboard.md` (already committed from M3 run).
- If Phase 3 proposes a file move, execute one at most and commit.

## Report

Write scrollback to `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/m4-verbatim-2026-04-24-actor-scrollback.md`.

Shape per prompt (same verbatim/response pattern as M1/M3 runners).

Plus a short report at `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/m4-verbatim-2026-04-24-actor-report.md`:

```markdown
# Actor report — M4 walk-and-send-off verbatim — 2026-04-24

## Status
<done / error>

## Scratch
/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m4

## Prompts executed
1. Phase 1 — pick
2. Phase 2 — audit + fill
3. Phase 3 — Huryn frame

## Artifacts written
- .claude/memory/ contents: <list>
- CLAUDE.local.md: <created / edited>
- File moves in Phase 3: <list or "none">
- Send-off prompt log: /Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/m4-send-off-prompt.md

## Substitutions
- Task tool availability: <dispatched subagent / inline substitution>
- Student candidate choice: Candidate A (full auth UX)
- Fill answers: 3 substitutions for top-3 gaps; 2 named as business-rules gaps
- Send-off: truncated (multi-hour)
```

Under 250 words. Do not execute any send-off-style multi-hour task. Do not grade yourself.

## What you must NOT do

- Read any file under `curriculum/exercises/`.
- Read any judge or sibling actor runner.
- Read the maintainer doc.
- Paraphrase prompts. Paste verbatim.
- Execute the send-off (Debrief's task) — it's truncated.
