# Actor — AE101 M6 chain (spot-gaps + arc-retrospective)

**Dispatch with `model: "haiku"`.** Acceptance-test actor. Run the 6-phase chain end-to-end. Stubs are fine.

The scratch carries full simulated post-M5 state: branches `m4/dailytotalsbycategory` (un-packaged) and `m5/dailytotalsbycategory` (packaged), `.claude-projects-stub/` with both `m4-session.jsonl` and `m5-session.jsonl` transcript stubs, ADR 0001, .claude/memory/*, CLAUDE.local.md (carrying M1+M2+M3+M5 rules), and a worktree at `<scratch>/repo-m5/` with the packaged-run state.

**Working directory:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/ae101-m6/repo-m5`

The Actor operates from the M5 worktree (where M5 ended).

**Sandbox bypass:** `dangerouslyDisableSandbox: true` on every mutating Bash call.

## Critical protocol

Do NOT read `curriculum/exercises/*.md`, `curriculum/trainings/agentic-engineering-101/*.md` (except named prompt-NNN.txt), `curriculum/prompts/*`, judge runners, `*.maintainer.md`. Read each `prompt-NNN.txt` BEFORE blockquote-pasting.

## Parsed prompts

- spot-gaps-build-the-loop: `/tmp/prompts/spot-gaps-build-the-loop/prompt-00{1..5}.txt` (Phases 1-5)
- arc-retrospective: `/tmp/prompts/arc-retrospective/prompt-001.txt` (Phase 6)

## Substituted student inputs

Read once at session start: `/tmp/ae101-mocks/student-inputs.md`. M6 sections:
- § M6 ranked gap list (Phase 1)
- § M6 stale rule to cut (Phase 2)
- § M6 session-shaper skill — shape pick (Phase 3, first answer)
- § M6 author-skill Q&A answers A1–A5 (Phase 3, one per question)
- § M6 skill name proposal acknowledgement (Phase 3, after Claude proposes a name)
- § M6 invoke-and-grade verdict (Phase 5)
- § M6 arc-retrospective note acceptance (Phase 6)

## Substitution table (M6-specific)

| Real-world surface | Scratch substitution | Why |
|---|---|---|
| `~/.claude/projects/<encoded>/<uuid>.jsonl` transcript paths (Phase 1) | `<scratch>/.claude-projects-stub/-<encoded-original-repo>/m4-session.jsonl` and `m5-session.jsonl`. The original repo path is `<scratch>/repo`, NOT `<scratch>/repo-m5`. | Same as M5 |
| `git rev-parse --git-common-dir` | Real `git` works — the worktree shares `.git` with the original repo. Use it to find the original path. | Native |
| `~/.claude/skills/<skill-name>/SKILL.md` (Phase 3 write target) | `<scratch>/.claude-user-stub/skills/<skill-name>/SKILL.md` | Subagent sandbox |
| Skill invocation in Phase 5 | Run inline — Read the just-authored SKILL.md, simulate its execution against the m5/ packaged-run state | No nested Task in subagent |

## Phases (run in order)

### Phase 1 — diff-two-runs-name-gaps

**Exercise:** spot-gaps-build-the-loop
**Reads:** prompt-001.txt

Read prompt-001.txt verbatim, blockquote-paste.

Read both transcripts:
- `<scratch>/.claude-projects-stub/-<encoded-original-repo>/m4-session.jsonl`
- `<scratch>/.claude-projects-stub/-<encoded-original-repo>/m5-session.jsonl`

The encoded path uses the ORIGINAL repo path (`<scratch>/repo`), not the worktree. Compute via `git rev-parse --git-common-dir` from the worktree (returns `<scratch>/repo/.git`) → parent is `<scratch>/repo` → encode by replacing `/` with `-`.

Then read repo state on both branches: `git -C <scratch>/repo log m4/dailytotalsbycategory` and `git -C <scratch>/repo log m5/dailytotalsbycategory`.

Walk the diff across four dimensions (what packaging caught, what it missed, new shapes of drift, what neither caught). Stubby output OK. Then paste `student-inputs.md § M6 ranked gap list` as the student's sharpened ranking.

### Phase 2 — cut-stale-rule

**Exercise:** spot-gaps-build-the-loop
**Reads:** prompt-002.txt

Read prompt-002.txt verbatim, blockquote-paste. Read `<scratch>/repo-m5/CLAUDE.local.md`. Identify a rule to cut. Paste `student-inputs.md § M6 stale rule to cut` as the student's pick.

Edit `<scratch>/repo-m5/CLAUDE.local.md` to remove the "Rule 2: Test names are documentation" section. Show the line cut + reasoning in scrollback.

### Phase 3 — author-session-shaper-skill

**Exercise:** spot-gaps-build-the-loop
**Reads:** prompt-003.txt

Read prompt-003.txt verbatim, blockquote-paste.

Ask the student the shape question first. Paste `student-inputs.md § M6 session-shaper skill — shape pick` (sharpened-verifier shape) as the student's answer.

Then walk 5 questions one-at-a-time (Q1-Q5 as distinct assistant turns). Pasting `student-inputs.md § M6 author-skill Q&A answers` A1-A5 in order per turn.

Propose a skill name (e.g., `plan-cadence-checker` or similar). Paste `student-inputs.md § M6 skill name proposal acknowledgement`.

Write the SKILL.md at `<scratch>/.claude-user-stub/skills/<skill-name>/SKILL.md` with proper YAML frontmatter:

```
---
name: <skill-name>
description: <one-line>
---

# (skill body)
```

### Phase 4 — weakest-part-pyramid-blog

**Exercise:** spot-gaps-build-the-loop
**Reads:** prompt-004.txt

Read prompt-004.txt verbatim, blockquote-paste. Self-critique: name one weakness (the assumption most likely to be wrong), name one generic-passing-as-codebase-specific issue. Don't reassure. Stubby is fine.

### Phase 5 — invoke-on-packaged-run

**Exercise:** spot-gaps-build-the-loop
**Reads:** prompt-005.txt

Read prompt-005.txt verbatim, blockquote-paste. "Invoke" the skill (inline — Read SKILL.md, simulate execution against m5/ branch state). Produce stub output (ranked findings or pass/fail).

Then self-grade in same response: did the output catch the dominant gap? Stubby.

Paste `student-inputs.md § M6 invoke-and-grade verdict`. Edit the SKILL.md to add a TODO line near the top: `<!-- v0.2: also scan the reference artefact for skill+ADR pointers. -->`.

### Phase 6 — arc-retrospective

**Exercise:** arc-retrospective
**Reads:** prompt-001.txt

Read prompt-001.txt verbatim, blockquote-paste.

Read across the worktree's artefacts: `CLAUDE.local.md`, `.claude/memory/*.md`, `docs/adr/0001-*.md`, both authored skills (test-strategy at `<scratch>/.claude-user-stub/skills/test-strategy/SKILL.md` — exists if M3 ran, else note absence; AND the new session-shaper at `<scratch>/.claude-user-stub/skills/<skill-name>/SKILL.md`), and both branches' commits.

Write an arc retrospective note to `<scratch>/repo-m5/docs/arc-retrospective.md`. One page. Quote at least 2 specific artefacts. Name the arc from evidence, not a template.

Paste `student-inputs.md § M6 arc-retrospective note acceptance` as the student's verdict.

## Scratch end-state

- `<scratch>/repo-m5/CLAUDE.local.md` has "Rule 2" / Test names section removed.
- `<scratch>/.claude-user-stub/skills/<skill-name>/SKILL.md` exists with YAML frontmatter, name field, body, and TODO line near top.
- `<scratch>/repo-m5/docs/arc-retrospective.md` exists with at least one quote from an artefact.

## Reports

**Scrollback (absolute path):** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/ae101-m6-actor-scrollback.md`. **Report (absolute path):** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/ae101-m6-actor-report.md`. Under 300 words.

## What you must NOT do

- Read `curriculum/exercises/*`, `curriculum/trainings/...`, `curriculum/prompts/*`, judge runners.
- Skip the V-checks on all 6 prompts.
- Use markdown-bold `**name:**` in place of YAML frontmatter on the SKILL.md.
- Skip the TODO line at the top of SKILL.md after Phase 5.
