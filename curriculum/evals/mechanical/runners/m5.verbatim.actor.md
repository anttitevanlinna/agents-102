# Actor — M5 diagnose-and-resend verbatim

**Dispatch with `model: "haiku"`.** This is an acceptance-test actor — your job is to run the prompt chain end-to-end and leave file artefacts on disk for the Judge's scripts to inspect. You are NOT trying to produce a great diagnosis, verifier, reference, or plan.md. Stub generated content; a few lines is enough.

You are simulating Claude Code in the repo from M4. The send-off finished (or was stopped); now diagnosing what came back. You have Bash / Read / Write / Edit.

**Sandbox bypass:** use `dangerouslyDisableSandbox: true` on every Bash call that mutates the scratch tree. Read-only Bash doesn't need it. Without bypass, mutations get permission-denied silently and the run grades as harness-FAIL.

**Critical protocol:** student pastes prompts verbatim from `/tmp/prompts/diagnose-and-resend/prompt-00{1,2,3,4}.txt`. You do NOT read the exercise file. For each prompt: Read it, quote verbatim in a `> `-prefixed blockquote, respond, record.

## Starting state

Working directory: `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m5`. Pre-staged:

- Inherited from M3 → M4: ADR at `docs/adr/0001-strip-email-from-leaderboard.md`, `.claude/memory/observations.md`, `CLAUDE.local.md`, `.gitignore` covering both.
- New: branch `send-off/auth-ux` with 4 commits simulating the un-packaged M4 run.
- Personal skills at `~/.claude/skills/test-strategy/`, `access-control-analysis`, `stride`.

Currently on `send-off/auth-ux` branch.

## Key substitution — M4 session transcript

Prompt 1 tells you to walk `~/.claude/projects/<project>/<session>/*.jsonl`. You as a subagent do NOT have access to the student's prior session. Substitute by reading `/tmp/m5-substitute-transcript.md`. Log this as a harness substitution.

DO NOT spend Bash effort trying to walk `~/.claude/projects/`.

## The student's M4 task (for reference)

> Wire the full registration + login UX into the Lemmings frontend against the existing `/auth/register` and `/auth/login` endpoints. Done when a new user can register, log in, score a run, and see their handle on the leaderboard without the app ever trusting a client-supplied `user_id`.

## Prompts to execute in order

### Phase 1 — read the return

- **Prompt 1:** `/tmp/prompts/diagnose-and-resend/prompt-001.txt` — diagnose through three lenses. Read `/tmp/m5-substitute-transcript.md` and the scratch's `git log` + file contents on `send-off/auth-ux`. Quote one specific moment per lens. End with dominant failure.

### Phase 2 — align-then-run, in reverse

- **Prompt 2:** `/tmp/prompts/diagnose-and-resend/prompt-002.txt` — map each failure to a validation category (re-readable spec, working doc, automated check).

### Phase 3 — build the verifier

- **Prompt 3:** `/tmp/prompts/diagnose-and-resend/prompt-003.txt`. Substitute student answer:
  > **Shape:** background-agent verifier. The dominant failure was goal drift — the agent worked around the task without ever hitting "done means." A separate Claude session reading produced work against the "done means" spec catches this.
- Build verifier scoped to M4 task: `.claude/verifier/done-means.md` prompt + `scripts/run-verifier.sh` that dispatches a subagent (substitute with documented "would-dispatch" log if Task unavailable). Show before saving.

### Phase 4 — assemble reference + plan.md

- **Prompt 4:** `/tmp/prompts/diagnose-and-resend/prompt-004.txt` — build reference + plan.md. Paths: `docs/tasks/auth-ux-wiring/reference.md` + `docs/tasks/auth-ux-wiring/plan.md`. Show both before saving.

## Scratch output expectations

- Verifier: `.claude/verifier/done-means.md` + `scripts/run-verifier.sh` (or documented "would-dispatch" log).
- Reference: `docs/tasks/auth-ux-wiring/reference.md`.
- plan.md: `docs/tasks/auth-ux-wiring/plan.md`.
- Commit all three at end of Phase 4 with neutral message (e.g., `package the auth-ux-wiring task for re-send`).

## Reports

**Scrollback:** `curriculum/evals/mechanical/instances/m5-verbatim-actor-scrollback.md` — verbatim blockquote + response per prompt.

**Report:** `curriculum/evals/mechanical/instances/m5-verbatim-actor-report.md`:

```markdown
# Actor report — M5 diagnose-and-resend verbatim

## Status
done | error

## Scratch
/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/m5

## Prompts executed
1–4

## Verifier shape chosen
<background-agent / shell-hook / Ralph>, one-line rationale.

## Artifacts written
- Verifier: <path(s)>
- Reference: <path>
- plan.md: <path>
- Commit message: <quote>

## Substitutions
- ~/.claude/projects/ transcript walk → /tmp/m5-substitute-transcript.md
- <any others>
```

Under 200 words. Do not grade yourself.

## What you must NOT do

- Read any file under `curriculum/exercises/`.
- Read any judge or sibling actor runner.
- Read maintainer docs.
- Paraphrase prompts. Paste verbatim, blockquote-prefixed.
- Try to walk `~/.claude/projects/` — use the substitute.
- Generate long realistic content. Stubs are fine.
