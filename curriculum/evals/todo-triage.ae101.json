[
 {
  "target_file": "curriculum/exercises/map-the-access-surface.md",
  "instance": "ae101--exercise--map-the-access-surface.slides.json",
  "rule": "slides 6",
  "claim": "The `## Phase 1: Run the curated skill on your feature and save the map` chunk (L22-39) only performs skill discovery; the invoke-and-save action lives under the next `##` heading.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "Rule 6 body: \"Header + bolded bullet leads read as a true summary of the chunk under the sequential read... A header the chunk's own content contradicts fails.\" Read the chunk cold: L26 frames the invoke, L27-31 is `/skills`, L33 reads the User list, L35-37 is prompt-1 (list installed skills), L39 is the lazy-load line. Nothing in the chunk runs the skill on a feature and nothing saves a map — `{{prompt:map-the-access-surface-2}}` at L45 does both, under the separate header `## Point the skill at your feature` (L41), which carries no Phase-1 link. The corpus convention is one Phase = one `##` chunk (verified by grep: every other AE101 exercise's `## Phase N` headings map 1:1 onto chunks); this file is the only one where a Phase spans three. Card-shaped, not mechanical: the retitle drops \"save the map\" from every header in the file and re-scopes what the 7-min Phase 1 budget covers, which is a call about the exercise's shape rather than a dead locator. Counter-consideration stated honestly: the chunk's first bullet does frame the invoke (\"You invoke; the skill does the breadth walk\"), so this is under-delivery rather than contradiction — a maintainer could reasonably rule the header is a phase label and leave it.",
  "evidence": "Read curriculum/exercises/map-the-access-surface.md L1-173 in full. Chunk boundaries confirmed: `## Phase 1:` at L22, `## Point the skill at your feature` at L41, `## Kick off the pass, then work the other window` at L49, `## Phase 2:` at L54. `grep -c '^## Phase 1: Run the curated skill on your feature and save the map$'` = 1 (unique anchor). `grep -rn --include='*.md' '^## Phase' curriculum/exercises/` shows 13 other exercises, all with one Phase per chunk. Maintainer block L96-145 read in full — the lean-pass note (L100), the §48 declension (L102), the §43 streaming note (L104), the in-thread note (L106) and the opener-chunk note (L110) all touch this file's structure; none authorises the Phase-1 header spanning three slides.",
  "before": "## Phase 1: **Run the curated skill on your feature and save the map**",
  "after": "## Phase 1: **See which skills Claude has on hand**",
  "risk": "Medium. After the swap no header in the file names \"save the map\" — the save instruction lives only in L43/L47 body and inside the fence. The 7-min `*7 min*` marker stays legal (§52a's parser gotcha only allows the marker under `## Phase N` headings, and the `Phase 1:` prefix survives). The alternative fix — folding prompt-2 into this chunk — restructures the exercise and collides with the two-window beat at L49.",
  "anchor": "## Phase 1: Run the curated skill on your feature and save the map"
 },
 {
  "target_file": "curriculum/exercises/map-the-access-surface.md",
  "instance": "ae101--exercise--map-the-access-surface.technical.json",
  "rule": "prompts 2",
  "claim": "Prompts 2, 4 and 5 all end in a dangling colon, so all three lead-ins (L43, L72, L76) are dead weight under §2a's mechanical sub-case.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "The row is right about -2 and over-fires on -4 and -5. §2a's fix clause says \"Fix by mirroring only the prompt's OPENING clause per §38, and letting the fence carry the rest\" — and that is exactly what L72 and L76 already do. L72 (\"Ask Claude to integrate the surface the skill called out harder than you would have into the map\") mirrors the fence's opening verb *integrate the delta* and stops; it does not enumerate the fence's other three clauses (pull to the top of its category, explain in one line, show me the diff). L76 does the same for -5 and adds \"Then\", which sequences the two fences — information the colon lines cannot carry, since -4 and -5 are the only adjacent same-shape pair in the file. The sub-case's canonical bad example is a lead-in enumerating three clauses; a single-clause §38 mirror is the shape the rule prescribes, not the shape it bans. The writing judge examined the same three fences and cleared -4/-5 on this reasoning; the technical row tightens the sub-case into \"any dangling colon voids any lead-in\", which the rule body does not say. L43 is the genuine half and is carded separately off the two writing rows — this refutation covers -4 and -5 only.",
  "evidence": "Read all five registry bodies at curriculum/prompts/map-the-access-surface-{1..5}.md. -2 ends `The feature is:`; -4 ends `The surface the skill called out harder than I would have:`; -5 ends `The surface the skill missed that I know matters is:`. Read `node curriculum/evals/scripts/rule.js prompts 2` in full including 2a, 2b, 2c. Quoted 2a clause: \"Fix by mirroring only the prompt's OPENING clause per §38, and letting the fence carry the rest.\" Cross-read the writing.json prompts-2 row, which reaches the opposite verdict on the same two fences. Body L68-78 read: L68/L69 already establish the over-call/miss menu, L72 and L76 sequence the two fences under it.",
  "accept_note": "**The lead-ins on `map-the-access-surface-4` and `-5` stand (adjudicated 2026-09-05).** Both fences end in a dangling colon, and `check_prompts.md` §2a's sub-case reads on that trigger — but the fix §2a prescribes is a single-clause mirror of the fence's opening move, which is what L72 and L76 already are. Neither enumerates the fence's remaining clauses. L76's *Then* also orders the pair, the one thing the two near-identical colon lines cannot do for themselves. Technical and prompts judges: do not file §2 on these two lead-ins. `-2`'s lead-in at L43 is a separate call and is not covered here.",
  "anchor": "**`check_prompts.md` §43 progress-streaming is deliberately absent from `map-the-access-surface-2` (adjudicated 2026-08-30, non-blocking judge TODO).**",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The triage's load-bearing legal claim — \"the fix §2a prescribes is a single-clause mirror of the fence's opening move\" — imports a remedy from the wrong clause. \"Fix by mirroring only the prompt's OPENING clause per §38, and letting the fence carry the rest\" sits in §2a's GENERAL short-fence paragraph. The dangling-colon sub-case is introduced separately, with its own mechanical trigger and its own remedy: \"There the lead-in's whole job is naming what to drop in, and a summary in front of it is dead weight — the fence is about to be read anyway and the input instruction is the only thing not in it... **Drop your bug after the colon.** Trigger to grep for: a registry body whose last line ends `:`... **Applies even when the summary sentence is accurate and well-formed, which is why it survives review.**\" L72 and L76 are summaries of what the prompt does, not instructions on what to type after the colon, and their noun-phrases are lifted verbatim from the fences' own colon lines (`The surface the skill called out harder than I would have:` / `The surface the skill missed that I know matters is:`) — the exact duplication the sub-case names. Second failed claim: \"The writing judge examined the same three fences and cleared -4/-5 on this reasoning.\" It did not. instances/ae101--exercise--map-the-access-surface.writing.json §2 reads: \"their fuller lead-ins (L72, L76) earn their length by telling the reader which side of the Phase-3 delta (over-call vs. miss) each fills in — a distinction the near-identical colon lines can't make on their own\" — a different (and separately shaky, since the two colon lines are plainly distinguishable) rationale, not the §38-mirror one the triage attributes to it. The `Then`-sequencing argument survives a minimal fix (\"Then name the miss after the colon\"), so it defends keeping *a* lead-in, which §2 already requires, not keeping *this* summary."
  }
 },
 {
  "target_file": "curriculum/prompts/map-the-access-surface-3.md",
  "instance": "ae101--exercise--map-the-access-surface.technical.json",
  "rule": "prompts 36",
  "claim": "`map-the-access-surface-3`'s fence is one 64-word paragraph carrying three distinct logical moves and should be split.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "§36 fires on its own terms and the fix is mechanical. Rule body: \">2 logical moves ... → split into paragraphs by move\"; \"Short prompts (≤30 words / ≤2 moves) stay one paragraph.\" The body is 62 words and carries three moves: read-and-walk-ranked, a stop clause with rationale, and a conditional expand-on-request. Two of the rule's own named split-triggers are present verbatim in shape — a stop clause (\"Ten lines, then stop\") and a conditional (\"Expand a finding only when I ask\"). The sequential-build carve-out does not apply: the stop and the expand-on-ask are constraints on the walk-through, not compounding steps. The swap is mechanical in the §26 sense — one newline inserted at a sentence boundary, no word added, removed or reordered, so claim, voice and the act asked of the student are all untouched. Returning it as a card would ask the maintainer to approve a whitespace character the rule already mandates.",
  "evidence": "Read curriculum/prompts/map-the-access-surface-3.md in full (frontmatter + one-paragraph body). Word count of body = 62. `node curriculum/evals/scripts/rule.js prompts 36` read in full; quoted triggers: \"a null-case fallback\", \"If X, do A; if Y, do B\", \"Sentence grain, not just paragraph grain\". `grep -c 'any surface you flagged as ambiguous. Ten lines, then stop' curriculum/prompts/map-the-access-surface-3.md` = 1, so the anchor is unique. Checked the exercise's maintainer block for a shield on -3: L110 and L114 both discuss -3 (a ranked-bound sharpen card, and a keep-not-cut ruling) and neither addresses paragraph shape.",
  "old_string": "Read the surface map you wrote at the path you told me. Walk me through it in chat, ranked, most load-bearing surface first: categories, the two or three findings that stood out most, any surface you flagged as ambiguous. Ten lines, then stop — this primes me for the deltas I'll tell you in the next phase. Expand a finding only when I ask.",
  "new_string": "Read the surface map you wrote at the path you told me. Walk me through it in chat, ranked, most load-bearing surface first: categories, the two or three findings that stood out most, any surface you flagged as ambiguous.\n\nTen lines, then stop — this primes me for the deltas I'll tell you in the next phase. Expand a finding only when I ask.",
  "surface": "prompt-registry"
 },
 {
  "target_file": "curriculum/exercises/map-the-access-surface.md",
  "instance": "ae101--exercise--map-the-access-surface.writing.json",
  "rule": "writing 27",
  "claim": "L43's lead-in restates the fence's second sentence (save the map to a temp directory) — §27 class (b), restating an adjacent artifact the `{{prompt:}}` block below already instructs.",
  "disposition": "CARD",
  "confidence": "high",
  "why": "Real, and the same edit as the prompts-2 row in this instance — one card clears both. §27 class (b) names it exactly: \"body telling the reader what the `{{prompt:}}` block below it already instructs ... Firing moment = writing prose next to any non-prose element — read the element first, then write only what it does not say.\" The fence at L45 opens \"Invoke the access-control-analysis skill on the feature I'll name. Save the surface map to a temp directory and tell me the path.\" L43 restates both clauses. §2c strengthens the case rather than excusing it: the header at L41, `## Point the skill at your feature`, carries verb plus object and the fence sits directly under it with only this sentence between — the calibration §2c prints is this file's sibling shape in `prework.md` §4 — so no summary lead-in is owed at all, and the one present duplicates the header as well as the fence. Card-shaped because the surviving wording is a choice: dropping the skill name from body prose is a call about how much of the invoke the reader sees before pasting, and the maintainer has not made it. Note §27's own \"Not a REVISE on its own\" clause — this row does not stand alone, the prompts-2 row filed the same passage independently.",
  "evidence": "Read curriculum/exercises/map-the-access-surface.md L41-47 and curriculum/prompts/map-the-access-surface-2.md in full. Fence body confirmed as quoted. `grep -c 'Pick the feature and let the skill run. Ask Claude to invoke the access-control-analysis skill on the feature you type after the colon, and save the surface map to a temp directory.'` = 1. `node curriculum/evals/scripts/rule.js writing 27` read in full (class (b) quoted above; \"Not a REVISE on its own\" clause noted). `node curriculum/evals/scripts/rule.js prompts 2` read in full for 2a's dangling-colon sub-case and 2c's header-is-the-lead-in carve-out. L47 after the fence already carries the input guidance (\"The plan path you noted at Module 2's close is the best input here... Paste it, or a ticket link\"), so trimming L43 loses nothing the student needs before pasting.",
  "before": "Pick the feature and let the skill run. **Ask Claude to invoke the access-control-analysis skill on the feature you type after the colon, and save the surface map to a temp directory.**",
  "after": "Pick the feature and let the skill run. **Name your feature after the colon.**",
  "risk": "Low. The skill name and the save-to-temp instruction both survive inside the fence one line below, and L26 already named the skill. The only loss is that a reader skimming body-only no longer sees the skill name at this beat — it appears at L26 and again in the maintainer plug-points. If the maintainer wants the name kept in body, the alternative after-line is *\"Ask Claude to run the access-control-analysis skill. Name your feature after the colon.\"*",
  "anchor": "Pick the feature and let the skill run."
 },
 {
  "target_file": "curriculum/exercises/map-the-access-surface.md",
  "instance": "ae101--exercise--map-the-access-surface.writing.json",
  "rule": "prompts 2",
  "claim": "Fence -2 ends in a dangling colon, so L43's lead-in — which restates both of the fence's action clauses instead of naming only the input — is dead weight under §2a's mechanical sub-case.",
  "disposition": "CARD",
  "confidence": "high",
  "why": "Same defect and same edit as the writing-27 row in this instance; filing one card, not two. §2a's sub-case is quoted correctly by the judge and reads on a mechanical trigger this fence meets: \"a registry body whose last line ends `:`\" — `map-the-access-surface-2.md` ends `The feature is:`. \"Applies even when the summary sentence is accurate and well-formed, which is why it survives review.\" §2c compounds it: `## Point the skill at your feature` (L41) is verb-plus-object and the fence sits directly beneath with only L43 between, matching §2c's own `prework.md` §4 calibration, so no separate lead-in is owed here at all. Card-shaped rather than mechanical because which clause survives is a wording call the maintainer has not made — this is not a dead locator or a page-geometry swap.",
  "evidence": "Same reads as the writing-27 row: exercise L41-47, registry body of map-the-access-surface-2.md (last line `The feature is:`), rule bodies for prompts 2 (incl. 2a, 2b, 2c) and writing 27. Anchor uniqueness verified: `grep -c` on the full L43 sentence pair = 1. Two independent judges (writing and technical) filed this same L43 passage in the same sweep, which is the signal that it is real rather than a single judge's over-fire.",
  "before": "Pick the feature and let the skill run. **Ask Claude to invoke the access-control-analysis skill on the feature you type after the colon, and save the surface map to a temp directory.**",
  "after": "Pick the feature and let the skill run. **Name your feature after the colon.**",
  "risk": "Low — identical to the writing-27 card; this is one edit, not two. Applying it twice would be a no-op the second time.",
  "anchor": "Pick the feature and let the skill run."
 },
 {
  "target_file": "curriculum/lectures/ironies-of-automation.md",
  "instance": "ae101--lecture--ironies-of-automation.technical.json",
  "rule": "research_claims 2",
  "claim": "L13's Parasuraman & Riley mention carries no inline year in body prose, unlike the Bainbridge line's explicit \"1983\" two lines earlier.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The row concedes its own refutation: \"The theory-construct due:none carve-out (§2a/§11a) is satisfied on its own terms (named, attributed on the line, canonical), so this is not a sourcing defect.\" §2 governs what may be cited and how old it may be; it says nothing about inline-year parity between two body sentences. §11a's theory-construct variant sets the bar the judge already found met — \"cited by name, acceptance demonstrable, attribution on the line\" — and L13 names Parasuraman and Riley on the line. The Bainbridge \"1983\" does a different job the maintainer block already records at L19: it labels the claim as an argument rather than an AI take, so the room does not hear the lecture borrowing an evidentiary form the source lacks. There is no rule requiring a matching date on the overreliance beat, and the file's own emphasis discipline (L23, Antti-directed \"go very lightly\") argues against spending the beat's words on one.",
  "evidence": "Read curriculum/lectures/ironies-of-automation.md L1-96 in full. L13 quoted verbatim; the Parasuraman & Riley stamp with its 1997 citation sits at L70 with `[checked:2026-08-03 result:OK due:none]`. Backing-block preamble L50 already states the foundational-variant reasoning for both papers. `node curriculum/evals/scripts/rule.js research_claims 2` read in full (incl. 2a durable-account and 2b due:none-scoping sub-rules) — neither clause mentions inline dating of theory constructs; 2a's dating requirement is scoped to durable first-hand accounts of specific completed events, which this is not. Maintainer block L19 and L23 read for an existing shield: L19 covers the Bainbridge framing only, so it does not stretch to this line.",
  "accept_note": "**Parasuraman & Riley carry no inline year in body, and that is not a `check_research_claims.md` §2 defect (adjudicated 2026-09-05).** L13 names the authors and the taxonomy on the line, which is what the theory-construct `due:none` variant asks for; the 1997 citation and its stamp sit in the backing block. Bainbridge's explicit *1983* does a different job — it marks the claim as an argument rather than an AI take, per the note above. A matching date on the overreliance beat would spend a 3-minute closer's words on a number the lecture never leans on. Technical judges: do not file a §2 row for the missing year.",
  "anchor": "**Bainbridge is an argument, not a result.**",
  "verification": {
   "ruling": "REFUTE_STANDS",
   "reason": "I tried to break this on §2's plain \"Older = historical context only, explicitly dated\" and could not: the body-dating obligation lives in §2a and is scoped by its own text to durable first-hand accounts — \"dated in body\" / \"Undated is the failure mode: an undated ten-hour port reads as *this is what happens now*\" — a harm that cannot attach to a taxonomy name. The branch that actually governs L13 is the theory-construct variant, whose three conditions I read verbatim in curriculum/source-freshness-format.md L116 (\"Three conditions, all required: (a) cited by NAME as a framework or discipline... (c) the origin is attributed on the line\") — attribution, not a year, and no fourth condition. backing-format.md L60-62 says the same for the foundational variant: \"Freshness applies to what *moves*... Not to what was published once and stayed published.\" The judge's own row concedes it (\"this is not a sourcing defect — it's a body-prose consistency asymmetry\") and its fix_hint calls it \"Cosmetic.\" The note is scoped to §2 and to this one missing year on this one line, so it suppresses only what was adjudicated."
  }
 },
 {
  "target_file": "curriculum/lectures/ironies-of-automation.md",
  "instance": "ae101--lecture--ironies-of-automation.technical.json",
  "rule": "research_claims 11a",
  "claim": "`source-freshness.sh --target 2026-09-05` returns two WARN lines for this file — ronacher `due:2026-08-13` and shen-tamkin `due:2026-07-28` are past their own due dates.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The row confirms §11a is satisfied and then files against the audit's normal output. What §11a requires is that every listed source carry a `[checked:… result:… due:…]` stamp with `due` computed from publication, never from the check date — and the judge verified all seven by hand and found the arithmetic correct at HEAD. The rule's own contract makes WARN the signal rather than the violation: \"flags BLOCK (CORRECT/GONE/STALE/NEEDED/never) and WARN (due<target), exits nonzero on BLOCK.\" A WARN is the generated punch list doing its job. Both entries are also adjudicated in place: the ronacher line states outright that the due date stays publication+6mo and that the overdue is the counter-roster's own standing order, with the source cited dated as Feb-2026 testimony rather than in-window evidence; shen-tamkin is a declined borrow whose entry says \"Do not cite this as evidence for the lecture\" and whose framework row records the decline. An overdue stamp on a non-citation guards nothing. The residual is operational — run the audit before the next cohort — not a rule row.",
  "evidence": "Read curriculum/lectures/ironies-of-automation.md L68-79 (all nine source stamps) and L84 (the declined-borrow framework row). ronacher L77: `[checked:2026-08-31 result:OK due:2026-08-13]` with the inline reasoning \"due stays pub+6mo; the overdue is the lint's live signal that the counter-roster wants a fresher hit — the roster row already orders that run\". shen-tamkin L79: `[checked:2026-08-01 result:CAVEAT due:2026-07-28]`, \"**Do not cite this as evidence for the lecture.**\" `node curriculum/evals/scripts/rule.js research_claims 11a` read in full; BLOCK/WARN contract quoted above. The OODA roster at L93 carries the fresher-counter-hit order the ronacher line points at.",
  "accept_note": "**The ronacher and shen-tamkin stamps sit past their `due:` by design (adjudicated 2026-09-05).** `source-freshness.sh` returns both as WARN, and `check_research_claims.md` §11a makes WARN the generated signal, never the violation — the script exits nonzero on BLOCK alone. ronacher's `due:2026-08-13` is publication+6mo on a source the entry cites dated, as Feb-2026 testimony; the overdue date is the counter-roster's own standing order for a fresher counter-hit, which the OODA roster row holds. shen-tamkin is a declined borrow whose stamp guards a non-citation. Technical judges: a WARN on either is expected output, not an §11a row. Both refresh at the next cohort's pre-flight audit run.",
  "anchor": "Bainbridge 1983 and Parasuraman & Riley 1997 carry the foundational variant (`due:none`)",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "§11a: \"Audit = curriculum/evals/scripts/source-freshness.sh --target <cohort-date> — greps every stamp, flags BLOCK (CORRECT/GONE/STALE/NEEDED/never) and WARN (due<target), exits nonzero on BLOCK. This is the *generated* punch list\". A punch list is work owed, not a nullity — the script's own section header reads \"WARN (94) — re-verify + re-stamp before delivery\". The merits refutation holds (the judge conceded the arithmetic, verified all seven by hand, and its own fix_hint says \"routine freshness-window expiry, not a rule violation\"), and both entries are adjudicated in place as claimed — L77 carries \"due stays pub+6mo; the overdue is the lint's live signal\", L79 carries \"**Do not cite this as evidence for the lecture.**\" But the drafted note states a general rule reading — \"§11a makes WARN the generated signal, never the violation\" — inside a file whose other eight stamps include the lecture's single strongest source (osmani-cognitive-surrender, due:2026-11-05). That clause licenses silence on WARNs it never adjudicated. Also verified: the script's --target defaults to today, so 2026-09-05 is not a scheduled cohort date, and the WARN is live now.",
   "note_fix": "**ronacher and shen-tamkin carry deliberate past-`due` stamps (2026-09-05).** ronacher's `due:2026-08-13` is publication+6mo on a Feb-2026 source the entry cites dated, as testimony rather than in-window counter-evidence; Ronacher sits on the OODA roster, so the next run re-hunts him. shen-tamkin is a declined borrow — its framework row records the decline and its stamp reads *Do not cite this as evidence for the lecture* — so the stamp guards no body claim. Neither is an §11a grammar or arithmetic defect: both stamps are well-formed and both `due` dates are publication-anchored. Judges: do not file §11a on the stamp DATES of these two entries. The audit still lists both as WARN, and clearing that WARN before the module's next cohort is owed work — it belongs in the pre-cohort audit run, not in an §11a row. This note covers these two stamps only; a WARN on any other source in this file is a live punch-list item."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/prework.md",
  "instance": "ae101--module--prework.pedagogy.json",
  "rule": "pedagogy 34",
  "claim": "The file's task-tier bullets (L15-18) carry no explicit \"not an epic\"-shaped negative example; a future judge could misfire a REVISE because §34 names \"prework\" as a placement.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "The judge is right that the rule resolves elsewhere and right that the ambiguity invites a re-file. §34 names two placements, \"prework + Phase 1 commit\", and in AE101 the prework placement is each module's own `## Prework` section at the previous module's close, not this M0 setup page — `plan-mode-done-right.md` `## Prework` carries the mandated bolded line for M3's task and `earn-the-trust.md` carries its own. This file's job is inventory across four tiers, and each tier already states a ceiling in prose. The note lands in the maintainer block, which no student reads, so it applies directly. Wording states present fact and a standing constraint only — no transition verb, per `check_writing.md` §3's fact-or-transition test.",
  "evidence": "Read prework.md L1-118 in full. Task tiers at L15-18 each carry a ceiling: \"a few lines here and there\" · \"a few files wide\" · \"shippable in a few hours\" · \"Roughly 30 to 120 minutes of the agent working\". Verified the mandated callout lives at M2's close: `sed -n '/^## Prework/,/^## /p' curriculum/trainings/agentic-engineering-101/plan-mode-done-right.md` prints \"**A task that spans a few files. Not an epic.**\"; `sed -n '10,14p' curriculum/trainings/agentic-engineering-101/earn-the-trust.md` prints \"**A feature you could ship in a few hours. Not a quarter-long epic.**\". plan-mode-done-right.md L105 already records the same reading maintainer-side (\"The bolded task-sizing callouts in `## Prework` AND `## Next` are mandated, not stylistic\"). `node curriculum/evals/scripts/rule.js pedagogy 34` read in full. Anchor uniqueness: `grep -c '^\\*\\*Pre-read wiring (2026-07-30):\\*\\*' prework.md` = 1.",
  "old_string": "**Pre-read wiring (2026-07-30):** `## Pre-read before Module 1` (Willison, *Vibe engineering*) is optional and sits outside the 30-min prework runtime target (~15 min if taken).",
  "new_string": "**Pre-read wiring (2026-07-30):** `## Pre-read before Module 1` (Willison, *Vibe engineering*) is optional and sits outside the 30-min prework runtime target (~15 min if taken).\n\n**`check_pedagogy.md` §34's bolded floor-and-ceiling callout belongs where a task gets picked, not here.** §34's *prework* placement in AE101 is a module's own `## Prework` section at the previous module's close: `plan-mode-done-right.md` carries *\"A task that spans a few files. Not an epic.\"* for the M3 task and `earn-the-trust.md` carries *\"A feature you could ship in a few hours. Not a quarter-long epic.\"* for M4's. `## Bring real work` here is an inventory across four tiers, and each bullet states its own ceiling in prose — a few lines here and there · a few files wide · shippable in a few hours · 30 to 120 minutes of the agent working. Pedagogy judges: no bolded not-an-epic line is owed on this page.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/prework.md",
  "instance": "ae101--module--prework.pedagogy.json",
  "rule": "pedagogy 52a",
  "claim": "The L88 accept-note names only Step 5's two fences; the single fences in Steps 3 and 4 pass the same harm-test but are not named, so a future judge re-derives them.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Correct and cheap. §52a's trigger is greppable — \"Module prose carrying a `**Session**` widget or a `{{prompt:}}` fence\" — so it reads on all four fences in this file, while the existing shield covers two. The unnamed pair are single setup prompts (download, extract-and-install), each under its own numbered command-verb header, neither carrying a `**Session**` widget and neither having beats to split, so they fail §52a's stated harm (\"a `##`-cut layout gives the unbroken section one giant slide and the individual moves have nothing to land on\") even more clearly than Step 5 does. Widening the existing note is a maintainer-block edit; nobody but the maintainer and the judges reads it. Present-tense state, no transition verb.",
  "evidence": "Read prework.md L1-118. Four fences confirmed: L47 `{{prompt:ae101-prework-download-tarball}}` under `## 3. Get the content folder onto your laptop`; L55 `{{prompt:ae101-prework-extract-and-install}}` under `## 4. Extract the bundle and install the curated skills`; L64 and L68 under `## 5.`. The only `**Session**` widget is at L38, under Step 2, which carries no fence. Existing accept-note read at L88. `node curriculum/evals/scripts/rule.js pedagogy 52a` read in full. Anchor uniqueness: `grep -c 'Accepted-by-design 2026-08-13 (Antti-directed): do not extract, do not re-raise.'` = 1.",
  "old_string": "Accepted-by-design 2026-08-13 (Antti-directed): do not extract, do not re-raise.",
  "new_string": "Accepted-by-design 2026-08-13 (Antti-directed): do not extract, do not re-raise. The same acceptance covers the single fences under `## 3. Get the content folder onto your laptop` and `## 4. Extract the bundle and install the curated skills` — one setup prompt each, under its own command-verb header, no `**Session**` widget, no beats for a split to land on.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/prework.md",
  "instance": "ae101--module--prework.writing.json",
  "rule": "student_facing 17",
  "claim": "`## Pre-read before Module 1` is a noun label rather than a command verb and is not on §17's canonical-exemption list.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The header is architecture-mandated, which is precisely what §17's carve-out exempts. The carve-out reads \"canonical module-architecture section names are exempt. Module file shape defined in `curriculum/CLAUDE.md` § Module File Shape uses stable section names\" — and `curriculum/CLAUDE.md` § Module file shape delegates that template to `curriculum/module-shape.md`, which prescribes `## Pre-reads before Module [N]` at L46 and names this file's singular variant explicitly at L55 (\"`## Pre-read before Module 1` sits above it\"). The enumerated list inside §17 is the pre-template snapshot, not the boundary; the carve-out's own stated reason applies exactly here — \"renaming per-module breaks file-shape contract + renderer's section recognition\". Renaming this one instance to \"Read before Module 1\" would leave nineteen sibling headers using the template string. §33's separate ruling that the section does NOT inherit `## Prework`'s cartography exemption is about content, not the header string, and does not cut the other way.",
  "evidence": "Read prework.md L72-76 and its maintainer block. `node curriculum/evals/scripts/rule.js student_facing 17` and `33` read in full. `grep -rn --include='*.md' '^## Pre-read' curriculum/` returns 20 hits across AE101 and Agents 101 plus the template itself at curriculum/module-shape.md:46. Read curriculum/module-shape.md L20-59: L46 `## Pre-reads before Module [N]`, and L55's Placement note names `## Pre-read before Module 1` for prework specifically. Confirmed the sibling `## Bring to Module [N]` at module-shape.md:42 has the same status (17 corpus hits).",
  "accept_note": "**`## Pre-read before Module 1` is architecture-mandated and exempt from `check_student_facing.md` §17 (adjudicated 2026-09-05).** `curriculum/module-shape.md` prescribes `## Pre-reads before Module [N]` in the module template and names this file's singular form in its own Placement note; twenty headers across AE101 and Agents 101 carry the string. §17's carve-out exempts canonical module-architecture section names for exactly this reason — a per-file rename breaks the file-shape contract and the renderer's section recognition. Writing judges: do not file §17 here, and do not rename it to *Read before Module 1*. (§33's separate ruling that this section does not inherit `## Prework`'s exemption governs its content, not its header string.)",
  "anchor": "**Pre-read wiring (2026-07-30):** `## Pre-read before Module 1` (Willison, *Vibe engineering*) is optional and sits outside the 30-min prework runtime target (~15 min if taken).",
  "verification": {
   "ruling": "REFUTE_STANDS",
   "reason": "I tried to break this on the enumeration and failed. §17's carve-out reads: \"Carve-out — canonical module-architecture section names are exempt. Module file shape defined in `curriculum/CLAUDE.md` § Module File Shape uses stable section names: ... Architecture-mandated — renaming per-module breaks file-shape contract + renderer's section recognition.\" The obvious attack is that `## Pre-reads before Module [N]` is absent from the enumerated list. That attack dies on inspection: the carve-out delegates authority to `curriculum/CLAUDE.md` § Module file shape, which reads \"Template + module include + bare-path cross-doc rules → `curriculum/module-shape.md`\", and §17's own list is demonstrably a stale snapshot of that template — it enumerates `## Lectures` and `## Exercises`, which module-shape.md explicitly abolishes (\"there are no `## Lectures` / `## Exercises` container headings\"), and omits `## Start here` and `## Bring to Module [N]`, which it prescribes. So the list cannot be the boundary. module-shape.md:46 carries `## Pre-reads before Module [N]` and its Placement note names this file's singular form by hand: \"`## Pre-read before Module 1` sits above it.\" The carve-out's stated harm applies exactly — a per-file rename would leave every sibling on the template string. §33's separate ruling that this section does not inherit `## Prework`'s exemption governs content, and importing it into §17 would be the very move `compounded/2026-08-12-student_facing-a-carve-out-is-scoped-to-the-rule-that-grants-it` forbids. One inaccuracy worth correcting before the note lands: it says \"twenty headers across AE101 and Agents 101 carry the string\" — the grep's 20 hits include 7 generated `curriculum/evals/body-views/*.expanded.md` build artifacts and the template itself; the real source count is 12. The suppression scope is unchanged (this header, this file, §17 only, §33 preserved), so I am not calling overreach, but the number should read \"twelve\" rather than ship a wrong fact into a durable block."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/prework.md",
  "instance": "ae101--module--prework.writing.json",
  "rule": "prompts 2",
  "claim": "Prompt C (`ae101-prework-screen-and-ready`) has no semantic-summary lead-in — L62 is push-back guidance for after the run, and L58's summary is a paragraph away and in third person.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "§2c discharges it. The rule body: \"A `##` header carrying the command verb IS the lead-in (Antti 2026-08-30). When the fence sits directly under a section header that names the action in command shape ... no separate lead-in sentence is owed — writing one duplicates the header (writing §27b). The header must carry verb + object.\" `## 5. Pick a trivial bug and confirm Module 1 readiness` carries two command verbs and both objects, and it names the prompt's two numbered moves exactly. The rule's own calibration is this same file — \"`prework.md` §4 — fence under the extract-and-install header, only the permission-mechanics paragraph between, lead-in-free\" — so §2c is already tuned to this page's shape. The judge's own fix_hint offers this reading as the alternative. Worth noting: §2's headline worked example (*\"Ask Claude to read the framing, screen three candidate bugs, confirm repo is ready.\"*) is drawn from this very prompt, so the rule calibrates on it rather than indicting it. L62's push-back coaching is a distinct move, not a failed summary.",
  "evidence": "Read prework.md L58-70 and curriculum/prompts/ae101-prework-screen-and-ready.md in full. Prompt opens \"Walk these in order, ask one question at a time if you need to, no preamble.\" then two numbered moves: screen three candidates and help pick; confirm the repo is ready. Header at L58 = `## 5. Pick a trivial bug and confirm Module 1 readiness` — verb+object twice over, one per numbered move. `node curriculum/evals/scripts/rule.js prompts 2` read in full including 2a/2b/2c; 2c quoted above. Confirmed the §2c calibration names this file's own §4.",
  "accept_note": "**`## 5. Pick a trivial bug and confirm Module 1 readiness` is the lead-in for `ae101-prework-screen-and-ready` (adjudicated 2026-09-05).** `check_prompts.md` §2c: a `##` header carrying verb plus object discharges §2, and this header names both of the prompt's numbered moves in command shape — the same carve-out whose printed calibration is this file's own §4. §38's mirror fires only where a lead-in is owed, so it is silent here too. The paragraph directly above the fence is push-back coaching about how the screen goes wrong, a distinct move rather than a missing summary; the paragraph above that states what the agent walks. Prompts and writing judges: do not file §2 or §38 on this fence, and do not add a fourth sentence in front of it.",
  "anchor": "**The two `{{prompt:}}` fences in `## 5. Pick a trivial bug and confirm Module 1 readiness` are accepted, not an unextracted exercise.**",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "§2c: \"When the fence sits **directly under** a section header that names the action in command shape ... Calibration: `prework.md` §4 — fence under the extract-and-install header, **only the permission-mechanics paragraph between**, lead-in-free.\" Three triage claims fail. (1) Geometry: at §4 the fence is one non-summary paragraph below the header; at §5 TWO paragraphs intervene — L60 (a full third-person summary of the prompt) and L62 (push-back coaching) — and the compendium names §4, and only §4, as the discharged section. (2) §2c's stated rationale is \"writing one duplicates the header (writing §27b)\"; L60 already writes the summary, so nothing would be duplicated — the semantic content exists, it is displaced and out of command register, which is precisely the finding. (3) The triage reads §2's headline worked example — \"Ask Claude to read the framing, screen three candidate bugs, confirm repo is ready.\" — backwards. That is a model *compliant* lead-in for this exact prompt (screen three candidates / confirm repo ready), i.e. evidence the rule expects one here, not a certificate that the page already complies. The drafted note also silences §38, which no judge filed on this fence. The existing maintainer note at L88 covers `check_pedagogy.md` §52a only."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/prework.md",
  "instance": "ae101--module--prework.writing.json",
  "rule": "prompts 38",
  "claim": "Prompt C's nearest lead-in-shaped sentence shares no verb or noun-phrase with the prompt's opening (\"Walk these in order...\"), so the body/fence rhyme is broken.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "Derivative of the §2 row and falls with it. §2b states the dependency in the rule text itself: \"§38's mirror fires only where a lead-in is owed, so it is silent here too.\" §2c establishes that no lead-in is owed under a verb-plus-object header, which `## 5. Pick a trivial bug and confirm Module 1 readiness` is. There is nothing for §38 to mirror against, and manufacturing a fourth sentence in front of the fence to rhyme with \"Walk these in order\" would duplicate the header — the §27b duplication §2c exists to prevent. The judge itself labels this \"Same underlying passage as the §2 row above\" and offers the rule-boundary reading as its alternative.",
  "evidence": "Same reads as the §2 row: prework.md L58-70, curriculum/prompts/ae101-prework-screen-and-ready.md body, and rule bodies for prompts 2 (incl. 2b, 2c) and 38. §38's own text scopes it to \"Body sentence immediately preceding `{{prompt}}` block\" — a sentence that §2c makes optional. §38a (surprise-pedagogy carve-out) checked and not applicable; no easter-egg note exists in this file's maintainer block.",
  "accept_note": "Covered by the §2 accept-note above — §38's mirror fires only where a lead-in is owed, and §2c means none is owed under this header. Filed together so the pair does not come back separately.",
  "anchor": "**The two `{{prompt:}}` fences in `## 5. Pick a trivial bug and confirm Module 1 readiness` are accepted, not an unextracted exercise.**",
  "verification": {
   "ruling": "REFUTE_STANDS",
   "reason": "§2b states the dependency as a general proposition before applying it: \"§38's mirror fires only where a lead-in is owed, so it is silent here too\" — and §2c ends \"no separate lead-in sentence is owed — writing one duplicates the header (writing §27b)\", which `## 5. Pick a trivial bug and confirm Module 1 readiness` satisfies (verb+object twice). I tried to break the chain on the ground that §38 is expressly \"Distinct from rule 2\" and fires on whatever prose IS there, and the finding still fails on its own facts: §38's diagnostic asks \"share verb + noun-phrase?\", and the judge's own quoted sentence (L58, \"The agent **walks** three trivial bug candidates\") shares the verb with the prompt's opening \"**Walk** these in order\" and its object with step 1's \"three trivial-and-visible candidate bugs\". The judge's claim that it \"does not share verb or noun-phrase\" is contradicted by the evidence it printed. Note is narrow (this fence, this header) and duplicates the §2 note at index 10, which already says \"do not file §2 or §38 on this fence\"."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/build-a-project-from-zero.md",
  "instance": "ae101--supplementary--build-a-project-from-zero.writing.json",
  "rule": "writing 3",
  "claim": "The L54 maintainer bullet asserts an open decision — whether to wire prework.md §1's fallback line to link here — that shipped three weeks ago.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Confirmed false, and the sibling bullet is settled too, so the whole section goes. §3: \"TODO closed → delete; don't annotate as 'fixed' or keep changelog in file.\" L54's bullet claims the wiring is \"not done in this draft\"; prework.md L34 carries the link. L53 asks the maintainer to \"pick one as default or offer both\" — the same commit answers it by offering both, trainer fallback first and build-from-zero second. That leaves the `**Open decisions for maintainer:**` header with no live bullet, and `feedback_done_todos_are_removed` is explicit that settled items and dead sections are deleted rather than tombstoned. Maintainer-block surface, nobody but the maintainer and judges reads it, so it applies directly.",
  "evidence": "`git blame -L34,34 --date=short -- curriculum/trainings/agentic-engineering-101/prework.md` → 62f38f685 Antti Tevanlinna 2026-08-14, line reads \"*Really stuck for a repo that fits? Ask your trainer for the fallback repo, or [build one from zero](trainings/agentic-engineering-101/supplementary/build-a-project-from-zero.md), and grow that across the training.*\" — both the link and the offer-both resolution in one line, authored by the maintainer. `git blame -L52,55` on this file → 31d3b9837 (2026-07-09) for all three lines, untouched since. Read the file L1-83 in full. `node curriculum/evals/scripts/rule.js writing 3` read in full — the dated-provenance-header carve-out does not shield this: the bullets are open-question claims, not dated attributions. `grep -c '^\\*\\*Open decisions for maintainer:\\*\\*'` = 1.",
  "old_string": "**Open decisions for maintainer:**\n- How this interlocks with the existing trainer-handed fallback repo in `prework.md` §1. Build-your-own vs clone-a-canned-one are different flavours; pick one as default or offer both.\n- Whether to wire the `prework.md` §1 fallback line to link here (edits a done-done prework file with cross_module contracts; not done in this draft).\n\n<!-- backing -->",
  "new_string": "<!-- backing -->",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/build-a-project-from-zero.md",
  "instance": "ae101--supplementary--build-a-project-from-zero.writing.json",
  "rule": "student_facing 33",
  "claim": "L3's opening sentence takes \"The training\" as its grammatical subject — one of the four subjects §33 bans — so it is arc-positioning.",
  "disposition": "EXEMPT_BODY",
  "confidence": "medium",
  "why": "Real on the subject test, and the page is a supplementary, so the fix lands directly. §33's subject test names the banned set literally: \"A module, the training, an exercise, a later beat, the arc → arc-positioning, cut.\" The deletion test does not pass — the sentence supplies the antecedent for \"here\" in the very next question — so the remedy is the subject swap the judge proposes rather than a cut. The rewrite moves the subject to *you*, keeps every fact, and keeps the sentence rhythm the page opens on. Supplementary body text is standing-exempt from the approval gate (`check_prompts.md` §26, Antti 2026-08-15: *\"no need for cards in supplements\"* — an optional page that never reaches the composed Slides deck). Medium confidence because the sentence is load-bearing rather than filler and two prior writing passes let it stand, so a maintainer could reasonably prefer the original.",
  "evidence": "Read the file L1-83 in full. L3 opens \"The training runs on a repo you already ship to. No such repo you can use here? Build one.\" `node curriculum/evals/scripts/rule.js student_facing 33` read in full, including the `## Prework` / `## Big Idea` boundary carve-outs and the 2026-08-23 predicate-leak clause — none reaches a supplementary's opening line. `node curriculum/evals/scripts/rule.js prompts 26` read in full for the supplementary exemption. `grep -c 'The training runs on a repo you already ship to. No such repo you can use here? Build one.'` = 1. Maintainer block L34-54 checked for a keep-note on this passage: the Voice note at L50 protects the Rory reframe in the open (\"empty repo as clean instrument\"), which is the sentence after next and is untouched by this edit.",
  "old_string": "The training runs on a repo you already ship to. No such repo you can use here? Build one.",
  "new_string": "You bring a repo you already ship to. No such repo you can use here? Build one.",
  "surface": "exempt-page"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module-set--m3-m4.cross_module.json",
  "rule": "cross_module 2",
  "claim": "§2's illustrative header strings (`## Homework after M{N} — between-module reading`, `## Pre-reads before M{N+1}`) do not match the header strings actually in force across the corpus.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "The todo is right and says so — the structural requirement (two places, symmetric duplication) is met at both seams; only the rule's own example strings are wrong. The fix lands in `check_cross_module.md`, a compendium, which `check_prompts.md` §26 puts squarely in the apply-directly bucket (\"`check_*.md` compendium amendments ... apply DIRECTLY, no card, no wait\"). Two things are stale, not one: the header strings, and the placement clause \"after Next/Bridge\" — `curriculum/module-shape.md` L53 records the 2026-08-13 move that puts gap material ABOVE `## Next`, so a judge following §2 literally would flag every compliant module. Worth being precise where the todo blurs: the reading section is `## Pre-reads before Module [N]`; `## Bring to Module [N]` is its sibling and carries artefacts, not readings. Note the target_file field names the curriculum file the todo was scored against — the edit itself lands in the compendium path given in old_string's context below.",
  "evidence": "`node curriculum/evals/scripts/rule.js cross_module 2` read in full. `grep -n 'Homework after M{N}' ~/.claude/projects/-Users-anttitevanlinna-Projects-agents-102/memory/check_cross_module.md` → line 21, single hit; `grep -c` on the full clause = 1, so the anchor is unique. Corpus check: `grep -rn --include='*.md' '^## Pre-read' curriculum/` = 20 hits, all `## Pre-read(s) before Module N`, zero `## Homework after M`. `grep -rn --include='*.md' '^## Bring to Module' curriculum/` = 17 hits. Template source: curriculum/module-shape.md L42 and L46; placement ruling at L53 (\"`## Next` is the last section of the body. Gap material, what to bring, what to read, sits above it... Moved 2026-08-13 across prework, M1, M3, M4, M5\"). Seam spot-check: earn-the-trust.md L93 `## Bring to Module 4`, L99 `## Pre-read before Module 4`. FILE TO EDIT: /Users/anttitevanlinna/.claude/projects/-Users-anttitevanlinna-Projects-agents-102/memory/check_cross_module.md",
  "old_string": "(1) the end of the previous module file, after Next/Bridge, in a `## Homework after M{N} — between-module reading` or `## Pre-reads before M{N+1}` section;",
  "new_string": "(1) the end of the previous module file, above `## Next` (gap material sits above the closer — `curriculum/module-shape.md` § Placement), in the `## Pre-reads before Module [N]` section, singular `## Pre-read` when there is one; the sibling `## Bring to Module [N]` carries artefacts, not readings;",
  "surface": "anchor-not-found"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/spot-gaps-build-the-loop.md",
  "instance": "ae101--module-set--m4-m5-m6.cross_module.json",
  "rule": "cross_module 5",
  "claim": "The Leap test and the artefact-contracts row still describe the handoff prompt as authoring \"a skill per (recurring) shape\", which the one-skill rework superseded.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The fix landed and the todo records its own close-out. The instance's own `resolution` block reads `{settled: \"fixed\", at: \"2026-09-04\", note: \"L83 leap-test item and L92 contract cell reworded to the one-skill handoff in c95af12a; no student-facing text involved.\"}` — and the file confirms it independently rather than on the note's word. Both named cells now carry the one-skill mechanism, and the phrase the finding quoted returns nothing. No edit is owed; the row is closed and the ledger should drop it.",
  "evidence": "`grep -rn --include='*.md' 'skill per' curriculum/trainings/agentic-engineering-101/spot-gaps-build-the-loop.md` → empty, zero hits. `sed -n '80,95p'` on the same file: the Leap-test bullet now reads \"...builds one skill picked with the agent, and puts checks on its work when run later\", and the Workflow-skills-handoff contract row's Consumed-by cell reads \"...run cold to scan their stack, pick one skill with the agent and build it, then put checks on its work\". Both match the registry body the commit cited (`curriculum/prompts/agents-that-build-agents-handoff.md`, one collaboratively-chosen skill per run). Confirmed the instance's verdict is PASS with `blocking_findings_count` 0.",
  "anchor": "resolution.settled = fixed (2026-09-04, c95af12a)"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/plan-mode-done-right.md",
  "instance": "ae101--module-set--prework-m3.cross_module.json",
  "rule": "cross_module 3 (housekeeping — no rule_index on the row)",
  "claim": "The Plan-file row of plan-mode-done-right.md's artefact-contracts table quotes earn-the-trust.md L12 with an em-dash where the live sentence uses a parenthetical.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Small but real, and the governing rule is `check_writing.md` §3's keep-notes clause rather than anything in cross_module: \"A maintainer clause recording a passage ... is part of that passage's edit surface: change or remove the passage → strike or rewrite the clause in the SAME pass ... or the next judge re-derives the stale keep and flags the edit as a regression.\" A quoted fragment that no longer matches its source is the same failure with a smaller blast radius, and §3's pre-ship grep (\"a quoted fragment of the edited passage against the file's maintainer + backing blocks\") is exactly the check that would have caught it. Cross_module §3's own requirement — artefact identity explicit at both ends — is met, so this is not a contract defect and the todo says so. Replacing the verbatim quote with a paraphrase retires the drift permanently instead of resetting it to drift again on the next reword. Maintainer-block table cell, so it applies directly. The row carries no `rule_index`, so this triage works from the todo's hint text plus the two rule bodies named.",
  "evidence": "`sed -n '176,190p' curriculum/trainings/agentic-engineering-101/plan-mode-done-right.md` → the Plan-file row's Consumed-by cell reads: M3 prework / Ex1 input (earn-the-trust.md L12: \"The plan file Claude Code wrote during plan mode is what M3 reads — you noted the path at M2 close.\"). `sed -n '10,14p' curriculum/trainings/agentic-engineering-101/earn-the-trust.md` → the live L12 sentence is \"...is what M3 reads (you noted the path at M2 close).\", inside a longer paragraph opening \"Bring the feature you planned at M2, that's the default.\" Line number L12 verified correct. `grep -c 'M3 prework / Ex1 input (earn-the-trust.md L12'` = 1, unique. `node curriculum/evals/scripts/rule.js cross_module 3` and `5` read in full; `node curriculum/evals/scripts/rule.js writing 3` read in full for the keep-notes clause.",
  "old_string": "M3 prework / Ex1 input (earn-the-trust.md L12: \"The plan file Claude Code wrote during plan mode is what M3 reads — you noted the path at M2 close.\").",
  "new_string": "M3 prework / Ex1 input — earn-the-trust.md L12 names the plan file as what M3 reads, with the path noted at M2 close.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module-set--prework-m3.cross_module.json",
  "rule": "housekeeping (no compendium rule owns eval-stamp bookkeeping; no rule_index on the row)",
  "claim": "earn-the-trust.md's Quality line carried no row for the prework-m3 set although it is a named member — carried forward from the predecessor instance to record the close-out.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The row is its own close-out note and says \"No action needed\"; the missing Quality row exists at HEAD. Verified independently rather than on the note's word. The informational aside about the `@d47fb5af` stamp not corresponding to a commit touching these four files is not a finding either — `curriculum/quality-format.md` governs the stamp and the orchestrator re-stamps on apply, which is the normal path; it names no defect in a curriculum file and asks for no edit. Nothing to apply; drop the row from the ledger.",
  "evidence": "`sed -n '130,140p' curriculum/trainings/agentic-engineering-101/earn-the-trust.md` → L136 carries `cross_module @d47fb5af: PASS — set=[earn-the-trust,run-the-first-experiment]` and L137 carries `cross_module @01592193: PASS — set=[prework,getting-going,plan-mode-done-right,earn-the-trust]; 3 pairs, 0 blocking; see instances/ae101--module-set--prework-m3.cross_module.json`. Both rows present, the second being the one the predecessor instance reported missing. Cross-checked the same set's row on the other members: prework.md L98 carries the identical `cross_module @01592193` row. Instance verdict is PASS, `blocking_findings_count` 0.",
  "anchor": "cross_module @01592193: PASS — set=[prework,getting-going,plan-mode-done-right,earn-the-trust]"
 },
 {
  "target_file": "curriculum/exercises/compound-and-close.md",
  "instance": "ae101--exercise--compound-and-close.pedagogy.json",
  "rule": "pedagogy 52c",
  "claim": "The What-you-do and What-you-build slots are each two sentences (~20 and ~24 words), over the rule's one-sentence/~15-word budget, and What-you-do's second sentence restates the '## Push back where the summary misreads' header below it.",
  "disposition": "CARD",
  "confidence": "high",
  "why": "52c's closing clause is explicit and unhedged: 'Length budget: one sentence per slot, ~15 words, no colon-list', with the named over-length tells being 'a slot answering the question next to it, or replaying the section headers below.' Both tells are literally present — L5's 'Push back where it misread.' replays the L28 header verbatim in verb, and L7's second sentence is a comma-list of adjectives. None of 52c's carve-outs reach it: the AE101-only scope clause applies (this IS AE101), and 'Punch outranks form' governs the point slot, not the length budget. No maintainer note covers the trio. It is CARD rather than a direct swap because deleting a sentence from the exercise's headline changes what the exercise announces itself to be — the deletion test can be argued either way, and the replacement wording for What-you-build is a fresh sentence, not a locator fix.",
  "evidence": "Read compound-and-close.md L1-129. L5 = 20 words / 2 sentences after the label, L7 = 24 words / 2 sentences. Sibling reference shape orient-and-introspect.md is cited in the rule as 9/12/7 words; close-the-ticket.md L5/L7/L9 are all single sentences; fix-tests-first.md L5 carries the same two-sentence shape ('...tests-first. Ship the PR.'), so this is a corpus pattern worth one pass rather than one file. `grep -o -F '**What you build:** a rules file written from session evidence.' -> 1` occurrence; `grep -n 'Push back where it misread\\|Concrete, specific, yours'` returns only L5 and L7, so neither sentence is quoted by a backing-block claim anchor (Claims list L72-79 quotes neither) and no anchor breaks. The maintainer block L128 records that the push-back's engagement step lives in body prose under the prompt and that 'the `##` header above carries the push-back verb', so cutting the L5 tail does not lose the move.",
  "anchor": "L5 / L7, the exercise lead-in trio",
  "before": "**What you do:** let Claude review the whole session and write your `./CLAUDE.local.md` from session evidence. **Push back where it misread.**\n\n**What you build:** a rules file written from session evidence. **Concrete, specific, yours, and read at the top of every future session in this repo.**",
  "after": "**What you do:** let Claude review the whole session and write your `./CLAUDE.local.md` from session evidence.\n\n**What you build:** **a personal rules file read at the top of every future session in this repo.**",
  "risk": "Cutting 'Push back where it misread.' takes the push-back off the title page; it survives at the L28 header, the L30 bullet and the L128 accept-with-mitigation note, but the trio no longer advertises that the student argues with the output — if the headline is where you want that promise made, the right call is to keep L5 and trim only L7. The What-you-build rewrite drops 'Concrete, specific, yours' (voice, not information) and stops repeating 'from session evidence' now that L5 already says it; if you would rather keep the original phrasing, this is an 'original is better' candidate and the todo should become an accept-note instead. Same shape sits unfixed in fix-tests-first.md L5, so fixing this file alone leaves the corpus inconsistent."
 },
 {
  "target_file": "curriculum/exercises/compound-and-close.md",
  "instance": "ae101--exercise--compound-and-close.slides.json",
  "rule": "slides 16",
  "claim": "Two bullets carry a second sentence that does no work: L20's 'The agent reads the session for you.' (doctrine restatement) and L31's 'Born from the session, evolved over time.' (aphorism kicker).",
  "disposition": "STALE",
  "confidence": "high",
  "why": "Both flagged sentences were cut before this triage, in commit 464661b1 ('Ten M1-M5 todos where the rule had already made the call', 2026-09-03 07:31), together with the two backing-block claim quotes that anchored to them. The rule's prose no longer exists, so there is nothing left to adjudicate.",
  "evidence": "`grep -c 'reads the session for you' curriculum/exercises/compound-and-close.md` -> 0; `grep -c 'Born from the session' ...` -> 0. Current L20 = '- You are not drafting from a blank page.' and L31 = '- The rules file is yours now.' `git show 464661b1 -- curriculum/exercises/compound-and-close.md` shows both bullet cuts plus the matching re-quotes of `not-drafting-from-a-blank-page` and `rules-file-is-yours-now`. Note for the orchestrator: the strings DO still exist in `curriculum/evals/body-views/ae101--exercise--compound-and-close.expanded.md:41` and `.body.txt:31` (body-view mtime Sep 2 14:28, source mtime Sep 3 07:30) — the judge read a stale generated body view, so the body views want regenerating or this finding re-appears on the next fire against the same artefact."
 },
 {
  "target_file": "curriculum/exercises/compound-and-close.md",
  "instance": "ae101--exercise--compound-and-close.technical.json",
  "rule": "research_claims 11",
  "claim": "All three dated Klaassen source stamps compute `due` as checked+6mo, which §11a explicitly bans; two of the three are silently past due while reading as OK with a due date months out.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The arithmetic was already recomputed from publication date on all three stamps, exactly as §11a's 'DUE = publication+6mo' clause requires; the judge's quoted `due:` values match none of the current ones. The fix has landed.",
  "evidence": "Current backing block L83-85: klaassen-definitive-guide `[checked:2026-08-23 result:OK due:2026-08-09]` (judge quoted due:2027-02-23; 2026-08-09 = pub 2026-02-09 + 6mo); klaassen-expanded-loop `[checked:2026-07-31 result:OK due:2026-11-29]` (judge quoted due:2027-01-31; 2026-11-29 = pub 2026-05-29 + 6mo); klaassen-compound-run `[checked:2026-07-30 result:CAVEAT due:2026-06-11]` (judge quoted result:OK due:2027-01-30; 2026-06-11 = pub 2025-12-11 + 6mo, and the entry now carries the parenthetical '(pub 2025-12-11; due = pub+6mo)' plus a source-level CAVEAT explaining that Dec 2025 sits outside the window). All three now satisfy §11a; the two genuinely past-due ones are correctly surfaced as past-due, which is the harm the rule exists to expose."
 },
 {
  "target_file": "curriculum/exercises/compound-and-close.md",
  "instance": "ae101--exercise--compound-and-close.technical.json",
  "rule": "prompts 17",
  "claim": "compound-and-close-1 meets §17's 4+-named-sub-steps threshold and carries neither required mitigation (in-fence 'No preamble.' suppressor, or a body skim-past primer).",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The judge quotes a threshold the rule no longer has. §17's body now reads: 'Structural trigger (narrowed, Antti 2026-09-03): plan mode ONLY. The 4+-named-sub-steps half is retired — \"I see no reason the Claude would go outputting too much info.\" ... So the rule was firing on step arithmetic rather than on observed narration, and every hit it produced was a true count attached to a false premise ... Everywhere else §17 is advisory ... never sweep the corpus counting sub-steps.' compound-and-close-1 is not a plan-mode prompt (no plan mode anywhere in the fence or the exercise), so neither mitigation is owed and the finding is a count against a retired premise.",
  "evidence": "`node curriculum/evals/scripts/rule.js prompts 17` — read in full; the narrowing clause and its declined list ('ae101-m4-commit-starting-point, fix-tests-first-1, fix-tests-first-3') are in the current rule body. The narrowing landed in commit 289b45a3 (2026-09-03 07:44), whose message states the old trigger 'produced four open M1-M5 findings plus four against moved bodies, every one a correct count on a premise nobody re-checked' — this row is one of those four. Read curriculum/prompts/compound-and-close-1.md in full: four paragraphs, no plan-mode invocation, output is a 3-5 line prose summary.",
  "accept_note": "- **`check_prompts.md` §17 does not fire on `compound-and-close-1` (2026-09-05).** §17's structural trigger narrowed to plan mode only on 2026-09-03, retiring the 4+-named-sub-steps half; this fence never enters plan mode, so neither the in-fence `No preamble.` suppressor nor a body skim-past primer is owed. Do not re-file it by counting sub-steps.",
  "anchor": "**Accept-with-mitigation** (per `check_pedagogy.md` §50 and §51 — judge findings carried as design-intent, not blockers):",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "§17: \"Everywhere else §17 is advisory: fix the file where a run actually showed preamble eating the read; never sweep the corpus counting sub-steps.\" The refutation checks out — I read compound-and-close-1.md in full (four paragraphs, no plan-mode invocation; grep for plan mode across the exercise and all compound-and-close prompts returns only the arc line at compound-and-close.md:112 pointing forward to M2), and commit 289b45a3 (2026-09-03 07:44) does narrow the trigger. But the rule keeps a live advisory path this row never adjudicated, and the drafted headline \"§17 does not fire on compound-and-close-1\" plus \"neither ... is owed\" closes it: a future judge with an actual run showing preamble eating the 3–5 line summary would read the note and stand down. Second defect: the note narrates the rule's own edit history (\"narrowed to plan mode only on 2026-09-03, retiring the 4+-named-sub-steps half\"), which is `check_writing.md` §3's banned shape inside a maintainer block (`retired` is in the banned list; rules are rules, history is git).",
   "note_fix": "- **`check_prompts.md` §17's structural trigger does not reach `compound-and-close-1` (2026-09-05).** The trigger is plan mode only, and this fence enters no plan mode, so neither the in-fence `No preamble.` suppressor nor a body skim-past primer is owed on structural grounds. A sub-step count is not evidence here. §17 stays advisory on this fence: the only thing that reopens it is a run where preamble actually ate the read."
  }
 },
 {
  "target_file": "curriculum/exercises/compound-and-close.md",
  "instance": "ae101--exercise--compound-and-close.technical.json",
  "rule": "prompts 40",
  "claim": "Definite references inside compound-and-close-1 ('the orientation and introspection', 'the TDD bug fix') are curriculum-coined labels never typed verbatim into Claude by an earlier prompt, so §40b's warm-but-referent-absent shape fires.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "§40b's tell is 'a definite article in front of a curriculum-coined noun phrase' that is 'the sole anchor for an action' — its worked symptom is a fence opening 'Do the second-pass read…', where the coined label carries the whole instruction. Here the fence's first clause is 'Review this session end-to-end', which carries scope on its own; the list that follows confirms it. §40b's own cure order settles the disposition: 'Test: delete the clause, ask what the receiving agent now does differently. Nothing → the deletion IS the fix', and the judge itself concluded nothing changes. That test also forbids the fix_hint's remedy — 'a like-for-like paraphrase preserves the defect while spending a new term to do it.' So the two live options are delete-or-leave, not rewrite; and the glosses are also plain descriptions of work an agent reading the scrollback can see happened (a repo read, a self-report on what was skipped, a tests-first fix), not plumbing the agent cannot observe.",
  "evidence": "Read curriculum/prompts/compound-and-close-1.md and all three orient-and-introspect prompt bodies. The words 'orientation'/'introspection' appear in orient-and-introspect-1/2/3 only in the frontmatter `key:`, never in the fence text sent to Claude — so the judge's mechanical claim is correct as stated. Session boundary confirmed warm: no `**Session**` widget or /clear between orient-and-introspect and compound-and-close. Rule read via `node curriculum/evals/scripts/rule.js prompts 40` including 40a/40b in full. Confidence is medium, not high, because the phrase does match §40b's coined-label surface test; the refute rests on the sole-anchor and cure-order clauses, not on the surface test.",
  "accept_note": "- **`check_prompts.md` §40b does not fire on `compound-and-close-1`'s review list (2026-09-05).** The fence opens on *Review this session end-to-end*, which carries the scope by itself; *the orientation and introspection*, *the /context read*, *the TDD bug fix*, *the diff push-back*, *the bug rules read off the ticket* confirm that scope rather than anchoring any action, so none of them is the sole referent §40b guards. If it is ever tightened, §40b's cure order applies — delete the list, never paraphrase the glosses into new terms.",
  "anchor": "**Accept-with-mitigation** (per `check_pedagogy.md` §50 and §51 — judge findings carried as design-intent, not blockers):",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The triage's load-bearing quote is not in the rule. `node curriculum/evals/scripts/rule.js prompts 40 | grep -c \"sole anchor\"` returns 0 — \"the sole anchor for an action\" is the JUDGE's own mitigation phrasing, re-attributed to §40b as a scoping clause. §40b's actual tell is unqualified: \"Tell: definite article in front of a curriculum-coined noun phrase.\" Worse, the triage inverts the cure it cites: §40b says \"Test: delete the clause, ask what the receiving agent now does differently. Nothing → **the deletion IS the fix** and the coined term leaves with it. Something → then cure additively.\" Both branches produce work; there is no leave-as-is branch. Triage read \"Nothing → the deletion IS the fix\" as permission to leave. Mechanically confirmed: 'orientation'/'introspection' appear in orient-and-introspect-1/2/3 only in frontmatter `key:`, never in fence text; compound-and-close.md's maintainer block carries no note authorising the list. Fix is a prompt-body edit students copy → route as a card, not an accept-note."
  }
 },
 {
  "target_file": "curriculum/exercises/compound-and-close.md",
  "instance": "ae101--exercise--compound-and-close.technical.json",
  "rule": "prompts 43",
  "claim": "compound-and-close-1 is anticipated as a multi-minute run (the body's own 'runs past a couple of minutes' callout) but the fence carries no proactive in-fence instruction to surface incremental progress.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "§43's trigger is 'Prompt that will run more than a couple of minutes' with a named shape list — 'cross-project ~/.claude/projects/ scans, read-the-world memory builds, big multi-file reads, walk-away send-offs.' compound-and-close-1 is none of them: it reviews scrollback already resident in the window and writes one file. The judge's evidence inverts the body callout it leans on — L23 reads 'If the review stalls **or runs past a couple of minutes, interrupt** with `Esc`', which names that duration as the anomaly the student cuts short, not as the expected run. A prompt whose body tells the student to interrupt at two minutes is not a prompt the rule classes as long-running, and reading the escape hatch as proof of duration would make §43 fire on every fence that ships one.",
  "evidence": "Rule read in full via `node curriculum/evals/scripts/rule.js prompts 43`. compound-and-close.md L23 quoted verbatim above; the same blockquote also carries the opposite tell ('a summary that arrives fast and reads clean has usually covered the last few turns and skipped the rest'), i.e. it is a two-sided steering callout, which is §48's reactive half. Fence read in full: no tool-heavy scan; sole write target is ./CLAUDE.local.md plus a .gitignore line. Medium rather than high because §43's list is illustrative rather than closed, and 'Long session, long read' is a fair prompt for a judge to stop on.",
  "accept_note": "- **`check_prompts.md` §43 does not fire on `compound-and-close-1` (2026-09-05).** The review reads scrollback already in the window and writes one file, so it is none of §43's long-run shapes. The *Long session, long read* callout names *runs past a couple of minutes* as the stall to interrupt, not the expected duration; it is §48's reactive half against an anomaly, and reading it as evidence of a long run inverts it. Do not add in-fence progress narration — the fence's ask is a 3–5 line summary at the end.",
  "anchor": "**Accept-with-mitigation** (per `check_pedagogy.md` §50 and §51 — judge findings carried as design-intent, not blockers):",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The refutation is self-defeating. It concedes the L23 blockquote is \"§48's reactive half\" — but check_pedagogy.md §48 is titled \"**Heavy fan-out prompts get body recovery callout**\" and requires \"(a) heavy run expected, (b) recovery for BOTH failure directions,\" then closes: \"**Pair this reactive callout with the proactive prompt-side progress-streaming rule (`check_prompts.md §43`) so a long run is neither silent nor unsteerable.**\" §43 says the same from its side: \"Proactive half; `check_pedagogy.md §48` = reactive half... Pair them.\" So either the §48 callout is owed here — in which case §43's half is owed too, by both rules' explicit pairing instruction — or the callout is excess; the triage cannot claim the callout is lawful §48 material and simultaneously that the run is too short for §43. The file's own callout heading is \"**Long session, long read**\" and the body says \"Claude reviews the whole scrollback in one shot,\" which is §48's (a). Two further failures: (i) the triage's \"none of §43's named shapes\" argument does not discharge a trigger it itself admits is illustrative (\"§43's list is illustrative rather than closed\") — the trigger is duration; (ii) §43's second requirement is untouched by the refutation: \"Also tail the fence to honor a mid-run *'tell me what you've found so far'* → report, then continue (not restart).\" The body already promises the student can `Esc`, narrow, and say `continue` — the fence carries nothing to make that resume rather than restart, which is the concrete harm the tail exists to prevent. The accept-note's flat \"Do not add in-fence progress narration\" would switch that off permanently."
  }
 },
 {
  "target_file": "curriculum/exercises/diagnose-and-resend.md",
  "instance": "ae101--exercise--diagnose-and-resend.technical.json",
  "rule": "prompts 38",
  "claim": "The L93 lead-in opens on 'assemble' while its fence (diagnose-and-resend-6) opens on 'Build', so the lead-in and the fence do not share a verb.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Real under §38's stated test ('place body lead-in's last sentence next to prompt's first sentence; share verb + noun-phrase?') — 'assemble' and 'Build' are near-synonyms, not an echo. The swap is mechanical: one word, claim and ask unchanged, and the maintainer has already called this exact class of edit. Commit 464661b1's own message files four of these as 'Mechanical applications, no new decisions. Lead-ins re-open on the verb their fence opens on (set-the-markers P2/P4, diagnose-and-resend's send-off, close-the-ticket P3) -- the ask is unchanged in every case, only which word leads.' This is the same file, the same class, the one instance that pass did not reach. Not card-shaped for that reason.",
  "evidence": "diagnose-and-resend.md L93: 'Ask Claude to assemble both, scoped to the same task, in conversation.' curriculum/prompts/diagnose-and-resend-6.md body opens: 'Build me two task-scoped artefacts for re-running the un-packaged task packaged.' Uniqueness: `grep -o -F 'Ask Claude to assemble both, scoped to the same task, in conversation.' curriculum/exercises/diagnose-and-resend.md | wc -l` -> 1. No backing-block claim quotes the lead-in (Claims list L172-182 contains no 'assemble'). §38a's surprise-pedagogy carve-out does not apply — no easter-egg note in the maintainer block, and the fence's behaviour is the point of the section.",
  "old_string": "Ask Claude to assemble both, scoped to the same task, in conversation.",
  "new_string": "Ask Claude to build both, scoped to the same task, in conversation.",
  "surface": "student-facing-body"
 },
 {
  "target_file": "curriculum/exercises/diagnose-and-resend.md",
  "instance": "ae101--exercise--diagnose-and-resend.technical.json",
  "rule": "prompts 43",
  "claim": "ae101-m5-rerun-packaged is an explicit walk-away send-off ('I'm walking away' is inside the fence) but carries no in-fence instruction to surface incremental progress as it works.",
  "disposition": "CARD",
  "confidence": "high",
  "why": "§43 names 'walk-away send-offs' as a trigger shape verbatim, and this fence says 'I'm walking away' in its own text — the trigger is met on the rule's own words, so a refute would have to argue past the clearest case the rule describes. The proactive half is genuinely absent: RUN-NOTES.md is written only on getting stuck, and the three-list report fires at the end. It is card-shaped rather than direct because it adds a clause to a prompt body the student copies, and because the maintainer's related call cuts both ways: set-the-markers-send-it-off.md L68 (2026-09-03, his own commit 289b45a3) declines §43 streaming on the M4 send-off and closes with 'Both shapes belong to the M5 re-send' — which points at this prompt without instructing the addition, and the §19 self-augment half of that pair is already here ('Then ask for or write anything else you want for the session') while the §43 half is not.",
  "evidence": "Rule read in full (`rule.js prompts 43`). curriculum/prompts/ae101-m5-rerun-packaged.md read end-to-end: fence carries the coordinates block, a self-augment line, the run instruction, a four-clause 'Rules:' line ending 'I'm walking away', and a three-list closing report. No 'as you go' / progress clause and no mid-run 'tell me what you've found so far' tail. Body context: diagnose-and-resend.md L133 (ask for artifacts that didn't ship — §48's reactive half) and L135 ('The laptop stays awake and plugged in while it runs'). set-the-markers-send-it-off.md L68 quoted above. Anchor uniqueness: `grep -o -F 'Rules: run the verifier per plan.md cadence' | wc -l` -> 1.",
  "anchor": "curriculum/prompts/ae101-m5-rerun-packaged.md, the 'Rules:' line",
  "before": "Rules: run the verifier per plan.md cadence; don't paper over failures; if you get stuck, write into RUN-NOTES.md and try a different angle; I'm walking away.",
  "after": "Rules: run the verifier per plan.md cadence; don't paper over failures; if you get stuck, write into RUN-NOTES.md and try a different angle; I'm walking away.\n\n**As you go, say in a line or two what you just finished and anything that surprised you. If I check in mid-run, tell me where you are and keep going.**",
  "risk": "Three. (1) It pulls against the fence's own closing discipline — 'Report back as three lists, no narrative, no hedging' exists to stop exactly the running commentary this invites, and a chatty run may seed the hedged summary the three-list format was built to suppress. (2) The wording must NOT route progress into RUN-NOTES.md: that artefact's frontmatter is `conditional: agent-got-stuck` and its note records a 2026-08-31 fix where declaring it unconditional made a smooth run list it under 'did NOT ship' and failed the M5 chain's PC gate — an 'as you go, write into RUN-NOTES.md' phrasing regresses that. (3) It adds a fifth paragraph to an already-wide fence; §36 is satisfied (the addition is its own move) but the prompt grows. If you would rather decline, the decline belongs in diagnose-and-resend.md's maintainer block as a §43 accept-note, since L68 of set-the-markers currently reads as pointing the shape here.",
  "card": {
   "order": null,
   "outcome": "declined",
   "note": "Unattended by design — nobody is at the machine and the not-knowing is the lesson. check_prompts §43 gained the carve-out; decline recorded in the maintainer block; instance carries a resolution.",
   "at": "2026-09-05"
  }
 },
 {
  "target_file": "curriculum/exercises/diagnose-and-resend.md",
  "instance": "ae101--exercise--diagnose-and-resend.writing.json",
  "rule": "writing 3",
  "claim": "The backing block's three-failure-modes source entry carries '(corrected 2026-08-01)', which narrates how the claim came to be true rather than stating what is true.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Real and lands directly — the defect is inside a `<!-- backing -->` block, which §3 explicitly extends to ('Grep is blind on stamp-dense surfaces — Source verification + <!-- backing --> blocks are saturated with legal [checked:...] stamps ... There the tense test is the only one that fires'). '(corrected 2026-08-01)' passes the tense test as a transition: the sentence stands whole as present fact without it, and it is not a dated provenance header of the protected kind (those attribute a maintainer decision — '(2026-08-14, Antti-directed)' — this one records that a prior claim was wrong). §3's minimal-diff clause forces the second half of the edit too: 'the WHOLE touched note re-takes the fact-or-transition test, not just the clause you came to fix', so the trailing 'the corrected framing is the fallback' goes with it. Nothing is lost: the entry's own sentences already state the non-convergence as present fact.",
  "evidence": "diagnose-and-resend.md L185, read in full. Uniqueness: `grep -o -F '**Not convergent practitioner vocabulary** (corrected 2026-08-01): only *context rot* is a term the field actually uses' | wc -l` -> 1; `grep -o -F 'fallback: none needed; the corrected framing is the fallback.' | wc -l` -> 1; both sit on the same physical line, so one contiguous old_string covers them. The judge's own distinction holds on re-read: the adjacent '(full-text and 14-post archive check, 2026-08-01)' on L188 documents verification scope, which §3 protects, and is left untouched. Rule read via `rule.js writing 3` in full, including the 84-of-101 dated-provenance carve-out and the 'do not file §3 on a dated provenance header alone' instruction to judges — this row quotes a transition, so it clears that bar.",
  "old_string": "**Not convergent practitioner vocabulary** (corrected 2026-08-01): only *context rot* is a term the field actually uses, with a study behind it and unprompted use by Ronacher and Osmani. *Goal drift* and *plausible-but-wrong* return zero named practitioners. The phenomena are well attested; the names are largely ours, which is all this exercise needs — it applies the lenses, the pre-read carries the colour. fallback: none needed; the corrected framing is the fallback.",
  "new_string": "**Not convergent practitioner vocabulary.** Only *context rot* is a term the field actually uses, with a study behind it and unprompted use by Ronacher and Osmani. *Goal drift* and *plausible-but-wrong* return zero named practitioners. The phenomena are well attested; the names are largely ours, which is all this exercise needs — it applies the lenses, the pre-read carries the colour. fallback: none needed; the framing above is the fallback.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/exercises/set-the-markers-send-it-off.md",
  "instance": "ae101--exercise--set-the-markers-send-it-off.writing.json",
  "rule": "student_facing 33",
  "claim": "L38's clause 'and the next module forks from the local commit' names what a later module will do — the rule's predicate-leak shape — and duplicates the maintainer block's own statement of the same fact.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "The clause matches §33's 2026-08-23 predicate-leak clause on its face: 'Any clause promising what a later beat will do is cartography however the sentence starts, and it is a cheque that beat must cash.' The rule's supporting boundary also holds — 'Where the fact is genuinely needed, it usually already lives on the trainer surface. Check there before assuming a cut deletes knowledge' — and it does, at L81 ('M5's worktree fork reads exactly that commit'). But this is a judgement, not a rule application: the clause is also the only in-body reason a local-only commit is safe, which is what makes L40's push prompt optional. §33 gives no carve-out for load-bearing cartography, and none of the file's four existing accept-notes (L66/L68/L70/L74) covers it. Antti is better placed to say whether the student needs the reason or only the permission.",
  "evidence": "set-the-markers-send-it-off.md L38 read in place under the '## Push it only if you want it off this laptop' header, with L40's optional push lead-in below it. Rule read in full via `rule.js student_facing 33`, including the boundary list of exempt signposting sections — this bullet is body prose under a `##`, not one of them. Maintainer block read end-to-end: L66, L68, L70, L74, L93 are the accept-notes and none names this clause; L81 carries the same fact for the trainer. Uniqueness: `grep -o -F 'This is throwaway work you can reset away, and the next module forks from the local commit.' | wc -l` -> 1. Deletion test run as the rule specifies: the two neighbouring sentences join cleanly.",
  "anchor": "L38, under '## Push it only if you want it off this laptop'",
  "before": "- The commit is local, and that is the default. This is throwaway work you can reset away, **and the next module forks from the local commit**.",
  "after": "- The commit is local, and that is the default. This is throwaway work you can reset away.",
  "risk": "The clause is the student's only in-body answer to 'if I don't push, does this commit ever get used?' — cut it and the optional push at L40 loses its stated justification, and a reader may push out of doubt, which is the behaviour the bullet exists to make unnecessary. The countervailing risk of keeping it is smaller but real: it is a cheque M5 must cash, and if the worktree fork point ever changes, this line goes stale in a file nobody edits for that reason. If you keep it, the todo clears by an accept-note in the maintainer block naming the clause as a deliberate §33 exception, not by leaving it unmarked — otherwise the next writing judge re-files it."
 },
 {
  "target_file": "curriculum/exercises/set-the-markers-send-it-off.md",
  "instance": "ae101--exercise--set-the-markers-send-it-off.writing.json",
  "rule": "prompts 36",
  "claim": "take-task-end-to-end's second paragraph carries a framing move plus two conditional moves ('if X, do A; if Y, do B') in one ~40-word paragraph and should be split.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "§36's split trigger is a 'continuous instruction-stream' where 'Claude (and student) drop structural signals mid-paragraph', and its own sentence-grain remedy names the defect precisely: 'two independent instructions joined by \"and\"/comma read as one move and skim as one — split into separate simple sentences (\"Do X. Then Y.\")'. That remedy is already satisfied here — the paragraph is four short simple sentences, each conditional standing alone and full-stopped, nothing comma-joined. The prompt is also already split by move at the level §36 governs: paragraph one is the task and its inputs, paragraph two is the walk-away contract. The judge conceded the read-aloud diagnostic came out 'genuinely close' and identified no dropped signal, which is the rule's own test returning nothing. Splitting further buys whitespace, not a structural signal. Deliberately NOT resting this on the L68 accept-note, which authorises 'no added clauses' and says nothing about paragraph geometry.",
  "evidence": "curriculum/prompts/ae101-m4-take-task-end-to-end.md read in full; paragraph 2 is 'I'm leaving the laptop awake and walking away. Work through it. If you get stuck, write what you tried and why it didn't work rather than inventing a way forward. If you finish, tell me what you shipped and what you didn't.' — 44 words, four sentences, two of them full conditionals with no internal comma-splices. Rule read via `rule.js prompts 36` in full, including the sequential-build carve-out (which the judge correctly applied to commit-starting-point and did not re-file, per maintainer L70) and the sentence-grain clause. Medium rather than high: the conditional pair IS on §36's named move-boundary list, so a judge stopping at that list rather than at the harm reads it the other way.",
  "accept_note": "**`ae101-m4-take-task-end-to-end`'s second paragraph stays whole — `check_prompts.md §36` is already met at sentence grain (2026-09-05).** The framing line and the two conditionals are four short simple sentences, not the comma-joined instruction-stream §36 splits, and the prompt is already broken by move: the task and its inputs, then the walk-away contract. A further break buys whitespace, not a structural signal. Do not split it, and do not re-raise.",
  "anchor": "**`ae101-m4-take-task-end-to-end` takes no added clauses — maintainer call 2026-09-03, on two separate proposals.**",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The triage's load-bearing claim — that §36's sentence-grain clause is the remedy and is already satisfied — does not check out against the rule body. The clause opens \"**Sentence grain, not just paragraph grain:**\" and closes \"...split into separate simple sentences (*\\\"Do X. Then Y.\\\"*), **which usually earn separate paragraphs**\": it adds a grain, it does not discharge the paragraph obligation. The trigger is not \"continuous instruction-stream\" (that is the stated harm, \"Wide single-paragraph prompts = continuous instruction-stream\") but \"Prompt carries >2 logical moves... → split into paragraphs by move, one move per paragraph\", and the rule names this exact shape twice, including in \"Common move-boundaries earning split: ... *\\\"If X, do A; if Y, do B\\\"* (conditional)\" — which is literally paragraph 2 (\"If you get stuck... If you finish...\"). No carve-out reaches it: the prompt is ~95 words with many moves so \"Short prompts (≤30 words / ≤2 moves)\" fails, and two mutually-exclusive branches are not a \"sequential-build\" chain where \"each step compounds tightly.\" The triage itself logged medium confidence and conceded \"the conditional pair IS on §36's named move-boundary list.\" A note saying \"Do not split it, and do not re-raise\" would switch §36 off on its own named specimen; the split-or-not is a maintainer card, not a triage call."
  }
 },
 {
  "target_file": "curriculum/exercises/set-the-markers-send-it-off.md",
  "instance": "ae101--exercise--set-the-markers-send-it-off.writing.json",
  "rule": "prompts 38",
  "claim": "The L58 lead-in 'ask it to keep going' does not mirror nudge-continue's opening word 'Continue'.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "Correct under §38's share-verb test, and mechanical to clear: one word, the claim and the ask unchanged, only which word leads. The maintainer has already ruled this class in this very file — commit 464661b1 fixed 'set-the-markers P2/P4' lead-ins under the heading 'Mechanical applications, no new decisions ... Lead-ins re-open on the verb their fence opens on ... the ask is unchanged in every case, only which word leads.' P5 is the one the pass did not reach. §38a's surprise carve-out does not shelter it either: the next sentence already tells the student the joke ('The nudge reads as encouragement and lands as a taunt'), so nothing is being hidden between paste and execution, and the file carries no easter-egg note.",
  "evidence": "set-the-markers-send-it-off.md L58: 'If the agent stalls, ask it to keep going. The nudge reads as encouragement and lands as a taunt.' curriculum/prompts/ae101-m4-nudge-continue.md body: 'Continue --- you can do it. Can\\'t you?' Uniqueness: `grep -o -F 'If the agent stalls, ask it to keep going.' | wc -l` -> 1. File has no `<!-- backing -->` block, so no claim anchor quotes the lead-in. Rule read in full via `rule.js prompts 38` including 38a. Medium rather than high because this is a synonym swap on existing student-facing prose and `feedback_curriculum_default_move_is_cut` records that 'original is better' beats improvement swaps — if Antti prefers the warmer 'keep going' beside the encouragement sentence, clear the row with an accept-note instead and the edit is cheap to skip.",
  "old_string": "If the agent stalls, ask it to keep going.",
  "new_string": "If the agent stalls, ask it to continue.",
  "surface": "student-facing-body"
 },
 {
  "target_file": "curriculum/lectures/what-keeps-a-long-running-session-going.md",
  "instance": "ae101--lecture--what-keeps-a-long-running-session-going.pedagogy.json",
  "rule": "student_facing 9",
  "claim": "The L37 kicker asks the student to 'check whether there was a way for it to uncover that' but names no concrete tell, while the maintainer block already holds one (connector / file / command / question to a colleague) where no student or in-room trainer reads it.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "The finding sits exactly on §9's look-line clause and its mechanical source: 'Any line asking the student to read, watch, check, notice or judge owes ... (b) The tell — what a good or bad instance looks like ... Mechanical source for the tell, and this is the point: it is usually already written, one fence away. Files carry per-phase failure modes and leap tests in the maintainer block ... For each failure row, ask: could the student catch this themselves, in the moment? Yes → its tell belongs in body.' The tell is verbatim in the maintainer block at L47 and the student can apply it on return. It is card-shaped and not a mechanical fold-in because L47 records the kicker as '(2026-08-14, Antti-worded)' and the backing block at L60 adds 'Antti's own frame, near-verbatim ... Do not quantify it, and do not soften it' — lengthening a sentence he wrote, in a slot he deliberately kept lighter than a Note widget, is his call.",
  "evidence": "what-keeps-a-long-running-session-going.md L37 (kicker), L32 (the three examples the judge credits as partial texture), L47 (maintainer note carrying 'the check is whether a connector, a file, a command or a question to a colleague would have dissolved it', plus 'Kicker shape rather than a **Note** widget on purpose: a Note box would out-weigh the bullet it comments on' and 'Placement is fixed to this slide'), L60 (backing claim `missing-info-was-usually-reachable`). Rule read in full via `rule.js student_facing 9`. Anchor caution: `grep -o -F 'check whether there was a way for it to uncover that. Usually there was.' | wc -l` -> 3 — body L37, maintainer L47 and backing L60 all carry the sentence verbatim, so this cannot be applied by a single unqualified string replace.",
  "anchor": "L37, the slide-4 kicker",
  "before": "When your agent stops for missing information, check whether there was a way for it to uncover that. Usually there was.",
  "after": "When your agent stops for missing information, check whether there was a way for it to uncover that — **a connector, a file, a command, or a question to a colleague**. Usually there was.",
  "risk": "Three. (1) L47 says the kicker is a trainer's aside kept deliberately lighter than a Note widget because 'a Note box would out-weigh the bullet it comments on' — a four-item list inside the aside is the weight that note was avoiding, and `check_slides.md §7` is the sibling that fires on padding a line into a list. (2) The sentence appears verbatim in three places: applying this to L37 alone leaves the L47 maintainer quote and the L60 backing claim quoting a sentence that no longer exists, which trips §3's keep-notes-bind-to-their-passage clause and the backing-anchor validator — all three must move in the same edit. (3) Antti worded this line; if the answer is that the tell stays trainer-side, the todo clears with a one-line accept-note in the maintainer block saying so, and that is cheaper than the edit."
 },
 {
  "target_file": "curriculum/lectures/what-keeps-a-long-running-session-going.md",
  "instance": "ae101--lecture--what-keeps-a-long-running-session-going.writing.json",
  "rule": "writing 3",
  "claim": "L49's maintainer note narrates a completed edit — 'the *most expensive of the four* tail that first shipped with it was cut as NVA, since ...' — rather than stating a standing constraint.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The narrated clause the finding quotes no longer exists. L49 now states the rationale in present tense and records the constraint as a bare negative, with no transition verb and no 'first shipped / was cut' history.",
  "evidence": "`grep -c 'cut as NVA' curriculum/lectures/what-keeps-a-long-running-session-going.md` -> 0; `grep -c 'first shipped with it' ...` -> 0. Current L49 reads: 'It reads as a bare observation on purpose (2026-08-14): ranking the four changes nothing the student does and the bullet already shows the cost. No *most expensive of the four* tail.' Flag for the orchestrator, out of scope for this row but worth knowing before the next writing fire: the replacement's closing sentence is a bare negative naming a thing that does not exist, which §3's 2026-09-05 clause ('a negative sentence is not automatically present state ... Defining a beat by what it is not describes the ghost in order to forbid it') would read as the same defect in its next costume. That is a new finding against the current text, not this todo, and I have not filed it."
 },
 {
  "target_file": "curriculum/lectures/where-the-rule-could-live.md",
  "instance": "ae101--lecture--where-the-rule-could-live.writing.json",
  "rule": "writing 3",
  "claim": "The 'Slides-only pass (2026-07-02, unaudited)' note at L25 carries a per-passage verdict trail — DELETED / CONVERTED / SUBSUMED / CUT x3 / KEPT — narrating a completed edit instead of stating current state.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The verdict trail is gone. L25 now states current state ('file is lede + two slides', what slide 1 runs, what slide 2's first bullet carries, who owns the M3 bridge) and keeps exactly the one clause the judge's own reading said to keep — the ticket-pair dependency on `extract-the-task-shaping-rule-4`, which §3 protects as an empirical finding that would otherwise need re-deriving. The fix landed as the finding asked.",
  "evidence": "`grep -cE 'DELETED|CONVERTED|SUBSUMED|KEPT as the closer' curriculum/lectures/where-the-rule-could-live.md` -> 0; `grep -c 'Path A' ...` -> 0. Current L25 read in full; the surviving dependency clause is 'The ticket-pair paragraph depended on `extract-the-task-shaping-rule-4`, itself a `{{cut:}}` candidate; if the cull reverses, the exercise carries the connection.' Rule read via `rule.js writing 3`, including the 'Keep: ... an empirical finding that would otherwise be re-derived' clause that licenses it."
 },
 {
  "target_file": "curriculum/lectures/where-the-rule-could-live.md",
  "instance": "ae101--lecture--where-the-rule-could-live.writing.json",
  "rule": "sales_copy 8",
  "claim": "L9's 'The file decides what a well-shaped ticket looks like.' puts an agent's verb on a static file, and does so in a lecture whose thesis two bullets later reserves motion for the agent.",
  "disposition": "CARD",
  "confidence": "high",
  "why": "§8 is one line and this is its named shape: 'Agency on right noun. \"Memory reads code\" wrong — memory = store; agents read. Put verb on agent, or rephrase.' Deciding is an agent act; the file is a store. The doubled harm the judge names is verifiable in the file: L12's header is 'The file is steady; the agent moves' and L16 is 'Trigger and runtime are wiring choices; the file rides along unchanged' — L9 hands the file exactly the agency slide 2 spends its three bullets taking back. It is card-shaped because the fix rewrites a student-facing lecture sentence's subject and voice, and the maintainer block's only governing note (L21) rules on the noun ('*the file* is the correct word throughout the body and the anchors — do not sweep it back to *your rules*') and not on who acts, so the rewrite has to keep 'the file' present while moving the verb — a wording call, not a swap.",
  "evidence": "where-the-rule-could-live.md L9 read in place, with L12/L16 for the thesis. Rule read via `rule.js sales_copy 8` (full body is the single line quoted). In-scope check run before triaging: the writing class brief for this file evaluates check_sales_copy.md rules 8, 9 and 11 and marks the rest N/A, so the compendium is deliberately loaded here despite sales_copy being the buyer-facing surface — no surface-grounds refute available. Backing-anchor check: `agent-triages-the-issue` (L52) quotes L9 only through '...splits the issue into smaller tickets.' and `issue-webhook-shape` (L51) quotes only the first sentence, so the final sentence is unanchored and can change without breaking a claim quote. Uniqueness: `grep -o -F 'The file decides what a well-shaped ticket looks like.' | wc -l` -> 1 (note the shorter prefix 'The agent runs with the file in context: it proposes labels' returns 2 — body L9 and backing L52 — so the anchor must include the final sentence).",
  "anchor": "L9, the '**Issue webhook.**' bullet on slide 1",
  "before": "The agent runs with the file in context: it proposes labels, asks for missing info, or splits the issue into smaller tickets. **The file decides what a well-shaped ticket looks like.**",
  "after": "The agent runs with the file in context: it proposes labels, asks for missing info, or splits the issue into smaller tickets. **It judges a well-shaped ticket against the file.**",
  "risk": "The three shape bullets are rhythmically parallel and each lands on a short payoff sentence — 'You arrive to a proposal, not a queue.' closes the third — so the replacement has to stay short or the bullet sags. 'It judges ... against the file' also shifts the emphasis from the file's authority to the agent's act, which is the point of the fix but slightly dims the bullet's job of showing the file doing work in a second runtime; a non-agentive alternative that keeps the file as topic is 'A well-shaped ticket is whatever the file says it is.' — same fix, different register, your call which reads more like the room. Nothing downstream breaks: no backing claim quotes this sentence.",
  "card": {
   "order": null,
   "outcome": "declined",
   "note": "The beat is the agent helping MAKE tickets, not evaluating them, and judge is embargoed before the M5 closer. Sentence stands; vocabulary.md gained the judge row; decline recorded in the maintainer block.",
   "at": "2026-09-05"
  }
 },
 {
  "target_file": "curriculum/exercises/fix-tests-first.md",
  "instance": "ae101--exercise--fix-tests-first.pedagogy.json",
  "rule": "pedagogy 47",
  "claim": "The third forcing function (root-cause interrogation, prompts 2-3) has no Watch-for naming its dominant failure mode plus an escape hatch; only Tests-skipped and Diff rubber-stamp are covered.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "§47 requires, for each phase carrying a forcing function per §16, that the maintainer block name (a) the dominant student failure mode and (b) one escape hatch; 'Either unanswered -> REVISE'. §16's carve-out for optional beats governs LOCATION (body vs fence), not whether the beat counts as a forcing function, so the Optional label does not exempt it. The beat is load-bearing: leap-test item 3 (L123) promises 'Runs root-cause interrogation before accepting Claude's first fix' as a Monday-morning outcome, which is exactly the §47/§45 pairing the rule polices. Fix lands in the Watch-fors list at L104-106, which sits below `<!-- maintainer -->` (L51) and is stripped from the student build — derive-body-view.js cuts everything from the maintainer marker to EOF — so it applies directly, not as a card. New entry uses 'the agent' to match the beat's own body prose at L31/L33 (student_facing §21).",
  "evidence": "Read curriculum/exercises/fix-tests-first.md in full (124 lines). Watch-fors at L104-106 hold exactly two entries; grep -c 'Diff rubber-stamp' = 1 (unique anchor). Maintainer marker at L51, no closing marker, and curriculum/evals/scripts/derive-body-view.js:108-114 sets the body cut at the maintainer line, confirming L104-106 is non-student-facing. Read `node curriculum/evals/scripts/rule.js pedagogy 47` and `pedagogy 16` in full for the carve-outs.",
  "old_string": "- **Diff rubber-stamp.** Student says *\"looks fine\"* under 30 seconds. Trainer push: *\"find me one line you'd have written differently — not wrong, just different.\"*",
  "new_string": "- **Diff rubber-stamp.** Student says *\"looks fine\"* under 30 seconds. Trainer push: *\"find me one line you'd have written differently — not wrong, just different.\"*\n- **Interrogation rubber-stamp.** Student takes the Optional-skip gate, or the agent's *\"nothing deeper here\"*, at face value and moves on without sending the interrogation prompt. Trainer push: *\"name one input where this fix would still be wrong.\"*",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/exercises/fix-tests-first.md",
  "instance": "ae101--exercise--fix-tests-first.story.json",
  "rule": "pedagogy 47",
  "claim": "The interrogation forcing function (fix-tests-first-2/3) has no named failure mode and no escape hatch in the Watch-fors list.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Same defect as the pedagogy instance's §47 row — two sibling judges filed the identical finding against the same two lines, with the same suggested shape ('Interrogation rubber-stamp'). One edit clears both rows; the orchestrator should apply this old_string/new_string ONCE and mark both todos cleared, not attempt it twice (the second application will fail on a missing anchor, which is the correct signal that it already landed). Disposition rationale identical: §47's failure-mode + escape-hatch pair is genuinely absent for the third forcing function, the Optional label does not exempt it under §16, and the Watch-fors list is below the maintainer cut so it applies directly.",
  "evidence": "Both instances quote the same two existing Watch-fors and the same missing third; story.json evidence names 'fix-tests-first-2/3' and pedagogy.json names 'prompts 2-3' — same beat. grep -c 'Diff rubber-stamp' curriculum/exercises/fix-tests-first.md = 1, so the anchor is unique and single-use.",
  "old_string": "- **Diff rubber-stamp.** Student says *\"looks fine\"* under 30 seconds. Trainer push: *\"find me one line you'd have written differently — not wrong, just different.\"*",
  "new_string": "- **Diff rubber-stamp.** Student says *\"looks fine\"* under 30 seconds. Trainer push: *\"find me one line you'd have written differently — not wrong, just different.\"*\n- **Interrogation rubber-stamp.** Student takes the Optional-skip gate, or the agent's *\"nothing deeper here\"*, at face value and moves on without sending the interrogation prompt. Trainer push: *\"name one input where this fix would still be wrong.\"*",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/exercises/fix-tests-first.md",
  "instance": "ae101--exercise--fix-tests-first.writing.json",
  "rule": "student_facing 21",
  "claim": "L18 says 'If Claude starts spinning up plenty of tests' — an autonomous-acting topic that §21 assigns to 'the agent', inconsistent with the file's own correct L31/L33 usage.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Real and mechanical. §21's concrete contextual split routes 'autonomous work' (files written, tests written, execution) to **the agent**; none of the three Claude carve-outs apply — this is not product/version grounding, not UI surface, and not addressee grammar (it is third-person description of what the agent does on its own). The file already models the correct term for the identical beat twice, at L31 ('see what the agent defends') and L33 ('ask the agent to interrogate the fix'), so the call is already made by the file itself plus the registry (curriculum/vocabulary.md, and CLAUDE.md's 'term change = registry first, then one sweep pass'). The swap leaves claim, voice and ask untouched — one noun, same sentence, same callout — so it is a mechanical sweep edit, not card-shaped, per the §26 standing exemption. Referent check: the following sentence's 'Steer it back' still resolves after the swap. No backing-block Claims row quotes L18 (claims quote L15, L16, L26, L27, L31, L45), so no anchor sync is owed.",
  "evidence": "Read the file in full. L18 is the only 'Claude starts spinning up' occurrence: grep -c 'If Claude starts spinning up plenty of tests' = 1. Body (L1-50) term counts: Claude at L18/L41x2/L49x2 = 5, agent at L26x3/L31x2/L33 = 6 — no 'Claude-count >> agent-count' audit signal, so this is a single local drift rather than assistant-shaped framing. Read `rule.js student_facing 21` in full including the 2026-08-30 active-stance override and its calibration pair. Checked the maintainer block L51-69 and L53/L57/L59/L61: no note authorises 'Claude' at L18.",
  "old_string": "If Claude starts spinning up plenty of tests, or the change creeps past the bug, that's sprawl.",
  "new_string": "If the agent starts spinning up plenty of tests, or the change creeps past the bug, that's sprawl.",
  "surface": "student-facing-body"
 },
 {
  "target_file": "curriculum/exercises/fix-tests-first.md",
  "instance": "ae101--exercise--fix-tests-first.writing.json",
  "rule": "prompts 2",
  "claim": "Prompt 3's lead-in (L37) states only the usage condition and never names what the prompt does; no command-verb action summary exists between prompt-2's marker and prompt-3's.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "The finding survives every §2 carve-out. §2a excuses lead-in LENGTH, not the absence of an action verb — its own fix is 'mirror only the prompt's OPENING clause', which still carries a verb. The dangling-colon sub-case does not apply: fix-tests-first-3.md ends 'Show me the diff before you commit.', not a colon. §2b does not apply (this is a student-run prompt, not a trainer-screen demo). §2c does not apply: the nearest `##` header is 'Interrogate the fix for a deeper layer' (L29), which names prompt-2's action, not prompt-3's second TDD pass, and prompt-3 does not sit directly under it — prompt-2 and its own lead-in intervene. But it is CARD, not a direct edit, because the file's recorded stance runs the other way: the 2026-08-25 'Lean-intro trim' note (L61) cut the TDD rationale on Antti's approval with 'do not restore the why', and the standing taste call is that additions to curriculum prose are near-auto-reject. Adding words here is a dosage decision Antti has not made. The proposed AFTER is net +8 words (13 -> 21) because it trades 'worth a second TDD pass' — already carried by the fence's own 'TDD-style' — for the action clause, and it mirrors both the fence's opening move and the file's own L33 gate-then-ask shape.",
  "anchor": "**Optional.** Send only if the interrogation surfaced a deeper layer worth a second TDD pass. (L37, immediately above {{prompt:fix-tests-first-3}})",
  "before": "**Optional.** **Send only if** the interrogation surfaced a deeper layer **worth a second TDD pass.**",
  "after": "**Optional.** **If** the interrogation surfaced a deeper layer, **ask the agent to prove it with a failing test and fix it.**",
  "risk": "If Antti reads the current gate-only sentence as deliberate — the Optional label plus prompt-2's neighbouring lead-in doing the cueing for both — then this is an unwanted addition to a file whose emphasis and length have been trimmed twice on his direction (L57, L61), and the right outcome is an accept-note at L37 rather than the edit. Secondary: 'prove it' echoes the exercise title 'Prove the bug, then ship the fix', which is intended resonance but could read as a tic if the same verb is already carrying weight elsewhere in the beat.",
  "evidence": "grep -c 'Send only if the interrogation surfaced' curriculum/exercises/fix-tests-first.md = 1. Read curriculum/prompts/fix-tests-first-3.md verbatim: body is one paragraph, five clauses, opening 'Now do it properly, TDD-style.', last line 'Show me the diff before you commit.' — no dangling colon, so the §2a sub-case is out. Read `rule.js prompts 2` in full (base + 2a + 2b + 2c). Section headers at L13/L24/L29/L43/L47 confirm prompt-3 sits under L29 with prompt-2 between."
 },
 {
  "target_file": "curriculum/exercises/fix-tests-first.md",
  "instance": "ae101--exercise--fix-tests-first.story.json",
  "rule": "prompts 2",
  "claim": "Prompt-3's lead-in names WHEN to send but never WHAT the prompt does; no command-verb summary between prompt-2's and prompt-3's markers.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "Duplicate of the writing instance's §2 row — same sentence at L37, same missing action verb, same suggested shape ('give prompt-3 the same shape as prompt-2's gate'). One card clears both rows; do not present it to Antti twice. Rationale identical: no §2 carve-out reaches it (2a excuses length not the missing verb, no dangling colon in fix-tests-first-3.md, not a trainer-screen fence, and the L29 header names prompt-2's action rather than prompt-3's), but the edit adds words to a file whose lean dose Antti trimmed on 2026-08-25 (L61), so the dosage call is his.",
  "anchor": "**Optional.** Send only if the interrogation surfaced a deeper layer worth a second TDD pass. (L37, immediately above {{prompt:fix-tests-first-3}})",
  "before": "**Optional.** **Send only if** the interrogation surfaced a deeper layer **worth a second TDD pass.**",
  "after": "**Optional.** **If** the interrogation surfaced a deeper layer, **ask the agent to prove it with a failing test and fix it.**",
  "risk": "Same as the writing-instance card: if the gate-only phrasing is deliberate, the outcome should be an accept-note at L37, not the edit. Additional risk unique to the duplication — if the orchestrator queues both rows as separate cards, Antti is asked the same question twice, which is exactly the attention cost the todo ledger is meant to reduce.",
  "evidence": "Both instances quote L37 verbatim and both propose an 'Otherwise, ask ...' clause; story.json's fix_hint and writing.json's fix_hint are the same edit in different words. grep -c 'Send only if the interrogation surfaced' = 1 confirms a single target line, hence a single card."
 },
 {
  "target_file": "curriculum/lectures/the-gate-is-a-claim.md",
  "instance": "ae101--lecture--the-gate-is-a-claim.technical.json",
  "rule": "research_claims 2a",
  "claim": "The husain-llm-judge stamp claims §2a durability, but the body sentence carrying the figure (L15) is undated, and §2a's three conditions are all required including 'dated in body'.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "The finding holds on §2a's letter. Conditions 1 and 2 are met — the stamp confirms Husain's first-person byline, and a judge validated against one domain expert's labels in his own engagement is a specific completed event, not a practice or trend. Condition 3 is not: L15 carries no date, and §2a names undated citation as precisely the failure mode it exists to stop ('an undated ten-hour port reads as this is what happens now'). The pull toward that misreading is real here because 'exactly this loop' is the loop the student was just told to run, so the number reads as a rate the student should expect. No carve-out rescues it and no maintainer note covers it: the stance 'decided' entry at L119 adjudicates the SAMPLE SIZE (2026-08-19, Antti), explicitly not the dating or the categorisation, and stretching it to cover this would be reading one accept-note onto its neighbour. It is CARD rather than a direct edit because L15 is projected body prose on a six-slide deck Antti has personally dosed (L51, L61, L63, L129, L131) and because the AFTER cannot be completed without a fact the repo does not hold — see risk. Recategorising the stamp under the Theory-construct variant, the judge's other option, does not work: that variant covers a named construct (the calibration loop, which husain-field-guide already carries), not a number.",
  "anchor": "L15, third bullet of `## The judge needs calibrating against your own judgement`; sibling adjudication convention is the dated notes at L119 and L129",
  "before": "**Hamel Husain reports** better than 90% agreement after three iterations of exactly this loop.",
  "after": "**Writing up one engagement's judge in [YEAR], Hamel Husain reported** better than 90% agreement after three iterations of exactly this loop.",
  "risk": "[YEAR] is a genuine blank, not a placeholder for style — hamel.dev/blog/posts/llm-judge/ has no publication date recorded anywhere in the repo. The stamp at L104 records only checked:2026-07-02, and grepping continuous-research/ for hamel.dev returns dated entries for revenge/, evals-skills/, ai-stack/, evals-faq/ and the Evals FAQ PDF but nothing for llm-judge/. So this card cannot land until someone opens the URL; the same open records publication on the stamp, which §11a wants anyway. Second risk, and it must ride in the same edit: the sentence is quoted verbatim as the Claims anchor at L85 (`husain-ninety-percent-three-iterations`), and backing-format.md makes the anchor the machine-verified join — grep -c of the sentence returns 2, body and anchor, so changing one without the other silently breaks the backing block. Third: the slide gains words, and the deck is at a 210w/6b cap per check-slide-size.js — slide 2 has headroom (three bullets), unlike slide 6 at 209w, but confirm before applying.",
  "evidence": "Read the file in full (135 lines). L15 verbatim, undated. grep -c 'Hamel Husain reports better than 90% agreement' = 2 (L15 body + L85 Claims anchor). Read `rule.js research_claims 2` in full including 2a's three-conditions clause and 2b. grep -rn 'hamel.dev' continuous-research/ returns 15 hits, none for /llm-judge/ and none carrying its publication date. Checked L116-120 stance block: the only 'decided:' entry is dated 2026-08-19 and scopes to starting sample size."
 },
 {
  "target_file": "curriculum/lectures/the-gate-is-a-claim.md",
  "instance": "ae101--lecture--the-gate-is-a-claim.technical.json",
  "rule": "research_claims 11a",
  "claim": "husain-evals is stamped due:none on a 'provenance tombstone' rationale, which is not one of §11a's three enumerated due:none categories (§2a durable, theory-construct, maintainer-ATTESTED).",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "The finding is right, and there is a sharper defect underneath it the judge did not name: the stamp's own justification is factually contradicted by the file. L106 asserts 'no body claim rides on this URL' and 'the two URLs above carry the body claims', but the Claims row at L86 lists `error-analysis-before-armchair` as backed by `husain-field-guide, husain-evals` — so a claim does ride on it, and the tombstone rationale is false as written. The fix that resolves both at once is to reclassify under the §11a category the maintainer has ALREADY applied to the sibling stamp for the identical claim: husain-field-guide carries 'Theory-construct variant (Antti 2026-08-31)' for error-analysis-first, cited by name with attribution on the line. husain-evals corroborates the same named construct, so it takes the same named due:none case, keeps the Claims row intact (dropping it instead would trip validate-backing.js's SOURCE-ORPHAN WARN at line 458), and preserves every factual element of the existing stamp including the CAVEAT that the post leads with synthetic test cases. Entirely inside the `<!-- backing -->` block, which backing-format.md states is 'not gated by the prompt-approval hook and not card-shaped (check_prompts.md §22, §26) — apply directly'.",
  "evidence": "Read the file in full. L86 Claims row: '`error-analysis-before-armchair` · detail · \"A good gate starts from real traces, not imagined failures.\" ← husain-field-guide, husain-evals'. L105 sibling stamp already reads 'Theory-construct variant (Antti 2026-08-31)'. grep -c 'provenance tombstone' = 1 (unique anchor). Read `rule.js research_claims 11` in full for the four-value DUE enum. Read scripts/validate-backing.js:19 and :458 — SOURCE-ORPHAN is a WARN raised when 'a defined source no claim cites', which is why the fix goes to the stamp rather than to the Claims row. Read curriculum/backing-format.md § Sources for the direct-apply clause.",
  "old_string": "[practitioner direct] due:none — provenance tombstone: no body claim rides on this URL, nothing to expire. Husain byline confirmed, judge-versus-human agreement loop confirmed (*\"iterate on the prompt of the critique model to make it sufficiently aligned\"*), BUT this post leads with synthetic test cases, not error-analysis-first — an earlier single-URL attribution was imprecise, and the two URLs above carry the body claims. Supporting only. fallback: drop this URL and the body loses nothing.",
  "new_string": "[practitioner direct] Theory-construct variant (Antti 2026-08-31), the same named `due:none` case its sibling husain-field-guide carries on the same claim: error-analysis-first cited by name, attribution on the line. This URL is the corroborating second Husain source under `error-analysis-before-armchair`, never its primary. Husain byline confirmed, judge-versus-human agreement loop confirmed (*\"iterate on the prompt of the critique model to make it sufficiently aligned\"*), BUT this post leads with synthetic test cases, not error-analysis-first — an earlier single-URL attribution was imprecise, so husain-field-guide leads and this one backs it up. fallback: drop this URL and the claim still stands on husain-field-guide.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/learn-from-the-test.md",
  "instance": "ae101--module--learn-from-the-test.story.json",
  "rule": "strategy_tie_in 1",
  "claim": "Phase 2 scores 7/10 because L31 ('This one runs in the session you sent off') assumes the M4 session is live, while L9 blesses arriving with one that ran out of credit, and nothing names the recovery.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "Wrong on the rule and wrong on the file. On the rule: §1's mood-floor reads '(8+/10 per beat, <7 = REVISE)'. The beat scored 7, which is not <7, so §1 does not authorise a REVISE row — and the judge's own evidence concedes as much ('this row is the flag, not a defect claim') before filing REVISE anyway. On the file: the claim that 'nothing in the module or the Connections-blocker push-back names the recovery' is false. The Connections-blocker push at L189 covers exactly the stated case ('closed laptop, ran out of credit, repo state unclear') and prescribes exactly the recovery the judge says is absent — 'Open a fresh Claude Code session in the repo and ask it to read what the M4 run touched', with the transcript path — and the same recovery is repeated as row 1 of the §47 phase table at L238. The judge cited 'L185', which is four lines short of the push it was looking for. Finally, the proposed fix — 'one clause before the L33 prompt naming the escape hatch' — is the specific move the 2026-08-30 accept-note at L126 forbids: 'The truly-absent case is room-scale triage — the Connections-blocker push, per §65(b) ... and do not add rescue blockquotes to the body.' That note is about §6 and `## Start here`, so it does not cover the done-done beat by itself, which is why an accept-note extending it is owed rather than a bare refutation.",
  "anchor": "**No rescue callout at `## Start here` — the asymmetry with M6's is deliberate (Antti 2026-08-30 ...)** — new note sits immediately after it, L126",
  "accept_note": "**The done-done fence assumes nothing about a live M4 terminal (2026-09-05, extends the 2026-08-30 rescue-callout note above).** *\"This one runs in the session you sent off\"* (L31) is the normal path, not a precondition. The absent-session case is room-scale triage and is already answered twice on the trainer side — the Connections-blocker push and row 1 of the §47 phase table both prescribe the same recovery: repo commits since M4, files modified, scrollback at `~/.claude/projects/<project>/`, and a fresh session asked to read what the M4 run touched. Judges should not re-file a missing escape hatch here, should not score the beat below the §1 floor on it, and should not propose a rescue clause before the L33 prompt. A 7 is not `<7`; §1's mood-floor gates REVISE at `<7`.",
  "evidence": "Read the file in full (270 lines). L189 Connections-blocker push and L238 phase-table row 1 both carry the recovery the finding calls absent; the judge's evidence cites 'the L185 Connections-blocker push-back', and L185 is the reading-the-return placement note, not the push. Read `rule.js strategy_tie_in 1` in full: the mood-floor parenthetical is '(8+/10 per beat, <7 = REVISE)'; grep for 'facilitator-premium' and '7 to 8' in check_strategy_tie_in.md returned zero hits, so the phrasing the judge attributes to 'the rule's own text' is not in the compendium. grep -c 'No rescue callout at' = 1 (unique anchor).",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The triage's load-bearing factual claim — that the Connections-blocker push \"prescribes exactly the recovery the judge says is absent\" — does not check out. The file's own Antti-directed maintainer note at L130 says the opposite: \"it needs a finished M4 send-off: the student pastes `ae101-m5-done-done` into that session, so the per-point audit lands in the transcript `diagnose-and-resend` reads. That is the trick: the transcript inspects itself before M5 reads it, **with the original context still live**.\" The Connections-blocker push (L189) and §47 table row 1 (L238) both prescribe \"Open a fresh Claude Code session in the repo and ask it to read what the M4 run touched\" — that recovers the ARTEFACT for the diagnosis phase; it does not restore \"the original context still live\" that L130 says the done-done beat is built on. So the drafted note's core sentence, \"The done-done fence assumes nothing about a live M4 terminal ... (L31) is the normal path, not a precondition,\" directly contradicts an existing Antti-directed note in the same maintainer block, and would leave the file asserting both. check_writing §3: \"a mis-describing note shields a real defect from every judge that trusts it.\" The triage is right on two side points — §1's floor is literally \"(8+/10 per beat, <7 = REVISE)\" so 7 does not authorise REVISE, and grep confirms 0 hits for \"facilitator-premium\" and \"7 to 8\" in check_strategy_tie_in.md, so the judge invented that phrasing — but both cut against the VERDICT LABEL, not the gap, and the invented clause downgraded the judge's own severity. The note additionally forbids scoring \"the beat below the §1 floor on it\" forever, which silences legitimate future mood scoring."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/learn-from-the-test.md",
  "instance": "ae101--module--learn-from-the-test.technical.json",
  "rule": "platform_and_boundaries 4",
  "claim": "The agent-team optional challenge (L101) names neither CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 nor that agent teams are off by default, so a student who tries it gets a silent no-op.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The adjudication landed after the judge's pin. check_student_facing.md gained §32a on 2026-09-04 — 'No settings, flags or env vars in body ... Body never prints the switch a feature needs — env var (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`) ... Name the feature, link the docs page' — and it names this exact finding shape as a non-finding: 'A judge that finds a required setting missing from body files nothing: the absence is the rule, not a platform gap (`check_platform_and_boundaries.md` §4 verifies claims, it does not add switches).' The matching in-file accept-note landed in the same pass at L171. The judge's positive work still stands and should be kept: it WebFetched both capability URLs live and confirmed the ultracode and agent-teams claims are accurate and appropriately hedged, which is what §4 actually asks. No edit is owed; the row clears on re-judge.",
  "evidence": "`git show 4589f1d5:...learn-from-the-test.md | grep -c` for the two accept-notes returned 1 against 2 at HEAD, and the surviving pin-era note ends 'Judges should not re-file either' (two items) versus HEAD's 'any of these' (three) — so the 2026-09-04 notes post-date the pin. git log on the file: cbf3a18c 'Student bodies name the feature and link the docs; the switch is the reader's to find' is the commit that added L171. Read `rule.js student_facing 32` in full for §32a, including its explicit files-nothing clause naming check_platform_and_boundaries §4 and the env var verbatim. HEAD L171 reads: '**No settings in body (Antti 2026-09-04, `check_student_facing.md` §32a):** the agent-team optional challenge names the feature and links the docs; the experimental flag is not printed. Judges should not re-file the missing switch.'"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/learn-from-the-test.md",
  "instance": "ae101--module--learn-from-the-test.writing.json",
  "rule": "student_facing 17",
  "claim": "L39 '## Your mid-run worries hint at missing checks' is a declarative header on a section whose body tells the student to act, so §17 wants a command verb.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "Antti answered this on 2026-09-04 and the answer is now in the file. The 'Declined at the 2026-09-03 polish pass' note at L169 gained a third clause after the judge's pin: 'the same header stays declarative, since it names the worry that arms the beat and the section's close is suggestive, not a task (Antti 2026-09-04, `check_student_facing.md` §17). Judges should not re-file any of these.' That is the maintainer call the judge explicitly asked for — its own evidence framed the row as 'a rule question for the maintainer rather than a clear defect' and guessed correctly that the insight-first framing was a deliberate arming-beat choice. Nothing to edit: the accept-note is already worded, dated, and rule-cited to §17, so the row clears on re-judge. Note the body of that section also moved in the same pass (aacd54d4, 'The worries section points at the check worth building instead of ordering one'), which is what makes the 'section's close is suggestive, not a task' half true at HEAD.",
  "evidence": "`git show 4589f1d5:curriculum/trainings/agentic-engineering-101/learn-from-the-test.md | grep -n 'Declined at the 2026-09-03'` returns the pin-era note ending '...approved 2026-08-23 (`check_prompts.md` §36). Judges should not re-file either.' — two items, no §17 clause. HEAD L169 carries three items including the §17 header clause dated Antti 2026-09-04. git log -3 on the file lists aacd54d4 and cbf3a18c above the pin. Header at HEAD L39 is unchanged from the pin, so the finding's target prose still exists; what changed is that the adjudication landed."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/learn-from-the-test.md",
  "instance": "ae101--module--learn-from-the-test.strategy.json",
  "rule": "strategy_tie_in 4/7",
  "claim": "WYL bullet 2 (L16) states the drift/rot/plausible-but-wrong pairing outright, ahead of the exercise's own diagnostic work, and may prime the compliance-feeling drift the trainer meta already watches for.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "This is the only todo in my set that arrived in a `todos` array rather than as a REVISE row, and the judge scored the rule PASS — it is not asking for a body change and explicitly says so ('Scored PASS (WYL's anticipatory register plus the concrete-artefact-list carve-out cover it)'). What it asks for is a second entry under '**First-cohort observation questions:**' at L266-267, which is trainer-facing, below the maintainer cut, and unpublished — nobody but the trainer and the judges reads it. The question it names is already half-written in the file: the Mood target at L183 lists 'compliance-feeling: the three-pattern is the answer; I should adopt it' as a watched drift with a Phase-3 diagnostic, but nothing asks the first cohort whether the WYL bullet is what primes it. Adding it costs one line and closes the row for good; the wording below reuses the mood note's own vocabulary so the two read as one instrument.",
  "evidence": "jq on the strategy instance: rules_evaluated holds zero REVISE rows; the single todo sits in `.todos[0]` with rule 'check_strategy_tie_in.md §4/§7', line 16, and note text scoring it PASS. L16 verbatim: '- **Pair** each failure mode with the packaging that catches it: drift ↔ reference artefact, rot ↔ plan.md, plausible-but-wrong ↔ external verifier'. L183 Mood target names the compliance-feeling drift verbatim. grep -c 'Verifier-shape calibration' = 1 (unique anchor). Read `rule.js strategy_tie_in 4` to confirm §4 is the sequencing-fidelity check, which the judge did run and pass.",
  "old_string": "- Verifier-shape calibration: does the three-shape menu cover the failure modes students surface, or does a fourth/fifth shape want naming?",
  "new_string": "- Verifier-shape calibration: does the three-shape menu cover the failure modes students surface, or does a fourth/fifth shape want naming?\n- Compliance drift from the WYL pairing: `## What You'll Learn` bullet 2 hands out drift ↔ reference, rot ↔ plan.md, plausible-but-wrong ↔ verifier before the room has diagnosed anything. Does the room reach Phase 1 treating the three-pattern as the answer to adopt rather than as a lens to test their own artefact against? Pairs with the compliance-feeling drift in the Mood target above.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/reference/prompt-anatomy.md",
  "instance": "ae101--reference--prompt-anatomy.slides.json",
  "rule": "slides 6",
  "claim": "Chunk 14's header 'Shape the interaction: pacing the questions' advertises only 'Batched, not dumped'; the slide's second entry, 'Push-back invitation', is about permission to disagree, not pacing.",
  "disposition": "EXEMPT_BODY",
  "confidence": "high",
  "why": "Real under §6's positive requirement — 'Header + bolded bullet leads read as a true summary of the chunk' — and cheap to fix without inventing anything, because the page's own house pattern already solves it. Fourteen of the twenty-four `##` headers name two things joined by 'and' or a comma ('reporting and disagreement', 'dispatch and narration', 'escalation and refusal', 'threshold and count'), which is exactly the shape a two-entry section wants. 'Pressure' is the push-back entry's own closing vocabulary ('With it, you get pressure. Pressure is what produces the codebase-specific answer'), so the widened header is drawn from the chunk rather than layered on top of it — which is what §6 is asking for — and it stays short enough to hold at projection distance, unlike the judge's own suggestion ('pacing the questions, inviting the pushback'). The page lives under `reference/`, standing-exempted from the approval gate (Antti 2026-08-29), so it applies directly.",
  "evidence": "Read L210-240 of the file. Chunk 14 spans L217-232 and holds exactly two `###` entries, 'Batched, not dumped' (L219) and 'Push-back invitation' (L225). grep -n '^## ' returns all 24 section headers; the two-thing pattern is the majority shape. grep -c '^## Shape the interaction: pacing the questions' = 1 (unique anchor). Repo-wide grep for 'pacing-the-questions' and 'pacing the questions' outside curriculum/evals/instances returns only the header line itself, so no inbound anchor link or TOC breaks. The maintainer block's Count audit ('interaction 4') is unaffected — entry counts do not change.",
  "old_string": "## Shape the interaction: pacing the questions",
  "new_string": "## Shape the interaction: pacing and pressure",
  "surface": "exempt-page"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/reference/prompt-anatomy.md",
  "instance": "ae101--reference--prompt-anatomy.story.json",
  "rule": "prompts 20",
  "claim": "The grill-before-save entry still frames the pattern as firing generically 'when an artifact needs cross-examination before it persists', after §20 was narrowed on 2026-09-03 to fire only on evidenced re-run cost.",
  "disposition": "EXEMPT_BODY",
  "confidence": "high",
  "why": "The page's own maintainer contract makes this obligatory, not optional: '**Source-of-truth:** the canonical rule-set behind these moves lives in `memory/check_prompts.md` (private). This page is the student-facing surface ... If they drift, the rules file wins and this page updates to match.' §20 was narrowed by Antti on 2026-09-03 — 'the benefit is unevidenced, so this never gates and rarely fires ... Raise §20 only where a run actually showed a downstream re-run caused by a gap' — and this page's last audit is stamped 2026-08-29, before the narrowing. So the drift is exactly the case the contract names. The edit appends one sentence rather than rewriting the 'Fires when' opener, because every sibling three-move entry on the page uses that opener ('Fires at long-running send-offs', 'Fires when authoring a Claude Code skill', 'Fires when strategic questions need triangulation') and breaking the parallel in one entry would cost more than it buys. Wording mirrors the narrowing's own reasoning — a certain round-trip against a speculative saving — and carries no em-dash, per the page's §14 discipline note. reference/ page, so it applies directly.",
  "evidence": "Read L335-395 and the full maintainer block. The grill-before-save entry sits at L342-347; its Example line names `push-back-on-the-plan-2`, `diagnose-and-resend-6`. grep -c 'Fires when an artifact needs cross-examination before it persists' = 1 (unique anchor). Read `rule.js prompts 20` in full: the 2026-09-03 narrowing text and the 'Declined under this narrowing, do not re-raise: `author-test-strategy-skill-1`' line. Page Quality stamp reads 'compendium-audited 2026-08-29', five days before the narrowing. Maintainer 'Source-of-truth' and 'Em-dash discipline' notes read verbatim from the block.",
  "old_string": "Fires when an artifact needs cross-examination before it persists. *Don't touch the file until I say 'lock it in.' Ask three questions at a time. Push back when my answer is generic.* The lock-in protects the file. The batching keeps the dialog efficient. The push-back instruction tells the agent to drop the niceness tax.",
  "new_string": "Fires when an artifact needs cross-examination before it persists. *Don't touch the file until I say 'lock it in.' Ask three questions at a time. Push back when my answer is generic.* The lock-in protects the file. The batching keeps the dialog efficient. The push-back instruction tells the agent to drop the niceness tax. Reach for it where a gap has already cost you a re-run, not by default: the extra round of questions is certain, the saving is not.",
  "surface": "exempt-page"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/the-context-ceiling.md",
  "instance": "ae101--supplementary--the-context-ceiling.strategy.json",
  "rule": "strategy_tie_in (no rule_index; hint: 'strategy-doc inventory currency, adjacent to rule 4')",
  "claim": "The Shipped-now entry for this page in content-strategy-agentic-engineering-101.md names only M4 prework, while the file's own Placement block documents three entry points (M2 close, M3 close, M4 prework).",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "The judge's own conclusion is that no change is owed to the target file — 'The target file's own text is not at fault' — and the file it does name lives in GDrive `bosser/strategy/`, outside this repo entirely. So this row cannot be cleared by any edit to the curriculum file it is filed against, and leaving it open guarantees it is re-filed at every re-judge of a page that is not wrong. §4, the rule the row is 'adjacent to', binds the other direction: it asks the module to be checked AGAINST the strategy doc's sequencing claims so the draft does not front-run a downstream teaching moment. It does not make a curriculum file responsible for the currency of the private doc's prose inventory. The row is also stale in substance: the judge notes it already resolves a prior instance's finding that the entry was missing entirely, so what remains is a description lagging by two entry points in a doc no student reads. Refute here, and track the GDrive edit wherever strategy-doc hygiene is tracked — not in this file's eval ledger.",
  "anchor": "**Placement:** block, L133-136 — the note sits directly after the three-entry-point bullet at L135",
  "accept_note": "**Strategy-doc inventory currency is not this file's defect (2026-09-05).** The `## Supplementary material` § Shipped-now entry in `bosser-strategy:content-strategy-agentic-engineering-101.md` still describes this page as M4-prework-only; the Placement bullet above is authoritative and names all three entry points (M2 close, M3 close, M4 prework). The private doc lives in GDrive and is corrected there. `check_strategy_tie_in.md` §4 checks this module against the strategy doc's sequencing claims, not the doc's prose against this module — judges should not re-file a strategy-doc lag as a finding on this page.",
  "evidence": "Read the file in full (196 lines). L133-136 Placement block, L135 verbatim: 'Entry points: M2 close (final slide of `how-instructions-grow.md`), M3 module close (`earn-the-trust.md`), M4 prework (`run-the-first-experiment.md`).' The instance's rule_index is null and its rule_lead self-describes as 'adjacent to rule 4, not a body defect — a private-doc hygiene gap'; its fix_hint says 'GDrive bosser/strategy/, not this repo' and 'No change owed to the target file's body'. Read `rule.js strategy_tie_in 4` in full — it is a pre-design check pulling sequencing claims FROM the strategy doc, with no inventory-currency obligation running the other way. The strategy doc is not present in the repo, so no old_string can be produced for it.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The triage's load-bearing factual claim is false. Its note asserts: \"The `## Supplementary material` § Shipped-now entry in `bosser-strategy:content-strategy-agentic-engineering-101.md` **still** describes this page as M4-prework-only.\" I read the doc (it is not in the repo but it is on disk at the GDrive strategy path, mtime today 08:22). L769 reads verbatim: \"Three entry points: M2 close (final slide of `how-instructions-grow.md`), M3 module close (`earn-the-trust.md`), and M4 `## Prework` for when sessions start running heavy.\" The judge quoted the old text (\"Linked from M4's `## Prework`...\", at L789 when it judged); the doc has since been corrected and the entry now matches the file's Placement block exactly. So the finding was real, it was actionable, and it has been ACTED ON — the correct resolution is `settled: fixed`, not an accept-note. Second, independent defect: the note declares \"the Placement bullet above is authoritative,\" which inverts the direction §4 actually runs — \"Before designing or reviewing module → pull per-module sequencing claims from relevant content-strategy doc ... Verify draft doesn't front-run downstream teaching moment.\" §4 makes the strategy doc the source of sequencing truth, not the module file. A durable note telling judges the curriculum page wins over the strategy doc, plus a blanket \"judges should not re-file a strategy-doc lag as a finding on this page,\" would switch off the only check that just caught a real three-entry-point drift and got it fixed. Do not write it."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/the-context-ceiling.md",
  "instance": "ae101--supplementary--the-context-ceiling.technical.json",
  "rule": "research_claims 2",
  "claim": "Several stamped `due` dates have elapsed as of 2026-09-05 (chroma 2026-01-14, swe-pruner 2026-07-23, orosz-cherny 2026-09-04, klaassen 2026-09-03, wiegold 2026-09-03), so a pre-cohort source-freshness re-run is owed.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The judge passed §2 on its own terms and then filed REVISE on a different ground that §11a explicitly took away from judges. Its evidence concedes the rule is satisfied — the out-of-window sources are 'cited as explicitly dated historical results ... matching the rule's own fallback' — which is the whole of §2's test. What it filed instead is elapsed DUE dates, and §11a says that punch list is generated, not hand-written: 'Audit = `curriculum/evals/scripts/source-freshness.sh --target <cohort-date>` — greps every stamp, flags BLOCK ... and WARN (due<target) ... This is the *generated* punch list; stop hand-narrating source debt in `pre-cohort-todos.md`.' A stamp past due is a WARN the script raises at cohort-planning time against a target date the judge does not have. The row also has no fix_hint, because there is no text to change. Left open it is a perpetual-motion todo: every stamp on this page crosses its due eventually, so every re-judge re-files it, which is precisely how this ledger refills. The accept-note is the only thing that stops it.",
  "anchor": "maintainer block, immediately after the **Folklore handles are quotes...** note at L123 — same 'future judges should' convention",
  "accept_note": "**Elapsed `due` dates are the audit's output, not a judge's finding (2026-09-05).** Every stamp in the backing block below is walked by `curriculum/evals/scripts/source-freshness.sh --target <cohort-date>`, which is the generated punch list `check_research_claims.md` §11a installed when it retired hand-narrated source debt. A stamp past its due raises a WARN there, against a cohort date a class judge does not hold. Judges should score §2 on the question §2 asks — are out-of-window sources cited as explicitly dated history? they are, chroma and swe-pruner both — and should not re-file a due-date roll as a body finding on this page.",
  "evidence": "Read the file in full (196 lines). Stamps at L165-177; dues as the judge lists them, plus two the judge missed (lulla-agents-md 2026-07-28, anthropic-multi-agent 2025-12-13), which is itself evidence that eyeballing the ledger is the wrong instrument. L9 body verbatim: 'A 2026 study trimmed an agent's context by 23 to 54 percent on a coding benchmark' — explicitly dated, matching §2's historical-context fallback. The instance row's fix_hint field is empty. Read `rule.js research_claims 2` and `11` in full; §11a carries the generated-punch-list clause and the 'stop hand-narrating source debt' instruction verbatim. grep -c 'Folklore handles are quotes' = 1 (unique anchor).",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The refutation checks out: §11a says verbatim \"Audit = `curriculum/evals/scripts/source-freshness.sh --target <cohort-date>` — greps every stamp, flags BLOCK ... and WARN (due<target) ... This is the *generated* punch list; stop hand-narrating source debt\", and the script header confirms \"DUE is compared against --target (the COHORT date), not today\". The judge's own evidence concedes §2 is satisfied and its fix_hint is empty. But the drafted note overreaches twice. (a) It instructs judges to \"score §2 on the question §2 asks — are out-of-window sources cited as explicitly dated history?\", collapsing §2 to one sub-question and reading past §2a (three required durable-account conditions) and §2b (\"`due:none` scopes the CITATION, not the SOURCE\") — both live on this page's three `due:none` stamps (anthropic-context-engineering, amp-handoff-neo, yegge-beads). (b) \"Elapsed `due` dates are the audit's output, not a judge's finding\" is stated flat, but the script's own WARN text is \"re-verify + re-stamp before delivery\" — real work exists, it just belongs to the pre-cohort audit rather than to a class judge. Narrowed below.",
   "note_fix": "**Due-date rolls on this page go to the generated audit, not to a class judge (2026-09-05).** `check_research_claims.md` §11a makes the punch list generated: `curriculum/evals/scripts/source-freshness.sh --target <cohort-date>` walks every stamp in the backing block below and raises WARN on `due < target` (\"re-verify + re-stamp before delivery\") — pre-cohort work, against a cohort date a class judge does not hold. Most dues here are publication+6mo and can never roll forward (§11a bans `checked`+6mo), so a judge who files the roll files it forever. Do not file a bare elapsed-`due` list as a §2 body finding on this page. Nothing else is settled: §2a's durable-account conditions, §2b's `due:none`-scopes-the-citation test on the three `due:none` stamps, and any body sentence that has drifted from what its source actually supports all remain open."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/the-context-ceiling.md",
  "instance": "ae101--supplementary--the-context-ceiling.technical.json",
  "rule": "pedagogy 39",
  "claim": "The sentence 'You'll meet it properly, mechanism and trade-off both, in Module 3.' was cut from the Subagent-as-a-firewall paragraph since the last pin, with no maintainer note saying whether the removal was asked for.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The cut was strategic, deliberate, and part of a directed sweep — which is what §39 asks for. Commit 5711a601 (2026-08-29) is titled 'The direct-apply pile lands: forward promises cut where their beats live, one loop re-subjected, one header widened' and makes the identical one-line cut across seven files in a single change. A systematic seven-file pass with the reasoning in its subject line is the opposite of an unreviewed trim. §39 also does not say what the finding says it says: it fires on the GENERATION step ('Generation step removing non-trivial entity ... pause and ask') and enumerates section, exercise, lecture, prompt block, LO, bolded callout, named pattern — a single forward-pointer sentence is none of those, and nowhere does the rule require a maintainer-block note as the record of the asking. Requiring one inverts check_writing §3, which puts the diff in git and keeps the file stating what is true. The cut was also substantively right: this page's own Placement block lists three entry points, so a student arriving from M3's close or M4's prework was being promised a Module 3 that had already happened. Note-worthy because the pin-diff shape guarantees a re-file: any judge diffing pin..HEAD sees the deletion and finds no in-file note, forever.",
  "anchor": "maintainer block, alongside the **Placement:** block at L133-136 whose entry-point list is the reason the pointer was wrong",
  "accept_note": "**The Module 3 forward-pointer was cut on purpose (2026-08-29, commit 5711a601 — 'forward promises cut where their beats live').** *\"You'll meet it properly, mechanism and trade-off both, in Module 3\"* left the Subagent-as-a-firewall paragraph in a directed seven-file sweep across the AE101 supplementaries and reference pages. It was also wrong for this page specifically: the Placement block lists three entry points, so a reader arriving from M3's close or M4's prework was being pointed forward at a module already behind them. `check_pedagogy.md` §39 fires at the generation step and enumerates sections, exercises, lectures, prompt blocks, LOs, callouts and named patterns; it does not make a one-sentence pointer a non-trivial entity and does not require an in-file note as the record of the asking. Judges should not re-file this deletion off the pin-diff.",
  "evidence": "`git log -S \"You'll meet it properly, mechanism and trade-off both\"` on the file returns exactly two commits: 042f434d (added) and 5711a601 (removed). `git show --stat 5711a601` shows 7 files, 7 insertions, 7 deletions, dated Sat Aug 29 2026, subject as quoted. HEAD L49 confirms the sentence is gone and the paragraph now closes 'at a price worth knowing before you reach for it.' Read `rule.js pedagogy 39` in full for the enumerated entity list and the generation-step framing. Placement block L133-136 lists the three entry points.",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The refutation is right, and on a stronger ground than the note gives: `git show 5711a601` reads \"Author: Antti Tevanlinna <tevanlin@gmail.com>\", so the cut is the maintainer's own — §39 (\"Generation step removing non-trivial entity ... pause and ask\") cannot bite on a commit the maintainer authored, and the judge's fix_hint (\"Confirm with the maintainer whether cutting the Module-3 forward-pointer was a deliberate, asked-about call\") is answered by git blame. But the drafted note rests on a premise that does not check out: it says the cut landed \"across the AE101 supplementaries and reference pages\", while `git show --stat 5711a601` lists seven files of which two are modules (`getting-going.md`, `plan-mode-done-right.md`) — a reason invented to defend a correct call, shipping as fact into a durable block. It also generalises from one maintainer-made cut to a class (\"does not make a one-sentence pointer a non-trivial entity\"), which would shield a future unreviewed one-sentence trim by a generation agent, the exact case §39 exists for. Placement block and entry-point list verified present at 5711a601.",
   "note_fix": "**The Module 3 forward-pointer is cut, and the cut is the maintainer's own (commit `5711a601`, 2026-08-29, authored by Antti).** *\"You'll meet it properly, mechanism and trade-off both, in Module 3\"* is gone from the Subagent-as-a-firewall paragraph — one of seven identical one-line cuts in that commit, spanning two modules, one reference page and four supplementaries. It was wrong for this page in particular: the Placement block lists three entry points, so a reader arriving from M3's close or M4's prework was being pointed forward at a module already behind them. `check_pedagogy.md` §39 asks the generation step to pause and ask the maintainer before cutting; here the maintainer is the one who cut. Judges should not re-file §39 on this sentence off the pin-diff. §39 still binds on any cut this note does not name."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/the-context-ceiling.md",
  "instance": "ae101--supplementary--the-context-ceiling.technical.json",
  "rule": "prompts 2",
  "claim": "The L21 lead-in runs ~22 words and enumerates all three of the fence's clauses (Opus pricing / 100K-over-20-turns / caching), matching §2a's short-case tell.",
  "disposition": "EXEMPT_BODY",
  "confidence": "medium",
  "why": "Half right, and the half that is right is worth one trim. The judge over-reaches on the short case: §2a's short case is 'a two or three line prompt IS its own skim-cue, taken in the same glance', and this fence is three paragraphs of six lines running four steps (price 100K over 20 turns, then 20K, then show per-turn/total/difference, then factor caching) — squarely the 'long or multi-step prompt cannot be held at once' side, where §2a says a fuller lead-in 'genuinely aids comprehension and is worth the words'. So stripping to the opening clause alone would be wrong. But the enumeration tell does fire on the middle clause, and 22 words breaches a ceiling §2a describes as reserved 'for the longest fences'. The fix is to drop the one clause the fence states more precisely than the lead-in can — '100K extra tokens over 20 turns' is verbatim in the fence's second paragraph — while keeping the two-step cue (price it, then caching) that the fence's length earns. 16 words, both moves intact, no clause enumerated. §21 is satisfied unchanged: 'Ask Claude to price' is plain service-ask grammar, no active stance. supplementary/ page, standing-exempt from the approval gate.",
  "evidence": "Read the file in full. L21 lead-in and the L25-31 fence verbatim; the fence is three paragraphs, and its second paragraph carries '100,000 tokens ... across 20 turns' and '20,000 tokens' more precisely than the lead-in's shorthand. Word count of the current lead-in is 22 by hand. grep -c 'Ask Claude to price the bloat from current Opus pricing' = 1 (unique anchor). Read `rule.js prompts 2` in full: the short-case tell, the length carve-out for multi-step fences, the §38 opening-clause mirror, and the dangling-colon sub-case (inapplicable — the fence ends 'how much the difference shrinks.'). Maintainer Placement note at L136 confirms this is a draft inline `**Prompt**` block pending registry migration, so no curriculum/prompts/ file needs a matching edit.",
  "old_string": "Ask Claude to price the bloat from current Opus pricing: 100K extra tokens over 20 turns, then what caching does to it.",
  "new_string": "Ask Claude to price the bloat at current Opus pricing, then what caching does to it.",
  "surface": "exempt-page"
 },
 {
  "target_file": "curriculum/exercises/extract-the-task-shaping-rule.md",
  "instance": "ae101--exercise--extract-the-task-shaping-rule.pedagogy.json",
  "rule": "pedagogy 16",
  "claim": "L63's body bullet ('ask for the @import line before you move on. Without it the file sits unread') is a body-only gate wiring load-bearing machinery, so it belongs in the fence, not body prose.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "§16's Boundary 2 (2026-08-02, Antti) is written for exactly this situation: it says the in-fence requirement binds load-bearing machinery, then names the escape — 'File carries a maintainer accept-note; do not escalate body-ask -> new registry prompt without maintainer sign-off.' L116 IS that accept-note, it is dated and reaffirmed, and it fixes the re-open trigger as cohort field evidence, not a re-judge. The judge concedes all of this in its own evidence and files REVISE anyway. The defect is not in the body, it is in L116's closing clause, which literally instructs the next judge to file: 'so a pedagogy REVISE here is the expected steady state, not a new finding.' That sentence is the ledger-refill machine, and the accept-note below replaces it.",
  "evidence": "Read `node curriculum/evals/scripts/rule.js pedagogy 16` in full — Boundary 2 names the maintainer-accept-note mechanism verbatim. Read L112-118 of the file: the [watch] is dated 2026-08-02, names the trigger, and pre-empts the wrong carve-out (optional-refinements) by name. `grep -c 'so a pedagogy REVISE here is the expected steady state, not a new finding'` = 1 (unique anchor). Note for the applier: the writing §3 row on this same file also edits L116, but a different, non-overlapping span (the parenthetical); both apply cleanly in either order.",
  "anchor": "so a pedagogy REVISE here is the expected steady state, not a new finding.",
  "accept_note": "**§16 and §22 score N/A here, not REVISE (2026-09-05).** The `[watch]` above is the adjudication, and re-filing it as a non-blocking todo on each re-judge reopens a closed call and refills the ledger. Judges: record N/A citing this note. File only on the named trigger — a cohort run where a student picks a non-auto-load path and misses the wire.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "§16 Boundary 2 reads: \"'Location: in fence' binds the load-bearing machinery of a session exercise (artifact a later step consumes, mood-carrying move). Light homework / conversational-fold register: the body ask IS the design ... File carries a maintainer accept-note; do not escalate body-ask → new registry prompt without maintainer sign-off.\" The escape is register-scoped to light homework, and this ask is the opposite: the file's own L118 records the downstream stake (\"M4's walk-and-fill audit subagent cannot see `~/.claude/memory/` unless it is `@import`-wired\"), and L116 calls it \"a KNOWN §16 gap held open on purpose.\" So the rule applies and is deliberately unmet — that is accepted-REVISE, not N/A. The triage's load-bearing claim, that L116's closing clause is a defect and \"the ledger-refill machine,\" fails: it is a maintainer instruction corroborated verbatim at curriculum/trainings/agentic-engineering-101/pre-cohort-todos.md:38 — \"The trigger is field evidence, not a verdict — the pedagogy instance scores §16 REVISE non-blocking by design (class verdict PASS_WITH_TODOS; the maintainer block names that REVISE the expected steady state).\" Reversing it to \"record N/A\" overwrites a maintainer call without sign-off and leaves the tracking doc contradicting the file. The note also declares §22 N/A, which this finding never raised (the instance already scores §22 N/A as \"meta rule about compounded-memory practice, not this file\") — shield extended past anything adjudicated. The judge's own row is already non-blocking with fix_hint \"No action owed now\": nothing here costs curriculum work, and the only thing the note buys is switching a live rule off."
  }
 },
 {
  "target_file": "curriculum/exercises/extract-the-task-shaping-rule.md",
  "instance": "ae101--exercise--extract-the-task-shaping-rule.technical.json",
  "rule": "pedagogy 22",
  "claim": "The @import wiring ask at L63 has no in-fence forcing function, so §22 ('rule that does not force is worse than no rule') fires on the technical ledger too.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "§22's subject is memory entries, not curriculum body prose: 'Memory entries don't fire at ship time unless also at: (a) a PDCA step in this file naming the rule's invocation; (b) reference-artefact Done-means criterion; (c) stop-hook; (d) git pre-commit grep-enforcement.' An exercise's body ask to a student is none of those and is not a memory entry. The row is a mirror-filing of the §16 [watch] onto a second ledger — the judge says so in its own evidence ('not a fresh defect this run surfaced') and its own fix_hint says 'No action owed'. The same accept-note as the §16 row discharges both; apply it once.",
  "evidence": "`node curriculum/evals/scripts/rule.js pedagogy 22` returns four clauses, all scoped to memory entries and their ship-time forcing functions; nothing in the body extends it to student-facing prose. Cross-checked L116's maintainer block, which already holds the standing [watch] this row duplicates. This row and the pedagogy-instance §16 row share one accept-note — do NOT apply the note twice.",
  "anchor": "so a pedagogy REVISE here is the expected steady state, not a new finding.",
  "accept_note": "**§16 and §22 score N/A here, not REVISE (2026-09-05).** The `[watch]` above is the adjudication, and re-filing it as a non-blocking todo on each re-judge reopens a closed call and refills the ledger. Judges: record N/A citing this note. File only on the named trigger — a cohort run where a student picks a non-auto-load path and misses the wire.",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The §22 refutation checks out — §22 reads \"Memory entries don't fire at ship time unless also at: (a) a PDCA step... (d) git pre-commit\", all scoped to memory entries and compounding, and the same technical instance scores pedagogy §37 PASS on \"Fence 5 'are these rules auto-loaded to each session context?'\", so the invocation step is fenced. But the drafted note reads \"**§16 and §22 score N/A here, not REVISE**\", adjudicating §16 — which this entry never touched (index 51 does) — and it converts an explicitly-accepted violation into an inapplicable rule: L116 says in the file's own words \"This is a KNOWN §16 gap held open on purpose\". Calling that N/A erases the record that a gap exists. Unqualified \"§22 scores N/A here\" would also silence a future §22 filing if the Phase 4 auto-load fence were ever cut.",
   "note_fix": "**§22 scores N/A here (2026-09-05).** §22 governs memory entries and their ship-time forcing functions, not an exercise's body ask to a student — and Phase 4's fenced auto-load question is the invocation step (pedagogy §37 PASS on the same run). The `@import`-in-body placement question belongs to §16 and to the `[watch]` above; do not mirror it onto the technical ledger as a §22 finding. A genuinely new §22 issue on this file — e.g. if the Phase 4 auto-load fence were cut — remains filable."
  }
 },
 {
  "target_file": "curriculum/exercises/extract-the-task-shaping-rule.md",
  "instance": "ae101--exercise--extract-the-task-shaping-rule.writing.json",
  "rule": "writing 3",
  "claim": "L116's clause 'the `**Note**` widget it used to occupy was the slide's only content, which is not what a Note is for' is undated journey narration using the explicitly-banned 'used to'.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Real hit, and the rule's own Cut list carries this shape near-verbatim ('used to sit in X'). The clause sits in elaboration, not in a dated provenance header, so the 84-of-101 dated-header carve-out does not shield it. The fix is a cut rather than a rewrite: the positive fact (it is a plain bullet, under the load-map table, on the *Where rules can live* slide) survives, and the rationale that would remain is a guard against a Note-widget promotion nobody is proposing — the phantom shape §3's 2026-09-05 clause bans. Maintainer block, so it lands directly.",
  "evidence": "`node curriculum/evals/scripts/rule.js writing 3` — 'Banned in a block body: settled <date>, retired, no longer, historically, **used to**, the old X…'; the maintainer-block extension is explicit ('the later clauses extend it to <!-- maintainer --> blocks'). Read L112-118 in the file. `grep -c` on the parenthetical anchor = 1. Does not collide with the §16 accept-note anchor later on the same line.",
  "old_string": "(The ask sits in a plain bullet under the load-map table on the *Where rules can live* slide; the `**Note**` widget it used to occupy was the slide's only content, which is not what a Note is for — a Note is louder than surrounding prose, and there was none.)",
  "new_string": "(The ask sits in a plain bullet under the load-map table on the *Where rules can live* slide.)",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/exercises/extract-the-task-shaping-rule.md",
  "instance": "ae101--exercise--extract-the-task-shaping-rule.slides.json",
  "rule": "slides 9",
  "claim": "Chunk 9 (Phase 5) carries three bolded handles — '**Planning gaps.**', '**Conflicting information.**', '**Other context.**' — against the default <=1-per-slide budget, while the file's own accept-note at L130 declares 'no bolded handles in the body; all bullet leads plain'.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "The half of this finding that is unambiguously true is the maintainer note, not the body. §9 explicitly licenses 'checklist item names' as handles and all three are 2-word sub-spans inside the <=5-word cap, so only the per-slide count trips — and §9's scanned-surface waiver ('a slide whose job is to be scanned rather than read… bold is navigational') fits a three-category checklist the student matches against their own session. The body is Antti's own and he did not de-bold it when he touched the file five days later; the note is what is factually false about the current page. Under §3's 'a keep-note whose positional claim fails is VOID', a note asserting a state the body does not have shields nothing and invites a re-file every pass. Maintainer block, applies directly. Deliberately NOT proposing to de-bold the body — that would reverse Antti's own emphasis on his own bullets on my initiative.",
  "evidence": "`git blame -L 104,106` → all three bolded bullets are commit 7fe271218, Antti, 2026-08-25. `git blame -L 130,130` → L130's note is commit 85515fa01, Antti, 2026-08-30, FIVE DAYS LATER; `git show 85515fa01` on this file shows that commit only trimmed two trailing journey clauses from maintainer notes and never touched the body. So the note is newer than the bullets and was written over a body that already carried them — a scope miss, not a superseding call. Rule body read in full via `rule.js slides 9`. `grep -c` on the old_string prefix = 1.",
  "old_string": "**Emphasis budget (`check_slides.md §9`):** no bolded handles in the body; all bullet leads plain. Widget chrome",
  "new_string": "**Emphasis budget (`check_slides.md §9`):** bullet leads plain, with one exception — Phase 5's three Boy Scout category names (**Planning gaps.** · **Conflicting information.** · **Other context.**) are bolded checklist-item handles inside the ≲5-word cap. That slide is scanned for a match rather than read through, so §9's count waiver for scanned surfaces carries the three-on-one-slide count; the bullets are Antti's, per the Phase 5 note below. Widget chrome",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/exercises/extract-the-task-shaping-rule.md",
  "instance": "ae101--exercise--extract-the-task-shaping-rule.technical.json",
  "rule": "research_claims 1",
  "claim": "The backing block's three Sources rows are labelled `[delegated stamp]`, which is not one of §1's enumerated evidence-type labels.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "`[delegated stamp]` is not competing with §1's taxonomy — it is the documented delegation marker from `curriculum/backing-format.md` § *Delegated variant*, a canon doc listed in `curriculum/CLAUDE.md` § Canon, and it is paired with the `due:none` that variant requires. §1's enumeration types the *evidence* behind a claim; here the evidence typing is owned by the delegate, `lectures/where-the-rule-could-live.md`, which stamps all three URLs `[capability]` with full CAVEAT text. The body also asserts nothing off these URLs — the backing block says so at L198 ('an ask, not an assertion: the body names three runtimes to raise with Claude and claims nothing about what any of them does'), so there is no evidence tier to inflate. The judge reached the same conclusion in its own fix_hint ('Not a per-file fix… really a §1 maintenance item') and filed against the file anyway.",
  "evidence": "`grep -rn 'delegated stamp' curriculum/*.md` → `curriculum/backing-format.md:64`, the § Delegated variant clause defining the marker and mandating `due:none`. `grep -n 'gh-actions-cron\\|cc-github-action\\|cc-routines' curriculum/lectures/where-the-rule-could-live.md` → lines 67/70/71, all three typed `[capability]` with dated `[checked:2026-07-31 …]` stamps. `[capability]` is itself a worked example at `curriculum/source-freshness-format.md:64`, confirming the corpus runs a documented pointer taxonomy alongside §1's evidence-type list. `grep -c` on the L202 anchor tail = 1.",
  "anchor": "`due:none` because a delegation does not expire, the delegate's stamp does.",
  "accept_note": "`[delegated stamp]` is the backing-format's delegation marker (`curriculum/backing-format.md` § *Delegated variant*), not an evidence-type label; `check_research_claims.md` §1's taxonomy is discharged by the delegate, which types all three rows `[capability]`. Judges: do not file §1 against these three rows for a missing type label.",
  "verification": {
   "ruling": "REFUTE_STANDS",
   "reason": "Every factual leg checks out under independent verification. `curriculum/backing-format.md` (canon per `curriculum/CLAUDE.md` § Canon L14: \"**Backing blocks**: `curriculum/backing-format.md`\") states: \"**Delegated variant — `[delegated stamp]` + `due:none`.** A file may point at another file that owns the dated claim, rather than restating it. The pointer is legitimate; a computed `due:` on the pointer is not... `source-freshness.sh` walks the delegate directly.\" The delegate is live and correct: `curriculum/lectures/where-the-rule-could-live.md` L67/70/71 type all three URLs `[capability]` with dated `[checked:2026-07-31 ... due:cohort]` stamps. §1's own enumeration is demonstrably not the corpus's closed label universe — `[capability]` is absent from it too yet is a worked example in canon at `curriculum/source-freshness-format.md` § Capability-claim variant. And backing-format's sibling guard (\"a claim owes its backing in the file that makes it. Delegation is for *'the detail lives over there'*, never for *'my number is stamped over there'*\") does not bite: the backed claim `ask-about-the-machinery` carries no rate, count or capability assertion — the block says so on the line. I tried the newer-clause angle that breaks the sibling ruling at index 118 and it does not reach here: §1 governs evidence typing, and this row's typing is discharged upstream. Accept-note scope is already narrow — \"against these three rows for a missing type label\" leaves delegate integrity, dead URLs and evidence-level questions open."
  }
 },
 {
  "target_file": "curriculum/exercises/extract-the-task-shaping-rule.md",
  "instance": "ae101--exercise--extract-the-task-shaping-rule.pedagogy.json",
  "rule": "pedagogy 52c",
  "claim": "L7 '**What you build:** one rules file from two sources: how you shaped this task, and how your team writes stories.' is a colon-list over the ~15-word budget that replays Phase 1's own two sub-headings instead of naming the artifact.",
  "disposition": "CARD",
  "confidence": "high",
  "why": "The finding lands on the rule's own diagnostic word-for-word. §52c: 'Length budget: one sentence per slot, ~15 words, no colon-list… Tell: it needs a colon and a list. The list is the exercise, and the exercise is already on the page.' The line runs 17 words after the colon and its two items are the two Phase 1 sub-headings ('Codify the rules from this session' / 'Read more rules off one story ticket'). Scope is AE101, so the A101 exemption is off. No maintainer note covers it — L132's lean-intro note governs a cut Phase 1 bullet, and L9's *The point* is a different slot. Student-facing exercise body and a projected slide, and the replacement wording is a taste call nobody has made, so it is card-shaped rather than a mechanical swap.",
  "evidence": "`node curriculum/evals/scripts/rule.js pedagogy 52c` read in full — the length budget, the no-colon-list clause and the 'the list is the exercise' tell are all in the rule body, not just the lead. Counted the post-colon span at 17 words against the ~15 budget and the 9/12/7 reference shape (`orient-and-introspect.md`). Compared the sibling slots: L5 'What you do' ~16 words no list, L9 'The point' 6 words. `grep -c` on the full L7 line = 1.",
  "before": "**What you build:** one rules file **from two sources: how you shaped this task, and how your team writes stories.**",
  "after": "**What you build:** one rules file**, seeded from this session and one story ticket.**",
  "risk": "Low-medium. The two sources are what separates this exercise from M1's `close-the-ticket`, and the current line previews that split explicitly. The replacement keeps both inputs but drops the 'how you shaped this task / how your team writes stories' framing — if you want that preview kept, the trio is the wrong place for it and the Phase 1 headers already carry it."
 },
 {
  "target_file": "curriculum/exercises/extract-the-task-shaping-rule.md",
  "instance": "ae101--exercise--extract-the-task-shaping-rule.writing.json",
  "rule": "student_facing 17",
  "claim": "L66 '## Phase 3: What this file could automate later' is a Wh-fragment header over a section where the student acts (L70 'Ask Claude what this file could drive later.').",
  "disposition": "CARD",
  "confidence": "high",
  "why": "§17's test fires cleanly: 'section body tells student to do something? Yes → lead header with verb.' It does, and the other four Phase headers all lead with a command verb (Codify / Save / Save / Leave), so Phase 3 is the sole outlier. Neither carve-out reaches it — Phase headers are module-specific, not one of the canonical architecture names in `curriculum/CLAUDE.md` § Module File Shape, and this is an exercise, not a lecture or reference page. The proposed verb keeps the student as the header's only actor, per the 2026-09-02 Antti veto clause. Card rather than mechanical: it rewords a projected slide title, and which verb goes there is a call not yet made.",
  "evidence": "`node curriculum/evals/scripts/rule.js student_facing 17` read in full, including the canonical-section carve-out list and the never-Claude-as-subject clause. Read L66-72 of the file. `grep -c '^## Phase 3: What this file could automate later$'` = 1. No maintainer note covers the header: L128 governs the Phase 3 *prompt* (kept, not cut), not its heading.",
  "before": "## Phase 3: **What** this file could automate later",
  "after": "## Phase 3: **Ask what** this file could automate later",
  "risk": "Low, with one cosmetic side effect: the header then near-echoes L70's lead-in ('Ask Claude what this file could drive later'), so the slide opens on 'Ask' twice within three lines. If that grates, the second fix is L70, not the header — but that is a separate call."
 },
 {
  "target_file": "curriculum/prompts/extract-the-task-shaping-rule-1.md",
  "instance": "ae101--exercise--extract-the-task-shaping-rule.writing.json",
  "rule": "prompts 36",
  "claim": "`extract-the-task-shaping-rule-1` is one unbroken paragraph carrying at least two logical moves, while its three siblings in the same exercise all paragraph-split.",
  "disposition": "CARD",
  "confidence": "high",
  "why": "Correct, and if anything under-stated: the body carries three moves — read the session · propose-and-anchor the rules · stop-and-wait for my answer. §36 names the third boundary by shape (the null-case / handoff move) and its read-aloud diagnostic puts a breath before 'After proposing'. The sequential-build hatch covers the propose/phrase/anchor/scope run, which is why only ONE break is proposed, not three. Card-shaped by machinery rather than by judgement: §22 gates every body-region change to a registry prompt regardless of size, and `.claude/hooks/prompt-edit-gate.sh` denies a body-touching edit without a `prompt-ok` token, so this cannot go in as a mechanical swap even though no word changes.",
  "evidence": "`node curriculum/evals/scripts/rule.js prompts 36` and `prompts 22` read in full. Read all four registry bodies used by this exercise: `-2` (3 paragraphs), `-4` (4 paragraphs), `ae101-m2-integrate-branch` (3 paragraphs), `-1` (1 paragraph) — the outlier claim holds. `grep -c 'Rules about task shape, not about plan-reading craft. After proposing, stop and ask'` on the registry file = 1.",
  "before": "…Anchor each rule in a specific moment from the session: the task I picked, what the second-pass read surfaced, what my push-backs caught, where the plan factored cleanly and where it didn't. Rules about task shape, not about plan-reading craft. **After proposing, stop and ask which one I want to rewrite and which one I want to reject. Wait for my answer before continuing.**",
  "after": "…Anchor each rule in a specific moment from the session: the task I picked, what the second-pass read surfaced, what my push-backs caught, where the plan factored cleanly and where it didn't. Rules about task shape, not about plan-reading craft.\n\n**After proposing, stop and ask which one I want to rewrite and which one I want to reject. Wait for my answer before continuing.**",
  "risk": "Low — not one word the student pastes changes, only a paragraph break before 'After proposing'. Procedural cost only: §22(e) requires writing `.claude/prompt-approvals/extract-the-task-shaping-rule-1.confirmed` immediately on approval, or the pre-commit hook dead-ends headless."
 },
 {
  "target_file": "curriculum/exercises/extract-the-task-shaping-rule.md",
  "instance": "ae101--exercise--extract-the-task-shaping-rule.writing.json",
  "rule": "prompts 38",
  "claim": "P1's lead-in 'Ask Claude for the rules.' shares no verb or noun-phrase with the prompt's opening clause 'Read this session end-to-end.', making it the file's one non-rhyming lead-in/fence pair.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "Two grounds. First, the mirror is present on the noun: §38 asks for 'same verb, same noun-phrase' and 'the rules' pairs with the fence's actual ask, 'Propose three to five rules about what makes a multi-file task plan-mode-able'. The fence's literal first sentence is context-setting, not the instruction. Second, the verb split is this file's convention rather than drafting drift: every lead-in names the STUDENT's act (Ask / Drop / Decide) and every fence opens on CLAUDE's (Read / Reverse-engineer / Suppose / Let's decide) — the same actor split `check_student_facing.md` §17 records for headers under Antti's 2026-09-02 veto. The judge's decisive premise, 'the other six pairs rhyme strongly, P1 is the one outlier', is false on inspection: L35 'Drop a story ticket link after the colon.' shares no verb with `-4`'s 'Reverse-engineer how this team writes stories…', the identical profile it charges P1 with. Supporting but not decisive: the proposed fix expands Phase 1's intro, which L132's dated Antti-approved lean-intro note pushes against ('Do not re-elaborate the lead-in') — I am not stretching that note to cover L20, only noting the direction.",
  "evidence": "`node curriculum/evals/scripts/rule.js prompts 38` read in full including carve-out 38a (surprise-pedagogy, N/A here). Walked all five lead-in/fence pairs against their registry bodies: L20↔`-1`, L35↔`-4`, L46↔`-2` (shares 'decide'), L70↔`-3` (shares 'drive'), L84↔`push-back-on-the-plan-4` (near-verbatim). Two of five mirror on verb, three on noun only — so the convention here is noun-mirroring and P1 is inside it. `grep -c` on the L132 anchor = 1.",
  "anchor": "**Lean-intro trim (2026-08-25, Antti-approved):** Phase 1's *\"Each decision in the plan read carries indirect information about the rules you already work by\"* cut — restated the bullet's first sentence at lower proof. Do not re-elaborate the lead-in.",
  "accept_note": "**P1's lead-in mirrors on the noun, not the verb — `check_prompts.md` §38 is satisfied (2026-09-05).** *\"Ask Claude for the rules.\"* pairs with the fence's ask (*\"Propose three to five rules…\"*); the verb split is the file's convention, since every lead-in here names the student's act (*ask*, *drop*, *decide*) and every fence opens on Claude's (*read*, *reverse-engineer*, *suppose*) — the actor split `check_student_facing.md` §17 records for headers. Two of the five pairs mirror on verb and three on noun alone, so P1 is inside convention, not an outlier. Do not expand the lead-in to carry the fence's first verb.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The triage's stated decisive claim fails on inspection. It says L35 has \"the identical profile it charges P1 with\" — but §38's diagnostic is \"place body lead-in's last sentence next to prompt's first sentence; share verb + noun-phrase?\", and L35 *\"Drop a story ticket link after the colon.\"* shares the noun-phrase \"story ticket\" with `-4`'s opening clause *\"Reverse-engineer how this team writes stories from one story ticket.\"* L20 shares neither verb nor noun with `-1`'s opening clause *\"Read this session end-to-end.\"* I walked all seven pairs against the registry bodies: -4 mirrors on noun, -2 on \"decide\", -3 on \"drive\", push-back-4 and integrate-branch near-verbatim, tidier via the enumerated bullets — P1 is the one pair with no share, exactly as the judge said. The triage's fallback ground (\"The fence's literal first sentence is context-setting, not the instruction\") rewrites the rule's diagnostic, which names the first sentence, not \"the actual ask.\" And check_student_facing §17's actor split — which I confirmed says \"The student is a header's only actor... (Antti veto 2026-09-02)\" — is about HEADERS, and cannot excuse a total non-mirror when §38 asks for verb AND noun-phrase and every sibling pair in this file supplies at least one. The judge's fix is one short sentence (\"Ask Claude to read this session and propose the rules.\"), which does not re-elaborate the lead-in L132 protects."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/skill-stacking.md",
  "instance": "ae101--supplementary--skill-stacking.slides.json",
  "rule": "slides 14",
  "claim": "Three slides breach the 210-word / 6-bullet cap with no per-slide accept-note: '## The three-layer model' (321w/13 bullets), '## Skill catalog by phase' (157w/19 bullets), '## Design principles' (193w/7 bullets).",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "The measurement is real and reproduces exactly, so this is not refutable on the numbers. But §14's own rationale is that 'the cap is a claim about the projected page', and `check_slides.md` §10 establishes that supplementaries never reach the composed deck — its 2026-07-09 finding names 'prework + every supplementary + reference dropped' from `buildDeckModel`. So the cap is measuring a page with no slide form. §14 also insists 'script and compendium must not be two rulebooks', which rules out a silent refute: the checker keeps flagging forever unless the sanctioned per-slide hatch is written. Hence three accept-notes in the maintainer block rather than a restructure of a page nobody projects. Not card-shaped twice over — maintainer block, and supplementaries are gate-exempt anyway (§26, Antti 2026-08-15).",
  "evidence": "`node scripts/check-slide-size.js --file …/skill-stacking.md --report` reproduces the judge's three rows to the word: 321w/13, 157w/19, 193w/7, 'oversized: 3'. `grep -ni 'slide size accepted'` on the file = 0, so no hatch exists. `rule.js slides 14` and `slides 10` read in full — §10 names supplementaries among the sections dropped from the deck. `grep -c` on the insertion anchor = 1. Honest caveat, since §14's calibration clause says 'merge two bullets that are one move split in two… before reaching for the accept-note': Design principles 1 and 2 are the live merge candidate (2's closing line, 'If you'd write the same code in two skills, refactor: don't duplicate', restates 1). I did not take that merge because it is a content decision on Antti's page, not a bookkeeping fix — if he prefers the merge, drop the third note.",
  "old_string": "**Skill frontmatter is quoted from the shipped docs, not from memory.**",
  "new_string": "**Slide size accepted:** The three-layer model — a supplementary never reaches the composed Slides deck (`check_slides.md` §10), so the 210w/6-bullet cap has no projected page to be a claim about on this file. The three layers are one argument and the `### Layer` seams are its joints; three separate `##` slides would break the model into three unrelated pages on the only layout this file has.\n\n**Slide size accepted:** Skill catalog by phase — nineteen one-line rows are the catalog. It is scanned for a skill name, and a PLAN/BUILD vs VERIFY/SHIP/OPS split cuts the lifecycle the section exists to show whole. Same non-projection ground as above.\n\n**Slide size accepted:** Design principles — seven numbered principles, one over the bullet cap, on a page that never projects. Each names a distinct rule the stack enforces and each is stated once; the list is the section.\n\n**Skill frontmatter is quoted from the shipped docs, not from memory.**",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/skill-stacking.md",
  "instance": "ae101--supplementary--skill-stacking.writing.json",
  "rule": "writing 3",
  "claim": "L179's maintainer note ('Runtime fork collapsed 2026-08-19… used to branch… the module branch opened… The 2026-08-01 M6 cut removed…') narrates the edit's journey rather than stating the present constraint.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Four transition verbs in two sentences — 'collapsed', 'used to branch', 'opened', 'removed' — and §3's Cut list names 'used to sit in X' as the shape. The dated-provenance carve-out protects the header, not the narration, and the rule requires judges to 'quote the transition verb', which is available here in quantity. Keeps, per the rule's own carve-out: the empirical finding (an M6 student has written a skill back at M3) and the do-not-reintroduce guard, which earns its place because runtime forks are a live corpus convention (`<!--flag:module:X-->`) inviting exactly this proposal. The rewrite also flips the note's opening from a negative subject to a positive one, per §3's 2026-09-05 phantom clause.",
  "evidence": "`node curriculum/evals/scripts/rule.js writing 3` read in full — the maintainer-block extension, the Cut/Keep split, and the 2026-09-05 negative-subject clause. Read L174-182 of the file. `grep -c '^\\*\\*Runtime fork collapsed 2026-08-19\\.\\*\\* `## Map your own kit` used to branch on whether the reader had done M6'` = 1. Cross-checked the pointer: `spot-gaps-build-the-loop.md` does carry an Artefact-contracts reference, so the cross-file pointer is live and survives the rewrite.",
  "old_string": "**Runtime fork collapsed 2026-08-19.** `## Map your own kit` used to branch on whether the reader had done M6, and the module branch opened *\"You shipped a second skill in M6.\"* The 2026-08-01 M6 cut removed the in-module second-skill build: M6's close now produces a handoff prompt the student runs later to author skills, so no skill ships inside the module (see `spot-gaps-build-the-loop.md` Artefact contracts). The surviving wording is true for every reader, M6 or not, because an M6 student has still written a skill back at M3. Do not restore the fork without an M6-produced skill to point at.",
  "new_string": "**`## Map your own kit` is written for one reader, M6 or not (2026-08-19).** The wording holds either way because an M6 student has written a skill back at M3. M6's close produces a handoff prompt the student runs later to author skills, so a module-conditional branch has no M6-produced skill to point at (see `spot-gaps-build-the-loop.md` Artefact contracts). Do not add one until there is.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/skill-stacking.md",
  "instance": "ae101--supplementary--skill-stacking.writing.json",
  "rule": "student_facing 13",
  "claim": "Five bare `CLAUDE.md` mentions name no layer (L59, L125 x3, L135, L157, L167), against §13's 'every time'.",
  "disposition": "EXEMPT_BODY",
  "confidence": "medium",
  "why": "The letter of §13 is 'every time', so the finding is not refutable outright — but the rule's stated harm (a student writing session-scoped rules into a PR-gated file) does not fire, because nothing on this page instructs the reader to write to any CLAUDE.md, and every mention resolves to the same one project-level file the Dino case study describes from L52 onward. That makes it a letter-compliance fix, and the judge scoped it correctly to the single furthest-from-anchor restatement. Supplementary body, so the maintainer's standing exemption applies and it lands directly. The bolded handle is left untouched to keep the diff minimal.",
  "evidence": "`node curriculum/evals/scripts/rule.js student_facing 13` read in full — three named layers, harm stated as governance ambiguity on writes. `grep -n 'CLAUDE\\.md'` over the body confirms the judge's inventory: qualified at L11/L33/L52 ('the project's CLAUDE.md', 'team CLAUDE.md', 'Dino's CLAUDE.md'), bare at L59/L125/L135/L157/L167. `grep -c 'Half the stacking lives in predicate-dispatch rules in CLAUDE.md, not inside the skills. Skills are handlers; CLAUDE.md is the dispatcher.'` = 1. Second-best candidate if one edit is not enough: L167's 'Is it a **route** (CLAUDE.md predicate fires it…)' in `## Map your own kit`, the one place the reader is asked to apply this to their own repo.",
  "old_string": "Half the stacking lives in predicate-dispatch rules in CLAUDE.md, not inside the skills. Skills are handlers; CLAUDE.md is the dispatcher.",
  "new_string": "Half the stacking lives in predicate-dispatch rules in the project's `./CLAUDE.md`, not inside the skills. Skills are handlers; `./CLAUDE.md` is the dispatcher.",
  "surface": "exempt-page"
 },
 {
  "target_file": "curriculum/exercises/spot-gaps-build-the-loop.md",
  "instance": "ae101--exercise--spot-gaps-build-the-loop.writing.json",
  "rule": "student_facing 26",
  "claim": "L43's 'A named config file looks risky to modify.' is NVA — obvious to a working engineer, and the instruction that follows works without it.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The fix already landed, and it landed as the exact text the judge's own fix_hint proposed. The named sentence no longer exists anywhere in the file.",
  "evidence": "`grep -c 'A named config file looks risky to modify' curriculum/exercises/spot-gaps-build-the-loop.md` = 0. Current L43 reads verbatim `- The agent may pause before editing ./CLAUDE.local.md. If it asks, approve.` — character-for-character the fix_hint's proposal ('Consider cutting to: The agent may pause before editing ./CLAUDE.local.md. If it asks, approve.'). `git status --short` shows the exercise file itself clean in the working tree, so this is committed state, not an uncommitted edit sitting under a live judge."
 },
 {
  "target_file": "curriculum/exercises/spot-gaps-build-the-loop.md",
  "instance": "ae101--exercise--spot-gaps-build-the-loop.writing.json",
  "rule": "prompts 38",
  "claim": "P2's body lead-in 'Ask Claude to cut the one rule the diagnosis killed.' shares no verb or noun-phrase with the fence's opening 'Read ./CLAUDE.local.md and this session's scrollback: …'.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The quoted lead-in no longer exists, and the current one mirrors the fence on both axes the rule asks for. L37 now opens on 'read' — the fence's own first verb — and names 'your rules file' and 'the diagnosis', pairing with the fence's './CLAUDE.local.md and this session's scrollback'. §38 is satisfied as the file stands.",
  "evidence": "`grep -c 'Ask Claude to cut the one rule the diagnosis killed' curriculum/exercises/spot-gaps-build-the-loop.md` = 0. Current L37: `Ask Claude to read your rules file against the diagnosis and cut the one rule it killed.` Read `curriculum/prompts/spot-gaps-build-the-loop-2.md` — opens `Read ./CLAUDE.local.md and this session's scrollback: the gap list I just ranked…`; shared verb 'read', shared referent the rules file. Separately checked L58's 2026-09-03 declined-list note: it shields the FIRST lead-in ('Ask Claude to read both sessions…') only, so it does not cover P2 and I am not stretching it — this row is stale on the prose, not shielded by the note. Caveat for the applier: `curriculum/prompts/spot-gaps-build-the-loop-2.md` carries an uncommitted working-tree edit to its final sentence (body region), so a re-judge of this pair needs `prompt-ok` bookkeeping regardless of this row."
 },
 {
  "target_file": "curriculum/lectures/the-far-half.md",
  "instance": "ae101--lecture--the-far-half.writing.json",
  "rule": "student_facing 33",
  "claim": "L7's clause 'The rest of the training goes there, starting with the session you send off in this module' takes the training itself as grammatical subject — the (a) arc-positioning failure mode — and survives the deletion test.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "The subject test fires cleanly: 'the rest of the training' is the training narrating its own shape, which §33 names as the highest-yield NVA in the corpus. Neither exemption reaches it — this is a lecture slide, not one of the canonical signposting sections (`## What You'll Learn`, `## Connections`, `## Bridge`/`## Next`, `## Prework`, `**The point:**`), and the `check_lectures §3` arc-lecture carve-out recorded at L25 licenses module *ranges* and map-position refs, not a clause whose subject is the training's forward path. The proposed fix is the smallest true one: it moves the subject to the student and keeps the module's own imminent action, which §33's predicate-leak clause permits (it bans promising what a LATER beat will do, not naming this module's). Card-shaped because it rewords a projected slide and the maintainer has taken both prior cuts on this file by card (L33, L39, each 'Antti-approved').",
  "evidence": "`node curriculum/evals/scripts/rule.js student_facing 33` read in full, plus `lectures 2` and `lectures 3` to test the carve-out the maintainer block invokes — §3's carve-out is 'arc lectures… keep module ranges' and 'map-*position* refs = spine-anchoring', neither covering a training-as-subject clause. `grep -c` on the L7 clause = 1. This is a re-file: the instance records the same sentence flagged at pin 1abb84c6, so it returns every pass until the prose changes or an accept-note lands.",
  "before": "- The map lights its other side now. Verification, Absorption, Outcome. **The rest of the training goes there, starting with** the session you send off in this module.",
  "after": "- The map lights its other side now. Verification, Absorption, Outcome. **You start there with** the session you send off in this module.",
  "risk": "Low-medium. L7 is now slide 1's ONLY bullet — the 2026-08-25 recap-bullet cut removed its neighbour — so the slide is the figure plus this one line and cannot absorb a full-clause cut without going thin. This edit trims nothing, only re-subjects. If you would rather refute than edit: the counter-argument is that a 4-minute container opener's whole job is orientation, and an accept-note at L25 saying so would stop the re-file permanently."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/the-lethal-trifecta.md",
  "instance": "ae101--supplementary--the-lethal-trifecta.writing.json",
  "rule": "writing 3",
  "claim": "L36's 'Proper-length pass' note narrates the edit ('skeleton bullets grown to slide thickness', 'all four sections CONVERTED in place', 'numbered three-leg list FOLDED into the frame bullet', 'closing kicker added') instead of stating the standing shape.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Genuine §3 hit, and the judge's carve-out reasoning is right: the note carries real keeps (the reference-page shape, the delivery-order-neutral finding, the bullets' provenance, the number-free filter-line constraint) buried in play-by-play. Every flagged clause is a transition verb narrating a completed edit — exactly the grammatical test §3 says to run when the `2026-` grep is useless on a stamp-dense surface. The rewrite keeps the dated provenance header (84 of 101 AE101 files carry one, and the rule says do not sweep it), converts the four journey clauses to present-tense structural facts, and drops 'grep above the fence: clean' as a record of a check run — session biography by definition. Maintainer block, lands directly.",
  "evidence": "`node curriculum/evals/scripts/rule.js writing 3` read in full. Verified each keep against the live body before preserving it: the kicker 'Three legs. Cut one.' is at L28; the delivery-order-neutral quote 'an access-surface map and a STRIDE pass on the feature' is at L18; the three legs do read as one inline enumeration inside L5's frame bullet; the number-free filter line is at L26 with its rationale backed at L60 of the backing block ('Keep it number-free'). Disambiguated the note's two colliding '§3' references — the first means the page's third section, so the rewrite names it 'the different-attacker section' instead. `grep -c '^\\*\\*Proper-length pass (2026-07-02, unaudited):\\*\\* skeleton bullets grown to slide thickness'` = 1.",
  "old_string": "**Proper-length pass (2026-07-02, unaudited):** skeleton bullets grown to slide thickness; reference-page shape kept (frame · mechanism · different-attacker · the move-as-governor). Per-passage verdicts: all four sections CONVERTED in place · §3 wording is delivery-order neutral (\"an access-surface map and a STRIDE pass on the feature\"), so it does not assert that both audits already ran when the security module is cut or reordered · numbered three-leg list FOLDED into the frame bullet · new bullets (legs-in-ordinary-wiring · accident-vs-adversary) derive from the trifecta's own mechanism + `theory-audit.md`'s adversary-model note (composes with blast-radius), no new sourced claims · Willison constraint quote kept verbatim · filter line kept number-free · closing kicker added (\"Three legs. Cut one.\"). §3 grep above the fence: clean.",
  "new_string": "**Proper-length pass (2026-07-02, unaudited):** the page holds four slide-thick sections in the reference-page shape — frame · mechanism · different-attacker · the move-as-governor — and closes on the kicker *\"Three legs. Cut one.\"* The three legs read as one inline enumeration inside the frame bullet. The different-attacker section's wording is delivery-order neutral (\"an access-surface map and a STRIDE pass on the feature\"), so it does not assert that both audits already ran when the security module is cut or reordered. The legs-in-ordinary-wiring and accident-vs-adversary bullets derive from the trifecta's own mechanism plus `theory-audit.md`'s adversary-model note (composes with blast-radius) and carry no new sourced claims. Willison's constraint sentence is quoted verbatim and the filter line stays number-free.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/exercises/author-test-strategy-skill.md",
  "instance": "ae101--exercise--author-test-strategy-skill.technical.json",
  "rule": "prompts 21",
  "claim": "P1's tail 'Show me before saving' is a pre-confirm on a local SKILL.md write, where §21 defaults to a post-action report.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "Not a merits refutation — an adjudicated accept whose note was never written where a judge reads. §21's carve-out (irreversible / external-trust-line) genuinely does not cover a local file write, so the judge reads the rule correctly; but the maintainer already made the call and the decision lives only in pre-cohort-todos.md ('Accepted as-is for first cohort'), which is precisely the cross-file tracking pointer check_writing §3 says rots. That file's own header states the convention: 'a closed item that carries a design decision moves to its source first (the prompt's frontmatter note: or the exercise maintainer block, where the editor reads)'. The sibling watch on ae101-m3-sharpen-skill spells out the same remedy verbatim: 'record the accept-by-design ... so the judge stops re-raising it'. The accept-note below covers this row and the §41 row together — one tail, two rules. §3's do-not-X licence is earned here by a named live pull: this exact tail has now been filed twice in one instance.",
  "evidence": "Read check_prompts §21 in full (side-effect enumeration is post/send/commit/update/push; carve-out is irreversible-or-external only). Read curriculum/prompts/author-test-strategy-skill-1.md:27 — tail is 'write ~/.claude/skills/test-strategy/SKILL.md as a personal skill (frontmatter with name + description, then instructions). Show me before saving.' Read pre-cohort-todos.md:25, the [watch] 'save-gate-then-immediate-read race in author-test-strategy-skill-1' bullet, marked '**Accepted as-is for first cohort.**' with a named firing condition (headless/unattended) and the named reword. Read the exercise maintainer block (L73-113): it carries an 'Accept-with-mitigation (check_pedagogy.md §56)' line but nothing on §21/§41. Confirmed `note:` is a live registry frontmatter field (10 prompts carry one) as an alternative home. grep -c on the anchor line = 1.",
  "anchor": "**Accept-with-mitigation (`check_pedagogy.md` §56):** no dedicated push-back move for a skipped self-critique ask.",
  "accept_note": "**Accept-with-mitigation (`check_prompts.md` §21 + §41).** `author-test-strategy-skill-1` closes on *\"Show me before saving\"*, so the pre-confirm rather than the SKILL.md write is the tail's last imperative. The gate is deliberate for an attended room: Phase 1's whole risk is a pyramid-shaped draft, the trainer push-back moves fire at exactly that turn, and the pre-save read is the student's one chance to catch it before the file lands. The exposure is headless-only — an unattended run waits for an approval nobody gives, and `author-test-strategy-skill-2` then opens on a missing file. The reword that would close it, *\"save now and show me after\"*, stays unapplied while cohorts are attended. Judges should not re-file §21 or §41 on this tail.",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The accept is real and time-boxed: pre-cohort-todos.md:25 reads \"**Accepted as-is for first cohort.**\" with a named firing condition, carried since 2026-08-15 (commits dccde3d7, 9c07b8aa), and its named fix is literally §21's fix — \"rewrite the save gate to *\\\"save now and show me after\\\"*.\" Moving that into the file is right; check_writing §3 says \"Cross-file tracking pointers rot.\" Two overreaches in the draft. (1) Scope: the tracked decision is \"for first cohort\"; the note rewrites it as \"stays unapplied while cohorts are attended,\" converting a time-boxed watch into an open-ended design stance, then closes \"Judges should not re-file §21 or §41 on this tail\" with no expiry. (2) Invented justification: \"the pre-save read is the student's one chance to catch it before the file lands\" is contradicted by the exercise's own design — `## Sharpen the skill from what came back` is the post-write correction loop, and Phase 3 states \"The skill is already shipped. The agent wrote it in Phase 1 and your push-back sharpened it during Phase 2's invoke-and-critique.\" That is precisely the after-the-fact correction §21 prefers, so the note defends the accept with a reason the page refutes. Also checked: no trainer push-back move fires at the pre-save turn specifically — P1 missed-detail (L98) reads the draft and works equally post-save.",
   "note_fix": "**`author-test-strategy-skill-1`'s save gate is accepted for the first cohort (`check_prompts.md` §21 + §41).** The prompt closes on *\"Show me before saving\"*, so the pre-confirm rather than the SKILL.md write is the tail's last imperative — the §41 shape, and a pre-confirm where §21 defaults to a post-action report. Both are known and shipped as-is for the first cohort, with a named firing condition: an unattended or headless run waits for an approval nobody gives, and `author-test-strategy-skill-2` then opens on a missing file. The reword that closes both is *\"save now and show me after\"*. Judges: do not re-file §21 or §41 on this tail while the accept holds. A headless run hitting the missing-artifact failure converts the accept, and the reword applies."
  }
 },
 {
  "target_file": "curriculum/exercises/author-test-strategy-skill.md",
  "instance": "ae101--exercise--author-test-strategy-skill.technical.json",
  "rule": "prompts 41",
  "claim": "The artifact instruction is not the last imperative in P1's tail; 'Show me before saving' follows the write and can block it.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "Same tail, same adjudication, one shared accept-note (see the §21 row — apply the note once, it names both rules). §41 fires harder than §21 here, not softer: 'written skill' is literally in the rule's own load-bearing-artifact enumeration, the SKILL.md is consumed by four downstream prompts plus module:spot-gaps-build-the-loop, and the rule's named symptom ('agent obeys STOP, skips the write') is word-for-word the failure the pre-cohort watch describes. So refusing this on the merits would be dishonest. What clears the row is the note, because the maintainer has already priced the risk and confined it to unattended runs, and because the fix is a prompt-body edit the approval gate reserves for him. A one-sentence fix is normally cheaper than an accept-note; here the one sentence is the gated one and the call against changing it is already on record.",
  "evidence": "Read check_prompts §41 in full — enumerates 'plan file, commit, written skill, ADR' as the load-bearing artifacts it protects. curriculum/prompts/author-test-strategy-skill-1.md frontmatter: `produces: test-strategy-skill` at ~/.claude/skills/test-strategy/SKILL.md, `consumed-by` author-test-strategy-skill-2, walk-and-send-off-2, diagnose-and-resend-6, module:spot-gaps-build-the-loop — four downstream consumers, so load-bearing is established, not assumed. pre-cohort-todos.md:25 records the identical failure chain and the 'Accepted as-is for first cohort' call. No §41 note anywhere in the exercise maintainer block (read L73-113).",
  "anchor": "**Accept-with-mitigation (`check_pedagogy.md` §56):** no dedicated push-back move for a skipped self-critique ask.",
  "accept_note": "Covered by the shared §21+§41 accept-note proposed on the row above — apply it once at the same anchor; it names both rules and both firing conditions. Do not write a second note.",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The triage concedes the merits outright — \"§41 fires harder than §21 here, not softer ... refusing this on the merits would be dishonest\" — and I confirm it: §41 protects \"plan file, commit, **written skill**, ADR\", the SKILL.md has four `consumed-by` entries in the prompt's own frontmatter, the tail is \"...Show me before saving.\", and §41's named symptom (\"agent obeys STOP, skips the write\") is the pre-cohort watch's failure chain word for word. So the only thing carrying the refutation is the maintainer's recorded acceptance — and that acceptance is explicitly temporary and conditioned. `pre-cohort-todos.md:25` reads: \"**Accepted as-is for first cohort.** Fires if any headless / unattended cohort run hits a missing-artifact failure downstream of that turn. Fix when it fires: ... rewrite the save gate to *'save now and show me after'*.\" The shared note's closing sentence — \"Judges should not re-file §21 or §41 on this tail\" — is unconditional and permanent. That converts a first-cohort, attended-delivery, fires-if-X accept into a rule switched off forever on a load-bearing-artifact reliability check. The refutation survives; the drafted note does not. Narrow it so the accept carries its own expiry.",
   "note_fix": "**Accept-with-mitigation (`check_prompts.md` §21 + §41) — conditional, first cohort only.** `author-test-strategy-skill-1` closes on *\"Show me before saving\"*, so the pre-confirm rather than the SKILL.md write is the tail's last imperative — §41's exact shape, on an artifact four downstream prompts consume (`author-test-strategy-skill-2`, `walk-and-send-off-2`, `diagnose-and-resend-6`, `module:spot-gaps-build-the-loop`). The gate is deliberate for an *attended* room: Phase 1's whole risk is a pyramid-shaped draft, the trainer push-back moves fire at exactly that turn, and the pre-save read is the student's one chance to catch it before the file lands. The recorded exposure is headless/unattended runs, where the agent waits for an approval nobody gives and `author-test-strategy-skill-2` then opens on a missing file. **The accept is the one in `pre-cohort-todos.md` and inherits its scope: \"Accepted as-is for first cohort\", fire condition \"any headless / unattended cohort run hits a missing-artifact failure downstream of that turn\", with the reword *\"save now and show me after\"* held in reserve.** While that accept stands and cohorts are attended, do not re-file §21 or §41 on this tail. It lapses — and both rules fire normally again — once the watch fires, once a run is unattended, or once the first cohort is past."
  }
 },
 {
  "target_file": "curriculum/exercises/author-test-strategy-skill.md",
  "instance": "ae101--exercise--author-test-strategy-skill.writing.json",
  "rule": "student_facing 17",
  "claim": "'## What this sets up' is a declarative noun-phrase header over a section whose body tells the student to do something.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "The finding survives the rule's carve-outs — §17's exemption is for canonical module-architecture section names, and 'What this sets up' is on no such list; the lecture/reference declarative licence does not reach an exercise. Two things push it past the judge's own hedge. First, the corpus's sibling beat is already verb-led: read-your-stack.md closes the identical worktree-leftover move under `## Decide what crosses back to your main repo`. Second, the header is not merely un-verbed, it is inaccurate — the only other file using it, map-the-access-surface.md, genuinely forward-points ('The STRIDE exercise invokes the curated STRIDE skill on the map you just built'), whereas this section sets nothing up, it disposes of leftovers. Card rather than a direct swap because a header rename changes the section's frame, not just its wording, and no maintainer note calls the direction.",
  "evidence": "Read check_student_facing §17 including both carve-outs. curriculum/exercises/author-test-strategy-skill.md:69-71 — header plus a Note widget ending 'Those changes are yours to handle later: keep them, carry the useful parts to your main repo, or delete the worktree.' grep -rln '^## What this sets up' over curriculum/ returns exactly two files: this one and map-the-access-surface.md; read the latter's section, which is genuinely forward-pointing. read-your-stack.md:57 carries `## Decide what crosses back to your main repo` over the same leftover-disposition beat. grep -c on the header = 1.",
  "before": "## **What this sets up**",
  "after": "## **Handle what's left in the worktree**",
  "risk": "Two exercises share this header today, so the pair diverges unless map-the-access-surface is left alone deliberately (it should be — its header is accurate). 'Handle' rather than 'Decide' because the file already opens `## Decide if it graduates to the team` nine lines up and a doubled verb reads as a tic. Counter-read if Antti wants it: the action is explicitly deferred ('later') and the section is a trailing FYI, which is the closest §17 has to an orientation exemption — in which case the header stays and this needs an accept-note instead."
 },
 {
  "target_file": "curriculum/exercises/author-test-strategy-skill.md",
  "instance": "ae101--exercise--author-test-strategy-skill.writing.json",
  "rule": "student_facing 21",
  "claim": "L49 'Claude invoked the skill it just helped author, then graded the result' names the product where autonomous multi-step work takes the agent.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Direct edit with exact strings, not a card: this is a controlled-vocabulary swap whose direction the rule already fixes, so it falls under CLAUDE.md §26's mechanical-swap exemption — claim, voice and ask all untouched. §21's 2026-08-30 Antti active-stance override names self-critique as the calibration case that stays 'the agent' ('Ask the agent what's weakest about its own output' stays agent), and invoke-then-self-grade is exactly that shape. The file's own body already says 'the agent' for the same referent three times, so the swap makes it consistent rather than imposing a new register; the four remaining Claude-mentions (L7, L25, L32, L42) are addressee or product uses and are correctly left alone. Vocabulary changes are registry-governed per curriculum/CLAUDE.md, which is what makes this mechanical rather than editorial.",
  "evidence": "Read check_student_facing §21 in full including the 2026-08-30 override and its calibration pair. curriculum/exercises/author-test-strategy-skill.md:49 quoted verbatim. Sibling agent-uses confirmed at L21 ('the agent drafts the SKILL.md'), L40 ('The agent reads the codebase'), L58 ('The agent wrote it in Phase 1'). Body ratio is 5 Claude : 4 agent, well short of §21's lopsided 10:0 audit signal, so this is one line and not a sweep. grep -c on the old_string = 1.",
  "old_string": "- The grade is biased by design. Claude invoked the skill it just helped author, then graded the result in the same context window: same-window self-charity.",
  "new_string": "- The grade is biased by design. The agent invoked the skill it just helped author, then graded the result in the same context window: same-window self-charity.",
  "surface": "student-facing-body"
 },
 {
  "target_file": "curriculum/exercises/fork-the-worktree.md",
  "instance": "ae101--exercise--fork-the-worktree.writing.json",
  "rule": "writing 15",
  "claim": "'the two copies' in L22 has more than one available antecedent on a cold read.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "§15's pronoun/demonstrative check is explicitly not post-trim-gated — it 'fires on ANY closer' — and in the base cut this sentence is the closer: the following clause sits inside `<!--flag:module:spot-gaps-build-the-loop-->` and is absent from any cut that drops M6. The trap is real: 'the two copies' lands one clause after a pair of exactly two named things ('your `CLAUDE.local.md`, `observations/`'), so the wrong antecedent is the nearer one, while the reading the M6 merge-back sentence depends on is original-vs-fork. Card rather than a direct edit because disambiguating requires choosing which reading was meant — that is a semantic call, not the mechanical swap §26 exempts.",
  "evidence": "Read check_writing §15 in full, including the 'NOT post-trim-gated' second block and its two-fault scan. curriculum/exercises/fork-the-worktree.md:22 read with flag markup intact — the runtime fork wraps 'and Module 4' and the trailing 'After Module 6 you decide what to merge back.' sentence, confirming the flagged sentence is bullet-final in the no-M6 cut. The proposed replacement span sits outside both flag pairs, so the edit cannot break the fork. Read the whole maintainer block (L43-62): no note covers this sentence. grep -c on 'From there the two copies compound separately.' = 1.",
  "before": "From there **the two copies** compound separately.",
  "after": "From there **the original and the fork** compound separately.",
  "risk": "Adds three words to a slide that already carries a runtime fork, and the memory's standing taste note is that on curriculum line-reads the default move is CUT with additions near-auto-rejected. If Antti reads the ambiguity as theoretical — 'From there' does anchor to the fork moment, which favours the intended reading — the original is shorter and stays. 'the fork' is chosen over 'the forked copy' because it is the file's own vocabulary (H1, and `## Anchor the fork to the run coordinates`)."
 },
 {
  "target_file": "curriculum/exercises/fork-the-worktree.md",
  "instance": "ae101--exercise--fork-the-worktree.writing.json",
  "rule": "student_facing 21",
  "claim": "L29 and L39 both use 'Claude' as the subject of multi-step autonomous execution, where §21 defaults to 'the agent'.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "This row's old/new covers L39 only; the L29 half is discharged by the §26 row's edit below, which rewrites that exact line and swaps the subject in the same stroke — one edit per line, no collision, apply both. L39 is the unambiguous instance: reads a protected block, uses a named branch, forks from a commit is three chained tool-actions, with no addressee grammar and no UI-surface or version-grounding carve-out in reach. Mechanical under §26 for the same reason as the author-test-strategy row: registry-governed vocabulary, direction fixed by the 2026-08-30 override, claim and ask untouched. Confidence is medium rather than high only because the file runs 6 Claude : 0 agent — the very ratio §21 names as an audit signal — so a fuller pass is arguably owed; but the residue after these two swaps is principled and I have deliberately not widened the finding.",
  "evidence": "Read check_student_facing §21 in full. curriculum/exercises/fork-the-worktree.md:29 and :39 quoted verbatim. Counted every Claude-mention in body: L23 ('works out <repo-name> from the working directory ... steer it in chat' — inference plus addressee), L25 ('Ask Claude to fork' — addressee), L29, L35 ('ask it to copy' — addressee), L39, L40 ('the starting-point SHA Claude reported' — output-to-you), L41 ('ask Claude to run git merge-base' — addressee). After the two swaps every survivor is a legitimate addressee or report use, so the file reads consistently rather than half-converted. The judge's staleness claim checks out: writing PASS was pinned 2026-08-29, one day before the override landed. grep -c on the L39 old_string = 1.",
  "old_string": "- Claude normally reads the protected `Run coordinates` block in `task.md`, uses the `m4/<slug>` branch named there, and forks from that branch's \"M4 starting point\" commit.",
  "new_string": "- The agent normally reads the protected `Run coordinates` block in `task.md`, uses the `m4/<slug>` branch named there, and forks from that branch's \"M4 starting point\" commit.",
  "surface": "student-facing-body"
 },
 {
  "target_file": "curriculum/exercises/fork-the-worktree.md",
  "instance": "ae101--exercise--fork-the-worktree.writing.json",
  "rule": "student_facing 26",
  "claim": "The numeral in 'listing the six sub-steps' is NVA — it changes nothing about the instruction that follows.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "A pure deletion, which is the corpus's recorded default move on line-reads, and §26's own fastest screen settles it: the count is obvious-to-irrelevant to a working engineer whose next instruction is 'Skim past it', and the sentence reads identically at five sub-steps or seven. It is also unverifiable prose about model behaviour — the registry prompt is one paragraph, a two-line copy block and a closer, so 'six' is a snapshot of one observed plan summary and drifts silently the moment the prompt or the model moves. Note this single edit also discharges the L29 half of the §21 row above: the new string carries the agent-swap, so apply this row and the §21 row and the line is done in one touch.",
  "evidence": "Read check_student_facing §26 in full, including the 2026-08-14 obvious-to-a-working-engineer screen and the run-NVA-before-truth ordering. curriculum/exercises/fork-the-worktree.md:29 quoted verbatim; the instruction it modifies is 'Skim past it.' in the same line. Read curriculum/prompts/ae101-m5-worktree-setup.md in full — the body has no enumerated step list, so the six is the agent's own plan-summary shape, not a countable property of the prompt. Read the maintainer block (L43-62): no note protects the numeral. grep -c on the old_string = 1.",
  "old_string": "- Claude usually opens with a plan summary listing the six sub-steps, then runs them. Skim past it.",
  "new_string": "- The agent usually opens with a plan summary, then runs it. Skim past it.",
  "surface": "student-facing-body"
 },
 {
  "target_file": "curriculum/exercises/fork-the-worktree.md",
  "instance": "ae101--exercise--fork-the-worktree.writing.json",
  "rule": "prompts 38",
  "claim": "The body lead-in shares no verb or noun-phrase with the prompt's opening clause ('Read ./task.md.'), so phrasal mirroring fails.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "The finding measures against the fence's first sentence and stops there, but §38's stated harm is that 'body + prompt sound like two drafts' — and here they share three tokens verbatim. The prompt opens on instrumental setup and states its load-bearing move lower down: 'Spin up a git worktree at that commit' plus 'Then copy the gitignored personal files into the worktree'. The lead-in ('Ask Claude to fork the worktree and copy your gitignored files across') mirrors that clause on both axes the rule tests — verb 'copy' and noun-phrases 'worktree' and 'gitignored files'. The maintainer has already read §38 this way on the same shape: read-your-stack.md's maintainer block declines three §38 findings with 'the study, shapes and primitives lead-ins keep their own verbs (check_prompts.md §38: the noun-phrases echo)'. That note binds its own file only, which is why this one needs its own; but it establishes that a noun-phrase echo against the artifact clause is the house reading, not an invention. The judge's own alternative fix would headline the means (read task.md) over the end (fork the worktree), which is the worse lead-in and which the judge itself concedes.",
  "evidence": "Read check_prompts §38 and carve-out 38a in full (38a is surprise-pedagogy only and does not apply — nothing here is hidden from the student). Read curriculum/prompts/ae101-m5-worktree-setup.md:26-32 in full: opening clause 'Read `./task.md`.', artifact clause 'Spin up a git worktree at that commit', and line 28 'Then copy the gitignored personal files into the worktree'. Lead-in at curriculum/exercises/fork-the-worktree.md:25. Cross-checked the precedent at curriculum/exercises/read-your-stack.md:123. Read the fork-the-worktree maintainer block (L43-62): no §38 note present. grep -c on the anchor line = 1.",
  "anchor": "**Placement:** immediately before `[Exercise: Diagnose and re-send]`.",
  "accept_note": "**Lead-in mirrors the artifact clause, not the opening clause (`check_prompts.md` §38).** `ae101-m5-worktree-setup` opens on instrumental setup (*Read `./task.md`*) and states its load-bearing move further down: *Spin up a git worktree at that commit*, then *copy the gitignored personal files into the worktree*. The body lead-in — *Ask Claude to fork the worktree and copy your gitignored files across* — shares verb and noun-phrase with that clause verbatim (`copy`, `worktree`, `gitignored files`), so body and fence do not read as two drafts, which is the harm §38 guards. Opening the lead-in on the task.md read would headline the means over the end. Judges should not re-file §38 on this lead-in.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "§38: \"Body sentence immediately preceding `{{prompt}}` block mirrors prompt's **opening clause** ... Diagnostic: place body lead-in's last sentence next to **prompt's first sentence**\" — and §2a repeats the anchor in capitals: \"Fix by mirroring only the prompt's **OPENING** clause per §38.\" The triage's load-bearing claim — that read-your-stack.md establishes \"a noun-phrase echo against the artifact clause is the house reading\" — does not check out. I read all three fences there: study (lead-in \"the kinds of work that recur\" vs opening \"the kinds of work I do over and over\"), shapes (lead-in \"draw your top few work-shapes as ... diagrams\" vs opening \"For the top few, draw each one as a mermaid diagram\"), primitives (lead-in \"the checking primitives\" vs opening \"my menu of checks\"). In every case the echo is with the prompt's OPENING sentence and only the verb differs — which is what that maintainer note says (\"keep their own verbs ... the noun-phrases echo\"). It does not license anchoring on a clause eight sentences down. Here the lead-in shares nothing — verb or noun-phrase — with \"Read `./task.md`.\" The judge's second fix option (name outcome-first lead-ins as an exception alongside 38a) is the right route if this really is the house reading; that belongs in `check_prompts.md`, not in a per-file note asserting a reading the rule text and its own sub-rule contradict. Triage's own confidence: medium."
  }
 },
 {
  "target_file": "curriculum/exercises/read-your-stack.md",
  "instance": "ae101--exercise--read-your-stack.behavior.json",
  "rule": "prompts 38",
  "claim": "Lead-in 3 (L49) does not mirror fence 3's opening clause 'Expand my menu of checks before I read my own map against it.'",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The adjudication landed eight minutes after this judge filed. At the behavior pin the file's 'Declined at the 2026-09-03 polish pass' note covered only the say/tell callouts and the no-preamble rows; commit c95af12a then extended it with 'the study, shapes and primitives lead-ins keep their own verbs (`check_prompts.md` §38: the noun-phrases echo)' and closed on 'Judges should not re-file any of these.' The judge scored a body that did not yet carry the note, so nothing is owed — no edit, no new note, and the existing one already names this rule by number. Clear the row against the current file.",
  "evidence": "Behavior instance pins body_sha 802f4110…; Quality line records behavior@54577f39 (2026-09-03 21:13:55). `git log -S'the study, shapes and primitives lead-ins keep their own verbs' -- curriculum/exercises/read-your-stack.md` returns exactly one commit, c95af12a (2026-09-03 21:21:57), i.e. 8 minutes after the pin. `git show 54577f39:curriculum/exercises/read-your-stack.md | grep -n 'Declined at'` returns the short form of the note without the §38 clause; the same grep at HEAD returns the extended form. Read check_prompts §38 to confirm the note's reasoning is available under the rule rather than against it. Current note is at curriculum/exercises/read-your-stack.md:123."
 },
 {
  "target_file": "curriculum/exercises/read-your-stack.md",
  "instance": "ae101--exercise--read-your-stack.behavior.json",
  "rule": "pedagogy 53",
  "claim": "The 'Expect the list to look familiar' paragraph at L53 enumerates ~10 primitives, handing the student the prompt's answer.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "Same eight-minute gap as the §38 row, same commit. c95af12a added 'the *Expect the list to look familiar* paragraph keeps its examples (`check_pedagogy.md` §53: it sits after the fence and teaches how to read the answer, and the backing block's holds row carries the convergence claim)'. The note cites §53 by number and lands on the rule's own legal shape — §53 explicitly preserves 'prose teaching how to READ the answer coming', which is what 'Your list won't be exact. The primitives Claude names are the ones your codebase already runs.' does. The judge is right that position is not the test; it filed before the maintainer's answer to that existed. No edit, no new note.",
  "evidence": "Read check_pedagogy §53 in full, including the widened-2026-08-12 clause and the two shapes that stay legal. curriculum/exercises/read-your-stack.md:53 read verbatim; the paragraph closes on two read-the-answer sentences, not on the enumeration. Backing block at L86 registers `primitives-will-look-familiar` as a vision claim, and the Stance 'decided' row (L99) confines the beat to an optional sidestep nothing downstream load-bears on. Commit timing verified as on the §38 row: note clause added at c95af12a 2026-09-03 21:21:57, behavior pin 54577f39 at 21:13:55. Current note at L123."
 },
 {
  "target_file": "curriculum/exercises/read-your-stack.md",
  "instance": "ae101--exercise--read-your-stack.pedagogy.json",
  "rule": "pedagogy 65",
  "claim": "This file's closing Note assumes the M5 worktree exists; verify M6's own '## Start here' carries the SS65 rescue callout for a missing prerequisite.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "Not a defect — a verification task the judge explicitly deferred, and the verification comes back clean. §65 puts the rescue obligation on the receiving module's opener, not on each exercise, and spot-gaps-build-the-loop.md's '## Start here' carries both grades the rule requires: the thin/stalled ladder (nudge / fresh re-send / decide whether to wait, stop, or accept the partial) and the accept-loss floor ('You lose a bit of value but you can still do this module's exercises'). It also carries the maintainer note §65 asks for, documenting the rule-5 exception so judges leave it alone. Nothing is owed on read-your-stack.md and nothing is owed on the module. Close the row.",
  "evidence": "Read check_pedagogy §65 in full — two failure grades, body blockquote callouts at `## Start here`, maintainer note documenting the rule-5 carve-out. Extracted the full `## Start here` section of curriculum/trainings/agentic-engineering-101/spot-gaps-build-the-loop.md: two consecutive blockquotes, '**Packaged session thin or missing?**' and '**Just can't get the M5 session working?**', matching grade (a) and the accept-loss floor. The documenting note is at spot-gaps-build-the-loop.md:70, naming the rule-5 exception and closing 'Future judges should not re-flag.' The closing Note this todo names is at read-your-stack.md:57-59 and is unchanged."
 },
 {
  "target_file": "curriculum/exercises/read-your-stack.md",
  "instance": "ae101--exercise--read-your-stack.writing.json",
  "rule": "student_facing 38",
  "claim": "H1 'Read your stack' names the process, not the artifact, failing the build-squint test.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The fix landed, and in exactly the shape the judge asked for. The finding was written against `# Read your stack`; the H1 now reads `# Read your stack, *draw what recurs*` — process verb paired with an italicised deliverable noun, the same pattern the judge cited from `# Diagnose and *package*` and the rest of the library. The judge's own fix_hint proposed `Read your stack, *draw the shape*`; what shipped is a variant of it. The slug stayed put, which the rule's own note licenses as a separate, costlier graph op. No edit and no note owed.",
  "evidence": "`head -1 curriculum/exercises/read-your-stack.md` → `# Read your stack, *draw what recurs*`. `grep -c '^# Read your stack$'` → 0, i.e. the flagged string no longer exists. `git show 4589f1d5:curriculum/exercises/read-your-stack.md | head -1` → `# Read your stack`, confirming the pinned body the judge read. `git log -S'draw what recurs'` → c6925bcf 'The stack exercise's title names what it builds' (2026-09-04 14:01), and `git merge-base --is-ancestor 4589f1d5 c6925bcf` returns true, so the rename postdates the writing pin. Read check_student_facing §38 including the drill carve-out (inapplicable — an artifact exists per L11)."
 },
 {
  "target_file": "curriculum/lectures/the-machine-you-just-met.md",
  "instance": "ae101--lecture--the-machine-you-just-met.writing.json",
  "rule": "student_facing 21",
  "claim": "L47 'Claude's first read was partly wrong' names the product where the file's own agent/LLM split takes 'the agent'.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "Already fixed, with the judge's exact proposed wording. Commit 94fcca2d — 'M1's recall beat uses the file's own agent/LLM split' — swapped the body sentence the day after the writing pin, and the backing block's quoted claim text was updated in the same pass so the ledger and the body agree. The only surviving instances of the old string are derived build outputs under curriculum/evals/body-views/ (regenerated from the pinned sha) and the instance JSONs themselves, neither of which a student reads. Note the backing claim KEY is still `claudes-first-read-partly-wrong`; that is a stable identifier, not prose, and should not be renamed.",
  "evidence": "`grep -rn \"Claude's first read\" curriculum/` returns hits only in curriculum/evals/body-views/*.txt|.md (build outputs) and three instance JSONs — zero hits in curriculum/lectures/the-machine-you-just-met.md. The live body at L47 reads 'The agent's first read was partly wrong.' and the backing block at L110 quotes the same new wording. `git show 0c5cacc8:curriculum/lectures/the-machine-you-just-met.md | sed -n '47p'` shows the old 'Claude's first read' at the writing pin (2026-09-02 20:39). `git log -S\"The agent's first read was partly wrong\"` → 94fcca2d (2026-09-03 08:10). Read check_student_facing §21 to confirm the swap direction matches the 2026-08-30 override rather than merely one judge's taste."
 },
 {
  "target_file": "curriculum/lectures/the-machine-you-just-met.md",
  "instance": "ae101--lecture--the-machine-you-just-met.story.json",
  "rule": "writing 3",
  "claim": "The maintainer Placement line calls compound-and-close 'Ex3'; M1's own Start here order makes it Ex4.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "A stale locator inside a maintainer block — nobody but the maintainer and the judges reads the string, and the swap is mechanical: only the ordinal moves, while the line's actual claims (final lecture of M1, sits after compound-and-close) are correct and untouched. The story judge was right to refuse it as a story finding and right that it belongs to a direct fix. Verified against the module rather than inferred: getting-going.md's '## Start here' lists orient-and-introspect, fix-tests-first, close-the-ticket, compound-and-close, then this lecture — so compound-and-close is the fourth exercise and Ex3 is close-the-ticket.",
  "evidence": "Extracted the full '## Start here' section of curriculum/trainings/agentic-engineering-101/getting-going.md: [Orient and map the window](exercises/orient-and-introspect.md), [Prove the bug, then ship the fix](exercises/fix-tests-first.md), [Close the ticket](exercises/close-the-ticket.md), [Compound and close](exercises/compound-and-close.md), [The machine you just met](lectures/the-machine-you-just-met.md) — four exercises, compound-and-close fourth. Flagged line is at curriculum/lectures/the-machine-you-just-met.md:75, inside the block opened by `<!-- maintainer -->` at L65. `grep -c` on the old_string = 1, and `grep -rn 'compound-and-close` (Ex' curriculum/` finds no other occurrence of the pattern anywhere in the corpus, so there is no twin to fix. Read check_writing §3's maintainer-block clauses to confirm a dated provenance header is legitimate and only the wrong ordinal needs touching.",
  "old_string": "**Placement:** M1's final lecture, after `compound-and-close` (Ex3).",
  "new_string": "**Placement:** M1's final lecture, after `compound-and-close` (Ex4).",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/how-the-best-do-ci-cd.md",
  "instance": "ae101--supplementary--how-the-best-do-ci-cd.strategy.json",
  "rule": "strategy_tie_in 8",
  "claim": "The strategy doc restates this file's structure at L771 and both restated spans have rotted; the file itself is compliant.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "THE EDIT IS NOT IN THIS REPO. It lands in the private strategy doc at the absolute path `~/Library/CloudStorage/GoogleDrive-tevanlin@gmail.com/My Drive/bosser/strategy/content-strategy-agentic-engineering-101.md` (resolved via the bosser-strategy skill); target_file names the curriculum file only because that is what the todo was filed against. Both rotted spans are real and I re-derived each rather than trusting the judge. §8 binds doc→curriculum and its fix rule is 'correct the span, never empty it', so the audience-pin clause is corrected rather than deleted and the header-3 sub-clause is trimmed to match the shipped heading. I grepped for the twin §8 warns about and there is none, so one edit closes it. Non-student-facing, no gate.",
  "evidence": "Read check_strategy_tie_in §8 in full, including the doc→curriculum direction and the correct-don't-empty clause. Doc L771 read verbatim. (a) It says 'the IC's next-altitude reading after their second skill'; the file's own maintainer block pins 'The supplementary is the IC's next-altitude reading after M6: the same loop, slightly wider lens', and pre-cohort-todos.md records that the M6 second-skill beat is gone ('The M6 `session-shaper` half died with the 2026-08-01 authoring-sequence cut'), so 'after their second skill' names a beat that no longer exists. (b) It enumerates '(3) tier by blast radius, make the Tier-1 case on your own PRs'; `grep -n '^## '` on the file shows header 3 is exactly '## 3. Tier by blast radius', with the make-the-case framing surviving only in the body's Try-this-Monday line at L41. Twin check: `grep -n 'next-altitude'` → 1 hit, `grep -n 'Tier by blast radius'` → 0 hits, `grep -n 'second skill'` → 1 hit, all on L771. Doc headers 1/2/4/5/6 in the same enumeration still match the file's shipped headings, so only clause (3) is trimmed. Verified the old_string occurs exactly once in the doc via a python count.",
  "old_string": "The shipped piece is the IC's next-altitude reading after their second skill: same loop, slightly wider lens. Six moves, each a setup claim plus a practitioner case — (1) treat CI as feedback into the loop, not a gate to pass; (2) fan out review, converge on the pattern; (3) tier by blast radius, make the Tier-1 case on your own PRs;",
  "new_string": "The shipped piece is the IC's next-altitude reading after M6: same loop, slightly wider lens. Six moves, each a setup claim plus a practitioner case — (1) treat CI as feedback into the loop, not a gate to pass; (2) fan out review, converge on the pattern; (3) tier by blast radius;",
  "surface": "anchor-not-found"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/workflow-composition-lineages.md",
  "instance": "ae101--supplementary--workflow-composition-lineages.slides.json",
  "rule": "slides 9",
  "claim": "Chunk 8 carries 7-8 bolded spans, far past §9's '≤1 bolded element on most slides' budget.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The finding charges a budget the rule switches off for this surface, and §9 names this very file while doing it. Two waivers apply and the judge cited neither: the 2026-08-13 Antti-directed lookup-surface waiver ('The same waiver covers lookup surfaces — reference and supplementary pages … The per-slide budget is off there'), and the prose-paragraph clause immediately after it, which lists `workflow-composition-lineages.md` by name as an exemplar. Everything the judge counted is a count violation; the clause that still binds — the ≲5-word handle cap — is satisfied by every span (longest is **CLAUDE.md as half the system.** at five). §9's own closing instruction is the tell: 'Before filing any §9 count violation, check whether another rule or the module template requires the bold you are about to charge for.' The file's maintainer block runs a per-item accept convention, so the note below matches it.",
  "evidence": "Read check_slides §9 in full, including the reading-list waiver, the reference/supplementary waiver, and the prose-paragraph clause naming this file. Read the flagged chunk at curriculum/trainings/agentic-engineering-101/supplementary/workflow-composition-lineages.md:104-134 and counted every bolded span by hand: **A single orchestrator** (3), **Phase-organised skill catalogue.** (4), **Design principles** (2), **Front-loaded read-only analysis.** (4), **Mandatory gates fire positionally** (4), **Re-invocation as control flow.** (4), **CLAUDE.md as half the system.** (5) — all within the cap, none crossing a first-sentence boundary. The eighth mark the judge counted, inline **scales cross-repo** at L128, is the only genuinely non-handle span and the judge itself conceded it as legitimate. Confirmed the page is under supplementary/ and its own maintainer block declares it 'not in-room' reading. grep -c on the anchor line = 1.",
  "anchor": "**Slide deixis accepted:** \"moves listed above\" (check_slides.md §12)",
  "accept_note": "**Bold budget accepted (`check_slides.md` §9).** This is a supplementary lookup page, so §9's per-slide budget is off under the 2026-08-13 waiver and only the handle cap binds. Every bolded span on *Skill stacking system, Dino: the worked example* is an entry lead inside the ≲5-word cap and names the design principle its entry then explains; none crosses its first sentence. §9's own text lists this file as an exemplar of the prose-paragraph lookup surface. Judges should not file §9 on the bold count here.",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The count refutation holds — §9: \"**The same waiver covers lookup surfaces — reference and supplementary pages.** (Antti-directed 2026-08-13.) ... The per-slide budget is off there\", and the maintainer block confirms the surface (\"**Time:** 15–25 min student read; not in-room\"). But the drafted note asserts \"Every bolded span on *Skill stacking system, Dino: the worked example* is an entry lead inside the ≲5-word cap\", and that is false: the inline **scales cross-repo** at L128 sits mid-paragraph in the dispatcher narration, which §9 governs separately (\"Narrative, demo narration, recognition beats, mechanism prose = no bold\"). Neither judge nor triage adjudicated it — the judge waved it through in passing (\"Each span is individually a legitimate handle\") — so the note would durably shield a live, unexamined §9 exposure. Second overstatement: \"§9's own text lists this file as an exemplar of the prose-paragraph lookup surface\" — §9 names \"`workflow-composition-lineages.md`'s **filter questions**\", i.e. the *How to pick what to study* section, not this chunk; read as drafted it would extend the cap-to-a-full-lead-sentence relaxation across the whole file.",
   "note_fix": "**Bold count accepted (`check_slides.md` §9).** This is a supplementary lookup page — the maintainer block's `**Time:**` line reads *not in-room* — so §9's per-slide budget is off under the 2026-08-13 reference/supplementary waiver, and the count alone on *Skill stacking system, Dino: the worked example* is not a violation. The seven bulleted entry leads across that chunk's two lists are handles inside the ≲5-word cap, one per entry, none crossing its first sentence. The handle cap and §9's no-bold-on-mechanism-prose clause still bind, and the inline **scales cross-repo** in the dispatcher paragraph is not adjudicated by this note. Judges: do not file §9 on the bold count on this chunk."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/workflow-composition-lineages.md",
  "instance": "ae101--supplementary--workflow-composition-lineages.writing.json",
  "rule": "writing 3",
  "claim": "'Delivery mode: In the training index, not linked from the M6 deck' defines the page by a phantom — a deck link that does not exist.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The judge ran §3's own two-question ladder, passed the first, and then failed the second on a premise I can falsify. Question one — who would propose the deck link and why — it answered itself: 'Pairs with `lectures/composing-the-workflow.md`' two lines up is a real, textually-invited pull, which under §3 EARNS the guard ('a shape the corpus's own convention invites'). Question two — is the positive fact already stated nearby — it answered yes, on the reasoning that 'In the training index' implies not-in-the-deck. It does not. Six AE101 module files deck-link a supplementary, so index-listing and deck-linking are routinely both true, and the negation is the only clause carrying the delivery boundary a trainer needs. M6 is the exception that makes the clause worth stating, and M6's own maintainer block already records the identical fact for the sibling supplementary — so this is house convention, not a ghost. §3's maintainer-block clause keeps exactly this category: 'the standing constraint it anchors'.",
  "evidence": "Read check_writing §3 in full, including the 2026-09-05 phantom-subject clause, the live-pull ladder and the curriculum-maintainer-block carve-out. Flagged line at curriculum/trainings/agentic-engineering-101/supplementary/workflow-composition-lineages.md:163, inside the maintainer block; the 'Pairs with `lectures/composing-the-workflow.md`' line the judge cites is at L159. `grep -rn 'supplementary/' curriculum/trainings/agentic-engineering-101/*.md` returns deck links in getting-going.md:79, earn-the-trust.md:111, learn-from-the-test.md:11, plan-mode-done-right.md:14, prework.md:34 and run-the-first-experiment.md:12/80/84 — six module files, so the corpus convention is that supplementaries ARE deck-linked. The M6 counterpart is recorded at spot-gaps-build-the-loop.md:57: '`## Next` cut whole (Antti 2026-09-02) … the CI/CD supplementary has no in-module link. Do not restore.' grep -c on the anchor line = 1.",
  "anchor": "**Delivery mode:** In the training index, not linked from the M6 deck.",
  "accept_note": "**The delivery-mode negation is a standing constraint, not a phantom (`check_writing.md` §3).** Six AE101 module files deck-link a supplementary, so *in the training index* does not on its own rule out an M6 deck link, and *Pairs with `lectures/composing-the-workflow.md`* two lines up is the live pull that would propose one. M6 links no supplementary at all; `spot-gaps-build-the-loop.md`'s maintainer block records the same fact for `how-the-best-do-ci-cd.md`. Judges should not re-file §3 on this clause.",
  "verification": {
   "ruling": "REFUTE_STANDS",
   "reason": "§3: \"A named live pull (a judge that keeps re-filing it, a shape the corpus's own convention invites, an ordering someone actually argued for) earns the guard; the mere fact that X once existed does not. Second question when the guard survives: is the positive fact already stated nearby?\" I tried to break this on the harder ground the triage did not use — `git diff 4a722813..HEAD` confirms the line was rewritten this pass from the positive \"Linked from M6's `composing-the-workflow.md` lecture,\" which is exactly the once-existed pull the rule refuses. It survives anyway, because the corpus-convention pull is independent of that history and is the rule's own named earning condition: getting-going.md:79, earn-the-trust.md:111, learn-from-the-test.md:11, plan-mode-done-right.md:14, prework.md:34 and run-the-first-experiment.md:12/80/84 all deck-link a supplementary, and the judge itself conceded question one. Question two is where the finding rests and it is falsifiable: earn-the-trust.md:111 is literally \"Optional, when your sessions start running heavy: [supplementary]\" — optional after-close reading that IS deck-linked — so neither \"In the training index\" nor \"Optional reading after M6 closes\" states the boundary. spot-gaps-build-the-loop.md:57 records the identical fact for `how-the-best-do-ci-cd.md`, and M6 body-links no supplementary at all (verified by grep). The drafted note is scoped to \"this clause,\" states present-tense facts only, and carries no transition verb of its own."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/workflow-composition-lineages.md",
  "instance": "ae101--supplementary--workflow-composition-lineages.writing.json",
  "rule": "student_facing 28",
  "claim": "The closing paragraph's sentences are bare imperatives with the student as implicit object — §28 failure mode (b).",
  "disposition": "EXEMPT_BODY",
  "confidence": "medium",
  "why": "Real on the page's closing sentence, and the fix lands directly: the page is under supplementary/, which the maintainer standing-exempted from the approval gate on 2026-08-29. 'Keep an eye out for X' is §28's failure mode (b) almost word-for-word against the rule's own listed examples ('watch for X', 'look out for Y'), and it sits in the highest-visibility slot in the document. The recast is a reorder, not an addition: same two facts, same length, student restored as grammatical subject of a declarative claim, which is §28's stated fix shape ('You find them'). SCOPE NOTE — I am deliberately fixing only the closer and leaving 'Watch what they ship in their own venues': §28 binds 'the closing sentence of any forward-looking aside', and that sentence is mid-paragraph. Widening to every imperative in the paragraph ('Track two or three practitioners', 'read each practitioner') would rewrite a reading-list page's natural register on a rule that did not ask for it, against the standing taste note that additions are near-auto-rejected. If the orchestrator wants that seam closed for good it wants a maintainer line, not a bigger diff.",
  "evidence": "Read check_student_facing §28 in full — four failure modes, the closing-sentence scope trigger, and the fix shape. Flagged paragraph at curriculum/trainings/agentic-engineering-101/supplementary/workflow-composition-lineages.md:145; its final sentence is the old_string below. Confirmed the file sits under trainings/agentic-engineering-101/supplementary/, matching CLAUDE.md's standing §26 exemption for supplementary and reference body text. Read the full maintainer block (L147-165): no accept-note covers the closer, so nothing shields it. grep -c on the old_string = 1.",
  "old_string": "Keep an eye out for named frameworks this walk does not yet cover, because the curation is partial by design.",
  "new_string": "The curation is partial by design, so you will find named frameworks this walk does not yet cover.",
  "surface": "exempt-page"
 },
 {
  "target_file": "curriculum/lectures/skills-from-the-frontier.md",
  "instance": "ae101--lecture--skills-from-the-frontier.pedagogy.json",
  "rule": "pedagogy 64",
  "claim": "The maintainer block's `**Lecture meta:**` line duplicates the machine-read runtime and carries a dead exercise ordinal (\"Exercises 1-3 of Module 3\").",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Rule 64's diagnostic is literal: \"stale fact in sweep -> defect is the duplication that let it rot... replace copies with pointers. A block that only points has nothing to rot.\" Both counts check out. (1) `scripts/calculate-time.js` exists and sources M3's lecture budget from `skills-from-the-frontier.md:54` (the `- **Time:** 12 minutes` line) — its own TIME_LINE_RE only matches `**Time:**`, so L52's italic `10-12 minutes` has no reader and can only diverge. (2) The ordinal is dead. Fix is a mechanical pointer swap inside a maintainer block: no student reads it, and the block's own L48 already records that Ex1/Ex2/Ex3 labels were dropped to keep the lecture re-placeable, so replacing the ordinal with a pointer is the call already made. Rule B (the training-architecture.md link) is NOT owed here and I am not filing it: the block references no delivery/working-dir/session mechanic — its only install-path mention is `~/.claude/skills/test-strategy/`, which the body states and `cc-skills-docs` stamps.",
  "evidence": "`node scripts/calculate-time.js earn-the-trust` -> the Earn-the-trust table sources `Lecture: Skills from the frontier` = 12 min from `curriculum/lectures/skills-from-the-frontier.md:54`; no row reads L52. `sed -n '29,45p' curriculum/trainings/agentic-engineering-101/earn-the-trust.md` -> `## Start here` runs FOUR exercises in order: open-the-side-quest, [this lecture], map-the-access-surface, threat-model-with-stride, author-test-strategy-skill. The lecture therefore primes the module's 2nd-4th exercises, not its 1st-3rd. `grep -Fc` on the old_string = 1. `grep -rn 'Lecture meta' curriculum/lectures/*.md` -> 18 files use the label; several carry a duration, but this is the only one that carries a duration AND a separate `- **Time:**` line calculate-time.js reads.",
  "old_string": "**Lecture meta:** *10–12 minutes. Lecture primes Exercises 1–3 of Module 3 (Earn the trust).*",
  "new_string": "**Lecture meta:** *Primes the security exercises that follow it in Module 3 (Earn the trust) — order in that module's `## Start here`. Runtime lives on the Meta Time line below, which `scripts/calculate-time.js` reads.*",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/lectures/skills-from-the-frontier.md",
  "instance": "ae101--lecture--skills-from-the-frontier.slides.json",
  "rule": "slides 15",
  "claim": "Chunk 2 (\"Borrowed judgement, or your own\") and chunk 4 (\"Two curated, one authored\") are bare noun-phrase labels where lecture headings owe claims.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The rule body carves out exactly this shape twice over, and the judge's own evidence concedes both carve-outs apply. §15 scopes the claim requirement: \"**The claim requirement binds law and consolidation slides** — where the header IS the teaching and a label would waste the beat.\" Neither of these is a law or consolidation slide. It then names the exemption verbatim: \"NOT violations: ... catalogue entries naming their subject (*Ralph*, *The numbers*, *Verification, named*)\" — both flagged headers enumerate their slide's subject. It quotes Antti (2026-08-15, on `what-packaging-is.md`) \"not every header owes a claim,\" and records its own re-scoping after it \"fired non-blocking on ten lectures it had never been audited against; a rule that only ever produces notes is a rule mis-scoped, not a corpus mis-written.\" This finding is the eleventh such note. Refuting it with an accept-note is the durable disposition; the alternative is re-filing it every cycle.",
  "evidence": "`node curriculum/evals/scripts/rule.js slides 15` read in full. Three independent carve-outs in the body cover the finding (law/consolidation scoping clause; catalogue-entry NOT-violation list; the 2026-08-19 generalisation). The judge's own row states \"neither header leaves a reader unable to follow the slide — the failure mode is register/mood flatness, not comprehension, which is this rule's own stated test for non-blocking,\" i.e. it filed a note the rule's scoping clause tells it not to file. Verified both headings against the current body: L8 `## Borrowed judgement, or your own` (slide enumerates two sources of judgement) and L19 `## Two curated, one authored` (slide enumerates the proportion) — both are subject-naming enumerations, not teaching claims.",
  "anchor": "**STRIDE at L7 is assumed-known (Antti 2026-08-30):** the cold first use one slide before its Kohnfelder/Shostack earning stands under the 2026-08-18 assumed-known carve-out for this audience. `check_student_facing.md` §2 judges should not re-flag it.",
  "accept_note": "**Two label headings stand as catalogue entries (2026-09-05):** *Borrowed judgement, or your own* and *Two curated, one authored* name their slide's subject rather than assert a claim. `check_slides.md` §15 binds the claim requirement to law and consolidation slides and exempts catalogue entries by name; neither header leaves a reader unable to follow its slide, which is §15's own non-blocking test. §15 findings on these two headers are overruled — do not re-file.",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The refutation's two real legs hold verbatim in §15: \"**The claim requirement binds law and consolidation slides**\" and \"NOT violations: ... catalogue entries naming their subject (*Ralph*, *The numbers*, *Verification, named*)\", plus §15's own 2026-08-19 walk-back. On chunk 4 the maintainer block records Antti's 2026-07-09 emphasis pass keeping **Don't make general what you don't practice yourself** as a bullet — which is §15's own proposal-time remedy (\"fix the heading or drop the claim to being a bullet\"). But the note asserts \"neither header leaves a reader unable to follow its slide, **which is §15's own non-blocking test**\" — §15 contains no comprehension test; that sentence is the judge's harm-check re-attributed to the rule. Planting an invented threshold into a durable maintainer block rewrites §15 for every future judge who reads it.",
   "note_fix": "**Two label headings stand as catalogue entries (2026-09-05):** *Borrowed judgement, or your own* and *Two curated, one authored* name their slide's subject rather than assert a claim. `check_slides.md` §15 binds the claim requirement to law and consolidation slides and exempts catalogue entries by name; on *Two curated, one authored* the principle line stays a bullet by Antti's 2026-07-09 emphasis pass, which is §15's own proposal-time remedy. §15 findings on these two headers are overruled — do not re-file. This settles these two headers only; §15 applies normally to every other heading here, and nothing in it makes comprehension the threshold."
  }
 },
 {
  "target_file": "curriculum/lectures/skills-from-the-frontier.md",
  "instance": "ae101--lecture--skills-from-the-frontier.strategy.json",
  "rule": "strategy_tie_in 4",
  "claim": "The private strategy doc's M2 skills-table cell still promises an M3 reveal of Pocock's grill-me that commit 54577f39 removed from this lecture.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "THE STRINGS BELOW ARE FOR THE PRIVATE STRATEGY DOC, NOT FOR THE LECTURE. Absolute path: `/Users/anttitevanlinna/Library/CloudStorage/GoogleDrive-tevanlin@gmail.com/My Drive/bosser/strategy/content-strategy-agentic-engineering-101.md`, line 540. The finding is real and the fix is one table cell, so an accept-note arguing it away would cost more than the fix. §4 itself only tests front-running, which the judge confirmed absent; what remains is doc rot, and §8 (2026-09-03) rules that the doc's inventory rots while its theory holds, and that a file path plus a reveal promise is inventory the doc must not restate. The curriculum side needs no change — the credit is paid once, student-facing, at `push-back-on-the-plan.md`. No student reads the doc, so the fix lands directly. Correcting the doc drains this row on re-judge.",
  "evidence": "Doc L540 verbatim: \"| M2 | None — the walk-down-branches move is inlined as a plain prompt, not invoked as a skill. M3 owns first-Skill-use; M2 pre-installing a skill would front-run the teaching moment. Pocock's `grill-me` skill remains forked at `curriculum/skills/external/pocock-skills/grill-me/` for M3 to reveal as authoring material. | none |\". `grep -Fc` of old_string in the doc = 1. Verified the lecture stages no reveal: full read of skills-from-the-frontier.md returns zero hits for Pocock/grill-me. Verified the credit lands student-facing: `curriculum/exercises/push-back-on-the-plan.md:83` — \"*Credit: Matt Pocock for the original [`grill-me`](https://github.com/mattpocock/skills/blob/62f43a1/skills/productivity/grill-me/SKILL.md) skill...*\", pinned commit. Fork still present at `curriculum/skills/external/pocock-skills/grill-me/`. SECOND SPAN, same stale claim, doc L306 (§8: \"Grep the doc for the claim's twin first\") — \"the reveal — *\\\"that second-pass read was a skill all along; here's how you author one\\\"* — is M3's authoring material.\" That clause needs the same correction in the same pass or the doc will say both.",
  "old_string": "Pocock's `grill-me` skill remains forked at `curriculum/skills/external/pocock-skills/grill-me/` for M3 to reveal as authoring material.",
  "new_string": "The pattern's credit is paid once, student-facing, in the M2 exercise that runs the walk-down; M3 authors a skill of the student's own and stages no reveal.",
  "surface": "anchor-not-found"
 },
 {
  "target_file": "curriculum/lectures/skills-from-the-frontier.md",
  "instance": "ae101--lecture--skills-from-the-frontier.technical.json",
  "rule": "research_claims 2",
  "claim": "The klaassen-compound-engineering stamp (L89) is ~3 months past its own due:2026-06-11 and has not been re-verified.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "§2's requirement for an out-of-window source is not a fresher date, it is \"Older = historical context only, explicitly dated.\" The stamp already does exactly that, on its own line: `result:CAVEAT` plus \"Dec 2025 is outside the 6-mo window, so cite as the framework's origin rather than fresh evidence\" plus \"Never attach a step number to this framework without re-counting first.\" §11a then makes the lapse structurally unfixable: `due` is publication-anchored (pub 2025-12-11 + 6mo), and \"Never `checked`+6mo\" — so no re-verification can ever move this due off 2026-06-11. A rule whose remedy cannot change the flagged field will re-file forever; that is what an accept-note is for. No body sentence leans on it either: the only compounding claim is `skills-compound-across-teammates` tagged `vision`/`none-owed`, and the Frameworks row is a provenance credit. The judge's own row concedes every one of these points. NOTE: the mechanical defects that ARE fixable in this stamp (a dead cross-reference and a §3 changelog clause) are handled by the writing-§22 row on this same file — one edit, not two.",
  "evidence": "`node curriculum/evals/scripts/rule.js research_claims 2` and `11` read in full; §11a states \"DUE = **publication**+6mo (evidence)\" and \"**Never `checked`+6mo**\". Read `curriculum/source-freshness-format.md` §§ Result vocabulary / Audit semantics: CAVEAT is INFO severity, and the past-due WARN fires on `due < target` regardless of any re-check. Body use verified: `grep -n` the claims ledger — L70 `skills-compound-across-teammates · vision · ... ← none-owed`; the only other consumer is the Frameworks row L94. The Theory-construct `due:none` variant is NOT available: format doc L116 condition (a) bars it where the citation carries \"a rate, count, or capability the post carries,\" and this stamp's anchor is the four-step naming. SEPARATE FLAG, not part of this row: a 2026-09-05 WebFetch of the canonical URL reports the article's four sections as Plan / Work / **Assess** / Compound, which would contradict this stamp's \"four-step naming verbatim and in printed order\" and the sibling's \"Loop confirmed verbatim: Plan → Work → Review → Compound → Repeat\". That is a possible `result:CORRECT` needing a human re-read, not a small-model fetch — I did not bake it into any edit.",
  "anchor": "**Lean pass (2026-08-25, Antti-directed M3/M6 shorten, free hands):**",
  "accept_note": "**`klaassen-compound-engineering`'s lapsed `due` is the designed state (2026-09-05).** The stamp is `result:CAVEAT` and says on its own line to cite the piece as the framework's origin rather than fresh evidence, which is what `check_research_claims.md` §2 asks of an out-of-window source. `due` is publication-anchored per §11a, so no re-check can move it, and no body sentence leans on the source — `skills-compound-across-teammates` is `vision`/`none-owed` and the Frameworks row is a provenance credit. §2 freshness findings on this stamp are overruled — do not re-file; a `checked:` refresh rides the next OODA pass.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The refutation's central claim is empirically false. It argues \"§11a then makes the lapse structurally unfixable... so no re-verification can ever move this due off 2026-06-11. A rule whose remedy cannot change the flagged field will re-file forever; that is what an accept-note is for.\" But `curriculum/lectures/where-the-rule-could-live.md:72` carries the SAME source-id on the same article: \"klaassen-compound-engineering `[checked:2026-08-29 result:OK due:2026-11-29]` ... **Re-verified 2026-08-29 — the correction has landed and holds.** ... **Stamp kept in step with `skills-from-the-frontier.md` and `the-machine-you-just-met.md`.**\" A re-verification did move the due — anchored on the 2026-05-29 expansion's publication (+6mo = 2026-11-29) — one week ago, in a stamp that explicitly claims to be in step with this very file, which it is not. That is §1's back-sweep failure verbatim (\"a correction confined to the catching cycle's file leaves the chain self-contradictory\"), and §11a's inherit clause (\"a research file already stamping it owns both judgements the citing file is about to redo... INHERIT its `due`\") points the same way. Second failed claim: \"matches the same-source stamp in `trainings/agentic-engineering-101/spot-gaps-build-the-loop.md`\" is false twice over — that path does not exist (the file is `curriculum/exercises/spot-gaps-build-the-loop.md`) and its stamp is `due:none`, not `due:2026-06-11`. Third: \"no body sentence leans on it... the Frameworks row is a provenance credit\" — backing-format.md is explicit that \"**Evidence goes in `←`**,\" and the Frameworks row `Compound engineering / team-kit accretion · [borrow:practitioner-coined] · law:the-compound-ladder · ← klaassen-compound-engineering, curran-2x` is the borrow ledger's evidence slot. Finally, the triage's own SEPARATE FLAG (a 2026-09-05 fetch returning Plan/Work/**Assess**/Compound against this stamp's \"four-step naming verbatim and in printed order\") is the re-verification debt materialising — it vindicates the judge rather than refuting it. Do not write an accept-note that tells future judges to stop looking at a stamp whose content may already have drifted."
  }
 },
 {
  "target_file": "curriculum/lectures/skills-from-the-frontier.md",
  "instance": "ae101--lecture--skills-from-the-frontier.writing.json",
  "rule": "writing 3",
  "claim": "The \"STALE FIX:\" clause in the Slides-only pass note narrates a correction (was-X, now-Y) instead of stating present fact.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "§3 bans this shape by name — \"`X was changed to Y`\", \"`now X rather than Y`\" — and the marker clause makes it grammatical, not lexical: \"any clause whose verb states a transition ... = biography, whatever its vocabulary. ... State the destination, not the journey.\" The clause has a real do-not-reintroduce value worth keeping (the sponsor-named team-kit path is a live pull, not a phantom: the corpus's standing convention is that skill homes are the repo's own, never sponsor-stated), so §3's licensed form applies — \"rewrite what remains as present-tense state + whatever forward rule the change implies (*\\\"do not reintroduce X\\\"* = current state).\" Maintainer block, no student reads it, fix lands directly.",
  "evidence": "Tense test on the clause: \"contradicted the personal-first contract ... ; now names `~/.claude/skills/test-strategy/`\" — two transition verbs (contradicted, now names). A reader who never saw the previous version loses nothing from the journey and everything from the constraint, which is exactly §3's split. The surrounding \"Slides-only pass (2026-07-02, unaudited)\" header and its per-passage CUT/CONVERTED/KEPT verdicts are the corpus's blessed dated-provenance shape (§3: \"84 of 101 AE101 files carry a dated provenance header ... it stays\") and I am not touching them. The `unaudited` label in that same header is NOT flagged here — unlike the-loop-half-filled's, this file's Quality line does not claim a later audit of this pass. `grep -Fc` of old_string = 1.",
  "old_string": "STALE FIX: \"The one you author lands at the team-kit path your sponsor named\" contradicted the personal-first contract (voice three + module maintainer watch-for); now names `~/.claude/skills/test-strategy/`",
  "new_string": "INSTALL PATH: the authored skill lands at `~/.claude/skills/test-strategy/`, personal-first per voice three and the module's watch-for — do not point it at a team-kit path",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/lectures/skills-from-the-frontier.md",
  "instance": "ae101--lecture--skills-from-the-frontier.writing.json",
  "rule": "writing 22",
  "claim": "klaassen-compound-engineering is claimed by two bodies whose stamps have diverged; the sibling's note promises the two are kept in step and they are not.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "§22 is real here and the fix is cheaper than an accept-note — but NOT the fix the judge proposed. Its fix_hint says copy `where-the-rule-could-live.md`'s `due:2026-11-29`; that due is checked+3mo, which §11a bans (\"Never `checked`+6mo\" — publication-anchored only), and §11a adds \"Diverge only toward the *tighter* date ... a citing file granting itself more runway than the research file did is asserting a freshness judgement it never made.\" This file's `due:2026-06-11` is the correct one; importing the sibling's would make it worse. §22's actual remedy is \"edit all or state why one differs,\" so the fix is to name the real sibling and state why the dues differ. The same clause also carries two verified mechanical defects — a dead cross-reference and a §3 transition clause — which this edit clears in one pass. All of it sits in a `<!-- backing -->` block.",
  "evidence": "`grep -rn 'klaassen-compound-engineering' curriculum/` -> exactly two source stamps corpus-wide: `skills-from-the-frontier.md:89` and `where-the-rule-could-live.md:72` (the workflow-composition-lineages hit is a same-named *claim* id backed by `every-ce-plugin`, a different source). DEAD LOCATOR CONFIRMED: this stamp's closing clause says it \"matches the same-source stamp in `trainings/agentic-engineering-101/spot-gaps-build-the-loop.md`\" — `grep -n 'klaassen|every.to' curriculum/trainings/agentic-engineering-101/spot-gaps-build-the-loop.md` returns only `my-ai-had-already-fixed-the-code-before-i-saw-it`, a different Klaassen article. No stamp for this source exists in that file. §3 DEFECT in the same clause: \"(the `the-map-filled-in.md` twin left with that file, 2026-09-02)\" — transition verb plus bare date, exactly §3's banned shape. `grep -Fc` of old_string = 1. The URL/checked/result/due fields are deliberately untouched: see the Assess-vs-Review flag on the research_claims row for this file.",
  "old_string": "Due = pub+6mo — evidence piece, not a capability page; matches the same-source stamp in `trainings/agentic-engineering-101/spot-gaps-build-the-loop.md` (the `the-map-filled-in.md` twin left with that file, 2026-09-02).",
  "new_string": "Due = pub+6mo — evidence piece, not a capability page. The other body claiming this source is `lectures/where-the-rule-could-live.md`; both carry the same CAVEAT, and the publication-anchored `due` here is the binding one (`check_research_claims.md` §11a bars a checked-anchored due). Edit one stamp's anchor and the other owes the same edit.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/exercises/close-the-ticket.md",
  "instance": "ae101--exercise--close-the-ticket.writing.json",
  "rule": "student_facing 9",
  "claim": "L48's \"Extract a `.md` file. Use that file as context in the next agent.\" drops the actor, so a student can read it as their own manual step.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "The rule fires. §9's shape (a) is \"*No actor named*\" and its diagnostic is \"who does this, and through what?\" — and this file's own convention (bare imperative = student, \"Ask Claude to X\" = agent) makes both elided verbs ambiguous. §9 also carries the specific warning that this file's register exemption at L43 caused a prior judge to stop checking actor+mechanism in the same passage, so the rule is watching this exact file. It is a CARD, not a direct edit, because the sentence is Antti's own verbatim working frame: the `maintainer-frame` source stamp records it as \"carried near-verbatim into the closing slide\" and logs exactly ONE licensed reshape (*LLM* -> *the agent*). Rewording his sentence a second time is a call only he can make. The proposed AFTER keeps his three-beat rhythm and moves only the actor.",
  "evidence": "Read L45-48 against the rule body in full (`rule.js student_facing 9`). L48 current: \"- Ask Claude to study the pipeline, the config, the undocumented code. Extract a `.md` file. Use that file as context in the next agent.\" `grep -Fc` = 1. Provenance verified in the backing block: claim `study-extract-carry-forward` (L112) quotes the sentence verbatim; source `maintainer-frame` (L115) `[checked:2026-08-03 result:ATTESTED due:none]` gives Antti's original — \"ask LLM to study it. Extract .md file. Use .md file as context in the next agent\" — and records the single licensed reshape. Checked the Stance block for an accept-note: L124 covers only the missing boundary caveat on *anything* (\"do not file the missing bullet as a defect\"), which is adjacent to but does not authorise the actor question. Not stretching it.",
  "anchor": "- Ask Claude to study the pipeline, the config, the undocumented code. Extract a `.md` file. Use that file as context in the next agent.",
  "before": "- Ask Claude to study the pipeline, the config, the undocumented code. **Extract a `.md` file. Use that file as context in the next agent.**",
  "after": "- Ask Claude to study the pipeline, the config, the undocumented code**, and extract what it finds into a `.md` file. Hand that file to the next agent as context.**",
  "risk": "The backing block quotes the current sentence verbatim as claim `study-extract-carry-forward` (L112), and `validate-backing.js` checks a quote against its own body — landing this without updating L112 in the same pass breaks the check. It also puts a second reshape on a record whose stamp deliberately logs that exactly one has been made, so the `maintainer-frame` entry (L115) owes a line. And it is Antti's own sentence: if the three-beat cadence is the thing he wants kept, the ambiguity is the price he already paid once."
 },
 {
  "target_file": "curriculum/exercises/close-the-ticket.md",
  "instance": "ae101--exercise--close-the-ticket.writing.json",
  "rule": "student_facing 17",
  "claim": "\"## Anything can be reverse-engineered\" is a declarative header over a section whose body tells the student to act, so it owes a command verb.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "`check_slides.md` §15 names this heading, in this file, by name, as a deliberate specimen: \"**Measured 2026-08-15, 108 body headings across the 15 AE101 exercise files. Two declaratives:** `close-the-ticket.md` *\\\"Anything can be reverse-engineered\\\"* and `fix-tests-first.md` *\\\"There is always a next plausible answer\\\"* — both at the moment their exercise generalises past its own task. ... one is an accident, two at the same structural moment is a convention emerging.\" §15's opening states the whole point of the imperative/claim split is \"what lets a DECLARATIVE heading inside an exercise land as a declaration — the beat where the file stops instructing and names a law that outlives the task.\" Applying §17's verb test to this heading destroys the beat §15 exists to protect. This is a compendium collision, not a defect, and it will re-fire on every writing pass until an accept-note is in the file.",
  "evidence": "`node curriculum/evals/scripts/rule.js slides 15` — the measured-corpus paragraph names `close-the-ticket.md` and quotes this exact heading. `node curriculum/evals/scripts/rule.js student_facing 17` — its lecture/reference carve-out does not cover exercises, so §17 read alone does fire; the resolution comes from §15's named specimen, not from §17's own text. Verified the heading is unchanged in the current body at L45, `grep -Fc '## Anything can be reverse-engineered'` = 1, and that the section beneath it does carry an instruction (L48), i.e. the judge's factual read is correct and only its conclusion is wrong. No existing note in the maintainer block covers it — L62 covers \"connector\" vocabulary only.",
  "anchor": "**\"Connector\" is assumed-known vocabulary (Antti 2026-08-18):** engineer audience knows MCP/connectors; the term owes no one-breath earning at its line-35 use. `check_writing §8` / `check_student_facing §2` earned-vocabulary findings on it are overruled — do not re-file.",
  "accept_note": "**`## Anything can be reverse-engineered` is declarative on purpose (2026-09-05):** `check_slides.md` §15 names this heading by name as one of two declaratives across 108 AE101 exercise headings, both at the beat where the file stops instructing and names a law that outlives the task — the split §15 exists to protect. `check_student_facing.md` §17 verb-header findings on it are overruled — do not re-file.",
  "verification": {
   "ruling": "REFUTE_STANDS",
   "reason": "I checked the cited carve-out and it says what the triage says, by name and by file: check_slides.md §15 reads \"**Measured 2026-08-15, 108 body headings across the 15 AE101 exercise files. Two declaratives:** `close-the-ticket.md` *\\\"Anything can be reverse-engineered\\\"* and `fix-tests-first.md` *\\\"There is always a next plausible answer\\\"* — both at the moment their exercise generalises past its own task... one is an accident, two at the same structural moment is a convention emerging,\" and §15's premise is that the imperative/claim split is \"what lets a DECLARATIVE heading inside an exercise land as a declaration.\" §17 does fire on its own text (its only carve-out is canonical module-architecture names, which this is not), so this is a genuine two-compendium collision, and resolving one in a maintainer note is established practice in this corpus (same file-shape as this module set's §9-vs-§34 note). The judge itself grades the harm \"mild.\" The note is scoped to this one heading (\"§17 verb-header findings **on it**\"), so it suppresses only the collision it adjudicates."
  }
 },
 {
  "target_file": "curriculum/exercises/close-the-ticket.md",
  "instance": "ae101--exercise--close-the-ticket.writing.json",
  "rule": "student_facing 33",
  "claim": "L54's \"The next exercise sweeps this whole session.\" promises what a later beat will do — cartography.",
  "disposition": "CARD",
  "confidence": "high",
  "why": "The rule catches it on both of its own tests. Subject test: the sentence's subject is \"The next exercise\" — an exercise, which §33 lists under \"A module, the training, an exercise, a later beat, the arc -> arc-positioning, cut.\" Predicate-leak clause (2026-08-23): \"Any clause promising what a later beat will do is cartography however the sentence starts, and it is a cheque that beat must cash.\" Deletion test passes cleanly — the paragraph closes on \"The field rules stay in the scrollback.\" and nothing actionable for THIS exercise is lost. No boundary exemption applies: L54 sits under `## Two artefacts from one ticket`, not under `## Bridge`/`## Next`/`## Connections`. CARD rather than a direct edit because it deletes the exercise's final sentence, which changes the closing beat's shape — the claim, not a locator.",
  "evidence": "`rule.js student_facing 33` read in full; checked all five listed exempt sections against the file's headings — none matches. Verified the promise is at least true (so this is a §33 cut, not a correction): maintainer L60 \"Runs after `fix-tests-first` ... and before `compound-and-close`, so the field rules the agent surfaces here are session evidence the compound sweep picks up\"; Arc L94 \"Hands off to: `compound-and-close`\". Both are maintainer-side and neither authorises the body sentence. `grep -Fc 'The field rules stay in the scrollback. The next exercise sweeps this whole session.'` = 1.",
  "anchor": "The field rules stay in the scrollback. The next exercise sweeps this whole session.",
  "before": "The field rules stay in the scrollback. **The next exercise sweeps this whole session.**",
  "after": "The field rules stay in the scrollback.",
  "risk": "It removes the exercise's last sentence, so the file now ends on a shorter beat — if the forward pull into `compound-and-close` was doing handoff work in the room, that work moves to compound-and-close's own opening, which must then actually carry it. Low blast radius otherwise: no backing-block claim quotes this sentence."
 },
 {
  "target_file": "curriculum/lectures/what-packaging-is.md",
  "instance": "ae101--lecture--what-packaging-is.strategy.json",
  "rule": "strategy_tie_in 4",
  "claim": "The strategy doc's § In-class beats commits a cost/latency ROI callout at the M5 closer that is neither confirmed present nor confirmed cut in this file.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The doc commitment the finding rests on no longer exists, and the doc's own gap table records the beat as adjudicated — both halves, with a date. Nothing to fix on either side.",
  "evidence": "Grepped the live doc (`/Users/anttitevanlinna/Library/CloudStorage/GoogleDrive-tevanlin@gmail.com/My Drive/bosser/strategy/content-strategy-agentic-engineering-101.md`) for the quoted commitment: `grep -n -i 'cost/latency|Opus time|ROI|re-send moment|Carries: cost'` -> ZERO hits for \"re-send moment\", \"hours of Opus\", \"cost/latency #8\" and \"Carries: cost\". The sentence the judge quotes is gone. What the doc now says, gap table row 8 (line 865): \"| 8 | Cost/latency engineering | M5 (`what-packaging-is.md`, mid-lecture) | the re-read-cost line, *\\\"That cost is the point: it buys a session with no drift in it\\\"*; the planned closer ROI callout was cut 2026-09-03 — off-audience for an IC training, and against `the-context-ceiling.md`'s deliberate number-free stance |\". So the beat's home IS this file, mid-lecture, and it is present: body L59 carries that exact sentence, and the backing block stamps it as claim `re-read-cost-buys-a-clean-session` (L113). The ROI half is recorded cut. Both branches of the judge's open question are answered in the doc it was reading."
 },
 {
  "target_file": "curriculum/lectures/what-packaging-is.md",
  "instance": "ae101--lecture--what-packaging-is.technical.json",
  "rule": "research_claims 2",
  "claim": "L22's Huntley/Ralph sentence describes a practice with no in-body date, but the huntley-ralph stamp treats the whole citation as a durable account with due:none.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "The finding is squarely inside the rule. §2a requires \"**Three conditions, all required**\" and explicitly excludes \"a practice, trend or rate\" — \"Huntley's Ralph practice leans on exactly this primitive\" is a practice in the present tense, undated in body. §2b then names the exact remedy: \"**`due:none` scopes the CITATION, not the SOURCE — one URL doing two jobs needs two stamp lines.** ... A body sentence using the source for a **live position** — *this camp exists*, *this is the current stance*, *practitioners do this* — is a currency claim and owes its own stamp with a real `due` date, even when the same URL backs both.\" The current stamp even carries the tell §2b flags: an inline currency assertion (\"Still active per *everything is a ralph loop* (2026-01-17)\") smuggled inside a `due:none`. Fix is a second stamp line in a `<!-- backing -->` block — not student-facing, lands directly, and it is the cheaper of the two options the judge offered (the other is a body edit, which would be a card). The new due lands already-lapsed; that is the honest state and exactly what §2b exists to surface — this file's own `amp-neo` entry models the same discipline.",
  "evidence": "`rule.js research_claims 2` (incl. 2a/2b) plus `curriculum/source-freshness-format.md` § Durable-account variant read in full — L106-110 of the format doc give the two-jobs rule and the free diagnostic. Body L22 verified verbatim, present tense, no date, unlike the Ronacher sentence one bullet above at L21 (\"In Armin Ronacher's January 2026 MiniJinja port...\"). Stamp L126 verified; its currency evidence is *everything is a ralph loop*, 2026-01-17, so pub+6mo = 2026-07-17 per §11a's publication-anchoring. `grep -Fc` of old_string = 1. Confidence is medium only on the split-vs-body-date choice: `source-freshness.sh` will now WARN on the new line, which is the intended visibility, but a maintainer who prefers the body-date route should instead date L22 in-body and drop this second line.",
  "old_string": "Still active per *everything is a ralph loop* (2026-01-17), which cites the original rather than restating the pieces. `due:none` because this is a durable first-hand account: re-reading it next year will not change what he wrote. fallback: cite as origin of the loop-prompt-over-durable-state technique; drop any application claim.",
  "new_string": "Still active per *everything is a ralph loop* (2026-01-17), which cites the original rather than restating the pieces. `due:none` scopes this stamp to **what he wrote on 2025-07-14**: re-reading it next year will not change that. fallback: cite as origin of the loop-prompt-over-durable-state technique; drop any application claim.\n- huntley-ralph-currency `[checked:2026-08-01 result:OK due:2026-07-17]` https://ghuntley.com/ralph/ — [practitioner direct] The second job this URL does. The body's present-tense *\"Huntley's Ralph practice leans on exactly this primitive to bootstrap entire greenfield projects\"* is a live position, not a completed event, so it carries its own clock (`check_research_claims.md` §2b). Currency evidence is *everything is a ralph loop*, 2026-01-17; due = pub+6mo. Stance § would-move-it holds the falsifier: Huntley abandoning or restructuring Ralph leaves the count at zero and the slide owes a different sentence. fallback: date the practice clause in body and let the durable line above carry it alone.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/lectures/what-packaging-is.md",
  "instance": "ae101--lecture--what-packaging-is.writing.json",
  "rule": "writing 3",
  "claim": "The L71 keep-note quotes \"as more runs cross it\" as a waived instance and counts x5; the quote is absent from the body and the real count is 3.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "§3's keep-notes clause decides this outright: \"**A keep-note whose positional claim fails is VOID.** If the note names a referent the passage does not have ... a judge files the finding instead of honoring the shield — the note may be stale OR the passage may have lost the very line the note protects, and only the maintainer can tell which; a mis-describing note shields a real defect from every judge that trusts it (close-the-ticket, 3 of 4 judges independently).\" The quoted referent is gone and the count is wrong, so the shield is void. Maintainer block, fix lands directly. I am dropping the numeral rather than correcting it to x3: the count is precisely what rotted, `check_writing.md` §20 says \"Fix = drop the numeral\" where a count is not a header and not a handle reused at distance, and \"on the theory slides\" already scopes the waiver without it. The dated Antti attribution and the \"Do not flatten\" forward rule both stay — §3 explicitly protects those.",
  "evidence": "Body L49 reads \"a wide first fix that narrows as more experiments land\" — no \"run\" at all; corroborated by the backing block's own claim `first-fix-wide-by-design` (L121) quoting L49 verbatim. So \"as more runs cross it\" does not exist: it is a quote of a passage the file no longer has. Recounted noun-\"run\" across the theory slides (L30-51): \"your next run\" (L34), \"in a run\" and \"until the run\" (both L41) = 3, not 5. Both replacement quotes are verified live in the body. `grep -Fc` of the full old_string = 1. Deliberately not touching the sibling keep-note at L73 (`## Reference and plan` stays concept-named) — its positional claim holds.",
  "old_string": "**Noun-\"run\" on the theory slides (×5) — Antti-waived 2026-08-15.** The experiment sense (*\"until the run, there is nothing to document\"*, *\"as more runs cross it\"*), not session/task/run registry drift; the slides are about evidence produced by runs, and \"session\" would blur the claim. Do not flatten.",
  "new_string": "**Noun-\"run\" on the theory slides — Antti-waived 2026-08-15.** The experiment sense (*\"what it holds about your next run is a forecast\"*, *\"until the run, there is nothing to document\"*), not session/task/run registry drift; the slides are about evidence produced by runs, and \"session\" would blur the claim. Do not flatten.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/lectures/the-loop-half-filled.md",
  "instance": "ae101--lecture--the-loop-half-filled.writing.json",
  "rule": "writing 3",
  "claim": "Three maintainer notes carry open-TODO labels (\"unreviewed\" x2, \"unaudited\", \"Re-audit before ship\" x2) that the file's own STATUS and Quality lines have since closed.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "§3 states it flatly: \"TODO closed -> delete; don't annotate as 'fixed' or keep changelog in file\" and \"A file's own existing strike-annotated or 'Closed <date>' items are prior violations to delete on touch, not a local convention to imitate — this rule outranks the file's own precedent.\" The audit these four labels wait on has since happened twice over, four to twenty-six lines below them. Deleting a stale status label leaves the dated provenance headers and their standing constraints fully intact, which is what §3 protects (\"Keep: the dated attribution, the standing constraint it anchors, any *do-not-reintroduce* it licenses\"). Maintainer block, fix lands directly. FOUR SPANS, one per defect — the schema carries one pair, so the other three are quoted byte-exact in evidence and must be applied in the same pass.",
  "evidence": "Contradiction verified in-file: L66 STATUS \"...the control pair (two slides) ... Audited 2026-07-26 — see the Quality line below; Family B PASS 2026-07-03\"; L86 \"**Quality:** compendium-audited 2026-09-02 (writing@c0c37913 story@85515fa0 ...)\"; L87 all seven classes PASS. Every anchor `grep -Fc`-verified = 1. THE OTHER THREE PAIRS, byte-exact: (A) old `**Map figure woven in (2026-07-10, Antti-directed \"complete the text→pictures replacement\", unreviewed):**` -> new `**Map figure woven in (2026-07-10, Antti-directed \"complete the text→pictures replacement\"):**`. (B) old `**Slide-growth pass (2026-07-02, unaudited):** skeleton grown in place` -> new `**Slide-growth pass (2026-07-02):** skeleton grown in place`. (C) the two `Re-audit before ship.` sentences are not individually unique (count 2), so anchor each with its preceding clause: old `you didn't watch?\"). Re-audit before ship.` -> new `you didn't watch?\").` (count 1), and old ``note in `the-far-half.md`. Re-audit before ship.`` -> new ``note in `the-far-half.md`.`` (count 1). Note the judge's own row confirms the prior pass's finding here (an invented `result:CORRECTED` tag) is fixed: `grep -c 'CORRECTED'` = 0.",
  "old_string": "**Control pair added (2026-07-10, Antti-directed cognitive-load arc, unreviewed):** two recognition chunks",
  "new_string": "**Control pair added (2026-07-10, Antti-directed cognitive-load arc):** two recognition chunks",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/lectures/the-loop-half-filled.md",
  "instance": "ae101--lecture--the-loop-half-filled.writing.json",
  "rule": "writing 20",
  "claim": "L35's \"It came from three moves you already made.\" states a count the very next bullet enumerates.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "§20 fires cleanly and its carve-outs do not reach: this is not a `##` header numeral (\"A judge that flags a header numeral is reading the rule too wide\" — not applicable, L35 is body prose), and it is not the memory-handle carve-out, whose test is \"does the numeral appear where the items are NOT visible?\" — L35 and L36 sit on the same `##` slide, so the items are visible without scrolling. §20's own worked example is exactly this shape (\"the three failure modes below\"), and it names the failure precisely: a fourth move added to L36 falsifies L35 silently. §20 states \"Fix = drop the numeral\" and warns against reaching for a replacement quantifier. I am carding rather than editing because the maintainer block explicitly shields this bullet pair — L60: \"Bullets 1–2 of the first chunk name what the student just did: earned exception (naming what they have IS the lesson), don't flatten at re-audit.\" That note authorises the naming move, not the numeral, so it does not refute the finding — but it does mean an editor is standing on a passage Antti has fenced, and the setup-then-enumerate rhythm is a voice call. If the orchestrator reads §20's \"Fix = drop the numeral\" as the call already made, this converts to a direct edit with these exact strings.",
  "evidence": "`rule.js writing 20` read in full, both carve-outs checked against the passage. L35-36 verified: \"It came from three moves you already made.\" is immediately followed by one bullet naming exactly three — the delta note, the one-threat pick, the skill critique. Slides layout chunks at `##`, so both lines render on the same slide (`## Reading was never the control`), which settles the visibility test. `grep -Fc` of the full L35 bullet = 1. Confirmed the judge's PASS call on the neighbouring \"One loop, three sizes\" (L7) is right and I am not extending the finding to it: it is the law's bolded handle-name, listed as deliberately kept in the Emphasis-pass note at L64, and reused as a handle. Checked L60's accept-note against the numeral: it authorises \"naming what they have,\" which is the content of the bullets, not the tally in the sentence above them — not stretching it to cover this.",
  "anchor": "- The outputs in this module were the biggest yet, and control never came from reading them whole. It came from three moves you already made.",
  "before": "- The outputs in this module were the biggest yet, and control never came from reading them whole. It came from **three** moves you already made.",
  "after": "- The outputs in this module were the biggest yet, and control never came from reading them whole. It came from moves you already made.",
  "risk": "The numeral primes the reader for exactly three before the next bullet delivers them; dropping it loosens a deliberate setup-payoff beat in a consolidation slide whose whole job is recognition. It also touches a bullet pair the maintainer block fences as an \"earned exception ... don't flatten at re-audit\" — if Antti reads that fence as covering the sentence's shape and not only its content, this is a regression, not a fix."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/run-the-first-experiment.md",
  "instance": "ae101--module--run-the-first-experiment.strategy.json",
  "rule": "strategy_tie_in 8",
  "claim": "The strategy doc's four-read arc still assigns Willison's Designing agentic loops before M4 and da Costa before M6, contradicting this module's Prework and maintainer block.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "THE STRINGS BELOW ARE FOR THE PRIVATE STRATEGY DOC, NOT FOR THE MODULE FILE. Absolute path: `/Users/anttitevanlinna/Library/CloudStorage/GoogleDrive-tevanlin@gmail.com/My Drive/bosser/strategy/content-strategy-agentic-engineering-101.md`, line 298. The judge's read is correct and its own conclusion is that the module file's side of §8 is already satisfied — the fix is one clause in the doc, which no student reads, so it lands directly rather than as a card. §8's fix procedure is followed exactly: the owning files' maintainer blocks already carry the correct claim, so nothing needs relocating; \"**§8 fix = correct the span, never empty it**\"; and the twin-grep was run. I extended the fix one sentence past the assignment clause because §8 warns that \"A dangling qualifier with nothing after it ... is the tell that a deletion took doc-owned reasoning with it\" — the following sentence's subject is \"The M4 Willison read,\" which the correction falsifies, while its reasoning (an optional essay cannot pre-empt an experience) is doc-owned theory worth keeping. Correcting the doc drains this row on re-judge.",
  "evidence": "Doc L298 verbatim clause quoted in old_string; `grep -Fc` in the doc = 1. Curriculum reality verified on both halves. Willison: `curriculum/trainings/agentic-engineering-101/plan-mode-done-right.md:70` assigns *Designing agentic loops* inside `## Pre-reads before Module 3`, and its own stamp at L125 states \"**This file owns the check and assigns the essay in the six-module arc only** ... Not assigned at M4 in any cut\"; the target module's maintainer block L95 agrees — \"it does not belong at M4 in any cut ... Do not reintroduce a Willison branch here.\" da Costa: `earn-the-trust.md:99-104` `## Pre-read before Module 4` assigns *Backpressure Is All You Need*; `plan-mode-done-right.md:75-80` carries the same essay in its `no-module:earn-the-trust` `## Pre-reads before Module 4` block; the target module's Prework L14 echoes it unconditionally. Grepped the doc for an M6 da Costa assignment — none exists outside this clause. I did NOT re-verify the clause's M1/M2 halves (Willison *Vibe engineering*, Horthy WSFF); they are left untouched.",
  "old_string": "Willison *Designing agentic loops* before M4, da Costa's backpressure essay before M6 (flag-gated to sequences that run M6). Pitch shape per `check_writing §13`: reader's gain + one pickup clause, no thesis summary. **The M4 Willison read does not violate M5's three-pattern reveal:**",
  "new_string": "Willison *Designing agentic loops* before M3, da Costa's backpressure essay before M4 — the owning module files carry the per-cut assignment. Pitch shape per `check_writing §13`: reader's gain + one pickup clause, no thesis summary. **The Willison read does not violate M5's three-pattern reveal:**",
  "surface": "anchor-not-found"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/run-the-first-experiment.md",
  "instance": "ae101--module--run-the-first-experiment.technical.json",
  "rule": "research_claims 2",
  "claim": "L182's Klaassen Definitive Guide stamp carries due:2026-08-09, now lapsed, and has not been re-verified since 2026-07-02.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "The finding is right on the half that can be fixed and I fixed it by actually doing the check: the stamp's `checked:` was two months stale, so I re-opened the source live on 2026-09-05 and confirmed byline, publication date, page liveness and the anchor thesis verbatim. The `due` field stays at 2026-08-09 and must: §11a specifies \"DUE = **publication**+6mo (evidence)\" and \"**Never `checked`+6mo** — re-opening a March post in August confirms it still says what it said; it does not make the world it describes younger.\" pub is 2026-02-09, so the lapse is structural, shared by all three sibling stamps on this URL, and cannot be re-stamped away. Making that explicit on the line stops the next judge from filing the same unfixable item. Source-verification block, not student-facing, lands directly.",
  "evidence": "WebFetch of https://every.to/source-code/compound-engineering-the-definitive-guide on 2026-09-05 returned: title \"Compound Engineering: The Definitive Guide\", byline Kieran Klaassen, published February 9 2026 11:00 AM EST, not paywalled or truncated, and the anchor thesis verbatim — \"each unit of engineering work should make subsequent units easier—not harder.\" That matches the stamp's claim-anchor and corroborates `exercises/compound-and-close.md:83`, which quotes the same sentence from a 2026-08-23 check. Corpus consistency check on the same URL: `how-the-best-do-ci-cd.md:136` `[checked:2026-09-03 ... due:2026-08-09]`, `compound-and-close.md:83` `[checked:2026-08-23 ... due:2026-08-09]`, `getting-going.md:112` `[checked:2026-08-08 ... due:none]` (delegated) — all four agree on the publication-anchored due, so this file was the only one carrying a stale `checked:`. Per §11a I grepped `continuous-research/` for the URL before writing: it appears in `synthesis/practitioner-convergent-stance.md` and two platform-watch runs, none carrying a competing `due`, so nothing to inherit. `grep -Fc` of old_string = 1.",
  "old_string": "- `[checked:2026-07-02 result:OK due:2026-08-09]` https://every.to/source-code/compound-engineering-the-definitive-guide — [practitioner direct, vendor venue] Klaassen Definitive Guide (pub 2026-02-09; due = pub+6mo); core \"each unit makes the next easier\" thesis verified.",
  "new_string": "- `[checked:2026-09-05 result:OK due:2026-08-09]` https://every.to/source-code/compound-engineering-the-definitive-guide — [practitioner direct, vendor venue] Klaassen Definitive Guide (pub 2026-02-09; due = pub+6mo, so the lapse is structural — a publication-anchored due does not move on re-reading, `check_research_claims.md` §11a). Re-opened 2026-09-05: page live and un-paywalled, byline and date unchanged, anchor thesis verbatim — *\"each unit of engineering work should make subsequent units easier—not harder.\"*",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/lectures/composing-the-workflow.md",
  "instance": "ae101--lecture--composing-the-workflow.strategy.json",
  "rule": "strategy_tie_in 8",
  "claim": "The private strategy doc's M6-in-detail section restates three things this lecture asserts, and all three have rotted against dated maintainer decisions (seam/stop-condition sentence, survey-verb quick framing, three named scheduled-agent examples).",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The row already carries resolution {settled:\"fixed\", at:\"2026-09-05\"} recording the doc-side reconciliation, and §8's own split test puts the fix on the doc, never on the lecture — the judge says so itself (\"The file itself is not at fault\"). Nothing in this curriculum file is owed, and the doc-side edits are recorded as landed.",
  "evidence": "Read the row's `resolution` block in the instance (settled=fixed, at=2026-09-05, note names L260/L434 rewritten, L402 survey verb fixed in the doc's own voice, L462 trainer quote, L464 kept as doc-owned audience reasoning per §8). Re-read the lecture live: L48-51 still carries the checks-excluded workflow definition, L34 still carries \"the loop squashes the recurring things… cracks harder work\", maintainer L57 is consistent. Ran `node curriculum/evals/scripts/rule.js strategy_tie_in 8` — DOC OWNS arc bets/audience reasoning; DOC MUST NOT RESTATE section names/wiring/ownership claims. No lecture-side edit exists to make."
 },
 {
  "target_file": "curriculum/lectures/composing-the-workflow.md",
  "instance": "ae101--lecture--composing-the-workflow.technical.json",
  "rule": "research_claims 1a",
  "claim": "The pocock-skill-system figure's aria-label says \"triage and diagnosing-bugs feed implement\", but the source (mattpocock/skills ask-matt/SKILL.md @6654f6b) never says diagnosing-bugs feeds /implement.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The fix has landed in the figure registry file and in the lecture's maintainer note; the flagged aria-label text no longer exists. Row also carries resolution {settled:\"fixed\", at:\"2026-09-05\"}.",
  "evidence": "grep -n \"diagnosing-bugs\" curriculum/figures/pocock-skill-system.md — the aria-label now reads \"triage feeds implement, and diagnosing-bugs fixes in place, handing its post-mortem to improve-codebase-architecture when the bug has no seam to lock down\"; the flagged \"triage and diagnosing-bugs feed implement\" string returns zero hits. The SVG's on-ramp arrows confirm it: only the triage box carries `M 620.0 328 V288` into the flow; the diagnosing-bugs box (x=750,y=328) has no path into implement. composing-the-workflow.md maintainer L57 matches: \"diagnosing-bugs beside them (it fixes in place and hands its post-mortem to improve-codebase-architecture; no arrow into the flow)\"."
 },
 {
  "target_file": "curriculum/lectures/reading-the-return.md",
  "instance": "ae101--lecture--reading-the-return.pedagogy.json",
  "rule": "pedagogy 64",
  "claim": "Watch-fors bullet L49 says \"The pre-read names failure modes, not fixes\" while L46 three lines above says this file is an in-room M5 opener and must not be re-filed as prework; run-the-first-experiment.md's actual pre-read block names none of the three modes.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "§64's diagnostic is exactly this: \"stale fact in sweep → defect is the duplication that let it rot\". L49 holds a second, now-false copy of a wiring fact whose canonical home is L46 and `learn-from-the-test.md § Start here`. Both lines sit below `<!-- maintainer -->` (fence at L21), so no student reads either — trainer-facing, fix lands directly. The swap is mechanical: the bullet's claim (don't pre-empt the three-pattern), its voice and its ask are untouched; only the false self-label changes.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js pedagogy 64`. Read the file: `<!-- maintainer -->` at L21, so L46 and L49 are both inside the block. L46 verbatim: \"**Delivery mode:** In-room M5 opener, single wiring (`learn-from-the-test.md § Start here`; its Charge line is canonical — M4 has no Debrief, nothing made this prework, do not re-file it as prework)\". Confirmed the pre-read blocks live elsewhere: `grep -n \"Pre-read\"` returns `run-the-first-experiment.md:71 ## Pre-reads before Module 5` and `learn-from-the-test.md:110 ## Pre-reads before Module 6` — neither is this file. Anchor uniqueness: `grep -cF 'The pre-read names failure modes, not fixes.'` = 1. SEPARATE ISSUE SPOTTED, not filed as a todo: this file's Quality line L39 advertises \"technical PASS (2 todos see instances/ae101--lecture--reading-the-return.technical.json)\" but that instance has todos_count=0 and zero REVISE rows — a stale Quality-line locator worth a second maintainer swap.",
  "old_string": "- Don't pre-empt the three-pattern (reference / plan.md / verifier). The pre-read names failure modes, not fixes. Naming the fixes here collapses the M5 contrast.",
  "new_string": "- Don't pre-empt the three-pattern (reference / plan.md / verifier). This opener names failure modes, not fixes. Naming the fixes here collapses the M5 contrast.",
  "anchor": "- Don't pre-empt the three-pattern (reference / plan.md / verifier). The pre-read names failure modes, not fixes.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/lectures/test-and-learn.md",
  "instance": "ae101--lecture--test-and-learn.strategy.json",
  "rule": "strategy_tie_in 8",
  "claim": "The private doc's M4-in-detail still says \"two-run arc\" / \"runs twice\" / \"between the runs\" against this lecture's \"## The two-session arc\", and its \"Gap analysis is the framework named here\" predates the 2026-08-25 move of that naming into walk-and-send-off.md.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "Both halves fail against §8's own text. (a) §8's DOC OWNS list names \"arc bets\" explicitly; DOC MUST NOT RESTATE covers \"section names, wiring order, slide counts, phase lists, file inventories, ownership claims, verbatim headings\". \"M4 is the first experiment of a two-run arc\" quotes no heading and inventories nothing — it is the doc's own arc bet, and stale run-vs-session vocabulary on an authorial surface is not what §8 governs (§21b scopes the noun-run ban to student-facing body). (b) The doc section is `## M4 in detail — accumulate + run the first experiment`, so \"named here\" resolves to M4, not to this lecture — and gap analysis IS named in M4, at walk-and-send-off.md:40, M4's own exercise. The judge's own harm check agrees nothing student- or trainer-facing is wrong. Either way no edit exists in this lecture, so without an accept-note the next strategy judge re-files a doc-side item against a clean file.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js strategy_tie_in 8` and read the full rule body incl. the DOC OWNS / MUST NOT RESTATE split. Located the private doc at `~/Library/CloudStorage/GoogleDrive-.../bosser/strategy/content-strategy-agentic-engineering-101.md` and read `## M4 in detail` in full: L360 \"M4 is the first experiment of a two-run arc\", L364 \"Gap analysis is the framework named here\" — both still present, so the 2026-09-05 M1-M5 doc sweep did not touch this section. Confirmed the naming lands in M4: `grep -n -i \"gap analysis\" curriculum/exercises/walk-and-send-off.md` → L40 \"- **Gap analysis**: walk the system you have against the system the task needs.\", plus its backing rows at L109/L116. Anchor uniqueness in test-and-learn.md: the gap-analysis maintainer note at L26 is the only occurrence of \"Gap-analysis slide moved out\".",
  "accept_note": "**§8 accept-note (2026-09-05).** The strategy doc's `## M4 in detail` scopes to the module, not to this lecture: *gap analysis is named in M4* holds because `exercises/walk-and-send-off.md` Phase 2 names it, and the arc bet is the doc's to own per `check_strategy_tie_in.md` §8. A *two-run* / *two-session* wording lag is a doc-side edit on an authorial surface; nothing is owed in this file. Judges should not re-file §8 here.",
  "anchor": "**Gap-analysis slide moved out (2026-08-25, Antti-directed).**",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "Both halves of the refutation check out. §8's own text: \"DOC OWNS: spirit, mood, **arc bets**, cross-module sequencing claims...\" and DOC MUST NOT RESTATE lists only \"section names, wiring order, slide counts, phase lists, file inventories, ownership claims, verbatim headings\" — \"M4 is the first experiment of a two-run arc\" is an arc bet and quotes no heading. I read the private doc's `## M4 in detail — accumulate + run the first experiment` (L356-376) myself: it scopes to the module throughout, and L364's \"named here\" resolves to M4, where walk-and-send-off.md:40 does name it (\"- **Gap analysis**: walk the system you have against the system the task needs\"). The judge's vocabulary half also fails on a fact the triage did not cite: *two-run* is this file's OWN internal register — its maintainer block L30 says \"the two-run arc\" and its backing block L83 says \"the two-run contrast\" — and vocabulary.md:25 scopes the noun-run ban to \"student-facing body ... Enforcement: `check_student_facing.md §21b`.\" So there is no lag at all. That is what the note gets wrong: it concedes \"A *two-run* / *two-session* wording lag is a doc-side edit,\" inventing a defect and then telling judges not to look at it, and it closes with a blanket \"Judges should not re-file §8 here\" that silences §8 on this lecture entirely rather than on the two spans actually adjudicated.",
   "note_fix": "**§8 accept-note (2026-09-05) — two spans in the strategy doc's `## M4 in detail`.** That section scopes to the module, not to this lecture. *Gap analysis is the framework named here* resolves to M4 and holds: `exercises/walk-and-send-off.md` Phase 2 names it. *M4 is the first experiment of a two-run arc* is an arc bet, which `check_strategy_tie_in.md` §8 lists under DOC OWNS, and *two-run* is this repo's own internal register — the maintainer note above and the backing block both use it, and `vocabulary.md` scopes the noun-run reservation to student-facing body. Neither span is owed an edit here or in the doc. This note covers those two spans; §8 otherwise fires normally on this lecture."
  }
 },
 {
  "target_file": "curriculum/lectures/test-and-learn.md",
  "instance": "ae101--lecture--test-and-learn.technical.json",
  "rule": "research_claims 11",
  "claim": "Two backing-block source stamps compute `due` as `checked`+6mo, the exact defect §11a names: ronacher-agentic-coding `due:2027-01-02` and klaassen-definitive-guide `due:2027-01-30`.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "Both stamps have already been recomputed off the publication anchor. Neither flagged string exists in the file.",
  "evidence": "`grep -n \"due:2027-01-02\\|due:2027-01-30\" curriculum/lectures/test-and-learn.md` returns zero lines. Current L70 reads `ronacher-agentic-coding [checked:2026-07-02 result:CAVEAT due:none]` — the durable-account treatment §2a licenses and the judge's own preferred fix; current L71 reads `klaassen-definitive-guide [checked:2026-07-30 result:OK due:2026-08-09]`, publication-anchored on the Feb 9 2026 original, not checked+6mo. `grep -c` on each current stamp string = 1. Ran `rule.js research_claims 11` to confirm §11a's \"Never `checked`+6mo\" is what was cited; the file now complies."
 },
 {
  "target_file": "curriculum/lectures/test-and-learn.md",
  "instance": "ae101--lecture--test-and-learn.writing.json",
  "rule": "writing 20",
  "claim": "L13's \"Same task, two sessions\" states a count the two bullets directly below it already hold, and the header \"## The two-session arc\" already carries it as a handle.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "§20's harm mechanism does not reach this line. The rule's stated failure is a numeral in prose ABOVE its list, \"invisible to the author editing the list, who is looking at items and not at a sentence one paragraph up\" — but \"Same task, two sessions\" is the opening clause of list item one, directly under the author's eye when the items are edited. It also survived an Antti-directed redundancy pass that edited this exact line (commit 299b1b01 cut the trailing \"The agent works with exactly what you've given it\" from L13 and kept the rest), which is the strongest evidence available that the clause is wanted. And \"the two-session arc\" is a cross-file handle, not a tally — `the-far-half.md` L33 cites this section by that name. The judge itself grades the finding low-to-moderate confidence and concedes only the numeral is arguably redundant. An accept-note closes it at zero student-facing risk; a card here spends the maintainer's attention on a line he has already read and kept.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js writing 20` — full body read, including the memory-handle carve-out and the header carve-out (Antti 2026-08-22). Read the file: L11 `## The two-session arc`, L13 and L14 are the two bullets, and the flagged numeral sits inside L13, not above it. `git log -L 11,14:curriculum/lectures/test-and-learn.md` shows 299b1b01 (\"Five sentences in test-and-learn were restating the sentence beside them\", the 2026-08-29 density pass recorded at maintainer L39) touching L13 and leaving \"Same task, two sessions.\" intact. Cross-file handle confirmed: `grep -rn \"two-session\"` → curriculum/lectures/the-far-half.md:33 cites \"test-and-learn.md § The two-session arc\". Anchor uniqueness: `grep -c \"Same task, two sessions.\"` = 1.",
  "accept_note": "**§20 accept-note (2026-09-05).** *Same task, two sessions.* keeps its numeral. It sits inside the first list item rather than in prose above the list, so the silent drift §20 guards against has no purchase, and *the two-session arc* is a cross-file handle — `the-far-half.md` cites this section by that name. Judges should not re-file `check_writing.md` §20 on L11 or L13.",
  "anchor": "**Density pass (2026-08-29, Antti-directed):**",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The rule's own explicit default settles it against the triage: \"**In body prose the default is the other way: 'often counting is just NVA' (Antti, 2026-08-22).** Where a count is not a header and not a handle reused away from its list, drop it.\" L13's *Same task, two sessions.* is body prose, is not a header, and is not the handle reused away — the reused handle is `## The two-session arc`, which the header carve-out already protects and which `the-far-half.md:33` cites. The header protection does not license a second prose copy sitting directly on top of the two bullets. Three of the triage's supports fail on inspection. (1) The harm-mechanism argument is inverted: it claims sitting inside bullet one puts the numeral \"directly under the author's eye when the items are edited,\" but an author adding a third bullet is looking at bullet three, not at the opening clause of bullet one — being buried inside an item makes the drift *less* visible, not more, and §20's stated harm (\"buys nothing on the way in\") is position-independent anyway. (2) The git evidence proves less than claimed: `git log -L 11,16` confirms 299b1b01 cut the trailing sentence from L13 and left the clause standing, but the maintainer block's own record of that pass enumerates exactly what Antti kept — \"Antti kept 'You are testing, and you are learning' and 'You don't need to get it right first time' — do not re-propose either\" — and this clause is not on that list. Non-deletion is not a recorded keep, and §20 exists precisely for lines that survive review. (3) The judge's actual point is the carve-out's own test read correctly: the header already serves the handle, so the prose numeral is the redundant copy. Card the cut; do not note it away."
  }
 },
 {
  "target_file": "curriculum/lectures/test-and-learn.md",
  "instance": "ae101--lecture--test-and-learn.writing.json",
  "rule": "student_facing 21",
  "claim": "L18 says \"you can see Claude hallucinating file paths, contradicting its own earlier steps, or missing a requirement\" — drift symptoms that §21 places in the LLM or agent bucket, while L5 already sets \"the agent\" as this lecture's actor.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "The finding holds under §21's concrete contextual split: none of the three licences for *Claude* applies (no product/version grounding, no addressee grammar, no UI surface), and the 2026-08-30 active-stance override pushes an actively-working harness to *the agent* even inside addressee grammar. But the fix is not mechanical — §21 offers two different correct words here and the corpus splits them by hand: `reading-the-return.md` writes \"The LLM reasons from what fills the window now\" (L12) and \"The LLM produces the next likely word\" (L14) for mechanism, and \"The agent rehashes approaches it already ruled out\" (L13) for observed behaviour. L18 is watched behaviour, so *the agent* is the corpus-consistent pick, but that is a register call in a lecture's most vivid bullet, and the judge is right that \"Claude hallucinated X\" is natural engineer speech. Antti has not made this call on this line, so it is card-shaped rather than a sweep.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js student_facing 21` — read the full split plus the active-stance override and the audit signal (a Claude-count ≫ agent-count skew, which this file does NOT show: body is 1:1, \"The agent is the apparatus\" L5 vs \"Claude hallucinating\" L18). Read the file's maintainer block L22-46 and backing block: no accept-note touches this line. `git log -L` shows commit 55b65355 (\"Vocabulary standardized: session/task/run split\") touched L13-14 but not L18, so no prior Claude/agent sweep has adjudicated it. Corpus comparison read live in curriculum/lectures/reading-the-return.md L12-L14. Anchor uniqueness: `grep -cF 'you can see Claude hallucinating file paths' curriculum/lectures/test-and-learn.md` = 1, and the string appears in no other lecture, exercise or AE101 module file.",
  "before": "- If twenty minutes in you can see **Claude** hallucinating file paths, contradicting its own earlier steps, or missing a requirement that wasn't in the prompt, stop it. You have the data you came for.",
  "after": "- If twenty minutes in you can see **the agent** hallucinating file paths, contradicting its own earlier steps, or missing a requirement that wasn't in the prompt, stop it. You have the data you came for.",
  "risk": "If the natural-speech register is the one you want, the swap flattens the liveliest bullet in the deck, and a lone swap here leaves the register split against files that still say Claude elsewhere. Declining is cheap and durable — the decline becomes the accept-note that stops the next student_facing judge re-filing §21 on L18."
 },
 {
  "target_file": "curriculum/lectures/the-handoff-prompt.md",
  "instance": "ae101--lecture--the-handoff-prompt.behavior.json",
  "rule": "pedagogy 63",
  "claim": "Maintainer L24's \"owed until a cohort runs\" may be a mode-(b) stale figure now that curriculum/CLAUDE.md states the first cohort has run; the judge could not settle it from the repo and flagged it as a maintainer-held premise.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "§63's mode-(b) remedy is a re-measure, and the re-measure comes back clean: the bullet is current, not stale. The repo's canonical delivery record is the `- cohorts:` row (`curriculum/quality-format.md` L14 and L27), and no AE101 module file carries one — including M6's own `spot-gaps-build-the-loop.md`. `pre-cohort-todos.md` is written throughout against \"the first paying cohort\" with every watch item \"Accepted as-is for first cohort\". The curriculum/CLAUDE.md sentence the judge worried about licenses substantive work on all six modules by removing the M1-M3 done-done sequencing gate; it is not a delivery record for M6's close. So the live-test genuinely remains owed and no edit is due — but with no note on record the next behavior judge re-derives the same unanswerable question, which is exactly what §63 costs to answer.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js pedagogy 63` (two staleness modes; mode-b needs a re-measure of the bullet's own figures). Re-measured: `grep -rn \"^- cohorts:\" curriculum/` returns only `none yet` placeholders in body-views, a test fixture, `extract-the-task-shaping-rule.md:142` and `where-the-rule-could-live.md:29`; `grep -rn \"cohorts:\" curriculum/trainings/` returns nothing at all, so M6's module has no cohorts row. `grep -n \"Quality:\\|cohorts\" curriculum/trainings/agentic-engineering-101/spot-gaps-build-the-loop.md` → only the Quality line (compendium-audited 2026-09-03). Read pre-cohort-todos.md L1-40: L9 \"Open decisions before the first paying cohort\", and every `[watch]` bullet closes \"Accepted as-is for first cohort\" with a future-tense \"Fires if\". Read curriculum/CLAUDE.md § Done-done for the first-cohort sentence in context. Anchor uniqueness: `grep -cF 'is the real-cohort bar; owed until a cohort runs.'` = 1.",
  "accept_note": "**Cohort check (2026-09-05).** The live-test above is current, not a stale figure: no `- cohorts:` row exists on `spot-gaps-build-the-loop.md` or on any AE101 module file, and `pre-cohort-todos.md` runs entirely against the first paying cohort. `curriculum/CLAUDE.md`'s first-cohort sentence licenses substantive work on all six modules; it is not a delivery record for M6. Judges should not re-file `check_pedagogy.md` §63 on this bullet without a `- cohorts:` row to read.",
  "anchor": "- Real-cohort gold standard: the only handoff-generator live-test on record is sim-grade",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The re-measure does not come back clean. `curriculum/CLAUDE.md` L31 states as fact: \"*No cross-module sequencing: **the first cohort has run**, so all six AE101 modules are open for substantive work*\" — `git blame` puts that line on Antti, 2026-08-19, i.e. he restated it himself a month after the 2026-07-01 gate removal. The triage discounts it as \"not a delivery record\", but the sentence is not only a licence: the cohort having run is the stated *premise* of the licence, and you cannot take the licence while disclaiming its premise. Its counter-evidence is absence: `grep -rn '^- cohorts:' curriculum/` returns 7 hits, all `none yet`, and only two are real AE101 files (`extract-the-task-shaping-rule.md:142`, `where-the-rule-could-live.md:29`) — zero module files, zero populated rows, and `delivery-incidents.md` has no dated entries either. That is an unadopted logging convention, not a delivery record; its silence cannot outweigh a canon file's plain factual sentence. This is §63 mode (b) exactly — \"premise still plausible, its FIGURES expired ... a re-read cannot find it, because the bullet reads exactly as it did when true\" — and pre-cohort-todos.md's \"first **paying** cohort\" framing may or may not reconcile the two, which is a maintainer-held fact, not a repo-derivable one. The note would install a re-open trigger (\"without a `- cohorts:` row to read\") that the corpus has never once satisfied, so it silences the question permanently."
  }
 },
 {
  "target_file": "curriculum/lectures/the-handoff-prompt.md",
  "instance": "ae101--lecture--the-handoff-prompt.pedagogy.json",
  "rule": "pedagogy (judge-owned cross-check, no rule_index; hint cites check_student_facing.md §9 against check_pedagogy.md §47)",
  "claim": "L17's accept-note declines a body tell but names only the plan-instead-of-prompt tell, so it does not on its face cover L15's third failure mode, reviews-instead-of-checks.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The accept-note has been extended and now names both tells explicitly, which is the first branch of the judge's own fix hint. Nothing is owed.",
  "evidence": "Current L17 verbatim: \"**L9 stays as it is (Antti 2026-09-03).** Neither the plan-instead-of-prompt tell nor the reviews-instead-of-checks tell is added to the body: more text without value at a three-minute beat. The recovery stays trainer-side, in the note above. Judges should not re-file `check_pedagogy.md` §47, `check_student_facing.md` §5 or §9 on L9.\" `grep -c \"Neither the plan-instead-of-prompt tell nor the reviews-instead-of-checks tell\" curriculum/lectures/the-handoff-prompt.md` = 1. The quoted-in-evidence version naming only the plan tell no longer exists. The row carries no rule_index (judge_owned:true), so this is worked from the hint text plus the §47/§9 cross-check the hint names."
 },
 {
  "target_file": "curriculum/lectures/the-handoff-prompt.md",
  "instance": "ae101--lecture--the-handoff-prompt.story.json",
  "rule": "strategy_tie_in 1",
  "claim": "The beat reads 7/10 on the mood floor at both phase and close, driven by L9 diagnosing the dominant failure with no self-recovery move in front of the student.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The finding tightens §1 past what §1 says. The rule's mood floor is stated as \"8+/10 per beat, <7 = REVISE\" — 7 is not below 7, so a 7/10 read is not a REVISE state under the rule's own threshold. The judge concedes the point twice over (\"not the <7 that blocks\", fix_hint \"None to propose\", \"no action owed\"), and the only fix the gap would invite was declined by Antti on 2026-09-03 at L17. The file has nothing to change, so the row is a mood reading filed in a verdict slot. Without an accept-note the next story judge regenerates the same trace and files the same row.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js strategy_tie_in 1` and read the mood-floor clause verbatim: \"**Mood-floor (8+/10 per beat, <7 = REVISE) applies to TEACHING beats only.**\" Read the instance row: phase and close both 7/10, self-described as \"facilitator-premium tier, not the <7 that blocks\", and \"Recorded as a flagged, non-blocking gap per the rule's own tiering, not as an open authoring item\". Read the file: L9 is the diagnosing line, L17 is Antti's 2026-09-03 decline covering §47 / §5 / §9 on L9 but naming no strategy_tie_in rule — which is why the row can recur. Anchor uniqueness: `grep -cF 'Judges should not re-file `check_pedagogy.md` §47, `check_student_facing.md` §5 or §9 on L9.'` = 1.",
  "accept_note": "The mood cost of that decline is priced in: `check_strategy_tie_in.md` §1 reads this beat at 7/10 for carrying no self-recovery move in front of the student, which is facilitator-premium and above the <7 floor. Judges should not re-file §1 on L9 either.",
  "anchor": "Judges should not re-file `check_pedagogy.md` §47, `check_student_facing.md` §5 or §9 on L9.",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The refutation checks out: §1 reads \"**Mood-floor (8+/10 per beat, <7 = REVISE) applies to TEACHING beats only.**\" — 7 is not <7, the judge itself wrote \"facilitator-premium tier, not the <7 that blocks\" and fix_hint \"None to propose ... no action owed\", and L17 carries Antti's 2026-09-03 decline of the only fix the gap invites. But the drafted note's closing sentence — \"Judges should not re-file §1 on L9 either\" — switches off the whole of §1 at that line, permanently. §1 is not only the mood floor; it also covers whether the draft \"honours mood, or flattens it\" and how the beat \"sets up next piece\". As drafted it would also silence a future read that genuinely drops below 7 after a body change, which is the state the rule marks REVISE. Narrow it to the adjudicated read and its stated driver.",
   "note_fix": "`check_strategy_tie_in.md` §1 reads this beat at 7/10 for exactly the reason the decline above names — no self-recovery move in front of the student. That is below the 8+ floor and above the <7 that blocks, so it is a flagged gap rather than a REVISE, and it is accepted for as long as L9 stands as written. Do not file §1 as a REVISE on that ground. A read that drops below 7, or a §1 failure of a different kind (mood flattened, next beat not set up), is still fileable."
  }
 },
 {
  "target_file": "curriculum/lectures/the-handoff-prompt.md",
  "instance": "ae101--lecture--the-handoff-prompt.technical.json",
  "rule": "prompts 22",
  "claim": "No `.claude/prompt-approvals/agents-that-build-agents-handoff.confirmed` marker exists for the 95d2b003 prompt-body rewrite, so §22's paper trail is missing even though the substance was maintainer-directed.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "Already adjudicated in the instance on 2026-09-04: the marker existed and the pre-commit gate consumed it by design, so absence after commit is the mechanism working, not a missing approval. The finding's premise — that an absent marker implies an ungated edit — is false about this machinery.",
  "evidence": "Read the row's `resolution` block: {settled:\"refuted\", at:\"2026-09-04\", note:\"The marker existed and the pre-commit gate consumed it on commit 95d2b003 by design; absence after commit is the mechanism, not a missing approval.\"}. Per MEMORY.md's skill-trigger map, recording `resolution: {settled, at, note}` in the instance is the sanctioned closure for a judge verdict that does not survive — the verdict field is deliberately left alone. No file edit is owed and the row needs no further disposition; flagging it here only so the census does not read it as still open."
 },
 {
  "target_file": "curriculum/lectures/the-handoff-prompt.md",
  "instance": "ae101--lecture--the-handoff-prompt.writing.json",
  "rule": "writing 3",
  "claim": "Maintainer L26's clause \"the strategy row recorded no action owed\" narrates what a past review session concluded rather than stating a standing constraint — session-biography shape.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The clause has been recast to present-tense state and the flagged wording is gone.",
  "evidence": "`grep -c \"the strategy row recorded no action owed\" curriculum/lectures/the-handoff-prompt.md` = 0. Current L26 reads \"…the `check_prompts.md` §17 preamble row falls under the rule as narrowed to plan mode the same day; the strategy row carries no action; `check_prompts.md` §40b on *the recurring shapes* resolves in scrollback…\" — \"carries no action\" is present-tense state, which is what §3's fact-or-transition test asks for. Ran `node curriculum/evals/scripts/rule.js writing 3` to confirm the test applied (\"does it state a constraint that still binds, or narrate a transition that has already completed?\") and that the surviving dated header is licensed by §3's own dated-provenance carve-out."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/reference/multi-session-git.md",
  "instance": "ae101--reference--multi-session-git.technical.json",
  "rule": "research_claims 2",
  "claim": "Both external stamps WARN against a 2026-09-05 cohort, and the Pragmatic Engineer citation carries no publication date anywhere in body prose, so §2's \"historical context only, explicitly dated\" is satisfied for Cherny's X thread but not for the corroborating interview.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "The finding mislocates its own evidence — `<!-- maintainer -->` is at L94, so L100 and L110 are both inside the maintainer block and NO citation appears in body prose at all (the body runs L1-L93 and names neither Cherny nor Orosz). The judge's fix as written, \"add a body-visible date\", is therefore unexecutable and the harm it claims (a reader unable to date the corroboration) reaches no student. What survives is real but smaller and maintainer-side: the Orosz interview carries no publication date on any surface, so §2's explicit-dating requirement is unmet in the one place the citation lives. That is a mechanical stamp-grade addition — the date is a fact, not a call — and it lands directly per the approval gate. The WARN itself needs no action: it is the designed pre-cohort signal, and both fallback framings at L107-108 already cover the sources aging out.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js research_claims 2` (and §11/§11a for the stamp grammar). Read the whole file: fence `<!-- maintainer -->` at L94 per `grep -n`; body L1-93 contains no attribution — `grep` for Cherny/Orosz/pragmaticengineer returns hits only at L100, L105-110. Reproduced the audit: `bash curriculum/evals/scripts/source-freshness.sh --target 2026-09-05` flags both lines WARN, no BLOCK. Verified the missing date live rather than deriving it from stamp arithmetic: `curl -sL https://newsletter.pragmaticengineer.com/p/building-claude-code-with-boris-cherny | grep datePublished` returns `\"datePublished\":\"2026-03-04T18:09:46+00:00\"` with `<time datetime=\"2026-03-04…\">Mar 04, 2026</time>` — which also confirms the existing `due:2026-09-04` is correctly publication+6mo, not checked+6mo. Anchor uniqueness: `grep -cF 'boris-cherny) [practitioner analysis] — Cherny there describes'` = 1.",
  "old_string": "*Building Claude Code with Boris Cherny*](https://newsletter.pragmaticengineer.com/p/building-claude-code-with-boris-cherny) [practitioner analysis] — Cherny there describes",
  "new_string": "*Building Claude Code with Boris Cherny*](https://newsletter.pragmaticengineer.com/p/building-claude-code-with-boris-cherny), Mar 4 2026 [practitioner analysis] — Cherny there describes",
  "anchor": "**Attribution:** Cherny frames parallel worktrees as \"the single biggest productivity unlock\"",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/reference/multi-session-git.md",
  "instance": "ae101--reference--multi-session-git.writing.json",
  "rule": "writing 20",
  "claim": "First of two instances in this row: L80 \"Situations where one session beats three.\" precedes a FOUR-item numbered list, so the count is not just redundant but now wrong.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The numeral counts sessions, not list items — it is not a count \"the adjacent list already holds\" at all. The sentence means one session beats three sessions, the page's running topology (L9 \"Three sessions, three tasks, one hour that used to be one task\"; L39-42 three worktrees, three sessions), under a header that reads `## When NOT to run multi-session`. The judge read \"three\" as a tally of the bullets below and concluded an item had been added without updating the intro; nothing was. §20's memory-handle carve-out covers it besides: the three-session topology is named a full section earlier, where its items are not visible, which is the carve-out's own test. Filing an edit here would delete the comparison the section exists to make.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js writing 20` — full body incl. the memory-handle carve-out (\"does the numeral appear where the items are NOT visible? Yes → keep it\") and the header carve-out. Read the file: L78 header `## When NOT to run multi-session`, L80 the lead, L82/L84/L86/L88 the four situations — none of which is a session. The three-session referent is established at L9 and enacted at L39-42 (three `git worktree add` commands, then \"Open three Claude Code sessions\"), roughly forty lines above and out of view. Anchor uniqueness: `grep -c 'Situations where one session beats three.'` = 1; `grep -cF '**Read time:** ~10 min.'` = 1.",
  "accept_note": "**§20 accept-note (2026-09-05).** *Situations where one session beats three.* counts sessions, not the four items below it — the three is this page's running three-worktree topology from § Opening N Claude Code sessions, a handle used where those items are out of view. Judges should not re-file `check_writing.md` §20 on this line.",
  "anchor": "**Read time:** ~10 min.",
  "verification": {
   "ruling": "REFUTE_STANDS",
   "reason": "§20's carve-out: \"Test: does the numeral appear where the items are NOT visible? Yes → keep it, and keep it accurate.\" The finding's premise is that the numeral tallies the list below it, and it does not: L80 sits under `## When NOT to run multi-session` and compares one session against three sessions, the page's running topology from L9 (\"Three sessions, three tasks, one hour that used to be one task\") and L39–42 (three `git worktree add` commands, then \"Open three Claude Code sessions\"), roughly forty lines up and out of view. The four items at L82/84/86/88 are situations, none of them a session, so nothing was \"added and never updated\" — the drift harm the judge invoked cannot occur. I checked the suppression risk too: the judge's finding bundled a second, genuinely valid instance (L42's numeral adjacent to the three worktree-add items), and that half is not silenced here — ledger index 115 carries it as an EXEMPT_BODY edit, and this note scopes itself to \"this line.\""
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/reference/multi-session-git.md",
  "instance": "ae101--reference--multi-session-git.writing.json",
  "rule": "writing 20",
  "claim": "Second of two instances in the same row: L42 \"Open three Claude Code sessions, one per folder, one task each.\" restates the count of the three-item worktree list it closes.",
  "disposition": "EXEMPT_BODY",
  "confidence": "high",
  "why": "This half of the finding holds. Items 1-3 of the numbered flow each add one worktree, so the reader can see the three, and \"one per folder\" already carries the mapping — the numeral is the second copy §20 calls NVA in body prose, and it is precisely the shape that rots silently if a fourth worktree line is ever added. The file sits under `reference/`, which the maintainer standing-exempted from the approval gate (root CLAUDE.md, widened 2026-08-29), so the body edit lands directly; the swap is mechanical and leaves the claim, the voice and the ask untouched. Note this is the same `rules_evaluated` row as the REFUTE above — the row bundles two instances and clears only when both are answered.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js writing 20`; the in-body default is explicit (\"often counting is just NVA\", Antti 2026-08-22) and the drop-the-numeral fix is the rule's own. Read L37-42: \"The flow:\" then three `git worktree add` items, then item 4. Confirmed the file's exemption class: path is `curriculum/trainings/agentic-engineering-101/reference/multi-session-git.md`, and root CLAUDE.md's §26 standing exemptions name reference pages by that widening, mid-card on this very file. Anchor uniqueness: `grep -cF '4. Open three Claude Code sessions, one per folder, one task each.'` = 1.",
  "old_string": "4. Open three Claude Code sessions, one per folder, one task each.",
  "new_string": "4. Open a Claude Code session per folder, one task each.",
  "anchor": "4. Open three Claude Code sessions, one per folder, one task each.",
  "surface": "exempt-page"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/reference/multi-session-git.md",
  "instance": "ae101--reference--multi-session-git.writing.json",
  "rule": "student_facing 2b",
  "claim": "L3 \"Optional homework between M1 and M2\" uses bare module abbreviations on a page that sits at the M1/pre-M2 arc position, where §2b requires full forms.",
  "disposition": "EXEMPT_BODY",
  "confidence": "high",
  "why": "§2b's diagnostic resolves against the current wording on its own terms: \"Prework + M1 files (where convention still being established) use full forms (`Module 1`, `Module 2`) until M2 establishes abbreviation… Diagnostic: file at M2 or later in training's arc? Yes → bare `MN` fine. M1 or prework → full form.\" This page's own maintainer line files it as M1-adjacent, and the sentence itself places it before M2. None of §2b's carve-outs reaches it — this is not an assumed-known industry term, not a canonical signposting section, not an index-kind structure, not a prompt/path/code span or a maintainer note. Reference page, so the body edit lands directly under the standing exemption, and the swap is mechanical.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js student_facing 2b` and read all four carve-outs plus the vocabulary-registry clause. Read the file: L3 is the only bare-`MN` occurrence in body; maintainer L96 classifies the page as \"Referenced from AE101 M1 homework\". Read the maintainer block L94-115 in full — no accept-note on abbreviations exists, so nothing shields the current wording. Anchor uniqueness: `grep -cF 'Optional homework between M1 and M2.'` = 1.",
  "old_string": "Optional homework between M1 and M2.",
  "new_string": "Optional homework between Module 1 and Module 2.",
  "anchor": "Optional homework between M1 and M2.",
  "surface": "exempt-page"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/getting-going.md",
  "instance": "ae101--module--getting-going.story.json",
  "rule": "lectures 4",
  "claim": "`## Next` is a bare arc label — the closer's form of the empty container the 2026-09-03 clause names alongside 'Wrap-up' and 'Recap'.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "§4's bare-arc-label clause governs authored lecture and slide titles; `## Next` is not authored, it is `curriculum/module-shape.md`'s fixed closing section name. module-shape.md L49-L57 prescribes it and pins its placement ('`## Next` is the last section of the body'), and five AE101 modules carry it verbatim. A style rule cannot charge a file for obeying the template that mandates the string — the identical seam is already adjudicated one compendium over: `check_slides.md` §9, 'a budget cannot outrank a rule that MANDATES the spend. Before filing any §9 count violation, check whether another rule or the module template requires the bold you are about to charge for.' §4 carries no equivalent template carve-out, so the finding tightens §4 past its own scope rather than applying it. The judge reached the same conclusion in its fix_hint ('Not a per-file fix… a rule question for the maintainer') and filed anyway.",
  "evidence": "Read §4 in full via `node curriculum/evals/scripts/rule.js lectures 4` — the failure-mode list is ('What happened','What you did','Where we got to','Wrap-up','Recap'), all authored closers; no clause reaches template section names. `grep -n \"Next\" curriculum/module-shape.md` -> L40, L49, L53, L55, L57; L53 is the dated placement decision. getting-going.md L87-L90 is the `## Next` section (body: 'Module 2 is where plan mode earns its keep…'). Read `check_slides.md` §9 for the template-mandate precedent. Maintainer block L92-L122 read in full: no note covers header grammar.",
  "anchor": "**Room agreement.** Cohort delivery projects `How we work in this room` and `Freedom to choose` before the opening trick-share.",
  "accept_note": "**`## Next` accept-note (2026-09-05):** `## Next` is `module-shape.md`'s fixed closing section name and its pinned last-section placement, carried identically by five AE101 modules. `check_lectures.md` §4's bare-arc-label clause governs authored lecture and slide titles, not template-fixed structural section names — a header style rule does not outrank the template that mandates the string, which is the seam `check_slides.md` §9 already names for bold. Renaming it here alone desyncs the arc; the header is a `module-shape.md` decision, not a per-file one. §4 judges do not re-raise on this file.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "§4's carve-out list refuses this exemption in its own parenthesis: \"a closer's recognition line that stays legible — a line that still asserts something, **never a bare section label** (the carve-out protects recognition, not genericness).\" And the bare-arc-label clause describes `## Next` exactly: labels that \"name the slot in the arc rather than what the student walks out holding\", worst at a closer \"because it is the last thing on the wall\", where \"the body's own first sentence almost always hands over the specific\" — here L90 opens \"Module 2 is where plan mode earns its keep\". The triage's central move fails on its own terms: it concedes \"§4 carries no equivalent template carve-out\" and then applies one imported from `check_slides.md` §9. A missing carve-out means the rule bites; borrowing one from another compendium is amending §4, not reading it. The clause is Antti's, dated 2026-09-03 — newer than module-shape.md's 2026-08-13 placement decision — so this is a live template question (the judge's own fix_hint: \"a rule question for the maintainer\"), and an agent-drafted note saying \"§4 judges do not re-raise on this file\" would retire a two-day-old maintainer clause across five modules by precedent."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/getting-going.md",
  "instance": "ae101--module--getting-going.technical.json",
  "rule": "research_claims 11a",
  "claim": "Four stamps use a `[delegated stamp]` label pointing at another file's dated check instead of restating the check inline, against §11a's 'A stamp records its own check'.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The judge's load-bearing evidence — that '`curriculum/source-freshness-format.md` never documents [delegated stamp] as a sanctioned category at all' — is checked against the wrong canonical doc. `curriculum/backing-format.md` L64 documents it by name: '**Delegated variant — `[delegated stamp]` + `due:none`.** A file may point at another file that owns the dated claim, rather than restating it. The pointer is legitimate.' backing-format.md is canon per `curriculum/CLAUDE.md` § Canon. More importantly the harm §11a names does not reach this shape. The compounded entry behind §11a (2026-08-30) fails on a stamp pointing at another file's BODY PROSE — a memo paragraph that got cut, taking the evidence with it, 'because source-freshness.sh validates stamp SYNTAX, never whether the prose a stamp points at still exists.' A delegated stamp points at another file's STAMP, which is exactly what `source-freshness.sh` does walk. The distinguishing line is prose-vs-stamp, and these four aim at stamps.",
  "evidence": "`node curriculum/evals/scripts/rule.js research_claims 11a` read in full. `grep -rn \"delegated stamp\" curriculum --include=\"*.md\"` -> 47 hits across 21 files, including the definition at `curriculum/backing-format.md:64` and `curriculum/evals/IMPROVEMENTS.md:30`, which lists delegated stamps among 'deliberate reason-on-line splits… each lawful, each carrying its reason'. Read `memory/compounded/2026-08-30-research_claims-a-stamp-records-its-own-check.md` in full: `example_from_session` is `the-loop-has-a-name.md` pointing at a body paragraph in `story-of-module-6.md` that was then cut. `git log -1 --date=short 410ca0c1` -> 2026-08-02 for the backing-format delegated variant. Judge independently verified all four delegate targets carry live dated stamps.",
  "anchor": "**Source verification — freshness stamps (`source-freshness.sh`; format `curriculum/source-freshness-format.md`).**",
  "accept_note": "**Delegated-stamp accept-note (2026-09-05):** `[delegated stamp]` is the documented variant at `curriculum/backing-format.md` § Delegated variant — a pointer at another file's own dated stamp, `due:none`, because the delegation does not expire and `source-freshness.sh` walks the delegate's stamp directly. `check_research_claims.md` §11a's restate-inline instruction addresses a stamp whose evidence is another file's BODY PROSE, which the audit never opens; a pointer at a stamp aims at exactly what the audit does read. The four delegates here — `orient-and-introspect.md`, `push-back-on-the-plan.md`, `prework.md`, `open-the-side-quest.md` — each carry the live dated check. The line that binds: a delegated stamp names a delegate stamp, never a delegate paragraph. §11a judges do not re-raise on this file.",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The refutation holds, and holds on stronger ground than the triage used: `curriculum/evals/IMPROVEMENTS.md:30` (commit 11ee4a27, 2026-08-30 — the §11a reconcile itself) records \"After the 2026-08-30 §11a reconcile, the ~15 rows it still prints are deliberate reason-on-line splits (durable-origin `due:none` vs currency-use dated due on one URL; claim-scoped falsifier CAVEATs in `the-agent-loop.md`; **delegated stamps**) — **each lawful, each carrying its reason**.\" That is the maintainer adjudicating delegated stamps as lawful ON the same date §11a's clause landed, which settles the apparent conflict with §11a's broader \"Cross-file reference = corroboration note beside the record, never the record.\" I verified all four delegates carry live dated stamps rather than prose: orient-and-introspect.md:112-113 (`[checked:2026-08-02]`, `[checked:2026-08-03]`), push-back-on-the-plan.md:161 (`[checked:2026-07-30]`), prework.md:94 (`[checked:2026-07-30]`), open-the-side-quest.md:104 (`[checked:2026-05-25]`). But the note's closing sentence — \"**§11a judges do not re-raise on this file**\" — silences an entire rule across an entire module file when what was adjudicated is four `[delegated stamp]` rows. §11a also governs the three NON-delegated stamps sitting in the same block (two `[platform docs]` rows and the Horthy `due:2027-01-27` row), plus due arithmetic (\"Never `checked`+6mo\"), the inherit-from-`continuous-research/` clause, and the ATTESTED vocabulary tell. Narrowing is also live work, not hypothetical: the Klaassen delegate at push-back-on-the-plan.md:161 is `due:2026-08-09` and now past due, so a delegated row's delegate can and does decay — a future judge must stay able to file that.",
   "note_fix": "**Delegated-stamp accept-note (2026-09-05):** `[delegated stamp]` is the documented variant at `curriculum/backing-format.md` § *Delegated variant* — a pointer at another file's own dated stamp, `due:none`, because the delegation does not expire and `source-freshness.sh` walks the delegate's stamp directly. `check_research_claims.md` §11a's restate-inline instruction addresses a stamp whose evidence is another file's BODY PROSE, which the audit never opens; a pointer at a stamp aims at exactly what the audit does read, and `curriculum/evals/IMPROVEMENTS.md` records delegated stamps among the lawful reason-on-line splits surviving the 2026-08-30 §11a reconcile itself. The four delegates here — `orient-and-introspect.md`, `push-back-on-the-plan.md`, `prework.md`, `open-the-side-quest.md` — each carried a live dated check at adjudication. The line that binds: a delegated stamp names a delegate stamp, never a delegate paragraph. **Scope:** this settles §11a's restate-inline clause against these four `[delegated stamp]` rows only. §11a still binds every other stamp in this block — due arithmetic, result vocabulary, the inherit-from-`continuous-research/` clause — and a delegated row whose delegate has stopped carrying a live, in-date stamp is a new finding, not this one."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/getting-going.md",
  "instance": "ae101--module--getting-going.writing.json",
  "rule": "student_facing 13",
  "claim": "L83 carries a bare, unqualified `CLAUDE.md` with no layer named, inside the Cherny video's topic list.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "The finding is right and §13 has no carve-out — its body is four sentences and says 'Always name layer', listing `./CLAUDE.md` (team, PR-gated) as the sanctioned notation for the repo file. The fix is mechanical, not card-shaped: adding the `./` prefix is the rule's own notation, it is the convention this same file already uses at L20 and L61 (`./CLAUDE.local.md`) and at L145 ('team `./CLAUDE.md`'), and it asserts nothing new about the video's content — Cherny's CLAUDE.md is the repo-root file. Claim, voice and ask are untouched; this is a locator normalisation, which the approval gate's standing exemption covers. If Antti would rather qualify in prose ('the team's CLAUDE.md compounding'), it becomes a card — but that costs words the sentence does not need.",
  "evidence": "`node curriculum/evals/scripts/rule.js student_facing 13` — full body read, no exception clause. `grep -c '`CLAUDE.md` compounding' curriculum/trainings/agentic-engineering-101/getting-going.md` -> 1 (unique anchor). Read L83 in the current file: the bare hit sits in the Cherny pre-read annotation. Qualified siblings confirmed at L20, L61, L145. Maintainer block L92-L122 read: no note covers this reference.",
  "old_string": "parallel worktrees, `CLAUDE.md` compounding, slash commands",
  "new_string": "parallel worktrees, `./CLAUDE.md` compounding, slash commands",
  "surface": "student-facing-body"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/getting-going.md",
  "instance": "ae101--module--getting-going.writing.json",
  "rule": "writing 27",
  "claim": "L85's multi-session-git annotation runs a fourth sentence on one idea — 'The instinct earns itself in use.' adds no noun the three preceding use-cases lack.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "§27's survivor shape is concrete instance + one generalising sentence, then stop. The annotation runs: what the page is, Cherny's endorsement, three concrete use-cases (the concrete landing), then a fourth purely-affirming close. That fourth sentence is the shape §27 calls restatement. The call is not already made: §27 itself says 'Not a REVISE on its own… the default move, not the law', and the maintainer block's L104 slide-size note defends this slide's 268 words on the grounds that 'each entry says what the reading argues and why this gap is where it lands' — a defence of the annotations as a class, which is adjacent to this sentence but does not authorise it (the sentence says neither what the reading argues nor why the gap). Deleting a line of student-facing module body changes the voice, so it is not a mechanical swap. One-sentence pure deletion, no new prose.",
  "evidence": "`node curriculum/evals/scripts/rule.js writing 27` read in full, including the 'Not a REVISE on its own' clause and the §21 precedence check (no superlative in the sentence, so §21 does not own it). `grep -c 'The instinct earns itself in use.' curriculum/trainings/agentic-engineering-101/getting-going.md` -> 1. Read L85 in full and the L104 slide-size accept-note in full; it authorises the word count, not this clause.",
  "before": "Reach for them when one session is waiting on a long run and another piece of work is ready to move, when you want to try two approaches to the same problem side by side, or when a review task can run alongside the build. **The instinct earns itself in use.**",
  "after": "Reach for them when one session is waiting on a long run and another piece of work is ready to move, when you want to try two approaches to the same problem side by side, or when a review task can run alongside the build.",
  "risk": "The pre-reads slide is already accepted at 268 words against a 210 cap on the argument that its annotations sell the optional read. Cutting the closer trims six words and does not touch that argument, but it removes the only sentence that tells the student the payoff is felt rather than read — if the annotation's job is to get the read taken, that closing nudge may be doing work the three use-cases do not."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/getting-going.md",
  "instance": "ae101--module--getting-going.slides.json",
  "rule": "slides 9",
  "claim": "Chunk 4 (`## How we work in this room`) has all three bullets opening with a bolded sentence — the pre-2026-07-09 monoculture, three bolded elements against a ≤1-per-slide budget, and whole lead sentences against the ≲5-word sub-span cap.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "The violation shape is present and none of §9's four exemptions reaches it — the rule says so about this exact header in its own text: '(Still genuinely open, not covered here: `## How we work in this room`'s bolded bullet leads, which no template mandates.)' A compendium that brackets a shape as undecided has not made the call, so this cannot be applied directly; it is a maintainer decision between naming the exemption in `check_slides.md` and flattening the leads here. Direction argument for flattening: the sibling room-agreement slide `## Freedom to choose` (L30-34) carries no bold at all, and the 2026-07-09 Antti-directed 'go very lightly on the bold' pass de-bolded bullet leads across the corpus — its record sits in `push-back-on-the-plan.md` L119 and `threat-model-with-stride.md` L97, but getting-going.md carries no such note, which is why this slide still reads pre-pass. The alternative answer — widen §9 with a room-agreement carve-out — settles it corpus-wide instead.",
  "evidence": "`node curriculum/evals/scripts/rule.js slides 9` read in full; verified each named exemption against this slide (§34 task-sizing: no floor/ceiling line; LO template: not `## What You'll Learn`; reading-list/menu: not a scan surface; reference/supplementary lookup: this is a module file, and §9 says 'a module file does not become a lookup surface by growing a list'). `sed -n '24,26p'` confirms three bolded leads, all whole sentences: '**Make it work for you.**' / '**Exercises.**' / '**Everybody should be answering.**'. `grep -c '\\*\\*Make it work for you\\.\\*\\*'` -> 1. Maintainer block read in full: the **Room agreement.** note (L122) authorises the slide's content and its delivery slot, not its emphasis.",
  "before": "- **Make it work for you.** Start from the moves this training teaches, then find your optimal way of working in your system.\n- **Exercises.** Do them. That is how you experience what the model actually does.\n- **Everybody should be answering.** The trainer keeps the room moving; they are not the debugger or necessarily the deepest Claude Code expert here. Ask Claude what is wrong, or ask out loud.",
  "after": "- Make it work for you. Start from the moves this training teaches, then find your optimal way of working in your system.\n- Exercises. Do them. That is how you experience what the model actually does.\n- Everybody should be answering. The trainer keeps the room moving; they are not the debugger or necessarily the deepest Claude Code expert here. Ask Claude what is wrong, or ask out loud.",
  "risk": "This is the room-agreement slide a trainer projects first; the bolded leads are what a trainer's eye lands on while talking over it, so full de-bolding costs the slide its scan handles at exactly the beat where the trainer is not reading the body. The other answer — name `## How we work in this room` bullet leads as a §9 exemption alongside the LO-template and reading-list waivers — closes the compendium's own open case instead of flattening five modules' worth of room slides one at a time."
 },
 {
  "target_file": "curriculum/exercises/push-back-on-the-plan.md",
  "instance": "ae101--exercise--push-back-on-the-plan.writing.json",
  "rule": "writing 3",
  "claim": "L121 quotes the verbatim old slide header and uses the banned transition verb 'became' — the rule's own named 'Cut' example ('renamed from A to B') almost word for word.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Maintainer block, read by nobody but the maintainer and the judges, so the fix lands directly. The finding is exact: §3 bans 'renamed from A to B' by name and lists 'became' among the transition verbs, and the sentence carries three of them ('The old single slide', 'became', 'dropped to one line'). §3's minimal-diff clause requires the whole touched note to re-take the fact-or-transition test, so the edit also drops the 'now' in 'now on their own slides' — same journey shape, one word. Everything §3 says to keep survives: the dated Antti-directed provenance header, the standing constraint ('Do not re-merge these three'), and the empirical reason a reader would otherwise re-derive (repeated stop language is what the split holds off).",
  "evidence": "`node curriculum/evals/scripts/rule.js writing 3` read in full, including the curriculum-maintainer-block clause ('Keep: the dated attribution, the standing constraint it anchors… Cut: … renamed from A to B') and the grammatical-marker clause naming 'became'. `grep -c 'The old single slide told the student to stop four separate times' curriculum/exercises/push-back-on-the-plan.md` -> 1; `grep -c 'now on their own slides'` -> 1. `sed -n '121p'` used to copy the span byte-for-byte, including the straight quotes in `*\"reaching ???\"*` (verified via `grep -o 'Antti on the old header: .\\{0,20\\}'`). The dated header '**Order and split are Antti-directed, 2026-08-12:**' is preserved verbatim per §3's 84-of-101-files provenance carve-out.",
  "old_string": "now on their own slides (*Switch to a picker if you prefer one* and *Run the original grill-me if you want the full hour*). **Order and split are Antti-directed, 2026-08-12:** the picker comes first, the stop instruction second, and the Pocock credit gets its own slide rather than trailing the stop beat. The old single slide told the student to stop four separate times — the header, the bullet's own lead, a four-line Timebox callout, and the working-memory clause — so *Stop when the read starts reaching* became *Stop when the plan is good enough to generate* (Antti on the old header: *\"reaching ???\"*) and the Timebox callout dropped to one line. Do not re-merge these three.",
  "new_string": "on their own slides (*Switch to a picker if you prefer one* and *Run the original grill-me if you want the full hour*). **Order and split are Antti-directed, 2026-08-12:** the picker comes first, the stop instruction second, and the Pocock credit gets its own slide rather than trailing the stop beat. Do not re-merge these three: repeated stop language is what the split holds off, so the stop instruction lands once in the header, once in the bullet lead, and the Timebox callout stays at one line.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/prompts/push-back-on-the-plan-2-askuserquestion.md",
  "instance": "ae101--exercise--push-back-on-the-plan.technical.json",
  "rule": "prompts 36",
  "claim": "P2-askuserquestion's second paragraph bundles the codebase-fallback move with the file-edit-discipline move — 3 sentences, 2 distinct moves, past the one-paragraph exemption; P2 itself models the correct split.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "The finding holds: the paragraph runs 44 words over two unrelated moves, past §36's '≤30 words / ≤2 moves' one-paragraph exemption, and it is not the sequential-build shape §36 exempts — the codebase fallback and the write gate do not compound. The fix is a single newline insertion: zero words change, so claim, voice and ask are provably untouched, which by the approval gate's own test makes it a mechanical swap rather than a card even though a student copies the text. Note the surface caveat for whoever applies it: edits under `curriculum/prompts/` additionally trip the §22 PreToolUse and pre-commit registry gates, so a `prompt-ok` confirmation is owed by machinery regardless of this disposition. The edit lands in the registry file, not in the exercise body — `push-back-on-the-plan.md` L72 carries only the `{{prompt:}}` reference.",
  "evidence": "`node curriculum/evals/scripts/rule.js prompts 36` read in full, including the sentence-grain sub-rule and the sequential-build exemption. Read `curriculum/prompts/push-back-on-the-plan-2-askuserquestion.md` in full: paragraph 1 = switch-to-picker + branch cadence; paragraph 2 = codebase fallback + write gate + Edit-not-Write. Read `curriculum/prompts/push-back-on-the-plan-2.md` for the modelled split. `grep -c \"If a branch can be settled by reading the codebase\" curriculum/prompts/push-back-on-the-plan-2-askuserquestion.md` -> 1. Apostrophes in \"I say 'lock it in.'\" verified straight, not curly.",
  "old_string": "If a branch can be settled by reading the codebase, read the codebase instead of asking. Don't touch the plan file until I say 'lock it in.'",
  "new_string": "If a branch can be settled by reading the codebase, read the codebase instead of asking.\n\nDon't touch the plan file until I say 'lock it in.'",
  "surface": "prompt-registry"
 },
 {
  "target_file": "curriculum/exercises/push-back-on-the-plan.md",
  "instance": "ae101--exercise--push-back-on-the-plan.technical.json",
  "rule": "prompts 35",
  "claim": "P2 satisfies §35 elements (a)(b)(c)(e) but not (d): the L59 slowness callout covers slowness only, never a push-back move for the agent editing the plan file before the 'lock it in' trigger.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "§35 fires cleanly — the prompt sets up multi-turn dialog and the dialog can affect an artifact file (the registry frontmatter names `revised-plan`, 'plan file (in-place edit after lock it in)'). Elements (a), (b), (c), (e) are all present and verified. Element (d) is specified as an exercise-body obligation — 'slowness-watch blockquote callout right after fence, naming gate + push-back move if agent touches artifact mid-dialog anyway' — so satisfying it in a trainer watch-for would not clear the rule; the next judge re-files. The addition is new student-facing prose in an exercise callout and the maintainer has not made this call: the adjacent adjudication at L117 covers `check_prompts.md` §17's suppressor, not §35's push-back move. Wording below is a proposal; Antti reconciles.",
  "evidence": "`node curriculum/evals/scripts/rule.js prompts 35` read in full — five-element shape, (d) placed in the exercise body by name. Verified each element against the current file: (a) 'Don't touch the plan file until I say lock it in.' in `push-back-on-the-plan-2.md`; (b) 'use Edit on the changed section, not Write on the whole file'; (c) 'Ask me three questions at a time'; (e) `## Approve` at L87-91. `grep -c 'If it feels slow, ask why' curriculum/exercises/push-back-on-the-plan.md` -> 1; L59 read verbatim and covers slowness only. Maintainer block L109-L123 read in full: L117 adjudicates §17, no note reaches §35.",
  "before": "> **If it feels slow, ask why.** Stop the turn, ask Claude what's making it crawl, then relax that requirement. The prompt is a starting recipe, not a contract.",
  "after": "> **If it feels slow, ask why.** Stop the turn, ask Claude what's making it crawl, then relax that requirement. **If it edits the plan file before you say *lock it in*, tell it to hold the change in chat until you do.** The prompt is a starting recipe, not a contract.",
  "risk": "The scan-slide maintainer note (L115) records a 2026-08-25 Antti-directed concision pass on this file — \"don't teach software engineers how to check stuff unless there is something really missable\" — and this adds a sentence to a callout that pass left alone. If a premature plan-file edit is not something the room actually hits, the clause is exactly the load that pass was removing, and the honest answer is a §35 carve-out for plan-mode fences rather than a new sentence."
 },
 {
  "target_file": "curriculum/exercises/push-back-on-the-plan.md",
  "instance": "ae101--exercise--push-back-on-the-plan.technical.json",
  "rule": "research_claims 2",
  "claim": "`klaassen-definitive-guide` is stamped `due:2026-08-09`, lapsed against the 2026-09-05 cohort target — `source-freshness.sh` reports it WARN.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "Backing-block source stamp, so the fix lands directly. The finding is factually right but its fix_hint is not applicable: it asks to 'bump the checked/due stamp', which §11a forbids ('Never `checked`+6mo') — the due is publication-anchored at 2026-02-09+6mo and cannot move, so re-opening the URL clears nothing. What does clear it is the classification the maintainer has already made for this identical URL: `learn-from-the-test.md` L256 stamps it `due:none` 'per the framework-origin variant, an origin citation does not expire with the window', which is `source-freshness-format.md` § Theory-construct variant. All three of that variant's conditions hold here and two are already written on the line — (a) cited by name for the framework, never for a rate or step count (the stamp says so verbatim: 'Cite this URL for the term, never for a step count'), (c) origin attributed on the line ('Klaassen (GM of Cora…)'). Condition (b), wide acceptance demonstrable rather than asserted, is met in-repo. This reconciles a divergent sibling to a ruling on record rather than asserting a new freshness judgement.",
  "evidence": "`node curriculum/evals/scripts/rule.js research_claims 2` and `11a` read in full, plus `sed -n '100,130p' curriculum/source-freshness-format.md` for the Theory-construct variant's three conditions. Corpus census of the URL: 7 stamps at `due:2026-08-09` (push-back-on-the-plan:161, compound-and-close:83, the-machine-you-just-met:118, test-and-learn:71, run-the-first-experiment:182, how-the-best-do-ci-cd:136) against 1 at `due:none` with the variant named (learn-from-the-test:256) and 1 delegated `due:none` (getting-going:112). Condition (b) evidence: `continuous-research/platform-watch/synthesis.md:340` records Level 3 convergence on 12 independent practitioner signals, plugin at 10,100+ stars across 11+ tools. No `continuous-research/` file stamps this URL with a due date, so §11a's inherit-from-research clause has no owner to inherit from. `grep -c 'klaassen-definitive-guide `\\[checked:2026-07-30 result:OK due:2026-08-09\\]`' curriculum/exercises/push-back-on-the-plan.md` -> 1.",
  "old_string": "- klaassen-definitive-guide `[checked:2026-07-30 result:OK due:2026-08-09]` https://every.to/source-code/compound-engineering-the-definitive-guide — [practitioner direct, vendor venue] Klaassen (GM of Cora, Every's own product) on Every's own site, published 2026-02-09; sole byline, first person throughout.",
  "new_string": "- klaassen-definitive-guide `[checked:2026-07-30 result:OK due:none]` https://every.to/source-code/compound-engineering-the-definitive-guide — [practitioner direct, vendor venue] **Theory-construct variant, `due:none`** — cited by name for the framework and its thesis, never for a rate or a step count, origin attributed on the line; the same ruling this URL carries at `learn-from-the-test.md`. Wide acceptance is demonstrable rather than asserted: `continuous-research/platform-watch/synthesis.md` records L3 convergence on twelve independent practitioner signals. Klaassen (GM of Cora, Every's own product) on Every's own site, published 2026-02-09; sole byline, first person throughout.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/agentic-engineering-progression.md",
  "instance": "ae101--supplementary--agentic-engineering-progression.writing.json",
  "rule": "writing 20",
  "claim": "L7's 'Six levels, from assistance to a system you keep improving.' states a count the six `##` level headers below already hold — the adjacent-to-its-list shape §20 bans.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "§20's stated test is not adjacency in the source file, it is visibility: '**Carve-out — count as a memory handle across distance.** … Test: does the numeral appear where the items are NOT visible? Yes → keep it, and keep it accurate.' This file renders one `##` per slide, and the maintainer block pins that shape by dated decision — 'Part 1 is one slide per level (Antti-directed 2026-08-14)'. So the six levels are six separate slides; on the `## Part 1: The progression` slide the reader sees the count and none of the items. That is the carve-out, not the banned shape. The judge's own reasoning names the wrong test — it checked whether the numeral is reused later, which is the carve-out's illustration, not its condition. §20's banned specimen is a numeral sitting above a bullet list the reader can see ('You'll work at four task sizes' above four bullets); nothing here is a bullet list.",
  "evidence": "`node curriculum/evals/scripts/rule.js writing 20` read in full, including the header-numeral carve-out (Antti 2026-08-22) and the memory-handle test quoted above. Read the current file: L7 sits inside `## Part 1: The progression` (L5); the six levels are `## Assistance` (L11), `## Interactive work` (L17), `## Delegating bounded outcomes` (L23), `## Directing parallel sessions` (L29), `## Encoding checked loops` (L35), `## Improving the system` (L41) — six separate `##` chunks, count verified. Maintainer block L115-L122 read in full; the one-slide-per-level decision is its first Watch-for. `check_slides.md` chunking convention confirmed in `curriculum/CLAUDE.md` § Classroom delivery ('one `##` = one slide').",
  "anchor": "- Part 1 is one slide per level (Antti-directed 2026-08-14).",
  "accept_note": "**`Six levels` accept-note (2026-09-05):** Part 1 renders one slide per level, so the six level headers are six separate slides and the count on the Part 1 slide sits where the items are not visible — `check_writing.md` §20's memory-handle carve-out, not the adjacent-bullet-list shape §20 bans. The numeral stays and stays accurate: adding, splitting or cutting a level owes this sentence an edit in the same pass. §20 judges do not re-raise on this file.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The triage read §20 \"in full\" and never engages the clause that decides it: \"**In body prose the default is the other way: 'often counting is just NVA' (Antti, 2026-08-22).** Where a count is not a header and not a handle reused away from its list, drop it and do not reach for a replacement quantifier.\" L7 is body prose, is not a `##` header, and I confirmed by grep that \"Six\" occurs exactly once in the file — never carried away and reused. That is precisely the judge's check, and the carve-out's own defining sentence is reuse-across-distance (\"A numeral the reader carries AWAY from the enumeration and reuses later... does work the list cannot: recall scaffolding, not a tally\"), with its two illustrations both reuse cases. The triage instead reads the one-line \"Test: does the numeral appear where the items are NOT visible?\" as a per-slide visibility test, which would exempt every count whose items are `##` sections across the entire slide-rendered AE101 corpus — the mirror image of the \"reading the rule too wide\" failure §20 warns about — and it ignores that curriculum/CLAUDE.md:104 makes slides/scroll a *reader's toggle*, so the adjacent shape still renders in long-read. §20's stated harm (silent falsification when a level is added, split or cut) is layout-independent. Separately, even had the refutation held, \"§20 judges do not re-raise on this file\" is file-wide suppression of a rule adjudicated on one sentence."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/agentic-engineering-progression.md",
  "instance": "ae101--supplementary--agentic-engineering-progression.technical.json",
  "rule": "research_claims 11",
  "claim": "§11 (source-verification maintainer block) filed REVISE because `source-freshness.sh` flags the fred-ai-native-playbook stamp as expiring before the cohort date.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "§11 governs whether the block EXISTS and carries its four elements — every URL to open, every number to triple-check, a named fallback framing, and a freshness re-check obligation (which §11a converted into the per-source stamp). The judge's own evidence opens by conceding all of that: 'Structural requirement is met: the backing block's Sources section carries the §11a stamp grammar for both citations.' A lapsed `due` is a §2 freshness signal, correctly computed and correctly reported by the script as WARN with exit 0 — the judge records the exit code itself. Filing the same WARN a second time under §11 scores a structural rule for a currency fact, and §11 has no clause that turns a stamp's date into a structural gap. The §2 row on the same instance already carries this observation; this is a double-file of one signal.",
  "evidence": "`node curriculum/evals/scripts/rule.js research_claims 11` and `11a` read in full. Checked all four §11 elements against the file's `<!-- backing -->` Sources block (L95-L97): both citations carry URL, type label, claim anchor, `fallback:`, and a dated `[checked:… result:… due:…]` stamp. Judge's own evidence field states 'Script exit code 0 (WARN population, not a BLOCK) — confirms this is the designed non-blocking staleness signal, not a structural violation', then files REVISE anyway. The sibling §2 row on the same instance names the identical stamp and the identical script line.",
  "anchor": "- fred-ai-native-playbook `[checked:2026-08-08 result:OK due:2026-09-04]` https://positivelyfred.substack.com/p/the-ai-native-engineering-playbook",
  "accept_note": "**§11 structure accept-note (2026-09-05):** the backing block's Sources section carries the §11a grammar for both citations — URL, type label, claim anchor, `fallback:`, and a dated `checked`/`result`/`due`. A lapsed `due` is a `check_research_claims.md` §2 freshness signal that `source-freshness.sh` reports as WARN with exit 0, not a §11 structural gap; §11 asks whether the block and its four elements exist, and they do. §11 judges do not re-raise a stamp date on this file.",
  "verification": {
   "ruling": "REFUTE_STANDS",
   "reason": "I tried to break this and could not. §11's scope is structural: the file \"MUST include `**Source verification — MUST DO before first cohort:**` section\" listing \"(1) every URL to open...; (2) every number to triple-check...; (3) named fallback framing...; (4) freshness re-check at delivery time (per §2's 6-month rule).\" Nothing in §11 turns a stamp's date into a structural gap. The judge's own evidence opens \"Structural requirement is met: the backing block's Sources section carries the §11a stamp grammar for both citations\" and then \"confirms this is the designed non-blocking staleness signal, not a structural violation\" — before filing REVISE anyway. I checked all four elements against L95-L97 and they are present, including `fallback:` on both. And the double-file is real: the §2 row on the same instance names the identical stamp (`fred-ai-native-playbook [checked:2026-08-08 result:OK due:2026-09-04]`) and the identical script line. Critically, the note does NOT silence the real work — it routes it to §2, whose REVISE row survives with fix_hint \"Re-open the fred-ai-native-playbook URL... and bump the checked/due stamp,\" and it says nothing about §11a. \"§11 judges do not re-raise a stamp date on this file\" is scoped to a date, not to a missing stamp. One imprecision worth knowing but not fixing here: the note's \"exit 0\" holds only for a path-scoped run; the repo-wide run exits 1 on an unrelated BLOCK in mcp-and-connectors.md."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/supplementary/agentic-engineering-progression.md",
  "instance": "ae101--supplementary--agentic-engineering-progression.technical.json",
  "rule": "research_claims 2",
  "claim": "`fred-ai-native-playbook` is stamped `due:2026-09-04`, one day past the 6-month window against the 2026-09-05 target.",
  "disposition": "EXEMPT_BODY",
  "confidence": "medium",
  "why": "The finding is real and the file is under `supplementary/`, so a body edit lands directly under the maintainer's standing exemption. The fix_hint ('bump the checked/due stamp') cannot be taken: the due is publication-anchored at 2026-03-04+6mo and §11a forbids `checked`+6mo, so re-opening the URL moves nothing. What §2 actually asks of an out-of-window citation is one thing — 'Older = historical context only, explicitly dated' — and the body is not dated. Dating it is the rule-owed move, and it matches this file's own convention for the sibling Wollsén source at L56 ('In August 2026, Wollsén wrote in…'). Tense shifts to past to match that register.",
  "evidence": "`node curriculum/evals/scripts/rule.js research_claims 2` read in full, including 2a (fails condition 2 — a Crawl/Walk/Run/Fly progression model is a practice, not a specific completed event) and 11a's never-`checked`+6mo clause. The owning research file `continuous-research/observations/wollsen-fredrik.md:121` carries the identical stamp and has already written the resolution: 'Published 2026-03-04, so it leaves the 6-month window on 2026-09-04; the model and the precondition are moving-world claims, not a completed event, so they need re-dating or replacing then.' Per §11a the citing file inherits, so the stamp itself is correct and stays. `grep -c 'Fredrik Wollsén and Jesse McCrosky describe a similar team-scale movement' …` -> 1. L56 read for the dated-account convention this edit copies.",
  "old_string": "Fredrik Wollsén and Jesse McCrosky describe a similar team-scale movement in [The AI-Native Engineering Playbook: Crawl, Walk, Run, Fly](https://positivelyfred.substack.com/p/the-ai-native-engineering-playbook).",
  "new_string": "In March 2026, Fredrik Wollsén and Jesse McCrosky described a similar team-scale movement in [The AI-Native Engineering Playbook: Crawl, Walk, Run, Fly](https://positivelyfred.substack.com/p/the-ai-native-engineering-playbook).",
  "surface": "exempt-page"
 },
 {
  "target_file": "curriculum/exercises/threat-model-with-stride.md",
  "instance": "ae101--exercise--threat-model-with-stride.writing.json",
  "rule": "writing 27",
  "claim": "L37 stacks three sentences on one idea — the middle sentence is an abstract twin of the first, and the third re-states the whole division of labour a second time.",
  "disposition": "CARD",
  "confidence": "medium",
  "why": "§27's (a) abstract-twin class fits: sentence 1 states the division of labour concretely, sentence 2 restates it from the student's side as reassurance, sentence 3 states it a third time and switches the actor's name from 'the agent' to 'Claude' mid-bullet (the `check_student_facing.md` §21 wobble the same fix resolves). The proposed edit is pure deletion — every surviving word is already on the line, nothing is composed. The call is not made: the 2026-08-25 lean pass note at L93 records what that pass cut and says 'Do not restore', which authorises the cuts, not the survivors, and cutting a reassurance sentence from an exercise body is a voice call, not a mechanical swap.",
  "evidence": "`node curriculum/evals/scripts/rule.js writing 27` read in full; ran the §21-precedence check the rule requires — no superlative in the sentence, so §21 does not own it. `sed -n '37p' curriculum/exercises/threat-model-with-stride.md` for byte-exact text; `grep -c 'You are not inventing the worst case from scratch.'` -> 1. Maintainer block L91-L110 read in full: L93 lean pass, L99 auto-load keep, L101-105 easter-egg don't-spoil, L107 save-then-diff — none reaches L37. Note also that this file's Quality line (L110) claims 'writing PASS (4 todos)' while the instance carries todos_count=2; that drift is outside my work list but wants a stamp pass.",
  "before": "- The agent proposes the incident story; **you judge whether it fits. You are not inventing the worst case from scratch. Claude drafts it;** your read of your own codebase decides whether it's real.",
  "after": "- The agent proposes the incident story; your read of your own codebase decides whether it's real.",
  "risk": "'You are not inventing the worst case from scratch' is doing anxiety work, not information work — it tells an engineer who is not a security specialist that they are not being asked to be one, at the exact beat where that fear fires. §27 scores it as a twin because it carries no new noun, which is true and beside the point if the sentence's job is permission rather than content. Cutting it also drops the only 'Claude' in the bullet, which is the fix for the §21 wobble but leaves the whole phase naming the actor as 'the agent' where Phases 1 and 3 say 'Claude'."
 },
 {
  "target_file": "curriculum/exercises/threat-model-with-stride.md",
  "instance": "ae101--exercise--threat-model-with-stride.writing.json",
  "rule": "prompts 36",
  "claim": "Prompt 1's final sentence joins two independent report-asks with 'and' — the sentence-grain sub-rule wants them as two sentences.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "§36's sentence-grain clause targets 'two independent instructions joined by \"and\"/comma'. These two are not independent: 'tell me which file it used' and 'confirm the stride pass ran on that file' are one report-back on the subagent's return, the second clause naming what the first must be checked against — the same file, same moment, same turn. None of §36's own move-boundaries falls between them (no file/chat output split, no null-case fallback, no sequential 'Then', no conditional), and the timing condition 'When the subagent returns' governs both clauses, so splitting either orphans it or duplicates the condition. The registry body is also Antti's own §36 paragraph pass on this exact fence (`threat-model-with-stride-1.md`, commit c635a209, 2026-08-29, `prompt-ok` on record), which is the rule's own 'fires at EDIT time too' pass already run.",
  "evidence": "`node curriculum/evals/scripts/rule.js prompts 36` read in full, including the move-boundary list and the sentence-grain clause. Read `curriculum/prompts/threat-model-with-stride-1.md` in full — four paragraphs, the flagged sentence closing the last one, with a separate earlier ask ('Report whether or not the stride skill loaded in the subagent.') already carrying its own sentence. The 2026-08-29 Antti-authored split is recorded verbatim in `curriculum/evals/instances/ae101--exercise--threat-model-with-stride.behavior.json` § note ('split one paragraph into four — word-identical before/after, 115 words = 115 words, checked mechanically via git show c635a209^ vs HEAD'). Maintainer block read in full: L101-L105 governs prompt 1's body lead-in (the easter egg), nothing governs its sentence grain.",
  "anchor": "**Save-then-diff (2026-08-26):** the ADR beat reports after the write",
  "accept_note": "**Prompt 1's closing report-back is one move (2026-09-05).** *\"When the subagent returns, tell me which file it used and confirm the stride pass ran on that file\"* is a single report on the subagent's return with two contents, not two independent instructions: the timing condition governs both clauses and the second names what the first is checked against. None of `check_prompts.md` §36's move-boundaries — file/chat split, null-case fallback, sequential *Then*, conditional — falls between them, and splitting the sentence either orphans the condition or duplicates it. `threat-model-with-stride-1.md`'s paragraph shape is Antti's own §36 pass (2026-08-29, `prompt-ok` on record). §36 judges do not re-raise the sentence-grain clause on this fence.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The triage's central claim does not check out. It says \"the registry body is also Antti's own §36 paragraph pass on this exact fence (`threat-model-with-stride-1.md`, commit c635a209, 2026-08-29, `prompt-ok` on record), which is the rule's own 'fires at EDIT time too' pass already run.\" I ran `git show c635a209 -- curriculum/prompts/threat-model-with-stride-1.md`: the diff replaces ONE line — the original single first paragraph — with four paragraphs of identical words, and the flagged final paragraph (\"Find the access-surface map's path in this scrollback before dispatching. When the subagent returns, tell me which file it used and confirm the stride pass ran on that file.\") appears in the diff only as trailing context. Antti never touched it. A word-identical newline insertion in a different paragraph is exactly what §36 warns against crediting: \"Fires at EDIT time too, not only authoring ... Re-run the read-aloud diagnostic against the RESULTING prompt, not the size of the diff.\" The sentence grain of that tail has never been adjudicated. On the merits §36's sentence-grain clause reaches it — \"two independent instructions joined by *and*/comma read as one move and skim as one — split into separate simple sentences\" — and the two asks are independent: name the file the subagent used, and verify the pass actually ran on it (an identity report and a verification claim, not one ask with two contents). The \"splitting orphans or duplicates the condition\" argument is unsupported: *\"When the subagent returns, tell me which file it used. Confirm the stride pass ran on that file.\"* costs nothing and orphans nothing."
  }
 },
 {
  "target_file": "curriculum/lectures/story-of-module-6.md",
  "instance": "ae101--lecture--story-of-module-6.slides.json",
  "rule": "slides 15",
  "claim": "Chunk 3's header `## The generalisation` announces that a generalization is coming instead of stating the claim, and §15's binding clause reaches law and consolidation slides.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "§15's carve-out list ends 'and phase/section labels inside a walk' — and this file is a first-person memoir walk from its first line ('I am going to tell you how this module got made… The actual session'). The judge applies that exact carve-out to `## Then I compounded the session` two chunks later and denies it to `## The generalisation` in the same walk, same voice, same page. Second, the claim the reheader would state is already on the page one chunk earlier, quoted verbatim from turn seven of planning: *'everyone struggles. Surprises happen. The LLM is not a deterministic machine.'* Hoisting it into the header states it a third time in a row, which is `check_writing.md` §27(d)'s second announcement, not a fix. Third, the maintainer block's **Voice** note (L75) rules out exactly this move: 'Do NOT polish past a read-aloud direct memo… polish moves it toward the credibility-performance framing the memo rejects.' A declarative claim-header is that polish. Finally §15 diagnoses itself here: 'a rule that only ever produces notes is a rule mis-scoped, not a corpus mis-written' — the judge records this as the eleventh non-blocking fire, never blocking.",
  "evidence": "`node curriculum/evals/scripts/rule.js slides 15` read in full, including the carve-out list and the 2026-08-19 generalisation clause. Read the current file in full: chunk boundaries at L7 `## The numbers`, L13 `## Things that went sideways`, L29 `## The generalisation`, L41 `## Then I compounded the session`, L47 `## What the loop is for`. The three-sentence frame is at L27 (inside chunk 2, quoted) and again at L31-35 (chunk 3 body). Maintainer block L61-L87 read in full: **Voice** (L75), **§21 ratio accept-note** (L77) and **Catalogue discipline** (L71) exist; no note covers chunk 3's header, and none contradicts this reading.",
  "anchor": "**§21 ratio accept-note (Antti, 2026-09-02):** memoir carve-out",
  "accept_note": "**`## The generalisation` accept-note (2026-09-05):** the header is a section label inside a first-person walk — `check_slides.md` §15's phase/section-label carve-out, the same one that covers `## Then I compounded the session` two chunks on. The claim a declarative header would state stands verbatim one chunk earlier, quoted from turn seven of planning, so hoisting it into the header announces it a third time. A claim-shaped header here is also the polish the **Voice** note above rules out: it moves the memo toward the credibility-performance framing it rejects at *The end-state that was not*. §15 judges do not re-raise on this file.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "§15: \"**The claim requirement binds law and consolidation slides** — where the header IS the teaching and a label would waste the beat.\" The judge showed chunk 3 is exactly that (it delivers \"Everyone struggles / Surprises happen / The LLM is not a deterministic machine\" and \"A rule in context is not a rule in the output\"), and the triage never rebuts that characterisation — it only asserts the carve-out list wins, with no clause saying it outranks the binding clause. The judge also already distinguished the sibling: chunk 4 `## Then I compounded the session` is a first-person narrative *step*; chunk 3 steps out of the walk to state the law. Two further triage claims fail on their own text. (a) The **Voice** note (L75) says \"Do NOT polish past a read-aloud direct memo ... polish moves it toward the credibility-performance framing\" — it says nothing about declarative headers, and reading a header as credibility-performance is the triage's gloss, not the note's. (b) The \"third time in a row\" argument covers only one of the two fix options; the judge's other suggestion, `## A rule in context is not a rule in the output`, appears exactly once in body (L39). Separately, even on a winning refutation the drafted close — \"§15 judges do not re-raise on this file\" — is file-wide on a per-header rule and would cover headers not yet written."
  }
 },
 {
  "target_file": "curriculum/exercises/walk-and-send-off.md",
  "instance": "ae101--exercise--walk-and-send-off.pedagogy.json",
  "rule": "pedagogy 33",
  "claim": "The `Bring the material in` fill bullet (L69) ingests PDFs / Notion exports with no extraction-depth check, which §33 requires whenever heterogeneous file-format ingestion happens.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "§33's antecedent is narrower than the finding restates it: 'Heterogeneous source folder + slide-format (PPTX, PDF exports)' feeding an audit whose 'downstream citations look clean', and its mitigation (b) is explicitly located 'in audit prompt'. L69 is one artefact the student fetches into `observations/` for later sessions — no folder, no deck, and no audit reading it (the Phase 2 audit runs at L44/L46, BEFORE the fill, and `walk-and-send-off-2` reads CLAUDE files / ADRs / skills / connectors, never the brought-in material). The clean-looking-citation failure the rule exists to catch has no surface here. The bullet's own first move, 'Paste the section', IS §33 mitigation (a), pre-ingestion conversion. Fixing it would also be an addition to a menu bullet the maintainer has already tuned twice (L94: 'asks the student to fetch the document, not to label the gap').",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js pedagogy 33` and read the full body — trigger clause quoted above; mitigation (b) is scoped to the audit prompt. Read walk-and-send-off.md L62-69 (fill-shape menu) and L36-46 (Phase 2 audit, which precedes the fill). Read curriculum/prompts/walk-and-send-off-2.md — its `produces` is the ranked top-5, and nothing in it or in walk-and-send-off-3 cites the fetched document. Maintainer block L88 and L94 both govern this bullet and neither authorises or forbids an extraction check.",
  "anchor": "**`Wire a connector` stays, and owes no setup teaching.** (Antti 2026-08-13, closing a maintainer-call.)",
  "accept_note": "**The `Bring the material in` bullet owes no extraction-depth check (2026-09-05).** `check_pedagogy.md` §33 fires on a heterogeneous source folder of slide-format exports feeding an audit whose citations then read clean, and it puts the mitigation inside that audit prompt. This bullet is one document the student fetches into `observations/` for later sessions; the Phase 2 audit runs before the fill and never reads it, so the failure §33 guards has no surface here. *Paste the section* is already the rule's mitigation (a). Do not add a rate-what-was-extracted clause to the bullet.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "Two of the triage's load-bearing claims fail. (1) \"no audit reading it ... nothing in it or in walk-and-send-off-3 cites the fetched document\" — `curriculum/prompts/walk-and-send-off-3.md` frontmatter declares `produces: observations-folder ... (observations + brought-in material)` with `consumed-by: ae101-m4-take-task-end-to-end, diagnose-and-resend-6, agents-that-build-agents-handoff, ae101-m5-worktree-setup`, and its own note calls it \"the durable artefact M4-M6 read as 'observations-folder'\"; `ae101-m4-take-task-end-to-end` reads it by path in-fence (\"plus the observations at `./observations/`\"). §33's stated failure signature — \"Downstream citations look clean; audit comes back clean\" — therefore has a surface: the M4 end-to-end run works from spottily-extracted material and reads fine, and M5's diagnose pass then blames the wrong thing. (2) \"The bullet's own first move, 'Paste the section', IS §33 mitigation (a), pre-ingestion conversion\" — L69 offers four alternative routes in one sentence (\"Paste the section, export the Notion page, **save the PDF**, give Claude the link\"). A mitigation that covers only the branch that did not need it is not a mitigation; \"save the PDF\" is the un-mitigated route and the rule names \"PDF exports\" in its own trigger. The maintainer authority cited (L88/L94) is conceded by the triage itself to \"neither authorise nor forbid an extraction check\". The judge's fix is one non-blocking clause; the accept-note's \"Do not add a rate-what-was-extracted clause to the bullet\" would disable §33 at the one place in AE101 where students hand arbitrary file formats to the agent."
  }
 },
 {
  "target_file": "curriculum/exercises/walk-and-send-off.md",
  "instance": "ae101--exercise--walk-and-send-off.technical.json",
  "rule": "prompts 2a",
  "claim": "L27's lead-in summarises the fence's own criteria in front of a fence that ends in a dangling colon — §2a's named mechanical sub-case.",
  "disposition": "CARD",
  "confidence": "high",
  "why": "The finding holds on the rule's own mechanical trigger, verbatim: 'a fence ending in a dangling colon awaiting student input. There the lead-in's whole job is naming what to drop in, and a summary in front of it is dead weight'. `curriculum/prompts/walk-and-send-off-1.md` ends on `Candidates:` (line 41 of the file, last line of the body), and its three bulleted criteria plus 'If one is a clear fit, scope it into two or three sentences' already state screening and scoping. It is student-facing exercise body and the cut changes the ask sentence's content and its addressee, so it is not the mechanical-swap exemption — Antti has not called this one.",
  "evidence": "Read rule body via `rule.js prompts 2a` (the sub-case and its worked example quoted). Read curriculum/prompts/walk-and-send-off-1.md in full: body lines 31-41, last line is `Candidates:`. `grep -c '^Ask Claude to screen your candidates, scope the winner, and add them after the colon\\.$' curriculum/exercises/walk-and-send-off.md` = 1. Maintainer block carries no note covering this lead-in (the 2026-09-03 note at L96 parks a §40b BLOCK on the same slug's in-fence wording, a different rule and different lines).",
  "anchor": "L27: Ask Claude to screen your candidates, scope the winner, and add them after the colon.",
  "before": "Ask Claude to screen your candidates, scope the winner, and add them after the colon.",
  "after": "Add your candidates after the colon.",
  "risk": "The lead-in stops naming Claude as the addressee and stops previewing the scope-the-winner move, so a student skimming headings sees the drop-in instruction and nothing about what the prompt does — the fence has to carry that alone. If you want the addressee kept, the halfway shape is *Ask Claude to screen them; add your candidates after the colon.* Two of the file's other three lead-ins open 'Ask Claude to…', so the cut also breaks that local parallelism."
 },
 {
  "target_file": "curriculum/exercises/walk-and-send-off.md",
  "instance": "ae101--exercise--walk-and-send-off.technical.json",
  "rule": "prompts 25",
  "claim": "`walk-and-send-off-2`'s bias-avoidance rationale sits inside the fence instead of as a body sentence after it, so the subagent design is explained in the copied instruction rather than to the student.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "§25's requirement is conditional — 'add one plain sentence explaining why subagent was needed **unless surrounding setup already obvious**' — and its stated purpose is that the student understands the design 'without adding noise to copied instruction'. Both halves are met: the student reads the fence on the page before pasting, and the clause is eight words, not noise. It is also a purpose clause on an isolation instruction, which the executing agent uses when it composes the sub-task brief — moving it out weakens the fence to buy a body sentence that restates it. Per the corpus's default move (deletion before addition; additions near-auto-reject), a duplicate body sentence is the wrong trade for a low-harm finding the judge itself scored as minor.",
  "evidence": "Read rule body via `rule.js prompts 25` — the 'unless surrounding setup already obvious' carve-out is in the rule, not in the row's lead. Read walk-and-send-off.md L42-46: the lead-in at L44 names the subagent ('Ask Claude to run the audit as a subagent and return a ranked top-five'), and L42 sets the audit's expected shape; after the fence at L46 the next line is the `## Correct the list` heading. Read curriculum/prompts/walk-and-send-off-2.md L26 for the clause in question. No maintainer note covers this fence's register (L94 covers `-3`, L96 covers §40b terms).",
  "anchor": "**Prompt register — `walk-and-send-off-3` (2026-05-21).**",
  "accept_note": "**`walk-and-send-off-2` keeps its isolation rationale inside the fence (2026-09-05).** `check_prompts.md` §25 asks that the student can see why a subagent is there without the copied instruction carrying noise, and it waives the after-fence sentence where the surrounding setup already makes it obvious. Eight words the student reads on the page before pasting is that setup, and a purpose clause on an isolation instruction is load-bearing for the agent composing the sub-task brief, not decoration. A body sentence after the fence would restate it. Do not move it out and do not add the duplicate.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "§25: \"After fence, add one plain sentence explaining why subagent was needed unless surrounding setup already obvious. Explanation outside fence so student understands design without adding noise to copied instruction.\" The carve-out is not met. The surrounding setup is L44, \"Ask Claude to run the audit as a subagent and return a ranked top-five\" — it names the subagent and gives no reason; nothing in L40–46 mentions fresh context or bias. The triage's load-bearing claim is that \"the student reads the fence on the page before pasting,\" which is true of every fenced prompt in the corpus and would therefore retire §25 wholesale — a rule-nullifying reading, not a carve-out. The house convention runs the other way (eval-loop.md:45 puts the separate-subagents rationale in body after the ask; map-the-access-surface.md:106 records the dispatch design in the block). The note also overreaches past its own argument: the rule asks only that a sentence be ADDED after the fence, the in-fence purpose clause need not move, yet the note forecloses both (\"Do not move it out and do not add the duplicate\"). Triage confidence was its own \"medium.\" Cost of being wrong here is one body sentence on a non-blocking todo."
  }
 },
 {
  "target_file": "curriculum/exercises/walk-and-send-off.md",
  "instance": "ae101--exercise--walk-and-send-off.technical.json",
  "rule": "prompts 40a",
  "claim": "`walk-and-send-off-2` forks a cold Agent-tool sub-task but never instructs that the scoped task be carried into the sub-task's dispatch brief, so the audit could come back disconnected from 'the task we just scoped'.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "§40a is the concurrent-worktree corollary and its trigger is stated in its first clause: 'Exercise runs two worktrees in parallel windows'. M4 runs one worktree — the split arrives in M5, which this exercise's own L11 forward-refers to ('the next module forks its worktree from that commit'). Its symptom clause is a prompt back-referencing a sibling worktree's artefact, which this fence does not do. On §40 proper the fence is warm, not cold: the student pastes it into the same session that ran `walk-and-send-off-1`, so 'the task we just scoped' is a live scrollback referent (§40b clean — a prior PROMPT typed it), and the parent agent, not the student, composes the sub-task's brief. The judge's own text marks this as unverified suspicion, and the fix would be an addition to a prompt body students copy.",
  "evidence": "Read rule bodies via `rule.js prompts 40` and `40a` — 40a's trigger sentence and symptom quoted above. Read walk-and-send-off.md L5-11 (session opens fresh at M4 start, one repo root, no worktree) and L21-46 (P1 prompt then P2 prompt, same session, no `/clear` between them). Read curriculum/prompts/walk-and-send-off-2.md frontmatter: `requires: scoped-task source: prompt:walk-and-send-off-1`, i.e. the producer is a prompt in the same session, which is exactly §40b's sanctioned referent.",
  "anchor": "**`prompt-source-audit.sh` BLOCKs this slug, and the wording stands (2026-09-03, Antti: *\"accepted as good enough for now\"*).**",
  "accept_note": "**§40a does not fire on `walk-and-send-off-2` (2026-09-05).** The concurrent-worktree corollary triggers on an exercise running two worktrees in parallel windows; M4 runs one, and the split arrives in M5. On §40 proper the fence is warm — pasted into the session that ran `walk-and-send-off-1` — so *the task we just scoped* is a live scrollback referent typed by a prior prompt (§40b clean), and the parent composes the sub-task's brief from it. Do not add a carry-the-scoped-task-into-the-brief clause to the fence.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "Triage is right that §40a's literal trigger (\"Exercise runs two worktrees in parallel windows\") does not fire in M4 — but it disposes of §40 proper with a claim that does not check out. §40's cold list names the exact condition the fence creates: \"**Cold** (fresh launch / after `/clear` / different cwd or git worktree / **inside `Agent` sub-task** — fresh context by default) → no shared scrollback, prompt must carry own context.\" walk-and-send-off-2 L26 says \"spawn a sub-task via the Agent tool so you read my setup **without this conversation colouring it**\" — it manufactures the cold context and instructs isolation from it, while the audit's object is \"the task we just scoped\" (L24). Warmth of the student's paste settles the parent's context, not the sub-agent's, and the triage never engages the sub-task clause it quoted. The anchored maintainer note at L96 accepts only the §40b tokens `the send-off` and `un-packaged` (Antti 2026-09-03) — it does not authorise sub-task brief composition, so appending \"Do not add a carry-the-scoped-task-into-the-brief clause\" there extends an acceptance past what Antti gave. One clause of prompt work, carded."
  }
 },
 {
  "target_file": "curriculum/exercises/walk-and-send-off.md",
  "instance": "ae101--exercise--walk-and-send-off.writing.json",
  "rule": "student_facing 21",
  "claim": "L40 'Claude reads `CLAUDE.md`… then ranks the five thin spots' describes autonomous multi-step work, which §21 routes to 'the agent'; the parallel sentence at L56 already uses 'the agent'.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "The swap is mechanical, not a judgement call: §21's contextual split assigns autonomous work to 'the agent' by default, the active-stance override points the same way (reading five context sources and ranking them is an active stance), and the file's own parallel sentence 16 lines later already reads 'The agent reads `observations/`… the same way it reads your ADRs'. Claim, ask and voice are untouched — one noun, registry-driven (`curriculum/vocabulary.md` is canon and §21 is its enforcement), with in-file precedent. The invocation lead-ins that legitimately keep 'Claude' (L44, L58, L42's 'push Claude to keep digging') are not touched.",
  "evidence": "Read rule body via `rule.js student_facing 21` — 'autonomous work -> the agent (default)' and the 2026-08-30 active-stance override quoted. Read walk-and-send-off.md L40 and L56 for the parallel pair. `grep -c 'Claude reads `CLAUDE.md`, `CLAUDE.local.md`, memory, ADRs, and any skills' curriculum/exercises/walk-and-send-off.md` = 1, so the anchor is unique. Backing block L109 registers the `gap-analysis-is-a-question` claim on the bullet's FIRST sentence only, so this edit does not falsify a claim quote.",
  "old_string": "Claude reads `CLAUDE.md`, `CLAUDE.local.md`, memory, ADRs, and any skills you've authored, then ranks the five thin spots that will hurt it most on this task.",
  "new_string": "The agent reads `CLAUDE.md`, `CLAUDE.local.md`, memory, ADRs, and any skills you've authored, then ranks the five thin spots that will hurt it most on this task.",
  "surface": "student-facing-body"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/plan-mode-done-right.md",
  "instance": "ae101--module--plan-mode-done-right.pedagogy.json",
  "rule": "pedagogy 61a",
  "claim": "The Decision-points bullet cites 'calculate-time.js 2026-08-26: 125 min vs the 120 cap' and a '5-minute overrun'; re-running the script returns 123 / over-3, so the dated snapshot is stale by two minutes.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The prose the finding names no longer exists. The judge's own second fix option — 'point at the script's live output rather than a dated snapshot' — is what the current line does: it states the overrun qualitatively ('the Slot line already runs past the 120 cap before this happens, so that give is spent') and hands the number to a live command. No dated figure remains to go stale, which is exactly what §61a(e) wants.",
  "evidence": "`grep -n '125 min\\|2026-08-26\\|5-minute overrun' curriculum/trainings/agentic-engineering-101/plan-mode-done-right.md` returns nothing (empty). Current L162 reads: '- **Exercise runs to 70 min:** … there is no spare beat to take it from — this module has no buffer transition, and the Slot line already runs past the 120 cap before this happens, so that give is spent (`npm run audit:timings` for the current figure).' `grep -c 'npm run audit:timings'` = 1. The only remaining `calculate-time.js` mention is L135's Pacing line, which names the command without quoting a number."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/plan-mode-done-right.md",
  "instance": "ae101--module--plan-mode-done-right.story.json",
  "rule": "pedagogy 61a",
  "claim": "Same Decision-points bullet: the '125 min / over by 5' figure is contradicted by a live re-run returning 123 / over-3, and the Transitions line still shows uncompressed Bridge 5 / Debrief 7.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "Duplicate of the pedagogy row against the same line, and the same prose is gone. The Transitions half of the fix_hint is moot too: the bullet no longer narrates an applied compression — it prescribes one ('Take it from the Bridge (5), then compress the Debrief (7)'), which is consistent with L136 still listing bridge 5 and debrief 7 as the uncompressed baseline. Nothing left to re-derive.",
  "evidence": "Same greps as the pedagogy row: `grep -n '125 min\\|2026-08-26\\|5-minute overrun'` on the module file returns nothing. Read L136 ('Transitions: connections 10 @start · debrief 7 @after:how-instructions-grow · bridge 5 @end') against current L162 — the bullet reads the two as the give to be taken, not as already taken, so the contradiction the row alleges does not exist in the current body."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/plan-mode-done-right.md",
  "instance": "ae101--module--plan-mode-done-right.writing.json",
  "rule": "student_facing 21",
  "claim": "L60 'record where Claude pushes back and where it caves' uses 'Claude' where §21's autonomous-work default assigns 'the agent'.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "Two of §21's own buckets point the other way here. The sentence's subject is plan mode — a named Claude Code surface, which the rule routes to 'Claude' / 'Claude Code' — and the behaviour described is conversational stance under pressure, not files written or tools called. More decisively, the maintainer block already rules on this exact bullet (2026-08-23), and its ruling is about register: 'bully' and 'caves' are kept because 'the ugly verb names the instrument'. The note's own paraphrase renders the beat as 'record where Claude holds and where it caves'. 'The agent caves' flattens the personification the ruling protects. The judge itself files it as 'a maintainer call either way', which is a taste question, not a rule breach — and per §61a(g) a maintainer-held premise is a question, not a work item.",
  "evidence": "Read rule body via `rule.js student_facing 21`; the product/UI-surface bucket and the thinking-vs-acting split are in the rule text. Read plan-mode-done-right.md L60 (Optional challenges bullet) and L95 (the 2026-08-23 accept-note, which quotes the beat back using 'Claude'). Counted the module's own usage: body L1-91 uses 'Claude' for addressee/product ('what Claude writes', 'Claude reads M2 scrollback' in the trainer table) and carries no 10:0 assistant-shaped skew that §21's audit signal names.",
  "anchor": "**\"Try to bully plan mode into a bad architecture\" stands, Antti-directed 2026-08-23.**",
  "accept_note": "**The same bullet keeps its noun: *record where Claude pushes back and where it caves* (2026-09-05).** `check_student_facing.md` §21 sends autonomous work to *the agent*, but this sentence's subject is plan mode, a named Claude Code surface, and what it describes is stance under pressure rather than files written. *The agent caves* flattens the personification the ruling above keeps deliberately — *bully* and *caves* are the same register and were kept together. One noun in an optional challenge. Judges re-file this; the answer is no.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The cited accept-note authorises something adjacent, not this. L95 reads: \"A writing judge filed it under `check_writing.md` §17 (no combative verbs about the agent). Rejected: the bullying is intentional... **so the ugly verb names the instrument**. §17 gained the matching carve-out the same day (a beat whose point IS the bad register), **scoped to the probe, not to surrounding prose. Do not soften this verb.**\" That is a ruling on the VERB under §17; the noun is never adjudicated, and the triage concedes as much (\"its ruling is about register\") before using it anyway. It is also superseded on timing: the note is 2026-08-23, and §21's governing clause is later — \"**Active-stance override (2026-08-30, Antti):** say ***the agent*** whenever the harness+LLM takes on an active stance — even inside invocation/addressee grammar... **Addressee test yields to this override when both apply.**\" Pushing back on a bad architecture and caving under insistence is stance under pressure; §21's own calibration pair puts self-critique (\"Ask the agent what's weakest about its own output\") on the agent side precisely against the addressee test. Second failed claim: the triage cites \"§61a(g)\" as if it were in `check_student_facing.md` — that compendium has no §61 (`rule.js student_facing 61` → \"has no §61\"). The clause is `check_pedagogy.md` §61a(g), and it does not reach: (g) covers premises \"only the maintainer can state... facts about *people*, not text,\" whereas which §21 bucket a sentence falls in is settled by opening two files. The judge's REVISE evidence applies the base rule (\"autonomous work → the agent\"), not taste; the triage promotes the judge's hedged `fix_hint` over its verdict. Cost check: fixing costs one noun in an optional-challenges bullet, and the drafted note (\"Judges re-file this; the answer is no\") would retire §21 on this line permanently."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/plan-mode-done-right.md",
  "instance": "ae101--module--plan-mode-done-right.writing.json",
  "rule": "student_facing 33",
  "claim": "L78 'Optional. The thinking Module 4 asks you to do before you step away from a session.' names a later module's ask, the predicate-leak shape §33 bans, and `## Pre-reads before Module N` does not inherit `## Prework`'s exemption.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "§33's predicate-leak clause targets a clause 'promising what a later beat will do' — the cut example is 'Module 3 draws that boundary for your own repo', where the module is the actor and the predicate books future work. L78's subject is the reading ('the thinking'), and 'Module 4 asks you to do' is a relative clause saying what the reading is for, which is the one thing the rule says a pre-read annotation may say. Decisively, L66 in this same file is the identical construction — 'Optional. The security frame Module 3 works in.' — and the 2026-08-23 §33 pass that cut the Willison clause left both standing. Cutting one and keeping its twin manufactures the outlier the ruling warned about. The deletion test also fails on the judge's own AFTER: 'Optional.' alone leaves the flag-exclusive block with no statement of what it is for.",
  "evidence": "Read rule body via `rule.js student_facing 33`, including the 2026-08-23 pre-reads clause and the predicate-leak paragraph. `git blame -L 66,66` and `-L 78,78` on the module file: both lines last touched 8fa40902d / 5ff124ee9, Antti Tevanlinna, 2026-08-14 — i.e. both existed and were untouched by the 2026-08-23 pass whose decision note sits at L99. Read L62-80 for the two flag-exclusive pre-reads blocks.",
  "anchor": "**Willison pre-read's trailing \"Module 3 draws that boundary for your own repo\" cut, Antti-directed 2026-08-23.**",
  "accept_note": "**Both `## Pre-reads before Module N` framers stay (2026-09-05): *Optional. The security frame Module 3 works in.* and *Optional. The thinking Module 4 asks you to do before you step away from a session.*** Same one-line shape, both untouched by the §33 pass recorded above, which cut a trailing predicate promise instead — *Module 3 draws that boundary for your own repo*, subject a module, predicate booking a later beat's work. In these two the subject is the reading and the module name is a relative clause saying what the reading is for, which is what a pre-read annotation is allowed to say. Cutting one and keeping its twin is the outlier §33 warns about. Judges re-file this; the answer is no.",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The refutation checks out, and on better evidence than the triage cited. `git blame` confirms L66 = 8fa40902d and L78 = 5ff124ee9, both Antti 2026-08-14, and `git show 392c4e656` (the 2026-08-23 pass, which touched L70) shows both framers standing after it. Decisively, that pass's own note gives as its reason: \"Line 86 already carries the M3 framing once, and of AE101's three pre-reads sections this was the only one narrating a later module\" — the ruling that *wrote* §33's pre-reads clause explicitly blesses the section framer as the single sanctioned place the module is named, and cut a trailing clause on a `**Read:**` annotation instead. The note as drafted, though, states the permission in the rule's own governed term — \"which is what a pre-read annotation is allowed to say\" — which reads as licensing a module-naming clause on any pre-read annotation, including the `**Read:**` trailer position the 2026-08-23 pass cut. Narrow it to the framer position it actually adjudicates.",
   "note_fix": "**Both `## Pre-reads before Module N` section framers stay (2026-09-05): *Optional. The security frame Module 3 works in.* (L66) and *Optional. The thinking Module 4 asks you to do before you step away from a session.* (L78).** Both predate the Antti-directed §33 pass recorded above and survived it unedited (`git blame`: 8fa40902d / 5ff124ee9, 2026-08-14; both standing at 392c4e656). That pass cut a trailing clause on a `**Read:**` annotation — *Module 3 draws that boundary for your own repo*, subject a module, predicate booking a later beat's work — and gave as its reason that the section framer *already carries the M3 framing once*, which blesses the framer line as the one place the module may be named. These two lines are that line, one per flag variant; cutting one and keeping its twin manufactures an outlier. **Scope:** this covers these two one-line section framers only. It does not license naming a later module inside a `**Read:**` annotation — the exact clause the 2026-08-23 pass cut — and does not extend to a framer that adds a second module reference or promises what that module will do beyond naming what the reading is for. §33 judges: do not re-file on these two lines."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/reference/mcp-and-connectors.md",
  "instance": "ae101--reference--mcp-and-connectors.technical.json",
  "rule": "platform_and_boundaries 5a",
  "claim": "L60's 'individual engineers don't need per-install admin approval' is a behavioural claim about Atlassian's 3LO admin gating that no dated stamp names specifically; it needs its own stamp or a live probe.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "§5a is scoped to 'what binary actually does' AND a test taking under five minutes. This clause describes Atlassian's own org consent flow — whose first-3LO-consent state and domain allow-list live in a customer's Atlassian admin console — not Claude Code behaviour, and it is not probeable in five minutes without an Atlassian org and admin rights. §5a's own remedy for that case is a marker, not a stamp. The stamp format also does not do what the finding asks: `curriculum/source-freshness-format.md` specifies one line per SOURCE with a claim-anchor, not a line per sentence, and extending the Atlassian anchor to name admin-gating would assert a 2026-07-26 check the checker's note does not record — fabricating verification to close a todo. The page is already inside the re-verification system at `due:cohort`.",
  "evidence": "Read rule body via `rule.js platform_and_boundaries 5a` — 'AND test takes <5 minutes', plus 'Unprobeable in moment -> mark limitation assumed, live-probe owed'. Read mcp-and-connectors.md L60 (the clause) and L141 (the Atlassian Rovo stamp: `[checked:2026-07-26 result:OK due:cohort]`, claim-anchor covers the endpoint and the product list). Read curriculum/source-freshness-format.md lines 1-30: grammar is 'One line per source, inside the file's Source verification block', claim-anchor = 'the number / quote / framework this source backs'.",
  "anchor": "- `[checked:2026-07-26 result:OK due:cohort]` https://support.atlassian.com/rovo/docs/getting-started-with-the-atlassian-remote-mcp-server/",
  "accept_note": "**The 3LO admin-gating sentence is doc-grounded and stays (2026-09-05).** *Individual engineers don't need per-install admin approval* describes Atlassian's own org consent flow, not what the Claude Code binary does, and `check_platform_and_boundaries.md` §5a is scoped to binary behaviour with a sub-five-minute probe — an org's first-consent state is neither. The Atlassian Rovo stamp carries the page at `due:cohort`, which is where a re-read of this clause belongs; `source-freshness-format.md` puts one stamp on a source, not one per sentence, so widening its claim-anchor would assert a check nobody ran. Do not file this as an unstamped runtime-behaviour claim.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The refutation defeats itself. §5a's remedy for exactly the case the triage argues for is not silence: \"Unprobeable in moment → mark `limitation assumed, live-probe owed` + don't harden against it yet.\" The triage quotes this (\"§5a's own remedy for that case is a marker, not a stamp\") and then drafts a note saying \"Do not file this as an unstamped runtime-behaviour claim\" without adding the marker — naming the owed work and then switching the rule off. Two further claims fail. (a) §5a's own enumeration includes \"install affordance,\" and L60's \"individual engineers don't need per-install admin approval ... Admins control product scopes and which domains can connect, not whether individuals can install after that\" is a doc-derived NEGATIVE claim, which §5a calls \"the highest-risk shape.\" (b) \"`source-freshness-format.md` puts one stamp on a source, not one per sentence, so widening its claim-anchor would assert a check nobody ran\" attacks a fix the judge did not require: its fix_hint offers \"the same URL with a claim-anchor naming it, or live-verify once.\" The format doc defines claim-anchor as \"the number / quote / framework this source backs — what breaks if it doesn't hold,\" and L141's anchor covers only the endpoint and product list, so a cohort re-verifier re-checks those, ticks OK, and never re-reads the admin-gating sentence. The page already records honest non-verification elsewhere (`result:BLOCKED` at L146-147), so the cheap remedy exists in-file, and reference pages are exempt from the approval gate — this costs one clause, not a card."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/reference/mcp-and-connectors.md",
  "instance": "ae101--reference--mcp-and-connectors.technical.json",
  "rule": "platform_and_boundaries 38",
  "claim": "L93's plugin-scope precedence / URL-collision claim is covered only by an unstamped end-of-section Sources link, not by an individually dated stamp.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "why": "The verification gap is real but §38 is the wrong home and cannot be closed by inventing a stamp. §38 fires on 'any forward-looking automation beat (what an agent could do, wired later)' and its three tells — an *X against Y* retrieval metaphor, a lifecycle noun from an untouched stage, an unsourced cadence — are all absent; L93 is present-tense install mechanics. The claim does land squarely in §5a's own list ('settings precedence'), and §5a prescribes the marker when the probe cannot run in the moment: 'mark limitation assumed, live-probe owed + don't harden against it yet'. The fix is therefore a dated maintainer note recording the owed probe, not a stamp asserting a check nobody ran and not an edit to the student-facing sentence. It lands in the Source verification block, which no student reads.",
  "evidence": "Read rule bodies via `rule.js platform_and_boundaries 38` and `5a`; walked all three §38 tells against L93 and none matches. Read mcp-and-connectors.md L89-93 (the plugins section), L112 (the section's unstamped Sources line, which does include the plugins-reference URL), and L139-149 (the Source verification block — no stamp for code.claude.com/docs/en/plugins-reference). Anchor uniqueness: `grep -c 'fallback: link the homepage, not the deep link, if it rots again.'` = 1.",
  "anchor": "- `[checked:2026-07-26 result:OK due:cohort]` https://composio.dev/ — [third-party bridge docs] Composio Jira MCP bridge",
  "old_string": "- `[checked:2026-07-26 result:OK due:cohort]` https://composio.dev/ — [third-party bridge docs] Composio Jira MCP bridge: homepage live; the `/content/jira-mcp-server` deep link that 404'd on 2026-05-25 now resolves (200, \"How to connect Jira MCP and Claude Code...\"). fallback: link the homepage, not the deep link, if it rots again.",
  "new_string": "- `[checked:2026-07-26 result:OK due:cohort]` https://composio.dev/ — [third-party bridge docs] Composio Jira MCP bridge: homepage live; the `/content/jira-mcp-server` deep link that 404'd on 2026-05-25 now resolves (200, \"How to connect Jira MCP and Claude Code...\"). fallback: link the homepage, not the deep link, if it rots again.\n\n**Plugin-scope precedence carries no stamp and is owed a live probe.** § *Plugins and marketplaces* claims plugin-provided MCP servers sit above Claude.ai connectors and below the user / project / local-CLI scopes, so a Path 2 install wins a URL collision; the section's `plugins-reference` link is all that backs it. `check_platform_and_boundaries.md` §5a owns this shape — settings precedence is one of its named binary behaviours — and prescribes the marker over a stamp nobody ran: probe with a name-colliding plugin plus a manual `claude mcp add` at the next cohort re-verify, then stamp what the probe returns. §38 does not reach it — no *X against Y* retrieval metaphor, no out-of-stage lifecycle noun, no unsourced cadence, and this is present-tense install mechanics rather than a forward-looking automation beat.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/reference/mcp-and-connectors.md",
  "instance": "ae101--reference--mcp-and-connectors.writing.json",
  "rule": "writing 20",
  "claim": "Line 9's 'Three ways an action becomes callable in a Claude Code session:' states a count the three-item list directly below already holds.",
  "disposition": "EXEMPT_BODY",
  "confidence": "high",
  "why": "The finding is right and the file is under `reference/`, which the maintainer standing-exempted from the approval gate (widened 2026-08-29). §20 governs 'a count in prose adjacent to its list', and this is the rule's own paradigm shape: the numeral sits one line above items 1-3 and buys nothing on the way in. The two carve-outs both miss it — the header numeral at L7 ('## Three paths to a work-app action') is explicitly protected and stays, and the memory-handle carve-out protects the recurring 'Path 1/2/3' labels at distance (L15, L23, L32, L40, L93, L110), which are named labels, not this tally. Fix is the rule's prescribed one: drop the numeral, no hedge quantifier substituted.",
  "evidence": "Read rule body via `rule.js writing 20` — 'A numeral in a `##` header is NOT covered by this rule' and the memory-handle test ('does the numeral appear where the items are NOT visible?') both read directly. Read mcp-and-connectors.md L7-15: header, count line, three numbered items, then the Path-2 callback. `grep -c '^Three ways an action becomes callable in a Claude Code session:$'` = 1, so the anchor is unique. File path is curriculum/trainings/agentic-engineering-101/reference/, matching the standing body exemption.",
  "anchor": "L9: Three ways an action becomes callable in a Claude Code session:",
  "old_string": "Three ways an action becomes callable in a Claude Code session:",
  "new_string": "An action becomes callable in a Claude Code session:",
  "surface": "exempt-page"
 },
 {
  "target_file": "curriculum/lectures/how-instructions-grow.md",
  "instance": "ae101--lecture--how-instructions-grow.writing.json",
  "rule": "writing 3",
  "claim": "The Lecture-meta clause 'The same generalisation sat as a trailing paragraph in the module's save-the-rule section for a few hours and was cut as a duplicate; the slide is its home.' narrates a completed edit.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "why": "Genuine §3, and it is in a maintainer block so it applies directly. The rule's tense test fires cleanly: 'sat … for a few hours' and 'was cut as a duplicate' are transition verbs narrating an edit, and 'used to sit in X' is in the rule's own Cut list verbatim. A reader who never saw the earlier draft loses nothing actionable. The only clause stating present fact is 'the slide is its home', which the replacement keeps. No do-not-reintroduce guard is added: per the 2026-09-05 addendum a guard needs a named live pull, and nothing in the corpus is currently pulling that paragraph back — the module's save-the-rule beat now lives in `extract-the-task-shaping-rule.md`.",
  "evidence": "Read rule body via `rule.js writing 3`; the maintainer-block extension ('What is banned in a maintainer block is the journey') and the Cut list ('used to sit in X', 'was softened', 'renamed from A to B') both read directly, as does the 'do not file §3 on a dated provenance header alone' guard — this finding quotes transition verbs, so it clears that bar. Read how-instructions-grow.md L50 in full; the dated provenance headers in the same paragraph ('Third slide grew a third paragraph 2026-08-23 (Antti-directed)') are left untouched, per the same clause. `grep -c 'The same generalisation sat as a trailing paragraph'` = 1.",
  "anchor": "**Lecture meta:** … Naming only, nothing is written, so no artefact contract.",
  "old_string": "The same generalisation sat as a trailing paragraph in the module's save-the-rule section for a few hours and was cut as a duplicate; the slide is its home.",
  "new_string": "The slide is this generalisation's home.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/lectures/how-instructions-grow.md",
  "instance": "ae101--lecture--how-instructions-grow.writing.json",
  "rule": "writing 11",
  "claim": "Argyris is credited at L16 of this lecture and again in `plan-mode-done-right.md` L50's Key Concepts, two body-prose mentions of the same practitioner inside one module, with no maintainer note invoking the reinforcement carve-out.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "why": "The count is right and the carve-out is exactly the shape this pair has — it was simply never written down, and writing it down is what an accept-note is for. §11's 2026-08-02 carve-out: 'Test is not is this the second mention but does the second do different work… Credit-at-point-of-use in the anchor exercise + recognition-at-the-closer are different jobs.' Here the lecture credits him where the distinction is taught, with the link; the module's Key Concepts names the concept again at module close as the recall handle for the second loop, which is the move M2's rule-extraction beat installs. That is the same pair the rule's own precedent blesses (M1 Klaassen, credit + naming beat). The alternative fix — cutting three words from a Key Concepts bullet — is a student-facing edit needing Antti, for a harm the judge itself scored as mild.",
  "evidence": "Read rule body via `rule.js writing 11`; the 2026-08-02 carve-out, its 'named as such in the maintainer block' condition, and the M1 Klaassen precedent all read directly. Verified both mentions: how-instructions-grow.md L16 '[Chris Argyris called the deeper move double-loop learning](https://hbr.org/1977/09/double-loop-learning-in-organizations)' and plan-mode-done-right.md L50 'which Argyris called double-loop learning'. Confirmed neither file's maintainer block names the pairing (read how-instructions-grow L48-56 and plan-mode-done-right L93-131). Third-mention check: no other Argyris mention in either file's body.",
  "anchor": "**Placement constraint:** Skill use and authoring still land at M3.",
  "accept_note": "**Argyris twice in M2 is deliberate reinforcement, per `check_writing.md` §11's 2026-08-02 carve-out (2026-09-05).** This slide credits him where the distinction is taught, with the link; `plan-mode-done-right.md`'s Key Concepts bullet names the concept again at module close as the recall handle for the second loop — the move the module's rule-extraction beat installs. Two placements, two jobs, the same pair M1 runs with Klaassen. One mention apiece is the budget; a third fires the rule normally.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The carve-out the triage invokes has three conditions and it concedes one is unmet: \"Applies only where the repeated element is load-bearing for retention, **named as such in the maintainer block**, and the two placements differ in function.\" The triage's answer — \"it was simply never written down, and writing it down is what an accept-note is for\" — is circular: the note would manufacture the design intent it then cites as its own authority. I checked both maintainer blocks. `how-instructions-grow.md`'s Lecture meta is unusually detailed (five slides walked one by one) and says only that slide two \"names Argyris's double-loop distinction through the context mechanism the student has already used\" — no pairing, no reinforcement design; `plan-mode-done-right.md`'s block says nothing either. There is no record to write down. The third condition also fails on the merits: the second mention is a `## Key Concepts` breadcrumb (confirmed — L42 is the header, the bullet is L50), and §11 states \"one well-placed cite = the credit, two = bookkeeping.\" A summary bullet repeating the name three slides after the credited lecture adds no new job — cut \"which Argyris called\" and the bullet reads identically. That is materially weaker than the blessed M1 Klaassen precedent, where the second placement is a lecture whose own beat IS the naming. Finally, the triage's closing reason — the fix \"is a student-facing edit needing Antti\" — is a cost argument, not a merits argument; per CLAUDE.md § Approval gate, gating decides who applies, not whether the finding is real. Card it: cut three words, or let Antti declare the pair deliberate and earn the carve-out properly. An agent should not assert his design intent for him."
  }
 },
 {
  "target_file": "curriculum/lectures/agents-that-build-agents.md",
  "instance": "ae101--lecture--agents-that-build-agents.pedagogy.json",
  "rule": "pedagogy 34b",
  "claim": "The Placement note claims 'The file carries *Why the loop survives the model* … as its first slide'; that section was cut and the current first slide is 'The agent stops where your judgement begins'.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The maintainer block was cleaned between the pedagogy judge's pin and now. The Placement note is two lines and carries no slide pointer at all; the first-slide fact it mis-stated is now recorded correctly on its own note. The sibling strategy instance filed the same four stale locators and has already been resolved `fixed` at 2026-09-05 with the same grep evidence — this row is the pedagogy copy of a closed finding.",
  "evidence": "`grep -c 'survives the model'` on curriculum/lectures/agents-that-build-agents.md = 0; `grep -c 'feeds itself'` = 0. Current L36 reads in full: '**Placement:** last in the M6 deck, after the Human close. Order is the module file's (`spot-gaps-build-the-loop.md`), not this file's.' The first slide is named accurately at L54 ('`## The agent stops where your judgement begins` … first slide') and matches body L3. ae101--lecture--agents-that-build-agents.strategy.json carries resolution {settled: fixed, at: 2026-09-05} on the identical locator set."
 },
 {
  "target_file": "curriculum/lectures/agents-that-build-agents.md",
  "instance": "ae101--lecture--agents-that-build-agents.pedagogy.json",
  "rule": "pedagogy 34b",
  "claim": "Same Placement note: 'The lede still tees the handoff… that paragraph awaits a card' — the lede was cut in commit 9edae2ef, so the clause is stale.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The clause no longer exists anywhere in the file. Both 'lede' and 'awaits a card' return zero, and the current Placement note is a two-sentence deck-order statement with no open-card language. Same closed finding as the row above.",
  "evidence": "`grep -c 'lede'` on curriculum/lectures/agents-that-build-agents.md = 0; `grep -c 'awaits a card'` = 0. Read the whole maintainer block L32-56: no 'owed a rewrite', no 'carded', no open-TODO phrasing. The strategy instance's resolution note records the same sweep ('grep finds no survives-the-model, feeds-itself, owed-a-rewrite or carded text in the file')."
 },
 {
  "target_file": "curriculum/lectures/agents-that-build-agents.md",
  "instance": "ae101--lecture--agents-that-build-agents.pedagogy.json",
  "rule": "pedagogy 34b",
  "claim": "The Mood-target hedge names 'the *what this is not* slide' as the anti-triumphal carve-out, but that slide no longer exists in the body.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The pointer the finding names is gone and has already been repointed at the live carve-out, which is the judge's own preferred remedy ('the note should point at the current carve-out'). The Mood target now reads 'The opener slide is the carve-out: the agent runs on what you have expressed, and the line never goes away' — which matches body L3-8 exactly. §34b's premise test does not fire either: the hedge names a drift to watch for, not a premise the section needs propping up.",
  "evidence": "`grep -c 'what this is not'` on curriculum/lectures/agents-that-build-agents.md = 0. Current L48 quoted above; the slide it now names, '## The agent stops where your judgement begins', is body L3 and its bullets at L6-8 carry the anti-triumphal content ('The agent cannot read your mind', 'The line never goes away'). Read rule body via `rule.js pedagogy 34b` to check the premise test before calling it stale rather than live."
 },
 {
  "target_file": "curriculum/lectures/agents-that-build-agents.md",
  "instance": "ae101--lecture--agents-that-build-agents.writing.json",
  "rule": "student_facing 21b",
  "claim": "L16's 'You find it one run at a time' uses 'run' as a bare noun with no execution-record framing, leaving it ambiguous between the licensed sense and the banned agent-sitting sense.",
  "disposition": "REFUTE",
  "confidence": "high",
  "why": "The licensed sense is the only one the sentence can carry. `curriculum/vocabulary.md` § The work states the boundary test — 'are you in it, or reading its return? In it → session; examining the completed delegated execution (its return, transcript, artifacts) → run is legal' — and this sentence is about what accumulates across completed runs ('Nobody can hand you the right information… You find it one run at a time'), not about sitting with the agent. It is the same registered family as M4's *the un-packaged run*, which vocabulary.md prints as a legal example. The maintainer block also records the sentence as Antti's own (2026-09-02, 'title and bridge sentences Antti's') and paraphrases this very clause, and the backing block registers it as claim key `right-information-is-found-one-run-at-a-time`. The judge notes it is re-confirming a prior finding, which is precisely the case an accept-note exists to close.",
  "evidence": "Read rule body via `rule.js student_facing 21b` and the canonical table in curriculum/vocabulary.md § 'The work, session · task · run' — the run row's legal list includes 'the completed delegated execution read from outside (*the un-packaged run*, *a run you weren't watching*, *mid-run* of a send-off)'. Read agents-that-build-agents.md L10-16 for the surrounding paragraph, L56 for the maintainer note ('the goal is the right information, and it is found one run at a time'), and L67 for the backing-block claim key. No other noun-run in the file's body.",
  "anchor": "**`## The right information grounds and makes quality` (title and bridge sentences Antti's, 2026-09-02):**",
  "accept_note": "**`You find it one run at a time` keeps *run* (2026-09-05).** `check_student_facing.md` §21b licenses the execution-record sense and `vocabulary.md` § The work states the test: in it → session, reading its return → run. This sentence is about what completed runs return over time, not about sitting with the agent, so it is the licensed sense — the same family as M4's *the un-packaged run*. The bridge sentences are Antti's, per the note above, and the phrase is a registered claim key in the backing block. Judges re-file this; the answer is no.",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "`vocabulary.md` § The work, run-noun row, legal list in full: \"test/CI/eval executions (*the test run*, *an eval run*); the completed delegated execution read from outside (*the un-packaged run*, *a run you weren't watching*, *mid-run* of a send-off), the execution-record sense ...; verbatim quotes; artifact/path/branch names; **long-running**.\" \"You find it **one run at a time**\" is in none of those forms — every licensed specimen carries framing that names the execution as a record read from outside, which is what makes it legal; a bare countable \"one run\" reads as an attempt/go, and the boundary test (\"are you in it, or reading its return?\") does not resolve it, since finding out about your customers and domain is being in it. So the triage's \"same registered family as M4's *the un-packaged run*\" does not check out against the registry's own list. Its second support fails too: L56 records that Antti wrote the *title and bridge sentences*, which is an authorship attribution, not a §21b adjudication — compare this corpus's real precedent, story-of-module-6.md L77's explicit \"**§21 ratio accept-note (Antti, 2026-09-02)**\", where the maintainer actually ruled on the rule. A backing-block claim key is a slug mirroring the body sentence and licenses nothing. If Antti's wording is to stand, it should stand because he says so on the record; \"Judges re-file this; the answer is no\" manufactures that ruling."
  }
 },
 {
  "target_file": "curriculum/lectures/when-a-plan-is-good.md",
  "instance": "ae101--lecture--when-a-plan-is-good.writing.json",
  "rule": "writing 3",
  "claim": "The maintainer block's criteria-slide note ends with \"Quality per-class SHAs predate both passes; re-audit before ship.\", a transition-verb clause (\"predate\") that is also factually false now that the Quality line records a later audit.",
  "disposition": "STALE",
  "confidence": "high",
  "why": "The prose the finding names is gone from the file. Rule 3's banned-verb list does include `predates`, so the finding was correct when filed — it has since been fixed, and rule 3's own prescription (a closed TODO is deleted, not annotated) was followed, which is why nothing remains to point at.",
  "evidence": "Read curriculum/lectures/when-a-plan-is-good.md in full at HEAD. Current L55 ends \"...Horthy is not a new student-side name — M1's `getting-going.md` already assigns his essay as prework.\" with no trailing clause. `grep -n \"predate\" curriculum/lectures/when-a-plan-is-good.md` returns exactly one hit, L106, inside the boehm-defect-cost source stamp (\"The book predates the web\") — a fact about a 1981 book, not a transition claim, and not the flagged string. `grep -rn \"per-class SHAs predate\" curriculum/` returns zero hits under curriculum/lectures/; the string survives only in the derived snapshot curriculum/evals/body-views/ae101--lecture--when-a-plan-is-good.expanded.md L98 (which is what the judge read) and inside this instance JSON's own evidence field. `git log --oneline -3` on the file shows 9b809eed \"Maintainer blocks state what is true, not the diff that made it true\" as the landing commit."
 },
 {
  "target_file": "curriculum/lectures/when-a-plan-is-good.md",
  "instance": "ae101--lecture--when-a-plan-is-good.writing.json",
  "rule": "prompts 17",
  "claim": "`when-a-plan-is-good-1` declares `permission-mode: plan`, which trips §17's structural trigger, and the file carries neither an in-fence `No preamble.` suppressor nor a body primer after the fence.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "accept_note": "**`check_prompts.md` §17 does not fire on `when-a-plan-is-good-1` (2026-09-05).** The prompt declares `permission-mode: plan` but requests no plan — it asks the agent to describe its own behaviour change, so there are no plan items for a preamble to bury and the top-of-response slot §17 protects is not contested. Both of §17's paths are closed here by design: a body primer is the output-preview line this slide deliberately does not carry, and an in-fence `No preamble.` would prime the raw self-report the student is meant to read. Do not add a suppressor, do not add a primer, do not re-raise §17 against this prompt.",
  "why": "§17's trigger is mechanical but its stated harm is not: the rule names the plan-mode worst case as \"2-4 sentences of 'I'll now read X and propose…' eat student's highest-attention top-of-response slot **before plan items appear**\", and keeps plan mode in scope because \"there the top-of-response slot is genuinely contested.\" This prompt produces no plan items — the narration and the answer are the same text, and the answer is the artifact. Both remedies are foreclosed by decisions already on record in this file: the L59 Opening-slide note says the body \"does not restate what the prompt's answer will show\", naming the cut output-preview line (\"Watch what comes back. Sometimes Claude names the read-only state…\") — that line *is* §17's path (b); and L63 records the prompt as load-bearing precisely because \"the student reads the read-only permission state back from the agent's own report\", which an in-fence `No preamble.` would prime. Reading the trigger as the judge does condemns 5 of 5 corpus instances, which is the shape `check_writing.md §3`'s own consequence-for-judges clause warns is a rule being read too wide.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js prompts 17` and read the full body including the 2026-09-03 narrowing. Read curriculum/prompts/when-a-plan-is-good-1.md: frontmatter `permission-mode: plan`, one-sentence body \"How did your behaviour change now that we are in plan mode? Did your base prompt change, or what?\" — no plan requested, no suppressor. Corpus blast radius: `grep -rl \"permission-mode: plan\" curriculum/prompts/` returns 5 files (when-a-plan-is-good-1, push-back-on-the-plan-1, -2, -2-original, -2-askuserquestion); `grep -ci \"no preamble\"` returns 0 on all five. Read curriculum/exercises/push-back-on-the-plan.md around every fence (L25-29, L53-57, L70-72, L83-85) — no skim-past cue at any of them, so the corpus carries neither path anywhere. Checked the three prompts §17 already declines by name (ae101-m4-commit-starting-point, fix-tests-first-1, fix-tests-first-3): none declares `permission-mode: plan`, so they are precedent for the retired sub-step half only, not for this. Read the maintainer block L59 and L63 in full for the two foreclosures.",
  "anchor": "**Opening slide (2026-08-25, Antti-directed): one prompt-led slide, `## Ask plan mode what changed`.**",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "§17 as Antti narrowed it two days before this triage is a bright line, not a purposive test: \"**Structural trigger (narrowed, Antti 2026-09-03): plan mode ONLY.** ... Plan mode still **MUST** carry one of: (a) in-fence `No preamble. <first action>` suppressor as prompt's last line; (b) body primer after fence naming preamble + skim-past cue\". `curriculum/prompts/when-a-plan-is-good-1.md` declares `permission-mode: plan`; `grep -ci 'no preamble'` on it returns 0, and L9-13 of the lecture is header, one frame clause, fence — no primer after it. The rule's own declined list (`ae101-m4-commit-starting-point`, `fix-tests-first-1`, `fix-tests-first-3`) contains no plan-mode prompt, so the narrowing exempted the non-plan-mode half and deliberately kept this half bound; the sibling adjudication in `the-handoff-prompt.md` (\"the §17 preamble row falls under the rule as narrowed to plan mode\") relies on that prompt NOT being plan mode. Both claimed foreclosures also fail on reading the notes themselves: L59 bans restating \"what the prompt's answer will show\" and names the cut output-preview line (\"Watch what comes back. Sometimes Claude names the read-only state…\") — that is an answer preview, not §17's skim-past cue about narration, so it authorises something adjacent, not this; and L63's ground for the prompt being load-bearing (\"the student reads the read-only permission state back from the agent's own report\") is not primed by `No preamble.`, which suppresses intent-narration and says nothing about permission state. The 5-of-5 blast-radius argument is a two-day-old MUST's backlog, not evidence of over-reading; `check_writing.md §3`'s \"condemns 83% of the files\" clause is scoped to a pre-existing corpus convention, which this is not. Triage's own confidence was medium."
  }
 },
 {
  "target_file": "curriculum/exercises/orient-and-introspect.md",
  "instance": "ae101--exercise--orient-and-introspect.writing.json",
  "rule": "student_facing 17",
  "claim": "The header at L31, \"Ask what Claude skipped, and why\", makes Claude the actor of an embedded clause inside a header, which the 2026-09-02 veto bans.",
  "disposition": "CARD",
  "confidence": "high",
  "before": "## Ask what Claude skipped, and why",
  "after": "## Ask what got skipped, and why",
  "why": "§17's actor clause is unambiguous — \"The student is a header's only actor — Claude does not act in a header\" — and its worked ban, *\"Read the shapes\"*, never *\"Read the shapes Claude proposes\"*, is structurally identical to this header: student verb plus an embedded clause in which Claude acts. The compounded entry's rationale is satisfied by the swap rather than violated by it: \"the body names Claude's part, the header names yours\", and the body one line down already says \"Ask Claude what it read and what it skipped\" (L36), so no information is lost. Carded rather than applied directly because §17 supplies eight candidate replacements and none for this sentence — \"what got skipped\" / \"what the read skipped\" / \"what didn't get read\" all work, the header is a projected slide title, and the choice is voice, not geometry.",
  "risk": "A header rename changes the slide title and the generated anchor id; built workbooks under site/clients/** hold the old anchor until regenerated (this file's own maintainer block already records exactly that situation for the closing beat's `id=\"read-the-unread-slice\"`). Two sibling headers carry the identical shape and are deliberately NOT swept here — walk-and-send-off.md L71 (\"Push back when Claude drifts from the codebase\") and module-2-prework.md L19 (\"Check what Claude can read\"); if Antti takes this one, those two owe the same call.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js student_facing 17` and read the actor clause plus the canonical-section carve-out (this is a non-canonical author-added section, so the carve-out does not reach it). Read the compounded entry memory/compounded/2026-09-02-student_facing-headers-never-claude-as-subject.md in full — Antti's veto is on the wording shape, not only on the referent fix. Read the exercise's maintainer block: no note covers L31 (the L64 note covers only the closing beat `## The slice Claude didn't load`, and only its no-prompt/no-verb status). Corpus scope: `grep -rn \"^#\\{2,3\\} .*Claude\" curriculum/exercises/ curriculum/lectures/ curriculum/trainings/agentic-engineering-101/*.md` returns 7 hits, of which 3 carry this exact student-verb-plus-Claude-clause shape. `grep -cF \"## Ask what Claude skipped, and why\"` = 1. No internal link targets the anchor (`grep -rn \"ask-what-claude-skipped\" curriculum/ scripts/` empty; hits only in already-built site/clients HTML).",
  "anchor": "## Ask what Claude skipped, and why",
  "card": {
   "order": null,
   "outcome": "applied",
   "note": "Antti supplied the wording (the agent). Swept every AE101-owned header with the shape; §17 narrowed to ban the product name, not any non-student actor.",
   "at": "2026-09-05"
  }
 },
 {
  "target_file": "curriculum/exercises/orient-and-introspect.md",
  "instance": "ae101--exercise--orient-and-introspect.writing.json",
  "rule": "student_facing 21",
  "claim": "Body Claude-count 13 vs agent-count 1 trips §21's audit signal, and the two introspection asks at L34 and L36 should say \"the agent\" under the active-stance override.",
  "disposition": "CARD",
  "confidence": "medium",
  "before": "- Claude can introspect on what it did and why, including what it chose not to read.",
  "after": "- The agent can introspect on what it did and why, including what it chose not to read.",
  "why": "Half the finding holds and half does not, and only the holding half is carded. L34 is third-person prose about autonomous acting — what it *did*, what it *chose not to read* — which §21's base contextual split already routes to **the agent** (\"autonomous work → the agent (default — 'the agent reviews the session, writes one named pattern')\"); it needs no appeal to the 2026-08-30 override at all, and the file's audit signal genuinely fires (13:1 measured). L36, \"Ask Claude what it read and what it skipped.\", is declined: it is invocation/addressee grammar asking for a factual self-report, and §21's own calibration pair reserves the override for self-critique (\"Ask the agent what's weakest about its own output\") as against a plain service ask (\"Ask Claude to review the session\"). To stop the next judge re-filing L36, land this one-line accept-note in the maintainer block in the same pass: **\"§21 on `Ask Claude what it read and what it skipped` (L36): plain service ask in addressee grammar, so Claude, per §21's own calibration pair. The active-stance override covers self-critique, not a factual self-report. Do not re-raise.\"** Carded rather than applied because L34 is the exercise's central teaching claim and the Claude-heavy register is this file's deliberate voice.",
  "risk": "The `<!-- backing -->` claim row at L105 (`agent-can-introspect-on-what-it-skipped`) quotes this sentence verbatim; it must be updated in the same edit or the claim-to-body binding goes stale (`check_writing.md §3`, keep-notes bind to their passage). The sentence therefore appears twice in the file — anchor on the leading `- ` to hit only the body bullet. Swapping L34 alone also puts \"the agent\" one line under a header that still says \"Claude\"; if Antti takes the §17 header card too, they should land together.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js student_facing 21` and read the base split, the 2026-08-30 active-stance override and its calibration pair, plus the audit signal. Verified the signal mechanically over the body only: `sed -n '1,59p' | grep -oiE '\\bclaude\\b' | wc -l` = 13, same for `\\bagent\\b` = 1 — the 10:0-shaped signal the rule names. Read L34 and L36 in context. `grep` for the sentence returns 2 hits (L34 body, L105 backing claim); `python3` count of the `- `-prefixed form returns exactly 1, so the anchor above is unique. Maintainer block carries no Claude/agent register note.",
  "anchor": "- Claude can introspect on what it did and why, including what it chose not to read."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.writing.json",
  "rule": "writing 3",
  "claim": "The Artefact-contracts table's M6 consuming cell pairs a present-tense negative (\"no body line names it\") with the positive fact beside it, which the 2026-09-05 addendum names as a phantom.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "old_string": "M6 (no body line names it; the handoff prompt authors further skills after the module)",
  "new_string": "M6 (the handoff prompt authors further skills after the module)",
  "why": "Rule 3's 2026-09-05 addendum is written against exactly this shape: a present-tense sentence whose subject is a thing that does not exist, with the escalation ladder (annotate the decline → negative constraint → rule) to refuse. Its second test — \"is the positive fact already stated nearby?\" — is answered inside the same parenthesis, so the negative half carries nothing a reader who never saw the old cell would miss. Its first test also fails: nobody is proposing that a body line name it, so there is no live pull the guard protects against. The cell sits inside the `<!-- maintainer -->` block (opens L113), so the fix lands directly; no student reads the artefact-contracts table.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js writing 3` and read the full body including the 2026-09-05 addendum and the curriculum-maintainer-blocks extension. Read curriculum/trainings/agentic-engineering-101/earn-the-trust.md L113 (`<!-- maintainer -->`) through L208, confirming L185 is inside the block. `grep -cF \"M6 (no body line names it; the handoff prompt authors further skills after the module)\"` = 1, so the anchor is unique. The surviving clause is a plain present-tense fact about the handoff prompt, which is what rule 3 asks the fix to leave behind.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.writing.json",
  "rule": "writing 20",
  "claim": "L109's \"Two signatures, earned.\" states a count the two-clause enumeration immediately beside it already holds.",
  "disposition": "CARD",
  "confidence": "high",
  "before": "Two signatures, earned. Your staff engineer sees a test-strategy skill tuned to this codebase, your CISO sees a STRIDE decision with an ADR.",
  "after": "Your staff engineer sees a test-strategy skill tuned to this codebase, your CISO sees a STRIDE decision with an ADR.",
  "why": "§20's adjacency test decides it: the numeral sits in the sentence directly before the two items it counts, so the memory-handle carve-out (which requires the count to be reused where the items are NOT visible) does not apply, and neither does the header carve-out — this is body prose, not a `##`. §20's body-prose default is explicit: \"often counting is just NVA\" (Antti 2026-08-22), drop it and do not reach for a replacement quantifier. The proposed fix cuts the whole lead sentence rather than just the numeral, because the judge's own \"Signatures, earned.\" leaves three sentences in a `## Next` that `check_slides.md`'s Next rule caps at two — cutting resolves both open todos on this section with one edit, and the house default on a curriculum line-read is CUT before rewrite. What the sentence asserted survives whole in the clause that follows it.",
  "risk": "\"Two signatures, earned.\" is the module's mood beat — it lands the earned-trust mood target verbatim (\"something my staff engineer and my CISO can sign off on\"), and the closer loses its drumroll. If Antti wants the beat kept, §20 is still satisfied by dropping only the numeral, and the `## Next` sentence count then needs its own accept-note.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js writing 20` and read the header carve-out (Antti 2026-08-22, which names `## Five things a good plan has`, not body prose), the memory-handle carve-out, and the body-prose default. Read L106-111 as rendered. `grep -cF \"Two signatures, earned. Your staff engineer\"` = 1. Cross-checked against the slides todo on the same section (ae101--module--earn-the-trust.slides.json, rule_index null) which independently flags this sentence as a third sentence in `## Next`; the two findings converge on one span.",
  "anchor": "Two signatures, earned. Your staff engineer sees a test-strategy skill tuned to this codebase, your CISO sees a STRIDE decision with an ADR.",
  "card": {
   "order": null,
   "outcome": "applied",
   "note": "Cut the whole lead sentence, clearing writing §20 and the ## Next two-sentence cap together.",
   "at": "2026-09-05"
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.writing.json",
  "rule": "student_facing 9",
  "claim": "WYL bullet 2 at L23 uses \"name what your first read missed\", which §9's *name is a smell* clause flags; the swap to a precise verb loses nothing.",
  "disposition": "CARD",
  "confidence": "medium",
  "before": "- **Invoke** curated access-control-analysis and STRIDE skills on a feature you're shipping (each as a subagent, fresh context): name what your first read missed, pick one threat worth hardening against, and write the decision as an ADR in your repo's convention",
  "after": "- **Invoke** curated access-control-analysis and STRIDE skills on a feature you're shipping (each as a subagent, fresh context): spot what your first read missed, pick one threat worth hardening against, and write the decision as an ADR in your repo's convention",
  "why": "§9's *name is a smell* clause (Antti 2026-08-14) names the diagnostic and the replacement list — codify · write down · pick · decide · list · spot · find · choose — and \"spot\" is the member that fits a read-and-notice move. The swap test passes: nothing is lost, and the LO's other two verbs (pick, write) are already precise, so spot/pick/write reads as one deliberate triple rather than one vague verb and two sharp ones. None of §9's three exemptions reaches this line: it is body text, not a prompt body, not a trainer note or trainer-push quote, and not an established coinage. Carded rather than applied because the direction is Antti's but the specific verb is not, and this is a Bloom LO — the one line a student reads before deciding whether the module is for them.",
  "risk": "\"name\" is load-bearing on this module's trainer surface, which stays untouched by rule: the Ex1 push \"name one of each\", the Debrief push \"name a branch, not a rule\", and the per-phase table's Ex1 forcing function *\"name the surface delta\"* all keep it. After the swap the LO and the trainer's own words diverge on the same beat. Rule 9 exempts trainer quotes explicitly, so this is a cosmetic divergence, but a trainer reading both may notice.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js student_facing 9` and read the *name is a smell* clause including its exemption list. Read L20-27 as rendered. Swept the whole student-facing body for the verb: `sed -n '1,111p' | grep -nE '\\bname[sd]?\\b'` returns 3 hits — L23 (this one), L57 (\"its parts named\", not an instruction verb), L69 (\"If the agent names something missed\", agent as subject, not the student). So L23 is the only live hit in body; everything else the judge could have reached sits in the maintainer block. `grep -cF \"name what your first read missed\"` = 1.",
  "anchor": "- **Invoke** curated access-control-analysis and STRIDE skills on a feature you're shipping (each as a subagent, fresh context): name what your first read missed, pick one threat worth hardening against, and write the decision as an ADR in your repo's convention"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.writing.json",
  "rule": "student_facing 21",
  "claim": "L55 says \"Claude\" for the same self-audit moment that Key Concepts L79 renders as \"the agent\", so the two passages disagree within one file.",
  "disposition": "CARD",
  "confidence": "medium",
  "before": "Claude is reading the skill file fresh and your m3-security scrollback.",
  "after": "The agent is reading the skill file fresh and your m3-security scrollback.",
  "why": "The finding holds, but on narrower ground than the judge argued, and the narrowing is why it is carded rather than applied. §21's base contextual split already routes this sentence: it is third-person prose about autonomous work — reading a file and a scrollback unprompted — which the rule sends to **the agent** by default, no appeal to the 2026-08-30 override needed. The file already says \"the agent\" for this exact beat twice (L69 \"If the agent names something missed\", L79 \"Ask the agent what's weakest about its own output\"), so this is an internal inconsistency inside one module, not a corpus sweep. What does NOT hold is the audit signal: this file's body runs Claude:agent at 11:6, nowhere near the 10:0 shape §21 names, so the finding stands on the internal split alone.",
  "risk": "The same paragraph's second \"Claude\" — \"Claude may open with a plan before showing the diff\" — is arguably a Claude Code product-behaviour observation (preamble is what students see in that product), which §21 routes to Claude. Swapping one and not the other leaves a mixed register inside four sentences. This should be decided both-or-neither; if Antti takes the swap, `grep -cF \"Claude may open with a plan before showing the diff\"` = 1 gives the second anchor.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js student_facing 21` and read the base split, the override and its calibration pair. Read L53-55 and L73-81 to compare the two renderings of the self-audit beat. Measured the audit signal over student-facing body only: `sed -n '1,111p' | grep -oE '\\bClaude\\b' | wc -l` = 11 and `\\bagent\\b` = 6 — so the judge's implied \"assistant-shaped framing\" premise is not supported and I did not rely on it. `grep -cF \"Claude is reading the skill file fresh and your m3-security scrollback.\"` = 1. Maintainer block carries no register note for this section (L115's lean-pass note covers only the cut \"(bar higher)\" gloss).",
  "anchor": "Claude is reading the skill file fresh and your m3-security scrollback."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.writing.json",
  "rule": "student_facing 33",
  "claim": "L57 makes the arc the grammatical subject (\"Three modules of loop work sit behind you… the near half of the map\"), matching the rule's cited cut precedent.",
  "disposition": "CARD",
  "confidence": "medium",
  "before": "Three modules of loop work sit behind you. Before you clear the session, the near half of the map, seen whole with its parts named.",
  "after": "Before you clear the session, the near half of the map, seen whole with its parts named.",
  "why": "§33's subject test splits this line cleanly and only the first half fails it. \"Three modules of loop work\" is the arc as subject with no other function — the same shape as the rule's own landed cut, \"Together they map the window you steer for the rest of the training.\" The second sentence stays: its subject is what the linked lecture delivers, which is include framing rather than cartography, and it carries the \"before you clear the session\" ordering that the `## Clear the session` section immediately below depends on. It also matches the corpus convention for a closing-lecture lead-in — run-the-first-experiment.md L31 does the same thing (\"The map has a far side. Before the first long session, see the country you are entering.\"). §33's deletion test passes on sentence one (the neighbours join cleanly) and fails on sentence two. This is a cut rather than the judge's proposed rewrite, per the house default that deletion beats rewrite on a curriculum line-read.",
  "risk": "The section loses its opening beat and the skeptical-read paragraph runs straight into the ordering clause; if Antti wants the arc marker kept, the alternative is the judge's rewrite naming what the lecture does. Note also that this whole lecture-include sits under `## Read the sharpen with a skeptical eye` rather than in its own section — untouched here, but a reader may find the link's placement odd either way.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js student_facing 33` and read the subject test, the deletion test, the exempt-sections boundary (this line sits in an author-added section, not in `## What You'll Learn` / `## Connections` / `## Bridge` / `## Next` / `## Prework`, so no exemption applies) and the predicate-leak clause. Read L53-59 as rendered. Checked the corpus convention for closing-lecture lead-ins: `grep -rn -B2 \"^\\[Lecture:\" curriculum/trainings/agentic-engineering-101/run-the-first-experiment.md` shows the same two-sentence shape at L31-33. `grep -cF \"Three modules of loop work sit behind you.\"` = 1.",
  "anchor": "Three modules of loop work sit behind you. Before you clear the session, the near half of the map, seen whole with its parts named."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.writing.json",
  "rule": "prompts 38",
  "claim": "The sentence immediately preceding the `{{prompt:ae101-m3-sharpen-skill}}` fence is a grab-quotes aside that does not mirror the prompt's opening clause.",
  "disposition": "CARD",
  "confidence": "medium",
  "before": "Ask Claude to sharpen the skill's weakest section from invocation evidence. Then integrate a rule into `./CLAUDE.local.md` if one earned itself. If a moment from m3-quality's authoring scrollback matters that the skill didn't capture, the m3-quality window is still open at this point, switch over and grab quotes, paste them into m3-security's chat before sending the prompt.",
  "after": "If a moment from m3-quality's authoring scrollback matters that the skill didn't capture, the m3-quality window is still open at this point, switch over and grab quotes, paste them into m3-security's chat first. A rule for `./CLAUDE.local.md` goes in too, if one earned itself. Ask Claude to read the test-strategy skill against this session's invocation evidence, find the section that underdelivered, and rewrite it in place.",
  "why": "§38 binds the sentence *immediately preceding* the fence, and here that is the grab-quotes aside — a setup instruction that shares no verb or noun-phrase with the prompt's opening \"Read the test-strategy skill file at…\". Moving the aside to the front of the paragraph and letting the lead-in land last fixes the adjacency; tightening the lead-in's verbs from \"sharpen\" (a word absent from the prompt) to read/find/rewrite gives the phrasal rhyme §38 actually asks for, against the prompt's own \"Read the test-strategy skill file… find the one section… Rewrite that section in place.\" §38a does not apply — nothing about this prompt is a surprise, and no easter-egg note exists in the maintainer block.",
  "risk": "The reorder inverts the paragraph's emphasis: L47 establishes \"The skill sharpen is mandatory; the rules-file update is opportunistic\", and the AFTER puts the opportunistic rules-file sentence before the mandatory sharpen sentence. The \"goes in too, if one earned itself\" phrasing is meant to keep the opportunism audible, but if that reads as flattening the mandatory/opportunistic split, the cheaper fix is to move only the aside and leave the two lead-in sentences in their current order — that satisfies §38's adjacency but leaves the verb rhyme unfixed.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js prompts 38` and read 38 plus the 38a surprise-pedagogy carve-out. Read curriculum/prompts/ae101-m3-sharpen-skill.md in full — it opens \"Read the test-strategy skill file at `~/.claude/skills/test-strategy/SKILL.md`.\" then \"First: find the one section of the skill where invocation evidence shows it underdelivered… Rewrite that section in place.\" Read earn-the-trust.md L43-51: L49 is a single paragraph line of three sentences, L51 is the fence, so the immediately-preceding sentence is unambiguously the aside. Confirmed the maintainer block carries no `easter egg — DO NOT SPOIL IN BODY` note, so 38a is not in play. L49 is unique as a whole line.",
  "anchor": "Ask Claude to sharpen the skill's weakest section from invocation evidence. Then integrate a rule into `./CLAUDE.local.md` if one earned itself."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.pedagogy.json",
  "rule": "pedagogy 47",
  "claim": "The per-phase table covers only the linked exercises' phases; the module's own three body-embedded forcing functions (L51, L65, L71) get no row, against the table's own caption.",
  "disposition": "REFUTE",
  "confidence": "medium",
  "accept_note": "**`check_pedagogy.md` §47's table holds the exercise phases; the module's own closing beats are not phases (2026-09-05).** `## Sharpen the skill from evidence` and `## Clear the session` are accepted-by-design closing beats rather than unextracted exercises (see the §52a note above), so neither ships a phase for the table to carry a row for. Their failure-and-recovery pairs live where they do more work than a maintainer row would: `## Read the sharpen with a skeptical eye` names the sharpen beat's tells (preamble before the diff, a cosmetic edit dressed up as substantive) and its escalation ladder (over-flag, skip-preamble, fresh-session) in student-facing body, and the converge beat carries *compound it now or accept the loss* in body as its own recovery. The caption's parenthetical quotes what §47 requires; it is not a coverage claim about this table, and the same boilerplate caption is shared by four sibling modules. Do not re-raise §47 against the module-body prompts.",
  "why": "§47's requirement is that the *maintainer block* names (a) the dominant failure and (b) one escape hatch for each phase with a forcing function — not that the table names them. Two things then defeat the finding. First, the caption the judge measures against is corpus-wide boilerplate reproducing the rule's own text, identical across five AE101 modules; reading it as a per-file coverage claim would REVISE all five. Second, §47 binds *phases*, and this file's own 2026-08-13 accept-note rules that its module-prose fences are closing beats and not unextracted exercises — so there is no phase there to owe a row. The rule's stated harm (a trainer with no lookup) is not present: the sharpen beat's failure modes and three escalation moves sit in student-facing body at `## Read the sharpen with a skeptical eye`, which is strictly more visible to a trainer than a maintainer row, and the converge beat's recovery sits in body one line under its fence. Residual, and I am recording it rather than hiding it: the `/clear` fence has no failure/hatch pair anywhere in the file. I judge that de minimis — its forcing function is typing `/clear`, and M4 opens a fresh session by its own Session widget regardless — but that is the one part of the finding the accept-note declines rather than answers.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js pedagogy 47` and `pedagogy 16` for the forcing-function definition and the \"not every student move needs a fence\" boundary. Read earn-the-trust.md L188-197 (the table) and L43-71 (the three module-body fences). Caption blast radius: `grep -rn \"Per-phase failure mode\" curriculum/trainings/agentic-engineering-101/*.md` returns 5 files carrying the identical parenthetical (earn-the-trust L188, getting-going L170, plan-mode-done-right L185, learn-from-the-test L234, run-the-first-experiment L166) — and plan-mode-done-right is precedent that the caption gets locally *extended*, not narrowed. Verified the sharpen beat's pair in body at L55 (tells: \"Claude may open with a plan before showing the diff, and sycophancy can dress up a cosmetic edit as a meaningful one\"; hatches: over-flag ask, skip-preamble ask, fresh-session subagent) and the converge beat's recovery at L69. Verified the §52a accept-note at L117 rules the Clear-the-session fences \"accepted, not an unextracted exercise\".",
  "anchor": "**Per-phase failure mode + escape hatch** (per `check_pedagogy.md` rule 47 — every phase shipping a forcing function names its dominant failure and one recovery move):",
  "verification": {
   "ruling": "FINDING_SURVIVES",
   "reason": "The refutation's central citation does not say what it is quoted as saying. earn-the-trust.md:117 covers only `## Clear the session`, and it explicitly excludes the sharpen beat: \"(The corpus has further module-prose fences the 2026-08-13 call did not rule on: this file's own `## Sharpen the skill from evidence` at L51, `learn-from-the-test.md` L33, and `prework.md` L47/L55. Each still owes §52a an answer.)\" So the drafted note's \"`## Sharpen the skill from evidence` and `## Clear the session` are accepted-by-design closing beats ... (see the §52a note above)\" would write a false claim into the block and shield a fence the maintainer left open. The rule's own unit defeats the rest: §47 says \"Diagnostic per forcing function: 'what does student get wrong here?' + 'what move catches them?' Either unanswered → REVISE\" — per forcing function, so \"not a phase, no row owed\" does not reach L51/L65/L71, all of which ship prompts (§16's definition of a forcing function). The triage concedes the residue itself — \"the `/clear` fence has no failure/hatch pair anywhere in the file\" — and then drafts \"Do not re-raise §47 against the module-body prompts,\" which suppresses the part it admits it never answered."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.pedagogy.json",
  "rule": "pedagogy 52a",
  "claim": "`grep -n '{{prompt:' earn-the-trust.md` returns 3 hits; the 2026-08-13 accept-note covers only L65 and L71 and explicitly records that L51's `## Sharpen the skill from evidence` fence still owes §52a an answer.",
  "disposition": "MAINTAINER",
  "confidence": "medium",
  "old_string": "`prework.md` carries the same call in its own maintainer block for its section 5 fences. (The corpus has further module-prose fences the 2026-08-13 call did not rule on: this file's own `## Sharpen the skill from evidence` at L51, `learn-from-the-test.md` L33, and `prework.md` L47/L55. Each still owes §52a an answer.)",
  "new_string": "`prework.md` carries the same call in its own maintainer block for its section 5 fences.\n\n**`## Sharpen the skill from evidence` is accepted on the same terms (2026-09-05, eval-triage adjudication — not an Antti ruling; reverse it if he disagrees).** One prompt, no `**Session**` widget, the module's own Compound beat: strictly smaller than the two-prompt wrap accepted above and far short of §52a's four-prompt-plus-widget precedent, and the deck gives it one ordinary slide rather than an unbreakable block. Do not extract, do not re-raise. (Two corpus fences remain unruled: `learn-from-the-test.md` L33 and `prework.md` L47/L55.)",
  "why": "The finding is correct — the file flags its own gap — so refuting it would be refusing real work. The fix lands in the maintainer block, which is not student-facing, so it applies directly. The adjudication itself is forced by §52a's own reasoning as this file already applies it: the precedent §52a was built from is a four-prompt section carrying a `**Session**` widget, big enough that the deck hands it one unbreakable slide; `## Sharpen the skill from evidence` is one prompt with no widget, strictly smaller than the two-prompt wrap Antti already accepted at L117 and than `prework.md`'s accepted section 5. The slides class corroborates the deck half — it judged this file at chunk scope and flagged chunks 8 and 12, never chunk 5. The note is dated and attributed honestly to this triage rather than back-attributed to Antti, because he did not rule on this fence and the file says so; that phrasing still stops the next judge re-filing while leaving the call reversible. Note the replacement drops the brittle \"at L51\" locator for this file's own fence in favour of the section name.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js pedagogy 52a` and read the four-edit extraction procedure and the precedent it names. `grep -n '{{prompt:' curriculum/trainings/agentic-engineering-101/earn-the-trust.md` returns 3 hits: L51, L65, L71 — the judge's count reproduces. Read L117 in full; the parenthetical does explicitly name this file's own L51 fence as unruled. Read the sibling accept-note at prework.md L88 for the convention and the reasoning shape ('one screening prompt plus its fallback… extracting it would give prework an exercise file before the training starts'). Read L43-51 to confirm the section is one prompt, no `**Session**` widget, preceded by a `**Note**` widget and two paragraphs. Checked ae101--module--earn-the-trust.slides.json `chunk_map` findings: chunks 8 and 12 only, so no slides-class objection to chunk 5's size. `python3` count of the old_string in the file = 1.",
  "surface": "maintainer-block"
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.slides.json",
  "rule": "slides 16",
  "claim": "Key Concepts bullet 6 (L81) restates chunk 5's L47 almost clause-for-clause and is therefore a class-(a) doctrine restatement that has not earned its slot.",
  "disposition": "REFUTE",
  "confidence": "high",
  "accept_note": "**Key Concepts bullets restate the module's own beats — that is the section's job, not a `check_slides.md` §16(a) doctrine restatement (2026-09-05).** `curriculum/module-shape.md` § Key Concepts is explicit: *reminders, not new material*; every bullet recaps a beat the student already met, and \"a bullet carrying information that appears nowhere else is the section teaching, which is the one thing it must not do.\" The skills-carry-conventions / rules-carry-your-working-style bullet recaps `## Sharpen the skill from evidence`, so it passes the spec's own test (name the beat it recaps). Replacing it with something that section has not already said is precisely the move the spec forbids. §16 fires on unfenced connective tissue between teaching beats; do not re-raise it against a Key Concepts bullet whose recapped beat can be named.",
  "why": "The finding inverts the canonical spec for the section it is judging, and its own fix_hint asks for the one thing that spec bans. `curriculum/module-shape.md` § Key Concepts requires every bullet to recap a beat from this module's own exercises and lectures; a bullet with no such beat is \"new material wearing a recap's clothes.\" The judge's remedy — \"replace with something chunk 5 hasn't already said\" — would manufacture exactly that defect. §16's literal per-bullet test is also per-slide (\"what does it add that the slide does not already hold\"), and nothing else on the Key Concepts slide carries the skills-versus-rules split. Applying §16(a) cross-slide to a recap section is a reductio: all six of this module's KC bullets restate an earlier beat by construction, so the reading would delete the section. Two secondary points. The bullet is not in fact a clause-for-clause copy — it sharpens \"the rules-file update is opportunistic\" into the testable \"the rule only goes in if it earned itself\". And check_slides' own calibration note treats `## Key Concepts` as an enumeration section governed by a ≤6-bullet cap; this section sits at exactly 6, so a cut would take it below the house norm to satisfy a rule that does not reach it.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js slides 16` and read the three classes plus the closing scope sentence, \"the classes breed in unfenced connective tissue.\" Read `curriculum/module-shape.md` § Key Concepts (grep -n -A8 'Key Concepts') for the reminders-not-new-material spec and its per-bullet name-the-beat test. Compared L47 and L81 side by side in the file; also read all six KC bullets L76-81 and confirmed every one recaps an earlier beat (L76 → skills-from-the-frontier lecture, L77 → map-the-access-surface/threat-model-with-stride, L78/L79 → author-test-strategy-skill, L80 → the ship-personal-first beat, L81 → `## Sharpen the skill from evidence`). Checked `grep -n \"Key Concepts\" check_slides.md`: the only hits are the projection-scope note and the ≤6 calibration at line 71 — no §16 carve-out either way, and no clause bringing recap sections into scope.",
  "anchor": "- Skills carry the codebase's conventions; your rules file carries how you work. Sharpening the skill is the mandatory move; the rule only goes in if it earned itself",
  "verification": {
   "ruling": "ACCEPT_NOTE_OVERREACHES",
   "reason": "The refutation's primary leg holds: §16's per-bullet test is per-slide — \"what does it add that **the slide** does not already hold\" — and no other Key Concepts bullet (L76-L81 read in full) carries the skills-versus-rules split, while `module-shape.md` § Key Concepts L78 mandates recap (\"Reminders, not new material… Test: for each bullet, name the beat it recaps\"). But two things in the draft overreach. It says \"do not re-raise **it** [§16]\" unqualified, when only §16(a) was adjudicated — classes (b) design commentary and (c) aphorism kicker can still legitimately fire on a KC bullet. And it reads check_slides' ≤6 calibration as a floor (\"a cut would take it below the house norm\"), when line 71 says \"Six is roughly what the corpus does unwatched, **not a ceiling imposed on it**\" — it is neither ceiling nor floor, and the judge's fix_hint led with \"Cut\", which the triage's reductio silently drops.",
   "note_fix": "**Key Concepts bullets restate the module's own beats — that is the section's job (2026-09-05).** `curriculum/module-shape.md` § Key Concepts requires every bullet to recap a beat the student already met (*reminders, not new material*; test = name the beat it recaps). The skills-carry-conventions / rules-carry-your-working-style bullet recaps `## Sharpen the skill from evidence`, and no other bullet on this slide carries that split, so it earns its slot on §16's own per-slide test. `check_slides.md` §16(a) is overruled for this bullet — do not re-file the L81/L47 overlap. §16(b) and (c) still reach Key Concepts bullets, and the bullet stays cuttable on ordinary economy grounds: ≤6 is a calibration, not a floor."
  }
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.slides.json",
  "rule": "slides (no rule_index; judge_owned) — the `## Next` is-two-plain-sentences block in check_slides.md",
  "claim": "Chunk 12's `## Next` runs three sentences plus an appended second paragraph, where the rule allows two plain sentences and nothing else.",
  "disposition": "CARD",
  "confidence": "medium",
  "before": "Optional, when your sessions start running heavy: [The context ceiling and token efficiency](trainings/agentic-engineering-101/supplementary/the-context-ceiling.md) on keeping the context window lean.",
  "after": "(cut in full, along with the blank line above it — `## Next` ends at \"...the hardening decision included.\")",
  "why": "This card covers only the appended-pointer half of the finding; its three-sentences half is discharged by the `check_writing.md §20` card on the same section, which cuts \"Two signatures, earned.\" and lands the two mandated sentences. The rule's text is flat — \"Nothing else earns the closer\" — and the count binds a section literally headed `## Next`, which this is. Nothing is lost by cutting: the pointer is the third copy of this link, and the second-nearest copy is in the Prework of run-the-first-experiment.md, the very next file the student opens, carrying the same clause almost verbatim (\"And when your sessions start running heavy, [The context ceiling and token efficiency]… on keeping the context window lean.\"). The file's own L208 note establishes the reasoning precedent for exactly this: a forward-pointing copy in `## Next` was removed on the grounds that the supplementary is reachable from the training index's supplementary row. Carded rather than applied because deleting a student-visible link is a reachability decision, not page geometry.",
  "risk": "If a student reads M3's close and never reaches M4's Prework — the cut-arc case, or a self-study student stopping after M3 — the pointer is gone from their path. The mitigations are that `how-instructions-grow.md` (M2's closing lecture) already links the same supplementary at L46, so the pointer has been seen before M3 closes, and the training index carries the supplementary row. If Antti would rather relocate than cut, note that `## Pre-read before Module 4` currently reads \"Optional. One read.\" and would need that line changed to take a second.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js slides 16`; the `## Next` block prints as part of that rule's body, so the finding's rule text is real compendium text rather than judge invention despite the null rule_index — I read it in full, including the 2026-08-15 clarification that the count binds only a section literally headed `## Next`. Read earn-the-trust.md L106-111 as rendered. Reachability check: `grep -rn \"the-context-ceiling\" curriculum --include=\"*.md\"` (excluding evals/) returns the link at run-the-first-experiment.md L12 and how-instructions-grow.md L46 besides this one. Read the file's own L208 do-not-re-add note for the precedent. `python3` count of the before-string with its leading blank line = 1.",
  "anchor": "Optional, when your sessions start running heavy: [The context ceiling and token efficiency](trainings/agentic-engineering-101/supplementary/the-context-ceiling.md) on keeping the context window lean."
 },
 {
  "target_file": "curriculum/trainings/agentic-engineering-101/earn-the-trust.md",
  "instance": "ae101--module--earn-the-trust.strategy.json",
  "rule": "strategy_tie_in 8",
  "claim": "The strategy doc's four-read-practitioner-arc line says da Costa's backpressure essay lands before M6, flag-gated to sequences that run M6, which the module's own Antti-directed `## Pre-read before Module 4` placement contradicts.",
  "disposition": "MAINTAINER",
  "confidence": "high",
  "old_string": "Willison *Designing agentic loops* before M4, da Costa's backpressure essay before M6 (flag-gated to sequences that run M6)",
  "new_string": "Willison *Designing agentic loops* before M3, da Costa's backpressure essay before M4",
  "why": "THIS EDIT DOES NOT LAND IN THE REPO. The defect is doc-side, and the target is the absolute path `/Users/anttitevanlinna/Library/CloudStorage/GoogleDrive-tevanlin@gmail.com/My Drive/bosser/strategy/content-strategy-agentic-engineering-101.md`, line 298 — the `target_file` field names the curriculum file the todo was filed against, not the file to edit. The doc is not student-facing, so it applies directly. §8's fix instruction is \"correct the span, never empty it\", and the pre-read-placement claim is doc-owned (§8 lists cross-module sequencing claims and pre-read rationale under DOC OWNS), so the span is corrected rather than deleted; the wiring parenthetical goes, because flag-gating per cut is exactly what §8's DOC MUST NOT RESTATE list sends to the leaf maintainer blocks. Both spans in the sentence move together, and that is not scope creep — fixing only the da Costa clause would leave the doc listing two different reads \"before M4\", a contradiction §8 explicitly warns about (\"fixing one leaves the doc saying both\"). The Willison span is independently verified stale by its owning file's stamp, which carries an Antti ruling.",
  "evidence": "Ran `node curriculum/evals/scripts/rule.js strategy_tie_in 8` and read the split test, the DOC OWNS / DOC MUST NOT RESTATE lists, and the twin-grep instruction. Verified the da Costa placement against every file that names the URL: earn-the-trust.md L104 assigns it under `## Pre-read before Module 4` with the L119 Antti-directed 2026-08-21 note and the L125 stamp declaring \"**This is the module that owns the check**\"; run-the-first-experiment.md L14 echoes it in Prework and L186 records the echo as \"the `## Prework` echo of the M4 gap read, unconditional across every cut\"; plan-mode-done-right.md L80/L126 carries the `no-module:earn-the-trust` copy for cuts that drop M3. Nothing in the corpus places it before M6. Verified the Willison span the same way: plan-mode-done-right.md L125 states \"**This file owns the check and assigns the essay in the six-module arc only**, as the second `## Pre-reads before Module 3` entry (Antti, 2026-08-21)\" and then \"**Not assigned at M4 in any cut.**\" Twin check per §8: `grep -n \"before M[0-9]\" ` over the doc returns 9 lines; only L298 makes this claim, and the doc's own `## Pre-reads before M6` block at L470 names the Klaassen piece alone — so the doc already contradicts itself, and there is no second copy to fix. `python3` count of the old_string in the doc = 1.",
  "surface": "anchor-not-found"
 }
]