# Smoke scenario — one turn, cwd-independent prompt.
# Asks Claude to list its available tools. Proves the loop closes:
# launch tmux → send prompt → Stop hook → sentinel → capture transcript.
ae101-agent-loop-tools-list
