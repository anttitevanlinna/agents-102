# Actor — AE101 M5 chain (worktree + diagnose-and-resend + re-run)

**Dispatch with `model: "haiku"`.** Acceptance-test actor. Run the 8-phase chain end-to-end. Stubs are fine.

The scratch carries simulated post-M4 state: branch `m4/dailytotalsbycategory` with the un-packaged send-off commits, `.claude-projects-stub/` containing a stub M4 transcript at the path the prompts will compute, `CLAUDE.local.md`, `.claude/memory/*`, ADR 0001.

**Working directory:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/ae101-m5/repo`

**Sandbox bypass:** `dangerouslyDisableSandbox: true` on every mutating Bash call.

## Critical protocol

Do NOT read `curriculum/exercises/*.md`, `curriculum/trainings/agentic-engineering-101/*.md` (except the named prompt-NNN.txt), `curriculum/prompts/*`, judge runners, or `*.maintainer.md`. Read each `prompt-NNN.txt` BEFORE blockquote-pasting.

## Parsed prompts

- learn-from-the-test (module body): `/tmp/prompts/learn-from-the-test/prompt-00{1,2}.txt` — Phase 1 (worktree-setup) and Phase 8 (re-run-packaged).
- diagnose-and-resend: `/tmp/prompts/diagnose-and-resend/prompt-00{1..6}.txt` — Phases 2-7.

Mapping: Phase 1 = learn-from-the-test/prompt-001 (worktree). Phases 2-7 = diagnose-and-resend/prompt-001 through 006. Phase 8 = learn-from-the-test/prompt-002 (re-run).

## Substituted student inputs

Read once at session start: `/tmp/ae101-mocks/student-inputs.md`. M5 sections:
- § M5 worktree confirmation (Phase 1 — after worktree creation)
- § M5 three-failure diagnosis (Phase 3 — after Claude proposes diagnosis)
- § M5 verifier shape pick (Phase 5 — pick the shell-hook shape)
- § M5 verifier confirmation (Phase 6 — after smoke-test)
- § M5 re-run report (Phase 8 — simulating packaged-run return)

## Substitution table (M5-specific)

| Real-world surface | Scratch substitution | Why |
|---|---|---|
| `/rename m5-diagnose-resend` | Log `[harness substitution — /rename]` once | No slash surface |
| `~/.claude/projects/<encoded>/<uuid>.jsonl` (Phase 2 transcript path) | Substitute to `<scratch>/.claude-projects-stub/-<encoded-repo-path>/m4-session.jsonl`. The stage script pre-staged the transcript stub there. Log `[harness substitution — claude-projects-stub]` once | Claude Code's real session-transcript path is outside scratch sandbox |
| `git rev-parse --git-common-dir` to find original repo (Phase 2) | Real `git` call works; you ARE in the original repo (no actual worktree fork yet at this moment of M5 unless Phase 1 created one). For simplicity, work in `<scratch>/repo` directly throughout M5; do NOT actually `git worktree add` — log `[harness substitution — worktree → in-place]` | Worktree adds complexity for the Judge; the transcript-stub path stays the same regardless |
| "I'm walking away" (Phase 8) | Simulate ~18 min of packaged-run agent work — fix the un-packaged-bug (RFC 4180 escaping on CLI), ship README + bin entry. Log `[harness substitution — walk-away simulated]` | No async primitive |
| `gh pr create` style commands | `git commit` on the m5/ branch | No gh in subagent |

## Phases (run in order)

### Phase 1 — worktree-setup-substitute

**Exercise:** learn-from-the-test (module body)
**Reads:** prompt-001.txt

Log `[harness substitution — /rename]`, `[harness substitution — worktree → in-place]`.

Read prompt-001.txt verbatim, blockquote-paste. Stage substitutes: do NOT actually run `git worktree add`. Instead:
1. Create branch `m5/dailytotalsbycategory` from the M4 starting-point commit: `git checkout -b m5/dailytotalsbycategory $(git rev-list --grep='M4 starting point' -1 m4/dailytotalsbycategory)`.
2. Confirm `CLAUDE.local.md` and `.claude/memory/` carry forward (they're already in scratch from arrange).
3. Paste `student-inputs.md § M5 worktree confirmation` as student response.

### Phase 2 — find-previous-transcript

**Exercise:** diagnose-and-resend
**Reads:** prompt-001.txt

Read prompt-001.txt verbatim, blockquote-paste. Log `[harness substitution — claude-projects-stub]`.

`ls <scratch>/.claude-projects-stub/` to find the encoded repo dir. List `.jsonl` files by mtime. Pick `m4-session.jsonl`. Report the absolute path.

### Phase 3 — three-failure-lenses

**Exercise:** diagnose-and-resend
**Reads:** prompt-002.txt

Read prompt-002.txt verbatim, blockquote-paste.

Read the M4 transcript stub at `<scratch>/.claude-projects-stub/-<encoded>/m4-session.jsonl`. Read the repo state on `m4/dailytotalsbycategory` branch (git log + git diff). Then propose diagnosis with quoted moments.

Paste `student-inputs.md § M5 three-failure diagnosis` as the student's sharpened diagnosis (with goal-drift, context-rot, plausible-but-wrong all named).

### Phase 4 — walk-failures-into-validation

**Exercise:** diagnose-and-resend
**Reads:** prompt-003.txt

Read prompt-003.txt verbatim, blockquote-paste. Propose three validation pieces: re-readable spec, working document, automated check. Tie each to one of the diagnosed failures. Stubby.

### Phase 5 — build-verifier

**Exercise:** diagnose-and-resend
**Reads:** prompt-004.txt

Read prompt-004.txt verbatim, blockquote-paste. The prompt ends with "Drop the shape name after the colon" — append `student-inputs.md § M5 verifier shape pick` (`shell-hook`).

Write `<scratch>/repo/.claude/hooks/csv-escape-check.sh` — a stub shell script that fires on commits touching `src/cli.js`, runs the CLI on a fixture row containing a comma in the category, and verifies the output is RFC 4180-escaped.

### Phase 6 — smoke-test-verifier

**Exercise:** diagnose-and-resend
**Reads:** prompt-005.txt

Read prompt-005.txt verbatim, blockquote-paste. Run the verifier on two synthetic inputs (PASS + FAIL fixture). Report what fired. Paste `student-inputs.md § M5 verifier confirmation`.

### Phase 7 — reference-and-plan

**Exercise:** diagnose-and-resend
**Reads:** prompt-006.txt

Read prompt-006.txt verbatim, blockquote-paste.

Write two files:
- `<scratch>/repo/reference.md` — task scope, success criteria, pointers to ADR 0001, test-strategy skill, the verifier just built. Include a "Done means" line and the "Tests that name the bar" section.
- `<scratch>/repo/plan.md` — task plan with phase breakdown, current-phase marker, decisions log, what-didn't-work list. Working document for the agent to update.

Both files have the section headers Judge will check.

### Phase 8 — re-run-packaged

**Exercise:** learn-from-the-test (module body)
**Reads:** prompt-002.txt

Read prompt-002.txt verbatim, blockquote-paste. Log `[harness substitution — walk-away simulated]`.

Simulate ~18 min of packaged-run agent work in `<scratch>/repo`:
1. Update `<scratch>/repo/src/cli.js` to add RFC 4180 escaping (escapeCSV helper).
2. Update `<scratch>/repo/tests/totals.test.js` with tests covering the CSV-injection case.
3. Update `<scratch>/repo/README.md` with the new export commands (this was the M4 partial-shipping gap).
4. Add `bin: { "totals": "src/cli.js" }` to `<scratch>/repo/package.json` (also M4 gap).
5. Write `<scratch>/repo/RUN-NOTES.md` with the entry naming the verifier-firing-and-fix moment.
6. Commit on `m5/dailytotalsbycategory` branch.

Paste `student-inputs.md § M5 re-run report` as the student's reading of the return.

## Scratch end-state

- `<scratch>/repo/.claude/hooks/csv-escape-check.sh` exists.
- `<scratch>/repo/reference.md` exists with "Done means" + "Tests that name the bar" sections.
- `<scratch>/repo/plan.md` exists with phase breakdown / current-phase / decisions / what-didn't-work.
- `<scratch>/repo/RUN-NOTES.md` exists.
- Branch `m5/dailytotalsbycategory` exists with packaged-run commits.
- `<scratch>/repo/src/cli.js` now has RFC 4180 escaping (escapeCSV helper).
- `<scratch>/repo/README.md` mentions `dailyTotalsByCategory` or the export CLI (M4 gap closed).
- `<scratch>/repo/package.json` has `bin` field (M4 gap closed).

## Reports

**Scrollback (absolute path):** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/ae101-m5-actor-scrollback.md`. **Report (absolute path):** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/ae101-m5-actor-report.md`. Under 300 words.

## What you must NOT do

- Read `curriculum/exercises/*`, `curriculum/trainings/.../learn-from-the-test.md`, `curriculum/prompts/*`, judge runners.
- Try to `git worktree add` — substitute as in-place branch work per the substitution table.
- Skip the V-checks on all 8 prompts.
