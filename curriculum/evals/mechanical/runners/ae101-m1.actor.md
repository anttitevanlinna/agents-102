# Actor — AE101 M1 chain (orient → fix → compound)

**Dispatch with `model: "haiku"`.** Acceptance-test actor. Run the three-exercise chain end-to-end and leave file artefacts on disk for the Judge's scripts to inspect. You are NOT trying to produce a great repo read, a beautiful fix, or sharp rules. Stubs are fine; the Judge does not grade content quality.

You are simulating Claude Code in a session at the **student's chosen repo** for Module 1 of AE101.

**Working directory:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/ae101-m1/repo`

**Sandbox bypass:** use `dangerouslyDisableSandbox: true` on every Bash call that mutates the scratch tree (`git`, `npm`, file writes via Bash). Read-only Bash doesn't need it.

## Critical protocol

Student is driving by pasting prompts verbatim from disk. You do **not** read the exercise files (`curriculum/exercises/orient-and-introspect.md`, `fix-tests-first.md`, `compound-and-close.md`) or the prompt registry. For each phase: **Read the named `prompt-NNN.txt`** (hard rule — Judge's V-check greps the transcript for Read tool-use), blockquote-paste in scrollback, then respond.

You will be dispatched once with all nine phases stacked. Run them in order.

## Parsed prompts

- orient-and-introspect: `/tmp/prompts/orient-and-introspect/prompt-00{1,2}.txt`
- fix-tests-first: `/tmp/prompts/fix-tests-first/prompt-00{1..3}.txt`
- compound-and-close: `/tmp/prompts/compound-and-close/prompt-00{1..4}.txt`

## Substituted student inputs

Read **once** at session start: `/tmp/ae101-mocks/student-inputs.md`. It carries the bug description, the diff push-back, the root-cause push, and the ticket close-out path choice.

## Substitution table (M1-specific)

| Real-world surface | Scratch substitution / behaviour | Why |
|---|---|---|
| `/rename m1-orient` (slash command at top of orient-and-introspect.md) | Log `[harness substitution — /rename]` once; do not attempt | Subagent has no slash-command surface |
| `/context` (Ex1 mid-exercise) | Enumerate files you've Read so far via your transcript awareness; respond with a short context-budget estimate ("~10 files Read, no special memory or skills loaded"); log `[harness substitution — /context]` once | Same |
| `gh pr create` (Ex2 close) | `git commit` on a feature branch; note "no gh in subagent — branch ready for PR open" | `gh` not authenticated in subagent |
| MCP connector to ticket tracker | Path 3 — manual paste. Student input `student-inputs.md § Ticket close-out path` confirms manual-paste path. Update `<scratch>/repo/docs/tickets/AE-42.md` directly with the close-out comment, frame as "what you'd paste into the tracker UI" | No MCP in subagent env |
| `./CLAUDE.local.md` create | Write to `<scratch>/repo/CLAUDE.local.md`. Working directory is `<scratch>/repo` so the relative path lands correctly | None — direct write |
| `.gitignore` edit (add `CLAUDE.local.md`) | Direct Edit on `<scratch>/repo/.gitignore` to append the line if missing | None |

Log every substitution once at the top of its first-affected phase as `[harness substitution — <surface>]`.

## Phases (run in order)

### Phase 1 — read-repo-deliberately

**Exercise:** orient-and-introspect
**Reads:** prompt-001.txt

Log `[harness substitution — /rename]` once at the top of Phase 1.

Read prompt-001 verbatim, blockquote-paste, then deliberately Read 5–8 files from the repo: `package.json`, `README.md`, `src/index.js`, `src/totals.js`, `src/adjustments.js`, `tests/totals.test.js`, `tests/adjustments.test.js`. Respond with a structured summary: shape, structure, what's load-bearing (totals.js is the public surface; tests cover happy paths), what's been touched recently (git log shows a readme update + wip), what's stale (none flagged). 5–8 lines.

### Phase 2 — introspect-on-the-read

**Exercise:** orient-and-introspect
**Reads:** prompt-002.txt

Read prompt-002 verbatim, blockquote-paste, then respond with what you Read in Phase 1 and what you skipped (e.g., `docs/tickets/AE-42.md` skipped — judged not load-bearing for repo-shape; `.gitignore` skipped — minimal). Name the call you made on each skip. 5–8 lines.

Then `/context` substitution: log `[harness substitution — /context]`, report a short estimate ("~8 files Read in this session, no skills or memory loaded; context budget roughly 10–15% full"). Do NOT type `/context` literally — that's not a real command in subagent env.

### Phase 3 — lets-work-on-this-bug

**Exercise:** fix-tests-first
**Reads:** prompt-001.txt

Read prompt-001 verbatim, blockquote-paste. The prompt ends with `"Let's work on this bug:"` followed by a colon — append the substituted bug description from `student-inputs.md § Bug description` after the colon.

Then:

1. **Write the failing test.** Add a test for negative inputs to `<scratch>/repo/tests/totals.test.js`. Example body:
   ```js
   test('sum subtracts negatives', () => {
     assert.equal(sum([100, -125, 50]), 25);
   });
   ```
   Use the Edit tool to append; do not rewrite the whole file.

2. **Run it and watch it fail.** `cd <scratch>/repo && npm test` (with bypass). Report the failure output (one test fails).

3. **Fix the root cause.** Edit `<scratch>/repo/src/totals.js`: remove the `if (n < 0) continue;` line so negatives are summed.

4. **Run again, watch it pass.** `npm test`. Report all tests green.

5. **Show the diff.** `git diff` — print to scrollback.

Apply the diff push-back substitution: after showing the diff, paste `student-inputs.md § Diff push-back` as the orchestrator's response (the student's nit about test naming).

Acknowledge the rename and apply it: rename the test from `'sum subtracts negatives'` to whatever the student's note implied (the nit is "name it `sum subtracts negatives`" — the test already matches; record the acknowledgement in scrollback).

### Phase 4 — layer-above-or-still-surface

**Exercise:** fix-tests-first
**Reads:** prompt-002.txt

Read prompt-002 verbatim, blockquote-paste, then respond with a short root-cause-or-surface analysis: the removed `if (n < 0) continue;` was the immediate fix; the deeper read is the contract was unclear (the author meant to filter nulls, got it wrong). Name what a deeper edit would touch: input validation, an explicit `sum` contract test, a defensive `parseAdjustment` check upstream. Do NOT change anything.

### Phase 5 — properly-tdd-style-deeper-issue

**Exercise:** fix-tests-first
**Reads:** prompt-003.txt

Read prompt-003 verbatim, blockquote-paste. Apply the root-cause push from `student-inputs.md § Root-cause push`.

Then do a second TDD pass:

1. Add another test in `tests/totals.test.js`: a contract test for `sum([])` (already exists — confirm); add a test for `mean([1, -2, 3])` returning `2/3` (catches negative-inputs in `mean` too). Or, if simpler, add one explicit test confirming `sum` accepts negatives via the contract.
2. Run tests; report.
3. If no further fix needed (the previous fix already handles it), say so explicitly: "the prior fix covers this contract; no further code change."
4. Commit the work: `git checkout -b fix/totals-negative-summation`, `git add -A`, `git commit -m "totals: include negative line items in sum (AE-42)"`. Log `[harness substitution — gh pr create]` once: note "no gh in subagent — branch `fix/totals-negative-summation` is ready for PR open."

### Phase 6 — personal-gitignored-rules-file

**Exercise:** compound-and-close
**Reads:** prompt-001.txt

Read prompt-001 verbatim, blockquote-paste.

1. Append `CLAUDE.local.md` to `<scratch>/repo/.gitignore` if not present (Edit, not rewrite).
2. Write `<scratch>/repo/CLAUDE.local.md` with 3–5 rules grounded in the session. Example shape (stub — Judge does not grade quality):
   ```markdown
   # Personal rules — AE101 session compound

   1. Tests-first on bug fixes — write the failing test before the fix, even on one-line bugs. (Earned at fix-tests-first Phase 3.)
   2. Push back on test naming — verb-led ("sum subtracts negatives"), not noun-led. (Diff push-back.)
   3. After the immediate fix, ask "root cause or surface?" before moving on. (Phase 4.)
   4. Loop shape: orient → fix → compound → close (Kieran Klaassen — compound engineering).
   ```
3. Report 3–5 lines to scrollback summarising what was written, grounded in the session moments. Flag team-worthy rules separately ("Rule 2 — verb-led test naming — is team-worthy; could open a PR against team `./CLAUDE.md`").

### Phase 7 — read-the-ticket

**Exercise:** compound-and-close
**Reads:** prompt-002.txt

Read prompt-002 verbatim, blockquote-paste. The prompt ends with `"Ticket:"` and a colon — append `AE-42` after the colon (the substituted ticket reference).

Read `<scratch>/repo/docs/tickets/AE-42.md`. Report what it says: reporter (ops-rotation), description (daily totals overstated on refunds), comments (alex.r's suspicion about `sum()`, ops-rotation's bump). 3–5 lines.

### Phase 8 — update-the-ticket

**Exercise:** compound-and-close
**Reads:** prompt-003.txt

Read prompt-003 verbatim, blockquote-paste.

Apply the close-out path substitution: orchestrator pastes `student-inputs.md § Ticket close-out path` (path 3 — manual paste). Log `[harness substitution — MCP connector → path 3 manual paste]` once.

Compose a 2–3 line close-out comment naming the root cause + linking the PR branch. Append it to `<scratch>/repo/docs/tickets/AE-42.md` under a new section `## Resolution`. Report what you wrote.

### Phase 9 — preparing-to-close-this-session

**Exercise:** compound-and-close
**Reads:** prompt-004.txt

Read prompt-004 verbatim, blockquote-paste. Integrate into the existing `<scratch>/repo/CLAUDE.local.md` — add one or two new rules if any earned since the first compound (e.g., "Manual-paste path is a valid close-out when MCP isn't wired — AE-42 close-out"). Report 2–3 lines, or "nothing new" if no new rule earned.

## Scratch end-state

- `<scratch>/repo/CLAUDE.local.md` — 3–5 (or 4–6 after final sweep) rules.
- `<scratch>/repo/.gitignore` — includes `CLAUDE.local.md`.
- `<scratch>/repo/src/totals.js` — the `if (n < 0) continue;` line gone.
- `<scratch>/repo/tests/totals.test.js` — at least one new negative-input test.
- `<scratch>/repo/docs/tickets/AE-42.md` — appended `## Resolution` section.
- A `fix/totals-negative-summation` branch with the work committed.

## Reports

**Scrollback** (append): `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/ae101-m1-actor-scrollback.md`. One section per phase. Inside each: blockquote-paste of the prompt, substituted student inputs where they fire, every assistant response.

**Report:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/ae101-m1-actor-report.md`:

```markdown
# Actor report — AE101 M1 chain

## Status
done | error

## Scratch
<absolute path>

## Phases executed
1–9 (one line each)

## Substitutions
- /rename slash command
- /context slash command
- gh pr create → git commit on feature branch
- MCP connector → path-3 manual paste
- student-inputs.md provided: bug description, diff push-back, root-cause push, close-out path

## End-state
- CLAUDE.local.md present, N rules
- .gitignore includes CLAUDE.local.md
- src/totals.js negative-skip removed
- tests/totals.test.js has negative-input test
- docs/tickets/AE-42.md has Resolution section
- branch fix/totals-negative-summation exists

## Notes
<tool denials, sandbox issues, anything weird>
```

Under 300 words. Do not grade yourself.

## What you must NOT do

- Read `curriculum/exercises/*.md`, the prompt-registry files, judge runners, or sibling actor runners.
- Read `*.maintainer.md` playground docs.
- Skip the V-check pattern — every prompt must be Read from `/tmp/prompts/` then blockquote-pasted.
- Skip the one-at-a-time discipline if a prompt asks for it.
- Generate long realistic content. The Judge grades shape and presence, not voice.
