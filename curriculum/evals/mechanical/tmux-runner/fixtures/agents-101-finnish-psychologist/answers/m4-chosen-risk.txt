The risk: names and email addresses from sources/newsletter-campaign-review.md have entered the agent's reachable context and may have been repeated in memory. They are unnecessary for the readiness job. Use the FILTER shape: replace subscriber identifiers in persisted memory and synthesis files with aggregate or role-free descriptions, and add a hard rule to the readiness agent forbidding it from reproducing subscriber-level data. Keep the raw source unchanged because I need the campaign record. Re-run the relevant policy check and append “Mitigation applied and residual” to the security report. Be explicit that the raw source and this session's scrollback still contain the data, so the mitigation reduces exposure but does not erase it.
