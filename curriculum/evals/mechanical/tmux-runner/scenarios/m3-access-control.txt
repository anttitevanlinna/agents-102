# M3 (AE101) security side — Map the access surface, step 2.
# Invokes the access-control-analysis skill on the feature described in the plan.
# The tail after the key is appended to the prompt body as the student-supplied
# "the feature is:" follow-on. The agent will Read the plan file itself.
map-the-access-surface-2 The feature is described in the plan at /Users/anttitevanlinna/.claude/plans/harmonic-stargazing-wren.md — read it first, then invoke the skill on what's described there.
