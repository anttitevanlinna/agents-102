# Regenerable audit outputs (scripts/build-prompt-audit-context.js + the
# AE101 prompt-rule-coverage workflow). Rebuild with the scripts; don't track.
prompt-audit-context.*.json
mechanical-findings.*.json
eval-coverage.*.json
coverage-sweep/
