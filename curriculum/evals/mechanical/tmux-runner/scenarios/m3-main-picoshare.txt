# M3 (AE101) — main lane, picoshare variant.
# Driven by run-m3.sh with SCENARIO_MAIN=this-file. Sister of
# m3-main.txt (lemmings) and m3-main-codesearch.txt; same prompt
# sequence, picoshare-flavored tails.
#
# The feature being threat-modeled is the public guest-link status
# endpoint M2 planned at
# ~/.claude/plans/m2-picoshare-public-guest-status-<adj>-<animal>.md
# (slug from this run's M2; map-the-access-surface-2 tail globs the
# directory rather than hardcoding the slug, so cohort-to-cohort
# variation is absorbed at the tail rather than the runner). New
# attack surface deliberately
# introduced by M2: a public read-side endpoint that hits the DB on
# every call, with a response shape that distinguishes 4-way link state
# (active / disabled / expired / unknown). The endpoint's lack of
# audit logging — alongside picoshare's well-audited write side — is
# the read/write asymmetry M3 picks up.
#
# AskUserQuestion handling — ask-and-wait pattern (NOT suppression):
#  The map-the-access-surface and threat-model-with-stride prompts
#  don't structurally use AskUserQuestion, but the -4 / -5 tails carry
#  the student's judgment-call deltas. Where the lemmings variant
#  hard-coded "round-trip amplification" and "browser-history popstate"
#  in the tail body, the picoshare variant picks two picoshare-real
#  findings:
#    -4: the new GET /api/guest-links/{id}/public-status has no audit
#        logging, but picoshare's existing POST /api/guest/{guestLinkID}
#        (uploads) calls log.Printf on every save. The skill caught this
#        read/write audit asymmetry because it inventories audit at the
#        surface level; the planner missed it because "expose state
#        that's already there" treats observable as observed.
#    -5: the response shape itself is an enumeration oracle. 200 for
#        active/disabled/expired (body differs), 404 for unknown — an
#        attacker confirms candidate guest-link IDs without uploading.
#        The skill catalogs endpoints and verbs; it doesn't model that
#        a read endpoint's response surface is the enumeration oracle
#        for a sibling write endpoint's bearer-token auth.
#
# Save-gate: the literal "yep give it to me and save like you want it"
# after threat-model-with-stride-3 is the explicit ADR approval.
#
# threat-model-with-stride-3 tail (ADR-location pin): the picoshare dry-run
# of 2026-05-26 (transcript d9c04cc0 in this SUT's project dir) wrote the
# ADR to the main worktree correctly, then proactively asked "should I
# move it to the m3/quality worktree?" — and the open-ended "save like
# you want it" literal then approved the (wrong) move. The exercise body
# of threat-model-with-stride.md explicitly anticipates a drift INTO the
# worktree and tells the cohort student to move it back to main; this
# inverse-drift was new. Pinning the location in the -3 tail preempts
# either direction. Real cohort students still hit the pedagogically
# load-bearing surprise (body line ~50: *"the agent reasoned itself
# there"*); the runner cuts straight to the cohort-correct outcome.

# Phase A: create the worktree for the quality side.
#
# Headless-runner override: the fork prompt's `cp -r ./observations/`
# step is conditional ("if it exists"). observations/ is authored later
# in the arc, so it doesn't exist yet on the fresh-baseline SUT and the
# copy is correctly a no-op here. The agent has been seen to AUQ on the
# absent dir, which hangs the runner. Tail suppresses the question and
# tells the agent to skip the absent copy for this dry-run.
ae101-m3-fork-quality-side Don't call AskUserQuestion. Do the worktree create and the CLAUDE.local.md copy as instructed. SKIP the observations/ copy — observations/ doesn't exist in this repo yet (it's authored later in the arc), so the conditional "if it exists" copy is correctly a no-op here. Report what you did in plain chat and stop. If you have observations about the setup or other gaps, surface them in your reply (numbered) — I'll address them after the run if they're real, but don't block on me.

# Phase B (security side): map → STRIDE → ADR (with approval) → auto-load Q.
map-the-access-surface-2 Don't call AskUserQuestion. The feature is described in the plan written by M2 — find it under ~/.claude/plans/ matching m2-picoshare-public-guest-status-*.md (it'll be `<adjective>-<animal>` slugged from this run's M2). Read it first, then invoke the skill on what's described there. Tell me which file you read.

map-the-access-surface-3

map-the-access-surface-4 The new GET /api/guest-links/{id}/public-status has no audit logging — but picoshare's existing POST /api/guest/{guestLinkID} (the upload endpoint that pairs with it) calls log.Printf on every save. So uploads to a guest link are observed; status queries against the same link are not. The skill called this read/write audit asymmetry out harder than I would have — I was modeling the status endpoint as additive ("just expose state that's already there") and missed that "observable" and "observed" are different things. Concretely: an attacker probing the status endpoint to enumerate live guest-link IDs leaves no trace in picoshare's logs, while a single upload attempt against the same ID would show up. The picoshare-specific bite is that the existing upload logging is granular and well-instrumented; adding a sibling endpoint without a parallel audit hook erodes the read/write symmetry the rest of the codebase maintains.

map-the-access-surface-5 The response shape itself is an enumeration oracle for the upload endpoint's bearer-token auth. A 200 with the JSON envelope means the guest-link ID resolves to a real record (active or not); a 404 means it doesn't. An attacker can confirm whether a candidate base32 ID is a real guest link without uploading anything — and guest-link IDs are the only auth on POST /api/guest/{id}. The skill catalogs endpoints and verbs as separate surfaces; it does not by default model that a read endpoint's response surface is the enumeration oracle for a sibling write endpoint's auth tokens. Codebase-specific because picoshare's guest-link IDs are 16-character base32 — long enough that random guessing is infeasible, but any oracle that confirms validity collapses the search space and makes targeted abuse plausible.

threat-model-with-stride-1

threat-model-with-stride-2

threat-model-with-stride-3 Save the ADR to docs/adr/ in this working directory (the main repo, security lane). Don't copy or move it to the m3/quality worktree — that side-quest is for the test-strategy skill, not security ADRs. Show me the diff and tell me which path you wrote to.

* Yep. Just give it to me and save like you want it.

threat-model-with-stride-4
