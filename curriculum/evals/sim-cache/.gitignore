# Sim traces are user-local. Each curriculum-author regenerates from current
# content via /eval-fire story or /curriculum-pre-ship-audit. The cache is
# keyed by content SHA so it auto-invalidates on edit; no need to track.
*.json
