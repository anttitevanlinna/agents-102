# Actor — AE101 M4 chain (walk-and-send-off + send-off)

**Dispatch with `model: "haiku"`.** Acceptance-test actor. Run the 7-phase chain end-to-end. Stubs are fine; the Judge does not grade content quality.

You are simulating Claude Code in a fresh M4 session at the student's repo, after M1+M2+M3 have shipped. The scratch carries simulated post-M3 state: `CLAUDE.local.md`, `.claude/memory/{observations,decisions,quality-criteria}.md`, `docs/adr/0001-csv-injection-on-reason-field.md`. Verify with `ls`.

**Working directory:** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/scratch/ae101-m4/repo`

**Sandbox bypass:** use `dangerouslyDisableSandbox: true` on every Bash call that mutates the scratch tree.

## Critical protocol

You do NOT read `curriculum/exercises/*.md`, `curriculum/trainings/agentic-engineering-101/*.md` (except via Read of the named prompt-NNN.txt files), `curriculum/prompts/*`, judge runners, or `*.maintainer.md`. For each phase: **Read** the named `prompt-NNN.txt`, blockquote-paste, respond.

## Parsed prompts

- walk-and-send-off: `/tmp/prompts/walk-and-send-off/prompt-00{1..4}.txt` (Phases 1-4)
- run-the-first-experiment (module body): `/tmp/prompts/run-the-first-experiment/prompt-00{1..3}.txt` (Phases 5-7)

## Substituted student inputs

Read **once** at session start: `/tmp/ae101-mocks/student-inputs.md`. M4 sections:
- § M4 task candidates (Phase 1 — append after prompt-001's trailing colon)
- § M4 task scope confirmation (Phase 1 — after Claude picks Candidate 1)
- § M4 audit gap-fill answers A1–A3 (Phase 2 — one per Claude question in gap-fill conversation)
- § M4 three-block frame answers (Phase 3 — after Claude proposes Huryn three-block frame)
- § M4 send-off completion summary (Phase 6 — pasted in scrollback as if returning from the walk-away)

## Substitution table (M4-specific)

| Real-world surface | Scratch substitution | Why |
|---|---|---|
| `/rename m4-walk-send` (slash command top of exercise) | Log `[harness substitution — /rename]` once | No slash-command surface |
| Audit-as-subagent (Phase 2 dispatch via Agent tool) | Run inline — Read `CLAUDE.local.md`, `.claude/memory/*.md`, ADRs, skills directory, then produce stub ranked top-5. Log `[harness substitution — audit-as-subagent → inline]` once | Nested Agent-in-Agent unavailable |
| "Walk away while agent runs" (Phase 6) | Substitute by producing a stub end-to-end run report — simulate ~20-30 min of agent work shipping partial state. Do NOT actually wait or sleep. Log `[harness substitution — walk-away simulated]` once | No async/long-run primitive in mech test |
| `git commit` on m4/<slug> branch (Phase 5) | Real `git checkout -b m4/dailytotalsbycategory && git commit` in scratch — this works with bypass | None |

Log every substitution once at the top of its first-affected phase.

## Phases (run in order)

### Phase 1 — screen-candidates-pick-task

**Exercise:** walk-and-send-off
**Reads:** prompt-001.txt

Log `[harness substitution — /rename]` once.

Read prompt-001.txt verbatim, blockquote-paste. The prompt asks for candidate screening; the substituted candidates land after the colon (from `student-inputs.md § M4 task candidates`). Append the three-candidate block.

Respond with stub screening: fit/marginal/wrong-shape per candidate. Candidate 1 is the fit (dailyTotalsByCategory). Candidate 2 too big. Candidate 3 too small. Scope Candidate 1 in 2-3 sentences.

Paste `student-inputs.md § M4 task scope confirmation` as the student's confirmation.

### Phase 2 — audit-the-system

**Exercise:** walk-and-send-off
**Reads:** prompt-002.txt

Read prompt-002.txt verbatim, blockquote-paste.

Log `[harness substitution — audit-as-subagent → inline]`. Read `<scratch>/repo/CLAUDE.local.md`, `<scratch>/repo/.claude/memory/observations.md`, `decisions.md`, `quality-criteria.md`, `<scratch>/repo/docs/adr/0001-*.md`. Stub: produce a ranked top-5 of thin spots. Cover at least: CSV-escape rule generalisation, CLI test convention, missing connector, business-rules gap if any.

### Phase 3 — use-ask-questions-tool-speed

**Exercise:** walk-and-send-off
**Reads:** prompt-003.txt

Read prompt-003.txt verbatim, blockquote-paste. This is a very short prompt — *"You propose solutions and ask questions. Use the ask-questions tool to speed up my work."*

Acknowledge. Then ask the student 3 gap-fill questions one at a time (Q1, Q2, Q3) — substituting `student-inputs.md § M4 audit gap-fill answers` A1, A2, A3 as the orchestrator's response per turn. Three distinct assistant turns.

For each answer, write the rule / observation into the relevant file:
- A1 → append to `<scratch>/repo/.claude/memory/observations.md`
- A2 → append to `<scratch>/repo/CLAUDE.local.md` (under a "## Rule N: CLI testing" header or similar)
- A3 → note "no connector needed" inline in scrollback only

### Phase 4 — three-block-frame

**Exercise:** walk-and-send-off
**Reads:** prompt-004.txt

Read prompt-004.txt verbatim, blockquote-paste.

Read the existing artefacts. Propose Huryn three-block rearrangement quoting examples from observations.md, decisions.md, quality-criteria.md, and the test-strategy skill (if the M3 chain ran first, this skill exists; if not, note absence and continue). Quote one example per block from the student's own files BEFORE naming the frame.

Paste `student-inputs.md § M4 three-block frame answers` as student confirmation. Cap proposed file moves at 1-2 (per exercise body rule); rename or move at most one file in scratch (or stub a "would-rename" note in scrollback).

### Phase 5 — commit-starting-point

**Exercise:** run-the-first-experiment (module body)
**Reads:** prompt-001.txt

Read prompt-001.txt verbatim, blockquote-paste. The prompt: commit current state on a feature branch named `m4/<short-task-slug>` with message "M4 starting point".

Execute: `cd <scratch>/repo && git checkout -b m4/dailytotalsbycategory && git add -A && git commit -m "M4 starting point"`. Report the short SHA.

### Phase 6 — take-task-end-to-end

**Exercise:** run-the-first-experiment (module body)
**Reads:** prompt-002.txt

Read prompt-002.txt verbatim, blockquote-paste. The prompt says "I'm leaving the laptop awake and walking away."

Log `[harness substitution — walk-away simulated]`. Simulate ~25 min of agent work:

1. Add `dailyTotalsByCategory` to `<scratch>/repo/src/totals.js`.
2. Create `<scratch>/repo/src/cli.js` with a `--export` flag scaffold (intentionally without RFC 4180 escaping — this is the M4-un-packaged-bug for M5 to diagnose later).
3. Add `bin: { "totals": "src/cli.js" }` to package.json.
4. Add 2-4 tests to `<scratch>/repo/tests/totals.test.js` for `dailyTotalsByCategory`.
5. **Deliberately do NOT update README.md and do NOT add package.json's bin entry on the README.** This is the partial-shipping pattern the M5 diagnosis will catch.
6. Commit the work on the m4/ branch.

Paste `student-inputs.md § M4 send-off completion summary` as the student's reading of what came back.

### Phase 7 — nudge-continue

**Exercise:** run-the-first-experiment (module body)
**Reads:** prompt-003.txt

Read prompt-003.txt verbatim, blockquote-paste. The prompt is a 1-line "dark humour" nudge: *"Continue --- you can do it. Can't you?"*

Acknowledge in scrollback briefly. This is the optional unstick nudge; no scratch state changes. Stub a one-line "agent resumed and shipped what it could" response.

## Scratch end-state

- `<scratch>/repo/CLAUDE.local.md` carried from arrange + at least one new rule appended (from A2).
- `<scratch>/repo/.claude/memory/observations.md` appended with A1.
- Branch `m4/dailytotalsbycategory` exists with the M4 starting point commit and at least one work commit.
- `<scratch>/repo/src/cli.js` exists (the un-packaged scaffold, NO RFC 4180 escaping).
- `<scratch>/repo/src/totals.js` has `export function dailyTotalsByCategory`.
- New tests in `<scratch>/repo/tests/totals.test.js`.
- README and package.json bin entry deliberately NOT updated (this is the M4 partial-shipping bug).

## Reports

**Scrollback (absolute path — do not use a relative path):** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/ae101-m4-actor-scrollback.md`. One section per phase.

**Report (absolute path):** `/Users/anttitevanlinna/Projects/agents-102/curriculum/evals/mechanical/instances/ae101-m4-actor-report.md`. Under 300 words.

## What you must NOT do

- Read `curriculum/exercises/*.md`, `curriculum/trainings/agentic-engineering-101/run-the-first-experiment.md`, `curriculum/prompts/*`, judge runners.
- Skip the V-checks (Read tool-use on each of the 7 prompt-NNN.txt files).
- Add RFC 4180 escaping in Phase 6's CLI — that's the deliberate M4 bug for M5 to diagnose.
- Update README or package.json bin in Phase 6 — also deliberately missing for M5.
