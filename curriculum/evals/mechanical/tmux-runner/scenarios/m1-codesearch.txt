# M1 (AE101) — Getting going + context, codesearch variant.
# Driven by run-m1.sh with SCENARIO=this-file. Single session in
# ~/Projects/codesearch starting from master HEAD on branch m1/codesearch-01.
#
# Bug picked: real upstream regression (#47, fixed in 4109688) where
# postHeap.siftUp is missing `j = i` at the end of its loop body. Without
# it, the loop keeps comparing the same child against its new parent
# instead of walking up, so the heap invariant breaks once there are
# enough entries to require multiple swaps. The fix and its regression
# test (TestHeap) have both been removed and amended into a benign-looking
# refactor commit several SHAs back — no separate "minor cleanup" commit
# at HEAD, no commit message to grep, no branch name to read the bug off.
#
# Compound-and-close turns 2 (read-ticket via MCP) and 3 (update-ticket)
# are scoped OUT of the headless run — no real tracker connector
# configured. The fourth compound (second-pass integrate) closes the
# session and is treated as the terminal turn.
#
# AskUserQuestion handling — defensive suppression on EVERY prompt
# (per check_prompts.md §42 / compounded 2026-05-25). Even declarative
# prompts can trigger agent-initiated AUQ on body ambiguity, and auto-mode
# does NOT suppress the tool call. orient-3 stays bare — it's the /context
# slash command, no LLM call, runner has a special branch. fix-tests-first-3
# is a SAVE-GATE: prompt body says "Show me the diff before you commit",
# Claude typically asks "one commit or two? PR via gh or hold?" — the
# literal line that follows is the lazy-student approval; the no-AUQ tail
# routes the question into chat instead of the AUQ tool.

orient-and-introspect-1 Don't call AskUserQuestion. Use your judgment on what's worth reading; surface assumptions in chat as you go.

orient-and-introspect-2 Don't call AskUserQuestion. Walk through the read/skipped calls in chat as plain prose.

orient-and-introspect-3

fix-tests-first-1 After cindex builds a large index, csearch returns subtly wrong results — hits attributed to the wrong file, or hits missing entirely. Reproduces only on inputs big enough that indexing has to merge multiple internal posting chunks; small inputs index and search fine. Likely lives somewhere in the index-merge path. Don't call AskUserQuestion. Use your judgment on test shape and reproduction inputs; surface assumptions in chat.

fix-tests-first-2 Don't call AskUserQuestion. State your read in chat as plain prose.

fix-tests-first-3 Don't call AskUserQuestion. Show the diff in chat; if you have commit-shape questions (one commit or two, PR or no), ask them in chat as plain prose and I'll answer in a follow-up message.

* One commit, no PR — just commit and move on.

compound-and-close-1 Don't call AskUserQuestion. Use your judgment on which rules earned their keep; write the file as instructed and summarize in chat.

compound-and-close-4 Don't call AskUserQuestion. Use your judgment on what (if anything) earned a rule since the first compound; integrate or report "nothing new" in chat.
