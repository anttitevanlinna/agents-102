# Pre-cohort punchlist, AE101 M1–M6

> **CLOSED = DELETE THE BULLET. Not "annotate done." Not "rewrite as smaller follow-up." Not "update with what landed." DELETE.**
>
> Git log is the history. This file is current state. A closed bullet rewritten as adjacent work is a NEW bullet pretending to be progress, surface the new work cleanly under a real section, or don't add it. If the new work isn't pre-cohort scope (M1–M6 done-done), it doesn't belong in this file at all.

Open decisions before the first paying cohort. Module files carry zero TODOs; this is the single tracking surface.

**Item types.** Bullets are open decisions or tactical fixes by default, something has to happen before cohort. Prefix a bullet with **`[watch]`** to mark *currently accepted, no pre-cohort action*, the behaviour may bite us, but we've decided to ship as-is and see. Watch items convert to action items if the cohort run surfaces the failure; otherwise they get deleted like any other closed bullet. Don't let watches accumulate as a dustbin: a watch with no named firing condition is procrastination dressed up.

Sim sweeps and platform-capability checks are not tracked here. The `curriculum-pre-ship-audit` skill auto-fires on touched files; `check_platform_and_boundaries.md` fires on platform claims at content-time. **Per-class verdicts (PASS / REVISE / grandfathered) live in each module's own `**Quality:**` block**, `update-quality.sh` stamps both PASS and REVISE with the instance-JSON path so a successor can grep Quality blocks for state without consulting this file.

**Harness / tmux-runner engineering is not tracked here either.** Runner bugs, scenario hardening, per-turn assertions, socket isolation, and state.json shape live in `curriculum/evals/mechanical/tmux-runner/IMPROVEMENTS.md`. This file tracks cohort-blocking *curriculum* decisions only; a finding that's fixed in runner code or a scenario file belongs there, not here.

**Source-freshness is not narrated here either, it is stamped per-source and generated.** Each source carries a checked / result / due freshness stamp in its file's `Source verification` block (format: `curriculum/source-freshness-format.md`); `curriculum/evals/scripts/source-freshness.sh --target <cohort-date>` produces the punch list. The 2026-05-25 full sweep cleared all source BLOCKs; per-source stamps now live in each file's `Source verification` block. Do not add new hand-narrated source-verification prose; stamp and run the script instead.

## Per-file specific concerns (not caught by auto-fire)

- **`[watch]` `extract-the-task-shaping-rule-2` save-path ambiguity (M2).** Prompt asks Claude to "propose two-three plausible paths" for the rules file and pick one. Codesearch M2 dry-run (2026-05-24) had the agent integrate the new rules INTO the existing `CLAUDE.local.md` (created at M1's compound), a legit "integrate, don't append" reading, but it means a downstream prompt that loads "the rules file" and `CLAUDE.local.md` as separate inputs sees one path, not two. Lemmings runs typically save to a distinct file. **Accepted as-is for first cohort.** Fires if any downstream module prompt (M3 sharpen-skill, M4 walk-and-send-off, M5 diagnose-and-resend, M6 spot-gaps) loads "the rules file" and `CLAUDE.local.md` as separate inputs and silently de-dupes or overwrites, observe whether any module behaves differently when the two paths collapse to one.
- **`[watch]` `ae101-m4-commit-starting-point` run-coordinates land at bottom of task.md (M4).** Prompt says "*append* a protected block" → coordinates go to end-of-file. The frontmatter `note` names the purpose as "later session can recover this run without searching", but a reader doing `cat task.md | head` scrolls past the entire task body before finding the coordinates. Codesearch M4 dry-run (2026-05-24) confirmed the agent appends faithfully. **Accepted as-is for first cohort.** Fires if any downstream M5/M6 prompt (worktree-setup, diagnose-and-resend-1/2, spot-gaps-1, arc-retrospective-1, agents-that-build-agents-1) silently fails to locate the coordinates, OR if a cohort student takes more than a few seconds to find them. Fix would change "append" → "prepend" / "insert at top".
- **`[watch]` skill-name collision breaks the hardcoded-literal chain on replays (M3 `test-strategy`, M6 `session-shaper`).** Both skills save to fixed names and ~6 consumer prompts cite the literal path (`~/.claude/skills/test-strategy/`, `session-shaper`). First-cohort clean `~/.claude/skills/` hits no collision. **Accepted as-is for first cohort.** Fires if a returning/replay student or an agents-101 alum already has either name, Claude suffix-renames (`test-strategy-<project>`), and a consumer prompt citing the literal misses. Fix when it fires: warn in the exercise body to check where Claude saved, OR have the creation prompt overwrite-on-conflict.
- **`[watch]` save-gate-then-immediate-read race in `author-test-strategy-skill-1`, `diagnose-and-resend-4`, `spot-gaps-build-the-loop-3`.** All three prompts ([author-test-strategy-skill-1.md:28](../../prompts/author-test-strategy-skill-1.md), [diagnose-and-resend-4.md:28](../../prompts/diagnose-and-resend-4.md), [spot-gaps-build-the-loop-3.md:28](../../prompts/spot-gaps-build-the-loop-3.md)) say *"show me before saving"* or equivalent, then the immediate next scenario step reads or invokes the file (M3 quality lane invokes the skill at `author-test-strategy-skill-2`; M5 verifier smoke-test at `diagnose-and-resend-5`; M6 critique at `spot-gaps-build-the-loop-4`). If Claude obeys the save gate literally and waits for an approval that doesn't come in autonomous mode, the file is never written and the next step fails on a missing artifact. **Accepted as-is for first cohort.** Fires if any headless / unattended cohort run hits a missing-artifact failure downstream of one of those three turns (codesearch M5, picoshare M3/M5 happy-path runs all saved through; this surfaces if Claude reads the save gate stricter). Fix when it fires: tail the downstream literal with an explicit approval, OR rewrite the save gate to *"save now and show me after"*.- **`[watch]` worktree-setup prompts carry literal `<repo-name>` placeholder paths inside the prompt body.** [ae101-m3-fork-quality-side.md:21](../../prompts/ae101-m3-fork-quality-side.md) and [ae101-m5-worktree-setup.md:26](../../prompts/ae101-m5-worktree-setup.md) sketch shell commands with `../<repo-name>-quality` / `../<repo-name>-m5` as placeholder text. Body says Claude should infer the actual repo name from the cwd, but the inference instruction lives outside the copied prompt, only the body has it; the shell sketch reads as the literal command. **Accepted as-is for first cohort.** Fires if Claude follows the shell sketch verbatim and creates a directory called literally `../<repo-name>-m5` (a sibling named `<repo-name>-m5`), causing run-m3 / run-m5's `--worktree-cwd` assertion to miss (codesearch M3/M5 + picoshare M3/M5 + lemmings M3/M5 all inferred correctly so far; this fires when prompt-following gets more literal). Fix when it fires: move the infer-from-cwd instruction inside the prompt body so it travels with the copy, OR fold the substitution into the prompt registry expander.
- **`[watch]` `prompt-source-audit.sh` returns BLOCK on `walk-and-send-off` + `spot-gaps-build-the-loop`.** `walk-and-send-off` blocks on curriculum-vocab leakage around "send-off" inside prompt text, plus a skill-path reference at [walk-and-send-off-2.md:25](../../prompts/walk-and-send-off-2.md). `spot-gaps-build-the-loop` blocks on bare M4/M5 prose inside copied prompts, especially [spot-gaps-build-the-loop-5.md:26](../../prompts/spot-gaps-build-the-loop-5.md). Linter-strict rather than runtime-breaking; the runs themselves succeed. **Accepted as-is for first cohort.** Fires when "done-done" gate is applied to these exercises (the cohort-tested or battle-tested Quality state needs static audits clean), OR when a cohort student looking at the static prompt asks why their copy has M4/M5 leak. Fix when it fires: rephrase the "send-off" prose to be self-contained, replace bare M-numbers with referenceable artifact names (the M3 ADR, the M5 packaged re-run) per the sessions-don't-know-module-names rule.
- **`[watch]` M4 task.md body `Branch:` line is agent-emergent, so the M5 worktree-setup decoy-disambiguation fires non-deterministically.** `walk-and-send-off-1` only says "save the scope as `task.md`"; nothing asks for a `Branch:` header. But the candidate brief frames the bug as the task "on this very branch", so the agent self-authors a body `Branch:` line at scope-time, *before* the real branch exists (created later at `ae101-m4-commit-starting-point`). Lemmings retest 2026-05-26: body line `m4/blocker-deadlock-terminal` (task.md:3, written turn-1) vs the actual `m4/blocker-deadlock-fix` (created turn-6, recorded in the Run-coordinates block). Whether the body line appears at all, and whether it diverges from the real branch, varies run to run. `ae101-m5-worktree-setup` was hardened 2026-05-26 (`df17bc0`) to read the `Run coordinates` block and ignore any other body `Branch:` line, and that guard tested green on a run where the divergent decoy WAS present. **Accepted as-is for first cohort**, the guard is correct and harmless when no decoy exists. **Fires if** a cohort run shows worktree-setup confused by the body line despite the guard, OR the maintainer decides the decoy-disambiguation should be a deterministic teaching beat. Fix when it fires: have `walk-and-send-off` / `ae101-m4-commit-starting-point` either suppress the body `Branch:` line or author it deterministically as a designed decoy. Sister of the (closed) M4→M5 fork watch.
- **`[watch]` M5 grill "lock it in" with an unanswered round leaves load-bearing decisions unowned.** `diagnose-and-resend`'s grill-before-save sets up multi-round Q&A but never names what happens when "lock it in" arrives before every question is answered. Lemmings retest 2026-05-26 (phase B): the agent settled three packaging decisions (keep constraints, reuse the win/loss text, commit both files) on its own recommendations rather than the maintainer's answers. **Accepted as-is for first cohort**, the agent's defaults were reasonable and the run passed. **Fires if** a cohort student says "lock it in" mid-grill and the agent bakes a wrong default into the packaged artifact, OR a student is surprised that unanswered questions got auto-decided. Fix when it fires: the grill prompt names the lock-in contract (`check_prompts §35/§20`), e.g. "if I say 'lock it in' before answering everything, use your best recommendation for the rest and tell me which you assumed."
- **`[watch]` M3 `ae101-m3-sharpen-skill` closer assumes scrollback from the quality lane while running in the main lane.** The main-lane closer tells the agent to read scrollback for "the moment the skill was invoked on the security-tested feature", but that invocation happened in the QUALITY-lane session, not the main lane where the closer runs; the cross-window bridge lives in a body callout, not the prompt fence (`check_prompts §34/§40`). Lemmings retest 2026-05-26: the agent self-corrected by grounding on the ADR; the same cross-lane scrollback assumption recurs at quality turn-4. **Accepted as-is for first cohort**, by-design for cohorts (the student bridges the two windows) and the agent recovered. **Fires if** a fresh main-lane session or unattended run can't locate the invocation moment and the closer mis-grounds. Fix when it fires: move the cross-lane anchor into the fence (§34), or have the closer ground on the ADR / artifact explicitly rather than on scrollback.
- **`observations/` rename leftovers (cosmetic; surfaced by the 2026-05-26 pre-ship audit, all non-blocking).** The `.claude/memory/` → `observations/` rename audited GO across writing/story/technical/pedagogy/cross_module; three tidy-ups remain. (a) `learn-from-the-test` maintainer-block artefact-contract table carries a `CLAUDE.local.md`-carried-into-worktree row but no parallel `observations/` row, even though the worktree-setup prompt copies it and M6 + arc-retro read it, a §5 row-completeness gap that pre-dates the rename. (b) Six prompts ([arc-retrospective-1.md:10](../../prompts/arc-retrospective-1.md), [walk-and-send-off-4.md:11](../../prompts/walk-and-send-off-4.md), [ae101-m4-take-task-end-to-end.md:15](../../prompts/ae101-m4-take-task-end-to-end.md), [ae101-m5-worktree-setup.md:22](../../prompts/ae101-m5-worktree-setup.md), [diagnose-and-resend-6.md:20](../../prompts/diagnose-and-resend-6.md), [ae101-m3-fork-quality-side.md:17](../../prompts/ae101-m3-fork-quality-side.md)) still carry the internal frontmatter graph id `id: memory-folder`; all `location:`/`if-present-at:`/`cp -r` path fields are correctly `observations/`, so the id is a stale-but-harmless label (not student-visible). Rename to `id: observations-folder` only as a batch with every `requires:`/`produces:` reference at once, or graph integrity breaks. (c) **Cross-repo:** the private `bosser-strategy:content-strategy-agentic-engineering-101.md:146` still names `.claude/memory/`, sweep it there when next in that repo (out of agents-102). Also owed: a per-file Quality re-stamp on the ~12 touched files (touch-degraded then audit-cleared this cycle); deferred as batch bookkeeping, not done by hand.

---

**Canonical home:** this file. Module files carry zero TODOs, all pre-cohort open items route here.

**When an item closes:** delete the bullet. Git log carries the history. Don't annotate "done", closed = gone.

**When a new item surfaces mid-session:** add it here, not in the module file.
</content>
</invoke>
