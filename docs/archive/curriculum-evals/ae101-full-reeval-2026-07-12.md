# AE101 full re-eval — 2026-07-12

Full `curriculum-pre-ship-audit` sweep (seven per-file classes + cross_module) across the 51 AE101 files touched by the 2026-07-09 emphasis pass, the quintet-voice + sequential arc-read pass (incl. the 2026-07-10 Huryn/three-block frame removal), and the 2026-07-10 map-figure pass. Closes all three `[action]` bullets those passes left in this file. Run via a Workflow sweep (358 dispatches: 51 files x 7 classes + 1 cross_module); one dispatch (`slides` on `map-the-access-surface.md`) stalled all 6 internal retries and was re-run standalone. Per-class verdicts are stamped into each file's own `**Quality:**` block with pointers to `curriculum/evals/instances/ae101--<slug>.<class>.json`; this report is the human-readable rollup.

**Result: 17 GO / 34 BLOCK** (51 files). cross_module: **REVISE** (6 blocking across the prework-M6 set).

Findings are hypotheses from a single judge pass, not settled fact — verify against `check_pedagogy.md §61/§61a` (deliberate pedagogy / wrong-layer diagnosis / cosmetic non-issue) before fixing, per this training's standing rule.

## Cross-module findings (prework -> M6 set)

**plan-mode-done-right.md -> earn-the-trust.md**
- §8 Bring-or-scramble plain-stakes-line shape: plan-mode-done-right.md L74-79 '## Next' has no 'Bring to Module 3' stakes line at all; earn-the-trust.md L8-16 '## Prework' states "Bring the feature you planned at M2, that's the default" but never names the cost of arriving without it. Every other module boundary in the set (prework→M1 L70, M3→M4 L96-98, M4→M5 L102-106, M5→M6 L108-112) carries the canonical stakes line; this is the one boundary where it's missing on both ends despite M3 requiring a brought artefact.
  - Fix: Add a 'Bring to Module 3' stakes line (M2 close or M3 Prework) in canonical form naming the cost of arriving without the planned M2 feature.

**earn-the-trust.md -> run-the-first-experiment.md**
- §2 Between-module reading in two places: earn-the-trust.md L92: "Optional forward-look before M4: [What is agentic engineering](...supplementary/what-is-agentic-engineering.md). And when your sessions start running heavy, [Token efficiency](...supplementary/token-efficiency.md)..." — neither link appears anywhere in run-the-first-experiment.md's '## Prework' section (L6-12), which contains only the scoped-task bring-item. Every other pair in the set (M1→M2, M2→M3) duplicates its between-module reading list at the receiving module's open; this pair drops it entirely.
  - Fix: Duplicate the two 'Optional forward-look before M4' links into M4's Prework section with matching one-line framing.

**run-the-first-experiment.md -> learn-from-the-test.md**
- §2 Between-module reading in two places: run-the-first-experiment.md L108-118 '## Pre-reads before Module 5' lists four items (Clean Code Is Steering, Entis/Klaassen video, Verification asymmetry, Klaassen Compound Engineering guide). learn-from-the-test.md L10 'Optional prep' duplicates only three (Uncle Bob, Entis/Klaassen, Compound Engineering) — 'Verification asymmetry' is omitted from the receiving module's open.
  - Fix: Add the 'Verification asymmetry' link to M5's Prework 'Optional prep' list alongside the other three M4 pre-reads.
- §5 Artefact-contract maintainer-block rows: run-the-first-experiment.md L171 'M4 starting-point branch + SHA' contract row names the stable identifier as "SHA noted in scrollback (or in `./CLAUDE.local.md` if the student writes it down)" — but the module's own body text (L63) and M5's consuming body text (learn-from-the-test.md L56) establish `task.md`'s 'Run coordinates' block as the actual, load-bearing stable identifier both prompts use. `task.md` has no dedicated contract row; the existing row's identifier column is stale relative to what the producing/consuming prompts actually do.
  - Fix: Add a dedicated task.md contract row (or rewrite the existing row's identifier column) naming the 'Run coordinates' block as the stable identifier, matching L63/L56.

**learn-from-the-test.md -> spot-gaps-build-the-loop.md**
- §2 Between-module reading in two places: learn-from-the-test.md L114-120 '## Pre-reads before Module 6' lists two items (Klaassen 'My AI Had Already Fixed the Code Before I Saw It' + 'Checks that push back' / backpressure.md). spot-gaps-build-the-loop.md L6-10 'Prework' duplicates only the Klaassen link; the backpressure.md pre-read is omitted from M6's open (it resurfaces later in M6's own '## Next' as a different forward-look pointer, but not at the receiving Prework section).
  - Fix: Duplicate the 'Checks that push back' (backpressure.md) pre-read into M6's Prework section alongside the Klaassen link.
- §4 Session boundaries + working directory: learn-from-the-test.md '## Next' (L104-106) names no session close and no working directory for M6. spot-gaps-build-the-loop.md '## Start here' (L23-29) names no working directory either — the rescue callout mentions "your worktree" in passing (L27) but never instructs the golden path where to open. M1 (getting-going.md L10) declared "you'll stay in this one repo across all six modules," but M5 forked a worktree at `../<repo>-m5`; unlike the M4→M5 boundary (which explicitly named the worktree open), nothing in either M5 or M6's body text tells the student whether M6 continues in the M5 worktree or returns to the original repo — exactly the ambiguity the rule warns compounds with branch confusion.
  - Fix: State explicitly in M6's 'Start here' (or M5's 'Next') that the session opens fresh in the M5 worktree at ../<repo>-m5, since M1's 'stay in one repo' default no longer holds after M5's fork.

## Blocked files (34)

### `curriculum/trainings/agentic-engineering-101/getting-going.md`

- **story** REVISE (1/2):
  - [BLOCKING] §4 Strategy-fidelity check (consolidates front-run check, module-to-module arc, Big Idea fidelity, mood-doesn't-resolve-early).: content-strategy-agentic-engineering-101.md lines 266, 629, 637, 664 all still describe the connector-wire + ticket-close as living inside Ex3 (compound-and-close / 'M1 P3'), and list only three M1 exercises. Current getting-going.md (Big Idea, What You'll Learn, Key Concepts, new ## Homework section) has moved that move into a new take-home exercise close-the-ticket.md. getting-going.md's own maintainer block admits it: 'Per-class Quality SHAs below predate the reframe — re-audit before ship.' Strategy doc not yet updated to match; the prior story-judge instance (2026-05-22, PASS) cited the now-stale match as evidence and is superseded.
    - Fix: Update content-strategy doc's M1-in-detail exercise list/P3 description + pre-engagement-contract ticket-tracker row + the two M1-exercises citations to name close-the-ticket.md and the homework split, or hold ship until strategy and module agree. Reconcile in /content-creation.
  - [todo] §1 Mood contract named; 8+/10 per beat, 7 = facilitator-premium signature not a floor breach.: Regenerated trace: phase mood_score 8 (in-room 'it works, on my repo' via PR + pushed-back-on CLAUDE.local.md). close mood_score 7 — the external-system 'it reached past my repo' spike (real ticket closes) relocated from in-room close to solo homework; the maintainer's Mood-target watch-for paragraph doesn't yet name this new handoff risk (still only watches for 'technical warm-up' drift).
    - Fix: Add one clause to the Mood-target watch-for paragraph naming the deferred-climax risk so the trainer closes the room framing homework as 'yours to finish,' not 'we ran out of time.'
- **strategy** REVISE (1/1):
  - [BLOCKING] §0 Big Idea match — file vs strategy-doc Big Idea/artifact row: File Big Idea (L6-7): 'The fourth move, closing the bug's ticket outside the repo, runs as homework.' + '## Homework' (L47-48) vs strategy doc M1 table Artifact col (L235: 'P3 retro extends CLAUDE.md + wire one connector + close the bug's ticket outside the repo' — three-phase loop, ticket-close INSIDE P3) and M1-detail Ex3 description (L266: 'compound-and-close ... wire one connector and close the bug's ticket outside the repo' — inside Ex3, in-class). Strategy doc's 'Homework after M1' (L284-290) names only two optional readings (Cherny video, multi-session-git); ticket-closing is not modeled as homework anywhere in the strategy doc. File's own maintainer note (L64) confirms: 'the connector + ticket close-out moved from Ex3 ... to the close-the-ticket homework exercise ... re-audit before ship' — this is that unreconciled drift.
    - Fix: Reconcile in the same edit: update strategy doc's M1 table Artifact col + M1-detail Ex3 prose to describe ticket-close as the loop's homework-owned fourth move (the file's redesign reads deliberate; strategy doc is stale) — or fold ticket-closing back into in-class Ex3 if the homework move was provisional.
  - [todo] §6 Key Concepts shape — theme breadcrumbs, floor 1 / ceiling 3 per section: Zero '**Remember: …**' breadcrumbs across the 6 Key Concepts bullets (L40-45); §6 floor is 1. Prior strategy-judge instance for this file recorded 'three breadcrumbs (context/claims/rules) at ceiling of 1-3 range' as PASS — since removed. pre-cohort-todos.md's open [action] item (2026-07-09/10 pass, 'Remember-wrapper strip ... Modules: getting-going, ...') already names this file for exactly this re-audit; this finding confirms that item's concern is live, not resolved.
    - Fix: Restore one breadcrumb on a strong-fit survivor bullet (bullet 3 self-aware/grain-of-salt 'Claims aren't evidence', or bullet 5 compounding/evidence-born rules) — or, if the Remember-wrapper strip is now deliberate house style superseding §6, amend check_strategy_tie_in.md §6 in the same pass. Either way should close the pre-cohort-todos.md action item, not sit alongside it unresolved.

### `curriculum/trainings/agentic-engineering-101/earn-the-trust.md`

- **story** REVISE (1/3):
  - [BLOCKING] §5 A slide's BODY is size-bounded (~210 words / 6 bullets).: node scripts/check-slide-size.js reports 3 of 9 slides oversized: 'Sharpen the skill from evidence' 334w (124 over ceiling, densest section in the file, the exact section the maintainer note flagged for re-audit); 'What You'll Learn' 8 bullets (2 over, up from an already-over 7); 'Key Concepts' 7 bullets (1 over, pre-existing). Not on the exemption list.
    - Fix: Split Sharpen-the-skill at the new lecture-link tail (own beat/header). For WYL/Key Concepts, merge an adjacent pair rather than adding a second templated section.

### `curriculum/trainings/agentic-engineering-101/run-the-first-experiment.md`

- **strategy** REVISE (1/0):
  - [BLOCKING] §4 Strategy-fidelity check (sequencing claims honored, no front-run): Lines 108-118 '## Pre-reads before Module 5' list FOUR pre-reads (Clean Code Is Steering/Uncle Bob; Bread in the AI Sandwich; Verification asymmetry; Compound Engineering: The Definitive Guide), framed 'All four point at the same surprise...'. Strategy doc's canonical spec for this slot ('Pre-reads before M5 — 80/20 frame', lines 412-416) names exactly TWO ('Two Klaassen pieces... ratio + metaphor') and the OODA gap-index (line 696) counts 'two pre-reads'. Supplementary material (planned) section (586-597) lists neither addition and says supplementaries are 'none built yet' (stale — both files exist). Sibling M5 file learn-from-the-test.md:10 lists a THIRD variant (3 items, drops Verification asymmetry) — three unreconciled lists for one canonical slot.
    - Fix: Reconcile in one edit: fold the two additions into the strategy doc's M5 pre-reads spec + gap-index (and align learn-from-the-test.md to match), or trim this file back to the canonical two and restore the 'ratio + metaphor' framing.

### `curriculum/trainings/agentic-engineering-101/learn-from-the-test.md`

- **slides** REVISE (1/1):
  - [BLOCKING] §1 Referent resolution under a sequential deck read.: chunk 12 "Bring to Module 6": line 140 (expanded view): "Read the scrollback or commits from the Debrief re-send" — "Debrief" never appears anywhere in this file's student-facing body (chunks 0-13, lines 1-151) before this point, and it is not established anywhere in the body of any prior AE101 module (grepped getting-going.md, plan-mode-done-right.md, earn-the-trust.md, run-the-first-experiment.md, spot-gaps-build-the-loop.md above the maintainer cut — zero hits) or in this training's own exercise phase headers (exercises/diagnose-and-resend.md uses "Phase 1-4", never "Debrief"). A cold sequential read has no antecedent. Note: the file's own Quality line cites a prior override ("override-r1-Debrief-canonical-section-vocab-per-carve-out") invoking check_slides.md rule 8's canonical-module-section-name carve-out ("## Debrief" as an architecture-mandated header per check_student_facing.md §17) — but that carve-out is rule 8's, not rule 1's, and no AE101 module file anywhere contains a literal "## Debrief" header (grepped; only Agents 101 files and module-shape.md use it). The override's stated premise does not hold for this training, so the finding is re-raised on this fresh pass.
    - Fix: Replace with a self-resolving phrase (e.g. "the re-send discussion" or "the exercise's closing conversation") or introduce "Debrief" as named vocabulary earlier in an AE101 module body before relying on it here.
  - [todo] §9 Emphasis budget — bold is for handles, and go very lightly.: chunk 7 "Run the fork from the original repo": line 50: "**Claude figures out `<repo-name>` from the working directory.**"; line 64: "**Claude will narrate before acting.**"; line 66: "**The coordinates are the anchor.**" — three separate whole-lead-sentences bolded in one chunk (excluding the architecture-mandated **Session**/**Prompt** widget-cue bolds), against a budget of ≤1 bolded element on most slides; none is a named/coined handle reused elsewhere in the training (contrast the blockquote's "**Long runs, short reads.**" in chunk 4, which IS the named M1 countermove reused across the arc). This is the pre-2026-07-09 bolded-topic-sentence monoculture the rule was written to flatten.
    - Fix: Drop the bold on two of the three (they're narrative orientation, not coined handles) and keep at most one per chunk, or fold them into plain prose.

### `curriculum/trainings/agentic-engineering-101/spot-gaps-build-the-loop.md`

- **technical** REVISE (1/3):
  - [todo] §4 Verify capabilities BEFORE asserting, not after — audit-mode not exempt.: Line 116 '20+ min' for agents-that-build-agents contradicts that lecture's own maintainer line 62 '5–7 min reading, closer-shaped'.
    - Fix: State the figure as the sum of the three timed close beats (~25–32 min) with sourcing parallel to the other two clauses, or re-derive after the 2026-07-03 re-sequence.
  - [todo] §6e Transcript-by-mtime selection is fragile when current session shares cwd with target folder.: M6 opens in the M5 worktree where both runs' transcripts live; the Connections-blocker push-back (line 123) names the folder but not marker-based selection — the exact scenario §6e cites as its canonical failure example.
    - Fix: Add a clause: filter by a phrase unique to the M4/M5 send-off, not recency alone; check the executable read in the sibling exercise file too.
  - [BLOCKING] §1 Source URL + type label mandatory; label honest about evidence level.: Line 149 labels 'Cherny's three verifier shapes — Boris Cherny [practitioner direct]', contradicting this file's own line 157 stamp, which correctly identifies the shapes as Kim's synthesis/paraphrase (not Cherny's own taxonomy), confirmed via WebFetch of the primary Substack source.
    - Fix: Relabel line 149 to [practitioner analysis] 'Kim on Cherny', or apply the stamp's own fallback (practitioner-convergent menu, no single attribution).
  - [todo] §1a OODA paraphrase ≠ primary source.: Same evidence as rule 1 — a paraphrase (Kim's) was transcribed as if primary-source (Cherny's).
    - Fix: Same fix as rule 1; not double-counted as blocking.
- **strategy** REVISE (2/1):
  - [BLOCKING] §4 Strategy-fidelity check — cross-module sequencing / same-edit sync: File maintainer block, line 174: '2026-07-05 (cut pass) — Antti. Two M6 beats cut fully. ... (2) The Arc-named retrospective exercise (arc-retrospective): did not advance the learning ... Removed everywhere.' vs strategy doc line 439: 'Forward-looking beat — arc-named retrospective. Between exercise and Debrief, you ask an agent to read your M1–M6 artefacts ... Runs as a second standalone exercise alongside the main three-move exercise; shared-library file so EM and other variants can adopt the shape.' The strategy doc (file mtime 2026-07-10, touched today for the unrelated §Woven-design-rules reading-contract addition) still describes this exercise as live M6 content five days after the file's own maintainer trail documents it being deliberately cut and git-rm'd. Doc and file are out of sync on a structural claim ('runs as a second standalone exercise').
    - Fix: Same-edit sync per the rule's resolution clause: either the doc's M6 'Forward-looking beat — arc-named retrospective' paragraph gets struck/updated to record the 2026-07-05 cut (file's decision trail is already well-documented and looks like the intended direction), or the beat gets restored to the file. Don't leave the doc describing removed content.
  - [BLOCKING] §5 Strategic themes recur in Key Concepts (≥2–3 distinct): Strategy doc line 449 names M6's two earned themes explicitly: 'Themes earned: self-aware + grain of salt (the whole module is conversation with Claude about Claude's output across two runs; the artifacts rule, the self-reports don't); compounding.' File's Key Concepts (lines 40–44) instances compounding three times over (bullets 1, 4, 5) but carries zero self-aware+grain-of-salt instance — no bullet touches Claude's self-report vs. the artefact, introspection, or verify-against-evidence. Sibling calibration: M5's Key Concepts (learn-from-the-test.md line 67) carries this exact theme as its own bullet — 'No benchmark told you what went wrong; the artefact did. The artefact rules, self-reports don't.' — near-verbatim to the strategy doc's own phrasing for that theme. M6's exercise body does practice the discipline (Phase 1 push-back: 'Expect over-credit on the packaging. A fair push-back is name one thing the verifier missed, concretely.') but it never surfaces as a Key Concepts bullet the way M5 distilled its own.
    - Fix: Add one Key Concepts bullet naming self-aware+grain-of-salt, mirroring the M5 sibling's shape — e.g. distill the Phase 1 push-back move (Claude's read of its own two runs is a hypothesis, verify against the quoted artefact) into a stated discipline bullet, not just an exercise-body push-back move.
  - [todo] §6 Key Concepts shape — theme breadcrumbs on 1–3 survivors: All 5 Key Concepts bullets (lines 40–44) pass §§3/4/5's per-bullet gates individually, so all are breadcrumb-eligible survivors — but none carries a short thematic hook decorating a tactical statement. Contrast sibling plan-mode-done-right.md line 63: 'Structure is persuasive. A 7-item plan with headers looks like a decision even when it's a draft. At least 10% wrong.' — a tactical bullet + trailing theme-essence hook. M6's bullets are either purely tactical (bullets 2, 3) or already theme-shaped as a whole statement without a separable hook (bullets 1, 4, 5); zero breadcrumb-shaped decoration anywhere in the section. Non-blocking per template (breadcrumb decoration excluded from the blocking list), but the section shows a real gap in this specific shape, not just a mechanical omission — same root cause as rule 5.
    - Fix: Once rule 5's self-aware+grain-of-salt bullet lands, breadcrumb-decorate it or one other survivor with a short 5–8 word hook in the house's unbolded-trailing-clause style (no literal 'Remember:' prefix — that's not current house practice per sibling modules).

### `curriculum/lectures/agents-that-build-agents.md`

- **slides** REVISE (1/1):
  - [todo] §7 LLM-cadence caps.: chunk 1 "The move widens to everything you do": line 9: "Not \"write me a skill.\" Not \"improve my setup.\" Ask it for a handoff prompt:" — the banned 'Not X. Not Y. Z.' construction. Checked and cleared: the anaphora triple at chunk 5 line 52 ("Each run surfaces the next gap. Each gap proposes the next move. Each move makes the next run cheaper.") — each leg carries new concrete content per the rule's own carve-out, not flagged; no self-grading superlatives, number-retractions, abstraction-noun stacks, or empty-leg verb triplets found elsewhere in the body.
    - Fix: Recast the two negated fragments and the ask into one declarative sentence — drop the 'Not ... Not ...' lead-in and open directly with the handoff-prompt ask.
  - [BLOCKING] §8 Internal dialect never lands on a slide.: chunk 2 "The agent stops where your judgement begins": line 15: "Agents that build agents is a collaboration in the same shape every other beat in this training has been a collaboration." — 'beat' is the compendium's own listed maintainer-dialect example (repo/authoring vocabulary for a narrative unit), used here as a body noun on a student-facing slide; not an architecture-mandated section name or file-attested scheme.
    - Fix: Swap 'beat' for plain language an engineer in the room would use (moment, part, lesson) — "every other part of this training has been a collaboration."

### `curriculum/lectures/how-this-training-was-built.md`

- **writing** REVISE (1/1):
  - [BLOCKING] §8 Don't smuggle terms of art from maintainer into body.: 23:became plan mode reshaping a whole file, then a subagent auditing that file against a checklist
    - Fix: Add one-sentence primers before technical terms: 'plan mode (where Claude works in planning rather than execution)' and 'subagent (Claude instance with fresh context for independent analysis)' before introducing them.
  - [todo] §2 Earn every technical term on first use.: 23:then a subagent auditing that file against a checklist
    - Fix: Add one-sentence primer before 'subagent' on first use: e.g., '(a subagent, Claude instance with fresh context for independent analysis)' to earn the technical term.

### `curriculum/lectures/ironies-of-automation.md`

- **strategy** REVISE (1/1):
  - [todo] §1 Mood contract named: Maintainer EYEBALL #2 (lines 27-29) leaves mood-landing unresolved: 'The paradox is meant to land as a live tension to watch for, not a prediction of failure ... if in rehearsal it still reads as "you will miss the bad run," thin it to one slide.' Placed immediately before the send-off prompt in run-the-first-experiment.md § Send the task off (line 51) — exactly where that module's Meta block names 'performance-anxiety ("what if it fails?")' and 'Send-off anxiety' as watched-for failure modes (module lines 131, 141). No Quality audit line exists yet (file states 'unaudited').
    - Fix: Rehearsal-read the closer ('when did you last do this kind of work by hand?') against the module's own named send-off-anxiety failure mode before shipping; thin to one slide or soften the close if it reads as prediction-of-failure rather than curious readiness.
  - [BLOCKING] §3 Lead with discipline, not failure mode: Title and both slide headers lead with failure-mode language, not a positive discipline: '# The ironies of automation' (line 1), '## The better the automation, the worse the operator' (line 3), '## Earned trust breeds the miss' (line 9). Sibling M4/M5 lecture titles are neutral/positive-framed ('The far half of the map', 'The agent loop', 'Learning through contrast', 'Reading the return'); this one is not. Strategy doc Positioning (7-13) is explicit and load-bearing: 'Never sold as a gap-fix. Engineers have pride and are often right; the training respects that.'
    - Fix: Retitle the piece and its two headers around the positive discipline (e.g. calibrated vigilance / watching well / earned vigilance); let 'worse the operator' / 'breeds the miss' live as the negative case the discipline addresses inside the body, per the rule's own resolution mechanism — not as the header. Bainbridge/Parasuraman content and sourcing can stay untouched; framing-level fix only.

### `curriculum/lectures/learning-through-contrast.md`

- **technical** REVISE (1/2):
  - [todo] §24a Quality stamps: tooling writes them; the SHA pins committed content.: Quality line pins five classes @1ff6f8a (git: 2026-05-15) but `git diff 1ff6f8a 6692710 -- curriculum/lectures/learning-through-contrast.md` shows that commit is the file's PRE-REWRITE version — different title, structure, prose throughout. File has since had a slides-rework (05fbd36, 2026-07-08) and an emphasis pass (6692710, 2026-07-09). The trailing note ('predates the slides-only rework') undersells this — the pin predates a full rewrite, not one pass; the slides@47f3357 pin (2026-07-07) also predates the 2026-07-09 emphasis pass per that pass's own note.
    - Fix: Already self-disclosed ('re-audit before ship') — this technical verdict fulfills that for its class; other five classes are the orchestrator's call to re-fire before update-quality.sh advances pins to HEAD.
  - [todo] §4 Hold short OODA loose.: source doc for /compact self-rates 'L2-strong, 3 signals — below L3 threshold'; three-failure-modes source self-rates 'L3 convergence, ~8 independents'. Maintainer block uses near-identical 'convergent practitioner vocabulary/pattern' language for both, blurring the tier distinction. No false claim made — precision gap, not fabrication.
    - Fix: Note the tier explicitly for /compact, e.g. '(3 independent posts, L2-strong)'.
  - [BLOCKING] §11a Each listed source carries a freshness STAMP, not just a to-do.: Ran source-freshness.sh: only the ccstatusline line matches the required [checked:.../result:.../due:...] grammar (1 ok). The two convergence-sourced citations are bare prose with no checked/result/due fields, not even in the claim-id-substituted no-URL form the spec explicitly provides. Audit script silently reports clean while 2 of 3 sources carry no verifiable ledger entry.
    - Fix: Convert both to the claim-id stamp variant per source-freshness-format.md, e.g. `[checked:2026-07-10 result:OK due:cohort] convergent:three-failure-modes — [practitioner analysis] ...`

### `curriculum/lectures/skills-from-the-frontier.md`

- **story** REVISE (2/0):
  - [BLOCKING] §3 No cross-module sequencing in lecture bodies (hard-grep M[0-9]/module-name above the maintainer fence).: Line 6: 'your Module 2 plan file' — a bare module-number reference in body prose. The file's own 2026-07-02 maintainer note claims this was already de-sequenced and that '§3 grep above the fence: clean' — false against current content; independent grep confirms the hit still exists.
    - Fix: Drop 'Module 2'; use the maintainer note's own already-approved phrasing: 'against your plan file … arrived unpackaged.'
  - [BLOCKING] §1 Mood contract named; mood-floor 8+/10 per teaching beat.: Phase mood 8 (slides 1-5 land well). Close mood 7: the maintainer block claims the 'Go.' closing kicker is present/untouched, but the current body has no closing send-off at all — it ends on an install-path caution ('Don't invent new homes'), undershooting its own documented close.
    - Fix: Restore a short closing send-off line (the documented 'Go.' or equivalent) after the final bullet.
- **technical** REVISE (2/2):
  - [BLOCKING] §11 Source-verification maintainer block MUST DO before first cohort.: File carries named practitioner (Curran) + specific URL (ideas.fin.ai) + load-bearing numbers (153/267/31%) but has NO Source verification section, unlike 6 sibling AE101 lectures with comparable citations.
    - Fix: Add **Source verification — MUST DO before first cohort:** section to maintainer block, matching sibling lectures' shape/position.
  - [BLOCKING] §11a Each listed source carries a freshness STAMP, not just a to-do.: No [checked:YYYY-MM-DD result:RESULT due:DUE] stamp anywhere in file; the 're-verified live 2026-05-22' prose note is exactly the bare to-do shape this rule replaces.
    - Fix: Stamp the Curran/Intercom citation per curriculum/source-freshness-format.md grammar; the re-verify already happened, just needs the canonical format.
  - [todo] §12 Denominator + axis discipline on every adoption/productivity number.: '31% of R&D by Intercom's own count' doesn't spell the axis; source confirms it means headcount-share of contributors (153/~493), only inferable from adjacency, not stated.
    - Fix: Spell the axis once: '31% of R&D headcount are active contributors.'

### `curriculum/lectures/story-of-module-6.md`

- **slides** REVISE (2/2):
  - [BLOCKING] §1 Referent resolution under a sequential deck read.: chunk 1 "The numbers": line 11: "the draft of this very lecture had drifted from the frame I had given it" — "the frame" is never named in chunks 0-1; it is only introduced two paragraphs later at chunk 2 line 31 ("The frame I did not cite"), a forward reference under the sequential read. chunk 2 "Things that went sideways": line 25: "the framing the Story opener said we would cut" and "the contradiction with...the opener they had also read" — "the Story opener" self-refers to this very file by an internal scheduling label (the module file's maintainer Session-runtime line names this slot "Story opener 5"); that label never appears in student-facing body text anywhere in the deck, so a reader cannot resolve it as "this document." chunk 4 "Then I compounded the run": line 47: "The diff found rules that lived in memory but never fired" — "the diff" is never established as a process or artifact; the module's own prior signposting (Start here / Big Idea) uses "contrast" and "diagnose," never "diff," so the definite article presumes a term the reader has not met.
    - Fix: Move or restate "the frame" after its chunk-2 introduction; replace the self-referential "the Story opener" with a plain deictic ("this opener"/"what I just said above") or cut; ground "the diff" in the plain phrasing already used (comparing the two runs) before leaning on the noun.
  - [todo] §7 LLM-cadence caps.: Anaphora/epistrophe sub-item — chunk 3 "The generalisation": line 41: "The writing compendium was loaded. The mood contract was loaded. The delivery architecture was loaded." immediately followed by "Claude typed the banned word anyway. Claude recommended the flattering end-state anyway. The first proposal was chat-shaped anyway." — two back-to-back epistrophe triples ("...was loaded" x3, "...anyway" x3) stacked on one slide. Abstraction-noun-stack sub-item — chunk 2 "Things that went sideways": line 25 header: "The credibility-performance I did not notice." — coined abstraction compound; the concrete thing (the three-phrase benediction quoted two sentences later) should carry the header instead. Self-grading-superlative, number-retraction, verb-triplet, and Not-X-Not-Y-Z sub-items: no instances found — PASS.
    - Fix: Vary sentence shape across the loaded/anyway cascade instead of six near-identical short declaratives in a row; retitle the credibility-performance paragraph with the concrete quoted line instead of the abstraction compound.
  - [BLOCKING] §8 Internal dialect never lands on a slide.: chunk 2 "Things that went sideways": line 19: "Same rule, same compendium, same task"; line 29: "The compendium says 'verify capabilities before asserting, not after.'" chunk 3 "The generalisation": line 41: "The writing compendium was loaded. The mood contract was loaded. The delivery architecture was loaded." chunk 5 "What the loop is for": line 57: "even when nothing about the surface changed" (surface used as the noun). "compendium," "mood," and "surface as noun" are the compendium's own listed banned-example terms; "delivery architecture" is the same category of maintainer/repo artifact-naming (training-architecture.md's own section name) — four dialect hits across three different chunks.
    - Fix: Swap for what an engineer in the room would say: "the rules file," "the tone brief," "the session-timing plan," "even when the task looked the same."
  - [todo] §9 Emphasis budget — bold is for handles, and go very lightly.: chunk 2 "Things that went sideways": lines 17, 19, 21, 23, 25, 27, 29 — seven consecutive bolded paragraph-leads ("**Turn one.**", "**The banned word.**", "**The end-state that was not.**", "**The sims and evals I did not run.**", "**The credibility-performance I did not notice.**", "**The paraphrase I shipped as a quote.**", "**The platform fact I got wrong.**") on a single chunk. Budget is ≤1 bolded element on most slides; none of these are named laws/governors/coined terms/checklist items per the rule's definition of a handle — they are informal topic labels for each anecdote.
    - Fix: Flatten the seven bolded leads to plain prose topic sentences or a light non-bold em-dash lead; reserve bold for an actual coined handle if one exists on this slide.

### `curriculum/lectures/test-and-learn.md`

- **slides** REVISE (1/0):
  - [BLOCKING] §1 Referent resolution under a sequential deck read.: chunk 1 "Every send-off is an experiment": line 9: "You're new to this country; don't travel it like a tourist." "This country" is never named anywhere in test-and-learn.md up to this point. It directly echoes the-far-half.md's body line "You are not handed the laws of this country up front" (the-far-half.md is the immediately preceding lecture in run-the-first-experiment.md's include order), but that grounding lives only in the sibling file, which sequential-deck-read scope excludes. The 2026-07-09 emphasis pass collapsed this slide's bullets into three prose paragraphs (maintainer block, 'near-verbatim'), the reflow shape rule 1 exists to catch.
    - Fix: Ground the metaphor in this file (name what the reader is new to directly, e.g. the un-watched long-run territory) or cut 'this country' and let the tourist/practitioner contrast carry the point on its own.

### `curriculum/lectures/the-gate-is-a-claim.md`

- **story** REVISE (1/0):
  - [BLOCKING] §1 Mood contract named; draft honours mood, or flattens it?: Trace mood_score 6/10 (floor 8+ for teaching beat, <7 REVISE). Opening line 'The re-send came back green. The verifier you built read the work and passed it.' asserts a specific already-resolved favorable outcome. Contradicts: (a) sibling closer what-packaging-is.md's own opening ('the packaged re-send is running and the laptop is closed again' — present progressive, still in flight); (b) trainer-modules.md's documented pacing ('the packaged re-send fires and runs through lunch' — result read only at M6 open); (c) the strategy doc's three delivery formats, all of which place result-reading at M6, not this closer; (d) this lecture's OWN maintainer 'Mood' note ('the verifier just landed the packaged re-send' = deployed, not resolved) — body overshoots the author's own stated intent. Mood contract demands 'NOT resolved optimism'; a definite favorable outcome asserted before it exists in the room is the opposite move. Sibling lectures consistently hedge open-outcome moments ('whatever came back,' 'finished, hit its budget, or was stopped') — this file breaks that house convention.
    - Fix: Open on the room's actual live state (run in flight, outcome unknown, hedged) as what-packaging-is.md and reading-the-return.md do — e.g. reframe as the general/hypothetical case ('say the re-send comes back green') rather than a settled first-person-past fact about 'the verifier you built.'
- **strategy** REVISE (1/1):
  - [BLOCKING] §4 Strategy-fidelity check — cross-module sequencing / front-run: File's 'One run is a sample' section ('passing again and again shows it is dependable ... run it repeatedly ... judge it on pass rates across several runs') delivers, at M5's own close, the exact realization the strategy doc assigns to the M6 pre-read: OODA gap table row 4 'Verifier reliability (10-run rule) | Home: M6 (pre-read)' (~line 699) and M6-in-detail's 'Student arrives at M6 already knowing reliability is a measured property' (~line 455). Module file learn-from-the-test.md already shows the drift downstream — its M6 pre-read blurb now says 'You'll recognise both in your own artefacts when Module 6 gets there' (lines 114-118), i.e. concedes the idea already landed once. Neither the OODA gap-table row nor the 'arrives already knowing' framing nor the M5 Delivery-shape lecture inventory (which lists only 3 lectures, not this one) has been updated to reflect the promotion. Target file's own maintainer block concedes: 'No Quality line by design (unaudited)... re-audit before ship,' and learn-from-the-test.md's Quality line shows its strategy pass (strategy@1ff6f8a) predates the lecture-link insertion at its line 102.
    - Fix: Either trim/reframe 'One run is a sample' so it doesn't fully pre-deliver the M6 pre-read's 'reliability is measured, not a vibe' realization, or — if plant-then-recognise is the deliberate design (module file's 'recognise' wording suggests it may be) — update the strategy doc's OODA gap-table row 4 + M5 lecture inventory + M6 'arrives already knowing' language to document the repeat as chosen. Keep file and strategy doc in the same state per curriculum/CLAUDE.md's 'Strategy and module file change in the same edit. Drift = process bug.'

### `curriculum/lectures/the-loop-half-filled.md`

- **slides** REVISE (1/0):
  - [BLOCKING] §1 Referent resolution under a sequential deck read.: chunk 5 "Reading was never the control": line 124: "The delta note held the map against what you already knew of the codebase, in both directions." — "the map" collides with this file's own load-bearing spine referent, established repeatedly in chunk 1 (line 5: "Back to the map" / "without the map" / "The map arrived at M2"; line 6: "the strands the map draws") and depicted in the figure as the Intent/Context/Work/Verification/Absorption/Outcome engine map. Nothing anywhere in this file names the M3 access-surface artifact from `map-the-access-surface.md` "the map" — a sequential-deck reader who has just spent the whole file on the spine map has no signal to re-resolve "the map" here to a different, unrelated per-exercise artifact.
    - Fix: Rename the delta-note bullet's referent (e.g. "held the access map against what you already knew") so it stops reusing the spine's "the map" noun for a second, unrelated artifact.

### `curriculum/lectures/the-machine-you-just-met.md`

- **strategy** REVISE (1/0):
  - [BLOCKING] §3 Lead with discipline, not failure mode: Line 12 header '## Unchecked, mostly-right steps stack into wrong' states the failure trajectory as the slide's title; the positive discipline that resolves it ('A check from outside the run resets the chain', line 16) is demoted to body copy three bullets down. Title (line 1), kicker (line 32, 'the rest is steering'), and slides 1/3 headers lead correctly — only slide 2's header inverts the rule's required order.
    - Fix: Retitle slide 2 around the already-present discipline (e.g. 'A check resets the chain'); let 'stack into wrong' live in the body as the negative case. Narrow, framing-only fix.

### `curriculum/lectures/the-whole-map.md`

- **strategy** REVISE (1/0):
  - [BLOCKING] §4 Strategy-fidelity check — cross-module sequencing / no front-run of downstream teaching moment: L88, L92-93 (Intent/Absorption/Outcome phase-bullets) already deliver mechanism/governor content that the strategy doc assigns downstream ('absorption-bottleneck (M4 names it)', L613) and that this file's own maintainer block (L123) commits to reserving for 'the M3-close job, not this opener's' — the-loop-half-filled.md L118 and the-map-filled-in.md L175/L183 later present near-verbatim wording under explicit 'named' headings.
    - Fix: Trim Intent/Absorption/Outcome bullets back to bare map-position, matching the discipline already held by the Verification and Context bullets.

### `curriculum/lectures/the-wizard-move.md`

- **writing** REVISE (1/0):
  - [BLOCKING] §13 Value-prop / positioning framing belongs in sales copy + project strategy docs, NOT student-facing curriculum body.: Line 7-9 contains defensive positioning against typical training expectations. "Nobody here needs the \"agents can write code\" setup. So this training won't run one. ... That's the posture of the whole training." explicitly announces curriculum posture.
    - Fix: Cut lines 7-9 entirely. Positioning should emerge implicitly from teaching, not be announced as meta-commentary. Flow directly from 'Who this is for' to 'Context is King' demonstration.

### `curriculum/lectures/what-packaging-is.md`

- **writing** REVISE (1/1):
  - [BLOCKING] §check_prompts.md:2 Every prompt block: one-sentence action lead-in.: Line 233 lead-in is 28 words; rule limit <20 words. Current text: 'In the session where you built the verifier, ask Claude to propose five hooks tied to this repo and the work you just did, beyond formatting and linting.'
    - Fix: None
  - [todo] §check_student_facing.md:23 Reflection beats stay invisible in flow.: Line 6: 'Pause for a second.' is a contemplative-pause prefix opening (banned shape per hard-grep list), announcing the reflection beat rather than letting it land implicitly.
    - Fix: None
- **slides** REVISE (1/1):
  - [todo] §9 Emphasis budget — bold is for handles, and go very lightly.: chunk 1 "One run, plotted": line 200: "**A check is a position fix**" — 6-word bold spanning a whole declarative clause, over the ≲5-word handle cap (pre-existing; already logged accept-with-note in the 2026-07-09 emphasis-pass judge run per pre-cohort-todos.md). chunk 2 "The three-pattern": lines 209-210: "**Reference artefact**" + "**plan.md**" — two bolded handles on one slide, over the ≤1-bold-per-slide budget (pre-existing; same 2026-07-09 run logged accept-with-note under the naming-slide exception). chunk 6 "Hooks for must-happen, prompts for taste": line 232: "**Must happen goes in a hook**" — same 6-word whole-clause-bold shape as the chunk-1 law; maintainer's 2026-07-09 note lists it as a kept "(governor)" handle but it is NOT among the two items that run's accept-with-note list names — an untracked instance of the same pattern.
    - Fix: Trim these three bolds to ≲5-word noun handles (or drop the bold), or log each explicitly as an accepted law/governor callout in the maintainer block — currently only 2 of 3 are recorded.
  - [BLOCKING] §11 Numbering — position is computed, content ordinals are linted.: This file declares no "## Phase N" headers of its own, so every "Phase K" mention in its body is necessarily a cross-file pointer at exercises/diagnose-and-resend.md's own Phase headers, none file-qualified. chunk 2 "The three-pattern": line 209: "the reference you assembled at Phase 4 plays it" (points at diagnose-and-resend.md's "## Phase 4: Write the reference and plan.md"). chunk 4 "Three shapes the verifier takes": line 223: "You picked one at Phase 3 against your dominant failure" (points at "## Phase 3: Build the verifier for your worst failure"). chunk 9 "At org scale: Intercom's tiers": line 262: "That is your verifier from Phase 3, scaled to an R&D org of roughly 500" (same Phase 3, third occurrence). Rule 11 requires cross-file phase refs to live in the maintainer block only, file-qualified; all three are student-facing body prose with no file name attached. scripts/check-slide-numbering.js does not catch this — its checkRefs() early-returns when a file declares zero Phase headers of its own, so it only lints within-file dangling refs, never cross-file ones; this is a judge-only catch.
    - Fix: Name the exercise at each mention ("the reference you assembled at diagnose-and-resend's Phase 4"), or drop the bare phase number — "the reference you assembled"/"the verifier you picked" alone already resolve fine under the session-artifact carve-out without the numeral.

### `curriculum/lectures/when-a-plan-is-good.md`

- **slides** REVISE (1/0):
  - [BLOCKING] §8 Internal dialect never lands on a slide.: chunk 1 "Plan mode is a read-only permission state": line 14: "The plan is an artifact, not a mood."
    - Fix: "mood" is on the compendium's literal banned-dialect list with no ordinary-usage carve-out written for it (unlike "surface"); this instance reads as plain-English ("not a whim/vibe"), not the curriculum's Mood-target sense — swap for "a whim"/"a feeling" to fully de-risk, or log an explicit accept-with-note if the maintainer judges this occurrence non-dialect.

### `curriculum/exercises/author-test-strategy-skill.md`

- **story** REVISE (2/1):
  - [BLOCKING] §16 Forcing functions live in prompts, not body prose.: Line 7 ('What you do:') promises 'Before you ship, ask the skill to disclose its own weakest part.' Neither curriculum/prompts/author-test-strategy-skill-1.md nor -2.md enacts this ask. Prompt 1 ends 'write ...SKILL.md... Show me before saving' with no self-critique; prompt 2 grades the produced TEST STRATEGY OUTPUT, a different target than the SKILL.md's own weakest part. This exercise's own maintainer Push-back-moves list has no recovery entry either. Git history (commit 677a584, 2026-04-28) shows a whole 'Phase 2: self-critique before shipping' beat with its own prompt ('What's the weakest part of this skill?') was deleted while line 7's promise was left standing — a 2+-month-old drift multiple audit cycles missed.
    - Fix: Restore a self-critique ask inside a prompt fence, or cut the line-7 promise and earn-the-trust.md's matching failure-mode-table row so body and mechanism agree. Prompt-fence fix is maintainer-approval-gated (check_prompts.md §22).
  - [todo] §41 Artifact-vs-STOP tail ordering — last imperative wins.: Prompt 1's tail 'write ...SKILL.md... Show me before saving' technically gates the persisted write behind an unscripted approval turn, while Phase 3's body asserts unconditionally 'The agent wrote it there in Phase 1.' Low-severity — natural conversation continues with 'save it' — but the fence's last imperative doesn't literally guarantee what the body assumes.
    - Fix: Make the continuation explicit in the fence ('...before saving; once I confirm, save it') if Phase 3's claim must hold exactly.
  - [BLOCKING] §4 Strategy-fidelity (consolidates front-run check, module arc, Big Idea fidelity, mood-doesn't-resolve-early).: Front-run check, module-to-module arc, and mood-doesn't-resolve-early are all clean. Big Idea fidelity fails: strategy doc names the self-critique as 'theme #4 used as designed' and the parent module file (earn-the-trust.md) restates it in Key Concepts, What-You'll-Learn, and its failure-mode table — but this exercise no longer contains the move in any prompt (same root cause as check_pedagogy.md #16).
    - Fix: Same fix as check_pedagogy.md #16 resolves this row.

### `curriculum/exercises/compound-and-close.md`

- **technical** REVISE (1/6):
  - [todo] §1 Source URL + type label mandatory.: line 61: Klaassen's every.to citation labeled bare [practitioner direct]; every.to is Klaassen's own employer's publication so the correct label per this rule's own sub-clause is [practitioner direct, vendor venue]. 7+ sibling AE101 files already use the correct label for this exact URL; only this file and push-back-on-the-plan.md still carry the bare label.
    - Fix: Change to [practitioner direct, vendor venue] on line 61.
  - [BLOCKING] §4 Hold short OODA loose — six practitioners != Level 3 (threshold 10-20).: line 61: 'Convergence Level 3' is asserted for Klaassen's compound-engineering framing, but the cited source file (continuous-research/platform-watch/coding-agents/runs/2026-04-21-klaasen-compounding-engineering.md) itself says: 'Convergence signal present but the term itself is still mostly Every-originated... Confirmation-bias guard: The term is Every's term traveling outward, not a widely-converged community vocabulary.' The source names one primary practitioner plus a handful of independent echoes — nowhere near the 10-20 threshold. Phrase 'Convergence Level 3' is unique to this file (grepped curriculum/ and continuous-research/, no other occurrence) — not an inherited repo-wide label.
    - Fix: Drop 'Convergence Level 3' or replace with what the source actually supports (single-practitioner-originated, early independent echoes — roughly L1-L2), per the source's own confirmation-bias guard.
  - [todo] §11 Source-verification maintainer block required on files with practitioner citations.: File cites named practitioner + URL but no load-bearing number, so the strict trigger is arguable. Repo convention centralizes the stamped 'Source verification' section at MODULE level (correctly present on run-the-first-experiment.md/M2, learn-from-the-test.md/M5, spot-gaps-build-the-loop.md/M6); M1's hub file getting-going.md is missing that section for this same citation — the debt sits one file up.
    - Fix: Add a stamped 'Source verification' section to getting-going.md for the every.to URL, matching M2/M5/M6.
  - [todo] §11a Each source carries a freshness STAMP, not just a to-do.: Ran source-freshness.sh --target 2026-09-01 against this file: 'No source-freshness stamps found' (exit 0, script treats this as benign given stamps live at module level). Same root cause as rule 11.
    - Fix: Bundle with rule 11's fix at getting-going.md.
  - [todo] §2 Every prompt block needs a one-sentence action lead-in immediately before the label.: compound-and-close-1's fence is immediately preceded by the 'Long session, long read' steering blockquote, not an 'Ask Claude to...' lead-in (contrast: compound-and-close-4 correctly has one; orient-and-introspect.md puts its blockquote BEFORE the lead-in, lead-in last).
    - Fix: Add 'Ask Claude to review the session and write your rules file.' between the blockquote and the marker.
  - [todo] §17 Preamble before action — 4+ named sub-steps need suppressor or primer.: compound-and-close-1 has 4 named sub-steps but neither an in-fence 'No preamble' suppressor nor a body skim-past primer.
    - Fix: Low-stakes; add 'No preamble.' as fence's last line if drift observed in practice.
  - [todo] §20 Grill before save — collaborative quality at artefact assembly.: Writes ./CLAUDE.local.md immediately with no grill-before-save step, though the registry shows 5+ downstream exercises consume it. Likely deliberate: exercise explicitly rejects an upfront interview ('doesn't interview you with three retro questions'), and the same-sitting 'Push back where the summary misreads' section substitutes for pre-write grilling.
    - Fix: Flag for maintainer judgment; a forced grill-before-save would fight the exercise's own stated point.
  - [todo] §34 Disambiguation/anchors/decision logic must bind Claude live inside the fence.: Same defect as rule 40b: 'the /context read' clause does not resolve against realistic session state.
    - Fix: See rule 40b fix_hint.
  - [todo] §40b Warm != referent-present — walk each definite reference in the fence.: Fence opens 'the orientation and introspection, the /context read, the TDD bug fix, the diff push-back.' Three of four referents check out via the registry graph (introspection turn, TDD fix, free-form diff-pushback turn). The fourth, 'the /context read' (prompt orient-and-introspect-3 is literally just '/context'), is per WebFetch of code.claude.com/docs/en/commands (2026-07-11) a client-side visualization command with no documented signal its output is logged into model-visible context — Claude reviewing 'this session end-to-end' may have no actual memory of that turn. Docs-derived, not live-tested; flagged live-probe-owed per check_platform_and_boundaries.md #5a. Low practical impact: one clause of four, other three give ample real material.
    - Fix: Live-probe /context visibility; if confirmed invisible, drop the clause or have the student narrate the reading back to Claude in chat.
  - [todo] §43 Long-running prompts stream progress — never go silent.: Body blockquote self-acknowledges a multi-minute run (reactive/steerable half, pedagogy #48 covered), but the fence carries no proactive in-fence progress-streaming instruction.
    - Fix: Add a light in-fence cue, e.g. 'If this takes a while, tell me what's standing out as you go.'
- **strategy** REVISE (1/0):
  - [BLOCKING] §4 Strategy-fidelity check (cross-module sequencing/scope claims honored; folds in Big-Idea-match): Five passages in the private strategy doc (M1 table Artifact cell L237; Ex3 bullet L268; 'why three exercises' artifact list L270; L272; done-criteria L280) still place the MCP connector-wire + ticket-close INSIDE Ex3/compound-and-close, and the 'Homework after M1' subsection (286-292) lists only the two optional readings, no ticket-close task. The file itself now says 'closing the bug's ticket outside the repo, is the Module 1 homework' and its maintainer block confirms a deliberate split into a new close-the-ticket.md exercise ('Re-audit owed... Re-audit before ship'). Verified curriculum-side: close-the-ticket.md exists (re-parented prompts compound-and-close-2/-3, cites check_pedagogy.md §17) though itself has no Quality line yet; getting-going.md's Big Idea/What-You'll-Learn/Key-Concepts/new Homework section all already reflect the split. The private strategy doc has not been updated to match — unreconciled drift.
    - Fix: Update content-strategy-agentic-engineering-101.md in the same pass: move ticket-close out of Ex3's scope in the M1 table (237), the Ex3 bullet (268), the artifact-list sentence (270), lines 272/280, and add close-the-ticket.md as a required (not optional) M1 homework item at 286-292. This is a strategy-doc edit, not a further edit to the exercise file.

### `curriculum/exercises/diagnose-and-resend.md`

- **strategy** REVISE (1/0):
  - [BLOCKING] §4 Strategy-fidelity check — 'M_k owns Z' sequencing claims honored, no front-run: File's closing line 'M6 will cut one stale rule once the contrast lands' assigns the rules-file-subtraction Compound move to M6. Strategy doc's Woven design rule #1 assigns it to M5 instead ('M5 *subtract a rule*... M6 *second-skill authoring in the exercise body IS the compound*'). The reassignment is deliberate and cross-file-consistent (learn-from-the-test.md: 'no M5 sharpening by design; the re-send + the M4↔M5 contrast IS M5's compound move'; spot-gaps-build-the-loop.md ships a full Phase-1 forcing function for cutting the stale rule) but the top-level doc line was never updated to match — contradicting its own source-of-truth claim.
    - Fix: Reconcile in one edit: most likely amend Woven design rule #1 to move 'subtract a rule' from M5 to M6 (matching the mature, already-shipped M6 apparatus), since reverting the shipped module/exercise design would be far more disruptive. Maintainer call, not a judge call.

### `curriculum/exercises/extract-the-task-shaping-rule.md`

- **writing** REVISE (2/1):
  - [todo] §21 LLM-vs-agent-vs-Claude vocabulary discipline.: None
    - Fix: None
  - [todo] §1 No placeholders inside fences.: None
    - Fix: None
  - [todo] §2 Every prompt block: one-sentence action lead-in.: None
    - Fix: None
- **pedagogy** REVISE (1/4):
  - [BLOCKING] §16 Forcing functions stay, scripted reactions go; forcing functions live in prompts, not body prose: 'Wire the file in' section is body prose (no fence): 'If the agent proposes a path other than ./CLAUDE.local.md, ./CLAUDE.md, or ~/.claude/CLAUDE.md, ask Claude to also propose the @import line that wires the file in.' Reading extract-the-task-shaping-rule-2's fence alone (rule's own diagnostic), it never asks for the @import wire-up. A body-skipping speed-runner who picks a non-auto-load path ends with a rules file that silently never fires — contradicting the exercise's own promise ('saved at a location you chose so it fires when you want it'). The module's Artefact-contracts table independently states the same expected mechanic, confirming it's load-bearing, not optional flavor.
    - Fix: Fold the conditional @import ask into extract-the-task-shaping-rule-2's fence so it survives a body-skipping speed-run; narrow-lens flag, reconcile via /content-creation.
  - [todo] §44 Plug points + UX: Plug points fine (session scrollback structurally required). UX: fast-operator walkthrough skipping body never surfaces the @import wiring step for a non-auto-load path — same root cause as rule 16, viewed through the UX lens.
    - Fix: Same fix as rule 16 — move the conditional @import ask into extract-the-task-shaping-rule-2's fence; closes both gaps in one edit.
  - [todo] §45 Per-exercise leap test — 3 observable Monday outcomes: Leap-test items 1 and 2 are falsifiable via scrollback/file inspection. Item 3 ('deflects the build instinct toward capture-first') reads as a dispositional/competency claim rather than an observable verb+object.
    - Fix: Rewrite item 3 as a falsifiable action, e.g. 'when asked to automate a backlog process, states the rules-file-first order before requesting any build/code.'
  - [todo] §52b Widgets carry semantics, not just emphasis: No widget used anywhere. The @import instruction is a must-not-be-missed silent-failure guard yet sits as plain bullet prose with no visual signal.
    - Fix: If the @import step stays in body after the rule-16 fix is weighed, wrap it in a Note/HOX widget per widgets.md.

### `curriculum/exercises/fix-tests-first.md`

- **story** REVISE (1/1):
  - [BLOCKING] §16 Forcing functions live in prompts, not body prose.: Lines 24-27, 'Read the diff and push back on a line' section: the push-back move ('Read the diff. If a line isn't what you'd have written, push back. Quote the line and say why.') has no fence at all — it is mandatory (named in 'The point' line 9 as one of two disciplines 'Both get practised here', Theme 4 in the maintainer block, and getting-going.md's Leap Test #2, a falsifiable Monday-morning outcome), so the loose-optional-prompt carve-out (compounded 2026-05-02) doesn't apply. The read-prompt-only diagnostic fails: a student pasting fix-tests-first-1 then fix-tests-first-2 never sees this instruction. The sim trace's own confusion_flags entry independently names this exact self-study-skip risk, mitigated only by in-room trainer narration which the artifact doesn't structurally guarantee.
    - Fix: Convert to a fenced open-hook prompt (check_prompts.md open-hook shape, mirroring fix-tests-first-1's own pattern), e.g. 'Push back on a line in the diff. Quote the line and say why:' — preserves student's-own-wording intent while making the beat reachable outside trainer narration.
  - [todo] §27 Classroom = default delivery; body teaches work, trainer manages room.: Line 51: 'chatter about making sure the LLM doesn't fake tests or write tests that just pass.' 'Chatter about X' is a cohort-management/conversation verb rather than a work-artifact instruction, though it names no explicit interlocutor (borderline; low severity, optional early-finisher aside only).
    - Fix: Reframe as agent-directed reflection ('ask Claude how it would catch...') or cut; not load-bearing for the module's leap tests.
- **strategy** REVISE (1/1):
  - [BLOCKING] §4 Strategy-fidelity check (cross-module sequencing / artifact-shape claims honored): PRIMARY: the strategy doc contradicts itself on what this exercise feeds, and auditing this file surfaces it. § M1 in detail (line 268) says Ex3 writes './CLAUDE.local.md from evidence — no Q1/Q2/Q3 interview, no pre-existing rule being extended; the rule is born here [Ex3]', and Ex2's own bullet (line 267) says 'Stop there; the compound step lives in Ex3.' But the M1 summary table (line 237) still reads 'P2 fix tests-first, no plan mode, seed CLAUDE.md mid-exercise; P3 retro extends CLAUDE.md' — wrong shape (seed-then-extend vs. born-once) AND wrong file (team CLAUDE.md vs. personal CLAUDE.local.md, which the module's Key Concepts and Artefact-contracts table both confirm as correct). fix-tests-first.md itself contains zero CLAUDE.md/CLAUDE.local.md editing action anywhere in its body — it correctly matches the detail section, not the stale table row. The table row is the stale side of a doc-internal contradiction, but it needs fixing since a maintainer reading only the summary table would misdescribe Ex2/Ex3. SECONDARY, non-blocking: this file's own maintainer Watch-fors line coins 'Quality-Gate entry (landing in M4)' — the backbone term's content-fit is M3, and M4's actual content (gap-analysis subagent over CLAUDE.md/memory/ADRs/skill/connectors) has no 'Quality-Gate entries ledger'; the phrase also contradicts this same file's own 'Decision points' line two rows below, which uses plain 'no verifier on this path' for the identical scenario, and getting-going.md's parallel maintainer note, which also skips the term.
    - Fix: Primary (strategy-doc edit, not an exercise-file edit — fix-tests-first.md is already correct): in content-strategy-agentic-engineering-101.md line 237, strike 'seed CLAUDE.md mid-exercise' / 'P3 retro extends CLAUDE.md'; sync to the M1-in-detail shape (Ex3 writes ./CLAUDE.local.md fresh from evidence, no interview, no extension). Secondary (log as todo): drop 'Quality-Gate' from this file's Watch-fors line, match the plainer 'no verifier here' phrasing already used two lines below and in getting-going.md's parallel note.

### `curriculum/exercises/map-the-access-surface.md`

- **slides** REVISE (2/2):
  - [BLOCKING] §1 Referent resolution under a sequential deck read.: chunk 7 "Push back until the reason names your codebase": line 100: "the sharpening question" has no antecedent anywhere earlier in the file; the paired companion prompt-sharpen card is noted in the maintainer tail as pending, never shipped into the body.
    - Fix: Surface/quote the actual sharpening question inline, or drop the definite article ("push back until the reason names something specific to your codebase").
  - [BLOCKING] §2 Cross-file and positional callbacks.: chunk 4 "Kick off the run, then work the other window": line 58: "(Exercise 3, Phase 1)" is a bare ordinal pointer into author-test-strategy-skill.md; that numbering exists only inside earn-the-trust.md's own maintainer-only tables, past its own maintainer cut -- unresolvable by a student reading this deck.
    - Fix: Name the target by title and unpack the claim instead of the bare "Exercise 3" ordinal.

### `curriculum/exercises/open-the-side-quest.md`

- **technical** REVISE (1/4):
  - [BLOCKING] §4 Verify capabilities BEFORE asserting, not after; audit-mode not exempt.: L43 claims 'Exercise 3 returns to the main lane to invoke it on the security-tested feature.' Cross-checked against curriculum/exercises/author-test-strategy-skill.md, whose own header reads 'Window: the side quest window (m3-quality). All phases run here' and whose Phase 2 prompt invokes the skill 'on this codebase and repository... in this worktree' -- not on the main lane, not narrowly on the security-tested feature. This is the module opener, read before the student meets Exercise 3, so the wrong mental model is set up front.
    - Fix: Rewrite L43 (and the echo at L65) to match Exercise 3's actual shape: quality lane stays in its own window for all phases and invokes on the whole codebase; the return-to-main-lane read happens at a later, separate closer beat that reads scrollback rather than re-invoking.
  - [todo] §30 Skills at ~/.claude/skills/<name>/ are user-global but per-codebase in content; namespace by SUT.: L43/L51/L65 all use the bare unsuffixed path ~/.claude/skills/test-strategy/SKILL.md.
    - Fix: Already logged and accepted-as-is for first cohort in pre-cohort-todos.md L22; no new action owed, flagged for ledger completeness only.
  - [todo] §1 Source URL + type label mandatory for a specific named technique.: L83: Boris Cherny, 'Mastering Claude Code in 30 minutes' cited with no URL and no [label]. The same talk already has a canonical URL elsewhere in this repo (getting-going.md L57).
    - Fix: Add the URL + [practitioner direct] label. Non-blocking: maintainer-block-only, no number/quote riding on it.
  - [todo] §2 Every prompt block gets a one-sentence action lead-in with a command verb.: Neither lead-in (L25, L43) is a command-verb summary of what its prompt does; L43 in particular describes a different, later exercise instead of the orient prompt it precedes.
    - Fix: Add semantic action lead-ins, e.g. 'Ask Claude to fork a sibling worktree...' / 'Ask Claude to confirm the worktree state and wait.'
  - [todo] §38 Body lead-in mirrors the prompt's opening phrasing.: L43's lead-in ('You author the test-strategy skill here...') shares no verb/noun-phrase with the orient prompt's opening ('Confirm worktree state:').
    - Fix: Fix alongside rule 2; re-check mirroring once the lead-in is rewritten to describe the actual prompt.
- **slides** REVISE (1/0):
  - [BLOCKING] §2 Cross-file and positional callbacks.: chunk 0 "Two windows on one codebase (H1 + lede)": line 9: "a quality side lane in a sibling worktree that waits until Exercise 3" — points at a sibling file (author-test-strategy-skill.md) purely by ordinal slot. Verified against the host module's own student-facing signposting: earn-the-trust.md '## Start here' (lines 33-41, above its maintainer cut) lists exercises by title only; the Exercise 1/2/3 numbering exists only in that module file's maintainer-only artefact-contract table (lines 156-157), so it is not prior context the module-signposting carve-out can lend. Repeated verbatim at chunk 3 "Open the quality lane in the new worktree": line 56: "The skill installs to user scope, so Exercise 3 returns to the main lane to invoke it on the security-tested feature." Reordering the module's exercise sequence would silently strand both references.
    - Fix: Name the target exercise by title or claim instead of ordinal — e.g. "until you author the test-strategy skill" rather than "Exercise 3".

### `curriculum/exercises/push-back-on-the-plan.md`

- **technical** REVISE (3/7):
  - [todo] §4b Compendium platform claims rot at 60 days.: 'current as of 2026-04-22' stamp (line 133) is ~81 days old; independently re-verified live and still accurate, but stamp should refresh.
    - Fix: Bump the date stamp to today after re-verification.
  - [BLOCKING] §1 Source URL + type label mandatory.: Pocock citation (line 134) has URL but no [label] at all; permission-modes.md (line 133) mislabeled [practitioner direct] for vendor docs; Klaassen/every.to (line 135) missing the compendium's own required [practitioner direct, vendor venue] qualifier (confirmed via WebFetch: Klaassen is GM of Cora, Every's own product); Roger Martin (line 136) has no URL at all.
    - Fix: Add the missing Pocock label; relabel Klaassen with vendor-venue qualifier; fix or drop the docs mislabel; add URL/[SOURCE NEEDED] to Martin.
  - [BLOCKING] §11 Source-verification maintainer block required on files with practitioner citations.: File cites three named practitioners (Pocock, Klaassen, Martin) with URLs/claims but has ZERO 'Source verification — MUST DO before first cohort' section anywhere in the maintainer block (confirmed by direct grep, no hits).
    - Fix: None
  - [BLOCKING] §11a Each listed source carries a freshness STAMP.: Same root cause as §11 — zero [checked:...result:...due:...] stamps present anywhere in the file.
    - Fix: None
  - [BLOCKING] §38 Body lead-in mirrors prompt's opening phrasing.: Body claims the walk-down runs 'one question at a time' in three places (lines 7, 59, 67), but push-back-on-the-plan-2's actual fence says 'Ask me three questions at a time... I'll confirm or correct three at a time.' Only line 77 states the true cadence. Independently corroborated by the tmux-runner scenario file's own comment: 'push-back-on-the-plan-2 loops "three questions at a time" by design.'
    - Fix: Fix lines 7, 59, 67 to say 'three questions/branches at a time' to match the prompt's real mechanism.
  - [todo] §16 Forcing functions live in prompts, not body prose (technical: prompt-encodes-the-mechanism).: Cross-references the same cadence-mismatch finding — the mechanism is correctly encoded in the prompt, but body prose describes a different one.
    - Fix: None
- **slides** REVISE (1/1):
  - [BLOCKING] §1 Referent resolution under a sequential deck read.: chunk 7 "Phase 4: Walk down every unresolved branch": line 64: "the lock-it-in convention only fires inside plan mode." — 'lock it in' is not named or explained anywhere earlier in the file (first occurrence of the word 'lock' at all). The fenced prompt directly below in the same chunk (line 72: "Don't touch the plan file until I say 'lock it in.'") only uses the phrase as an instruction to Claude, not an explanation to the student of what the convention does; the actual payoff — "Say lock it in. The agent writes the sharpened plan." — doesn't land until chunk 10 ("## Approve", line 115), two slides later. A reader hits a definite, named 'convention' at line 64 with nothing yet on record to resolve it against.
    - Fix: Either drop 'the lock-it-in convention' phrasing here and say plainly that plan-file edits stay locked while in plan mode, or pull a one-clause explanation of what saying 'lock it in' does ahead of this bullet.
  - [todo] §7 LLM-cadence caps.: chunk 9 "Stop when the read starts reaching": line 87: "Approve when it's yours. Not Claude's, not the second read's." — a paired-negation-after-declarative tic, close kin to the banned 'Not X. Not Y. Z.' construction (order flipped: affirmation first, then the two negations merged into one sentence). Other sub-items checked and cleared across the whole body: no anaphora/epistrophe triples; no self-grading superlatives; no number+retraction hedges ('Eight to twelve isn't unusual' at line 32 and '5 to 12 questions' at line 87 are stated ranges, not self-undermined); no abstraction-noun stacks; no empty-leg verb triplets. The one other lone 'Not' (chunk 1, line 16, 'Not an epic.') is a single negation, not the tic.
    - Fix: State ownership once — e.g. 'Approve when it reads like your plan' — and drop the Claude's/second-read's negation pair.

### `curriculum/exercises/spot-gaps-build-the-loop.md`

- **technical** REVISE (1/5):
  - [todo] §30 Skills user-global but per-codebase — namespace by SUT.: Prompt-3 writes to bare '~/.claude/skills/session-shaper/SKILL.md'. Live-verified: that exact path currently holds a real, in-use skill on this machine (7605 bytes, mtime 2026-06-10) — a present collision, not hypothetical.
    - Fix: Tracked as 'accepted as-is, fires on future replay' in pre-cohort-todos.md line 22; this audit found the trigger already satisfied today. Backport the mechanical harness's SUT-suffix workaround (session-shaper-<project>) into the shipped prompt.
  - [BLOCKING] §1 Source URL + type label mandatory on every claim.: 'Three skill shapes... convergent practitioner vocabulary across Ramp Dojo, Intercom Tier 1/2/3...' carries zero URL and zero [label]. Sibling module file carries the fully-cited version of the identical claim; underlying claim is real and KB-backed.
    - Fix: Import the module file's existing label+URL for both practitioners, or replace with a one-line pointer to the module's maintainer block.
  - [todo] §11 Source-verification maintainer block required.: No 'Source verification — MUST DO before first cohort' section anywhere in this file's maintainer block. grep across curriculum/exercises/ for the section header returns zero files.
    - Fix: Systemic gap across the whole exercises/ library — batch policy decision, not a single-file fix.
  - [todo] §11a Each source carries a freshness STAMP grammar.: Same root cause as rule 11 — no stamp block exists to check.
    - Fix: Resolves automatically once rule 11 addressed.
  - [todo] §36 Verification machinery needs a smoke-test before trust.: Prompt-5 invokes the new skill only against the M5 run that already passed; no perturbation step proves the skill can go RED on a known-bad case.
    - Fix: Already PROMOTED action item in pre-cohort-todos.md line 28 with two named fix shapes; independently reconfirmed still open.
  - [todo] §2 Every prompt block gets a one-sentence action lead-in.: Prompt-3 and prompt-4 markers have no 'Ask Claude to...' lead-in sentence directly before them (only bullets/blockquote). Prompt-1's lead-in also runs ~25 words, over the <20-word guideline.
    - Fix: Add one-sentence lead-ins before prompt-3 and prompt-4 markers.
  - [todo] §3 Canonical shape — lead-in, label, blank, fence.: Same evidence as rule 2.
    - Fix: Same fix as rule 2; not double-counted.
  - [todo] §40b Warm ≠ referent-present.: Four instances of 'in Phase 1'/'we named in Phase 1' inside prompt fences (primitives, prompt-3, prompt-4, prompt-5) — a curriculum-coined heading label never typed by the agent in scrollback.
    - Fix: Drop 'in Phase 1', say 'the dominant gap we ranked' — one-line fix, low real-world risk (recency+uniqueness likely resolves it anyway).
- **strategy** REVISE (1/0):
  - [BLOCKING] §4 Strategy-fidelity check — cross-module sequencing claims honored ('M_k owns Z'): Strategy doc Woven design rule #1 (line 136): 'M5 *subtract a rule* ... M6 *second-skill authoring in the exercise body IS the compound* ... no canonical compound prompt.' This exercise (M6) instead ships the rule-cut itself: '## Cut one stale rule the diagnosis killed' (line 33) with prompt {{prompt:spot-gaps-build-the-loop-2}} (line 40) and its own push-back beat (lines 42-45); listed as one of only three headline artefacts (line 11: 'one stale rule cut from your rules-file'). Corroborated cross-module: M5's own exercise defers it explicitly — diagnose-and-resend.md:99, 'M6 will cut one stale rule once the contrast lands.' Three independently-authored files (this exercise, diagnose-and-resend.md, and the M6 module's artefact-contracts table) agree the cut ships at M6; only the strategy doc's rule #1 still assigns it to M5. Re-verified live: the doc's last edit (2026-07-10) added an unrelated reading-contract rule (#3); rule #1's M5/M6 split text is unchanged and the drift is current today, not stale prior-audit residue.
    - Fix: Convergent evidence (3 curriculum files) points at the strategy doc's rule #1 as the stale side, not this exercise. Fix in the same edit: update rule #1's M5 line to describe M5's actual Compound-step shape (packaged re-send + M4↔M5 contrast, per learn-from-the-test.md — no rule-subtraction at M5), move the rule-subtraction description to the M6 line. Do not relocate the rule-cut out of this exercise — that would contradict M5's own explicit handoff and orphan the M6 module's artefact-contract row.

### `curriculum/exercises/walk-and-send-off.md`

- **story** REVISE (2/0):
  - [BLOCKING] §1 Mood contract named; mood-floor 8+/10 per TEACHING beat.: trace mood_scores 8/8/6/close-6. Phase 3's teaching sub-beat ('What closes Module 4' steps 1-2) promises a rules-file compound pass that run-the-first-experiment.md's live close (line 53: 'No compound pass at the close... Compound on evidence, not anticipation') explicitly bans — verified directly against current HEAD, not just the cached trace.
    - Fix: Reconcile 'What closes Module 4' steps 1-2 against the module's live close sequence before this phase can clear 8+.
  - [BLOCKING] §4 Strategy-fidelity (consolidates front-run, module-to-module arc, Big Idea fidelity, mood-doesn't-resolve-early).: Front-run PASS, Big Idea fidelity PASS (Phases 1-3 map 1:1 onto strategy doc's M4 items 1-4). Module-to-module arc REVISE: this file's '## What closes Module 4' steps 1-2 (compound + push-back) match the private strategy doc's M4 item 5 near-verbatim, but run-the-first-experiment.md's live '## Send the task off' explicitly forbids a compound pass and its actual 6-prompt sequence has no compound/push-back step. Confirmed via git show 05fbd36 (2026-07-08, 'AE101 slides sweep'): the module file gained its no-compound line in the SAME commit that touched this exercise file's Quality stamp two lines below the untouched, now-stale compound/push-back steps — a three-way drift (strategy doc + exercise agree; module file disagrees) that this file's own 2026-07-03 maintainer note tried to reconcile once on a belief that no longer holds five days later.
    - Fix: Maintainer call: either restore a compound-at-close step to run-the-first-experiment.md (matching strategy doc item 5 and this file), or strip the compound/push-back framing from this file's line 15 and 'What closes Module 4' steps 1-2 to match the module's current design — then propagate the choice to the strategy doc, which currently sides with this file.
- **pedagogy** REVISE (1/1):
  - [BLOCKING] §16 Forcing functions stay, scripted reactions go. Forcing functions live in prompts, not body prose.: Lines 76–82, '## Phase 3: Settle the tree before the send-off', has three body bullets ('Ask Claude to propose any tidy-up...', 'Ask to see the diff before it lands...', 'Consider whether each rule is stored where it will fire') with NO {{prompt:...}} fence. Git-verified via 2f0c8a2 diff: the pre-rewrite Phase 3 carried a live prompt through the cut-candidate widget {{cut:walk-and-send-off-4|meta-retrospective}} (functionally equivalent to {{prompt:...}} per the renderer's own comment — 'the material is retained'), which drove the settle-the-tree action. The frame-removal commit retired that prompt and folded the surviving bullets into one section, but added no replacement fence. Every other 'Ask Claude to X' lead-in in this file (Phase 1 ~line 30, Phase 2 ~line 44, gap-fill ~line 59) is immediately followed by a {{prompt:...}} fence; Phase 3 is now the sole exception, and it is not a push-back/refinement carve-out — it IS the phase's primary, load-bearing action ('What you build' promises the tree 'settled on disk'; 'What happened' line 82 closes on it). Diagnostic per §16 (read each phase prompt-only) fails: skipping body leaves Phase 3 with nothing to execute.
    - Fix: Give Phase 3 a fenced 'propose tidy-up + show diff' prompt matching Phase 1/2/gap-fill's pattern, reconciled with the post-frame-removal settle-the-tree bullets (not a re-add of the retired three-block prompt).
  - [todo] §48 Heavy fan-out prompts get body recovery callout.: Phase 2's subagent audit prompt (walk-and-send-off-2) is a full-memory-audit-shaped fan-out under §48. Two gaps: (1) placement — §48 requires the recovery callout BEFORE the prompt; 'Heavy run expected...' sits in '## Read the ranked thin-spots' (line 48), the NEXT ## section AFTER the {{prompt:walk-and-send-off-2}} fence (line 44) — a later slide the student reaches only after already pasting the prompt. (2) content — covers over-reading recovery but never names the premature-stop failure the rule requires explicitly.
    - Fix: Move a short 'heavy run expected' clause into Phase 2 body ahead of the prompt fence, and add a premature-stop clause, e.g. 'if the ranked list looks thin, push Claude to keep digging.'
- **slides** REVISE (1/0):
  - [BLOCKING] §3 Every backward reference points at a beat that exists.: chunk 0 "H1/lede": line 15: "At the close, compound your rules file and send the task off, un-packaged, to the same Claude Code session." / chunk 9 "What closes Module 4": lines 120-121: "You nudge the compound step: Claude reads the session, rewrites your personal `CLAUDE.local.md` from evidence, integrates, sharpens, removes, and reports 3-5 lines. Team-worthy rules get flagged in the summary, not auto-PRed. [step 2] You push back on the 3-5 line summary." Both slides assert a compound pass happens at Module 4's close. Verified against the host module (curriculum/trainings/agentic-engineering-101/run-the-first-experiment.md § Send the task off, current line 53): "No compound pass at the close. The send-off is the close of the work. ... Compound on evidence, not anticipation." Git-blame-verified: that line read "No Debrief." (compatible with a compound step existing under a different label) until commit 05fbd36 (2026-07-08) rewrote it to the substantive "No compound pass at the close" denial. 47f3357 -- the SHA this exercise's own Quality line stamps as its last slides@ audit -- is an ancestor of 05fbd36, so the stale stamp predates the module's change. The file's own 2026-07-03 maintainer note reconciled this section's wording against the old "No Debrief" phrasing only; it was never re-run against the later "No compound pass" wording. Steps 1-2 (and the chunk-0 line) describe a beat the module now explicitly says doesn't happen.
    - Fix: Cut or rewrite chunk-0's "compound your rules file" close-line and chunk-9 steps 1-2 to match the module's actual close (rules get sharpened live during Phase 2 fills, not via a post-close rewrite pass); renumber the remaining steps (transcript/commit markers, trifecta check, send-off paste) down from 3-5.

### `curriculum/trainings/agentic-engineering-101/supplementary/verification-asymmetry.md`

- **technical** REVISE (2/1):
  - [BLOCKING] §1a OODA paraphrase ≠ primary source.: Body bolds "the verifier's law" as Wei's corollary; live re-fetch of jasonwei.net on 2026-07-12 shows the post's own H1 and in-body heading both say "Verifier's rule" — Wei never uses "law" except in a stale URL slug.
    - Fix: Correct the bolded handle to "Verifier's rule" throughout (body, maintainer block, theory-audit.md), or disclose the deliberate rename instead of claiming verbatim fidelity.
  - [BLOCKING] §11a Each listed source carries a freshness STAMP, not just a to-do.: Freshness stamp reads result:OK but the 2026-07-02 check missed the rule/law mismatch — honest value is CAVEAT.
    - Fix: Downgrade stamp to result:CAVEAT, or fix the naming first and keep OK.

### `curriculum/trainings/agentic-engineering-101/supplementary/clean-code-is-steering.md`

- **strategy** REVISE (1/0):
  - [BLOCKING] §4 Strategy-fidelity check — cross-module sequencing claims honored: Strategy doc L412-416 ('Pre-reads before M5 — 80/20 frame') and the OODA gap-index L696 both name exactly TWO pre-reads for the M4→M5 gap (Entis interview + Klaassen guide). This file is a third, live-wired pre-read for that same slot, referenced from two module files with two different counts (learn-from-the-test.md lists 3, run-the-first-experiment.md lists 4, adding verification-asymmetry.md too) — neither matches the strategy doc's canonical 2, and § Supplementary material (planned) doesn't acknowledge this file exists. Content itself doesn't front-run M5's Ronacher-three-pattern reveal (verified: zero hits on 'Ronacher'/'reference artefact'/'plan.md'/'verifier' in this file). Sibling audit ae101--run-the-first-experiment.strategy.json already flags this drift as blocking; sibling ae101--learn-from-the-test.strategy.json marks the same rule PASS with no evidence — an unreconciled cross-audit inconsistency surfaced here.
    - Fix: Maintainer: reconcile strategy doc's Pre-reads-before-M5 spec + OODA gap-index + Supplementary-material-planned list against the module files' actual pre-read counts in one edit (fold this file + verification-asymmetry.md in, or trim the module files back to 2). Re-run ae101--learn-from-the-test.strategy.json once resolved.
- **slides** REVISE (1/2):
  - [BLOCKING] §1 Referent resolution under a sequential deck read.: chunk 0 "Clean Code Is Steering: Insights from Uncle Bob": line 21: "As his Empire game grows, the AI's raw power begins to show a second face." — "Empire" is never named anywhere earlier in the file (lines 1-19 introduce Uncle Bob's two-Claude-window practice but no project/game called Empire); matches the 'Never named' violation shape verbatim.
    - Fix: Name what Empire is on first mention (his coding project/game) instead of assuming the reader already knows it.
  - [todo] §7 LLM-cadence caps.: chunk 0 "Clean Code Is Steering: Insights from Uncle Bob": line 33: "Not steering as vibes. Not steering as \"write a better prompt.\" Steering as executable constraint." — banned 'Not X. Not Y. Z.' construction. Other sub-items checked and cleared: line-21 triple escalates with distinct concrete content per leg (not repeated-refrain anaphora); no self-grading superlatives; no number+retraction pairs; 'the abstraction shift' (line 71) is immediately unpacked by the following sentences, not left as fog; no empty-leg verb triplets. Direct Uncle Bob blockquotes (lines 13, 29, 37-43, 59-61, 67-69, 75-79, 83) are verbatim citations, out of scope.
    - Fix: Recast the line-33 pair as one declarative opening sentence instead of two 'Not X' negations.
  - [todo] §9 Emphasis budget — bold is for handles, and go very lightly.: chunk 0 carries 8 bolded checklist-handle leads inside one un-chunked slide — lines 45, 47, 49, 51, 53, 55, 57, 65 (Acceptance Tests / Unit Tests / TDD / Mutation Testing / CRAP And Complexity Metrics / Small Decoupled Units / Architecture Visibility / Human Mental Model). The file has zero `##` headings anywhere (verified via grep), so site/layouts/slides.js's buildSingleDoc renders the entire ~90-line body as ONE cover-style slide, blowing the '≤1 bolded element on most slides' budget 8x over.
    - Fix: Add `##` breaks per checklist item so each bold handle gets its own slide instead of stacking eight on one.

### `curriculum/trainings/agentic-engineering-101/supplementary/token-efficiency.md`

- **story** REVISE (2/1):
  - [todo] §4 Slide/lecture headers pass the squint test AND the truth clause.: 7/8 headers pass. 'Where this leaves you' (line 81) fails the squint diagnostic — cover the body, the header names no concept, could head almost any closer. Actual payoff ('one lever with two payoffs... /context, starting clean, subagent as firewall') goes unnamed.
    - Fix: Retitle to name the section's own payoff, e.g. 'One lever, two payoffs.'
  - [BLOCKING] §5 A slide's BODY is size-bounded (~210w/≤6 bullets), not just its header.: Re-ran `node scripts/check-slide-size.js --file ... --report`: 'oversized: 3'. 'Drivers of inefficient context' 437w (2.1x ceiling), 'The craft: moves that hold up' 402w (1.9x), 'Sources' 12 bullets (2x cap). File is not on STORY_EXEMPT. Trace corroborates felt pacing drag on both prose sections.
    - Fix: Split 'The craft' at its own named 5-move seam; split 'Drivers' at its own named durable-vs-loud seam; exempt or split 'Sources'.
  - [BLOCKING] §4 Strategy-fidelity — consolidates front-run check, module-to-module arc, Big Idea fidelity, mood-doesn't-resolve-early.: FRONT-RUN (blocking facet): strategy doc's M3-in-detail names the subagent primitive's 'first visible use' as deliberately earned at M3, and the same doc protects an analogous reveal elsewhere (M2's grill-me pattern inlined specifically so it 'doesn't front-run M3's first-Skill-use moment'). This file's 'Use a subagent as a firewall' section fully teaches the subagent mechanism and its token-cost trade-off in prose, ahead of any earned in-module moment — and the file is already live in the AE101 supplementaries index today, independent of where its still-open module-entry-point eventually lands. BIG IDEA FIDELITY passes (opening matches stated Role). ARC/MOOD-RESOLVE facets N/A (file carries no mood-arc obligation).
    - Fix: Anchor any future module-entry-point at M3 or later, not M2; or trim the subagent section to a lighter forward-pointer if an earlier placement is required.
- **technical** REVISE (2/4):
  - [BLOCKING] §1a OODA paraphrase ≠ primary source.: Line 5: 'Anthropic's engineering team named this context rot.' WebFetch of both primaries (2026-07-12) shows Chroma's paper (titled 'Context Rot', 2025-07-14) is the term's origin; Anthropic's post (2025-09-29) hyperlinks 'context rot' to Chroma's research, i.e. it cites/uses the term, doesn't name it. Attribution is reversed.
    - Fix: Credit Chroma as the term's origin; frame Anthropic's post as the later mitigation/framing (context engineering).
  - [BLOCKING] §37 Context-loading tiers as staged progression.: Line 51: 'skills the agent loads on demand, so they cost nothing until they fire' contradicts §37: skill names+descriptions are eager-loaded (cost tokens every turn); only the body is lazy.
    - Fix: Reword to 'so only their full instructions cost tokens once they fire.'

### `curriculum/trainings/agentic-engineering-101/reference/multi-session-git.md`

- **technical** REVISE (4/4):
  - [BLOCKING] §1 Source URL + type label mandatory, honestly attributed.: L98 lumps two distinct sources under one ambiguous combined label; the Pragmatic Engineer interview (byline Gergely Orosz, confirmed live) isn't individually attributed per the compendium's practitioner-interview sub-clause, and is cited as if it corroborates a quote it doesn't contain.
    - Fix: Split into two labeled citations; apply the already-drafted 2026-05-25 sweep fix.
  - [BLOCKING] §1a OODA paraphrase ≠ primary source — re-fetch before transcribing.: Live re-fetch (2026-07-12) of the Pragmatic Engineer interview: zero hits for 'worktree'/'worktrees'/'productivity unlock' — it describes separate checkouts, not worktrees. Same drift already caught and fix drafted 2026-05-25 (curriculum/evals/scratch/source-sweep-2026-05-25/group-E.md L178-191), never applied. Unqualified half of claim has since shipped into AE101 M1 body prose (getting-going.md L59).
    - Fix: Apply the already-drafted 2026-05-25 fix.
  - [BLOCKING] §11 Source-verification maintainer block required.: No '**Source verification — MUST DO before first cohort:**' section exists at all, despite a load-bearing named-practitioner quote + two URLs (L98). Stakes raised by the M1 propagation (getting-going.md L59).
    - Fix: Add the required section per §11/§11a.
  - [BLOCKING] §11a Each listed source carries a freshness STAMP, not just a to-do.: Cascades from §11 — no stamps present for either URL despite both being verifiable today.
    - Fix: Stamp both sources per §1's fix.

### `curriculum/trainings/agentic-engineering-101/reference/mcp-and-connectors.md`

- **technical** REVISE (1/6):
  - [BLOCKING] §4 Verify capabilities BEFORE asserting; audit-mode not exempt.: Line 46: '...covering Jira, Confluence, Compass, and Bitbucket (Jira Service Management rides on Jira).' Live-checked 2026-07-12 against support.atlassian.com/atlassian-rovo-mcp-server/docs/supported-tools/: JSM has its own read_jsm/write_jsm scopes and API-token-only auth, separate from Jira -- it does not 'ride on Jira.' Separately: the 'reserved names include anthropic-marketplace, agent-skills' claim at line 104, flagged blocking by the prior 2026-07-11 technical instance of this file, is CORRECTED to verified-accurate this run (see platform_claims_checked).
    - Fix: Cut or rewrite the JSM parenthetical: 'JSM ships its own read/write scopes, API-token auth only' or drop it.
  - [todo] §22 Coverage tools enumerate universe, not index of past work.: source-freshness.sh prints 'No stamps found' and exits 0 for this file -- clean-success shape, not a flagged gap; can't distinguish 'never audited' from 'nothing to report' at whole-tree scope, which is why the staleness in rule 4 went uncaught by two consecutive audit runs.
    - Fix: Emit a WARN row per scanned file with zero stamp matches.
  - [todo] §1 Source URL + type label mandatory (maintainer-block scope).: All 8 sources carry URL + label, correctly kept out of body prose -- but `[platform docs]` is not in §1's canonical taxonomy nor matches source-freshness-format.md's `[capability]` label for this exact claim-type; a one-off coinage.
    - Fix: Re-label `[capability]` when migrating to stamp grammar (see §11a).
  - [todo] §2 Freshness rule -- 6 months.: 'Last verified: 2026-05-14' is 59 days old (inside the nominal 6-month window) but live-checking already found drift ahead of that window -- capability claims on this fast-moving surface should run due:cohort, not the blanket 6-month clock.
    - Fix: Adopt due:cohort stamps per §11a.
  - [todo] §11a Each listed source carries a freshness STAMP grammar; unified scope covers capability claims.: Source-verification block uses prose format, not `[checked:...result:...due:...]` stamp grammar. Confirmed via source-freshness.sh --target 2026-07-12 -> 'No source-freshness stamps found' (exit 0). Systemic across all curriculum/trainings/agentic-engineering-101/reference/ files; the 2026-05-25 stamp sweep (commit f7e83e2) touched 4 lines of this file's Source-verification block (dead-link fix) without adding stamps.
    - Fix: Migrate to `[checked:2026-07-12 result:...due:cohort]` grammar per source-freshness-format.md.

## Clean files (17)

- `curriculum/trainings/agentic-engineering-101/prework.md` — GO (11 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/trainings/agentic-engineering-101/plan-mode-done-right.md` — GO (10 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/composing-the-workflow.md` — GO (0 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/painting-the-picture-with-the-llm.md` — GO (2 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/quality-is-grounding.md` — GO (3 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/reading-the-return.md` — GO (3 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/the-2-frontiers.md` — GO (2 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/the-agent-loop.md` — GO (9 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/the-far-half.md` — GO (1 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/the-loop-has-a-name.md` — GO (1 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/the-map-filled-in.md` — GO (7 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/where-the-rule-could-live.md` — GO (6 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/lectures/will-company-memory-emerge.md` — GO (2 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/exercises/orient-and-introspect.md` — GO (9 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/exercises/threat-model-with-stride.md` — GO (9 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/trainings/agentic-engineering-101/supplementary/backpressure.md` — GO (3 non-blocking TODOs logged in Quality-linked instance files)
- `curriculum/trainings/agentic-engineering-101/supplementary/the-lethal-trifecta.md` — GO (2 non-blocking TODOs logged in Quality-linked instance files)

## Known limitations of this sweep

- **Instance-path slug collision.** `curriculum/trainings/agentic-engineering-101/spot-gaps-build-the-loop.md` (module) and `curriculum/exercises/spot-gaps-build-the-loop.md` (exercise) share a basename; both wrote to the same `curriculum/evals/instances/ae101--spot-gaps-build-the-loop.<class>.json` path concurrently, so one overwrote the other for all seven classes. Findings for both files are still correct here (captured from the workflow's in-process results, not the collided disk files); the disk instance files were reconstructed post-hoc as `ae101--spot-gaps-build-the-loop--module.<class>.json` / `--exercise.<class>.json` with REVISE findings intact but PASS/N/A ledgers not recoverable for whichever file lost the race. Worth a durable fix in the eval-fire / curriculum-pre-ship-audit instance-path convention (disambiguate by directory, not basename, when a module and exercise share a slug).
- **cross_module structured-output drift.** The cross_module judge's tool-forced structured return omitted per-pair `rules_evaluated` detail that its own `Write` call to `curriculum/evals/instances/ae101--prework-m6.cross_module.json` preserved in full. This report's cross-module findings were pulled from that disk file, not the workflow's in-memory return. Worth tightening the judge template or schema so the two outputs can't diverge.
