The risk: the named SMB pilot-account contacts (and their per-account usage figures) in sources/wiki/pilot-cohort-notes.md are sitting in the agent's reachable context and have bled into memory/ as named references. If that memory or a transcript is ever shared, those customer names leak — a clear R-DU-1 violation. Mitigate it with the FILTER shape: replace the named contacts with role + account-number references wherever the agent persists or summarises them (memory pages and any synthesis note), and add a hard line in the agent file forbidding the agent from reproducing the names in any output that could be shared. Keep the raw source file as-is — I need it — but the agent's persisted layer must be name-free. Then re-run the policy lens for R-DU-1 only and tell me the new verdict, and be honest about the residual: the names still exist in the raw source and may already sit in this session's scrollback, so filtering reduces the exposure, it does not eliminate it.
