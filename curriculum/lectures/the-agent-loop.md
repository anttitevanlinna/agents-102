# The agent loop

## The agent, the harness, the loop

- An **agent** is an LLM calling tools in a loop. Four parts: the context it reads, the tools it can call, the goal it works toward, and the autonomy to pick its own next step. Each iteration the LLM receives context (system prompt, conversation, tool results) and either responds to you or uses tools.
- The **agent harness** is the surrounding code that runs the loop. It exposes tool definitions to the LLM, executes the tool calls, and feeds results back in. Claude Code is one harness; Cursor, Cline, Gemini CLI are others.
- The **agent loop** is that iteration itself. Prompt → reason → tool calls → results → reason again, until the agent decides to stop.
- A long-running session is this same loop, iterated. Out of the box, nothing new takes over when the session gets long; the loop keeps choosing the next step from context and tool results, hour after hour.

## See the loop in your live session

Three prompts. Dig into whatever catches you.

Ask for the loop as a diagram.

{{prompt:ae101-agent-loop-ascii}}

Ask what tools this session is carrying.

{{prompt:ae101-agent-loop-tools-list}}

Ask where that list comes from.

{{prompt:ae101-agent-loop-tool-injection}}

<!-- maintainer -->

**Emphasis pass (2026-07-09, Antti-directed "go very lightly on the bold"):** definitional slide keeps bullets; bold reduced to the three defined terms as sub-spans — **agent** · **agent harness** · **agent loop** — and the fourth bullet de-bolded. Slide 2 (live-session prompts) carried no bold and is untouched; blockquote and `{{prompt:}}` refs byte-intact. Per `theory-plan.md § Slide format — emphasis budget` + `check_slides.md §9`. Wording near-verbatim; no claims added or cut.

**Promotion (2026-07-02, Antti-directed):** M1 supplementary → M4 lecture, wired into `run-the-first-experiment.md § Start here`. Slide-format pass per `theory-plan.md` § Slide format + dosage: two slides (anatomy + live introspection). Body changes beyond re-chunking: the defining paragraph became slide-1 bullets (wording preserved); the fourth anatomy bullet (*a long-running session is this same loop, iterated*) is the ONLY new content — the M4-fit line. The three `{{prompt:ae101-agent-loop-*}}` refs are byte-intact. No packaging laws (0.85ⁿ / triad / principal–agent stay out until M5), no "one loop, three sizes" naming (M3's beat), no cross-module sequencing in body (`check_lectures §3`).

**Placement rationale:** inlined between the far-half opener and Test and learn. The far half names the country (feedback goes quiet, the result arrives all at once); this names the machine that keeps stepping through it — and the three prompts run on the live session that will carry the send-off, so the tool list that comes back is the one the long run rides. Vocabulary lecture at module open per `check_lectures §1`; it arms (mechanism + one introspection move), it doesn't teach the far-half laws.

**Unwiring (promotion = relocation):** both M1 callout links removed — `getting-going.md` "if time allows" blockquote (deleted) and `compound-and-close.md` timebox note (pointer sentence trimmed; the note itself stays). Reversibility: git carries the old wiring — restoring = the two callout lines + the `the-agent-loop` row in `TRAININGS['agentic-engineering-101'].supplementaries` in `site/layouts/curriculum.js`.

**Eyeball questions (Antti):**
1. Placement beat: after the far-half container, before Test and learn — or later, right before the send-off prompts?
2. The fourth anatomy bullet is the only new sentence — close enough to M3's loop-sizes beat to cut?
3. The three prompts' registry `origin:` frontmatter still reads `agentic-engineering-101/supplementary/the-agent-loop` (documentation-only field per `compile-prompts.js`; left alone to avoid a registry edit). Update on the next prompt-registry pass?

**Definitional contract — this slide is downstream of Agents 101, not parallel to it.** The definition bullet lists **four parts** (context · tools · goal · autonomy). Agents 101's `supplementary/what-is-an-agent.md` lists **five facets** (multi-step work · context · tools · goal · bounded autonomy). The difference is one fold, not a disagreement: multi-step work is carried here by *"in a loop"*, which is the term this lecture exists to install. Two altitudes, one definition. Three standing rules for anyone editing this bullet:
- **Don't re-cut the list without cutting the sibling.** Any properties named here must map onto that file's facets or the house is teaching two theories.
- **Context and goal stay.** A draft of this bullet once enumerated *repeats / picks its own next step / has tools*, which dropped both. Dropping context in the training whose near half is Intent → Context → Work is the hole worth naming.
- **"Autonomy" here is deliberately narrower than the sibling's "bounded autonomy."** This slide cashes out only the choosing, because that is what the long-run bullet needs. **Do not restore a claim that the bounding is taught elsewhere in AE101.** An earlier version of this note credited M2 (plan mode as a permission state) and M3 (cutting one leg of the trifecta); the M3 half was never true of the body and the trifecta beats were removed 2026-08-12, Antti-directed. The narrowing stands on its own: this slide teaches choosing, and the bounding is not this arc's material.

**Plural round shape (Antti-directed, 2026-08-01).** A single round carries as many tool calls as the step needs, and independent calls go out together. Bullet 1 reads *"uses tools"*; bullet 3 reads *"tool calls → results"*. Bullet 2 was always plural (*"executes the tool calls… feeds results back in"*) and is untouched. Do not re-singularise any of the three: a student watching an hour-long run sees batched calls on screen, and a serial machine on the slide would be contradicted inside the same module. Stamp: `parallel-tool-calls`, maintainer-attested.

**Quality:** compendium-audited 2026-08-03 (writing@5fc7188 story@eaae8cd technical@1c765f2 behavior@1c765f2 pedagogy@eaae8cd strategy@1c765f2 slides@5fc7188)
- judges @5fc7188: writing PASS, story PASS, technical PASS, behavior PASS, pedagogy PASS, strategy PASS, slides PASS

**Meta:** *M4 teaching lecture, ~5 minutes projected. Audience: the engineer about to send a multi-hour task off in this same session. The three prompts demonstrate the loop on the student's live session; that's the teaching move — the anatomy slide is scaffolding for the prompts, not the lesson.*

**Time:** ~5 minutes.

**Voice:** Boris-flat technical (mechanism description, not framing).

**Watch-fors (delivery):**
- The tool list the prompts return is the one the send-off will carry, MCP-connector tools included. A student surprised by their own list IS the lesson; don't rush past it.
- **The returned list is a summary, not a manifest**, per Anthropic's own docs. MCP tool search is on by default: only tool names and server instructions load at session start, and full definitions arrive on demand. The docs then say the chat reply is *"a conversational summary"* and route exact MCP names through `/mcp`. So expect lists that disagree slightly between students, and expect names without descriptions. If someone asks, `/mcp` is the authoritative answer and the two-tier loading is the real reason — don't improvise a different one.
- Keep it under 5 minutes; past that it starts teaching harness trivia the send-off doesn't need.

<!-- backing -->

Format → `curriculum/backing-format.md`. Highest-decay file in the M4 block: every claim above is a mechanism or a product fact, so this block's stamps go stale on platform time, not evidence time. Most sources here are `due:cohort` by design.

**Claims**
- `agent-is-llm-in-a-loop` · borrowed · "An **agent** is an LLM calling tools in a loop. Four parts: the context it reads, the tools it can call, the goal it works toward, and the autonomy to pick its own next step." ← willison-agentic-patterns, willison-agents-2025, anthropic-effective-agents, a101-what-is-an-agent
- `loop-iteration-anatomy` · detail · "Each iteration the LLM receives context (system prompt, conversation, tool results) and either responds to you or uses tools." ← cc-glossary-harness, anthropic-effective-agents, parallel-tool-calls
- `harness-is-the-runner` · detail · "The **agent harness** is the surrounding code that runs the loop. It exposes tool definitions to the LLM, executes the tool calls, and feeds results back in." ← cc-glossary-harness, anthropic-harness-post, chua-codex-harness, gerganov-harness
- `named-harnesses` · detail · "Claude Code is one harness; Cursor, Cline, Gemini CLI are others." ← cc-overview, cursor-agent-docs, cline-repo, gemini-cli-repo
- `loop-runs-until-the-agent-stops` · detail · "Prompt → reason → tool calls → results → reason again, until the agent decides to stop." ← cc-glossary-harness, cc-hooks, parallel-tool-calls
- `long-run-is-the-same-loop` · detail · "A long-running session is this same loop, iterated. Out of the box, nothing new takes over when the session gets long; the loop keeps choosing the next step from context and tool results, hour after hour." ← cc-costs-compaction, cc-subagents, cc-goal, cc-scheduled-tasks, cc-hooks, ronacher-coming-loop — "Out of the box" is the load-bearing scope: the CAVEAT sources (scheduled tasks, hooks, `/goal`) name opt-in machinery that does take over, all off by default.
- `tool-list-is-introspectable` · detail · "Get the tool list this session is actually carrying." ← cc-tools-reference, cc-mcp-tool-search — the session reports its own tool set on request; the fenced prompt does the asking.
- `mcp-tools-join-the-same-set` · detail · "Surface where the tool list comes from, and what it leaves out." ← cc-permissions-mcp, cc-mcp-tool-search — connector tools join the same set, not a second mechanism.

**Sources**
- parallel-tool-calls `[checked:2026-08-01 result:ATTESTED due:none]` attested:Antti 2026-08-01 daily-practice — [practitioner direct] a single round can carry more than one tool call, and independent calls go out together. Caught on the card for this slide: the body taught *"calls a tool"* and *"tool call → result"*, a strictly serial machine the student would have seen contradicted on screen inside the same module. Bullet 2 was already plural (*"executes the tool calls… feeds results back in"*), so the slide disagreed with itself. `[maintainer-attested]` per `check_research_claims.md §1` — the maintainer runs these hourly and the observation needs no third-party URL. fallback: none; if a stamp is ever wanted, Anthropic's tool-use docs cover parallel calls, but do not swap an eyewitness for a page nobody has opened.
- a101-what-is-an-agent `[checked:2026-08-01 result:OK due:none]` `curriculum/trainings/agents-101/supplementary/what-is-an-agent.md` — [house canonical] the sibling training's reference doc, and the reason this slide says *four parts* rather than inventing its own list: *"An agent is an LLM doing multi-step work with context, tools, a goal, and bounded autonomy."* Five facets there; four here, because AE101 folds **multi-step work** into *"in a loop"*, which is the term this lecture exists to install. Same definition at two altitudes, not two definitions. **Do not re-cut this list without cutting the sibling** — an earlier draft of this slide enumerated *repeats / picks its own next step / has tools*, which quietly dropped context and goal, and dropping context in the training built on context is the hole worth naming. `due:none` because a house doc does not expire; it changes, and if it changes here is where to look.
- willison-agentic-patterns `[checked:2026-08-01 result:OK due:2027-02-01]` https://simonwillison.net/guides/agentic-engineering-patterns/what-is-agentic-engineering/ — [practitioner direct] Willison, 15 Mar 2026. The in-window restatement, and the reason this borrow is not a 2025 relic: *"Agents run tools in a loop to achieve a goal"*. fallback: none needed; if the guide moves, the 2025 post below carries the same sentence.
- willison-agents-2025 `[checked:2026-08-01 result:CAVEAT due:none]` https://simonwillison.net/2025/Sep/18/agents/ — [practitioner direct] Willison, 18 Sep 2025. The canonical minting: *"An LLM agent runs tools in a loop to achieve a goal."* CAVEAT is the freshness window, and it does not bite — a `borrowed` claim owes attribution, not currency, and the restatement above supplies currency anyway. **Note the grammar, because our body inverts it:** in Willison's construction *tools* are the object of the loop-running verb. Ours makes the LLM the thing that loops. See Flagged.
- anthropic-effective-agents `[checked:2026-08-01 result:OK due:none]` https://www.anthropic.com/engineering/building-effective-agents — [vendor press release] Anthropic, 19 Dec 2024, **bylined Erik S. and Barry Zhang** (the hunt reported this as unbylined; the refutation opened the page twice and found the Acknowledgements byline). L0 — cited as the origin of a phrasing, never as evidence: *"They are typically just LLMs using tools based on environmental feedback in a loop."* Foundational-variant `due:none` because a published sentence does not expire.
- cc-glossary-harness `[checked:2026-08-01 result:OK due:cohort]` https://code.claude.com/docs/en/glossary — [capability] Anthropic's own canonical definition, and it settles the vocabulary question outright: *"Agentic harness: The tools, context management, and execution environment that turn a language model into a capable coding agent. Claude Code is the harness; Claude is the model inside it."* Note the term is *agentic* harness; the bare phrase "agent harness" our body uses is found verbatim nowhere.
- anthropic-harness-post `[checked:2026-08-01 result:OK due:cohort]` https://claude.com/blog/a-harness-for-every-task-dynamic-workflows-in-claude-code — [vendor press release] Thariq Shihipar and Sid Bidasaria, 2 Jun 2026. L0, but it is the word in live use inside the freshness window, not a glossary entry nobody says out loud.
- chua-codex-harness `[checked:2026-08-01 result:CAVEAT due:2027-02-01]` https://www.linkedin.com/pulse/how-i-think-codex-gabriel-chua-ukhic — [practitioner direct] Gabriel Chua, 21 Feb 2026. Cross-vendor use of the same word for the same component: *"Codex = Model + Harness + Surfaces … Without the harness, you have suggestions. With the harness, you have execution."* CAVEAT: he flags the post himself as *"personal (and very unofficial)"*, so it is one engineer's framing, not OpenAI's.
- gerganov-harness `[checked:2026-08-01 result:OK due:2027-02-01]` https://news.ycombinator.com/item?id=48555993#48557304 — [practitioner direct] Georgi Gerganov (llama.cpp), 16 Jun 2026: *"Currently, I have a very lightweight harness…"* A third unrelated project using the noun bare and unglossed, which is the mark of settled vocabulary rather than a coinage. A second instance (30 Mar 2026) reaches us only through Willison's link-blog quoting a Gerganov tweet — looser than practitioner-direct, recorded and not leaned on.
- cc-overview `[checked:2026-08-01 result:OK due:cohort]` https://code.claude.com/docs/en/overview — [capability] Claude Code live, matches the harness definition.
- cursor-agent-docs `[checked:2026-08-01 result:OK due:cohort]` https://cursor.com/docs/agent/overview — [capability] Cursor runs an unbounded tool-calling loop — *"There is no limit on the number of tool calls Agent can make during a task"* — so it fits the definition even though Cursor's own docs never use the word "harness". A changelog entry the hunt dated 2026-07-29 is actually from 2026-07-10; Cursor's currency holds on other entries, so the correction changes nothing but is recorded rather than quietly dropped.
- cline-repo `[checked:2026-08-01 result:OK due:cohort]` https://api.github.com/repos/cline/cline — [capability] not archived, `pushed_at` 2026-08-01, three tagged releases on 2026-07-31. Alive, and now shipping a CLI surface as well as the IDE extension.
- gemini-cli-repo `[checked:2026-08-01 result:OK due:cohort]` https://api.github.com/repos/google-gemini/gemini-cli — [capability] not archived, not renamed, `pushed_at` 2026-08-01 with a nightly cut the same day. The churn `memory/reference_gemini_cli_capabilities.md` warns about is real and faster than "sub-monthly", but it is version churn, not identity churn — the name in the body is safe.
- cc-costs-compaction `[checked:2026-08-01 result:OK due:cohort]` https://code.claude.com/docs/en/costs — [capability] the near-miss on the long-run claim, and it misses: *"Claude Code summarizes older history to free space"* and *"/compact reads the conversation it summarizes"*. The trigger is a harness-owned token threshold; the summary itself is a model-authored request. Compaction changes **what** the model reads, not **who** chooses.
- cc-subagents `[checked:2026-08-01 result:OK due:cohort]` https://code.claude.com/docs/en/sub-agents — [capability] *"When Claude encounters a task that matches a subagent's description, it delegates"* — delegation is itself a model tool-choice, and each subagent runs its own model-driven loop. Second pre-registered falsifier, also missed.
- cc-goal `[checked:2026-08-01 result:CAVEAT due:cohort]` https://code.claude.com/docs/en/goal — [capability] the first mechanism that genuinely complicates the claim: *"/goal adds a separate evaluator that checks your condition after every turn, so completion is decided by a fresh model rather than the one doing the work."* Still a model deciding, so it survives the strict test the body makes; it does not survive the colloquial reading of "nothing new takes over". Opt-in, off by default.
- cc-scheduled-tasks `[checked:2026-08-01 result:CAVEAT due:cohort]` https://code.claude.com/docs/en/scheduled-tasks — [capability] the sharpest one. `/loop` at a fixed interval is a literal scheduler outside any model, and it carries a harness-authored default: *"If an iteration ends without either rescheduling or stopping, Claude Code schedules one fallback wakeup about 20 minutes later."* When the model chooses nothing, the harness chooses for it. That is a non-model component picking the next action — narrow, opt-in, and real.
- cc-hooks `[checked:2026-08-01 result:CAVEAT due:cohort]` https://code.claude.com/docs/en/hooks — [capability] a `Stop` hook *"Prevents Claude from stopping, continues the conversation"*, and `PreToolUse` can deny a tool call the model chose. An arbitrary script, not a model, overriding both ends of the loop. Opt-in.
- cc-tools-reference `[checked:2026-08-01 result:CAVEAT due:cohort]` https://code.claude.com/docs/en/tools-reference — [capability] confirms the prompt works and supplies a caveat the lecture does not carry: *"Claude gives a conversational summary. For exact MCP tool names, run `/mcp`."* Anthropic's own docs route the authoritative list away from the chat answer.
- cc-permissions-mcp `[checked:2026-08-01 result:OK due:cohort]` https://code.claude.com/docs/en/permissions — [capability] the naming convention students will see: `mcp__<server>__<tool>`, and `mcp__claude_ai_<server>__<tool>` for claude.ai connectors. Built-in and MCP tools are matched and removed from context by one identical rule mechanism, which is the strongest support for "same set, not a second mechanism".
- cc-mcp-tool-search `[checked:2026-08-01 result:CAVEAT due:cohort]` https://code.claude.com/docs/en/mcp — [capability] **the finding that touches delivery.** Tool search is *"enabled by default"*; *"Only tool names and server instructions load at session start"* and full definitions arrive on demand. So a session's tool set is two-tier — named versus loaded — and a naive listing is definition-incomplete by design. Corroborated at the API layer: https://platform.claude.com/docs/en/agents-and-tools/tool-use/tool-search-tool.
- ronacher-coming-loop `[checked:2026-08-01 result:OK due:2027-01-31]` https://lucumr.pocoo.org/2026/6/23/the-coming-loop/ — [practitioner direct] Ronacher, 23 Jun 2026. Names the layer above this lecture's subject and keeps it distinct: *"some harness decides whether that was actually the end. If not, the harness continues the same session, injects another message, starts a fresh session with modified context, or sends the task to another machine."* He scopes that as an **outer** loop, separate from *"the agent loop inside every coding agent"* — which is precisely the loop this file teaches. Same stamp and same reading as `the-far-half.md`, `the-whole-map.md`, `the-loop-half-filled.md` and `where-the-rule-could-live.md`; keep all five in step.

**Frameworks**
- The agent loop as closed-loop control · [borrow:control theory] · law:is-a-closed-loop-controller · ← cultural-vocab. `theory-plan.md` tags this file as the enacting surface (`[curr:the-agent-loop]`), and the body walks act → observe → correct without ever naming it. The naming happens at M6; nothing here should reach for it.
- Agent = model + tools + loop · [borrow:practitioner-coined] · law:none · ← willison-agentic-patterns, anthropic-effective-agents. Willison's coinage on Anthropic's phrasing. **Not a field consensus** — Thorsten Ball defines an agent with no loop language at all, and Solomon Hykes' circulating variant drops tools as the object. Six named practitioners, two of them diverging, is L1–L2; do not let a future cycle write "the field agrees".
- Harness / model split · [borrow:practitioner-coined] · law:none · ← cc-glossary-harness, chua-codex-harness, gerganov-harness
- The outer loop above the agent loop · [borrow:practitioner-coined] · law:none · ← ronacher-coming-loop. Named here only to hold the boundary: this lecture teaches the inner loop, and Ronacher's outer loop is M5/M6 territory.

**Stance** `[stance:2026-08-01 level:L2]`
- holds: the harness/model split is real, current and cross-vendor — Anthropic's glossary defines *agentic harness* in almost this lecture's terms, and two practitioners outside Anthropic (OpenAI-adjacent, llama.cpp) use the bare noun unglossed. The four-name list is true: all four confirmed alive on 2026-08-01, with Cline and Gemini CLI both pushing the same day. The definition itself is a live coinage rather than a consensus, which is why the body names nobody and should keep naming nobody. **`agent harness` is our own compound** — Anthropic writes *agentic harness*, the practitioners write bare *harness*, and the exact two-word form the body bolds appears verbatim in no source reached. Harmless as house vocabulary, and the gloss right after it does the teaching; recorded so a later cycle does not "restore" an attribution that never existed.
- contested: **the long-run claim, on scope rather than on fact.** For the vanilla session a student runs straight after this lecture, nothing new takes over and the sentence is exactly right — compaction is model-authored, subagent delegation is a model tool-choice, and both pre-registered falsifiers missed. But the same product ships four opt-in mechanisms that hand some part of the continuation decision to something other than the working model: `Stop` hooks (a script vetoing the model's own stop), `/goal` (a separate evaluator model), agent-teams dependency bookkeeping, and `/loop`'s fixed-interval scheduler — whose 20-minute fallback acts when the model chooses nothing at all. That last one is a genuine non-model component picking the next action.
- decided: **resolved by the 2026-08-02 wording card, and the card's own proposed fix was rejected.** The bullet read *"An agent is an LLM in a loop"*, which listed tools as one of four equal parts while Willison (*"runs tools in a loop"*) and Anthropic (*"models using tools in a loop"*) both put tools in the object position. Body now reads *"An agent is an LLM calling tools in a loop"*. The Flagged entry had proposed *"an LLM that runs tools in a loop"* — **do not restore that form**: it makes the LLM run the loop and contradicts the very next bullet, where the harness is the thing that runs it. `calling` is the precise verb, because the LLM emits tool calls and the harness executes them, which is what bullet 2 says. One seam is known and accepted: `in a loop` now scopes onto `calling tools`, in mild tension with the same bullet's *"either responds to you or uses tools"* since not every iteration calls a tool. It resolves inside the slide at bullet 3 and was logged TODO, not REVISE, by the 2026-08-02 story judge.
- would-move-it: any of those four becoming **default** rather than opt-in. That flips the sentence from "true of the run you are about to watch" to "false of the product", and it is a one-line docs change away. Re-check `cc-scheduled-tasks` and `cc-hooks` every cohort, not every six months.

**OODA**
- question: is the four-name harness list still true on the day a cohort reads it? (The scope tension on *"nothing new takes over"* is resolved in body — "Out of the box" scopes the sentence to the default session, which is what the sources warrant; the opt-in machinery is the CAVEAT stamps' record.) The 2026-08-01 run: list holds. **Closed 2026-08-02, do not re-open:** MCP tool search is on by default, so the list a student gets back is two-tier — names at session start, full definitions on demand — and Anthropic's docs call the chat answer *"a conversational summary"*. The three prompts already teach exactly that: the second one's answer is deliberately incomplete and the third asks *"whether any of them are deferred until you need them, and what that means for the list you gave me earlier."* The incompleteness is the setup, not a defect.
- roster: Simon Willison, Anthropic engineering (Boris Cherny, Cat Wu, Thariq Shihipar, Sid Bidasaria), Armin Ronacher, Thorsten Ball, Georgi Gerganov, Dex Horthy; plus `platform-watch/coding-agents/state.md` and `memory/reference_gemini_cli_capabilities.md`.
- last-run: 2026-08-01

<!-- /backing -->
