# Agents 101 prework — extract starter → snake → meetings.
# Driven by run-a101.sh --module prework. Session opens in the synthetic
# training dir (arrange-agents-101.sh seeds the starter tarball there).
#
# Connector path is OUT (no live calendar in headless). The screenshot turn
# is fed the calendar as a TEXT export at <MEETINGS_FILE> — same teaching
# contract (read real week data, save a summary), deterministic input.

a101-prework-extract-tarball

a101-prework-snake-game

a101-prework-meetings-screenshot-cli I'm doing a headless dry-run, so there is no screenshot and no calendar connector. My calendar week is exported as a text file at <MEETINGS_FILE> (absolute path). Read that file instead of looking for a screenshot, then do exactly as asked: list my main meetings and summarize the shape of the week in 5 lines, saved to prework/meetings.md.
