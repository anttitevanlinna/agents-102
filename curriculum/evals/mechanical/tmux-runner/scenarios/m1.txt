# M1 (AE101) — Getting going + context (+ MCP).
# Driven by run-m1.sh. Single session in lemmings repo, starting from
# bdd0919 (Initial commit) on branch m1/<task-slug>.
#
# Bug picked from CLAUDE.local.md M1 rules: HUD reads state.spawned
# (cumulative) for 'Out' instead of state.active (currently in-play),
# so after the round resolves the HUD still reads 12 'Out' with zero
# lemmings on the playfield. Real, small, TDD-able, single-file root.
#
# The close-the-ticket exercise (close-the-ticket-1/2/3 — read the bug's
# ticket, reverse-engineer the team's field conventions, write and SEND
# the close-out) is scoped OUT of the headless run: no tracker connector,
# and -3 writes back to a real tracker, which this battery has no business
# doing. Those three keys were compound-and-close-2/3 until 2026-08-12,
# when M1 split them into their own exercise; the scope-out is unchanged,
# only the names are. The fourth compound (second-pass integrate) closes
# the session and is treated as the terminal turn.
#
# Consequence, handled in the compound-and-close-1 tail below:
# compound-and-close-1 now REQUIRES bug-rules from close-the-ticket-2 and
# its body reviews "the bug rules read off the ticket". With the ticket
# exercise scoped out there are none, so the tail tells Claude to skip
# that clause rather than invent a ticket read that never happened.
#
# AskUserQuestion guards: orient-1/2 are non-interactive; orient-3 is
# the /context slash command (no LLM call, no Stop hook — runner has a
# special branch).
#
# Save-gate pair: BOTH fix-tests-first-1 AND fix-tests-first-3 end with
# "Show me the diff before you commit." Claude sometimes interprets each
# as a commit decision point (curriculum design allows committing at
# either pass) and sometimes folds both into one end-of-flow commit. The
# form of the ask also varies between runs: TEXT ("one commit or two?
# PR via gh or hold?") in some, the AskUserQuestion CHOICE UI ("Commit
# scope" with options) in others — the UI form hangs the runner because
# no Stop hook fires while the UI is waiting for an arrow-key+Enter.
#
# Headless workaround: lazy-student tail on both fix-tests-first-1 and
# fix-tests-first-3 says (a) don't call AskUserQuestion, (b) don't
# commit yet on the first-pass, (c) do commit at end of the second-pass.
# This collapses the commit shape to ONE commit at the end and keeps
# the runner advancing. Real cohort students may legitimately commit
# twice; the mechanical battery doesn't need to.

orient-and-introspect-1

orient-and-introspect-2

orient-and-introspect-3

fix-tests-first-1 The HUD 'Out' counter reads the cumulative spawned count, not the currently-active count. After the round resolves with twelve lemmings saved+lost the HUD still reads 'Out: 12' even though zero lemmings are still on the playfield. Likely lives in how src/main.js's step() updates state and how src/ui.js reads it for the HUD. I'm doing a headless dry-run, please don't call AskUserQuestion. Show me the diff in chat but DO NOT commit yet — the next prompt may sharpen the fix, and we'll commit once at the end. Continue without waiting for approval.

fix-tests-first-2

fix-tests-first-3 I'm doing a headless dry-run, please don't call AskUserQuestion. Show me the diff in chat. After showing it, go ahead and commit as one commit on the current branch, no PR. Don't wait for approval.

compound-and-close-1 I'm doing a headless dry-run with no tracker connector, so we never ran the ticket step — skip the "bug rules read off the ticket" clause and review only what actually happened in this session. Don't call AskUserQuestion; write the file and report.

compound-and-close-4
