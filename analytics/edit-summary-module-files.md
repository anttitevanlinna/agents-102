# Edit summary — AE101 module files (homework / pre-read sections)

Four module files edited; each owns its own section. Strategy file already specifies these additions verbatim (added 2026-04-24 markers in § "M{n} in detail"); module files caught up. Link format: target title, not filename. Framing: permission-giving, skipping does not break the next module. No em-dashes in new body text; no banned words.

## File 1 — `getting-going.md` (M1)

Added `## Homework after M1 — between-module reading` section after Bridge, before the `<!-- maintainer -->` cut. Lands at lines 53–59. Two pieces: (a) Cherny video (URL marked pending verification per spec; ~30 min; practitioner voice framing for M2–M6); (b) Multi-session and Git survival guide (local reference page link; ~10 min; Git footing for multiple sessions). Closes with the permission line *"You will want to run multiple sessions. Find your way to do it over time. There is no rush."*

## File 2 — `earn-the-trust.md` (M3)

Added `## Pre-reads before M3` section after Bridge, before maintainer cut. Lands at lines 84–90. Two pieces: (a) Simon Willison, *The lethal trifecta for AI agents* (~10–15 min; modern threat class framing before STRIDE + access-control); (b) optional OWASP Top 10 for LLM Applications (~20 min; wider surface beyond the trifecta). Both optional.

## File 3 — `learn-from-the-test.md` (M5)

Added `## Pre-reads before M5` section after Bridge, before maintainer cut. Lands at lines 91–97. Two Klaassen pieces: (a) *You're the Bread in the AI Sandwich* (~5 min; identity metaphor primes the 80/20 reframe); (b) longer *Compound Engineering: The Definitive Guide* (~15–20 min; 80/20 ratio stated verbatim so it arrives as recognition at the M5 closer).

## File 4 — `spot-gaps-build-the-loop.md` (M6)

Three additions: (a) identity beat appended as italic one-line close inside `## Debrief` at line 52 — *"You know how to test… you are the bread, not the filling."*; (b) `## When to reach for parallel worktrees` callout paragraph (~95 words) after Bridge at lines 64–66, anchored on Cherny's "single biggest productivity unlock," links the survival guide, closes with *"Find your way to do it over time"*; (c) `## Pre-reads before M6` section at lines 68–72, single Klaassen piece (*My AI Had Already Fixed the Code Before I Saw It*, ~10 min) carrying both reliability-as-number and fan-out review shape.
