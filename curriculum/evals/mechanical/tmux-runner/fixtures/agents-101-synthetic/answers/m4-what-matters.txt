Five lines, my own voice — what this reusable check must catch in MY system, not generic GDPR:

1. The pilot-cohort notes name real SMB account contacts and their per-account usage. Those names must never sit inline in memory/ or get echoed in a transcript I might share — reference them by role and account number, never by name.
2. My agents read outside web sources I don't control. I want the check to assume any retrieved source could carry an instruction aimed at the agent, and to treat that as hostile until proven otherwise.
3. The pricing memo my M2 agent drafts is internal-only until I've read it. Nothing an agent writes should be able to leave the building — emailed, sent to the CEO, pushed to a pilot account — without me in the loop.
4. Every claim my synthesis memory makes about SMB churn must trace back to the source file it came from. An uncited claim is a problem even when it happens to be true.
5. The agent that owns one challenge must not quietly reuse another challenge's memory as context. Bleed between challenges is a leak, not a convenience.
