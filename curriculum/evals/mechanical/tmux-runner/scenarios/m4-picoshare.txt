# M4 (AE101) — Run the First Experiment. picoshare variant.
# Driven by run-m4.sh with SCENARIO=this-file. Single session in
# ~/Projects/picoshare on whatever branch M3 left HEAD (m1/picoshare-01).
# Carries forward M1's missing-return fix + post-#761 tests +
# CLAUDE.local.md, M2's plan
# (~/.claude/plans/m2-picoshare-public-guest-status-<adj>-<animal>.md
# — slug from this run's M2),
# M3's ADR (docs/adr/0001-*.md — slug from M3's threat-model pick)
# and the test-strategy skill at ~/.claude/skills/test-strategy-picoshare/.
#
# Task choice: implement the M2 plan PLUS the M3 ADR's Filter+Lock
# pattern. The plan ships the four-field public-status endpoint and
# the server-rendered panel on /g/{id}; the ADR adds the negative-
# assertion test (Label / Created / MaxFileLifetime must NOT appear
# in JSON body OR rendered panel HTML). Real, bounded, multi-file,
# several subtle ambiguities (two-clock determinism, inactive
# precedence, the "lock" half being easy to under-implement as
# DTO-field-absence-only). Decoy is a download counter on the
# file-info page — UX work, no live risk, obvious lower leverage
# after the audit weighs the ADR exposure against a counter
# nice-to-have.
#
# AUQ suppression discipline (per M3 run-1 post-mortem and the §27
# sentinel-completion lesson): every prompt that could plausibly
# trigger AskUserQuestion on the agent's own ambiguity-driven
# reasoning carries an explicit suppression tail. The 1h autonomous
# send-off is the highest risk — the tail forbids AUQ AND tells the
# agent how to make decisions without it. No canned-reply literals
# in this scenario (matches lemmings m4.txt and codesearch m4 shape;
# run-m4.sh hardcodes turn-4.transcript.txt for the commit-SHA grep,
# so literals before turn 4 would shift the index).

# T1: scope + screen against two candidates. Tail provides both
# candidates with file paths + scope estimate. Agent should pick the
# security work after the leverage comparison.
walk-and-send-off-1 Don't call AskUserQuestion — make the leverage call yourself and state your reasoning in chat. Candidates: (1) Implement the decision in M3's ADR at docs/adr/0001-*.md (slug from M3's STRIDE pick — read whichever file is present and tell me which one) — build the public-status endpoint per the M2 plan written to ~/.claude/plans/ matching m2-picoshare-public-guest-status-*.md (it'll be `<adjective>-<animal>` slugged from this run's M2; tell me which file you read) — a new GET /api/guest-links/{id}/public-status returning the four-field DTO: uploads_remaining, expires_at, byte_budget_remaining, is_active — plus the server-rendered status panel above the upload form on /g/{id}, with the M3 ADR's Filter+Lock pattern: explicit allowlist DTO projection (never marshal the raw gl struct) AND a negative-assertion test verifying that Label, Created, and MaxFileLifetime appear in NEITHER the JSON body NOR the rendered panel HTML. Touches handlers/guest_links.go (or new handlers/guest_link_status.go), handlers/routes.go, handlers/views.go (panel rendering + a new humanizeDurationUntil helper — none exists today), handlers/templates/pages/upload.html, handlers/guest_links_test.go, README. About 30-60 min agent work. (2) Add a download counter to the file-info page showing how many times each entry has been downloaded. Picoshare already tracks download events (e2e/download-history.spec.ts exercises the existing UI); the counter is a small aggregate query + a field on the file-info template. Touches store query, handlers/views.go (file-info page), template, README. UX improvement, ~30 min. Heads-up: Nix isn't installed on this host — skip nix flake check from AGENTS.md and use ./dev-scripts/run-go-tests or go test ./... directly.

walk-and-send-off-2 Don't call AskUserQuestion — return the structured report in plain chat. The Agent-tool sub-task is fine to use as instructed; just keep the final report inline, not hidden behind a tool the runner can't answer.

# T3: gap-fill. The lemmings version of this prompt body calls
# AskUserQuestion explicitly ("Use the AskUserQuestion tool. One
# question per call.") — that's structural, not an ambiguity-trigger.
# Override the prompt's structural AUQ requirement with the same shape
# the lemmings m4.txt and codesearch m4 scenarios use: suppress AUQ,
# tell the agent to pick 2-3 itself from the audit's top-5, and
# proceed without confirmation. Picoshare-flavoured gap shapes most
# likely to land: "Lock half is easy to under-implement as
# DTO-field-absence-only" (sharpened rule, highest leverage),
# "Two-clock determinism — far-future UrlExpires + injected mockClock"
# (sharpened rule, M2 plan flagged it), "Inactive precedence —
# cleared-to-0 overrides unlimited-to-null" (test-row pin or
# business-rules gap), "byte_budget_remaining is per-upload, not
# aggregate — README must reflect" (observation, scope hygiene).
walk-and-send-off-3 I'm doing a headless dry-run, please don't call AskUserQuestion — use your judgment. From the audit's top-5, pick the 2-3 gaps that most directly affect a send-off for the M3 ADR's Filter+Lock implementation and would hurt the run most if left thin. For each picked gap, propose the fill in the shape the audit named (observation / sharpened rule / business-rules gap / connector setup), write it in the destination the audit named (`observations/` for observations and business-rules gaps; `./CLAUDE.local.md` for rule sharpening), and proceed without confirmation. Don't over-fill — 2 or 3 picks is the target, not all five.

# T4: commit starting point. The prompt body requires reading
# ae101-m4-locate-transcript output for the transcript path; the
# lemmings m4.txt scenario doesn't run that prompt either, so the
# agent is expected to locate the transcript itself from
# CLAUDE_CODE_SESSION_ID + cwd encoding. Run-m4.sh greps a 7-12 hex
# SHA from turn-4.transcript.txt — keep this as turn 4.
ae101-m4-commit-starting-point Don't call AskUserQuestion. The task-slug for the branch should be `implement-public-status` or similar — pick one in that shape and proceed; locate the transcript path yourself from CLAUDE_CODE_SESSION_ID and the encoded cwd, then write it into the Run coordinates block as instructed.

# T6: the long autonomous send-off. Highest AUQ risk — runs for up
# to 1h, will hit many decisions. The tail forbids AUQ AND tells the
# agent how to make decisions without it: state the assumption,
# proceed, surface anything genuinely unclear in the final summary.
# Anchors the agent on the ADR + test-strategy carve-out so it
# doesn't drift into building auth (out of scope per the ADR),
# rate-limiting (also rejected in the ADR's alternatives), or
# faking a Playwright test for the panel render (the skill says
# the panel is render-testable via handlers_test since templates
# embed; Playwright is for browser-driven flows the existing
# guest-links.spec.ts already covers).
ae101-m4-take-task-end-to-end I'm walking away. Don't call AskUserQuestion at any point during this run — the laptop is unattended and any question will hang for the full hour. When you hit an ambiguity, state your assumption in your working notes and proceed; surface anything genuinely unclear in your final summary at the end, not as an interactive question. Anchor points the test-strategy skill and the ADR already pin: the ADR (docs/adr/0001) is Filter+Lock — explicit DTO projection PLUS a negative-assertion test against the rendered output, NOT auth and NOT rate-limit (both were rejected in the ADR's alternatives section). The Lock half asserts that Label, Created, and MaxFileLifetime appear in NEITHER the JSON body NOR the rendered panel HTML — that's a body-bytes/HTML grep, NOT just "field absent from DTO" (filter masquerading as lock is the easy mistake). The skill's panel-render carve-out: templates are embedded so handlers_test can render `GET /g/<id>` and inspect the body; Playwright is for browser-driven flows, not panel-text assertions. Two-clock setup is load-bearing — `IsExpired()` reads real wall-clock, the panel's "expires in Y" reads the injected `mockClock`, so any expiry-dependent test needs far-future `UrlExpires` AND `mockClock` set a known interval before it. Run `./dev-scripts/run-go-tests` (or `go test ./...`) before declaring done; Nix is not installed here so skip `nix flake check`. If you get stuck, write what you tried and why it didn't work in your final summary rather than inventing a way forward.
