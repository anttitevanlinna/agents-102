**Context:** Prework Task 2 — meetings summary. Live test 2026-04-21.

**Signal:** Teacher asked the student "*Path A (you have a calendar connector enabled — Microsoft 365 or Google Workspace) or Path B (no connector)?*" — but *connector* is a load-bearing concept the student has never met. Nothing in setup or prework Task 1 introduced it. Student can't honestly pick a path because they don't know what they have or how to check.

**Gap:** The "30-second shoutout" rule from content-creation SKILL.md (every new concept gets a just-enough primer at first use) wasn't applied to *connector* in prework. The original prework parenthetical *"(Microsoft 365 or Google Workspace, enabled for Claude by your IT)"* assumes the student already knows what a connector is. They don't.

**Fix applied:** prework.md Task 2 now opens with a one-paragraph primer — what a connector is, that IT enables it, the exact click-path to check (**+** button → Settings → Connectors), and a "default to Path B if in doubt" guard.

**Generalize:** scan all prework + Module 1 files for other unearned concepts. *Connector* was the obvious one because the user hit it live; there may be others. Run a "first-use" lint per concept: every term that names a thing the student must do something about gets a shoutout at first use.
