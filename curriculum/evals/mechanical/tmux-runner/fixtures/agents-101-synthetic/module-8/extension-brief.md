# Extension brief — pilot-pricing review preparation

This is an input brief, not a prebuilt agent.

Build one narrowly scoped agent that prepares a pilot-pricing variance report for Mara Viken. It reads the grounded pricing memory, the latest adjudicated claims, and the selected M7 assumptions. It writes `outputs/pilot-pricing-variance.md`, separating source-backed differences from unresolved analyst judgment. It must not change memory, contact accounts, or recommend a price.

Run it once against the real synthetic Nordveil files already present in the training directory.
